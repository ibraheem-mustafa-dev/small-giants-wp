node_modules/
build/

# Ad-hoc Chrome DevTools MCP capture dumps (console/CWV/network), including
# stage8-audit.js run output — timestamped per-run JSON, not durable
# documentation (unlike the hand-written reports/*.md files, which stay
# tracked).
reports/console/
reports/cwv/
reports/network/

# Icon-library proposals: local pipeline working state (one row per unseen icon; rewritten on every run).
# Decisions are recorded by promote-icon.py in the tracked sgs-icons.json, not here.
/assets/icons/icon-proposals.jsonl
/assets/icons/icon-proposals.jsonl.lock
