node_modules/
build/

# stage8-audit.js run output — timestamped per-run JSON, not durable documentation
# (unlike the hand-written reports/*.md files, which stay tracked).
reports/cwv/
reports/network/
reports/console/
