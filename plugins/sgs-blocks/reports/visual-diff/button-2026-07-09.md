# Visual-diff / LANDED evidence — sgs/button box-object no-inline migration

**Date:** 2026-07-09 · **Verdict: PASS** · **Block version:** 1.5.0
**Surface:** page 8 (Mama's homepage, re-cloned) + crafted asymmetric test page 1356 (`/sgs-box-object-test/`)
**Freshness guard:** deployed → re-cloned → OPcache reset (HTTP) + LiteSpeed purge (Hostinger MCP) → anonymous Playwright + cache-bust.

## Pilot Acceptance Test (A1–A9) — LANDED, live computed-style

| # | Assertion | Result | Evidence |
|---|-----------|--------|----------|
| A1 | Zero inline declarations across the button subtree | **PASS** | All 8 page-8 buttons: `inlineDeclaredProps: []`; inline style empty or only `--sgs-btn-icon-gap` (permitted custom-prop VALUE). The D291 inline padding/margin defect is fixed. |
| A2 | Base styling paints scoped = draft | **PASS** | Primary btn padding `14/24`, radius `10px`, border-width `2px` paint from the scoped `#uid`/style.css rule (not inline). Naked "Find out more" = `6px 0` faithful link (RC2). |
| A3/A3b | Asymmetric 4-side tier accumulator (THE #1 risk) | **PASS** | Test-page button `paddingTablet {11,22,33,44}` → computed `11px 22px 33px 44px` at 768; container `{14,27,40,53}` → `14px 27px 40px 53px`. All 4 sides distinct + correct, scoped, zero inline. |
| A7 | `:hover` works | **PASS** | Primary btn on hover: bg `rgb(230,138,149)`→`rgb(58,46,38)`, colour `rgb(58,46,38)`→`rgb(255,250,245)`. Both differ live. |
| A8 | Editor-canvas parity | **PASS** | Editor canvas preview radius `24px 0px 24px 0px` = frontend; `previewStyle` reflects the object attr; shared `ResponsiveBoxControl`/`ResponsiveBorderRadiusControl` (proven on container D292). |
| A9 | 4-CORNER radius incl. asymmetric | **PASS** | Test-page button base `style.border.radius {topLeft:24,topRight:0,bottomRight:24,bottomLeft:0}` → computed `24px 0px 24px 0px`, zero inline. |

## Gates (re-run by orchestrator — STOP-16)
- `check-box-family-guard.py --check` → 0 violations. Button box attrs carry `box_family` (padding/margin/borderWidth/borderRadius), `attr_type='object'`.
- `npm run build` → webpack success; prebuild dead-controls + hardcoded-defaults gates pass.
- `pytest converter/tests` → 428 pass, 1 skip (baseline held).
- `/sgs-update` → Stage-1 +7 object attrs, Stage-10 pruned 44 orphaned flat rows (STOP-66).

## Notes
- Native `color` support has no `__experimentalSkipSerialization`, but the button routes colour to `--sgs-btn-*` vars (not `style.color`), so nothing colour-inlines today (A1 confirmed on the live subtree). Full color-support skipSerialization is a rollout-consistency item, not a defect.
