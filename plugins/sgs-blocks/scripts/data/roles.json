{
  "__doc": "Canonical role vocabulary for the SGS cloning pipeline. THE source of truth for the `roles` table — db_lookup._migrate_roles_table() seeds from this file on module load (INSERT OR REPLACE + DELETE-NOT-IN two-way sync).",
  "__why": "Was a hardcoded Python dict of 21 entries while the live DB held 29. The 8 extra were each added by a one-off migration that wrote the DB row and never back-wrote the seed, so a rebuild silently lost them. That is the decay class Phase 0/1 exists to end, and a hardcoded routing dict also breaches R-31-1. Captured from live 2026-08-02.",
  "__format": "role_name: [classification, description]",
  "__classifications": [
    "content-bearing",
    "styling-behaviour",
    "unclassified"
  ],
  "a11y-text": [
    "styling-behaviour",
    "Accessibility metadata: a screen-reader-facing string (aria-label, alt on a decorative image, a nav landmark label). SEEDED FOR DOCUMENTATION, NOT ROUTING (Bean, 2026-08-04). Classification is deliberately 'styling-behaviour' so these rows are EXCLUDED from the content walk: they are content in the everyday sense, but nothing in the converter reads a draft's aria-label into an attr (the only aria-label consumer is field_extractors' STAR-rating count), and marking them content-bearing would let the walk lift screen-reader text into visible clone content. Do NOT use 'image-alt' for these -- that role means alt text for a SIBLING image-object attr, resolved via block_attributes.alt_companion_attr and consumed by the image arm (db_lookup.py:2611-2636, walk.py:485); an aria-label with no companion image would feed a consumer expecting one. This role exists so a future agent reading the DB knows what these attrs ARE rather than finding a bare NULL and assuming something is missing."
  ],
  "behaviour": [
    "styling-behaviour",
    "Excludes the attr from the content-bearing walk (FR-31-2.2) -- equivalent_block_for()/content_attrs_for_identity() deliberately skip role='behaviour' rows (converter/walk.py:405-408). Real consumer: style_preset_attrs_for_identity() (converter/db/db_lookup.py:3518-3553) mirrors a nested identity's inheritStyle onto the parent's OWN behaviour-role string attr (e.g. sgs/product-card.ctaStyle mirroring a nested sgs/button's --primary/--secondary modifier); also grouped into the BEM-modifier probe at db_lookup.py:4889-4896 alongside select-from-enum/enum-class-probe. MISCLASSIFICATION FOUND + FIXED 2026-08-04: `placeholder` on 11 sgs/form-field-* blocks + sgs/filter-search + sgs/product-search carried role='behaviour', which silently dropped a draft's placeholder text (it never reaches the content walk) -- changed to role='content' (see the 'content' entry) since a placeholder is client-authored text, not a style preset."
  ],
  "boolean-visibility": [
    "styling-behaviour",
    "A boolean editor-only toggle (e.g. core/navigation.overlay, sgs/modal.closeOnOverlay, sgs/team-member.overlayHover). NO consumer in the converter -- no code anywhere branches on the literal 'boolean-visibility'; classification alone (styling-behaviour) keeps it out of the content walk. A draft's HTML/CSS carries no signal a boolean toggle could be lifted from, so the absence of a resolver is expected, not a gap."
  ],
  "color": [
    "styling-behaviour",
    "Marks an attr whose value is a colour (design-token slug / hex / rgb() / named colour). Consumer: attr_is_colour_role() (converter/db/db_lookup.py:1075-1103) is the sole legitimate gate for routing an OUTER/CONTENT-layer value through colour serialisation (extract_token_or_hex) instead of the generic string/numeric branches -- deliberately implemented as a SQL WHERE clause, not a Python `role == \"color\"` compare, so the no_slug_literal AST gate never sees it. Also feeds the px/string CSS-kind classifier _kind_for() (db_lookup.py:1955-1956)."
  ],
  "colour-gradient": [
    "styling-behaviour",
    "Marks a background-image gradient/url attr (property_suffixes suffix='Gradient', css_property='background-image'). Consumer: _kind_for() (converter/db/db_lookup.py:1962-1963) resolves it to CSS kind 'string' (a raw `url(...)`/gradient() value); converter/resolvers/outer_box.py:51-57 resolves the block's own Gradient-suffix attr when a background-image declaration needs a destination."
  ],
  "colour-text": [
    "styling-behaviour",
    "Seeded in the role vocabulary but currently UNUSED -- zero block_attributes rows carry it (verified 2026-08-04: `SELECT COUNT(*) FROM block_attributes WHERE role='colour-text'` = 0) and no converter code reads the literal 'colour-text'. A role nothing reads: a genuine finding, not yet wired to any attr or resolver."
  ],
  "content": [
    "content-bearing",
    "Generic content-bearing text marker. Treated as a first-class ALIAS of 'text-content' throughout extraction: converter/walk.py:270-274 (_typed_value_for_role), converter/services/field_extractors.py:172-184 (extract_field_value), converter/db/db_lookup.py:5228,5257 (primary_content_attr), converter/resolvers/scalar_content.py:121,164 (lift_scalar_content). Both roles are content-bearing per _content_bearing_roles() (db_lookup.py:3744) and route identically through the Tier-A canonical_slot join in equivalent_block_for() (db_lookup.py:3747-3751) -- so a 'content' row with canonical_slot='text' promotes into a standalone sgs/text child block exactly the same way a 'text-content' row does; the promotion is role-agnostic among content-bearing roles, not text-content-exclusive. The ONE place the two are NOT interchangeable: converter/resolvers/array_content.py:249-254's L1d leaf-item text self-extraction fallback checks `frole == \"text-content\"` only, so a flat/leaf array-item field declared role='content' does not get that fallback (it would concatenate a structured item's children if it did; a leaf has none, so the exclusion is conservative, not a proven bug)."
  ],
  "enum-class-probe": [
    "styling-behaviour",
    "A BEM `--modifier` class carries this attr's value (a variant/style selector expressed only as a modifier suffix on the draft element, never as a CSS declaration). Consumer: converter/db/db_lookup.py:4889-4896 scans a block's attrs for role IN (select-from-enum, enum-class-probe, behaviour) whose derived_selector or canonical_slot ends with `--<modifier>`, then matches it against the draft's actual BEM modifier class."
  ],
  "icon-dashicon": [
    "content-bearing",
    "Icon-source value: a Dashicons name (e.g. sgs/icon.dashiconName). NO consumer in the converter: icon resolution (converter/services/field_extractors.py:201-213) walks a fixed priority chain -- data-icon/data-lucide attribute, then inline <svg> via icon_resolver, then BEM modifier -- and never reads block_attributes.role to choose which of sgs/icon's 4 icon-source attrs (dashiconName/emojiChar/iconName/wpIconName) to populate; the role rows were added by a one-off migration (db_lookup.py:112-115) whose consumer was never built. A role nothing reads: a genuine finding, shared with icon-emoji/icon-lucide/icon-wp-icon below."
  ],
  "icon-emoji": [
    "content-bearing",
    "Icon-source value: a bare emoji glyph (e.g. sgs/icon.emojiChar). NO consumer in the converter -- see the icon-dashicon entry for the shared root cause (converter/services/field_extractors.py:201-213 never branches on this role)."
  ],
  "icon-lucide": [
    "content-bearing",
    "Icon-source value: a Lucide slug (e.g. sgs/icon.iconName). NO consumer in the converter -- see the icon-dashicon entry for the shared root cause (converter/services/field_extractors.py:201-213 never branches on this role); the icon-resolver's fingerprint-match path (services/icon_resolver.py) also never reads block_attributes.role."
  ],
  "icon-wp-icon": [
    "content-bearing",
    "Icon-source value: a @wordpress/icons name (e.g. sgs/icon.wpIconName). NO consumer in the converter -- see the icon-dashicon entry for the shared root cause (converter/services/field_extractors.py:201-213 never branches on this role)."
  ],
  "identity": [
    "content-bearing",
    "Dual-purpose per caller, both content-bearing. (a) On a plain string attr, the block's identifying TEXT (e.g. productName) -- routed through the text-content extractor via converter/walk.py:277-289 (_typed_value_for_role: `role == \"identity\" and attr_type == \"string\"` -> extract_field_value(el, \"text-content\", ...)`), explicitly NOT the field_extractors 'identity' branch (that would mean icon-slug resolution -- an unrelated overloaded semantic, converter/context.py:277-283). (b) On an icon-source leaf, 'identity' is grouped with 'icon-slug' in the icon priority-chain resolver (converter/services/field_extractors.py:207-213) and in converter/resolvers/array_content.py:168-169's _FLAT_SELF_ROLES (a flat item, e.g. a social-icon <a>, may self-extract this role). Content-bearing per _content_bearing_roles() (db_lookup.py:3744), so it also participates in Tier-A canonical_slot promotion."
  ],
  "image-alt": [
    "content-bearing",
    "Alt text for a sibling image-object attr (e.g. sgs/product-card.imageAlt, sgs/media.imageAlt). Content-bearing display value the cloning image arm lifts alongside the image URL, not a styling property. The specific image attr it belongs to is named by block_attributes.alt_companion_attr, resolved by image_alt_companion_for() (converter/db/db_lookup.py:2611-2636, queries `role = 'image-alt' AND alt_companion_attr = ?`) and consumed at converter/walk.py:485 -- never guessed by name-parsing."
  ],
  "image-object": [
    "content-bearing",
    "Scalar media/image value: an {url,id,alt} object, or (for a string-typed image-object attr) the bare URL string. Consumer: the Tier-A canonical_slot join in equivalent_block_for() (converter/db/db_lookup.py:3744-3751, gated content-bearing) for InnerBlock promotion (e.g. sgs/icon.iconSource -> child sgs/icon); converter/resolvers/scalar_content.py:172 is_media_object lift for block-private scalar image attrs (e.g. sgs/team-member's photo); converter/services/field_extractors.py:189-193 resolves the dict/URL from the matched element's <img> descendant."
  ],
  "layout": [
    "styling-behaviour",
    "Marks a structural/spacing attr -- explicitly NOT content-bearing. converter/db/db_lookup.py:3841-3844: a row with canonical_slot='content' + role='layout' routes its CSS value to the PARENT's own contentPadding* attrs, never emitted as a child InnerBlock (e.g. sgs/hero.contentPaddingTop). Also grouped into the shared px-unit CSS-kind inference at _kind_for() (db_lookup.py:1955-1957)."
  ],
  "link-href": [
    "content-bearing",
    "An href/URL value. Consumer: converter/services/field_extractors.py:248-253 extracts the nearest <a href> (sanitised via _safe_href); converter/resolvers/array_content.py:168-169's _FLAT_SELF_ROLES lets a flat item (e.g. a bare social-icon <a>) self-extract its own href; converter/services/extraction.py:1069-1070 gates the single-leaf lift's 'ONE url' arm on `role in (\"link-href\", \"url-href\")`."
  ],
  "motion": [
    "styling-behaviour",
    "A transition/animation-duration-shaped CSS value. Consumer: grouped into the shared px-unit CSS-kind inference at _kind_for() (converter/db/db_lookup.py:1955-1957) alongside layout/typography/visual/spacing/shadow/number-css-px -- no motion-specific branch exists beyond that shared unit classification."
  ],
  "number-css-percent": [
    "styling-behaviour",
    "A numeric CSS value whose native unit is %. Consumer: _kind_for() (converter/db/db_lookup.py:1959-1960) resolves it to CSS kind 'number_px' -- the % suffix is stripped the same way px is, since the wrapper re-applies the correct unit at emit time."
  ],
  "number-css-px": [
    "styling-behaviour",
    "A numeric CSS value whose native unit is px. Consumer: grouped into the shared px-unit CSS-kind inference at _kind_for() (converter/db/db_lookup.py:1955-1957)."
  ],
  "position": [
    "styling-behaviour",
    "Stacking-context / positioning value (e.g. sgs/decorative-image.zIndex, css_property='z-index') -- a layout-positioning property, not content. Added 2026-07-15 (F6: enrichment derived the role before its roles row existed; the D128 scalar-media class of gap). CONSUMER NOTE (2026-08-04): no converter code branches on the literal 'position' anywhere -- zIndex's value transfers via the generic css_property join, not a role-specific check. Beyond zIndex, the wider position/inset/top/right/bottom/left family has property_suffixes rows seeded (D250) but 'NO block currently declares a matching attr for any of these six' (converter/resolvers/outer_box.py:43-46) -- a prepared-but-unused CSS destination family, honest NO_DESTINATION gap rather than a bug."
  ],
  "query-descriptor": [
    "styling-behaviour",
    "Seeded in the role vocabulary but currently UNUSED -- zero block_attributes rows carry it (verified 2026-08-04: `SELECT COUNT(*) FROM block_attributes WHERE role='query-descriptor'` = 0) and no converter code reads the literal 'query-descriptor'. A role nothing reads: a genuine finding, not yet wired to any attr or resolver."
  ],
  "rating": [
    "content-bearing",
    "Star/numeric rating value (e.g. sgs/testimonial.ratingStars) -- a content-bearing display value, not a styling property. Consumer: converter/resolvers/scalar_content.py:122,165 (is_rating: role == 'rating' and attr_type == 'number' -> star-count lift via aria-label/glyph count); converter/resolvers/array_content.py:168-169's _FLAT_SELF_ROLES also lets a flat array item self-extract a rating role (L1c)."
  ],
  "scalar-media": [
    "styling-behaviour",
    "A composite's own built-in scalar media SLOT (e.g. sgs/hero.splitImage/splitImageMobile) -- distinct from image-object: it marks a composite-interior COLUMN that must be lifted as a scalar object attr rather than folded as bare InnerBlocks. Consumer: has_scalar_media_attrs()/scalar_media_attr_for() (converter/db/db_lookup.py:2842-2874, FR-31-19) gate _route_composite_interior's routing decision, joined via block_attributes.role='scalar-media' + the slots aliases -- no per-block slug literal. Classified styling-behaviour (excluded from the generic content walk) DELIBERATELY: it is routed by this dedicated composite-interior mechanism instead, and check_row_floor.py pins sgs/hero's splitImage/splitImageMobile rows as a VALUE_ASSERTION because this role flipped to 'image-object' once already, silently breaking art-directed (mobile/desktop) image cropping."
  ],
  "select-from-enum": [
    "styling-behaviour",
    "A string CSS value chosen from a fixed enum (e.g. font-style: italic/normal). Consumer: _kind_for() (converter/db/db_lookup.py:1964-1965) resolves it to CSS kind 'string'; also grouped into the BEM-modifier probe at db_lookup.py:4889-4896 alongside enum-class-probe/behaviour."
  ],
  "spacing-token": [
    "styling-behaviour",
    "A design-token-backed spacing value. Consumer: _kind_for() (converter/db/db_lookup.py:1966-1968) lifts ONLY the BlockGap suffix (gap: Npx, a single CSS property) as CSS kind 'number_px'; the Spacing suffix (a padding/margin preset, multi-property) is deliberately NOT CSS-lifted through this path."
  ],
  "styling": [
    "styling-behaviour",
    "Generic STYLING attribute: the row carries a non-NULL css_property, so a structural mechanism has already proven it paints CSS, but no more specific styling family (layout / color / typography / visual / motion / position) was established for it. Classification 'styling-behaviour' keeps it OUT of the content walk, exactly like those specific families. ADDED 2026-08-05 (Bean) to end the ambiguity of a bare NULL: before this, 109 sgs/% rows had a css_property AND role IS NULL, which reads identically to 'nobody has looked at this yet' and sent repeated sessions re-investigating rows that were already understood. WHY GENERIC RATHER THAN DERIVED: 81 of 90 distinct css_property values do map to exactly one role elsewhere in the table, but only 53 of the 109 had any precedent at all, and a precedent-derived role is inference from OTHER rows' classifications, not evidence about THIS row -- it would also be self-referential at seed time, so one wrong row could cascade. This role claims only what is proven: it is styling. Assigned by assign-canonical.apply_role_detection_inline() as a structurally-LAST pass, so it can never pre-empt a content-bearing verdict."
  ],
  "svg": [
    "content-bearing",
    "Raw inline SVG markup (sgs/hero.svgContent, sgs/media.svgContent, and the four bgSvgContent attrs consumed by includes/class-sgs-container-wrapper.php:458). CONTENT-BEARING with its own extractor branch at converter/services/field_extractors.py (role == 'svg'), which returns the <svg> element's MARKUP. It exists because routing SVG through the text branch is ACTIVELY DESTRUCTIVE, not merely imprecise: rich_text_content() keeps only a text-tag whitelist, so <svg>/<path>/<g> are stripped and a real icon arrives as the empty string -- these attrs carried role='content' and would have been mangled the moment a draft matched. Distinct from the icon-* roles and from 'identity': those resolve an <svg> to a NAMED library icon SLUG via icon_resolver; this returns the bespoke markup itself, which has no slug. Added 2026-08-05 (Bean) as a 3-part build -- role + extractor branch + reclassification in one change -- deliberately NOT as a bare DB relabel, because four icon-* roles in this table already have no consumer and do nothing."
  ],
  "tag-identity": [
    "styling-behaviour",
    "The attr stores the source element's HTML tag (sgs/heading.level, sgs/media.mediaType). R-31-2 shape fact written by assembly step 3a2 (converter/services/assembly.py:160), gated on enum membership and read back via converter/db/db_lookup.py:1110-1125. Structural identity, never content-walked (classified styling-behaviour on purpose)."
  ],
  "text-content": [
    "content-bearing",
    "Rich-text content value -- extracted via rich_text_content (converter/services/field_extractors.py:182-184). 'content' (see that entry) is a first-class alias for every extraction path except one: converter/resolvers/array_content.py:249-254's L1d leaf-item self-extraction fallback checks `frole == \"text-content\"` specifically, so only a text-content-role field on a flat/leaf array item gets that fallback. The Tier-A canonical_slot->standalone_block promotion into a child sgs/text InnerBlock (db_lookup.py:3747-3751, worked example at db_lookup.py:5650-5651) is role-agnostic among content-bearing roles -- 'content' rows promote identically when canonical_slot matches -- so text-content is the more common spelling for this role, not an exclusive licence."
  ],
  "typography": [
    "styling-behaviour",
    "A font/type CSS value (font-size, line-height, font-family, letter-spacing, etc.). Consumer: grouped into the shared px-unit CSS-kind inference at _kind_for() (converter/db/db_lookup.py:1955-1957); no typography-specific branch exists beyond that shared unit classification."
  ],
  "visual": [
    "styling-behaviour",
    "A generic visual/effects CSS value (opacity, box-shadow, and similar). Consumer: grouped into the shared px-unit CSS-kind inference at _kind_for() (converter/db/db_lookup.py:1955-1957); converter/resolvers/outer_box.py:82,307 additionally give role='color' priority over role='visual' when both resolve to the same box-shadow destination attr (rowid-ordering tie-break, e.g. Shadow vs BoxShadow)."
  ]
}
