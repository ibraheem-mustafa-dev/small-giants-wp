"""excluded-gate — F5 excluded-literal tripwire gate.

Package stub so sibling modules can be imported as 'excluded_gate.*' when
the runner is bootstrapped via importlib (mirroring db-consistency/__init__.py).
"""
