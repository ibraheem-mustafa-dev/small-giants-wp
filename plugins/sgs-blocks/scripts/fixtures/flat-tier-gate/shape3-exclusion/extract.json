{
  "_fixture_purpose": "SHAPE-3 EXCLUSION CONTROL for check_flat_tier_regression.py, added 2026-08-12. sgs/text's 'borderWidth' is object-typed with no Tablet/Mobile siblings in the live block.json -- satisfying the NAIVE block.json-only signal the gate shipped with on 2026-08-11 -- but text/render.php:141 reads it via a direct flat box read ($border_width_obj = is_array( $attributes['borderWidth'] ?? null ) ? $attributes['borderWidth'] : array();), never through sgs_responsive_normalise_object()/sgs_emit_responsive_css()/sgs_typography_css_rule()/sgs_resolve_on_tiers(). It has NO device-tier destination at all (a base-only box property, not a migrated tier-object). Emitting it as a flat scalar here is NOT a regression -- the gate must NOT fire. This is the exact false positive class the naive attr_type/box_family-only signal could not avoid: verified live against sgs-framework.db 2026-08-12, borderWidth and a genuinely migrated property such as sgs/hero.contentBandPadding are indistinguishable on attr_type, box_family, is_responsive AND default_value alike -- only the real PHP consumer tells them apart.",
  "block_markup": "<!-- wp:sgs/text {\"content\":\"Shape-3 sample\",\"borderWidth\":\"2px\"} -->\n<p class=\"wp-block-sgs-text\">Shape-3 sample</p>\n<!-- /wp:sgs/text -->"
}
