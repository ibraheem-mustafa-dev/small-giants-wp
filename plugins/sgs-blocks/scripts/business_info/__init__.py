"""Business-details extraction and push for Spec 33 FR-33-14.

The CLI lives in `scripts/sync-business-info.py` (a hyphenated name cannot be imported); the
logic lives here so no single file mixes vocabulary, extraction, credentials and the REST push.
"""
