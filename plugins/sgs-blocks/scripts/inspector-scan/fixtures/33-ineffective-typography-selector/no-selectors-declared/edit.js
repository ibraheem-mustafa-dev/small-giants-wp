// No `selectors` key in block.json at all -> WP resolves typography styling
// to its own auto-generated `.wp-block-sgs-no-selectors-declared` root class.
export default function Edit() {
	return <div>Preview</div>;
}
