{
	"apiVersion": 2,
	"name": "sgs/no-gradient-row",
	"title": "RETIRED mustFlag 2026-09-07 (Bean's ruling): row-missing-gradient dropped entirely, now census-covered by classify-end-shape.js --check. Kept as a mustNotFlag fixture — the narrowed rule correctly stays silent.",
	"category": "sgs",
	"attributes": {
		"borderColour": { "type": "string", "default": "" },
		"borderColourHover": { "type": "string", "default": "" }
	}
}
