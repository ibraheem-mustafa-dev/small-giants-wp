{
	"apiVersion": 2,
	"name": "sgs/presets-true",
	"title": "Fixture: ResponsiveBoxControl already carrying presets={true} shorthand (must not flag)",
	"category": "sgs",
	"attributes": {
		"padding": { "type": "object", "default": {} },
		"paddingTablet": { "type": "object", "default": {} },
		"paddingMobile": { "type": "object", "default": {} }
	}
}
