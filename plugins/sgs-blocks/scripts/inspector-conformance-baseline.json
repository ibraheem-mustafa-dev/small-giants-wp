{
	"sgs/responsive-logo": {
		"animation-no-reduced-motion": {
			"reason": "TRUE observation, FALSE alarm — the guard MOVED, it did not vanish (Spec 38 FR-38-15 / D408, Wave C 2026-07-31). This block's Vivus view.js was DELETED when its runtime swapped to GSAP DrawSVG, taking its in-block reduced-motion check with it. The guard now lives in the shared effect module src/shared/effects/gsap/fx-draw.js:118, which wraps setup in the provider's withMotionAllowed(), so under (prefers-reduced-motion: reduce) the undrawn initial state is never applied and the logo renders FULLY DRAWN — Spec 38 §10's 'simplify' arm, and an upgrade on Vivus's non-canonical 1ms draw. This gate globs the BLOCK directory only, so it structurally cannot see a guard living in a shared module. VERIFIED by reading fx-draw.js:118 directly, not assumed. RE-EXAMINE if responsive-logo ever stops routing its animation through data-sgs-fx=\"draw\" — no shared guard would cover it then."
		}
	},
	"sgs/google-reviews": {
		"raw-url-link": {
			"reason": "external-service config URL; target/rel hardcoded server-side; page search inapplicable"
		}
	},
	"sgs/trustpilot-reviews": {
		"raw-url-link": {
			"reason": "external-service config URL; target/rel hardcoded server-side; page search inapplicable; parsed for business ID"
		}
	},
	"sgs/media": {
		"raw-url-link": {
			"reason": "videoUrl is a media SOURCE (external YouTube/Vimeo/MP4 embed src), not an anchor href — no target/rel/page-search semantics apply; the block's separate image linkUrl/linkOpensNewTab/linkRel trio already migrated to SgsLinkControl (Spec 35 Part 2)"
		}
	}
}
