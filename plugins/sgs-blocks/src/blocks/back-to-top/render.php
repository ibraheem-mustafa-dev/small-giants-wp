<?php
/**
 * Back to Top Block — DEPRECATED.
 *
 * This block is deprecated. Use Appearance > Customise > SGS Floating UI instead.
 * Render returns empty so legacy instances do not display duplicate UI.
 *
 * @package SGS\Blocks
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}

// Return empty to avoid duplication with the new theme-level implementation.
return '';
