/**
 * Pricing Table — save.js
 *
 * Returns null because this is now a dynamic block rendered via render.php.
 * Previous static save outputs are NOT preserved - all deprecations were deleted at D271.
 */

export default function save() {
	return null;
}
