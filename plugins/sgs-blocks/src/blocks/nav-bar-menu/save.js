/**
 * SGS Nav Bar Menu (sgs/nav-bar-menu) — save.
 *
 * Dynamic block: all frontend output comes from render.php. No InnerBlocks, so
 * save returns null (nothing is serialised into post_content).
 *
 * @package SGS\Blocks
 */
export default function save() {
	return null;
}
