{
	"$schema": "https://schemas.wp.org/trunk/block.json",
	"apiVersion": 3,
	"name": "sgs/media",
	"version": "1.7.0",
	"title": "SGS Media",
	"category": "sgs-content",
	"description": "Content media block — image or video. Use mediaType to choose image (with imageUrl) or video (with videoUrl for external embeds or videoId for WP-library uploads). Media styling (object-fit, max-width/height + tiers, border-radius, alignment, etc.) is driven entirely by this block's own NATIVE attributes and its Media Styling panel — this block deliberately OPTS OUT of the shared universal image-controls extension (no supports.sgs.imageControls) so there is ONE source of truth the cloning converter writes and the editor edits. supports.filter.duotone (T3.5, D402 ADOPT) is a separate native WP support — WP core renders it as a generated class + <style> tag (not an inline style= attribute), so it needs no render.php involvement and satisfies the no-inline contract as-is. supports.dimensions.aspectRatio (Spec 35 wave-B, D402 ADOPT) replaces the old hand-rolled `aspectRatio` scalar attr + TextControl — UNLIKE duotone, WP core's aspect-ratio support (`wp_render_dimensions_support()` in wp-includes/block-supports/dimensions.php) is a bespoke render_block filter that unconditionally writes an inline `style=\"aspect-ratio:…\"` on the block's ROOT wrapper tag and does NOT consult block.json `selectors` at all — so it both violates the no-inline contract AND targets the wrong element (the <figure> wrapper, not the <img>/<video>). `__experimentalSkipSerialization` on `supports.dimensions` makes WP emit nothing automatically (the render_block filter returns early); render.php then reads `style.dimensions.aspectRatio` itself and emits the CSS via `wp_style_engine_get_styles()` scoped to `.sgs-media__img, .sgs-media__video` — same pattern as the native border group above. The `selectors.dimensions.aspectRatio` entry below is declared for consistency/future-proofing but is currently INERT for this feature (core's aspect-ratio filter never reads it).",
	"keywords": [
		"image",
		"video",
		"media",
		"photo",
		"picture",
		"figure"
	],
	"textdomain": "sgs-blocks",
	"icon": "format-image",
	"supports": {
		"sgs": {
			"scalarStylingLift": true,
		"_comment_mediaElements": "Wave 5a. Declares this block's ONE media element to the shared atom layer. `atoms` is deliberately narrow: naming an atom injects its attributes on both sides, so an atom listed here without a renderer wired in the same commit becomes a dead control. Widen it as each atom is wired, never ahead of one. THREE atoms are DELIBERATELY EXCLUDED as Wave 5a findings, not oversights: `box-shape` — its flat borderRadius base would create a second mechanism alongside this block's existing WP-native style.border.radius. `video-behaviour` — its JS control has no Tablet/Mobile tiers while this block's existing Playback Options panel already has them (the atom's PHP-side autoplay/muted/playsinline coupling already runs at render time independent of this list). `overlay` — its CSS paints via `.sgs-media-box::after`, a wrapper class `box_classes()` exists to add but that NO render surface in this plugin calls yet (verified by grep — zero consumers); wiring it without also threading that class through all four of this block's render.php output paths plus the editor canvas would silently emit custom properties nothing paints.",
		"mediaElements": [
			{
				"prefix": "",
				"context": "element",
				"atoms": [ "media-type", "source", "meaning", "svg-presentation", "object-fit", "focal-point", "motion" ]
			}
		],
			"boxFamilies": {
				"borderRadius": [
					"borderRadiusTablet",
					"borderRadiusMobile"
				]
			},
			"elements": {
				"media": {
					"label": "Media",
					"order": 1,
					"clusters": [
						"fill",
						"layout"
					],
					"isWrapper": true,
					"_note_css_element_resolves_to_wrapper": "objectFit/objectPosition/opacity/boxShadow resolve css_element to wrapper in the DB, not media, despite this element being named media — CONFIRMED INTENTIONAL 2026-08-27. isWrapper:true routes them through check-element-manifest-conformance.js's native-fallback path (nativeSupportsPath, around line 225), which only ever resolves against the wrapper element. Do not change this to media.",
					"attrMap": {
						"css:opacity": "opacity",
						"css:height": "height",
						"css:max-width": "maxWidth",
						"css:max-height": "maxHeight",
						"css:border-width": "native:__experimentalBorder.width",
						"css:border-style": "native:__experimentalBorder.style",
						"css:border-color": "native:__experimentalBorder.color",
						"css:border-radius": "native:__experimentalBorder.radius",
						"css:box-shadow": "boxShadow",
						"css:object-fit": "objectFit",
						"css:object-position": "objectPosition"
					}
				},
				"caption": {
					"label": "Caption",
					"order": 2,
					"clusters": [
						"text"
					],
					"prefix": "caption"
				}
			}
		},
		"anchor": true,
		"className": true,
		"color": false,
		"html": false,
		"filter": {
			"duotone": true
		},
		"__experimentalBorder": {
			"radius": true,
			"width": true,
			"style": true,
			"color": true,
			"__experimentalSkipSerialization": true
		},
		"dimensions": {
			"aspectRatio": true,
			"__experimentalSkipSerialization": true
		}
	},
	"selectors": {
		"filter": {
			"duotone": ".sgs-media__img, .sgs-media__video"
		},
		"dimensions": {
			"aspectRatio": ".sgs-media__img, .sgs-media__video"
		}
	},
	"attributes": {
		"mediaType": {
			"type": "string",
			"default": "image",
			"enum": [
				"image",
				"video",
				"svg"
			]
		},
		"svgContent": {
			"type": "string",
			"default": ""
		},
		"_comment_svgArtDirection": "Per-device SVG SOURCE (Spec 35 Part D5). Type MATCHES the base attr (string) — a mismatched type is silently coerced to the default and the whole value is dropped. There is no *Desktop sibling: the unsuffixed `svgContent` above IS the desktop tier. An empty tier falls back UP (mobile -> tablet -> desktop) per D3, so a blank tier means 'use the one above', never 'render nothing'. Inline SVG is markup with no extra fetch, so tiers render as SIBLINGS toggled by scoped @media CSS (the images pattern), NOT the video runtime swap.",
		"svgContentTablet": {
			"type": "string",
			"default": ""
		},
		"svgContentMobile": {
			"type": "string",
			"default": ""
		},
		"svgAnimation": {
			"type": "string",
			"default": "none",
			"enum": [
				"none",
				"pulse",
				"float",
				"wave"
			]
		},
		"svgAnimationSpeed": {
			"type": "string",
			"default": "medium",
			"enum": [
				"slow",
				"medium",
				"fast"
			]
		},
		"videoUrl": {
			"type": "string",
			"default": "",
			"role": "content"
		},
		"videoSource": {
			"type": "string",
			"default": "external",
			"enum": [
				"external",
				"internal"
			]
		},
		"videoId": {
			"type": "integer"
		},
		"videoMimeType": {
			"type": "string",
			"default": ""
		},
		"thumbnail": {
			"type": "string",
			"default": ""
		},
		"thumbnailId": {
			"type": "integer"
		},
		"_comment_videoArtDirection": "Video-source art-direction tiers (2026-08-07, Bean-decided). Completes the VIDEO half: the image half already ships imageId/imageUrl tiers, and the playback-behaviour tiers (videoAutoplay* etc.) already exist. Unlike images, tiers here CANNOT be sibling elements toggled by CSS — three <video>s each begin fetching and three embeds each load a player — so the source is swapped by view.js, reusing sgs/hero's established data-src-desktop/tablet/mobile contract with the same upward fallback. The server still renders the DESKTOP source as real markup, so a no-JS visitor gets a working video. Bean chose to include YouTube/Vimeo (2026-08-07) with the trade-off stated: crossing a breakpoint mid-watch rebuilds the iframe and loses playback position. Each tier's embed URL is built from THAT tier's own playback flags, so a mobile tier that wants muted autoplay gets it.",
		"videoUrlTablet": {
			"type": "string",
			"default": "",
			"role": "content"
		},
		"videoIdTablet": {
			"type": "integer"
		},
		"videoUrlMobile": {
			"type": "string",
			"default": "",
			"role": "content"
		},
		"videoIdMobile": {
			"type": "integer"
		},
		"thumbnailTablet": {
			"type": "string",
			"default": ""
		},
		"thumbnailIdTablet": {
			"type": "integer"
		},
		"thumbnailMobile": {
			"type": "string",
			"default": ""
		},
		"thumbnailIdMobile": {
			"type": "integer"
		},
		"videoAutoplay": {
			"type": "boolean",
			"default": false
		},
		"videoAutoplayTablet": {
			"type": [
				"boolean",
				"null"
			],
			"default": null,
			"_comment": "Per-device playback-behaviour tiers (D-pending). null = inherit the tier above (Tablet inherits Desktop, Mobile inherits Tablet) — the framework's null-means-inherit convention, same as maxWidthTablet etc. Desktop has no *Desktop sibling; the unsuffixed attr above IS the desktop value."
		},
		"videoAutoplayMobile": {
			"type": [
				"boolean",
				"null"
			],
			"default": null
		},
		"videoLoop": {
			"type": "boolean",
			"default": false
		},
		"videoLoopTablet": {
			"type": [
				"boolean",
				"null"
			],
			"default": null
		},
		"videoLoopMobile": {
			"type": [
				"boolean",
				"null"
			],
			"default": null
		},
		"videoMuted": {
			"type": "boolean",
			"default": true
		},
		"videoMutedTablet": {
			"type": [
				"boolean",
				"null"
			],
			"default": null
		},
		"videoMutedMobile": {
			"type": [
				"boolean",
				"null"
			],
			"default": null
		},
		"videoControls": {
			"type": "boolean",
			"default": true
		},
		"videoControlsTablet": {
			"type": [
				"boolean",
				"null"
			],
			"default": null
		},
		"videoControlsMobile": {
			"type": [
				"boolean",
				"null"
			],
			"default": null
		},
		"videoPlaysInline": {
			"type": "boolean",
			"default": true
		},
		"videoPlaysInlineTablet": {
			"type": [
				"boolean",
				"null"
			],
			"default": null
		},
		"videoPlaysInlineMobile": {
			"type": [
				"boolean",
				"null"
			],
			"default": null
		},
		"videoLazyLoad": {
			"type": "boolean",
			"default": true
		},
		"videoLazyLoadTablet": {
			"type": [
				"boolean",
				"null"
			],
			"default": null
		},
		"videoLazyLoadMobile": {
			"type": [
				"boolean",
				"null"
			],
			"default": null
		},
		"imageId": {
			"type": "integer"
		},
		"imageUrl": {
			"type": "string",
			"default": "",
			"role": "content"
		},
		"imageIdTablet": {
			"type": "integer"
		},
		"imageUrlTablet": {
			"type": "string",
			"default": "",
			"role": "content"
		},
		"imageIdMobile": {
			"type": "integer"
		},
		"imageUrlMobile": {
			"type": "string",
			"default": "",
			"role": "content"
		},
		"imageAlt": {
			"type": "string",
			"default": "",
			"role": "content"
		},
		"imageIsDecorative": {
			"type": "boolean",
			"default": false,
			"_comment": "Spec 35 T3.4 / Part C 'A11y/SEO as controls' + E6 — WCAG 2.1 AA 1.1.1 (Non-text Content): a purely decorative image must be hidden from assistive tech (empty alt + aria-hidden) rather than forcing the operator to leave alt blank and hope. Narrow v1 scope: sgs/media + sgs/decorative-image only."
		},
		"imageWidth": {
			"type": "integer"
		},
		"imageHeight": {
			"type": "integer"
		},
		"maxWidth": {
			"type": "object",
			"default": {}
		},
		"maxWidthUnit": {
			"type": "string",
			"default": "px"
		},
		"maxHeight": {
			"type": "object",
			"default": {}
		},
		"maxHeightUnit": {
			"type": "string",
			"default": "px"
		},
		"height": {
			"type": "object",
			"default": {}
		},
		"heightUnit": {
			"type": "string",
			"default": "px"
		},
		"objectFit": {
			"type": "string",
			"default": "cover",
			"enum": [
				"cover",
				"contain",
				"fill",
				"none",
				"scale-down"
			]
		},
		"objectPosition": {
			"type": "string",
			"default": "center center"
		},
		"_comment_mediaSizing": "C19 shared media-sizing panel (2026-08-27). Three-way mode picker (auto/height/ratio) that makes `height` (above) and `style.dimensions.aspectRatio` (native support) mutually exclusive instead of letting both be set at once. NO `default` key deliberately — a `\"default\": null` on a typed attribute 400s every SSR preview (memory `a-null-default-is-worse-than-no-default`); render.php + edit.js both derive an absent value the same way (ratio set -> ratio; else height set -> height; else auto), so existing/old content renders identically with no edit needed.",
		"mediaSizing": {
			"type": "string",
			"enum": [
				"auto",
				"height",
				"ratio"
			]
		},
		"borderRadiusTablet": {
			"type": "object",
			"default": {}
		},
		"borderRadiusMobile": {
			"type": "object",
			"default": {}
		},
		"boxShadow": {
			"type": "string",
			"default": ""
		},
		"boxShadowColour": {
			"type": "string"
		},
		"boxShadowColourHover": {
			"type": "string",
			"default": ""
		},
		"opacity": {
			"type": "number",
			"default": 1
		},
		"alignment": {
			"type": "string",
			"default": "left",
			"enum": [
				"left",
				"center",
				"right"
			]
		},
		"order": {
			"type": "object",
			"default": {}
		},
		"caption": {
			"type": "string",
			"default": "",
			"role": "content"
		},
		"captionTag": {
			"type": "string",
			"default": "figcaption",
			"enum": [
				"figcaption",
				"div"
			]
		},
		"captionColour": {
			"type": "string",
			"default": ""
		},
		"captionFontSize": {
			"type": "integer"
		},
		"captionFontSizeUnit": {
			"type": "string",
			"default": "px"
		},
		"linkUrl": {
			"type": "string",
			"default": "",
			"role": "content"
		},
		"linkOpensNewTab": {
			"type": "boolean",
			"default": false
		},
		"linkRel": {
			"type": "string",
			"default": ""
		},
		"videoCaptionsId": {
			"type": "integer"
		},
		"videoCaptionsUrl": {
			"type": "string",
			"default": ""
		},
		"videoCaptionsLabel": {
			"type": "string",
			"default": "English"
		},
		"videoCaptionsSrcLang": {
			"type": "string",
			"default": "en"
		}
	},
	"render": "file:./render.php",
	"editorScript": "file:./index.js",
	"editorStyle": "file:./index.css",
	"style": "file:./style-index.css",
	"viewScriptModule": "file:./view.js",
	"example": {
		"attributes": {
			"mediaType": "image",
			"imageUrl": "https://via.placeholder.com/800x600",
			"imageAlt": "Example image",
			"imageWidth": 800,
			"imageHeight": 600,
			"objectFit": "cover",
			"alignment": "center"
		}
	}
}
