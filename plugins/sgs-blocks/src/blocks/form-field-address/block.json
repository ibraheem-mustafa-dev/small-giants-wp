{
    "$schema": "https://schemas.wp.org/trunk/block.json",
    "apiVersion": 3,
    "name": "sgs/form-field-address",
    "version": "0.1.0",
    "title": "SGS Address Field",
    "category": "sgs-forms",
    "description": "An address field with optional postcode lookup.",
    "keywords": [
        "address",
        "postcode",
        "location",
        "field"
    ],
    "textdomain": "sgs-blocks",
    "icon": "location",
    "parent": [
        "sgs/form",
        "sgs/form-step"
    ],
    "usesContext": [
        "sgs/formId"
    ],
    "supports": {
        "html": false,
        "anchor": false,
        "sgs": {
            "hideExtensions": [
                "animation",
                "blockLink",
                "clickEffects",
                "hover",
                "parallax"
            ],
            "elements": {
                "label": {
                    "label": "Field label",
                    "order": 1,
                    "clusters": [],
                    "_note": "Renders as <label class=\"sgs-form-field__label\"> — no per-instance colour/font-size/weight attribute exists on this block; the label is styled entirely by shared framework CSS across the form-field siblings. clusters: [] is a genuine gap, not hidden."
                },
                "input": {
                    "label": "Address sub-inputs",
                    "order": 2,
                    "clusters": [],
                    "_note": "The compound address group (line1/line2/city/county/postcode/country, plus the optional postcode-lookup input) renders as repeated <input class=\"sgs-form-field__input sgs-form-field__address-{field}\"> inside a <div class=\"sgs-form-field__address-fields\">. Every sub-input shares the same framework-CSS `sgs-form-field__input` styling; there is no per-sub-field or per-instance style attribute (`fields`/`enableLookup` select WHICH sub-fields render, not how they look), so one 'input' element honestly represents the whole group rather than fabricating a separate styleable element per sub-input. clusters: [] is a genuine gap."
                },
                "help": {
                    "label": "Help text",
                    "order": 3,
                    "clusters": [],
                    "_note": "helpText itself is field-behaviour content, not a style property (Bean's non-cluster rule) — the rendered <p class=\"sgs-form-field__help\"> also carries no per-instance style attribute of its own. clusters: []. The conformance checker's orphan scanner reports helpText as an ORPHAN here (element-key-as-fallback-prefix convention coincidentally matches help+Text); this is a known false-positive of the checker, not evidence of an unaccounted style control."
                },
                "error": {
                    "label": "Error message",
                    "order": 4,
                    "clusters": [],
                    "_note": "Populated by client-side validation JS, not a block attribute; the <span class=\"sgs-form-field__error\"> has no per-instance style attribute. clusters: []."
                }
            }
        }
    },
    "attributes": {
        "fieldName": {
            "type": "string",
            "default": ""
        },
        "label": {
            "type": "string",
            "default": "",
            "role": "content"
        },
        "required": {
            "type": "boolean",
            "default": false
        },
        "enableLookup": {
            "type": "boolean",
            "default": false
        },
        "fields": {
            "type": "array",
            "default": [
                "line1",
                "line2",
                "city",
                "county",
                "postcode",
                "country"
            ]
        },
        "conditionalField": {
            "type": "string",
            "default": ""
        },
        "conditionalOperator": {
            "type": "string",
            "default": "equals"
        },
        "conditionalValue": {
            "type": "string",
            "default": ""
        },
        "placeholder": {
            "type": "string",
            "default": "",
            "role": "content"
        },
        "helpText": {
            "type": "string",
            "default": "",
            "role": "content"
        },
        "width": {
            "type": "string",
            "default": "full"
        }
    },
    "render": "file:./render.php",
    "editorScript": "file:./index.js",
    "example": {
        "attributes": {
            "label": "Delivery Address",
            "required": true
        }
    }
}
