/**
 * Save component for sgs/responsive-logo.
 *
 * Returns null — this is a dynamic block rendered entirely server-side
 * via render.php. WordPress stores only the block comment delimiter and
 * serialised attributes.
 */
export default function save() {
	return null;
}
