// Dynamic block — server-rendered via render.php. No InnerBlocks slot (typed
// image attrs only, like sgs/audio / sgs/trust-bar), so save returns null.
export default function Save() {
	return null;
}
