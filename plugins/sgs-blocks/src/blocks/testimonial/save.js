/**
 * Dynamic block — render.php handles all frontend output.
 *
 * Converted from a static block on 2026-05-05 as part of the unified
 * media-slot migration (Gap H-3). The previous static save outputs are
 * preserved as deprecations so existing posts continue to validate. New
 * saves emit only the block delimiter comment with attribute JSON.
 */
export default function Save() {
	return null;
}
