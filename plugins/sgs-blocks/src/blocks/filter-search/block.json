{
	"$schema": "https://schemas.wp.org/trunk/block.json",
	"apiVersion": 3,
	"name": "sgs/filter-search",
	"version": "1.0.0",
	"title": "Filter Search",
	"category": "sgs-interactive",
	"description": "A type-to-find input that narrows a WooCommerce attribute filter's options. Auto-shown only when the attribute has more than 15 terms. Nest inside a Product Filter (Attribute) block, before its chips.",
	"keywords": [
		"filter",
		"search",
		"attribute",
		"woocommerce",
		"shop",
		"type to find"
	],
	"textdomain": "sgs-blocks",
	"icon": "search",
	"ancestor": [
		"woocommerce/product-filter-attribute"
	],
	"supports": {
		"html": false,
		"reusable": false,
		"sgs": {
			"colourExemptions": {
				"text": {
					"rule": "gradient",
					"reason": "Paints text inside a live <input> field — background-clip:text gradient rendering on typed/placeholder text is non-standard UX and cross-browser inconsistent, and this is the only row in the colour-conformance TEXT census painting a form control rather than static content. Hover state is unaffected by this exemption (textColourHover is flat-only, not gradient)."
				}
			},
			"boxFamilies": {
				"margin": [
					"margin",
					"marginTablet",
					"marginMobile"
				]
			},
			"elements": {
				"root": {
					"label": "Search box",
					"order": 1,
					"clusters": [ "layout" ],
					"isWrapper": true,
					"attrMap": {
						"css:margin": "margin"
					},
					"_note": "Single-element block — render.php:162-167 renders ONE root <div class=\"sgs-filter-search\">. Margin (base + marginTablet/marginMobile responsive tiers) is the ONLY styling capability declared in supports; padding/gap/width/border/fill/text/position/motion have no supports or attrs on this block (spacing.margin is the sole `sgs` support) — every other layout member is an honest GAP, not an omission. No prefix declared (attrs are bare `marginTablet`/`marginMobile`, not element-prefixed) — attrMap only, per ROLLOUT REQUIREMENT 1."
				}
			}
		}
	},
	"attributes": {
		"margin": {
			"type": "object",
			"default": {}
		},
		"attributeId": {
			"type": "number",
			"default": 0
		},
		"threshold": {
			"type": "integer",
			"default": 16,
			"minimum": 2
		},
		"placeholder": {
			"type": "string",
			"default": ""
		},
		"inputBorderColour": {
			"type": "string",
			"default": ""
		},
		"inputBorderColourGradient": {
			"type": "string",
			"default": "",
			"description": "CSS gradient string painting the search input's border instead of a flat colour. Non-empty wins over inputBorderColour at render time (D636)."
		},
		"inputBorderColourHover": {
			"type": "string",
			"default": ""
		},
		"inputBorderColourHoverGradient": {
			"type": "string",
			"default": "",
			"description": "CSS gradient string painting the search input's border on hover instead of a flat colour. Non-empty wins over inputBorderColourHover at render time."
		},
		"focusRingColour": {
			"type": "string",
			"default": ""
		},
		"textColour": {
			"type": "string",
			"default": ""
		},
		"textColourHover": {
			"type": "string",
			"default": ""
		},
		"marginTablet": {
			"type": "object",
			"default": {}
		},
		"marginMobile": {
			"type": "object",
			"default": {}
		}
	},
	"render": "file:./render.php",
	"editorScript": "file:./index.js",
	"editorStyle": "file:./index.css",
	"style": "file:./style-index.css",
	"viewScriptModule": "file:./view.js"
}
