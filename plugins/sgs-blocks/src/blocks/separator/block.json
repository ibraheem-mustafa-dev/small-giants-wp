{
	"$schema": "https://schemas.wp.org/trunk/block.json",
	"apiVersion": 3,
	"name": "sgs/separator",
	"version": "0.1.0",
	"title": "SGS Separator",
	"category": "sgs-content",
	"description": "A styleable horizontal divider — line style, responsive width/thickness, colour or gradient, alignment, and an optional centred icon or text label. Replaces core/separator framework-wide.",
	"keywords": [
		"separator",
		"divider",
		"rule",
		"hr",
		"line"
	],
	"textdomain": "sgs-blocks",
	"icon": "minus",
	"render": "file:./render.php",
	"supports": {
		"sgs": {
			"iconPicker": true,
			"boxFamilies": {
				"padding": [
					"padding"
				],
				"margin": [
					"margin"
				]
			},
			"elements": {
				"line": {
					"label": "Line",
					"order": 1,
					"clusters": [
						"fill",
						"layout"
					],
					"isWrapper": true,
					"attrMap": {
						"css:border-width": "thickness",
						"css:border-style": "lineStyle",
						"css:border-color": "colour",
						"css:padding": "padding",
						"css:margin": "margin",
						"css:width": "width",
						"css:opacity": "opacity",
						"css:border-color-gradient": "lineGradient"
					},
					"_note": "The visible divider — the block ROOT itself when contentMode='none' (a single <hr>, no wrapper — single-semantic-element rule), or each `.sgs-separator__line` flanking span when contentMode is 'icon'/'text' (render.php: one mechanism renders the border-bottom for both shapes, module header). `thickness`/`lineStyle`/`colour` map onto css:border-width/style/color because the visible line IS rendered as a border-bottom (render.php:140-156), not a background bar — no css:background-color member resolves (honest gap: a line has no fill). `lineGradient` claims css:border-color-gradient (Spec 35 vocabulary fix, 2026-08-20) — renders as border-image in HTML, which is the only mechanism that preserves dashed/dotted gradients while respecting lineStyle. Note updated 2026-08-16 (D643): it previously named the gradientColourStart/End/Angle scalars, collapsed to the single `lineGradient` CSS string that session. No border-radius/box-shadow/max-width/height/gap/aspect-ratio attrs — honest gaps. No `layer` — a line has no children to arrange."
				},
				"content": {
					"label": "Content (icon or text)",
					"order": 2,
					"clusters": [
						"text"
					],
					"prefix": "content",
					"attrMap": {
						"css:color": "contentColour",
						"css:background-image": "contentColourGradient"
					},
					"_note": "Optional centred icon OR text label, shown when contentMode is 'icon'/'text' (render.php, `.sgs-separator__content`). css:color resolves to the UNIFIED `contentColour` (2026-07-23: contentIconColour + contentTextColour merged — the slot renders an icon OR text, never both, so one foreground colour serves both modes; the former two mode-split colour attrs both painted color on .sgs-separator__content and collided). Typography (contentFontSize/FontWeight/FontStyle/LineHeight) resolves via the default `content`+suffix convention. contentIconSize/contentIconSource/contentIconName/contentIconEmoji/contentIconDashicon/contentIconWpIcon are bespoke icon-source controls (honest orphans). text-transform/text-decoration/text-align/font-family have no content-specific attribute — honest gaps. `contentColourGradient` (D636 text-colour gradient rollout) is deliberately only APPLIED when contentMode='text' (render.php) — background-clip:text clips to real glyph outlines, so it is safe for the literal text span but would silently blank a lucide/wp-icon inline-SVG glyph in 'icon' mode; the flat contentColour keeps painting the icon in that mode regardless of the gradient attr's value."
				},
				"icon": {
					"label": "Icon",
					"order": 3,
					"clusters": [],
					"_note": "The `<span class=\"sgs-separator__icon\">` glyph rendered inside `.sgs-separator__content` when contentMode='icon' (render.php:323-342). Added 2026-08-15 so `contentIconSize` (css_element='icon' in the DB — a genuinely distinct DOM node from the `content` element's own text/colour surface) has a manifest home; this audit only checks that the css_element STRING resolves to a declared key, so `clusters: []` is deliberate and unchanged in behaviour — `contentIconSize` remains an honest by-design orphan (a bespoke icon-size control, same treatment as sgs/icon-list's `iconSize` and sgs/icon's own `iconSize`), not a cluster member."
				}
			}
		},
		"className": true,
		"html": false,
		"color": false
	},
	"attributes": {
		"padding": {
			"type": "object",
			"default": {
				"desktop": {}
			}
		},
		"margin": {
			"type": "object",
			"default": {
				"desktop": {}
			}
		},
		"lineStyle": {
			"type": "string",
			"enum": [
				"solid",
				"dashed",
				"dotted",
				"double",
				"none"
			],
			"default": "solid"
		},
		"width": {
			"type": "object",
			"default": {
				"desktop": 100
			}
		},
		"widthUnit": {
			"type": "string",
			"enum": [
				"px",
				"%"
			],
			"default": "%"
		},
		"thickness": {
			"type": "object",
			"default": {
				"desktop": 1
			}
		},
		"thicknessUnit": {
			"type": "string",
			"default": "px"
		},
		"colour": {
			"type": "string",
			"default": ""
		},
		"opacity": {
			"type": "number",
			"default": 100
		},
		"alignment": {
			"type": "string",
			"enum": [
				"left",
				"center",
				"right"
			],
			"default": "center"
		},
		"lineGradient": {
			"type": "string",
			"default": ""
		},
		"contentMode": {
			"type": "string",
			"enum": [
				"none",
				"icon",
				"text"
			],
			"default": "none"
		},
		"contentIconSource": {
			"type": "string",
			"enum": [
				"lucide",
				"wp-icon",
				"dashicon",
				"emoji"
			],
			"default": "lucide"
		},
		"contentIconName": {
			"type": "string",
			"default": "star"
		},
		"contentIconEmoji": {
			"type": "string",
			"default": ""
		},
		"contentIconDashicon": {
			"type": "string",
			"default": ""
		},
		"contentIconWpIcon": {
			"type": "string",
			"default": ""
		},
		"contentIconSize": {
			"type": "number",
			"default": 24
		},
		"contentColour": {
			"type": "string",
			"default": ""
		},
		"contentColourGradient": {
			"type": "string",
			"default": "",
			"description": "CSS gradient string painting the content slot's text with a background-clip:text mechanism instead of a flat colour (D636 text-colour gradient rollout). Non-empty wins over contentColour — but only when contentMode='text'; in 'icon' mode this attribute is read but has no visible effect (see the content element's manifest note)."
		},
		"contentText": {
			"type": "string",
			"default": ""
		},
		"contentFontSize": {
			"type": "object",
			"default": {}
		},
		"contentFontSizeUnit": {
			"type": "string",
			"default": "px"
		},
		"contentFontWeight": {
			"type": "string",
			"default": ""
		},
		"contentFontStyle": {
			"type": "string",
			"enum": [
				"",
				"normal",
				"italic"
			],
			"default": ""
		},
		"contentLineHeight": {
			"type": "number"
		},
		"contentLineHeightUnit": {
			"type": "string",
			"default": ""
		}
	},
	"editorScript": "file:./index.js",
	"editorStyle": "file:./index.css",
	"style": "file:./style-index.css",
	"example": {
		"attributes": {
			"width": 60,
			"widthUnit": "%",
			"thickness": 2,
			"lineStyle": "solid",
			"alignment": "center"
		}
	}
}
