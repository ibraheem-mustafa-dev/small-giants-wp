/**
 * WhatsApp CTA — dynamic block.
 *
 * Rendered server-side by render.php. save() returns null so WordPress
 * stores only the block comment with JSON attributes; render.php handles
 * all output.
 */
export default function Save() {
	return null;
}
