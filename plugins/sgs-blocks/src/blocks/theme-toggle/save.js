/**
 * SGS Theme Toggle — static save.
 *
 * Dynamic block: render.php drives all frontend output (it also decides whether
 * anything renders at all — a site with no derived dark palette gets an editor-only
 * notice and nothing on the frontend). save() returns null per the SGS dynamic-block
 * convention.
 *
 * @package SGS\Blocks
 */
export default function Save() {
	return null;
}
