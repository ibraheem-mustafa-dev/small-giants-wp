/**
 * Brand Strip block — save returns null.
 *
 * Frontend output is handled entirely by render.php (dynamic block).
 * Attributes are preserved in the block delimiter comment.
 */
export default function Save() {
	return null;
}
