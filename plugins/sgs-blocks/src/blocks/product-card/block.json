{
	"$schema": "https://schemas.wp.org/trunk/block.json",
	"apiVersion": 3,
	"name": "sgs/product-card",
	"version": "1.16.13",
	"title": "Product Card",
	"category": "sgs-content",
	"description": "Product card with pack-size variant pills, price and CTA. Typed mode = built-in elements authored directly from block attributes (no InnerBlocks). Live product data mode = render a live WooCommerce or SGS product (price, image, stock, add-to-cart) through the SGS design.",
	"keywords": [
		"product",
		"card",
		"ecommerce",
		"shop",
		"variant",
		"pack",
		"pricing"
	],
	"textdomain": "sgs-blocks",
	"icon": "products",
	"supports": {
		"sgs": {
			"scalarStylingLift": true,
			"_comment_mediaElements": "Two independently-scoped media elements (rule 37-media-no-handroll). 'main' covers EVERY render branch's main product image — typed .sgs-product-card__image (product-card-builtin-render.php), bound read-only/non-variable .product-card-img (render.php WC-below-floor + non-variable-live branches), and bound variable-configurator .product-card__media .product-card-img — deliberately ONE control, per R-31-9, since only one of these three ever renders on a given page load. 'thumb' is the bound variable-configurator's OWN thumbnail-rail image (.product-card__thumb img) — a distinct role with its own 48x48 box, never governed by 'main'. Mirrors sgs/buybox's already-migrated main/thumb split (its gallery-col.php composes a SEPARATE render instance of the same markup, so that migration does not cover this block's own render.php paths). CORRECTED 2026-09-03 via /qc-council: imageControls/imageControlsExplicit REMOVED (were dead weight even before this migration — render.php's own comment three lines below confirms the legacy sgs_media_position_css() call \"never had a declared sgsObjectFit/sgsObjectPosition attribute pair... so it always emitted ''\"), because leaving the flags in place still made the universal image-controls extension (src/blocks/extensions/image-controls.js) mount its own dead 'Object fit' dropdown alongside the two live MediaElementPanel controls below — three controls in the sidebar for what is really one property.",
			"mediaElements": [
				{ "prefix": "main", "context": "element", "atoms": [ "object-fit", "focal-point" ] },
				{ "prefix": "thumb", "context": "element", "atoms": [ "object-fit" ] }
			],
			"arrayContentLift": true,
			"boxFamilies": {
				"ctaPadding": [
					"ctaPadding"
				],
				"cardPadding": [
					"cardPadding"
				],
				"ctaBorderWidth": [
					"ctaBorderWidth"
				],
				"ctaBorderRadius": [
					"ctaBorderRadius"
				],
				"tagPadding": [
					"tagPadding"
				],
				"borderWidth": [
					"borderWidth"
				],
				"borderRadius": [
					"borderRadiusTablet",
					"borderRadiusMobile"
				]
			},
			"variantAttr": "variantStyle",
			"variants": {
				"standard": [],
				"trial": [
					"trialTag"
				],
				"featured": [
					"featuredTag"
				]
			},
			"elements": {
				"box": {
					"label": "Card",
					"order": 1,
					"clusters": [ "fill", "layout" ],
					"isWrapper": true,
					"attrMap": {
						"css:background-color": "backgroundColour",
						"css:background-color-gradient": "backgroundColourGradient",
						"css:color": "textColour",
						"css:color-gradient": "textColourGradient",
						"css:margin": "native:spacing.margin",
						"css:border-width": "borderWidth",
						"css:border-style": "borderStyle",
						"css:border-color": "borderColour",
						"css:border-color-gradient": "borderColourGradient",
						"css:border-radius": "borderRadius",
						"css:max-width": "cardMaxWidth"
					},
					"states": {
						"hover": {
							"attrMap": {
								"css:background-color": "backgroundColourHover",
								"css:background-color-gradient": "backgroundColourHoverGradient",
								"css:color": "textColourHover",
								"css:color-gradient": "textColourHoverGradient"
							}
						}
					},
					"_note": "R2c (B-3/B-8, 2026-08-22): root box colour/border moved OFF WordPress's native style path onto block-private attrs (backgroundColour/textColour/borderColour + Gradient/Hover siblings, borderWidth, borderStyle) so the box has a SINGLE owner instead of the native Styles-tab gradient panel and the SGS panel both writing into the same style.color object. Native __experimentalBorder is reduced to radius only (a corner-shape control, not a colour/paint decision — stays on the native path). Content-KIND composite (D294, box+width only) — wraps sgs/container via SGS_Container_Wrapper. spacing.padding is FALSE in block.json supports (padding is custom-var driven, not native), so css:max-width resolves via attrMap not native fallback. `css:padding` was RE-KEYED off this element 2026-07-24 (FR-31-22 consistency fix) — the image (`.sgs-product-card__media-wrap`) is full-bleed OUTSIDE the body, so card padding never belonged on the root; it now lives on the `body` element below, matching where render.php actually paints it."
				},
				"body": {
					"label": "Card body",
					"order": 2,
					"clusters": [ "layout" ],
					"isWrapper": false,
					"attrMap": {
						"css:padding": "cardPadding"
					},
					"_note": "Content-body wrapper (`.sgs-product-card__body` / `.product-card-body`) — image fills the card full-bleed, content is padded (Bean-approved product-card design). Re-keyed 2026-07-24 (FR-31-22 consistency fix) from the `box` (root) element: cardPadding was previously mapped on the ROOT but render.php has always painted it on this body element, so the LANDED computed-parity probe (which measures the root) never found it. Keying attrMap to the element the value ACTUALLY targets fixes the mismatch at its source, per R-31-11 (verify rendered output, not internal metrics)."
				},
				"image": {
					"label": "Product image",
					"order": 3,
					"clusters": [ "layout" ],
					"attrMap": {
						"css:height": "imageHeight"
					},
					"_note": "Governed by the universal image-controls extension (supports.sgs.imageControls). imageHeight resolves via css:height (render.php:129-130 `--sgs-product-card-image-height`, consumed as `height: var(--sgs-product-card-image-height, 220px)` in style.css:53/665/704/938 — Spec 35 vocabulary fix). Full-bleed direct child of the card root, OUTSIDE the padded body — never wrapped in card padding."
				},
				"title": {
					"label": "Title",
					"order": 4,
					"clusters": [ "text" ],
					"prefix": "title",
					"attrMap": {
						"tag": "headingLevel"
					},
					"_note": "`attrMap.tag` added (D-pending, E12 scoping fix) — `headingLevel` sets the rendered tag on this element (render.php:454-456 bound branch, render.php:1022-1435 typed branch, `.sgs-product-card__title`) but doesn't match the `title` prefix convention on its own. This is what lets check-hardcoded-render-defaults.js's E12 gate resolve `headingLevel` to THIS element and correctly NOT resolve `ctaFontWeight` here (it resolves to the separate `cta` element below via that element's own `cta` prefix)."
				},
				"price": {
					"label": "Price",
					"order": 5,
					"clusters": [ "text" ],
					"prefix": "price"
				},
				"desc": {
					"label": "Description",
					"order": 6,
					"clusters": [ "text" ],
					"prefix": "desc"
				},
				"description": {
					"label": "Description colour",
					"order": 6,
					"clusters": [],
					"prefix": "",
					"_note": "CORRECTION 2026-08-15: an earlier pass in this same fix RENAMED 'desc' to 'description', which was WRONG - the DB tags descFontSize/descLineHeight css_element='desc' (matching the ORIGINAL key) and only descColour css_element='description'. Renaming would have cleared 1 orphan while creating 2 new ones. 'desc' is restored above; this bare 'description' element exists purely so the audit script's exact-key-match sees 'description' declared too. descColour already resolves fine functionally under 'desc' (prefix 'desc' + suffix Colour, default convention, text cluster) regardless of this element's own (empty) clusters. Found live 2026-08-15: DB tagged 1 attr css_element='description', undeclared until now."
				},
				"priceNote": {
					"label": "Price note",
					"order": 7,
					"clusters": [ "text" ],
					"prefix": "priceNote"
				},
				"price-note": {
					"label": "Price note colour",
					"order": 7,
					"clusters": [],
					"prefix": "",
					"_note": "CORRECTION 2026-08-15: same pattern as 'description' above - the DB tags priceNoteFontSize css_element='priceNote' (matching the ORIGINAL key) and only priceNoteColour css_element='price-note'. 'priceNote' is restored above; this bare 'price-note' element exists purely to satisfy the audit script's exact-key-match. priceNoteColour already resolves fine functionally under 'priceNote' (prefix 'priceNote' + suffix Colour, default convention). Found live 2026-08-15: DB tagged 1 attr css_element='price-note', undeclared until now."
				},
				"priceFromLabel": {
					"label": "\"From\" label",
					"order": 8,
					"clusters": [ "text" ],
					"prefix": "priceFromLabel",
					"_note": "No colour attribute exists for this element (priceFromLabelColour) — an honest gap; the label inherits price-note colour visually."
				},
				"tag": {
					"label": "Tag (trial/featured badge)",
					"order": 9,
					"clusters": [ "text", "fill", "layout" ],
					"prefix": "tag",
					"attrMap": {
						"css:background-color": "tagBackgroundColour",
						"css:width": "tagFullWidth",
						"css:background-image": "tagTextColourGradient"
					}
				},
				"pill": {
					"label": "Option-picker pill",
					"order": 10,
					"clusters": [ "text" ],
					"prefix": "pill",
					"_note": "Targets the real sgs/option-picker pill rendered inside the card (both typed and bound modes) — see render.php sgs_typography_css_rule( $attributes, 'pill', … )."
				},
				"cta": {
					"label": "Call to action",
					"order": 11,
					"clusters": [ "text", "fill", "layout" ],
					"prefix": "cta",
					"attrMap": {
						"css:background-color": "ctaColourBackground",
						"css:background-image": "ctaColourBackgroundGradient",
						"css:color": "ctaColourText",
						"css:color-gradient": "ctaColourTextGradient",
						"css:border-color": "ctaColourBorder",
						"css:padding": "ctaPadding",
						"css:border-color-gradient": "ctaColourBorderGradient"
					},
					"states": {
						"hover": {
							"attrMap": {
								"css:background-color": "ctaColourBackgroundHover",
								"css:background-image": "ctaColourBackgroundHoverGradient",
								"css:color": "ctaColourTextHover",
								"css:color-gradient": "ctaColourTextHoverGradient",
								"css:border-color": "ctaColourBorderHover",
								"css:border-color-gradient": "ctaColourBorderHoverGradient"
							}
						}
					},
					"_note": "css:padding maps to the single ctaPadding box-object attr {top,right,bottom,left} — migrated from the ctaPaddingX/ctaPaddingY axis-pair scalars to the universal box-object standard (mirrors sgs/button's padding box family + the tagPadding attr already on this block). Resolves the prior converter AmbiguousLayerAttrError collision and the elementless-ctaPaddingX misroute onto the card root. ctaColourTextGradient/ctaColourTextHoverGradient added 2026-09-04 (helpers-button-style.php D942/D956 gate) — keyed css:color-gradient since ctaColourBackground already claims css:background-color; sgs_button_element_style_css() only paints the text gradient per-state when that state's own background attr is unset. ctaColourBackgroundGradient/ctaColourBackgroundHoverGradient added 2026-09-06 (colour-conformance FILL surface, Case E) — sgs_button_element_style_css() already read these keys via its generic ColourBackground(Hover)Gradient closure (mirrors sgs/button's own colourBackgroundGradient), they were simply never declared as attributes here; keyed css:background-image to match sgs/button's own DB row (sgs_background_paint_decl() emits background-image for a gradient, never background-color)."
				}
			}
		},
		"html": false,
		"align": false,
		"interactivity": true,
		"color": {
			"background": false,
			"gradients": false,
			"text": false,
			"__experimentalSkipSerialization": true
		},
		"spacing": {
			"margin": true,
			"padding": false,
			"__experimentalSkipSerialization": true
		}
	},
	"attributes": {
		"sourceMode": {
			"type": "string",
			"default": "typed",
			"enum": [
				"typed",
				"wc-product",
				"sgs-cpt"
			]
		},
		"productId": {
			"type": "number",
			"default": 0
		},
		"taxDisplayMode": {
			"type": "string",
			"default": "auto",
			"enum": [
				"auto",
				"inc-suffix",
				"ex-plus-vat"
			]
		},
		"image": {
			"type": "string",
			"default": "",
			"description": "Permanent URL fallback for the card's main product image. NEVER removed, NEVER made conditional — kept forever alongside imageId so content stored before the imageId migration (2026-09-01) keeps rendering identically. When imageId resolves to a live attachment, imageId wins; image is the read-time fallback."
		},
		"imageId": {
			"type": "number",
			"default": 0,
			"description": "Attachment post ID for the card's main product image, added 2026-09-01 alongside the pre-existing image URL string (which stays as the permanent fallback — see its own description). Forward-only: edit.js's 4 image-picker write sites store media.id here from now on; existing content authored before this attribute existed has imageId 0/unset and keeps rendering off image until backfilled by scripts/migrate-product-card-image-id.py. When set and it resolves to a live attachment (wp_get_attachment_image_src()), it wins over image at render time — unlocking real srcset/sizes. render.php falls back to the raw image URL whenever imageId is 0 or the attachment no longer exists."
		},
		"imageAlt": {
			"type": "string",
			"default": "",
			"role": "content"
		},
		"imageDecorative": {
			"type": "boolean",
			"default": false,
			"description": "Typed mode only. When true, the product image is purely decorative (visual/promotional tile rather than a real product photo the shopper needs) and is hidden from assistive tech — alt text is blanked and aria-hidden is set. Never applies in bound/live-product mode (sourceMode='wc-product'/'sgs-cpt'), where the photo is genuine shopping content."
		},
		"productName": {
			"type": "string",
			"default": ""
		},
		"description": {
			"type": "string",
			"default": "",
			"role": "content"
		},
		"variantStyle": {
			"type": "string",
			"default": "standard",
			"enum": [
				"standard",
				"trial",
				"featured"
			]
		},
		"trialTag": {
			"type": "string",
			"default": ""
		},
		"featuredTag": {
			"type": "string",
			"default": "",
			"description": "Tag text for featured variant (e.g. Most popular, Best selling, Flagship product). Mirror of trialTag for the featured variant. Rendered as a coloured strip above the card content when variantStyle='featured'."
		},
		"packSizes": {
			"type": "array",
			"default": [],
			"items": {
				"type": "object",
				"properties": {
					"label": {
						"type": "string"
					},
					"selected": {
						"type": "boolean"
					}
				}
			}
		},
		"priceLarge": {
			"type": "string",
			"default": ""
		},
		"priceNote": {
			"type": "string",
			"default": ""
		},
		"ctaText": {
			"type": "string",
			"default": ""
		},
		"ctaUrl": {
			"type": "string",
			"default": ""
		},
		"ctaColourBackground": {
			"type": "string",
			"default": "",
			"description": "Built-in CTA background colour override. EMPTY (default) = inherit the shared button-preset channel via the sgs-button--{style} class (so a --secondary/--outline CTA gets its transparent background, a --primary gets the solid fill — mirroring a standalone sgs/button). Set a token slug or raw colour only to override."
		},
		"ctaColourBackgroundGradient": {
			"type": "string",
			"default": "",
			"description": "CSS gradient string painting the CTA's resting background with a gradient instead of a flat colour (mirrors sgs/button's colourBackgroundGradient). Non-empty wins over ctaColourBackground via sgs_button_element_style_css()."
		},
		"ctaColourText": {
			"type": "string",
			"default": "",
			"description": "Built-in CTA text colour override. EMPTY (default) = inherit the shared button-preset colour channel via the sgs-button--{style} class (composite-mirror, WCAG-correct per client — a light-primary palette gets dark CTA text automatically). Set a theme token slug or raw CSS colour only to override. (Was 'text-inverse', which force-painted near-white and failed WCAG on light palettes — D-pending.)"
		},
		"ctaColourTextGradient": {
			"type": "string",
			"default": "",
			"description": "CSS gradient string painting the CTA's resting text with a background-clip:text mechanism instead of a flat colour (D636 text-colour gradient rollout). Non-empty wins over ctaColourText, and only paints when ctaColourBackground/ctaColourBackgroundGradient are unset (helpers-button-style.php D942/D956 gate — a gradient here would otherwise clip an operator-set CTA background)."
		},
		"ctaColourBorder": {
			"type": "string",
			"default": "",
			"description": "Built-in CTA border colour override. EMPTY (default) = inherit the shared button-preset channel via the sgs-button--{style} class. Set only to override."
		},
		"ctaColourBorderGradient": {
			"type": "string",
			"default": "",
			"description": "CSS gradient string painting the CTA's resting border with a masked ring instead of a flat colour (D636 border-gradient rollout). Non-empty wins over ctaColourBorder."
		},
		"ctaColourBackgroundHover": {
			"type": "string",
			"default": "",
			"description": "Built-in CTA hover background colour override. EMPTY (default) = inherit the shared button-preset hover channel via the sgs-button--{style} class. Set only to override."
		},
		"ctaColourBackgroundHoverGradient": {
			"type": "string",
			"default": "",
			"description": "CSS gradient string painting the CTA's hover background with a gradient instead of a flat colour (mirrors sgs/button's colourBackgroundHoverGradient). Non-empty wins over ctaColourBackgroundHover via sgs_button_element_style_css()."
		},
		"ctaColourTextHover": {
			"type": "string",
			"default": "",
			"description": "Built-in CTA hover text colour override. EMPTY (default) = inherit the shared button-preset hover colour channel via the sgs-button--{style} class. Set only to override. (Was 'text-inverse' — WCAG footgun on light palettes.)"
		},
		"ctaColourTextHoverGradient": {
			"type": "string",
			"default": "",
			"description": "CSS gradient string painting the CTA's hover text with a background-clip:text mechanism instead of a flat colour (D636 text-colour gradient rollout). Non-empty wins over ctaColourTextHover, and only paints when ctaColourBackgroundHover is unset (same D942/D956 gate as the resting state)."
		},
		"ctaColourBorderHover": {
			"type": "string",
			"default": "",
			"description": "Built-in CTA hover border colour override. EMPTY (default) = inherit the shared button-preset hover channel via the sgs-button--{style} class. Set only to override."
		},
		"ctaColourBorderHoverGradient": {
			"type": "string",
			"default": "",
			"description": "CSS gradient string painting the CTA's hover-state border with a masked ring instead of a flat colour (D636 border-gradient rollout). Non-empty wins over ctaColourBorderHover on :hover/:focus-visible."
		},
		"ctaBorderStyle": {
			"type": "string",
			"default": "solid",
			"enum": [
				"solid",
				"dashed",
				"dotted",
				"none"
			]
		},
		"ctaBorderWidth": {
			"type": "object",
			"default": { "top": "2px", "right": "2px", "bottom": "2px", "left": "2px" }
		},
		"ctaBorderRadius": {
			"type": "object",
			"default": { "topLeft": "10px", "topRight": "10px", "bottomLeft": "10px", "bottomRight": "10px" }
		},
		"ctaFontWeight": {
			"type": "string",
			"default": "600"
		},
		"ctaFontSize": {
			"type": "number"
		},
		"ctaPadding": {
			"type": "object",
			"default": {},
			"description": "Box padding for the built-in CTA button { top, right, bottom, left }. Migrated from the ctaPaddingX/ctaPaddingY axis-pair scalars to the universal box-object standard (FR-31-22) — mirrors sgs/button's padding box family and this block's own tagPadding attr. Rendered scoped via sgs_button_element_style_css()."
		},
		"ctaWidthType": {
			"type": "string",
			"default": "fit",
			"enum": [
				"fit",
				"full"
			],
			"description": "'full' stretches the built-in CTA (.product-card__view / .product-card__add-to-cart) to 100% width."
		},
		"cardMaxWidth": {
			"type": "string",
			"default": "",
			"description": "Override the card max-width (e.g. 320px, 100%). Empty = use theme default (380px). Sets --sgs-product-card-max-width inline."
		},
		"imageHeight": {
			"type": "string",
			"default": "",
			"description": "Override the product image box height (e.g. 180px, 16rem). Empty = use theme default (220px). Sets --sgs-product-card-image-height inline."
		},
		"cardPadding": {
			"type": "object",
			"default": {},
			"description": "Box padding for the card's content BODY { top, right, bottom, left } — the product image is full-bleed and sits outside the padded body. Migrated 2026-07-24 (FR-31-22) from the single-value innerPadding scalar to the universal box-object standard — mirrors ctaPadding/tagPadding. Rendered scoped (never inline) via sgs_box_object_shorthand() on the card's own uid selector, targeting .product-card-body/.sgs-product-card__body. Empty = theme default (20px, style.css)."
		},
		"backgroundColour": {
			"type": "string",
			"default": "",
			"description": "Card root background colour override. R2c (B-3/B-8): block-private, replaces the native style.color.background path so the root box has a single owner."
		},
		"backgroundColourGradient": {
			"type": "string",
			"default": "",
			"description": "CSS gradient string painting the card root background. Non-empty wins over backgroundColour."
		},
		"backgroundColourHover": {
			"type": "string",
			"default": "",
			"description": "Card root background colour on hover/focus-within."
		},
		"backgroundColourHoverGradient": {
			"type": "string",
			"default": "",
			"description": "CSS gradient string painting the card root background on hover/focus-within. Non-empty wins over backgroundColourHover."
		},
		"textColour": {
			"type": "string",
			"default": "",
			"description": "Card root inherited text colour override. R2c (B-3/B-8): block-private, replaces the native style.color.text path."
		},
		"textColourGradient": {
			"type": "string",
			"default": "",
			"description": "CSS gradient string painting the card root text via background-clip:text. Non-empty wins over textColour."
		},
		"textColourHover": {
			"type": "string",
			"default": "",
			"description": "Card root inherited text colour on hover/focus-within."
		},
		"textColourHoverGradient": {
			"type": "string",
			"default": "",
			"description": "CSS gradient string painting the card root text on hover/focus-within. Non-empty wins over textColourHover."
		},
		"borderColour": {
			"type": "string",
			"default": "",
			"description": "Card root border colour. R2c (B-3/B-8): block-private, replaces the native __experimentalBorder.color path (no hover pair — the root border has none)."
		},
		"borderColourGradient": {
			"type": "string",
			"default": "",
			"description": "CSS gradient string painting the card root border with a masked ring instead of a flat colour. Non-empty wins over borderColour."
		},
		"borderWidth": {
			"type": "object",
			"default": {},
			"description": "Card root border width { top, right, bottom, left }. R2c (B-3/B-8): block-private, replaces the native __experimentalBorder.width path."
		},
		"borderRadius": {
			"type": "object",
			"default": {},
			"description": "Card root border radius { topLeft, topRight, bottomLeft, bottomRight }. Block-private (2026-08-30 radius target-shape correction), replaces the native __experimentalBorder.radius path."
		},
		"borderRadiusTablet": {
			"type": "object",
			"default": {}
		},
		"borderRadiusMobile": {
			"type": "object",
			"default": {}
		},
		"borderStyle": {
			"type": "string",
			"enum": [
				"none",
				"solid",
				"dashed",
				"dotted",
				"double",
				"groove",
				"ridge",
				"inset",
				"outset"
			],
			"default": "solid",
			"description": "Card root border style. R2c (B-3/B-8): block-private, replaces the native __experimentalBorder.style path."
		},
		"maxWidth": {
			"type": "object",
			"default": {}
		},
		"indexVariationUrl": {
			"type": "number",
			"default": 0,
			"description": "FR-27-E2 escape-hatch: set to a variation ID to override the page canonical URL to that variation's URL, enabling it to be indexed. 0 = default (WP core canonical, recommended)."
		},
		"framingMode": {
			"type": "string",
			"enum": [
				"savings",
				"loss-aversion",
				"neutral"
			],
			"default": "loss-aversion",
			"description": "Spec 28 P1 (FR-28-8): how the comparative value-ladder frames the per-unit saving. loss-aversion = 'save Xp each vs …'; savings = 'save X%'; neutral = no saving text. Placement-level (a card block renders many products)."
		},
		"decoyEnabled": {
			"type": "boolean",
			"default": false,
			"description": "Spec 28 P1 (FR-28-9a): when true, the 'Best value' badge targets the 2nd-largest pack (decoy) instead of the largest. In live-data mode the per-product meta _sgs_decoy_enabled overrides this."
		},
		"headingLevel": {
			"type": "string",
			"default": "h3",
			"enum": [
				"h2",
				"h3",
				"h4",
				"p"
			],
			"description": "HTML heading level for the product title in typed built-in mode (h2, h3, or h4)."
		},
		"ctaBehaviour": {
			"type": "string",
			"default": "learn-more",
			"enum": [
				"learn-more",
				"add-to-basket",
				"buy-now"
			],
			"description": "Primary CTA behaviour in bound mode. 'learn-more' = plain link. 'add-to-basket' = SGS cart proxy add. 'buy-now' = add then redirect to WC checkout. Typed mode always renders plain links regardless of this setting."
		},
		"ctaStyle": {
			"type": "string",
			"default": "primary",
			"enum": [
				"primary",
				"secondary",
				"outline"
			],
			"description": "Primary CTA visual style (primary/secondary/outline) — maps to the sgs-button--{ctaStyle} modifier class, mirroring a standalone sgs/button's inheritStyle. The cloning converter lifts the draft CTA's --modifier here (Spec 31 §13.5). Picking a style in the editor also seeds the ctaColour*/ctaBorder*/ctaFontWeight fields; leaving those empty makes the CTA inherit the shared button-preset colour channel (composite-mirror, WCAG-correct per client)."
		},
		"cta2Text": {
			"type": "string",
			"default": "",
			"role": "content",
			"description": "Secondary CTA label. Empty string = no secondary button rendered."
		},
		"cta2Url": {
			"type": "string",
			"default": ""
		},
		"cta2Style": {
			"type": "string",
			"default": "secondary",
			"description": "Secondary CTA visual style — maps to btn-{cta2Style} class."
		},
		"visibleAxes": {
			"type": "array",
			"default": [],
			"description": "Bound mode only: taxonomy slugs of attribute axes to show in the option picker. Empty array = show all axes. Display filter only — never alters the seeded context or manifest."
		},
		"overrideElements": {
			"type": "array",
			"default": [],
			"items": {
				"type": "string",
				"enum": [
					"name",
					"description",
					"badge",
					"image",
					"cta"
				]
			},
			"description": "Connected (bound) mode only: which card elements render the operator's typed value instead of the live product value. Members: 'name','description','badge','image','cta'. An element's typed value is preserved when its override is off; an empty typed value never blanks the card (live value renders). PRICE IS NEVER OVERRIDABLE (page-schema-feed parity) - deliberately not an enum member."
		},
		"showLadder": {
			"type": "boolean",
			"default": true,
			"description": "Show the comparative per-pack price ladder. The grid block sets this false on its cards by default - browsing context shows just price + per-item note."
		},
		"pickerLabelFontSize": {
			"type": "string",
			"default": "",
			"description": "Bound/configurator mode only: font-size for the in-card option-picker labels (Size/Flavour legends). Raw CSS size (e.g. 18px, 1.2rem). Empty = default. Forwarded to each sgs/option-picker as labelFontSize."
		},
		"pickerLabelColour": {
			"type": "string",
			"default": "",
			"description": "Bound/configurator mode only: colour for the in-card option-picker labels (Size/Flavour legends). Theme colour token slug (e.g. primary) or raw hex. Empty = default. Forwarded to each sgs/option-picker as labelColour."
		},
		"pickerColourPreset": {
			"type": "string",
			"default": "solid",
			"enum": [
				"",
				"soft",
				"solid"
			],
			"description": "Colour preset forwarded to each in-card sgs/option-picker as colourPreset. Default 'solid' preserves the pre-2026-07-10 bound-configurator look (primary border resting, solid-primary fill selected) now that the card no longer hardcodes the picker's border colour (R5 — see product-card/style.css)."
		},
		"pickerShowSelectedTick": {
			"type": "boolean",
			"default": true,
			"description": "Forwarded to each in-card sgs/option-picker as showSelectedTick."
		},
		"pickerPillBgColour": {
			"type": "string",
			"default": "",
			"description": "Forwarded to each in-card sgs/option-picker as pillBgColour (resting pill background)."
		},
		"pickerPillBgColourGradient": {
			"type": "string",
			"default": "",
			"description": "Forwarded to each in-card sgs/option-picker as pillBgColourGradient — sgs/option-picker already carries the full gradient sibling for its pill background (2026-09-05); this attribute was the missing forward. Non-empty wins over pickerPillBgColour."
		},
		"pickerPillTextColour": {
			"type": "string",
			"default": "",
			"description": "Forwarded to each in-card sgs/option-picker as pillTextColour (resting pill text)."
		},
		"pickerPillBorderColour": {
			"type": "string",
			"default": "",
			"description": "Forwarded to each in-card sgs/option-picker as pillBorderColour (resting pill border)."
		},
		"pickerPillBorderRadius": {
			"type": "string",
			"default": "",
			"description": "Forwarded to each in-card sgs/option-picker as pillBorderRadius. CSS length string (e.g. 6px); empty = the option-picker's own default."
		},
		"pickerPillSelectedBgColour": {
			"type": "string",
			"default": "",
			"description": "Forwarded to each in-card sgs/option-picker as pillSelectedBgColour."
		},
		"pickerPillSelectedTextColour": {
			"type": "string",
			"default": "",
			"description": "Forwarded to each in-card sgs/option-picker as pillSelectedTextColour."
		},
		"pickerPillSelectedBorderColour": {
			"type": "string",
			"default": "",
			"description": "Forwarded to each in-card sgs/option-picker as pillSelectedBorderColour (R2 decoupled selected border)."
		},
		"pickerPillSelectedBorderRadius": {
			"type": "string",
			"default": "",
			"description": "Forwarded to each in-card sgs/option-picker as pillSelectedBorderRadius. CSS length string (e.g. 6px); empty = match the resting pill radius."
		},
		"titleFontSize": {
			"type": "object",
			"default": {}
		},
		"titleFontSizeUnit": {
			"type": "string",
			"default": "px"
		},
		"titleFontWeight": {
			"type": "string",
			"default": ""
		},
		"titleFontStyle": {
			"type": "string",
			"default": ""
		},
		"titleFontFamily": {
			"type": "string",
			"default": "",
			"description": "Font family for the product title heading, sourced from theme.json's typography.fontFamilies preset list via the shared TypographyControls font-family picker. Empty = inherit the theme's default (body) font — the draft's title has no explicit font-family, so it inherits the draft's body font (Inter) rather than the block's own Fraunces heading font."
		},
		"titleLineHeight": {
			"type": "number"
		},
		"titleLineHeightUnit": {
			"type": "string",
			"default": ""
		},
		"titleColour": {
			"type": "string",
			"default": "",
			"description": "Colour for the product title heading. Theme token slug or raw hex. Empty = theme default."
		},
		"titleColourGradient": {
			"type": "string",
			"default": "",
			"description": "CSS gradient string painting the product title heading's text with a background-clip:text mechanism instead of a flat colour (mirrors the tagTextColour/tagTextColourGradient D636 rollout). Non-empty wins over titleColour."
		},
		"priceFontSize": {
			"type": "object",
			"default": {}
		},
		"priceFontSizeUnit": {
			"type": "string",
			"default": "px"
		},
		"priceFontWeight": {
			"type": "string",
			"default": ""
		},
		"priceColour": {
			"type": "string",
			"default": "",
			"description": "Colour for the price element. Theme token slug or raw hex. Empty = theme default."
		},
		"priceColourGradient": {
			"type": "string",
			"default": "",
			"description": "CSS gradient string painting the price element's text with a background-clip:text mechanism instead of a flat colour (mirrors the tagTextColour/tagTextColourGradient D636 rollout). Non-empty wins over priceColour."
		},
		"priceFontFamily": {
			"type": "string",
			"default": "",
			"description": "Font family for the price element, sourced from theme.json's typography.fontFamilies preset list via the shared TypographyControls font-family picker. Empty = inherit the theme's default (body) font."
		},
		"descFontSize": {
			"type": "object",
			"default": {}
		},
		"descFontSizeUnit": {
			"type": "string",
			"default": "px"
		},
		"descLineHeight": {
			"type": "number"
		},
		"descLineHeightUnit": {
			"type": "string",
			"default": ""
		},
		"descColour": {
			"type": "string",
			"default": "",
			"description": "Colour for the product description text. Theme token slug or raw hex. Empty = theme default."
		},
		"descColourGradient": {
			"type": "string",
			"default": "",
			"description": "CSS gradient string painting the product description text with a background-clip:text mechanism instead of a flat colour (mirrors the tagTextColour/tagTextColourGradient D636 rollout). Non-empty wins over descColour."
		},
		"descFontFamily": {
			"type": "string",
			"default": "",
			"description": "Font family for the product description text, sourced from theme.json's typography.fontFamilies preset list via the shared TypographyControls font-family picker. Empty = inherit the theme's default (body) font."
		},
		"pillFontSize": {
			"type": "object",
			"default": {}
		},
		"pillFontSizeUnit": {
			"type": "string",
			"default": "px"
		},
		"priceNoteFontSize": {
			"type": "object",
			"default": {}
		},
		"priceNoteFontSizeUnit": {
			"type": "string",
			"default": "px"
		},
		"priceNoteColour": {
			"type": "string",
			"default": "",
			"description": "Colour for the price-note text. Theme token slug or raw hex. Empty = theme default (muted)."
		},
		"priceNoteColourGradient": {
			"type": "string",
			"default": "",
			"description": "CSS gradient string painting the price-note text with a background-clip:text mechanism instead of a flat colour (mirrors the tagTextColour/tagTextColourGradient D636 rollout). Non-empty wins over priceNoteColour."
		},
		"priceNoteFontFamily": {
			"type": "string",
			"default": "",
			"description": "Font family for the price-note text, sourced from theme.json's typography.fontFamilies preset list via the shared TypographyControls font-family picker. Empty = inherit the theme's default (body) font."
		},
		"priceFromLabelFontSize": {
			"type": "object",
			"default": {}
		},
		"priceFromLabelFontSizeUnit": {
			"type": "string",
			"default": "px"
		},
		"tagFontSize": {
			"type": "object",
			"default": {}
		},
		"tagFontSizeUnit": {
			"type": "string",
			"default": "px"
		},
		"tagPadding": {
			"type": "object",
			"default": {},
			"description": "Box padding for the in-body trial tag { top, right, bottom, left }. Rendered scoped via the shared sgs_label_box_css_rule() helper (mirrors sgs/label). The cloning converter lifts the draft .sgs-product-card__tag--trial padding here."
		},
		"tagBackgroundColour": {
			"type": "string",
			"default": "accent",
			"description": "Background colour for the in-body trial tag. Theme token slug or raw CSS colour. Matches the retired style.css class default (accent tint) so an untouched card renders unchanged. Cloning converter lifts the draft trial-tag background-color here."
		},
		"tagTextColour": {
			"type": "string",
			"default": "text",
			"description": "Text colour for the in-body trial tag. Theme token slug or raw CSS colour. Matches the retired style.css class default so an untouched card renders unchanged. Cloning converter lifts the draft trial-tag color here."
		},
		"tagTextColourGradient": {
			"type": "string",
			"default": "",
			"description": "CSS gradient string painting the in-body trial tag's text with a background-clip:text mechanism instead of a flat colour (D636 text-colour gradient rollout). Non-empty wins over tagTextColour."
		},
		"tagBorderRadius": {
			"type": [ "string", "number" ],
			"default": "",
			"description": "Border radius for the in-body trial tag as a CSS length (e.g. '16px', '1.5rem'). Empty = the style.css default. A bare number is treated as px for pre-2026-08-13 instances. Cloning converter lifts the draft trial-tag border-radius here."
		},
		"tagFullWidth": {
			"type": "boolean",
			"default": true,
			"description": "When true the in-body trial tag stretches to full width (block, 100%) instead of hugging its text. Defaults TRUE because the trial tag is a full-width child of the flex-column card body by design (draft canon) — a card-design constant, not a converter-detected value (the parent-flex fullWidth heuristic was dropped per council). Toggle off to make the trial tag hug its text."
		},
		"showPickers": {
			"type": "boolean",
			"default": true,
			"description": "Bound/live-data mode only: when false, suppresses all option-picker renders (both variable-axis and non-variable pill paths) and falls through to the static From-price card appearance. Use on collection grids where picker UI is unwanted. Default true (existing behaviour unchanged)."
		}
	},
	"usesContext": [
		"postId"
	],
	"editorScript": "file:./index.js",
	"style": "file:./style-index.css",
	"editorStyle": "file:./index.css",
	"viewScriptModule": "file:./view.js",
	"render": "file:./render.php",
	"example": {
		"attributes": {
			"productName": "Sample Product",
			"description": "A short product description goes here.",
			"variantStyle": "standard",
			"packSizes": [
				{
					"label": "8-pack",
					"selected": true
				},
				{
					"label": "12-pack",
					"selected": false
				},
				{
					"label": "20-pack",
					"selected": false
				},
				{
					"label": "40-pack",
					"selected": false
				}
			],
			"priceLarge": "£10.00",
			"priceNote": "8-pack · Free delivery over £35",
			"ctaText": "Add to Cart — £10",
			"ctaUrl": "#"
		}
	}
}