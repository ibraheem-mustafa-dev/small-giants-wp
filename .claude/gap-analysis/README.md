# Gap Analysis Reports — Sgs Wp

Per-project gap-analysis reports live here. Each time `/gap-analysis` runs
on something in this project, the report should be saved as:

    YYYY-MM-DD-{target-slug}.md

Global/cross-project gap reports live in `C:/Users/Bean/.claude/gap-analysis/reports/`
(also visible to OC via the `gap-analysis` extra in the memory location
registry). Per-project reports are the ones relevant to this project
specifically — grades for this project's skills, agents, pipelines,
sites, designs, etc.
