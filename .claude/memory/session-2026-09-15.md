# Session snapshot — 2026-09-15 — Front A (nav-menu split) full close-out narrative

Swept from `.claude/LEDGER.md` by the `ledger-rotate.py` discipline (D101: history preserved, not
deleted) when the living-status section was condensed to stay under the byte cap. This is the full
narrative; LEDGER.md itself now carries only the condensed pointer.

## Front A — nav-menu split (D1059/D1060) — FULLY DONE + DEPLOYED + LIVE-VERIFIED

`sgs/nav-bar-menu` + `sgs/nav-drawer-menu` fully split, all 8 steps shipped (`b54c9b347`,
`31886a2ea`, `c149de5b4`, 2026-09-15) — split-nav layout (`ColumnShapePicker` `auto` track,
`justifyContent`, `splitAfterItemId`/`splitSide`/`showBurger`), SOON badge (shared
`sgs_nav_shared_badge_html()`, real visually-hidden text for the accessible name, 24-char cap),
drawer two-tier split. **The Step 5 blocker (179 rogue `sgs/nav-drawer-menu` DB `css_property`
seeds) is RESOLVED** (`e219265d2` + `71a23fe4b`) — root cause was `sync-container-wrapping-blocks.py`'s
ground-truth roster still naming the deleted `sgs/nav-menu`; fixed + re-ran `/sgs-update` in full;
`db-consistency/run.py --check` (build-deploy.py's F6 gate) now exits 0. Two more pre-existing,
unrelated-to-nav gate failures found+fixed while getting a clean deploy (`0b5593dc9`): stale
`sgs/nav-menu` fixtures in `converter/tests/test_variant_detect.py` (renamed to
`sgs/nav-drawer-menu`, matching live DB truth), and a stale product-card assertion (unrelated,
predates this session — `colourSwatches` gained a `label` field in `0a93cbec9`).

**DEPLOYED to sandybrown (`1376084dd`) and LIVE-VERIFIED via Playwright** — homepage nav renders
unchanged (regression-safe), and a scratch test page proved every new capability actually works:
badge renders + correct accessible name, split-before/after render the right item subsets,
unresolvable split id correctly falls back to the FULL menu, drawer two-tier split works. Scratch
page + test menu-item edit deleted/reverted after verification.

**Two REAL bugs found live, fixed, redeployed, re-verified (`439321df6` + `1376084dd`) — neither
Steps 6-8's own work, both much older:** (1) `itemBorderWidth`'s FR-41-36 "always visible" 1px
default read as a stray shadow under every nav item at rest (Bean-flagged on the real homepage) —
changed to `{}` (off) on both blocks. (2) That alone didn't fix it: a DEAD `elseif` branch in
`nav-menu-item-border-featured-css.php` (dead until today, because width was never previously
empty on a live instance) painted `border-style:solid` with no width whenever width was cleared —
which CSS resolves to the browser's ~3px default border, not none. Branch removed. Both
live-confirmed via computed-style capture on the real homepage: `border-bottom: 0px none`.

**`P-NAV-DROPDOWN-STACKING-IN-PAGE-CONTENT` — RESOLVED 2026-09-15** structurally, not the
CSS-mirroring patch it tracked: `nav-bar-menu/block.json` now carries `"ancestor":
["sgs/site-header-row"]` (mirrors `nav-drawer-menu`'s `["sgs/nav-drawer"]`), editor-blocking the
only placement the bug needed; header placement verified unaffected. `reparentPanelIfNeeded()`
kept — read in full, every caller exists solely to serve the page-content case, but the `ancestor`
constraint is an editor-inserter guard only (doesn't stop a misplaced instance from `post_content`/
WP-CLI/REST/migration), so deleting it would regress that edge case to the original worse bug.
Detail: `memory/parking-archive.md` ("page-embedded nav-bar-menu dropdown stacking").

**`P-NAV-HOVER-TYPOGRAPHY-CONTROLS` — RESOLVED 2026-09-15, false positive, not a real gap.** The
6 Hover attrs (`item`/`submenu` × `FontWeightHover`/`TextDecorationHover`/`TextTransformHover`)
were always correctly wired: `TypographyControls.js`'s `showHover` block (mounted with
`showHover: true` on both targets in both blocks' `edit.js`) + `sgs_nav_shared_typography_hover_rule()`
in both `render.php` files, genuinely called from `includes/nav-menu-css.php` (`'item'` prefix) and
`includes/nav-menu-submenu-link-css.php` (`'submenu'` prefix). The audit tool's
`PREFIXED_HELPER_SUFFIXES` allowlist (in `audit-block-file-consistency.py` and its JS twin
`check-dead-controls.js`) just never registered that helper, so it couldn't resolve the
prefix-constructed attr names (e.g. `$prefix . 'FontWeightHover'`) and reported them as orphaned.
Registered in both files; 12 stale baseline entries removed via manual merge (not
`--update-baseline`, which is documented elsewhere as destructively non-mergeable). Before/after:
14 findings → 2 (only the unrelated bar-only `_note_*` doc-attrs remain). Detail:
`memory/parking-archive.md` ("nav hover-typography missing controls").

**3 stale plan doc-items also closed 2026-09-15** (from the nav-menu-split plan's own "Docs to
update" checklist, caught only because Bean directly challenged the earlier "fully closed, no
open blockers" claim rather than accepting it):
- Spec 37 FR-37-42's heading dropped its stale "NOT built" (contradicted its own corrected Status
  line below it) and now records that Step 6's `auto` track (`ColumnShapePicker.js`'s new
  `fit-centre` SHAPES entry) closes the `1fr auto 1fr` wide-centre gap the picker previously
  couldn't express.
- Spec 00 §3.1 gained the `sgs/form-field-*` counter-example: 14 registered blocks (not 13 as
  first guessed — corrected against the filesystem) share `.sgs-form-field__` as BEM root, and
  `sgs/form-field` itself isn't registered, so the whole family is permanently invisible to the
  cloning pipeline — live evidence for why the nav split gave its two new blocks separate roots.
- STOP-CATALOGUE.md gained `E26` (`STOP-A-SHARED-BEM-ROOT-MAKES-A-BLOCK-FAMILY-INVISIBLE-WITH-NO-GATE`),
  D101 carry-forward receipt 281 → 282 STOPs, nothing removed or reworded.

**Spec 36/41 citation drift** — 13 gating `CITE-SYMBOL` findings on Spec 41 (Step-3-rename drift,
re-disclosed on every nav-menu-split commit through `1376084dd`), not yet re-fixed. Spec 36's bare
`sgs/nav-menu` prose mentions (ungated) also untouched — bigger job. Still genuinely open, carried
forward in LEDGER.md's condensed pointer.

**Whole track: read as CLOSED.** No more nav-menu-split steps, no known blockers, deploy current
and verified. This was re-verified against the plan's own checklist (not just re-asserted) after
Bean's direct challenge — three real doc gaps were found and closed as a result.
