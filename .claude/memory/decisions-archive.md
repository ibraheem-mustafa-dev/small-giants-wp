# decisions-archive.md — Archived decisions from decisions.md

Entries moved here are no longer active in the main log. They are ordered chronologically (oldest first within each session). D-numbers preserved; none reused.

---

## 2026-05-22 — Session B decisions (D21-D32 Session B)

These are the concrete shipping outcomes of the D27-D36 architecture-staging decisions. The staging decisions are the canonical record; these are the implementation detail. Compressed summaries only.

**D21(Session B) — WP style-variation overlay system retired (commit `43a93df9`).** Per-client snapshots moved to `sites/<client>/theme-snapshot.json`. Three PHP files deleted; `push-theme-snapshot.py` CLI created. `/sgs-clone` Stage 10 replaced REST POST with subprocess call to new CLI. WP theme_json filters hide Browse-styles UI. Archive: implementation detail of D28.

**D22(Session B) — Customiser migration shipped (commit `60220b13`).** Three new Customiser sections (`sgs_header` / `sgs_footer` / `sgs_site_info`). postMessage transport for colour/typography/spacing/sticky; refresh transport for conditional rules. Old admin pages preserved (not redirected). Archive: implementation detail of D35.

**D23(Session B) — Customiser paint targets: `header.wp-block-template-part` / `footer.wp-block-template-part` (commit `0ef032fe`).** Initial `.wp-site-header` / `.wp-site-footer` targeting failed — SGS template parts use `.wp-block-group` wrappers. CSS custom properties moved to `:root` for cascade availability. Archive: implementation detail of D35 (gotcha, captured in mistakes.md).

**D24(Session B) — Button-presets CSS bridge deleted (commit `60220b13`).** WP 6.9+ natively generates 100% of `--wp--custom--button-presets--*` props. `class-button-presets-admin.php` deleted. Archive: implementation detail of D30.

**D25(Session B) — View Transitions wired with WP 7.0 native + WP 6.9 inline fallback (commit `60220b13`).** `function_exists('wp_enqueue_view_transitions_admin_css')` check — calls natively on WP 7.0+, falls back to inline `@view-transition{navigation:auto;}` on WP 6.x. Archive: implementation detail of D35.

**D26(Session B) — Markup examples seeded for 73 SGS blocks (commit `d307c8b0`).** `generate-markup-examples.py` auto-generates ~56 blocks; 13 complex composites hand-authored. 69 rows in `markup_examples`. Archive: one-time seeding, not an ongoing architectural rule.

**D27(Session B) — block_supports audit found ZERO gaps; original 2:1 under-documentation prediction was wrong (commit `d307c8b0`).** Every block.json supports declaration already had a matching DB row. Archive: audit outcome, not architectural rule.

**D28(Session B) — apiVersion 3: all 69 blocks already at v3 (commit `d307c8b0`).** No bulk bump needed. 87 content-bearing attrs now carry `"role": "content"`. `wp_set_script_module_translations()` wired for 25 blocks. Archive: one-time verification outcome.

**D29(Session B) — Lucide REST migration shipped defensively (commit `d307c8b0`).** `class_exists('WP_REST_Icons_Controller')` + `function_exists('wp_register_icon_collection')` guards. `wp_register_icon_collection` does NOT exist on WP 7.0 — registration entry point unknown. `sgs_get_lucide_icon()` shim continues working. Archive: implementation detail (research pending in D34).

**D30(Session B) — Device-visibility coexistence documented (commit `d307c8b0`).** Header comment in `device-visibility.php`: WP-native preferred for simple cases; SGS extension reserved for finer-grained multi-condition controls. Archive: documentation note.

**D31(Session B) — WP 6.9.4 → 7.0 upgrade on sandybrown.** Pre-upgrade mysqldump at `~/domains/sandybrown.../sandybrown-pre-wp7.sql`. Post-upgrade API verification: key WP 7.0 functions exist. Two surprises: `wp_register_icon_collection` absent; `register_block_variation` absent. Archive: one-time ops event.

**D32(Session B) — Two-commit archive-then-delete pattern for safe class retirement.** Phase 5a Commit A moved 3 PHP picker classes to `_retired/`; Commit B (delete) deferred. Archive: process note, captured in mistakes.md.

---

## 2026-05-21 — Decision block entries archived (skill + pipeline administration)

**Decision 22(skill) — `verification-rationalisations.md` shared reference created.** Content extracted from deleted `test-driven-development` + `verification-before-completion` skills. Consumed by `/qc`, `/qc-inline`, `/qc-council`, `/verify-loop`, `/systematic-debugging`. Archive: skill administration detail.

**Decision 23(skill) — Skills only called by others get `user-invocable: false`.** Applied to `/requesting-code-review`, `/subagent-prompt`, `/deploy-check`. blub.db row 277. Archive: skill metadata captured in MEMORY.md.

**Decision 24(skill) — Deletions: source skills absorbed into successors deleted, not archived.** `/test-driven-development` + `/verification-before-completion` deleted; `/review` command stub deleted; `/deploy-check` command stub deleted. Archive: skill administration detail.

---

## 2026-05-21 — Option A cleanup sprint decisions (D10-D20)

**Decision 10 — cv2 is the ONLY converter path (commit `ee8db653`).** `--converter-v2` default flipped True. Legacy extract.py subprocess removed. Non-SGS-BEM boundaries halt with clear error. Files remain on disk (physical deletion deferred — hero.py had ~30 hero-specific lifts not yet matched). Archive: superseded — cv2 is now the only path (captured in active spec).

**Decision 11 — Stage 8 Playwright stub never silently passes (commit `ee8db653`).** Stub now returns `{diff_ratio: None, stage_8_skipped: True}` sentinel; `autonomy_decision` returns `surface-to-operator`. Archive: implementation detail, captured in cloning-pipeline-flow.md.

**Decision 12 — Four documented-but-broken gates enforced (commit `7d713ba0`).** Per-section pixel-diff CaptureContext.selector; `unresolved_slots == 0` deploy gate; `STAGE_2_CONFIDENCE_THRESHOLD = 0.7`; `require_schema=True` default. Archive: implementation detail, captured in pipeline flow.

**Decision 13 — Universal-extraction CSS D3 destination wired (commit `e60fe58e`).** `walk()` emits `attribute_gap_candidate` rows for every CSS property failing D1 AND D2. `seed_gap_context()` at startup for traceable provenance. Archive: superseded by D(spec16-2) and later walker work.

**Decision 14 — `LEGACY_ROLE_LOOKUP` migrated to DB (commit `e60fe58e`).** New `legacy_role_lookup` table in sgs-framework.db (17 entries). Idempotent `seed-legacy-role-lookup.py`. Archive: implementation detail, one-time migration complete.

**Decision 15 — `RETIRED_BLOCK_REMAP` soft-emptied (commit `e60fe58e`).** 7 Indus Files referencing `heritage-strip` migrated to use `brand`. Voter dict emptied to `{}`; consultation branch retained as no-op. Archive: migration complete.

**Decision 16 — Truth-doc layer consolidated to TWO docs (commit `13dc3161`).** `tooling-map.md` + `skills-commands-map.md` + `db-tables-map.md` absorbed into `cloning-pipeline-flow.md`. Three sibling docs replaced with ~9-line redirect stubs. Archive: doc consolidation complete.

**Decision 17 — Spec 15 absorbed into Spec 16 (inline).** Spec 15 content folded into Spec 16 §12 Appendix A. Spec 15 file retained for historical reference. Archive: superseded — canonical spec is now Spec 16.

**Decision 18 — Licensing-rule clarification (Wave 2b revert).** 16-keyword licensing reject in `uimax-write-validator.py` reverted. Bean's "no licensing" rule = don't add licensing-VALIDATION infrastructure at all; the gate was theatre. 3 regression-guard tests added. Archive: superseded by D(step5) which is the canonical record of this decision.

**Decision 19 — Universal extraction is partial-pass defining next session's work.** 4 specific root causes: RC-3 slot_synonyms DB gaps for composite slot names; RC-2 `_SUPPORTS_HANDLED_PROPS` over-exclusion; RC-1 D3 Mode 2 breakpoint coverage; RC-4 `_collect_css_decls_for_element` grouped-selector bug. Archive: diagnosis, superseded by subsequent wave work.

**Decision 20 — Gemini panels untrustworthy for this project's audits.** Every Gemini report fabricated specific line citations. Sonnet panels were grounded. Any Gemini finding must be verified by grep before treatment as fact. Archive: operational lesson, captured in mistakes.md / handoffs.

---

## 2026-05-19 — Phase 9 evidence infrastructure + brand walkdown decisions

**D1(2026-05-18) — Evidence stack triple: trace + expected-rules + split-metric coverage.** Per-section `convert-trace-<boundary>.jsonl` (14 walker_branch_taken labels) + `expected-rules-<boundary>.jsonl` + `attribute_coverage` block in diff.json. Gated behind `--debug-trace`. ~5% runtime overhead OFF in production. Archive: implementation detail captured in cloning-pipeline-flow.md.

**D2(2026-05-18) — 4-rater /qc panel with Cerebras-stall replacement protocol.** When Cerebras stalls 10+ min, kill via TaskStop + dispatch Sonnet with adversarial-lens prompt. Gate's purpose is 4 INDEPENDENT lenses, not 4 specific models. Archive: operational protocol captured in QC skill.

**D3(2026-05-18) — Suffix-anchored attribute-coverage match.** Key must `endswith(suffix)` OR `endswith(suffix + breakpoint_tail)`. SGS attr naming `<slot><Property>[Breakpoint]` — bare endswith too strict; pure substring too permissive. Archive: implementation detail.

**D4(2026-05-18) — Trace lifetime discipline: try/finally reset in `convert_section`.** `finally: v3.set_trace(None, "")` guarantees clean reset. Archive: implementation detail.

**D1(2026-05-19) — Universal core-block CSS lift via `_lift_core_block_style()`.** 26-entry data-driven `_CORE_BLOCK_STYLE_MAP`. Wired into atomic_image / atomic_heading / atomic_paragraph / atomic_text_fallback. Parked for DB migration as P-CORE-STYLE-MAP-DB-MIGRATION. Archive: implementation detail captured in cloning-pipeline-flow.md.

**D2(2026-05-19) — Tag-selector blast-radius guard.** `_lift_core_block_style` skips when node has no `sgs-` class. Emits `css_decl_skipped` trace event. Archive: implementation detail, superseded by D48/D49 walker discipline.

**D3(2026-05-19) — Shallow-merge `attrs["style"]` rather than blind assignment.** `attrs["style"] = {**attrs.get("style", {}), **style_dict}`. Archive: implementation detail.

**D4(2026-05-19) — QC panel rule extension: assert file artefact existence end-to-end.** When QC artefact is a FILE, every rater MUST include "list run-dir + assert file appears + wc -l + head -1". Function-level byte-equality insufficient. Archive: captured in mistakes.md + MEMORY.md.

**D(brand-walkdown-d1) — WP-native alignment + per-client theme.json widths (surfaced 2026-05-17).** Root cause: WP `single.html` post template constrains `.entry-content` to max-width 800px while mockup HTML has no WP wrapper. Hero-clone-poc on `page.html` template with `alignfull` class renders PERFECTLY. Decision: implement widthMode attr system. Archive: implementation shipped as D(width-system); root-cause diagnosis captured.

**D(brand-walkdown-d2) — Three new dynamic SGS blocks shipped (2026-05-17).** `sgs/media` (36 attrs), `sgs/text` (79 attrs), `sgs/quote` (92 attrs). Archive: one-time block shipping event.

**D(brand-walkdown-d3) — Converter atomic-branch SGS-class guard.** `has_sgs_class` guard at top of `_lift_core_block_style`. Also skips `.sgs-brand__body p` parent-qualified selectors (parked as P-PARENT-QUALIFIED-TAG-LIFT). Archive: implementation detail, superseded by D48/D49 walker discipline.

**D(brand-walkdown-d4) — `<tag <?php echo $wrapper_attrs;` leading-space convention.** Without literal space before `<?php echo $wrapper_attrs`, WP's block-supports filter injects style attr producing malformed `<divstyle="..."`. Pattern: `<div <?php echo $wrapper_attrs; ?>>`. Swept across 6 blocks. Archive: implementation gotcha, captured in CLAUDE.md Gotchas section.

**D(brand-walkdown-d5) — `gridTemplateColumns` attr added to sgs/container.** Asymmetric grid tracks (5fr 3fr, etc.) cannot use `columns: N` which only emits `repeat(N, 1fr)`. New attrs `gridTemplateColumns` + tablet + mobile. Archive: one-time block attr addition.

---

## 2026-05-17 — P-PHASE8-NEW-1 — Retired-block remap

**D(retired-remap-a through e) — `RETIRED_BLOCK_REMAP` mechanism for heritage-strip → brand pattern routing.** Voter-level dict maps retired SGS-BEM slug-roots → replacement pattern slug. Voter owns remap (not confidence-matrix); ALL `sgs-` classes scanned before literal-slug match; Mama's mockup source migrated to `sgs-brand*` classes; `sgs/brand` future block would require deleting the remap entry. Archive: superseded by D15 (2026-05-21 — dict soft-emptied) and D42 (hand-authored patterns deleted).

---

## 2026-05-16 — Spec 16 Phase 8 decisions (archived granular entries)

**D(phase8-a) — Walker FR1 precedence above CSS-driven container override.** Swap fixes universally — FR1 fires at every depth where BEM class resolves to registered status='built' block. Commit `a2d58a3d`. Archive: implementation detail, superseded by later walker restructure.

**D(phase8-c) — Composite-element-to-standalone fast path.** `target=sgs/container` + `bem.element` resolves to canonical slot with `standalone_block` + node has element children → emit standalone block. Universal. Archive: implementation detail captured in Spec 16.

**D(phase8-d) — Bucket router gives ACCURATE info, not approximate.** `chrome_skipped` + `cv2_handled_no_top_level_match` buckets added. `severity_totals` dashboard. Archive: implementation detail captured in leftover-bucket-router code.

**D(phase8-e) — All-blocks attribute harvest (was first-block-only).** `_harvest_all_wp_block_attrs()` walks every block comment via brace-depth scanning. Archive: implementation detail.

**D(phase8-f) — `block_attributes.role` populated via slot_synonyms.role + property-suffix-guarded backfill.** text-content count jumped 26 → 78. CRITICAL GUARD: skip rows where a property suffix was peeled. Archive: one-time DB seeding.

**D(phase8-g) — Wrong-block-type plausibility check for cv2-handled sections.** Depth-0 section-root emission checked against BEM root-class via depth-aware traversal. Archive: implementation detail.

**D(phase8-i) — Heritage-strip retired as block; replaced by `theme/sgs-theme/patterns/brand.php`.** Commit this session. Archive: superseded by D42 (2026-05-23 — pattern itself deleted as hand-authored).

**D(phase8-j) — Universal BEM-child array lifter (no per-attr-name pattern dict).** `_lift_bem_child_array(node, parent_slug, attr_name, schema)`. Zero hardcoded class names. Trust-bar items now lift 4 entries. Archive: implementation detail captured in Spec 16.

**D(phase8-k) — All cv2-emittable blocks now dynamic.** 7 static blocks converted via parallel agents (commit `9a32a164`); trust-bar + label (commit `7a2a777d`); heritage-strip retired. `block.json` versions bumped 0.1.0 → 0.2.0. Archive: superseded by D3(2026-05-19) which completed the full conversion.

**D(phase8-l) — Parallel agent dispatch validated for non-shared-file work.** 7 Haiku agents + 1 Sonnet agent, ~15 min wall-time vs ~70 min sequential. Archive: operational pattern, captured in CLAUDE.md.

---

## 2026-05-15 — Spec 16 Phase 7 closure decisions

**D(phase7-a through j) — Phase 7 ships partial; per-section pixel-diff gate; heritage-strip routing interim; extract.py legacy path; SECTION_AS_CONTAINER_OVERRIDES retired; gridTemplateColumns attr; convert_section bug fixed; secrets canonical store; leftover-buckets mandatory diagnostic; multi-model QC panel mandatory.**

All captured as blub.db rows 254-256 + MEMORY.md feedback files. Archive: superseded by subsequent phases; binding rules captured in MEMORY.md/mistakes.md.

---

## 2026-05-14 — Phase 6 v2 Step 4a through 4k (wire-in entries)

Step 4a (token_resolver), 4b (variation_router), 4c (supports_writer + inheritance transitive), 4d (modifier_extractors), 4e (stage1_boundary_hook + lingua_franca transitive), 4f (attribute-gap-writer), 4g (functionality-gap-detector), 4h (gap-review-report), 4i (3 apply modules), 4j (wp_integration), 4k (critical-fix-verification).

All 11 wire-in steps are complete. Canonical state is recorded in `cloning-pipeline-flow.md` (stage-index) and `tooling-map.md`. Individual implementation decisions per step are in git log for the 2026-05-14 session. Archive: implementation details of a completed sequential series.

**D(step4-retrospective) — 6-fix retrospective QC panel covering steps 4b-4k.** 2 ship-blockers + 4 high-severity concerns addressed in one commit. Fixes: path math for gap-review.md; theme_json staleness between sections; per_section_results schema mismatch (7-key vs 13-key); 4b silent suppression on 4a failure; cloning-pipeline-flow.md gap-list stale; truth-doc placement drift for 4i+4j. Archive: retrospective fix, details in git log.

---

## 2026-05-14 — Phase 6 v2 Step 5 and Spec 16 framing (archived details)

**D(spec16-1) — Spec 16 framed as Spec 15 §7 implementation, NOT successor.** Archive: framing decision, captured in spec headers.

**D(spec16-4 through 8) — Phase 4 baseline is WP-rendered mockup-as-post; max-iteration cap = 2; FR8 legacy extract.py retirement gate; sgs/heading composite block added to Phase 2; model-routing patterns.** Archive: planning details, all superseded by completion of Phase 6 and later work.

---

## 2026-05-13 — Spec 15 Phase 5 pre-flight; Phase 5g; Phase numbering refresh

**D(phase5-preflight) — Phase 5 reads/writes canonical sgs-framework.db exclusively; form-instance scope exclusion; non-form NULL backfill; hero baseline re-capture.** Archive: pre-flight decisions, all one-time setup.

**D(phase5g-state) — Phase 5 live E2E exposes recogniser-hallucinates-blocks bug; partial closure accepted; CSS-lift stage shipped; structural gaps 5h.4/5h.5/5h.6 named.** Archive: phase closure notes, all superseded by Phase 6+ work.

**D(phase-renumber) — Phase 7 → Phase 6 renumbering; Phase 5 closure accepted.** Archive: numbering decision, now historical.

**D(phase6-step0) — Spec 15 Phase 6 Step 0: entry-script rewire composes Phase 5 modules; +REGISTER tail wired.** Archive: wire-in detail for a completed phase.

---

## 2026-05-12 — Spec 15 Phase 1: slot vocab scoped to content-identity only

**D(phase1-slots) — Slot vocab is content-identity only; structural attrs legitimately resolve to `canonical_slot = NULL`.** NULL is valid for structural attrs; flagged as gap candidates. Archive: captured in Spec 15 §11 (absorbed into Spec 16 §12 Appendix A).

---

## 2026-05-12 — Spec 15 ratified; Spec 14 FR18 missing-recogniser-script decisions

**D(spec15-ratify) — Specs 12, 13, 14 absorbed into Spec 15; six locked decisions; verification discipline; asset inventory.** Archive: spec administration, superseded — canonical spec is now Spec 16.

**D(spec14-fr18) — Four missing recogniser scripts resolved: `heuristic-fallback-builder.py` RETIRE; `computed-style-passport.py` RETIRE; `recursion-guard.py` BUILD; `critical-fix-verification.py` BUILD.** Archive: planning decisions, all resolved and shipped.

---

## 2026-05-11 — Spec 14 additional decisions

**D(spec14-mockup-slug) — Mockup-migration pattern-slug convention: short form (Option A).** `.sgs-header__inner` not `.sgs-header-mamas-munches__inner`. Archive: captured in Spec 13 / Spec 15 §8.1.

**D(spec14-classes-patterns) — Classes map to PATTERNS not blocks (Spec 13 amendment).** Captured as `feedback_classes_map_to_patterns_not_blocks.md`. Archive: captured in MEMORY.md.

**D(phase4-propagation) — Phase 4 propagation method: hybrid inline + Python helper (Option C).** B2 shipped inline; B3-B9 via idempotent Python helper. Archive: one-time propagation complete.

**D(skill-type-rubric) — Skill-type rubric mismatch is BASELINE, not debt.** Mini-skill / command / agent files below threshold because rubric is wrong tool for file type. Archive: captured in lifecycle skill notes.

**D(phase2-db-cleanup) — No DROPs applied this pass (conservative-keep).** 8 empty tables kept pending cross-reference evidence. Archive: one-time audit outcome, all tables since either populated or explicitly resolved.

---

## 2026-05-10 — Additional archived entries

**D(phase3-css-lift-2026-05-09) — Parallel CSS-lift agents; render.php echo pattern.** Archive: implementation details of completed work.

---

## Archived 2026-06-02 — Entries moved from decisions.md (pruned from 766 lines)

Entries below met all 4 archive criteria: retired/superseded surface AND no binding rule/methodology AND not cross-referenced by active spec/parking AND a newer decision supersedes.

**D1(2026-05-20) — Path A: site-wide variation activation (NOT per-page meta override).** Stage 10 activates variation via `set_theme_mod('active_theme_style', $slug)` site-wide. Per-page override rejected — clients get own WP install. Archive reason: explicitly marked "superseded-by-D28" in live doc; style-variation system killed 2026-05-21 (D28).

**D(a-2026-05-17) — `parse_css` regex was the single biggest recognition hole.** Old regex matched 0 of 13 `@media` blocks. Replaced with brace-balanced scanner. 13/13 media blocks captured. Archive reason: Spec 16-era implementation detail; Spec 22 walker rewrite (D79/D82) superseded all Spec 16 extraction code.

**D(c-2026-05-17) — Walker preserves SGS-BEM grouping wrappers.** Non-top-level `sgs/container` with `bem.element` set AND inner blocks → preserve as nested `sgs/container` with className. Archive reason: superseded by §FR-22-4.1 (D118) which is the canonical single rule for every sgs-classed wrapper below section root. D118 explicitly states §FR-22-4.1 supersedes D52 and related patchwork mechanisms.

**D(phase8-b) — Slot→standalone-block routing is DB-driven, not code-driven.** `slot_synonyms.standalone_block` column added; hardcoded `SLOT_TO_STANDALONE_BLOCK` dict removed. Archive reason: superseded by D99 — `slot_synonyms` table unified into new `slots` table; standalone_block routing still DB-driven (R-22-1) but now via the `slots` table, not `slot_synonyms`.

**D(spec16-2) — "CSS drives emission, never drop" (R5 re-architected).** 3-destination routing: typed-attribute lift / markup wrapper with className / attribute_gap_candidates row. Archive reason: Spec 16 CSS router superseded by Spec 22 §FR-22-3 single universal walker with 3 permitted exceptions only. D78 explicitly retired Spec 16's layered architecture.

**D(spec16-3) — sgs/container is MANDATORY at section-level, AVAILABLE elsewhere.** Auto-emission only at top-level section boundary. Nested wrappers pass through UNLESS CSS rules target them. Archive reason: superseded by §FR-22-4.1 (D118) which provides the canonical container/wrapper resolution rule for all depths.

**D(voter) — Deterministic SGS-BEM voter over probabilistic AI matcher.** Stage 1 voter does literal slug match on `.sgs-<block>` → `sgs/<block>` at confidence 1.0. Archive reason: superseded by Spec 22 R-22-2 (BEM is the only recognition signal) + D107 which retired the voter's literal-slug-match short-circuit in favour of DB-tier queries.

**D(phase5g) — Phase 5 partial closure accepted; canvas pipeline structural defect closed.** 6 of 9 blocks routed to unregistered blocks. Fix path chosen: hard gate in confidence-matrix; bucket-c-classifier + atomic-block-scaffold. +REGISTER wired. Archive reason: Spec 15 Phase 5 superseded by Spec 22 which replaced the layered pipeline architecture entirely.

**D37 — Source 2 counter gates on extraction-count, not insert-count.** `s2_extracted > 0` is canonical Mode B Source 2 health signal. Archive reason: architecture-staging Phase 4 close-out; Mode A/B multi-source extraction architecture superseded by the current single-converter (cv2-only) pipeline path.

**D38 — Source 4 calibration threshold tightened (100 → 30).** Live test: page returns 91 rows with simple HTTP fetch — below old threshold. `playwright-fetch.js` created for JS-render step. Archive reason: same as D37 — multi-source extraction architecture superseded.

**D39 — GitHub PAT format: classic `ghp_*` required for Mode B.** Fine-grained tokens returned 401 on Source 5 (GitHub API). Classic PAT with `public_repo` scope succeeds. Archive reason: Mode B GitHub Source superseded; ops detail captured in `~/.openclaw/.env` note.

**D41 — `core/group` → `sgs/container` as Stage 2 confidence-matrix fallback.** `core/group` has no SGS-BEM attributes; fallback changed to `sgs/container`. Archive reason: Stage 2 confidence-matrix fallback routing superseded by Spec 22 universal walker with DB-tier resolution (D107 retired the literal-slug-match short-circuit; D99 unified the slot lookup tables).

**D42 — Hand-authored patterns deleted; deterministic-only rule enforced.** `brand.php` + `ingredients-section.php` deleted. Pattern count: 53 (was 55). Archive reason: one-off mechanical deletion, done; rule captured in CLAUDE.md architecture rules section.

**D43 — Stage 0.7 CSS dump relocated from `theme/sgs-theme/styles/<client>.css` to `pipeline-state/<run>/variation-d0-d2.css`.** Archive reason: one-off relocation done 2026-05-23; destination path is now the canonical pipeline state path.

**D44 — Stage 10 silent-failure fix: named exit codes 4/5/6.** exit 4 (phantom page), exit 5 (id-mismatch), exit 6 (no-id-in-body). Archive reason: one-off implementation detail, done; exit codes documented in pipeline code.

**D45 — Stage 11 added: per-section pixel-diff against actual deployed page.** Stage 8 = pre-deploy autonomy gate; Stage 11 = post-deploy verification. Output: `pipeline-state/<run>/stage-11-pixel-diff.json`. Archive reason: one-off wiring done 2026-05-23; Stage 11 is now described in cloning-pipeline-stages.md.

**D46 — Walker pre-pass addresses Stage 4 emit shape, not Stage 2 match.json confidence.** `_walker_pre_pass` changes WHAT Stage 4 emits but NOT Stage 2 confidence. Archive reason: Stage 4 walker pre-pass was Spec 16-era; Spec 22 universal walker rewrite (D82) superseded all Spec 16 walker code including the pre-pass architecture.

**D50 — `/sgs-update` Stage 1 tail invokes `assign-canonical.py`.** Script was never wired. Fix: `stage_1_sgs_codebase_scan()` calls `assign-canonical.py` as subprocess after scan. Archive reason: superseded by D99 + D110 which rebuilt assign-canonical against the post-D99 `slots`+`roles` schema; the wiring mechanism changed.

**D51 — `assign-canonical.py` array-attr fallback: singularise + Tier B registered-block reverse-lookup.** Plural collection attrs missed slot_synonyms. Fix: singularise → alias lookup → block reverse-lookup. Archive reason: superseded by D99 (slot_synonyms replaced by unified `slots` table) and D110 (assign-canonical ported to post-D99 schema).

**D52 — Transparent-wrapper absorb at section root (one-section-one-container).** Walker pre-pass `_absorb_transparent_wrappers()` absorbs single direct-child wrapper with no block-spacing CSS. Archive reason: D118 explicitly states §FR-22-4.1 "supersedes `_absorb_transparent_wrappers` (D52 merge-side pre-pass) — folded in as rule #2's 1-child case."

**D53 — Brand mockup BEM renamed for Spec 00 consistency.** `sgs-brand__body` → `sgs-brand__quote`; `<footer>` → `<p class="sgs-brand__attribution">`. Archive reason: one-off rename done 2026-05-24; no forward-looking relevance.

**D55 — `block_compositions` table merged into `patterns.block_composition` JSON column; standalone table dropped.** 35 of 37 rows ported. Archive reason: one-off data migration done 2026-05-24; `patterns.block_composition` is the current state (further evolved by D108 `block_composition` table which is distinct).

**D56 — Standalone source DBs deleted; data migrated into sgs-framework.db with `source` column.** `blocks.db` + `hooks.db` + caches deleted. ~647 MB disk recovered. Archive reason: one-off migration done 2026-05-24; single-DB architecture is now the live state captured in CLAUDE.md.

**D29(arch-staging) — INNER_BLOCK_PATTERNS dict retired; DB-backed lookup.** Hardcoded two-entry dict replaced by `blocks.parent_block` + `slot_synonyms.standalone_block` DB lookups. Archive reason: superseded by D99 — `slot_synonyms` table unified into `slots` table; `standalone_block` lookup still DB-driven but the table changed.

**D32(arch-staging) — Phase 1.5 inserted: block variations + styles.** 67 of 69 SGS blocks had zero inserter-discoverable variations. Phase 1.5 authors 12 composite blocks × 2-4 variations × 2-3 styles each via PHP sibling files in `includes/variations/`. Archive reason: this Phase 1.5 plan was superseded — D142 implemented variations as WP block-styles (`register_block_style`) not the PHP sibling-files pattern described here.



---

## Archived 2026-06-03 (theme-thread Spec 26 wrap-up) — 2026-05-22-and-earlier decisions

_Moved from decisions.md to keep it under the 600-line cap (archive-on-resolve, D150). These are architecture-programme + Spec 15/16-era decisions, all superseded by Spec 22 and the current data-layer architecture. Preserved verbatim for git-blame continuity. D-number citations from current docs (e.g. D158 → Decision 18) resolve here._

## 2026-05-22 — Architecture programme close-out (Phase 4 + Phase 7 + parking sweep)

**D40 — Council predictions are hypotheses; empirical gate mandatory before treating as specs.** Wave 1 G2+G4 fixes produced zero pixel-diff movement despite correct implementation — fix-shape proposals targeted wrong code paths. Rule: any council output proposing a fix shape requires empirical-validation step before subagent dispatch. blub.db row 276.
Status: active

---

## 2026-05-22 — Phase 1.5 inserted + Phase 2 parser strategy change

**D33(arch-staging) — Phase 2 parser strategy: runtime enumeration, not source parsing.** Static PHP source parsing crashed twice. Replacement: `wp eval` against live WP block type + styles registry (`WP_Block_Type_Registry` + `WP_Block_Styles_Registry`). Canonical going forward — any future variation/style indexing reads runtime state.
Status: active

---

## 2026-05-21 — Architecture session (31-decision holistic redesign)

**D27 — DB consolidation: three databases merged into sgs-framework.db with `source` column.** wp-blockmarkup-mcp blocks.db + wp-devdocs-mcp hooks.db + sgs-framework.db → one DB. `docs` table extended with `doc_type='cli-command'`; `indexed_files` added for incremental `/sgs-update`. Shipped 2026-05-24 (was Phase 1 target). See `.claude/plans/2026-05-21-architecture-staging.md` §3 D1,2,11.
Status: active (shipped)

**D28 — Style-variation system killed; per-site theme.json adopted.** 9 client variation JSONs replaced with per-client `sites/<client>/theme-snapshot.json`. Three PHP files deleted; new `push-theme-snapshot.py` CLI deploys snapshots. Commit `43a93df9`. See staging doc §3 D14′,16′,17′,18,19.
Status: active (shipped)

**D30 — Button presets migrated to native WP 7.0 theme.json.** WP 7.0 natively generates 100% of `--wp--custom--button-presets--*` props from `theme.json`. `class-button-presets-admin.php` deleted; `wp_options[sgs_button_presets]` absent on sandybrown. Commit `60220b13`.
Status: active (shipped)

**D31 — Structural QC enforcement hook wired.** PostToolUse hook at `.claude/hooks/qc-on-converter-edit.py`. Fires when Write/Edit targets `converter_v2/convert.py` or `sgs-clone-orchestrator.py`. Commit in staging doc §11 D31.
Status: active

**D34 — Lucide icons refactored to WP 7.0 Icons REST controller (Phase 6).** Consumers get unified REST endpoint instead of bespoke resolution code. CAUTION: `wp_register_icon_collection` does NOT exist on WP 7.0 despite `WP_REST_Icons_Controller` existing — research correct entry point before implementing. See staging doc §11 D28.
Status: active (pending)

**D35 — Customiser migration of header/footer/site-info admin with View Transitions (Phase 5b).** New PHP classes `Sgs_Header_Customiser` + `Sgs_Footer_Customiser` + `Sgs_Site_Info_Customiser`. Pattern follows `Sgs_Floating_UI_Customiser`. Commit `60220b13`. See staging doc §3 D21,27.
Status: active (partially shipped — see D22/D23 Session B)

**D36 — WP 7.0 alignment audit for 10 wp-* skills (Phase 7).** Checks: deprecated API refs, missing WP 7.0 APIs, stale code examples across `wp-block-development`, `wp-block-themes`, `wp-interactivity-api`, `wp-plugin-development`, `wp-rest-api`, `wp-wpcli-and-ops`, `wp-performance`, `wp-abilities-api`, `wp-site-extraction`, `wp-project-triage`. See staging doc §11 D29.
Status: active (pending)

---

## 2026-05-21 — QC trio + skill cleanup + Wave 2 reshape

**D21(skill) — `/qc-council` created as empirical-validation gate before subagent dispatch.** 8-stage protocol: DETECT → LOAD GROUND TRUTH → SEED PERSONAS → DISPATCH → DEBATE → EMPIRICAL VALIDATION (HARD GATE, blub.db row 276) → EXPERIMENT DESIGN → IMPLEMENTATION → REPORT. Skillscore 92% A-. (no commit — skill file)
Status: active

**D25 — `/gap-analysis` Step 7.75 delegates to `/qc-council`.** Primary path replaces ~80 lines of duplicate 3-rater panel logic. `qc_review` JSON schema preserved for backwards compatibility. (no commit — skill file)
Status: active

**D26 — Wave 2 reshape: G1+G3+G5 are ONE wiring gap, not three problems.** `/wp-blocks dump` confirmed all mapping data exists in sgs-framework.db. G1+G3+G5 symptoms all from same gap: cv2 doesn't walk all classes + assign CSS ownership + record parent-child relations using existing DB tables. FR1 fast-path "fix" = one-line consistency add; not hero-special. (no commit — diagnosis)
Status: active

---

## 2026-05-20 — Phase 1 Spec 16 §FR6 rewrite + Phase 2 future capabilities

**D2(2026-05-20) — Token-snap requires strict exact-match.** Nearest-match snap caused visible regressions. Per Bean's binding: "if value matches global default, use token; if not, insert literal." ΔE2000 ≤ 1.0 for colour; ≤ 1px for spacing/font-size. Commit `8a996194`.
Status: active

**D3(2026-05-20) — Spec 16 §FR6 four-destination CSS router replaces verbatim Stage 0.7 dump.** `css_router.py` routes every CSS rule to D0 (global) / D1 (typed-attr-lift) / D2 (wrapper-CSS scoped) / D3 (gap-candidate). Every rule routes to exactly one destination — no silent drops. Commits `05fb38a4` + `44ba373b` + `dce5a496`.
Status: active

**D4(2026-05-20) — Header/footer/nav structural defence-in-depth (two layers).** Tool layer: PostToolUse hook `no-header-footer-block.py` blocks Write/Edit on `src/blocks/(header|footer|nav)/` (commit `8838b6fb`). Source layer: `_is_chrome_section()` in Stage 9b detects chrome at 4 boundary signals (commit `3a70587c`).
Status: active

**D5(2026-05-20) — Attribute-gap promotion is end-of-line cleanup, NOT primary pixel-diff path.** 3-rater council confirmed promotion closes last 5-10%; dominant 50-85% gap is structural (G1-G5). Ship G1-G5 + F5 FIRST. See `reports/2026-05-20-pipeline-root-gap-council/real-path-synthesis.md`.
Status: active

**D6(2026-05-20) — Block-variation system uses native WP `register_block_variation()`.** Confidence 0.70-0.90 against existing block + attribute differences → emit `wp:sgs/<block> {variantStyle:'featured'}`. PHP loader at `includes/variations/class-sgs-block-variations.php` auto-discovers `sgs-*.php` files. Commit `36ef9552`.
Status: active

---

## 2026-05-19 — cv2 RCs + deploy consolidation + Stage 10 + skill rename

**D1(2026-05-19) — `/deploy` → `/wp-sgs-deploy` rename + `/deploy-check` absorbed as Phase 1.** Three deploy concepts conflated. New canonical homes: `/wp-sgs-deploy` (framework + checklist), `/sgs-clone --deploy-target page:<id>` (per-page). `--skip-check` flag for trusted micro-patches.
Status: active

**D2(2026-05-19) — Stage 10 wired: per-page deploy in cloning pipeline.** `upload_and_patch.py` wired as Stage 10 with `--deploy-target page:<id>` / `post:<id>`. Fires after Stage 9c; soft-fails. Commit referenced in skill.
Status: active

**D3(2026-05-19) — All 10 static SGS blocks converted to dynamic.** Mixing static + dynamic caused "invalid block" errors in cv2 (self-closes only valid for dynamic). `_STILL_STATIC_SGS_BLOCKS = frozenset()`. Deprecated.js shims for backward compat. Commit batch 1 + 2 this session.
Status: active

**D4(2026-05-19) — Container block is canonical advanced-background wrapper.** Hero block.json dual-cascade anti-pattern removed. Container extended with 4 background modes (Image, Video, Animation incl. parallax + ken-burns, Overlay incl. gradient). 15 new attrs. Hero defaults removed.
Status: active

---

## 2026-05-19 — Spec 17 Header/Footer Architecture (Waves 1+2+2.5+3) — summary

**D(spec17) — CPT REST gating: `sgs_header` + `sgs_footer` CPTs require `edit_theme_options` for REST access.** Anonymous REST calls return 401. File: `includes/class-sgs-block-cpts.php`. Variation picker uses resolver-only `_resolve_global_styles_post_id()` path (single direct DB lookup, cached). Two-layer ReDoS guard on rules engines: 256-char input cap + static blocklist of catastrophic-backtracking constructs.
Status: active

---

## 2026-05-18 — P-WP-ALIGNMENT-WIDTH-SYSTEM shipped

**D(width-system) — cv2 pipeline targets WP PAGES not POSTS; `widthMode` infrastructure built.** Posts use `single.html` (max-width 800px); pages use `page.html` (no constraint). 14.3-point pixel-diff drop from this single change. New `widthMode` enum (default/wide/full/custom × per-viewport) on sgs/container; converter maps mockup max-width → semantic widthMode or literal. Commits `c7f42003` + `86172812`.
Status: active

---

## 2026-05-17 — Universal recognition + conversion session close

**D(b-2026-05-17) — DB-first lookups, no hardcoded dicts.** `_CSS_PROP_TO_SUFFIX` (21 rows) replaced by `db.css_property_suffixes()` reading `property_suffixes` table (117 rows). `_BREAKPOINT_SUFFIXES` replaced by `db.breakpoint_suffix_rules()`. blub.db row 260 + Rule 11 HARD-GATE in `/sgs-clone`. Commit `168fd2ca`.
Status: active

**D(function-exists) — `function_exists()` guards universal on all render.php top-level helpers.** "Cannot redeclare" fatals recurred. Every top-level function in any render.php MUST be wrapped in `if ( ! function_exists() ) { ... }`. Applied to all helpers.
Status: active

---

## 2026-05-16 — Spec 16 Phase 8 session: accuracy + universality

**D(phase8-h) — WP `file:` render wrapper discards return values (CRITICAL).** `_wp_block_render_callback_from_file()` wraps file in its OWN `ob_start()` + `ob_get_clean()`. File's return value is thrown away. render.php MUST echo directly via `printf()` / interleaved `<?php ?>` HTML — never `return ob_get_clean()`.
Status: active

---

## 2026-05-14 — Spec 16 decisions (core architecture)

**D(spec16-9) — Parallax scroll NOT applicable to logo / icon / header blocks.** Parallax-on-logo breaks visual anchor; parallax-on-header breaks sticky/transparent behaviour + causes jank. `supports.sgs.parallax` is opt-in but MUST NOT be wired for `sgs/responsive-logo`, `sgs/icon`, or the header behaviour wrapper. Permanent.
Status: active

---

## 2026-05-14 — Phase 6 v2 Step 5: Rosetta Stone chokepoint + IP-defence framing removed

**D(step5) — Rosetta Stone chokepoint propagated; IP-defence framing removed at root.** `_insert_uimax_pattern` + `sgs-update-uimax-sync.py` route through `uimax_write.validate_and_write`. `LICENSING_BANNED_SUBSTRINGS` + `find_licensing_violations()` removed entirely — UI patterns aren't copyrightable; the gate was theatre. 3 regression-guard tests added to prevent re-introduction. 109/109 tests pass. Commits this session.
Status: active

---

## 2026-05-12 — Spec 15 Phase 4.5: cloning preserves intentional bespoke detail

**D(phase45) — `/sgs-clone` token lint defaults to ADDITIVE mode.** Non-token CSS values → `NewTokenCandidate` rows written to client style variation JSON (NOT snapped to nearest token). Base `theme.json` stays lean; client variation absorbs bespoke differences. Bean's framing: "small differences are all intentional." Commits `8599faf3`, `55a6d73e`, `3c2c07b7`, `a9b9b1c3`.
Status: active

---

## 2026-05-11 — Deterministic SGS-BEM voter; Trustpilot; third-party widget split

**D(trustpilot) — `sgs/trustpilot-reviews` as first-party block; Browserless auth via `?token=` query string.** Official WP plugin paywalls display widgets on free plan. Browserless `/content` rejects Bearer — auth is `?token=<key>` query string. Failure surface: settings page activity log only. Commits `c6bd4980` + `06df2807`.
Status: active

**D(widget-split) — Locked brand identity + theme-inherited typography split for embedded third-party widgets.** Platform logo / brand colour for stars / verified badge = locked. Font-family + colour + base font-size = inherit from host theme. Border + hover = `var(--wp--preset--color--primary, <brand-fallback>)`.
Status: active

---

## 2026-05-10 — SGS-BEM canonical naming + cross-platform deferral

**D(spec13) — SGS-prefixed BEM is canonical for all Bean-controlled drafts (Spec 13 locked).** `.sgs-<block>__<element>--<modifier>`. Hard pre-flight gate on production runs; `--draft-mode` = soft warning; `--legacy` bypasses for pre-rule mockups. Live scrapes use lingua-franca-conversion. blub.db row 236. Canonical: `.claude/specs/15-DETERMINISTIC-DRAFT-TO-SGS-CONVERTER.md` (absorbed Specs 12+13+14).
Status: active

**D(cross-platform-defer) — Cross-platform emit pathways (P-CP-1/2/3) deferred until M9 production-stable + ≥3 successful clones banked.** Rosetta Stone infrastructure structurally ready. Cost = engineering pass per platform target — non-trivial. M9 ships first; cross-platform emit downstream of unreliable clone is wasted work.
Status: active

---

<!-- Block archived 2026-06-06: decisions 2026-05-24 -> 2026-05-30 (D107-D113 down to doc-op programme), moved verbatim from decisions.md most-recent-first. Grep by D-number; global order is oldest-first except this trailing block. -->

## 2026-05-30 — XS-2 / XS-3 / XS-4 / XS-5 / XS-6 / D6-inheritance / D5-methodology session-close batch (D107-D113)

Session aggregate: mean pixel-diff 58.6% → 56.40% (-2.20pp) across the canary trajectory below. One walker fix shipped then reverted (D108 / D109) because of regression on featured-product (+13.07pp) and social-proof (+10.40pp). Schema and tooling work landed cleanly; walker consumption of the new shape signals is deferred to the next session with a refined trigger.

| Step | Commit | Mean pixel-diff |
|---|---|---|
| Session start (post-D93-D100 baseline) | — | 58.6% |
| Post-XS-1 (CSS sentinel fix) | 80cfc9ad | 56.85% |
| Post-XS-8/9/10 (slot-list filter + comment skip + rich-text core) | 6087594f | 56.37% |
| Post-XS-9.1+9.2 + mojibake + featured variant | b3feb9a2 + deploy | 56.47% |
| Post-D1+XS-2+XS-5 | e2c8597e | 56.39% |
| Post-XS-3 walker (REVERTED) | f173b351 | 57.71% (+1.32pp regression) |
| After revert | c76aa107 | ~56.39% |
| Post-XS-4 backfill | 04fa0f2b | 56.40% |
| Post-D6 inheritance script | 062c69d1 | n/a (no walker change) |
| **Final aggregate** | | **-2.20pp** |

**D107 — XS-2 voter literal-slug-match short-circuit retired; `blocks.tier` column + `is_section_root` flag (commit e2c8597e, ships with XS-5).** `per-section-convention-voter.py:295-305` previously returned `f"sgs/{slug_root}"` at confidence 1.0 for every `sgs-`-prefixed BEM class before consulting the DB. That short-circuit emitted phantom slugs no `block.json` registered, forcing Stage 2 fallback to `sgs/container` at confidence 0.0 across 9 of 9 boundaries on the Mama's canary. Fix shipped three coordinated changes:

1. `block.json` schema extension. Section-root blocks declare `supports.sgs.is_section_root: true`. Operator-set, not algorithm-derived (Bean rejected algorithmic detection as false-positive prone on hero). Current roster: `sgs/hero`, `sgs/cta-section`.
2. `blocks.tier` column added. `/sgs-update` Stage 1 reads `supports.sgs.is_section_root` per block.json and writes `tier='class-section'`.
3. Voter refactor at lines 295-305. For section-root BEM classes the voter queries `SELECT slug FROM blocks WHERE tier='class-section' AND slug=?` and returns the hit at confidence 0.95; for inner-div classes it excludes class-section rows. Literal-slug-match retained only as last resort, gated on a registered `block.json`.

R-22 compliance: R-22-1 (tier lives in DB), R-22-9 (universal mechanism), R-22-11 (Stage 11 pixel-diff gates the commit). Sibling parking entry P-XS-2-TIER-CRITERIA-DECISION closed.

**D108 — XS-3 / XS-6 unified `block_composition` table; walker consumption SHIPPED then REVERTED (commit f173b351 → c76aa107).** Bean's D6-expanded scope folded XS-3 (per-wrapper slot rows) and XS-6 (separate shape signals) into a single `block_composition` table. Schema: `block_slug PRIMARY KEY, wraps_block TEXT, composition_role TEXT CHECK IN ('section-root','wrapper-shell','content-block','leaf'), has_inner_blocks INTEGER, accepts_allowed_blocks TEXT`. 188 rows seeded by /sgs-update extension. Walker queries the table at Stage 5 to disambiguate multi-candidate BEM resolutions by shape before applying the FR-22-15 capability tiebreaker. Generic slot names (`products`, `cards`) never hardcode-route — routing is shape-driven (R-22-1, R-22-9).

**D109 — XS-3 walker code REVERTED (commit c76aa107) due to regression; refined trigger queued for next session.** The shipped walker condition produced +13.07pp on `sgs-featured-product` and +10.40pp on `sgs-social-proof` — aggregate +1.32pp. Root cause analysis deferred to next session. The `block_composition` DB table (188 rows) PERSISTS — schema and data are valid and reusable. Only the walker code that consumed `composition_role` was reverted. Sibling parking entry P-XS-3-TRIGGER-REFINEMENT records the next-session task: define a narrower trigger condition that excludes the two regressors.

Spec 22 implication: the new FR-22-16 / FR-22-15.1 sub-FRs documented in this session's edit-list are **DEFERRED** — they describe the intended walker consumption, not the live walker. The shape-signal data layer ships; the walker consumes it only when the refined trigger lands.

**D110 — XS-4 `assign-canonical.py` ported to post-D99 slots+roles schema; batch backfill ran cleanly (commit 04fa0f2b + follow-ups 52408c7e).** Old script queried the dropped `slot_synonyms` table; 9 references migrated to `slots WHERE scope='element'` + `roles` table. Dry-run + apply ran without errors. Coverage on `block_attributes.canonical_slot` rose from 52/2074 (2.5%) to 659/2074 (31.8%) at initial backfill, then to 692/2074 (33.4%) after XS-4 follow-ups (camelCase regex + slot vocab + Tier B2 compound selectors); `role` from 110/2074 (5.3%) to 676/2074 (32.6%) initial, then to 689/2074 (33.2%) post-follow-ups. ~1382 rows remain NULL — gap candidates for future enrichment. Predicted vs actual delta: predicted 80–90% coverage; actual 33.4% because slots.aliases lookup is conservative and most attrs lack canonical-vocabulary aliases. The expansion is real; the high-end estimate was inflated.

**D111 — XS-5 wrong/dead section-scope rows in `slots` deleted; testimonial + testimonial-slider re-inserted at element scope (commit e2c8597e, shipped with XS-2).** Twelve rows retired: 8 wrong-tier (`ingredients-section`, `ingredients`, `gift-section`, `social-proof`, `testimonial` section-scope, `testimonial-slider` section-scope, `brand-story`, `featured-product`) + 4 chrome-dead (`header`, `footer`, `site-header`, `site-footer`). Keepers at section scope (4): `core/group`, `hero`, `cta`, `cta-section`. Re-inserts at element scope (2): `testimonial → sgs/testimonial`, `testimonial-slider → sgs/testimonial-slider`; plus `inner` passthrough element row added. Resulting counts: 92 element + 4 section = 96 total. Featured-product takes no slot row — walker reaches `<section class="sgs-featured-product">`, no slots match, the post-D107 voter returns gap-candidate, Stage 2 FR-22-4 falls through to `sgs/container`. Rationale: section-root aliases that route to content-block primitives collapse sibling structure (Fix 2 +21.53pp regression cause, captured 2026-05-27).

**D112 — `sync-container-wrapping-blocks.py` inheritance script SHIPPED (commit 062c69d1).** 468 LOC. Populates `block_composition.wraps_block` for 4 blocks (`sgs/hero`, `sgs/cta-section`, `sgs/modal`, `sgs/quote`). Flags 174 attrs missing from wrapping blocks plus 14 naming-drift dedups across diff Markdown files in `pipeline-state/section-root-sync/`. Operator review path; never auto-edits block.json. Closes the D107 sibling parking entry P-SECTION-ROOT-INHERITANCE-SCRIPT.

**D113 — Methodology rule captured: per-row measurement gating on multi-row DB changes.** STOP catalogue entry #12 + pre-flight ritual question Q6 added to `next-session-prompt.md` (commit 32c70774). Reinforces blub.db 287 `feedback_row_by_row_measurement_gate_per_db_change.md`: any DB change touching more than one row ships ONE row at a time with /sgs-clone Stage 11 between each. XS-4 was the live counterexample — batched 600+ rows produced uninterpretable aggregate delta; future XS-4-shaped work splits per-block.

**Also shipped this session (commit a23ff53f, no D-number):** `plugins/sgs-blocks/scripts/build-deploy.py` — single-command build + deploy + OPcache reset replaces the multi-step manual sequence in dev-setup.md.

Status: D107, D110, D111, D112, D113 active. D108 / D109 — schema active, walker code reverted, refined-trigger work parked at P-XS-3-TRIGGER-REFINEMENT.

---

## 2026-05-30 — XS-1 / XS-8 / XS-9 / XS-10 mechanical cv2 fixes; XS-11 retracted as subagent false positive (D102-D106)

**D102 — XS-1 CSS serialiser sentinel leak (`'@media ... :: .selector'` → proper @media nesting).** `_parse_css()` at convert.py:352 flattens nested @media rules into dict keys with `' :: '` internal sentinel. `collect_css_for_classes` at line 417 emitted these keys verbatim into inline `<style>` blocks, producing invalid CSS browsers refused. Fix: detect sentinel + reconstruct `@media (...) { .selector { ... } }`. Empirical: 57.96% → 56.85% mean (-1.11pp), 19 → 0 sentinel leaks in extract.json, sgs-featured-product -9.00pp (the only large win because most sections target wrappers that don't render — XS-3 gating). Commit `80cfc9ad`.

**D103 — XS-8 leftover-bucket noise filter (Stage 9 extraction_failed).** `route_extraction_failed()` enumerated every Stage 3 declared slot as "extraction_failed" when not filled, producing ~69 noise entries per section (sgs/container's 69 default-bearing attrs) drowning real defects. Fix: skip emission when both `attribute_role == 'auto-derived'` AND `default not in {None, '', []}`. Empirical: leftover-bucket extraction_failed 723 → 387 (-336 entries, -46%), 0pp pixel-diff (observability only). Commit `6087594f`.

**D104 — XS-9 rich-text preservation in core/* atomic-tag swaps + XSS hardening.** `_atomic_attrs_for` used `node.get_text(strip=True)` collapsing inline tags (`<br>`, `<strong>`, `<a>`). New `_rich_text_content` helper preserves bounded inline-tag allowlist for core/heading / core/paragraph / core/quote / core/button. **SGS atomic emissions (sgs/heading, sgs/text, sgs/button, sgs/quote) retain get_text behaviour pending render.php audit** — parked at P-SGS-ATOMIC-RICH-TEXT-AUDIT. XSS hardening: HTML-escape text nodes, scheme-allowlist `<a href>` ({http, https, mailto, tel, relative}), attribute-escape href value, drop all other attrs. Empirical: 0pp impact on Mama's canary because hero H1 routes via sgs/heading (excluded by conservative scope), not core/heading. Architecturally correct + benefits future core/* mockups. Commit `fec91c5d`.

**D105 — XS-10 walker skips bs4 Comment before NavigableString check.** HTML comments (`<!-- Main product -->`) leaked as bare text because `Comment` is subclass of `NavigableString`; the walker's Exception 0 matched Comment via NavigableString branch and emitted `str(node).strip()` (the comment's inner text). Fix: explicit Comment check returning None placed BEFORE NavigableString branch. Empirical: sgs-featured-product -5.47pp (the only section with HTML comments in mockup). Commit `943e52b2`.

**D106 — XS-11 RETRACTED: subagent false positive on emoji mojibake.** Gift-section subagent in 2026-05-30 diagnostic register claimed `🏥` → `🏥` mojibake in extract.json. Byte-level verification proved otherwise: raw bytes `\xf0\x9f\x8f\xa5` = correct UTF-8 encoding of U+1F3E5. The subagent saw mojibake-shaped text in its tool output because its rendering environment misinterpreted UTF-8 as cp1252; the file itself is correctly encoded. sgs-clone-orchestrator.py + convert.py file reads already use `encoding="utf-8"` explicitly. New captured lesson: `feedback_subagent_evidence_must_be_byte_verified_for_encoding.md` — any encoding-related defect claim from a subagent must be byte-verified via `open(p, 'rb').read()` + `.hex()` before treating as a real bug. R-22-11 extension: "verify rendered output, not internal metrics" extends to "verify the bytes, not the rendering of the bytes."

**Empirical lesson from XS-1 + XS-8 + XS-9 + XS-10 measurement cycle:** subagent fix-shape predictions (range -15 to -30pp aggregate) overshot real impact by ~10× (actual aggregate -1.59pp from 57.96% to 56.37%). Per blub.db 276 council-predictions-need-empirical-validation: fix-shapes are HYPOTHESES, not specs; predicted impact must be empirically measured. Going forward: each fix-cycle records pre/post + predicted vs actual ratio for calibration accumulation. After 5-7 calibration points the ratio will inform future predictions.

**Per-section pixel-diff noise floor observed:** sgs-social-proof showed +2.04pp delta from XS-8/9/10 measurement run even though block_markup is byte-identical pre/post. Confirms inherent per-section pixel-diff variability of ~±2pp from font rendering / screenshot timing / browser state. Future fix-cycles must interpret per-section deltas with this noise floor when isolating impact.

---

## 2026-05-29 — slot_synonyms + legacy_role_lookup unified into `slots`; new `roles` table; html_tag_to_core_block INSERT OR REPLACE; property_suffixes.kind_override (D99)

**D99 — slot_synonyms + legacy_role_lookup unified into new `slots` table; new `roles` table replaces slot_synonyms.role_classification; html_tag_to_core_block seed switched to INSERT OR REPLACE; property_suffixes.kind_override column replaces _KIND_BY_SUFFIX hardcoded dict.**

Four DB-driven cleanups in one atomic migration (`2026-05-29-d99-db-unification.py`). All honour R-22-1 (DB-first, no hardcoded dicts).

**Fix 1 — `slots` table (105 rows, replaces slot_synonyms 89 + legacy_role_lookup 16).** PK is `(slot_name, scope)` composite — required because the same name can exist at both scopes (`header` is element-scope identity slot in slot_synonyms AND section-scope class in legacy_role_lookup). `scope='element'` rows = former slot_synonyms; `scope='section'` rows = former legacy_role_lookup. `html_semantic_tag` column NOT migrated (was only 27/89 populated; `atomic_tag_map()` never consulted it; `html_tag_for_slot()` now returns None). Consumer updates: `db_lookup._slot_synonyms()` → `SELECT slot_name, aliases FROM slots WHERE scope='element'`; `_slot_to_standalone_block()` → same table; `_slot_alias_to_standalone()` → same; `legacy_role_lookup_for()` / `_load_legacy_role_cache()` → `SELECT slot_name, standalone_block FROM slots WHERE scope='section'`; `sgs-update-v2.py` Stage 5 → `SELECT rowid, slot_name FROM slots WHERE scope='element'` + `UPDATE slots SET standalone_block=? WHERE rowid=? AND scope='element'`. Old tables `slot_synonyms` and `legacy_role_lookup` DROPPED after count verification (element=89, section=16). Both DBs (`.claude` + `.agents`) migrated.

**Fix 2 — `roles` table (20 rows, replaces slot_synonyms.role_classification).** This closes the link-href bug: `_content_bearing_roles()` was querying `SELECT DISTINCT role FROM slot_synonyms WHERE role_classification='content-bearing'`, which returned only 4 roles because no slot_synonyms row had `role='link-href'` — the migration that seeded the column only UPDATEd existing rows. The `roles` table is seeded from `_ROLE_CLASSIFICATION_MAP` (20 entries defined by role name, not derived from slot rows) via `INSERT OR REPLACE`. After D99, `_content_bearing_roles()` returns 5 roles (adding link-href) and `equivalent_block_for('sgs/media', 'videoUrl')` correctly returns `'sgs/media'` (was None). `_styling_behaviour_roles()` returns 15. Schema: `(role_name TEXT PK, classification TEXT CHECK('content-bearing'|'styling-behaviour'|'unclassified'))`. `_migrate_roles_table()` runs at module load; INSERT OR REPLACE ensures seed-dict updates propagate (not OR IGNORE).

**Fix 3 — html_tag_to_core_block seed: INSERT OR IGNORE → INSERT OR REPLACE.** db_lookup.py line change only. Prevents seed-dict/DB divergence when `_HTML_TAG_TO_CORE_BLOCK_SEED` entries are updated — DB picks up changes on next module load without manual row edits.

**Fix 4 — property_suffixes.kind_override column (17 rows seeded).** `_KIND_BY_SUFFIX` dict (17 entries: LineHeight, LetterSpacing, FontFamily … Alignment) was a hardcoded Python lookup. Now seeded into `property_suffixes.kind_override` via idempotent `_migrate_property_suffixes_kind_override()` at module load. `_kind_for()` queries `kind_override` column first (R-22-1 DB-first); falls through to role-based inference for uncovered suffixes. `UPDATE WHERE kind_override IS NULL` preserves manual overrides. `_KIND_BY_SUFFIX` dict retained in code as seed source.

Migration script: `plugins/sgs-blocks/scripts/migrations/2026-05-29-d99-db-unification.py` (idempotent, applies to both DBs, verifies counts before DROPs, uses explicit `BEGIN`/`COMMIT` to wrap DDL in one transaction).
Status: active

---

## 2026-05-29 — Stage 10 v3 deletes retired blocks from blocks table + phpunit intelephense stub (D100)

**D100 — Stage 10 v3 deletes retired blocks from blocks table + phpunit added to intelephense.stubs.** (1) `_prune_orphans_on_conn` in `sgs-update-v2.py` gains a new category (d): sgs/* slugs present in the `blocks` table but absent from `src/blocks/<basename>/block.json` are retired blocks that should not persist. Aggressive mode (default) DELETEs the blocks row; conservative mode marks `is_stale=1` (new column on blocks table, added via `_ensure_blocks_is_stale_column` helper — idempotent ALTER TABLE). Non-sgs/* slugs (core/*, etc.) are intentionally skipped — different lifecycle managed by Stage 2. Result dict gains `orphan_blocks_deleted` key. Cleaned 4 known ghost rows on live run: sgs/back-to-top, sgs/data-display, sgs/icon-block, sgs/reading-progress (plus 2 orphan capabilities that were child rows). Idempotent — second run reports 0. (2) `"phpunit"` added to `intelephense.stubs` array in `.vscode/settings.json` between "Phar" and "posix" — fixes "Undefined type 'PHPUnit\\Framework\\TestCase'" P1009 warnings on PHPUnit test files (e.g. BlockDeprecationsTest.php); clears on next VS Code intelephense rescan.
Status: active

---

## 2026-05-29 — sgs/container ship-blockers + quality-blockers landed (D98)

**D98 — 5 capability extensions shipped to sgs/container: SB-1 (grid item defaults + providesContext), SB-2 (editor grid preview uses gridTemplateColumns string), QB-1 (gridTemplateRows/gridAutoRows/justifyItems/alignContent), QB-2 (gridTemplateColumns string inspector control), QB-3 (templateMode / allowedBlocks guidance).** New attrs added to block.json (13 total): `gridItemPadding`, `gridItemBackground`, `gridItemBorderRadius`, `gridItemBorder`, `gridItemShadow`, `gridItemTextColour` (SB-1); `gridTemplateRows`, `gridTemplateRowsTablet`, `gridTemplateRowsMobile`, `gridAutoRows`, `justifyItems`, `alignContent` (QB-1); `templateMode` (QB-3). `providesContext` block added for all 6 `sgs/gridItem*` keys (SB-1). render.php emits `--sgs-gi-*` CSS custom properties when any grid item default is set; emits `grid-template-rows`, `grid-auto-rows`, `justify-items`, `align-content` inline; responsive gridTemplateRows/Columns tablet+mobile overrides via scoped `<style>` block (same uid pattern as existing gap/widthMode responsiveness). edit.js: SB-2 editor preview reads `gridTemplateColumns` string attr instead of hardcoded `repeat(N,1fr)`; new "Advanced grid layout" collapsible section in Layout panel (QB-2 column template + QB-1 row template + gridAutoRows + justifyItems + alignContent responsive controls); new "Grid item defaults" PanelBody (SB-1 — only visible when layout=grid); new "Template mode" PanelBody (QB-3). style.css: `.sgs-container--grid > .sgs-container` rule consumes all `var(--sgs-gi-*)` defaults with no fallback (CSS custom properties fall through transparently when unset). deprecated.js: v3 entry covers all pre-D98 blocks — migrate() backfills 13 new attrs with block.json defaults; export order now `[v3, v2, v1, v0]`. Build: webpack compiled successfully, 0 WPCS errors.
Status: active

---

## 2026-05-29 — sgs/certification-bar retired; merged into sgs/trust-bar

**D95 — sgs/certification-bar retired; all capability merged into sgs/trust-bar as badgeStyle variants (Bean approved, Item 11). Block subsequently renamed from sgs/trust-badges → sgs/trust-bar 2026-05-31 (matches mockup section class `.sgs-trust-bar`).** Both blocks were "horizontal strips of trust signals with labels". Different visual treatments unified under one block via a `badgeStyle` enum: `icon-circle` (existing default), `text-only` (cert-bar pill shape), `image-badge` (cert-bar logo/cert shape). New attrs added to trust-bar: `badgeStyle`, `title`, `titleColour`, `titleFontSize`, `labelColour`, `labelFontSize`, `badgeSize` (all carried from cert-bar); plus new auto-scroll capability: `autoScroll` (boolean), `autoScrollSpeed` (slow/medium/fast), `autoScrollPauseOnHover` (boolean). Version bumped 0.1.0 → 0.2.0. render.php branches on `badgeStyle` with three fully-separate render paths. view.js (new ES module, `viewScriptModule`) activates conditional overflow-detecting marquee scroll (same clone+measure pattern as sgs/brand-strip) only when items genuinely overflow the container — no animation wasted on short lists. `prefers-reduced-motion` honoured. Cross-block migration: `deprecated.js` v3 handles rename alias sgs/trust-badges → sgs/trust-bar (identity pass); v2 entry with `isEligible({ name }) => name === 'sgs/certification-bar'` + `migrate()` mapping all 8 cert-bar attrs to trust-bar equivalents (badgeStyle text-only/image-only/image-and-text → text-only/image-badge/image-badge). Source: `src/blocks/trust-bar/` (was trust-badges). Source deleted: `src/blocks/certification-bar/` + `build/blocks/certification-bar/`. DB: `sgs/certification-bar` rows deleted from `blocks` (1), `block_attributes` (8), `block_supports` (8), `block_capabilities` (3) in both sgs-framework.db copies. Tests: `BlockDeprecationsTest.php` cert-bar entry removed; `blocks.spec.ts` EXPECTED_BLOCKS updated. `/sgs-update` required post-deploy to populate new trust-bar attrs in DB.
Status: active

---

## 2026-05-29 — sgs/media extended to image + video

**D97 — sgs/media extended from image-only to unified image/video block (Bean original intent).** Block was named "media" from the start but only shipped the image side; `block_attributes` had a ghost `sgs/media.videoUrl` row (canonical_slot=media, role=link-href) left over from earlier design intent. Extension adds 12 new attrs: `mediaType` (enum: image|video, default image), `videoUrl`, `videoSource` (enum: external|internal), `videoId`, `videoMimeType`, `videoPoster`, `videoPosterId`, `videoAutoplay`, `videoLoop`, `videoMuted` (default true — browser autoplay requirement), `videoControls` (default true), `videoPlaysInline` (default true — prevents iOS fullscreen), `videoLazyLoad` (default true). Version bumped 1.0.0 → 1.1.0. render.php branches on `mediaType` (auto-detected from populated attrs when absent for backwards-compat): image path preserves 100% of existing naked-mode / figure / link / srcset logic; video path routes to `<iframe>` for YouTube/Vimeo (regex detect + watch→embed URL conversion, youtube-nocookie.com domain, query params from playback attrs) or native `<video>/<source>` for direct MP4/WebM/internal WP library files (MIME auto-detect from URL extension). All shared styling attrs (objectFit, objectPosition, aspectRatio, maxWidth, maxHeight, borderRadius, opacity, responsive order) applied uniformly to both media element types via a single `$media_styles` array. edit.js adds Media Type ButtonGroup toggle at inspector top; Image tab keeps existing MediaPlaceholder/MediaUpload flow; Video tab adds source toggle, URL TextControl, MediaUpload for internal video + poster, playback ToggleControls. style.css extended with `.sgs-media__video` rules mirroring `.sgs-media__img` + iframe aspect-ratio fallback (16/9). deprecated.js v1 covers the pre-1.1.0 image-only schema with a migrate() that backfills mediaType:'image' + all video attrs with defaults so existing posts load cleanly. index.js wired with deprecated array. Build confirmed clean. Note for main session: `/sgs-update --stage 1` needs to run to populate the 12 new video attrs in block_attributes and resolve the ghost `videoUrl` row.
Status: active

---

## 2026-05-29 — Stage 10 prune-mode default → aggressive + attr-level orphan detection

**D94 — Two changes to `sgs-update-v2.py` Stage 10 (`prune_orphans`).**

**Change 1 — `--prune-mode` default flipped to `aggressive`.** Previously `mark-stale` (set `is_stale=1`) was the default; now `aggressive` (DELETE) is the default. Reasoning: `block.json` is the source of truth; a support that is no longer declared in `block.json` would be re-INSERTed by Stage 1 if the block returns, so marking it stale rather than deleting it just creates permanent clutter. Conservative mode is now opt-in via `--prune-mode conservative`. Legacy CLI alias `mark-stale` maps to `conservative` internally. Result: the 26 pre-existing `is_stale=1` rows (from 4 retired blocks — `sgs/back-to-top`, `sgs/data-display`, `sgs/icon-block`, `sgs/reading-progress` — all with `block.json` absent from disk) were deleted on first aggressive run. Post-run `SELECT COUNT(*) FROM block_supports WHERE is_stale=1` = 0.

**Change 2 — Attr-level orphan detection added to Stage 10.** Previously Stage 10 only caught block-level orphans (block_slug absent from `blocks` table). It missed the case where the block exists in `blocks` but a specific `attr_name` was removed from `block.json` (Stage 1 only INSERTs/UPDATEs attrs, never removes them). Fix: `_prune_orphans_on_conn` now builds a `live_attrs` map (`slug → frozenset[attr_names]`) from the current block.json scan and walks all SGS `block_attributes` rows whose `block_slug` is in the `blocks` table. Rows where `attr_name` is absent from `live_attrs[block_slug]` are deleted. Also handles the edge case where a block is in the `blocks` table but has no `block.json` on disk (all its attrs are ghost rows). `block_attributes` has no `is_stale` column, so conservative mode is a no-op here (a warning is printed but rows are still deleted — no alternative). New result-dict key: `orphan_attributes_deleted_attr_level` (distinct from `orphan_attributes_deleted` which is block-level). Live run deleted 205 ghost attr rows. Idempotency confirmed (all counters 0 on second run).

**Stale-supports detection scope fix (discovered during implementation):** The stale-supports check was previously gated on `b_slug in live_supports`, which silently skipped blocks that are in the `blocks` table but have no `block.json` on disk. These blocks' supports were perpetually is_stale=1 but never deleted. Fix: check `b_slug not in live_supports` as an additional match condition (all their supports are stale). Same fix applied to attr-level orphan detection for the same edge case.

Status: active

---

## 2026-05-29 — block_capabilities wired into pipeline; capability-aware BEM tiebreaking

**D96 — block_capabilities first-class pipeline integration: capabilities_for() + blocks_with_capability() helpers + capability-aware tiebreaking in resolve_slug_from_bem (FR-22-15).** Three changes land together. (1) `populate-db.py:CAPABILITY_RULES` seed propagation bug fixed: `INSERT OR IGNORE` replaced with `INSERT OR REPLACE` + a pre-pass `DELETE` of rows whose capability tag is no longer present in `CAPABILITY_RULES` — previously, any edit to `CAPABILITY_RULES` had no effect after first populate (rows already in DB were silently preserved by IGNORE). (2) Two new DB-driven helpers in `db_lookup.py`: `capabilities_for(block_slug) → frozenset[str]` (LRU-cached, maxsize=256) and `blocks_with_capability(capability) → frozenset[str]` (LRU-cached, maxsize=64). Both query `block_capabilities` table directly; R-22-1 compliant (zero hardcoded routing dicts). Private `_capability_rank(block_slug) → int` helper ranks blocks by semantic specificity using `_CAPABILITY_PRIORITY` list (convention-ordering, not a routing dict — precedent: `_BREAKPOINT_RULES`). (3) `_resolve_slug_from_bem_tuple` Path 1 multi-candidate tiebreaker upgraded from alphabetical-first to `(_capability_rank, slug)` sort (FR-22-15, Spec 22 §3). Concrete example: `sgs-testimonial-slider sgs-container` on a social-proof section root now resolves to `sgs/testimonial-slider` (rank 11, `carousel` tag) over `sgs/container` (rank 16, `grid-layout` tag) — previously alphabetical gave `sgs/container`. Test case in FR-22-15 §PASS test. Files touched: `db_lookup.py` (helpers + tiebreaker), `populate-db.py` (seed fix), `specs/22-*.md` (FR-22-15 + data-layer table row updated). No commit per dispatch constraints.
Status: active

---

## 2026-05-28 — sgs/svg-background retired; capability merged into sgs/container

**D93 — sgs/svg-background retired; SVG background capability merged into sgs/container (Bean approved).** `sgs/svg-background` was a standalone block wrapping InnerBlocks with an SVG animation layer. Its entire feature set (SVG paste field, background/foreground position, 3 animation types × 3 speeds, opacity, min-height, text-shadow) is now a capability of `sgs/container`'s existing background system, alongside image/video/parallax/ken-burns. New attrs use `bgSvg*` namespace: `bgSvgContent`, `bgSvgPosition`, `bgSvgAnimation`, `bgSvgAnimationSpeed`, `bgSvgOpacity`, `bgSvgMinHeight`, `bgSvgTextShadow`. Editor: new "SVG" tab added to the Background PanelBody in `container/edit.js`. Render: `sgs-container__svg-bg` div emitted by `container/render.php` before or after `$content` based on `bgSvgPosition`. CSS: animation keyframes + layer z-index rules added to `container/style.css` (renamed from `sgs-svg-background--*` → `sgs-container--svg-*`). Migration: `deprecated.js` v2 entry in container handles cross-block migration of existing `sgs/svg-background` posts — `isEligible()` matches the retired block name; `migrate()` maps old attrs → `bgSvg*`. Source deleted: `src/blocks/svg-background/` + `build/blocks/svg-background/`. DB: `sgs/svg-background` rows deleted from all 4 tables in both sgs-framework.db copies. Rationale: eliminates a redundant block (every svg-background use-case is a container with a special background); reduces block menu noise; aligns with SGS principle of universal mechanisms over per-block special cases (R-22-9).
Status: active

---

## 2026-05-27 — Spec 22 Phase 1.5 close + Phase 2 reorder + R-22-14 binding rule

**D90 — Phase 1.5 CLOSED with just Fix 1 shipped (walker FR-22-3 #3 ordering, commit `5731dc36`).** Mean pixel-diff 81.55% → 58.6% aggregate (−22.9pp); 17 of 18 non-hero body cells improved substantially (trust-bar −72pp, social-proof −53pp, brand −33pp, gift-section −33pp). 0/21 body cells PASS ≤5% gate but that's not the close criterion — the close criterion was diagnostic-value-complete + clear-path-to-pixel-target-identified. Both met. Phase 1.5's empirical role was to surface structural prerequisites: walker collapses `__content`/`__media` BEM wrappers + 61 SGS blocks have hybrid render.php that ignores `$content`. These surfaced via (a) Fix 2 DB-row attempt (rolled back +2.34pp regression) and (b) Fix 4 hero render.php attempt (BLOCKED at /qc-council). Bean directive: stop trying to hit ≤5% in Phase 1.5; close it; reorder Phase 2 ahead. Phase 1.5 plan file `.claude/plans/2026-05-26-phase-1-spec-22-implementation.md` to be archived to `plans/archive/`.
Status: active

**D91 — Phase reorder: Phase 2 = hybrid block migration BEFORE pixel-diff target; Phase 2.5 = bridge to ≤1% (was original Phase 1.5 stretch).** Original Spec 22 §7 sequencing put Phase 1.5 noise-floor work between Phase 1 and Phase 2. New sequencing: Phase 1 (architectural rewrite) → Phase 1.5 (Fix 1 walker ordering, CLOSED) → Phase 2 (61-block hybrid render.php migration via FR-22-6) → Phase 2.5 (≤1% pixel-diff noise floor). Reasoning: walker can't preserve `__content`/`__media` wrappers correctly until Fix 2b DB rows land (Stream A of Phase 2 plan), AND hero's 88/63/58% can't drop without render.php migration. Both are Phase 2 work. ≤1% bridge is downstream of structural fixes per `feedback_operator-promotion-is-end-of-line-cleanup` discipline. New Phase 2 plan at `.claude/plans/2026-05-28-phase-2-hybrid-block-migration.md`; current active scope = Stream A only (DB-quality pre-pass + corrected Fix 2b via seed-slot-synonyms.py + /sgs-update downstream + re-baseline measurement). Streams B/C/D deferred until Stream A measurement closes.
Status: active

**D92 — R-22-14 binding rule added 2026-05-27: FR-22-6 migrations never carry server-side legacy fallback hacks (Bean P1 locked).** Captured after Fix 4 hero render.php migration hit /qc-council BLOCK with Rater B demanding `if (empty($content) && !empty($headline)) { ...legacy scalar render... }` fallback. Bean reframed: "is this an sgs-only issue?" → YES (FR-22-6 hybrid problem is exclusively SGS framework debt; core/Gutenberg blocks use WP-native InnerBlocks.Content save or modern render-callback patterns; zero core blocks on Phase 0.4 roster) → "then just fix the whole roster, no legacy fallbacks". The fallback hack would add ~10-20 lines × 61 blocks = 600-1200 lines of dead-but-load-bearing scalar guard code permanently in the codebase — violates R-22-9 (universal mechanism) at the operational layer. Correct path: batch-migrate full 61-block roster via Phase 2 parallel /subagent-driven-development per FR-22-6.1 + ship WP-CLI batch existing-post migration script that walks every post on every production site + forces deprecated.js v(N+1) migration via headless block-parser. If WP-CLI script isn't ready, DELAY the deploy — never ship the per-block fallback hack. Captured lesson `feedback_fr22_6_hybrid_problem_is_sgs_only_no_legacy_fallback_hacks.md`. R-22-14 now in Spec 22 §6 binding rules list.
Status: active

---

## 2026-05-27 — Spec 22 Phase 1.3 array-attr backfill (corrected scope) + drift fix

**D89 — Spec 22 §FR-22-2.5 "Phase 0.1 backfill priority list" drift caught + corrected.** The original list of 4 priority attrs (product-card.packSizes, social-proof.testimonials, certification-bar.badges, info-box.items) was a spec-drafting drift — only 1 of 4 entries grep-verifies against the current codebase. `sgs/social-proof` block doesn't exist; `info-box.items` attr doesn't exist (real array attr is `elementOrder`, a slot-name config list — not item objects); `certification-bar.badges` attr name was wrong (real attr is `items`, already populated). Phase 0 was closed claiming this gate was met — it wasn't, because the spec was wrong about WHICH attrs needed backfilling. Caught 2026-05-27 by main-thread grep-verification before Phase 1.3 implementer dispatch (would have wasted Sonnet tokens producing empty work). Pattern: `feedback_grep_verify_handoff_diagnostic_premises.md` (2026-05-25) — every load-bearing claim in a spec or next-session-prompt must grep-verify against the actual codebase before driving action. Corrected priority list ships in Phase 1.3a: product-card.packSizes → button; gallery.mediaItems → media (image-object); form-field-address.fields → options (content); form-field-tiles.tiles → options (content). Plus 3 config arrays explicitly flagged role=`layout` to make positive-allowlist gate behaviour explicit: form-field-file.allowedTypes, info-box.elementOrder, table-of-contents.headingLevels. Spec 22 §FR-22-2.5 priority-list paragraph rewritten in same commit. New helper `db_lookup.array_item_slot_for(block_slug, attr_name)` lands alongside (17/17 unit tests PASS). Triple-NULL baseline recaptured 1090 → 1101 to account for 11 new sgs/heading attrs added by /sgs-update on 2026-05-27 (legitimate drift, not regression).

**D93b — sgs/team-member.socialLinks: flat array → InnerBlocks composition.** (Disambiguated 2026-06-01 doc-sweep: this entry collided with the `sgs/svg-background` D93 above — both were assigned D93 in parallel 2026-05-27/28 sessions; this one renamed → **D93b**. Originally renumbered from prior-session draft D90 → D93 on 2026-05-27 because new this-session decisions D90/D91/D92 took the next available slots; previous D90 captured pre-Fix-1.) Bean directive 2026-05-27 after research-check confirmed WP Core `core/social-link` + Kadence `iconlist` both converge on `{url, icon-discriminator, label}` per-item shape. sgs/team-member's flat `socialLinks` array attribute is replaced by an InnerBlocks slot defaulting to one `sgs/social-icons` child block. Same atomic-blocks-+-composition direction as the sgs/heading γ-rebuild (commit 35fdab62). Eliminates rendering-surface duplication between team-member and social-icons. deprecated.js migrates old `socialLinks: [...]` posts → new InnerBlocks template with sgs/social-icons populated from old data. sgs/social-icons.icons per-item shape extended `{platform, url}` → `{platform, url, label}` — adds explicit aria-label override (closes WCAG 2.2 SC 4.1.2 — Level A). Platform enum preserved (matches WP Core's `service` enum pattern; client UX wins over open-shape extensibility per CLAUDE.md "client experience is primary"). Phase 1.3b ships via /subagent-driven-development (parallel to 1.3a DB work).

---

## 2026-05-27 — Spec 22 Phase 0.1 /qc-council findings + role-exclusion fix + Tier C deletion + pixel-diff measurement methodology

**D85 — Role-exclusion positive-allowlist closes FR-22-2.2 NULL-role hole.** /qc-council Task 2 Rater A's adversarial 5th unit test caught a real bug: `equivalent_block_for('sgs/cta-section', 'textTransform')` returned 'sgs/text' when it should return None. Root cause: `db_lookup.py:839` was `if role and role in _ROLE_EXCLUSION_ALLOWLIST: return None` — short-circuits on falsy role. DB audit confirmed 171 rows have content-bearing `canonical_slot` + `role IS NULL`; 3 confirmed misroutes (`sgs/cta-section.textTransform`, `sgs/hero.textTransform`, `sgs/info-box.textTransform` all canonical_slot='text'). This is the exact FR-22-2.2 "typography looks like heading" trap the spec was designed to prevent — the role-exclusion gate had a hole. Fix: positive-allowlist (`if role not in _CONTENT_BEARING_ROLES: return None`) — content routes via block-equivalence ONLY when role is explicitly in the content-bearing set; role-NULL defaults to scalar (styling-safe). Five unit tests now PASS including the adversarial case. Side-effect: the 4 already-applied Tier B rows (sgs/icon.iconSource/iconName/linkTarget + sgs/timeline.entries) have correct canonical_slot but role=NULL → walker won't activate them until role-detection backfill lands (P-SGS-UPDATE-ROLE-DETECTION-IMPROVE, dispatched Wave A 2026-05-27). The DB state is correct + preparatory; walker activation is deliberate-gated on role enrichment.

**D86 — Tier C deleted from equivalent_block_for(); Spec 22 amended to 2-tier system.** Bean directive 2026-05-27 after /qc-council Task 2 Rater B verdict (DELETE-NOW). Tier C in `db_lookup.py:761-796` + `:859-865` was 40+ LoC of untested code with 0 inputs in current DB state. Risks Rater B flagged: bit-rot, future maintainer "fixing" the dormant warning without understanding why it was dormant, hardcoded frozenset violating R-22-1. Counter-view (preserves spec symmetry) overruled: Tier C will re-add with empirical evidence when P-SGS-UPDATE-ROLE-DETECTION-IMPROVE generates real Tier C inputs. Re-add carries DB-derived role classification + unit tests against real data. Spec 22 §FR-22-2.1 amended from 3-tier to 2-tier (Tier A direct join + Tier B BEM-element). §FR-22-2.3 (Tier C uses existing slot_synonyms) marked obsolete. §10 F-PE-5 + §15 F-AP-2 / F-SC-11 marked RESOLVED via deletion. Wave A Sonnet 2 executes the deletion + spec amendment.

**D87 — pixel-diff.py architectural divergence from Spec 22 §7 Commit 0.3 wording.** Spec wording said "detect chrome at top of SGS screenshot, crop before comparison". /qc-council Task 5 Rater B confirmed the empirical falsification: cropping made the score WORSE (57% > 54.5% pre-fix baseline). The actual mitigation that shipped: `visibility:hidden` on detected `position:sticky;top:0` template-part header BEFORE `el.screenshot()`, NOT post-screenshot crop. Mechanism: sticky chrome re-anchors to viewport-top during `el.scroll_into_view_if_needed()` and paints over the captured element. The cream-coloured chrome band on the saved PNG is `tph_bottom − target_viewport_offset` (66px at 1440) NOT the full chrome height (247px). Post-screenshot crop-by-chrome-height over-cropped by the difference. Empirical result: hero-clone-poc 1440 went 54.5% → 10.3% (−44.2pp); Mama's hero 1440 IMPROVED 69.6% → 60.8% (−8.8pp; honest new baseline since chrome was contaminating prior measurement). Rater B verdict: DIVERGENCE JUSTIFIED — UPDATE SPEC. Spec 22 §7 Commit 0.3 wording amended in this commit. The R-22-7 binding rule (council fix-shapes are hypotheses) was honoured: the spec-literal approach was tried, measured, falsified, then revised.

**D88 — pixel-diff.py brand-375 +2.4pp is REAL methodology shift, NOT flake.** /qc-council Task 5 Rater A correction. Three byte-identical-PNG re-runs (`md5(sgs.png)` matched across `mamas-brand-375-no-wait-fonts`, `mamas-brand-375-rerun`, `mamas-section_sgs-brand-375x812`) confirmed determinism. The +2.4pp delta from 53.225% → 55.619% is a measurement-method-driven change (visibility:hidden of 83px sticky chrome on brand-375 alters layout-rearrangement bleed into the cropped region). Parking entry P-G4-MEASUREMENT-DECONTAMINATION transition wording updated accordingly: "+2.4pp on brand-375 is methodology shift, not flake". Implication: every chrome-affected Mama's cell is partially stale on the 2026-05-26 mean 63.0% baseline. Wave B re-captures the full Mama's baseline with new pixel-diff.py.

---

## 2026-05-27 — Spec 22 Phase 0.1 scope correction

**D84 — Phase 0.1 scope corrected from "backfill 1,214 NULL canonical_slot rows" to "backfill ≤72 Tier B derived_selector candidates."** Empirical DB audit 2026-05-27 (verified by parallel queries against `sgs-framework.db`): of 1,214 rows with `canonical_slot IS NULL`, 1,142 (94%) are triple-NULL (canonical_slot + derived_selector + role all NULL) and are **correctly NULL by design** — behavioural / sizing / styling / enum / identity attrs catalogued in `block_attributes` (the table catalogues every block × every attr; `canonical_slot` is sparsely populated by intent, not gap). 72 rows (6%) have `derived_selector` set and are the actual Tier B backfill candidates. 0 rows have `role` set without `derived_selector` (Tier C dormant — wired for future-proofing per Spec 22 §FR-22-2.1 but has no inputs to act on today). Sample triple-NULL rows confirm the pattern: `back-to-top.position`, `reading-progress.wpm`, `icon.size` — content-NULL by design, not by gap.

Phase 0.1 script guardrail added: `assign-canonical.py` MUST refuse to operate on any row where `derived_selector IS NULL` (structural input filter; not optional). This makes the F-RA-1 "mis-tag behavioural attr as block-equivalent" failure mode **impossible by input shape**, not by regression test. F-RA-1 mitigation downgraded from "golden corpus regression test" to "structural guardrail + dry-run JSON diff + Bean inline review" — risk surface reduced from 1,214 rows to ≤72 reviewable rows (one screen of JSON). Golden corpus (`.claude/specs/22-golden-corpus.json`) and F-RA-9 mitigation both DROPPED — the dry-run diff IS the review surface; a hand-authored corpus would test a category-error premise.

Spec 22 §FR-22-2.1 empirical-state framing rewritten in same commit. §10 F-RA-1 row downgraded. §11 success criteria adds "≤72-row Tier B diff reviewed + applied; 1,142 triple-NULL rows verified unchanged" bullet. §7 Commit 0.1 rewritten. Phase 1 plan Phase 0.1 row rewritten (estimate 3h → 1.5h). Next-session-prompt Task 1 DROPPED, Task 2 rescoped.

Lesson captured at `~/.claude/projects/c--Users-Bean-Projects-small-giants-wp/memory/feedback_grep_verify_handoff_diagnostic_premises.md` continues to apply — the next-session-prompt + Spec 22 empirical-state section both carried a category-error framing (treating sparse-by-design as gap) until DB audit pressure-tested it. Sibling rule: R-22-8 (schema enumeration before "missing X" claim) — when an empirical-state count is load-bearing for a downstream action (script extension, Sonnet brief, council finding mitigation), the count itself is subject to the same gate as the action.

---

## 2026-05-26 — Spec 22 ratification + Spec 16 retirement

**D83 — Spec 22 acceptance gate softened to ≤5% Phase 1 + ≤1% Phase 1.5 stretch.** Bean directive 2026-05-26 after empirical validation surfaced: ZERO sections have ever hit ≤1% across 128 pipeline runs (best-ever absolute: trust-bar 24.6% at 768 viewport). D72 trust-bar retirement delivered −50.4pp / −27pp / −66.9pp relative wins but landed at 33.1/24.6/37.0% absolute — directional proof, not absolute. Hero-clone-poc page 29 (`/hero-clone-poc/`) is the visual-parity proof-of-concept: content matches mockup visually but pixel-diff reports 54.5% due to a 60px chrome-bleed alignment artefact in `scripts/pixel-diff.py`. Phase 1.5 stretch goal addresses the measurement methodology to bridge the residual ~4pp. R-22-13 makes Bean visual sign-off co-authoritative with the script number.

**D82 — Spec 22 walker rewrite is cold replacement, no feature-flag dual-run.** Phase 1 splits into 5 commits per R-22-5 (1.1 pre-rewrite snapshot; 1.2 atomic-tag-map migration; 1.3 ARRAY_LIFT_PATTERNS retirement + array-of-objects resolution; 1.4 universal walker — THE core; 1.5 measurement + halt/proceed decision). Pre-rewrite DB snapshot to `pipeline-state/_snapshots/sgs-framework-pre-spec22.db` enables true rollback per F-RA-2.

**D81 — Hybrid block render.php migration is empirically scoped (Phase 0.4 audit).** Bean directive: spec doesn't hand-curate "container-shaped composite" lists. The classification dissolved; replaced by FR-22-6 `equivalent_block_for() returns non-NULL for ≥1 attr after role-exclusion`. **Audit shipped 2026-05-27 (commit `de300eb2`): actual roster = 61 hybrid blocks across 77 SGS audited.** Earlier "8-15" figure was a guess at high-content-composite count only; the canonical FR-22-6 criterion captures wider truth. Phase 2 prioritises by hybrid_attr_count descending (hero=11 first; single-attr blocks last). Per-block migrations parallel-session-eligible per FR-22-6.1 (no shared-file edits — render-helpers.php additions sequenced through main session).

**D80 — `wp-blocks.py` becomes unified data CLI over sgs-framework.db + selected uimax tables (Spec 22 FR-22-8).** Existing dual-DB connection (line 41) extended with 6 new subcommands (~150 LoC): `equivalent-block`, `recognition-log --write`, `naming-convention`, `gap-candidate --write`, `animation`, `component-library-match`. Performance threshold committed: ≤2ms cache-warm, ≤20ms cold per call. Subprocess for ad-hoc use; library import for walker-critical path. Per Bean correction 2026-05-26: not "greenfield" — extension of existing CLI.

**D79 — Spec 22 (Universal Block-Equivalent Extraction) lands.** Replaces Spec 16's layered architecture with a single universal walker (FR-22-3, exactly 3 permitted exceptions). BEM-only recognition (FR-22-1). Block-equivalent attrs become child blocks (FR-22-2). Role-exclusion rule (FR-22-2.2) shrinks hybrid scope. Array-of-objects resolution (FR-22-2.5). Cross-DB invariants (FR-22-8.1). 4-rater /gap-analysis council 2026-05-26 produced 48 findings: 33 valid + addressed in v0.3/v0.4; 10 partial-recalibrated; 5 dropped as category errors. Council findings table at Spec 22 §15. /docscore Grade A 100%. Status flips draft → active after Commit 0.0 cross-doc sync (this commit).

**D78 — Spec 16 (Deterministic Slot-Aware Converter) retires in full.** Archived at `.claude/specs/archive/16-DETERMINISTIC-CONVERTER-V2-retired-by-spec-22.md` with retirement header. The layered FR1 fast path / FR4 normal route / lift_subtree_into_block_attrs / F1 fallback / 9-branch walk() / ARRAY_LIFT_PATTERNS / hardcoded ATOMIC_TAG_MAP architecture is dissolved. Rules R5 (CSS-drives-emission), FR6 (four-destination CSS router), FR7 (visual-QA verification) migrate into Spec 22. The "double-render" bug measured 2026-05-26 (sgs/product-card emitting 3.7× expected markup post-F1) is structurally eliminated by Spec 22's single-path architecture.

---

## 2026-05-25 — qc-council session → consolidated recovery plan + binding rules

**D75 — `/qc-council` 4-rater council validated the consolidated cloning-pipeline recovery plan (Section R of `.claude/reports/2026-05-25-qc-council-issue-register.md`).** Verdict: CONDITIONAL APPROVE pending F1 spike on brand alone before full Phase 1 dispatch. Raters surfaced: (a) F1 needs smallest-slice baseline measurement before Phase 1 dispatch (R4 — ghost-of-2026-05-21-Wave-1 risk); (b) acceptance gate corrected per Bean to per-section ≤30% × 3 viewports for Phase 1, then ≤1% in Phase 1.5; (c) F2 cascade-fold must ship same-wave as F1 (R4 — ghost-of-2026-05-24 regression); (d) ~17 commits per Phase 1 with /qc-council gate between high-leverage commits (per blub.db row 288 captured this session); (e) 1G G5 per-block fixes made CONDITIONAL not pre-committed (R1 — Spec 16 §15 line 982 says G5 dissolves if walker is universal); (f) 0B allowed-nesting AUTO-DERIVED from block_attributes + slot_synonyms + block_capabilities not hand-curated; (g) 1D `count_stars` replaced via universal COUNT-children-by-slug not "special extractor kind". Gaps R3 raised (state-based CSS, modifier CSS, multi-class selectors, G4) all confirmed COVERED by existing DB schema (modifier_suffixes state-kind rows + property_suffixes hover suffixes + naming_conventions SGS-BEM regex) OR resolved (G4 per Spec 16 §14.4). Plan = `.claude/plans/2026-05-25-phase-1-universal-extraction.md` (Section R promoted to canonical phase plan).

**D74 — Phase 1 scope = consolidated qc-council recovery plan (universal-extraction backbone).** Phase 1 ≠ a single step; it's the full architectural solution to every gap the qc-council surfaced (F1 universal-nesting + extended DB-driven ATOMIC_TAG_MAP + universal child-block extraction + universal array-attr extraction + G3 visual slot extraction + G1 OPEN-block for FR1 composites + conditional G5 per-block + sgs/quote render.php β-migration + patterns.block_composition population + pattern fast-path + cheat removal). Phase 2 (header/footer cloner) is parked because it depends on a working body pipeline. Acceptance: per-section ≤30% × 3 viewports for all 7 body sections. Subsequent Phase 1.5 closes per-section to ≤1%, after which Phase 2 opens. Old phase-1 plan archived at `.claude/plans/archive/2026-05-24-phase-1-structural-recovery-superseded-by-phase-1-universal-extraction.md`.

**D73 — Phases never ship as single commits (binding rule).** Captured as blub.db row 288 / `feedback_phases_never_ship_as_single_commits.md`. Every major task in any Phase commits separately with: (a) `/qc-council` or `/qc-inline` pre-commit gate; (b) living-docs updates per trigger table; (c) `/sgs-clone --debug-trace` + Stage 11 measurement comparing pre/post; (d) commit message citing predicted vs actual delta. Per-task skill bindings: `/subagent-driven-development` + `/delegate` + `/verify-loop` + `/capture-lesson`. Sibling rules: blub.db row 255 (multi-model /qc per converter commit) + row 276 (council fix-shapes are hypotheses) + P20 (one fix at a time). Anti-pattern of record: 2026-05-24 second-pass session (5 changes shipped as one wave, regressed pixel-diff 70.5% → 73.9%, regression unattributable).

---

## 2026-05-25 — sgs/trust-bar block retired; universal-nesting takes over

**D72 — sgs/trust-bar block retired in favour of universal-nesting via canonical primitives.** Block source files deleted (`src/blocks/trust-bar/`, `build/blocks/trust-bar/`). DB rows deleted across 9 tables (69 rows in sgs-framework.db, 1 in uimax.component_libraries). Hardcoded special-case removed from `confidence-matrix.py` COMPOSITE_PRIORITY, `seed-legacy-role-lookup.py` ENTRIES (2 rows), `lingua_franca.py` slot_map, `generate-markup-examples.py` composites set, `BlockDeprecationsTest.php` AFFECTED_BLOCKS, `blocks.spec.ts` block list + markup test fixture, `test_ensure_root_section_class.py` self-closing test (switched to sgs/notice-banner). Block was a duplicate concept: counter use-cases now use sgs/counter (canonical single-counter block); badge use-cases now emit via universal-nesting (sgs/container > sgs/label children, with the converter resolving `__badge` BEM elements to sgs/label via `slot_synonyms.standalone_block`). Mama's homepage Stage 11 results post-retirement (run `mamas-munches-homepage-2026-05-25-101222`): trust-bar pixel-diff 375 87.4% → 37.0% (-50.4pp); 768 51.6% → 24.6% (-27.0pp); 1440 100.0% → 33.1% (-66.9pp). Mean page diff 68.4% → 63.2%. Remaining trust-bar gap is styling-shape (sgs/label badge render vs mockup span+svg shape) — block-level extensions territory, not block-level special-cases. critical-fix-verification flagged `no_canonical_block_mutation` as false-positive from uncommitted source deletions; cleared at commit.

---

## 2026-05-25 — Step 1.7 G3 reframing (architectural correction after D70)

**D71 — Step 1.7 G3 reframed: pixel-diff side closed by D70; failure-count side empirically misframed; universal-nesting (§15) is the canonical closure path.** Multiple earlier prescriptions in Spec 16 §14.3, §15 row 977, Phase 1 plan Step 1.7, and `.claude/next-session-prompt.md` (all from same-day patches earlier on 2026-05-25) pointed Step 1.7 at file paths that didn't exist (`slot_list.py`) then at `convert.py:_slot_attr_prefix` extension. A Sonnet implementer ran the prefix fix end-to-end and demonstrated empirically it addresses only 7 of the 473 `extraction_failed` events — 440 of which are legitimate `value_empty` for features genuinely absent from mockups (hover states, bgVideo, transitionDuration). The original `<30` failure-count gate is unachievable without semantically falsifying absent features. The operational symptom G3 was meant to close (pixel-diff regression on body sections) was closed independently by D70 (Stage 10 inline-CSS deploy). All four docs re-framed in this commit. Lesson captured at `feedback_read_full_spec_before_proposing_architectural_fix_shape.md` (blub.db row `capture-f9063d258798`). Sub-rule: when patching specs to correct file-path drift, fix paths ONLY — do not bundle unverified fix-shape prescriptions into the same patch (that converts drift correction into drift introduction). Step 1.8 (universal-nesting / G1 extension to non-hero composites) absorbs the residual failure-count work.

---

## 2026-05-25 — Stage 10 inline-CSS deploy (closes 4-section pixel-diff regression)

**D70 — Stage 10 (`upload_and_patch.py`) now deploys `variation-d0-d2.css` as an inline `<style>` block prepended to the page's `post_content` (wrapped in `wp:html` so Gutenberg preserves it across edits).** Closes the architectural gap where the 4-destination CSS router (Spec 16 §FR6) wrote correctly-scoped D0/D2 rules to `pipeline-state/<run>/variation-d0-d2.css` but Phase 5a's snapshot-only deploy never moved them off-disk. Result on Mama's homepage: Stage 11 mean 74.1% → 68.4% (-5.7pp), with localised body-section drops of -15 to -41pp (gift-section -39pp at desktop, social-proof -40pp at desktop, ingredients -22pp at mobile). Header at 375px regressed +59pp due to pre-existing duplicate-header issue (mockup header now visible alongside framework header part) — separately tracked at parking entry `P-DUPLICATE-HEADER-EXPOSED-BY-INLINE-CSS-FIX`, resolves at Phase 2 (header+footer cloner). Rules already scoped via `.page-id-N` prefix in router output → no cross-page leak. Spec 16 §FR6 D2 description updated. Pipeline-stages doc updated. ~20-line addition at `upload_and_patch.py` after block_markup image-URL patching, before extract.patched.json write.

---

## 2026-05-25 — Doc-op programme: adoption surface + hook + council remediation

**D66 — PostToolUse hook `docscore-on-doc-edit.py` shipped.** Auto-runs `docscore.py` on every Write/Edit to in-scope `.claude/`/`specs/`/`plans/` markdown. Silent on pass; advisory to stderr when score < 90% Grade A-. Registered in `~/.claude/settings.json` under the existing `Write|Edit` PostToolUse matcher. Out-of-scope dirs (memory/, scratch/, reports/, .git/) skipped at the hook level for parity with docscore's own scope rule. Means future doc edits get scored automatically without operator-remembered docscore invocation.

**D67 — Adoption-surface updates across global + project + working-area.** Phase 13 standards now propagated to every surface that drives behaviour:
- `~/.claude/CLAUDE.md` (global) — added `## Doc-op standards (2026-05-24)` 12-line section listing all 17 canonical doc-types + 6 rules
- `~/.claude/commands/handoff.md` — added `Gate 4.6 — Docscore on changed in-scope docs` between Gate 4.5 and Gate 4.8 + parking.md shape check in Gate 4.5
- `~/.claude/skills/autopilot/SKILL.md` — added `docs/doc-op/docscore/template/frontmatter` row to Domain Classification + strategic/phase-plan preload rule in Stage 0
- `<project-root>/CLAUDE.md` — added `## Doc-op standards (Phase 13 close, 2026-05-25)` 11-line section pointing at D57-D65 + 4 per-session rules
- `~/.agents/skills/subagent-prompt/SKILL.md` — fixed stale "11 doc types" → "17 doc types"
- `.claude/CLAUDE.md` (working area) — updated D57-D60 reference to D57-D65

**D68 — /qc-council triangulated 3 high-confidence template fixes (applied):**
- T1 (over-engineering): 4-layer Verification was mandatory on every step. Now conditional — `Happy:` + `Outcome:` always; `Edge:` + `Fail:` + `Integration:` only on `Marker: QA` steps OR steps where `Files:` touches live infra (SSH/DB/REST/deploy). "N/A" acceptable elsewhere. Drops phase-plan writing overhead ~40% on simple plans.
- T2 (functional bug): templates declared `required_sections: []` empty while prose said certain sections were mandatory. Mandatory sections now in YAML: strategic-plan `[Out of scope, Phase overview]`, phase-plan `[Pre-conditions, Parking lot]`. Docscore checks now machine-enforce.
- H1 (ADHD Rule 3 alignment): strategic-plan frontmatter gains `motivation:` field — re-entry after 7+ days surfaces "why this matters".

5 active plan docs updated with the new mandatory sections (Out of scope + Phase overview on strategic; Pre-conditions + Parking lot on each phase) — all 5 still 100% Grade A.

## 2026-05-25 — Doc-op programme: strategic-plan + phase-plan templates (merged from skill spec + research)

**D65 — `strategic-plan` and `phase-plan` doc-type templates created** in `~/.agents/skills/shared-references/doc-templates/`. Built by merging OUR skills' learned output specs (`strategic-plan` plan-template + `phase-planner` 14-field step block) with research-distilled rules (PMI / SAFe / Shape Up / OKR / Stage-gate / DevOps runbook / Ansible / Claude Code best practices).

**Strategic-plan template** keeps OUR sophistication (Work Units with Files/Depends/Blocks/Estimate/Risk/checkbox Steps/VERIFY, GATE blocks with Pass/Fail/Decision, Effort Summary + Session Plan tables, Hidden Decisions pass, per-phase handoffs) AND adds RESEARCH anti-pattern fixes (mandatory `timebox` frontmatter field per Shape Up, mandatory `## Out of scope` section per Shape Up + Stage-gate, ROAM risk-status enum per SAFe, dependency owner column per SAFe, phase overview table ≤13 rows per PMI, problem statement ≤200 words).

**Phase-plan template** keeps OUR sophistication (Plan-Level Label `[PLAN: opus|sonnet|blub]`, USP / Aggregate cost in phase header, 4-layer Verification block — Happy/Edge/Fail/Integration, Marker enum — SESSION-START/HANDOFF/QA, Cold-Entry + Prompt fields for subagent dispatch, Tooling Index table) AND adds RESEARCH additions (mandatory `## Pre-conditions` checklist BEFORE start, per-step `Pre-condition:` + `Rollback:` fields completing the PAVR shape, mandatory `## Parking lot` section even if empty, no-shared-file-parallel HARD GATE via Files-field cross-check, cold-prompt-readiness rule for parallel-dispatched steps).

**Verification:** 5/5 project plan docs score 100% Grade A against the new templates. Total 15/15 in-scope docs now passing at 100% A (CLAUDE / parking / mistakes / decisions / dev-setup / cloning-pipeline-stages / spec 17 / spec 21 / 2 archived plans / 5 active plans).

Full research: `~/.openclaw/workspace/memory/research/2026-05-24-strategic-plan-and-phase-plan-best-practice.md` (11 cited sources). Skill output specs (`strategic-plan/references/plan-template.md`, `phase-planner/SKILL.md` Step Format) remain as-is for now — next session can update them to emit the new mandatory sections automatically.

## 2026-05-24 — Doc-op programme: docscore.py technical-debt close-out

**D64 — 10 SonarLint warnings on docscore.py closed (cognitive complexity + dead-code).** Per Bean's "nothing parked" rule: refactor extracted 11 helper functions (`_coerce_fm_scalar`, `_find_fm_end`, `_detect_doc_type_from_path`, `_warn_doc_type_constraint`, `_score_folder_json`, `_score_folder_human_subprojects`, `_score_folder_human_flat`, `_resolve_walk_root`, `_build_doc_context`, `_registry_missing_paths`, `_group_by_subproject`, `_score_paths_human`). `parse_frontmatter` cc 25→≤15; `detect_doc_type` cc 16→≤15; `score_folder` cc 49→≤15. Removed 5 Unicode-box-drawing section dividers SonarLint flagged as commented-out code. Removed unused `current_key` variable. Behaviour preservation gate verified: 10/10 in-scope docs still 100% Grade A; adversarial back-door test still rejects. Lives in `~/.agents/skills/shared-references/docscore.py` (1168 → 1185 lines = +17 net for the helpers).

## 2026-05-24 — Doc-op programme: doc-type back-door close + skill alignment

**D62 — doc_type self-declaration back-door closed in `docscore.py`.** Per qc-council Rater B finding (2026-05-24): any file could declare `doc_type: spec` in frontmatter and bypass spec-only checks. Fix: spec/archived-plan/dev-setup templates gain `filename_glob` + `required_dir` + `required_grandparent_dir` fields. `detect_doc_type` validates these constraints before honouring a declared doc_type; mismatches emit stderr warning + fall through. `reference` catch-all has no constraints (any doc can claim it). Adversarial tests pass: `doc_type: spec` on non-numbered or wrong-dir files rejected. Off-tree commit in `~/.agents/skills/shared-references/docscore.py` + 3 template files.

**D63 — Doc-touching skills aligned to Phase 13 standards.** Audit (Sonnet subagent 2026-05-24) of every doc-producing skill/command found 4 minor + 3 major drifts from the new templates. All 7 fixed in `~/.claude/skills/` + `~/.agents/skills/`:
- `docscore` command: doc-type list updated 11 → 15 (added archived-plan, dev-setup, reference, spec)
- `spec-writer` output-templates: status enum 6 → 8 values (added superseded, retired)
- `spec-writer` CLAUDE.md template: rewritten to reference Karpathy R1-R7 rules (≤80 lines, ≤5 IMPORTANT markers, verb-first, no inferable knowledge, hooks over repeated rules)
- `phase-planner`: `doc_type: plan` → `doc_type: archived-plan` for completed phases
- `project-init`: Phase 0 scaffold expanded 13 → 14 items (added `dev-setup.md`)
- `strategic-plan` plan-template: added YAML frontmatter stub with status enum
- `project-consolidate`: scaffold count 13 → 14; added Phase 13 doc-type addendum (archived-plan / dev-setup / reference + scope rule excluding memory/scratch/reports)

`capture-lesson` + `handoff` command + `gap-analysis` + `qc-council` already aligned (shipped earlier this session). 8 doc-touching skills/commands verified in-sync.

## 2026-05-24 — Doc-op programme: Phase 13 full /docscore rule integration

**D60 — Phase 13 expanded: U5 + X1 + X5 checks added to docscore.py + 5 doc-type templates rewritten to Phase 6c canonical shape.** Built on D59 by recovering U1-U8 / X1-X5 rule spec from `2774a269` (dropped by `3488b537`'s prompt update). Lives in `~/.agents/skills/shared-references/`. Template gains `meta: dict` field; `check_heading_hierarchy` honours per-template `require_h1: false`; 3 new checks: `check_no_dead_links` (U5), `check_registry_resolves` (X1), `check_memory_md_size` (X5). Verified: parking=100% A, next-session-prompt=100% A, decisions=79% B-, handoff=68% C, mistakes=69% C. Commit `3488b537` + this session.
Status: active

---

## 2026-05-24 — Doc-op programme: parking restructure + spec relocation + retention policy

**D57 — Parking.md formatting v2 (Phase 6c).** 6 stable taxonomy buckets replace date-ordered sections; every entry carries `**Status:**` field (OPEN/PARTIAL/BLOCKED/DEFERRED); slug-uniqueness gate added to `/handoff`. 103 active entries (was 135). Commits `52e34171` + `ed05757f` + session close.
Status: active

**D58 — Spec relocation (Phase 9C').** `.claude/specs/` = project-scoped framework specs only; cross-cutting specs → `~/.claude/specs/`. Comparator reports → `reports/`. Strategy/staging docs → `plans/strategy/` or `plans/archive/`. 33 → 19 specs. Commits `ed05757f` + `e8958433`.
Status: active

**D59 — Per-doc-type retention TTL on `.claude/memory/` (Phase 7 F4).** `next-session-prompt-*.md` → 30-day TTL; `handoff-*.md` → 60-day TTL; everything else permanent. Encoded in `docs-registry.yaml`. Commit `c23d8948`.
Status: active

---

## 2026-05-24 — BEM-is-canonical walker + Stage 4 wiring

**D48 — BEM element name IS canonical signal for block recognition; HTML tag is rendering shape only.** Tag-based routing (`canonical_for_html_tag`) reverted — created second canonical path conflicting with BEM-as-canonical. Correct fix: data-layer (move "quote" alias in slot_synonyms); zero walker code changes. Commit `124e1d06` area.
Status: active

**D49 — Walker code stays universal; data-layer drives recognition.** Zero per-tag/per-block/per-section hardcoding in walker composite_element + section_inner_absorb branches. All recognition from slot_synonyms.aliases + standalone_block + block_attributes. Adding new block recognition = DB rows, not walker edits. Commit this session.
Status: active

**D54 — ARRAY_LIFT_PATTERNS hardcoded dict deletion DEFERRED.** Universal BEM-child array lifter (1e-B) now resolves via canonical_slot but doesn't yet replicate: (a) `count_stars` extractor for ratings, (b) multi-selector fallback chains. Full removal parked as P-ARRAY-LIFT-PATTERNS-FULL-MIGRATION. (no commit — planning-only)
Status: active

---

## 2026-05-24 — Step 1.6 wp-sgs-developer audit

**D47 — Structural improvement + visual regression coexist when CSS lift is pending.** A structurally correct emit can INCREASE pixel-diff relative to a structurally wrong emit if per-block CSS hasn't been lifted yet. This is NOT a reason to revert structural fixes — it is a reason to sequence CSS lift as step+1 in the same session. Never commit structural fix without CSS lift following immediately. Commit `124e1d06`.
Status: active

---

## Archived 2026-06-06 (doc-council HIGH#6 safe-shrink — verifiably-superseded entries moved from decisions.md)

_D114/D115/D116 superseded by D118 (FR-22-4.1), D117 (content-routing shipped), and Spec 27 (D161, absorbed Spec 24) respectively. D133 self-flagged superseded by D134/D135 (variant detection shipped). Verbatim below._

**D114 — XS-3 reconciled: Spec 23 branch merged to main (eeac99a1).** The "two competing XS-3" framing was wrong — `git merge-base main eced119b == 0a212e3c`, so the branch was a strict SUPERSET of main's XS-3, not a rival. Clean head-to-head on page 144 (same baseline, OPcache reset between): main XS-3 alone 58.39% vs branch (XS-3+B2/B3+A1) 58.98% (+0.59pp). The featured-product 768 "+19.7pp" was a vertical-anchor crop artefact between two equally-EMPTY renders. Bean approved merge to keep Spec 23 momentum; marginal cost is hybrid-content noise, not layout. (Supersedes the never-written "D114" referenced in eced119b's commit message.)

**D115 — FR-22-6 hybrid problem is FRACTAL; root cause = walker never runs FR-22-2.** featured-product + social-proof render EMPTY on the live page (verified: live DOM textLen=0, NOT the pixel-diff which showed a FALSE −30.9pp win — empty section = shorter crop = more matching background; see memory `empty-section-false-pixel-diff-win`). Container blocks (product-card/testimonial/testimonial-slider) were migrated to echo $content (WIP commit c9c6544d on `feat/fr22-6-content-render`) — necessary but NOT sufficient. Investigation (opus subagent, read-only) found the real defect: the walker (`convert.py`) resolves nodes + recurses but **never calls `equivalent_block_for()`/`array_item_slot_for()`/`lift_behavioural_attrs()`** (the FR-22-2 content-routing layer). Three symptoms: (1) `sgs-product-card__body` mis-resolves to the `sgs/text` LEAF because the `slots` table lists `body/intro/description` as `text`-slot aliases — the walker then nests heading/price/pills inside the leaf, which ignores its children → swallowed; (2) leaf text emitted as inner markup, not into the scalar `text` attr → `sgs/text`/`sgs/label` render empty; (3) array items (packSizes) never emitted. **Fix is CONVERTER-side (1-2 files, G1-G4), NOT a 61-block migration** — leaf blocks STAY leaves (R-22-14). **XS-3 must be EXTENDED, not closed**: its guard only fires on slug=None, so it never catches a wrapper that mis-resolves to a leaf (this is why "recent work should have fixed container routing" didn't — the blind spot is leaf-misresolution). Pills must route to `sgs/button` (interactive), NOT `sgs/label` (eyebrow). Phase plan: `plans/2026-05-31-converter-content-routing-fix.md`. Deferred to a focused next session per STOP #19 (sensitive sequential converter work, not iterated inline at session tail).

**D116 — Spec 24 (Query-Driven Content Cards) drafted + product CPT Phase A foundation built.** Research (research-buddies + library-docs, blub.db): presentational-vs-query is a FALSE fork — build ONE dual-mode card (typed OR CPT-bound) on a custom CPT + core Query Loop + Block Bindings API. **Custom CPT, NOT WooCommerce** (maintenance tax unjustified for marketing sites), NOT both. Same engine generalises to testimonials/team/case-studies = highest-leverage framework primitive. `sgs_product` CPT + 2-product seed built (committed; not yet deployed). Spec `specs/24-QUERY-DRIVEN-CONTENT-CARDS.md`. Card dual-mode binding gated on the converter fix (FR-24-9).

**D133 — Universal variant detection (Spec 22 §FR-22-20) specced; the hero `$is_split` fix CONCEDED as a cheat, flagged for reversal (Bean-directed).** Bean rejected the 2026-06-01 hero `$is_split = ('split'===$variant) || !empty($split_image)` fix as a per-block cheat: not universal, and mis-fires on stale data (a block carrying both standard + split attrs from prior edits would be mis-detected). The proper fix chooses the variant from **what the draft pulled THIS run** (the extract), not the block's stored attrs, universally for all 33 variant-bearing blocks (hero/product-card/gallery/mobile-nav/…). Mechanism (§FR-22-20): blocks declare `supports.sgs.variants` (variant → its attrs/slots) in block.json + a new `blocks.variant_attr` column; `/sgs-update` populates a new `variant_slots` table (each variant's DISCRIMINATING slots via set-difference, excluding shared attrs); the converter counts which variant's `variant_slots` were extracted from the draft, picks the highest count (insurance tie-break), sets the variant attr. render.php's ORIGINAL variant gate then works → the `$is_split` band-aid is REVERTED. DB-driven (R-22-1), universal (R-22-9), draft-data-driven (closes the stale-data hole), drafts stay variant-unaware (free-form, not limited to current design capacity). Scaffolding partly exists (33 variant-selector enum attrs + the `variations` table, 206 rows); the new piece is the discriminating-slot map. **BUILD = next session's opening task** (DB schema → block.json declaration → /sgs-update population → converter detector → revert $is_split → re-clone+verify → generalise to the other 32 variant blocks). **→ SHIPPED for hero (Commits 1–5 of 6) + LIVE-DOM verified — see D134/D135. Commit 6 (modifier-class→enum path) redesign-pending per D135.**


## Archived 2026-07-28 — decisions.md fat-cut sweep (D134–D348 routine/superseded entries)

_58 entries (D134–D348 range) moved verbatim from decisions.md. Each was a routine progress record (shipped/verified/superseded build narrative) with no standing constraint still binding a future session — confirmed by cross-checking every D-number against CLAUDE.md (both files), LEDGER.md, STOP-CATALOGUE.md, goals.md, parking.md, and specs/*.md for a live citation; none were cited. Original order preserved (as they appeared in decisions.md, top to bottom)._

**D332 — Bean-directed pre-sign-off polish (4 sequenced items). Plugin 0.1.6→0.1.7. Two of the three build items turned out already-built (verified live, not rebuilt).**

- **(1) `sgs/button` responsive `labelCollapse` (built via `/subagent-driven-development`).** A per-instance "Collapse label to icon" setting, **default `none` (OFF)** — hides the button's text and collapses to just the icon from a chosen breakpoint down (`mobile`/`tablet`/`all`), mirroring the business-info feature 1:1. The bare label is now wrapped in `.sgs-button__label`; scoped `@media` clip on `.{$uid}.sgs-button .sgs-button__label` (a11y-preserving clip, no aria change). **Implementer = Sonnet subagent; code-review = Opus subagent → APPROVED (spec + Bean's no-icon rule + quality, 0 critical/important).** **Bean rule (both blocks): the collapse is a NO-OP when there is no icon/emoji** — else it leaves an empty button. Gated on `$icon_html` (button) / `$show_icon` (business-info). Server-probe-verified all 4 cases: icon+all→clip, icon+mobile→@media clip, **no-icon+all→NO clip**, icon+none→no clip. Paint mechanism live-proven identical on business-info. block.json version 1.5.1→1.5.2.
- **(2) Per-row background + outline on `sgs/site-header-row` + `sgs/site-footer-row` — ALREADY BUILT, live-verified.** Both blocks already declare `supports.color` (background/text/gradients) + full `supports.__experimentalBorder` (radius/width/color/style), skip-serialised, and self-emit them into scoped `<style>` via `wp_style_engine_get_styles` (composite-mirror — the shared wrapper deliberately does NOT emit root bg/border; each block does, exactly like `sgs/container`). WP renders the native Color + Border panels automatically (client-editable, no bespoke UI needed). **LIVE-VERIFIED:** a header row with bg+border set paints `background-color:rgb(255,244,230)` + `border:2px solid rgb(15,126,128)` (computed, on the real page). **This RESOLVES the D328 "wrapper has no border emit → footer divider never renders" note** — the rows self-emit, so their borders DO render; the D328 gap was about the wrapper root / a different element, not these row blocks.
- **(3) Empty top/bottom rows render nothing — ALREADY BUILT, live-verified.** Both row render.php open with an unconditional `if ( '' === trim( (string) $content ) ) { return ''; }` (before the wrapper/style emit), covering every rowSlot. **LIVE-VERIFIED:** the footer's empty `top` row is ABSENT from the live DOM (only `columns` + `bottom`, which have content, render).
- **(4) Full-scale `/adversarial-council` on the WHOLE §S9 system — the pre-sign-off gate (runs after 1-3, this session).**
- **Method note:** used `/subagent-driven-development` for (1) per Bean (implementer + cross-model reviewer); adapted for the SGS reality (worked on `main` path-scoped, not worktrees, because the deploy script + Playwright live-verify need the real repo/canary — I integrate + live-verify myself, the live check being the real gate). (2)+(3) were scoping-investigated by 3 parallel Explore agents which found them already-built → verification, not build.

## 2026-07-14 — D331: FR-S9-8 BUILT + LIVE (per-device content adaptation) → Spec 17 §S9 now 11/11 (pending adversarial-council + Bean sign-off)

**D331 — the LAST §S9 build. All four FR-S9-8 acceptance bullets live-verified on sandybrown (full cache-clear incl. Hostinger CDN). D-ceiling D330→D331. Plugin 0.1.3→0.1.4, theme 1.5.17→1.5.19.** Delivered by EXTENDING existing infra (directive: extend, don't rebuild). Three Bean decisions this session: nav iconOnly → descoped to contact + Call-button; Indus verification → sandybrown + by-construction; visibility breakpoints → align to 767/1023 now.

- **(a) Per-tier visibility — routed through the canonical breakpoint source (R-31-1).** The universal `device-visibility.php` already gave every block `sgsHideOnMobile/Tablet/Desktop`, but at hardcoded 600/1024. Bean chose to align now: removed the 3 static rules from `extensions.css`, GENERATE them from `SGS_Breakpoints::MOBILE_MAX/TABLET_MAX` (767/1023) as inline CSS on the extensions handles → one breakpoint source shared with the FR-S9-6 engine, no second hardcoded pair. Editor help text updated (both `responsive-visibility.js` + `DeviceVisibilityPanel.js`).
- **UNIVERSAL BUG FIXED (root cause proven on the live DOM + a server HTTP probe, not inferred).** The `render_block` visibility filter's `next_tag()` grabbed the FIRST tag — for any no-inline block (which prepends a scoped `<style>`, e.g. business-info `printf('%s<div…', $scoped_style_html, …)`) that's the `<style>`; the class landed there, then the D311 CSS collector (priority 99) lifted the `<style>` (with the class) to `<head>`, so the hide class VANISHED and the wrapper never hid. Fix: skip leading `<style>`/`<script>`, target the first visible wrapper. **Affects every no-inline block** — a real latent bug the no-inline migration (D293+) introduced silently. Proven: hide classes now land on the wrapper divs live (STOP-static-vs-live — caught only on the live DOM; the direct filter call passed in isolation).
- **(b) Responsive icon collapse on `sgs/business-info` (`labelCollapse` enum).** Bean refined this mid-session: instead of duplicate elements + visibility-toggle, a SINGLE per-instance setting that collapses the item to just its icon from a chosen breakpoint down. `labelCollapse` = `none|mobile|tablet|all` (default `none`); the label is always emitted in `.sgs-business-info__label` and clipped via scoped `@media` at the chosen tier (mobile ≤767 / tablet ≤1023 / all), staying in the a11y tree so an icon-only phone/email link keeps its accessible name (no aria-label needed). One SelectControl (control-ux gate: a single enum is NOT a responsive family, so no device-switcher required — the initial `showLabel`+`showLabelTablet`+`showLabelMobile` triple TRIPPED the RESPONSIVE-FAMILY-WITHOUT-SWITCHER gate, which is why it became one enum). **Nav iconOnly DESCOPED (Bean):** adaptive-nav links have no per-item icons; the Indus pattern collapses nav to the drawer + makes the primary contact a button. **`sgs/button` gets the same `labelCollapse` as a fast-follow (DEFERRED — sensitive shared block, not used in this header; confirm with Bean).**
- **Bean row-fix (Point B, incorporated + re-verified live):** the "Call" business-info was in the middle row and forced a wrap (logo alone on row 1). Moved it to the BOTTOM row → **logo + cart + burger sit together on the top row**, Call on the bottom row (live top offsets: logo 16 / burger 27 / cart 27 same row; Call 83 below; no overflow).
- **Bean architecture-fix (contact button IS a business-info variant, not a theme hack — same day):** Bean noted the "button that shows the number/email" is a display style of `sgs/business-info`, not a separate `sgs/button` and not an ad-hoc theme class. Replaced the `.sgs-header-call` theme-CSS hack (in `utilities.css`, not client-editable) with a first-class **"Button" block style** on business-info (`block.json` `styles: [text(default), button]`, styled block-private in the block's OWN `style.css` with theme tokens — `is-style-button`). Now any operator can render ANY contact field (phone/email/address) as a button from the Styles panel, and it composes with `labelCollapse` (a contact button can collapse to an icon-only button per tier — Bean's original Point A). This is the correct home; generic CTAs stay `sgs/button`. The deferred `sgs/button` labelCollapse is therefore a separate general-nicety, not needed for contact. Live-verified: `is-style-button` renders identically (primary bg, 44px, `tel:`), `.sgs-header-call` gone, 0 console errors. Plugin 0.1.3→0.1.6, theme 1.5.17→1.5.21 across the D331 redeploys.
- **(c) Move-to-drawer.** `sgs/mobile-nav` `ALLOWED_BLOCKS` widened to accept `sgs/business-info` → an operator places a contact/social copy in drawer zone 6 (renders only there, `<body>`-portaled) + hides the header copy per-tier. Place-then-toggle, no magic primitive. Live: email (`mailto:`) + socials (instagram) inside the open drawer, absent from the collapsed header.
- **(d) Indus slim-bar reproduction.** Authored in `parts/header.html` + `framework-header-default.php`: desktop = icon-only phone/email/social utility strip (hidden ≤1024); **≤1024** = logo + a prominent "Call" button (a `sgs/business-info` phone styled via `.sgs-header-call` theme-token CSS in `utilities.css`, reads Site Info → working `tel:01214960123`, 44px, primary bg) + email/social moved to the drawer; **footer 3→1 at 768** (already D325). **Binding pivot:** the `sgs/site-info` binding on a `sgs/button` `url` did NOT resolve on WP 7.0 (block bindings unproven on custom-block attrs — live href was null) → pivoted to the business-info-as-button approach (proven, R-31-1-clean, no hardcoded phone).
- **Live-verified (sandybrown, all tiers 320–1440):** desktop icon-strip + nav + cart, no Call button; ≤1024 logo + Call button (tel:, 44px) + nav collapsed + top contact hidden; drawer email/social present + absent from header; footer 3-col ≥768 → 1-col <768; hide-mobile `≤767` / hide-tablet `768–1023` CSS confirmed; sr-label clipped ≤2px; 0 console errors; NO header/footer overflow (the 1024 page overflow is the accepted testimonial-slider carousel, not the header — `headerOverflow:false`).
- **2nd-client (honest):** verified live on sandybrown only; universality by-construction (client-agnostic blocks + per-site Site Info store/tokens, grep-clean) — no live indus deployment exists (same caveat as FR-S9-10). Reports: `reports/visual-diff/business-info-2026-07-14.md` + `mobile-nav-2026-07-14.md`. `/sgs-update --stage 1` registered the `showLabel` attr + the widened allowed_blocks.
- **§S9 status: 11/11 BUILT + live.** Next per Bean's directive: a thorough `/adversarial-council` on the WHOLE §S9 as built → Bean's "totally covered" sign-off (HARD gate) → Spec 33 Part 2.

## 2026-07-14 — D330: FR-S9-9 SHIPPED (3 header behaviours as no-code block controls) + theme-side header-mode system RETIRED (Task 2b) + FR-S9-10 live-confirmed → §S9 now 10/11

**D329 — Bean-locked (2026-07-14), planning-only (no code this session).** Two linked directions surfaced when Bean asked "did you build a header/footer builder into the Site Editor like we discussed":

- **Site Editor is the single home (best practice for a block theme) — NOT the Customiser.** The LIVE header/footer already render from **file-based template parts** (`theme/sgs-theme/parts/{header,footer}.html`) containing the new `sgs/site-header`/`sgs/site-footer`/row/`adaptive-nav` blocks, edited natively in the Site Editor (Appearance → Editor → Template Parts) with the FR-S9-6 per-device controls (D327/D328). **For a block theme the Site Editor IS the builder — there is no separate builder UI to construct.** BUT an earlier-wave **Customiser path is still ACTIVE and contradicts this:** `Sgs_Header_Customiser` + `Sgs_Footer_Customiser` (both hooked on `customize_register`) + `Sgs_Header_Renderer` (reads the Customiser options) hold header bg/text/link colour, max-width, an "Enable sticky header" toggle, and conditional rules — **duplicating the block controls**. Verified the live site renders via the template-part/block path (all D328 live measurements came from `parts/*.html`), so the Customiser path is most likely stale/superseded but was never retired. **Direction: audit the Customiser `Sgs_Header_Renderer` path (confirm dormant, no double-render/conflict) → RETIRE the Customiser header/footer sections → one home = the Site Editor.**
- **FR-S9-9 scope = 3 header behaviours as SITE-EDITOR block-inspector controls (Bean, 2026-07-14):** **sticky**, **transparent-at-rest→solid-on-scroll**, and **shrink-on-scroll-down** (Bean added shrink). The mechanics ALL EXIST in the behaviour layer + `header-modes.css` (`class-sgs-header-behaviours.php` VALID_BEHAVIOURS `transparent`/`sticky`/`hide-on-scroll-down` + `is-header-scrolled`/`is-header-scrolling-down`; theme parts `header-{shrink,sticky,transparent}.html`; header-modes.css handles sticky/transparent→solid/shrink/smart-reveal). The GAP: selection is via template-part VARIANTS + the Customiser toggle today, NOT a no-code inspector control on `sgs/site-header`. FR-S9-9 build = expose the 3 behaviours as block-inspector toggles driving the existing behaviour-layer classes (add `shrink` to the recognised set; route state via CSS custom-property token, Spec 32 no-inline) — replacing BOTH the template-part-variant selection AND the Customiser "Enable sticky header". This is the concrete replacement that lets the Customiser header path retire.
- **Also corrected this session (Bean):** FR-S9-10 is DONE by capability (business-info in both header+footer rows reads the one Site Info store; the 3 blocks are literal-free) — a CONFIRM, not a build. So §S9 open work = FR-S9-8 (build) + FR-S9-9 (build, 3 behaviours) + the Customiser→Site-Editor consolidation + FR-S9-10 confirm.

## 2026-07-14 — D328: FR-S9-6 CLOSED — live instances migrated flat→object (fidelity bug fixed) + box/width/link-font-size on the object model across all 3 blocks

**D324 — P1 of the header/footer/nav system SHIPPED and live-verified (sub-400px WCAG overflow emergency FIXED); a stray WooCommerce mini-cart/account in the header was root-caused to the WP Block Hooks API and fixed by architecture, not a workaround filter.** Branch `main` (D-ceiling D323→D324). Commits `a1433f82` (v1) + `c575a41d` (v2), pushed.

- **Built two new blocks:** `sgs/site-header` (section KIND) + `sgs/site-header-row` (layout KIND, the never-overflow cluster row). Both delegate to `SGS_Container_Wrapper` per the composite-mirror rule (R-31-9/Spec 31 §13.6). 3 optional named rows, empty-row zero-output. Live-verified on the sandybrown canary at 320/375/414/768/1440: `scrollWidth<=innerWidth`, 0 overflow, no inline `style=""`, toggle+cart 44×44 touch targets.
- **The sub-400px WCAG 2.2 SC 1.4.10 header horizontal-overflow emergency is FIXED.**
- **v2 rebuild — SGS-native swap:** replaced `woocommerce/mini-cart` with `sgs/cart` and `core/site-logo` with `sgs/responsive-logo`; grouped cart+burger into `core/group.sgs-header-icons`. Designed collapse-to-drawer behaviour: nav `display:none` below 768 (lives in the drawer), burger visible only below 768, cart+logo always visible — matching the mamas-munches draft faithfully.
- **`sgs_header` CPT gained a `template` key** (`'template' => [['sgs/site-header']]`) in `class-sgs-block-cpts.php` — this was previously absent (confirmed live code had no `template` key at all, so this is an ADD not a swap, matching the FR-S3-4 addendum's fact-check). Footer template deferred to P3.
- **Confirmed the header is genuinely block-based in the Site Editor** (not a Customiser-driven builder — the WordPress Customiser is legacy tooling for classic themes; this is a block theme, so the Site Editor + template parts + blocks is the correct and only mechanism, consistent with §S1's architecture).
- **PROVEN root cause of the stray WooCommerce mini-cart + customer-account in the header:** WooCommerce auto-injects `woocommerce/mini-cart` and `woocommerce/customer-account` after ANY `core/navigation` block via the WP Block Hooks API. Verified directly in WooCommerce source: `src/Blocks/BlockTypes/MiniCart.php` and `CustomerAccount.php` both register `'anchor' => 'core/navigation'` via `add_filter('hooked_block_types', ..., 9)`; the `BlockHooksTrait.php` docblock confirms the mechanism. This is not an inferred cause — it's read directly off the hooking code.
- **Bean's decision: fix via architecture, not a workaround filter.** A `hooked_block_types` suppressor filter was built and validated as functionally working, then REVERTED per Bean — the correct closure is P2 (`sgs/adaptive-nav`, FR-S9-4) replacing `core/navigation` in the header, so WooCommerce has no `core/navigation` anchor left to hook onto and the injection stops by construction. Documented as the "ROOT FIX" note on FR-S9-4 in Spec 17 §S9 and in `.claude/plans/2026-07-13-header-builder-remaining-work.md`.
- **NEXT:** P2 `sgs/adaptive-nav` — both the scoped nav block AND the architectural fix for the WC Block-Hooks injection. Once it ships, remove the interim nav-visibility CSS hack in `sgs/site-header/style.css`.

---

## 2026-07-13 — D323: Header/Footer/Nav SYSTEM design-gate APPROVED + P0 drawer-fix SHIPPED + 6-doc rollout

**D319 — the extractor palette is now additive by construction; the D318 palette-rename regression is fixed.** Branch `main`. Bean caught it live: after the D318 deploy, sections with `surface-pink` background (hero band, trust bar, gift section) + the gift-card labels went CREAM.

- **Cause (proven on the live DOM):** the D318 palette generator renamed `surface-pink`→`custom-surface-pink` (my `custom-` prefix for any non-baseline-slug colour) AND dropped declared tokens it saw no resting usage for (`success`/`cookie-brown`/`accent-dark`) AND its generated palette was a SUBSET missing hand-added colours the draft can't provide (`surface-peach`/warm-axis/`footer-bg`/`client-*` aliases). The already-cloned blocks reference `var(--wp--preset--color--surface-pink)` (baked into their markup) → now UNDEFINED → transparent → cream. Live probe: `--…--surface-pink` = `rgba(0,0,0,0)`, my `custom-surface-pink` defined but unreferenced. **This VIOLATED my own FR-33-11** ("NO snapshot-only push of a regenerated palette to a client whose pages aren't re-cloned in the same change").
- **Fix (Bean-directed "add the customs, don't ruin the palette; reclone fixes the new setup"):** (1) `palette.py` — a client colour keeps its OWN draft-token name as the slug (`surface-pink`, never `custom-`/dropped); EVERY declared solid `:root` colour is emitted (usage only informs the ROLE, never whether to emit). (2) `extract.py --merge-onto <existing>` — ADDITIVELY merges the generated globals/base onto an existing client snapshot: generated wins on shared slugs (carries the drift fix), but every existing slug the draft can't provide is PRESERVED + component `styles.css`/pattern refs carried forward. Non-destructive by construction.
- **LANDED live (sandybrown page 8):** all pink restored (hero/trust/gift/labels = `rgb(245,194,200)`); surface-pink + surface-peach + all 21 slugs defined; base 16px; h2 lh 1.2; primary coral, secondary transparent; 0 console errors. 18 tests (2 new guards: raw-name-not-custom + additive-merge-preserves-extras). **Architectural rule:** a regenerated palette is NEVER a straight replace on an already-cloned site — it is additive + gated by a reclone (FR-33-11/12). Lesson `feedback_extractor_palette_must_be_additive_not_destructive_replace`.

## 2026-07-13 — D318: Spec 33 Part 1 BUILT — the draft global-styles extractor; D303 killed live on Mama's

**D317 — the header/footer setup pipeline Part 1 is now a build-ready spec.** Bean picked the header/footer setup pipeline as the next front, in 2 parts: Part 1 = a universal draft→theme global-styles/token extractor (kills the D303 inherited-base drift by construction); Part 2 = header/footer clone (Spec 17, later). Branch `main` (D-ceiling D316→D317). NO code built — this is design + design-gate only.

- **Grounded first (not from memory):** `/library-docs` (WP theme.json fluid-typography + `styles.elements` schema) + a focused research pass + a full survey of ALL `sites/` drafts (Bean-directed) → an empirical union inventory of 3 real design systems (Mama's role-named tokens, Indus literal-colour-named + rem, dogfood spacing-tokenised + clamp) + a token-less Spectra scrape. Wrote **Spec 33 v0.1** (`.claude/specs/33-DRAFT-GLOBAL-STYLES-EXTRACTOR.md`).
- **6-persona `/adversarial-council`** (Cynic/Spec-Lawyer/Ship-PM/Extraction-Correctness/Support-Realist/Systems-Integration; all C-band, GO-conditional). Convergent must-fixes: **FR-33-1 "declared value wins" would RE-SHIP D303** (3 raters — the emitted value must be COMPUTED on a real rendered node, declared supplies only the name); the role-inference "route by role not name" had **no algorithm**; **Pass B frequency INVERTS the palette** (most-frequent = body-text/border, not primary — Bean's #1 worry, confirmed); regenerating **6 live snapshots** with no diff/rollback/per-client-parity gate (4 raters); no provenance trace/goldens; non-determinism; reuse-by-widening would regress the live D306/D307 helper.
- **Bean's scope call (overturned the Ship-PM's cut, correctly):** keep the COMPLETE spine (all DECLARED value types in v1 — colour/typography/spacing/radius/shadow/buttons/layout). The council conflated value-type BREADTH with the risky MECHANISM; the real trust boundary is **provenance, not value type**: declared (Pass A) auto-applies after computed-validation; derived (Pass B) is PROVISIONAL/advisory, never auto-pushed live.
- **Spec 33 → v0.2** (`e77eec79`) applies every convergent fix: computed-value-wins (FR-33-1), a defined role rule-table by usage-context not name/frequency (FR-33-2), base from a real rendered `<p>` + rem-vs-real-root (FR-33-3), complete declared spine (FR-33-4), advisory Pass B + baseline-on-empty (FR-33-5), dark-theme/preview-shell safety (FR-33-6), provenance trace + goldens + schema-validate (FR-33-7), determinism (FR-33-8), conservation (FR-33-9), reuse-by-composition freezing the live hex-only helper (FR-33-10), deploy safety Mama's-only + backup/rollback/diff-approve (FR-33-11), bootstrap ordering — extractor before ANY clone (FR-33-12), forward contracts — Spec-26 acyclicity inert-until-Phase-3 + Part-2 header/footer namespace reserved (FR-33-13). All 4 open questions resolved into FRs (full-snapshot-now / advisory-Pass-B+baseline / raw-hex-custom-two-tier / role-by-context-not-frequency). **NEXT: BUILD Spec 33 v0.2, prove on Mama's.**

## 2026-07-12 — D316: selection-tick gap fix (universal, option-picker + product-card pills)

**D313 — the two Lighthouse-flagged page-8 a11y items were fixed by editing the DRAFT mockup + re-cloning, never the clone or the converter.** Branch `main`, commit `904fe02e` (pushed). **Bean-locked principle:** "we should not depart from the draft at all" — a fidelity/a11y issue that is INHERITED FROM THE DRAFT is corrected at the source-of-truth (the mockup) so the clone stays a faithful mirror; NO clone-side patch, NO converter carve-out. Verified the draft actually had each issue before fixing.

- **Proven all 5 Lighthouse a11y flags were draft-inherited, not clone bugs:** gift-capsule text = the draft's `.sgs-gift-section__card-tag { color: var(--primary-dark) }` (#C56A7A on surface-pink = 2.34); ghost button + Trustpilot = draft colours; ingredient cards = the draft's `<h2>…<h4>` (skips h3). The clone reproduced them correctly.
- **Fixed in the draft + re-cloned page 8:** gift-tag `var(--primary-dark)`→`var(--text)` (on-brand charcoal #3A2E26; contrast on surface-pink 2.34→**8.4**, WCAG AA); the 4 ingredient headings `<h4>`→`<h3>` + the `.sgs-info-box h4`→`h3` selector (section hierarchy now h2→h3, no skip). LANDED live (charcoal 8.4, all H3).
- **Left faithful-to-draft (Bean's call):** the "Find out more" ghost button (3.67) + Trustpilot brand green (#00b67a) — the mockup's own design; changing them would depart from the draft.
- **Lighthouse (mobile):** Performance CWV green (LCP 274ms / CLS 0.06), Best Practices 100, SEO 100, Accessibility 95→**96** (remaining 2 contrast flags = the intentionally-kept draft colours). Full DOM audit confirmed 0 body SGS `<style>`, 1 head `<link>`, and zero SGS-block forbidden inline `style=` declarations. Memory: `test-with-actual-cache-layer-and-self-consistent-render` (D312); D313 principle captured as `fix-a11y-at-draft-source-not-the-clone`.
- **NEXT (Bean-directed):** (1) an exhaustive draft-vs-live DOM diff to PROVE the 100% clone; (2) rebuild the computed-parity tool (`scripts/parity/computed-parity.js`, Stage 11.6) to be universally trustworthy (tags/classes/elements/content/CSS, no cheating, validated vs a manual ledger).

## 2026-07-12 — D312: per-block `<style>`-tag consolidation SHIPPED + LANDED (P-STYLE-TAG-CONSOLIDATION)

**D312 — the ~100 per-block scoped `<style>` tags (~33KB, page 8) are consolidated into ONE head stylesheet, operator-selectable file vs inline.** Branch `main`, commits `9dfcaa6e` (design + Spec 32 v1.2/FR-32-11) + `72c0387a` (collector) + `c30dd5e2` (final modes + settings), all pushed. Full research + `/qc-council` GO-WITH-FIXES trail. Canonical: Spec 32 §6.2.

- **Collector = one `render_block` chokepoint**, NOT ~60 per-block emit-site edits. A late (`priority 99`) filter lifts every `sgs/*` block's `<style>` out of its rendered HTML into a per-request buffer (dedup by content hash; D303 residual-last order preserved). Captures all 6 emit shapes the council found (incl. the container wrapper's prepended tag + `custom-css.php`'s appended residual) without touching either file; universal (R-31-9). Dissolves the "6-shapes audit" risk the council flagged.
- **Delivery = ONE output buffer** (`template_redirect`) injects the consolidated CSS into the `<head>` (before `</head>`, after block `style.css` → overrides win) on EVERY front-end render → self-consistent under full-page caching (no pointer, no cold/warm, no freeze). **Two modes** (SGS → CSS Output settings, `sgs_css_output_mode`, default `file`): `file` = cached content-hashed external `<link>` (immutable `Cache-Control` .htaccess, atomic tmp+rename, epoch-invalidated on save_post/template/global-styles/deploy + `litespeed_purge_all` + GC; an optimisation plugin defers it); `head` = inline `<style>` in head (the draft's own model), self-contained. Settings page lists LiteSpeed/Autoptimize/WP Rocket/Perfmatters + the exact setting.
- **Key build finding (Bean-directed test):** the initially-designed generate-then-serve external-file model was **reproduced FAILING live under the LiteSpeed page cache** (froze the cold inline response — exactly the council's prediction) → replaced by the unified buffer. First verified **no LiteSpeed Cache plugin was installed** (only sgs-blocks+woocommerce), then installed it on the canary to test file mode. Research (web.dev 14KB rule + "LiteSpeed optimises external not inline") corrected a hasty inline-in-head lean. Memory: `test-with-actual-cache-layer-and-self-consistent-render`.
- **Editor parity (live-verified):** frontend-only gate `! is_admin() && ! wp_is_serving_rest_request()` — block-renderer REST (`context=edit`) keeps the block `<style>` inline; naive `! is_admin()` would have unstyled 8 ServerSideRender blocks in the editor.
- **LANDED (sandybrown page 8, both modes, incl. under LiteSpeed):** 1 head style/link, 0 body `<style>`, correct cascade + computed values 375/768/1440, D303 intact, 0 console errors, settings page renders. Render-side PHP only (no converter/JS; conformance goldens unaffected); no block version bump.

## 2026-07-12 — D311: page-8 Fix 4 — label padding ungate + product-card trial-tag full mirror SHIPPED + LANDED

**D311 — the gift-card `sgs/label` capsules now paint their padding, and the product-card trial tag is a full functional mirror of a standalone label.** Branch `main`, commit `cef1fca9` (pushed). Bean-directed FULL MIRROR (chose it over the minimal fix). Every cause proven on the LIVE page-8 DOM + a real-node converter trace BEFORE building; design settled via `/qc-council` (2 cross-model raters, BOTH returned RESHAPE — the gate paid off).

- **Two defects (proven live, Bean-confirmed).** (a) Gift capsules (`.sgs-gift-section__card-tag` → `sgs/label`) rendered `padding:0` — a tight coloured chip — because `label/render.php` gated padding behind the `is-style-pill-*` class; a cloned label has no pill class, so the padding the converter ALREADY transferred (`padding:{4,10,4,10}`, proven vs the conformance "actual") was dropped. (b) The trial tag ("New? Start here", a product-card built-in element, NOT a standalone label) had padding but hugged (`width:max-content`) instead of stretching to the card width like the CTA.
- **Attributes over variants (Bean delegated the call).** Chose value-presence ungate over the register's "set the pill block-style" — the pill gate is exactly the hardcoded structural gate the no-inline/STOP-D228 programme removes; variants survive as one-click editor value-seeders. The register's mechanism was superseded by Bean's earlier "fix as CSS attributes not variants" directive.
- **Fix (shared helper, no inline per Spec 32 §6.1).** (1) New `includes/helpers-box.php :: sgs_label_box_css_rule($box,$selector)` emits a label-style box (padding base+tiers, radius, background, display/fullWidth) as SCOPED class-level CSS from a NORMALISED struct — both `sgs/label` AND the product-card trial tag call it → byte-identical output (composite-mirror R-31-9, Spec 31 §13.6 D294 block-private clean reproduction). (2) **label:** padding + custom-bg ungated to value-presence; new display model (`fullWidth`→block+100%, box→inline-block, bare→block so `margin-bottom` paints — draft-faithful), suppressed when an `is-style-*` class is present (variant CSS owns display); block.json `padding {}`/`borderRadius 0`/`backgroundColour ""` + new `fullWidth` bool. Static pill-chrome `style.css` left intact (borrowed by the product-card discount/best-value badges — Rater A catch). (3) **product-card:** trial tag renders via the shared helper (`tagFullWidth` default true = a card-design constant, not a converter carve-out) scoped to `.{uid}.sgs-product-card__tag--trial` (0,2,0); new client-editable `tagPadding`/`tagBackgroundColour`/`tagTextColour`/`tagBorderRadius`/`tagFullWidth` attrs; `style.css` moves `width:max-content` off the base onto `--featured` (protects the featured overlay badge + image-less fallback — Rater A catch) + var-default padding/radius (F3 gate). FEATURED badge NOT mirrored (empty on this draft + a distinct overlay component — capability preserved).
- **/qc-council RESHAPES applied (both raters, pre-build).** Rater A: per-variant treatment (don't wholesale-delete the base tag box — it strips the featured badge's typography); helper takes a normalised struct not label `$attrs`; keep the borrowed pill chrome. Rater B: key box-presence off NATIVE `style.color.background`; suppress render-driven `display` when an `is-style-*` class is present (else editor pill-fill collapses); DROP the speculative converter parent-flex fullWidth heuristic (FS5 — no page-8 label needs it; trial full-width is `tagFullWidth`). The council also forced resolution of a stale-golden contradiction (padding IS emitted — my finding held; the golden is pre-existing stale).
- **LANDED live (sandybrown page 8, reclone `…001022`, CDN + OPcache cleared), computed-style 375/768/1440:** gift capsules = padded hug (`4px 10px`, surface-pink `rgb(245,194,200)`, radius 6, w78/140); trial tag = FULL-WIDTH (w276/316 @375, w217/257 @768, w340/380 @1440); the 4 section eyebrows (incl. hero, all `.sgs-section-heading__label`) stay BARE (padding 0, no bg) — Bean's #1 regression concern holds; now `display:block` so their 6px margin paints (fidelity gain). Converter unit suite **449 pass**/1 skip; conformance goldens unchanged (15 pre-existing red, 0 new — render-side change); `/sgs-update --stage 1` seeded 6 new attr rows. **No block version bump** (all visible change lands render-side via inline `<style>`; the trial full-width rule wins over any stale base `width:max-content` by source order).
- **Deferred (documented, non-blocking):** per-element GEOMETRY transfer (`tagPadding`/`tagBorderRadius`) is NOT converter-routed (the universal styling lift handles colour/typography only — a design-gated shared-mechanism change); moot on page 8 (draft tag box = SGS defaults). The 15 pre-existing stale conformance goldens (accordion/cta-section/hero/product-card/quote/trust-bar/…) predate this work — reseed with LANDED proof when touched.
- **Remaining page-8 items:** Fix 9 (inline-styles Spec-32 investigation, read-only). D-ceiling was D310.

---

## 2026-07-11 — D310: page-8 Fix 2 — product-card CTA mirrors sgs/button style + colour SHIPPED + LANDED

**D310 — the cloned product-card CTA now mirrors how a standalone `sgs/button` clones.** Branch `main`, commit `6fdf54d7`. Bean-directed ("mirror the way sgs/button blocks get cloned; the only difference is the product-card button spans the card's full width — confirmed"). Every cause proven on the LIVE page-8 DOM before building (the register + one qc-council rater were both WRONG about the mechanism — see below); design settled via qc-council (2 cross-model raters) + a live-DOM baseline.

- **Bug (live baseline).** Trial CTA rendered `sgs-button--primary` (draft wants `--secondary`); featured CTA text was rgb(255,250,245) near-white on pink = WCAG ~2.1 FAIL.
- **Real cause (NOT the register's).** (a) The converter never emitted `ctaStyle` (grep=0) → both CTAs defaulted to `--primary`. (b) The white text was NOT `style.css:246` (the register's + first-design target — that rule LOSES by source order); it was the per-instance `.{uid} .sgs-product-card__cta--primary{color:text-inverse}` rule that `sgs_button_element_style_css` emits from the `ctaColourText` DEFAULT, clobbering the already-correct dark `--sgs-btn-color`. Editing style.css would have changed nothing — proven by live matched-rule enumeration. The TYPED clone render already honoured `ctaStyle` (`product-card-builtin-render.php:82/228`); the 5 hardcoded `sgs-button--primary` sites are BOUND-mode only (deferred).
- **Fix (mirror, universal).** (1) **Converter:** the nested CTA's BEM `--modifier` lifts onto the composite's `ctaStyle` via the SAME preset-modifier mechanism as `sgs/button` `inheritStyle` — new shared `db_lookup.preset_style_for_element` (factored out of `assembly.py` step 5, ONE impl) + `db_lookup.style_preset_attrs_for_identity` (surfaces the `role='behaviour'` style attr that `content_attrs_for_identity`/`equivalent_block_for` deliberately EXCLUDE per FR-31-2.2, reusing the SAME `canonical_slot→standalone_block` map minus the content-role filter). Gated DB-drivenly on the identity declaring a string `inheritStyle`; no attr-name/slug literal (Spec 31 §13.5, R-31-1/R-31-9). (2) **Colour (composite-mirror, STOP-D228):** all 6 `ctaColour*` preset defaults emptied so a preset CTA injects no explicit colour and inherits the shared `sgs-button--{style}` class channel per variant — primary solid, secondary transparent — WCAG-correct per client. (3) **Editor merge (Bean's "one setting"):** removed `ctaPreset` attr + the duplicate "Apply preset" control; ONE `ctaStyle` picker (seeds colours on change) + a "Reset colours to preset" button (a SelectControl can't re-fire on same-value — qc-council catch); dropped the dead `data-cta-preset` emits.
- **Full-width confirmed FAITHFUL (Bean's question, verified not assumed):** draft `.sgs-button` is `inline-flex` but stretched by the flex-column `.sgs-product-card__body` (align-items normal→stretch); clone renders full-width too. No width work.
- **LANDED live (sandybrown page 8, plugin redeploy — no reclone needed, markup carries no `ctaColour*`; CDN + OPcache cleared).** Featured: `--primary`, solid pink bg, dark #3A2E26 text, **5.28:1 PASS**. Trial: **`--secondary`**, **transparent bg**, dark text, pink #C56A7A border, **11.86:1 PASS**. Exact draft parity (`.sgs-button--primary` solid / `.sgs-button--secondary` transparent). Converter suite **448 pass** (+1 new `test_real_draft_cta_style_preset_mirrors_the_nested_button_modifier`); build + cheat/pipeline/anti-mirror gates green.
- **Follow-ups (logged, non-blocking):** `BUTTON_PRESETS.primary.colourText` still seeds `text-inverse` on an editor style-pick (authored-card footgun on light palettes — the CLONE is unaffected); the 5 bound-mode `sgs-button--primary` render sites; product-card self-contained `--secondary` CSS (currently relies on button style.css being enqueued — true on page 8). DB reseed (`/sgs-update --stage 1`) to drop the stale `ctaPreset` row + sync the enum/defaults.
- **CORRECTION (same day, Bean caught it):** the first pass verified only resting contrast — it MISSED that the product-card's OWN `style.css` `.product-card .sgs-button--primary/secondary` rest+`:hover` rules (0,2,0) still OVERRODE the shared button channel (trial border→primary-dark `#C56A7A` not primary; secondary:hover text stayed dark not white; primary:hover border→`#8B3A47`). The snapshot `buttonPresets` are perfect (match the draft), so the real composite-mirror fix = REMOVE those per-block divergences (R-31-9) so the CTA inherits the faithful channel. Also bumped block version 1.16.11→1.16.12 to cache-bust the version-pinned block stylesheet (a `style.css`-only change can't reach a cached browser without a `?ver` change — the render-side default changes DID land because they emit fresh inline `<style>`). **Now verified live REST + HOVER (Playwright `.hover()`):** both CTAs exact-match the draft — trial border `#E68A95`, both hovers dark bg + white text + dark border. LESSON: colour verification MUST test hover + compare each value to the draft, never resting-contrast-only (`feedback_verify_hover_and_compare_to_draft_not_contrast_only`, pending). Commit `<pending>`.
- **Remaining page-8 items:** Fix 4 (labels attribute-driven pill + fullWidth), Fix 9 (inline-styles Spec-32 investigation). D-ceiling was D309.

---

## 2026-07-11 — D309: page-8 Fix 5 — universal hover (naming standardisation + converter modifier-append) SHIPPED + LANDED

**D297 — no-inline rollout Wave 1: 8 single-element/leaf blocks migrated block-private + LANDED; plus a real D295 converter gap (hero L4 per-area padding cloning) found and fixed.** Branch main; commit `ec5063a9`; D-ceiling was D296. Continues D294–296.

- **8 leaf blocks (label, icon, counter, whatsapp-cta, social-icons, star-rating, business-info, breadcrumbs)** migrated to the no-inline contract, block-private (heading/text/button pattern). Each: every declared WP styling support (`spacing`/`color`/`__experimentalBorder`/`typography`) flipped to `__experimentalSkipSerialization` + emitted scoped via `wp_style_engine_get_styles($style,['selector'=>"#uid"])`; box padding/margin (+ counter/whatsapp-cta border-radius) → named objects with Tablet/Mobile tiers; device tiers 1023/767 only; semantic element as root (no useless wrapper); free-text keyword sanitisers + `wp_strip_all_tags`. **box_family seeds added centrally** in `sgs-update-v2.py` (FR-31-22.1). No version bumps, no deprecations (D293). Block-specific: social-icons renamed its own `style` variant attr → `iconStyle` (collided with WP-native `style` object); whatsapp-cta collapsed its wrapper to the `<a>` root (D288 pattern); label merged 4 flat padding attrs → `padding` object, kept `borderRadius` scalar-but-scoped; counter guarded a top-level function (`function_exists`, redeclare-fatal fix); icon kept `backgroundPadding` scalar (single-value, §6.1c).
- **LANDED (dispatched to 8 solo Sonnet subagents, disjoint dirs, FR-31-6.1; orchestrator gated every block):** built once, deployed to sandybrown, verified live on page 1356 at 375/768/1440 via a NEW reusable harness `scripts/no-inline-land-verify.js` + `no-inline-wave1-manifest.json` — RESULT ALL PASS: zero inline property declarations on the whole subtree + asymmetric computed padding/border-radius correct at every tier. (margin left/right is owned by WP-core `is-layout-constrained` centering `!important` — a non-block-fidelity axis; the harness asserts margin top/bottom, the block-controlled axis.) 8 visual-diff reports at repo-root `reports/visual-diff/`.
- **Roster: 15/59 styling-support blocks migrated (7 prior + 8); ~44 remain** (phased waves — Wave 2 = more leaf blocks + F3-drains; see next-session-prompt.md).
- **Hero L4 converter gap (found + fixed):** the routine `--stage 10` aggressive prune correctly removed hero's orphaned flat `contentPaddingTop/Left/…` attrs (D295 migrated hero's per-area padding flat→OBJECT `contentPadding`), which surfaced 4 stale converter tests + a REAL latent gap: the L4 per-area CLONING path (`fold_helpers.route_area_css_to_block_attrs` — the live path via assembly step 3d — AND `resolvers/grid_area.py`) was left on the flat model, so `attr_for_area_property` returned None and **hero content-padding silently gapped on every clone** (D295's earlier "72/64 LANDED" was an authored value, not cloned — the coincidental-default false-win Spec 31 §7b warns about; hero style.css has NO padding default). Fix: made BOTH L4 paths box-object-aware, mirroring `content_band._content_band_box_write` — route the four padding sides into the `{area}Padding{Tier}` object, gated on `db_lookup.box_family_for` (a `box_family` VARIABLE ref to satisfy the FR-31-22.2 AST gate), never a name regex. 4 stale flat-asserting tests updated to the object contract + 1 new test locking the live fold_helpers path. **Converter suite 440 pass; cheat/excluded/ledger/db-consistency/box-family gates all green. LANDED on a page-8 reclone:** hero `.sgs-hero__content` padding now clones from the draft → **72/64 @1440, 28/20/40 @375, zero inline** (was gapped). Lessons: STOP-34 (a synthetic unit test passed while the real draft still gapped — the fix's first location `grid_area` wasn't even the live path; the trace showed 0 grid_area dispatches, `route_area_css_to_block_attrs` is the live path), R-31-11 (verify rendered output, not internal metrics), prove-the-cause (D295's LANDED claim fact-checked against the draft + style.css before trusting the fix).

## 2026-07-09 — D296: universal grid-scoping — SGS_Container_Wrapper grid/flex CSS inline→scoped (STOP-68 CLOSED)

**D282 — the diagnosis-first QC batch executed (systematic-debugging + 3 parallel read-only investigators, each fact-checked vs code+DB+live DOM). SHIPPED + LANDED:** (1) **L3 gap-gate fix** (`82c3182a`) — `root_supports.py:356` native-lift gate collapsed `sup_sub` to None for every 2-element rule (gap→spacing.blockGap), checking "any spacing feature"; `sgs/container` has {padding,margin} but NO blockGap → gap wrongly consumed into a dead `style.spacing.blockGap` leaf (wrapper reads the `gap` attr) → grids rendered flush. Fixed: check `sup_sub` specifically. **Closes QC #1** — container grids now show gaps (products 16 / brand 60 / feature-grid 14px LANDED); parity 69/70/70 → 70/71/71. Phase-4 red→green test in `test_root_supports`; 2 `test_css_pass_partition` tests updated (they'd codified the buggy gap→blockGap). (2) **QC #4 hero split image** (`bb0d1a4a`) — hardcoded `.sgs-hero--split{padding}` (D228) insetting BOTH columns MOVED to `.sgs-hero__content`; image flush. (3) **QC #9 testimonial** (`bb0d1a4a`) — hardcoded `__slide--card{background:surface}` (=#FBF3DC cream, D228) → transparent; cream box gone. (4) **Hero splitGap de-dupe** (`2f4a1e4a`) — a 30-block audit confirmed splitGap was the ONLY true attr dupe on the roster (every other block mirrors shared attrs by identical name = intended, not dupes); removed splitGap* + its "Column gap" control, split grid now reads the shared `gap`; also removed the hardcoded 24px `.sgs-hero--split{gap}` default (D228). (5) **Dead-schema cleanup** (`aff01e19`) — customWidth/customWidthUnit orphan stripped from 27 composites (+53 DB rows pruned); dead blockGap removed from accordion/product-faq. **D2 NO LONGER DEPLOYED** (`6a83281c`, D281b) — the page shows honest gaps, which is why the QC batch surfaced (parity 80/81/81 masked → 69/70/70 honest → 70/71/71 after L3). Root causes proven for the 2 remaining converter gaps: **L4 per-area extraction** ("no value extracted" for hero contentPadding — the composite grid-area box-CSS isn't fed to the wired area resolver) + the **L3 fix's universal reach** (Bean: universal = all L3 CSS landing across different examples, not one rule). 872 tests + gates green. Common thread: D228 hardcoded defaults are the recurring cheat. Next front = the container L1–L4 cascade deep-dive.

## 2026-07-06 — D281: declarative css_property column MECHANISM shipped (parity-neutral) + Track-B multi-button/parity fixes; button-colour SEED trialled+reverted (draft var() resolution gap); Bean opened a 9-defect page-8 QC batch

**D277 — Bean's mandated post-programme QC ran in full: 4 read-only phase raters + 2 full-shape tracers (universality, DB-rootedness) + a solo doc-regen agent, with EVERY finding fact-checked by main-session tracing before acceptance (STOP-15/45 — one more false rater claim caught: "assembly.py not in the no_slug_literal scope" is FALSE, `_SCAN_DIRS` covers services/).** **Per-step verdict: all 16 steps PASS spec/rules/cheats/drop-attribution** — no step introduced a cheat, carve-out, or unattributed homepage drop; Step 14's byte-copy + Step 13's routing-order claims independently verified at diff level. **Full-shape verdict: substantially universal + DB-rooted** — walk.py signature/registry, dispatch_table, resolvers clean; routing tables genuinely seeded (variant_slots 4/4 blocks consistent; parent_block 18 rows exact). **Defects found + FIXED (commit `4a35134a`, 2-rater GO/GO, emits byte-identical on the real draft, LANDED re-verified):** (1) **HIGH, Rule-4 at the caller** — the orchestrator never inspected converter `status:'failed'`: a failed section vanished from the page while Stage 4 reported 'complete', and the Stage-9 queue (exact-match `== "unmatched"`) missed it AND the two `unmatched-*` softfail statuses whose own comments always intended queueing. Now: aggregate_errors + trace + status 'failed' with failure_reason carried into the widened queue filter; 5 planted-failure tests. (2) **The 'ghost' cheat** — assembly.py's hardcoded `'ghost'→'outline'` branch, whose own comment admitted it was SHAPED TO EVADE cheat-gate Check #9 (pre-programme `603cbaaf`; Step 4 only moved it) → replaced by `db_lookup.inherit_style_for_modifier` (slots alias→default_attrs channel — the DB already carried ghost-button→outline); COMPOUND-probe-only so block-scoping is structural; `no_slug_literal._TARGET_IDENTS` widened with modifier idents + plant-tested (the ident set was the gate's blind spot). (3) **R-31-1 drift** — `content_attr_for_element`'s in-code 5-role tuple had drifted from `roles.classification` (missing link-href — the ctaUrl residual's role — + the 4 icon-* roles) → DB-sourced like its 2 siblings. (4) **Dead code deleted** — `block_accepts_inner_blocks` (read the DROPPED has_inner_blocks column; OperationalError soft-fail made it always-True; zero callers) + `detect_content_layer` (the Step-3 MF-3 guard landed on a zero-caller function; the LIVE guard is `layer_detect.py:26` — MF-3 test re-pointed there). **A2 re-baselined 6→5** (fresh clone deploy → live page-source accounting → the stale trial-tag key removed; gate green — NOTE: the check's canonical `--markup` input is the LIVE PAGE SOURCE, not the raw emit, whose JSON-escaped attr values false-fail array content). **Docs:** flow docs + Spec 31 §12 regenerated to post-D276 reality (`d8769e8d`; solo agent + STOP-45 QC; D1-KEEP + the failed-status gap documented). **Parity unregressed: content 90% / CSS 67-69-76.** 752 canonical + 18 orchestrator tests; all gates green. **Parked with evidence (parking P-QC-*):** emit_shape NULL semantics (66/194 content-role rows NULL; walk legs treat NULL oppositely — HIGH, investigate-before-fix per R-31-7), FR-31-8 raw-sqlite3 ×3, capability-rank in-code list (FR-31-15's "DB-driven" false as implemented), conformance-golden re-seed, metamorphic real-draft extension (relations are synthetic-only, STOP-34 class), parity-instrument distortion vectors, A2 commit-gate no-op, minor hygiene bundle. Card residuals (ctaText/ctaUrl multi-attr lift, packSizes schema, imageAlt) = the next build front.

---

## 2026-07-05 — D276: the converter completion programme EXECUTED (Steps 3-16, Gates A/B/C) — the frozen engine is DELETED and converter/ is the only path

**D272 — the content-extraction rebuild is scoped end-to-end + its per-attr `emit_shape` fork foundation is built. Bean-directed; the parity backlog is DOCUMENTATION-DERIVED and must be fact-checked next session.** **(1) `emit_shape` foundation (built + committed, `df9e20cf`):** Spec 31 §13.3 FR-31-2.6 — the per-attr nested-vs-child fork that replaces the block-level `has_inner` dispatch. Added `block_attributes.emit_shape` column + `/sgs-update _populate_emit_shape` (Stage-1 sub-step D, source-derived via `render_reads_attr` — a raw `$attributes[attr]` read-check with NO type filter so number-typed content like `ratingStars` classifies right; fail-loud on parse failure, flagged 8 form-field blocks) + `db_lookup.emit_shape_for` accessor + `render_emits.render_reads_attr`. Additive + INERT (not wired into the walk). Verified: product-card 9/9 nested, hero content=child + badges=nested, accordion.title nested, testimonial 7/7 nested. Spec 22 redirect stub re-removed (was wrongly recreated post-D253). **(2) Reframed the three signals (Bean-corrected):** `role` = the content-vs-styling FILTER; `equivalent_block_for` = the universal functional IDENTITY resolver (attr→canonical_slot→standalone_block: description→sgs/text, headline→sgs/heading), NOT the fork; `emit_shape` = the nested-vs-child fork. They are complementary. **(3) Architecture evidence-locked (research-check):** ONE walker owns recursion (unconditional descent — a nested element can still have children) + a TOTAL registry dispatch of pure per-type handlers with EXPLICIT priority (CSS-cascade style, not elif source-order) + assembly as the one legitimate final pass = Babel merged-visitor model = Spec 31 FR-31-3. **(4) Completion phase-plan** `.claude/plans/2026-07-04-new-engine-to-parity-delete-converter-v2.md`: 6 phases, **DELETE-LAST** — `convert.py`/`converter_v2` is the production default + the new engine's fallback, so it is deleted only AFTER the new engine reaches §2.7 parity (Phase 5). New FR-31-2.7 proposed: a container-vs-composite classifier before the emit-gate (`sgs/container` is the ONLY true arbitrary holder; card-grid is a TYPED composite). **(5) ⚠ The Phase-5 parity backlog is UNVERIFIED — sourced from Spec 31 §12.6 + code comments + the STALE `cloning-pipeline-flow.md` (references archived files).** Bean: the "media-map loader not built, images broken" claim is WRONG — the live clone LOADS media. Next session MUST fact-check the whole scope against the running scripts + a real clone FIRST (lesson `fact-check-scope-against-running-scripts-not-docs`, blub 378). **Verify:** 373 tests + cheat-gate + Gate A green (product-card WIP leaf conversion REVERTED — needs the Phase-2 walk; `has_inner` resynced to 1). Committed `df9e20cf`, pushed; branch `main`. SoT = handoff.md top + next-session-prompt.md.

## 2026-07-04 — D271: core→SGS routing consolidated to git-tracked data files + plugin-wide deprecation removal + TOC sgs/heading fix

**D269 — sgs/media: audio mode REMOVED (moved to sgs/audio) + branded video player re-skin.** Completes D268. **(1) Audio removed:** the D266 audio mode is fully deleted from sgs/media — mediaType `audio` enum + 8 `audio*` attrs + the render branch/emission/whitelist + the editor UI (button/panel/canvas/handlers/destructure) + the `.sgs-media__audio` style; `replaces` = `[core/image, core/video]` (core/audio → sgs/audio, uniqueness kept); v1.4.0→1.5.0. Both raters confirmed ZERO dangling `audio` ref. **(2) Branded video player** (NEW `view.js` viewScriptModule): replaces the native `<video controls>` chrome for DIRECT video with a themed player — centre-play overlay + hover-reveal bottom bar (play/pause, scrubber, timecode, mute+volume, fullscreen), keyboard-operable, accent = theme primary. YouTube/Vimeo `<iframe>` untouched (guard `tagName!=='VIDEO'`). Progressive enhancement: SSR native `<video controls>` = no-JS fallback; view.js strips + upgrades. **/qc-council (2 read-only raters): GO** — Rater A (correctness) all clean (no dangling ref, iframe/image/svg untouched, edge cases NaN-duration/multi-instance/fullscreen-guard/idempotency, mute-volume-scrub logic, PE); Rater B (a11y/security/regression) clean on security[innerHTML all static ICON constants, no XSS]/reduced-motion/regression. **Sole blocker (both raters): the wrapper `keydown` stole arrow keys from the scrub/volume range inputs** — FIXED pre-commit (`if (e.target !== wrap) return;` → shortcuts fire only when the wrapper itself has focus; sliders/buttons keep native keys) + **verified LIVE** (ArrowRight on the focused scrubber = native +0.005s fine-step, not a +5s seek). Polish (Rater B): control buttons 40→44px (project standard) + icon drop-shadow + timecode text-shadow for scrim contrast. **LANDED on sandybrown:** direct video enhances (wrapped, native chrome removed, full control bar + a11y role/tabindex/aria-labels), plays 0→5.1s with timecode + state tracking, ZERO console errors. npm build clean (dead-control + F5/F6 + cheat-gate green), 374 tests, WPCS 0 errors. `reports/visual-diff/media-2026-07-04.md`.

**D268 — dedicated `sgs/audio` block (7 player styles + Web Audio visualiser); `core/audio` re-pointed off sgs/media.** Bean's design review of the audio-player artifact (D267-era) → a dedicated block, not an sgs/media mode. Built `sgs/audio` (category sgs-content): a native `<audio>` player that `view.js` (viewScriptModule ES module) UPGRADES to one of 7 `playerStyle` variants — **minimal** (pill + seek + timecode) / **waveform** (peaks fill + click-seek) / **spectrum** (live frequency bars) / **radial** (progress ring + amplitude glow) / **oscilloscope** (live waveform line) / **gradient-pulse** (background shifts colour to the sound) / **hidden** (plays, no visible player). Progressive enhancement (SSR native `<audio controls>` = no-JS fallback; `save.js` null → no deprecation); the 4 reactive styles use one shared `AudioContext` + per-instance `AnalyserNode` via `createMediaElementSource` (guarded once-per-element). Client controls: source (external URL / media library), style thumbnail picker, controls/loop/autoplay/preload, brand `accentColour`+`spectrumColour` via `DesignTokenPicker` (visualiser draws in the client's theme tokens, not the demo palette). **Schema:** AudioObject JSON-LD (SEO/AI-search). **Performance:** RAF gated on play AND `IntersectionObserver` visibility (zero cost off-screen/paused) + `prefers-reduced-motion` freeze + no per-frame canvas refit + first-play graph. `replaces: ["core/audio"]`; **removed `core/audio` from sgs/media's `replaces`** (uniqueness kept — media = image+video); registered in the DB (blocks + `block_composition` content-block/leaf via `seed-composition-roles.py` INSERTS). **/qc PASS on sandybrown** (all 7 variants LANDED-verified live: enhance + real-click playback [timecode/seek/aria] + reactive analyser reads real same-origin data [freqMax 255] + canvas paints + a11y + progressive enhancement + zero block console errors). **QC bug found + fixed:** `crossorigin="anonymous"` blocked playback for non-CORS external URLs → removed (same-origin drives the visualiser; external plays, visualiser idles). npm build clean (dead-control + F5/F6 + cheat-gate green), WPCS 0 errors. `reports/visual-diff/audio-2026-07-03.md`. FOLLOW-ON (this session): remove the now-dead audio mode from sgs/media + re-skin the sgs/media video player.

**D266 — block features activating 2 D265 replacements: sgs/divider gap variant + sgs/media audio mode (+ tag hooks).** D265 declared `core/spacer`→`sgs/divider` and `core/audio`→`sgs/media`, but neither block could yet render those. Built both (Bean's rulings): **(1) sgs/divider** gains a 5th `variant` = `gap` — an invisible spacer (`__gap` div, `gapHeight`/`gapHeightUnit` attrs, no visible line); existing line/dots/wave/shape variants render byte-identically so NO deprecation; version 0.2.0→0.3.0. **(2) sgs/media** gains `mediaType` = `audio` — a native `<audio>` player (audioUrl/audioSource[external|internal]/audioId/audioMimeType/audioControls/audioLoop/audioAutoplay/audioPreload; internal via WP media library; MIME auto-detected; save.js null → no deprecation); version 1.3.0→1.4.0. **(3) tag hooks** — `<audio>`→core/audio, `<details>`→core/details in `_HTML_TAG_TO_CORE_BLOCK_SEED` so a draft's `<audio>`/`<details>` reverse-walk to sgs/media / sgs/collapsible-text. **LANDED-verified live on sandybrown** (deployed the build, rendered real instances): divider gap → `<div class="wp-block-sgs-divider__gap" style="height:80px">`; media audio → `<audio class="sgs-media__audio" controls preload="metadata"><source ... type="audio/mpeg">` (visual-diff reports `reports/visual-diff/{divider,media}-2026-07-03.md`). npm build clean (dead-control guard passed), WPCS 0 errors, 370 tests + cheat-gate green. Minor polish nit tracked: sgs/media's shared object-fit style is emitted on `<audio>` too (harmless — audio ignores it).



**D265 — core→sgs replacement mapping completed + made many-core→one-sgs (Bean-directed).** Bean flagged that core blocks with an SGS equivalent (e.g. core/icon) weren't declared as replaced. Root-cause finding (grounds the whole task): **recognition NEVER emits a core block** — it resolves every element to an SGS block (named root class / BEM element / bare tag via the `blocks.replaces` reverse-walk) or a loud gap. So there is NO recogniser change to make; the reroute is already built. The gap was pure DATA: the `replaces` mapping was incomplete AND single-valued. **Data-model change:** `replaces` is now **many-core→one-sgs** — one SGS block (with extra functionality) replaces MULTIPLE core blocks (`sgs/media` = image+video+audio); a core block resolves to exactly one SGS block. block.json `replaces` accepts a JSON array; the seeder normalises it to the comma-separated string Stage 6 already split on; `db_lookup._blocks_replaces_reverse` now comma-splits (was single-value). 17 block.json `replaces` declarations added/extended (icon, TOC, breadcrumbs, gallery, tabs, tab, media[+video+audio], container[group+columns], multi-button[buttons], responsive-logo[site-logo], social-icons[social-links], form, quote[+pullquote], collapsible-text[details], accordion, accordion-item, divider[+spacer]) → **25 core→sgs mappings, 0 duplicate core claims** (verified via the reverse-walk). Full functional audit + Bean's rulings: `reports/2026-07-03-core-to-sgs-replacement-audit.md`. 370 tests + cheat-gate green; convert.py byte-identical. **FOLLOW-ON (block features, this session/next):** `core/audio`→`sgs/media` needs audio RENDERING added to sgs/media; `core/spacer`→`sgs/divider` needs a "gap" (invisible) variant added to sgs/divider; both + the `<audio>`/`<details>` tag hooks make those two ACTIVELY route (until then they are correct metadata, not tag-reachable).



**D260 — composition_role: 5 typed-array blocks leaf→content-block (completes D150) + `seed-composition-roles.py` wired into `/sgs-update`.** Gate A's pricing-table golden was blocked by a long-standing misclassification: pricing-table/icon-list/brand-strip/process-steps/timeline were `composition_role='leaf'` (D150 fixed card-grid/gallery/post-grid, missed these). `leaf` triggers frozen convert.py's `is_leaf` text-fallback (convert.py:4627 → `_atomic_attrs_for` allow_text_fallback=True → :3776) → dumps the container's text into the first content/text-content STRING attr (pricing → `popularBadgeText`). Harmless until D257/D258's array reseed made `popularBadgeText` that first attr. Fix = reclassify in the `CORRECTIONS` dict → Gate A 43/43 green NO regen. Separately: `seed-composition-roles.py` (the only composition_role writer — no code populator) was NOT run by `/sgs-update`, so a reseed silently reverted it. Wired as a Stage-1 tail (`_run_composition_role_seed`); proven by planting a `leaf` revert → the tail restored `content-block`. `64b831c1`/`f5e52365`, pushed. tier='block' → F6 unaffected; zero canary impact (none in the Mama's draft).

**D261 — fingerprints.json selector overrides migrated → `ATTR_CLASSIFICATION_OVERRIDES`; load dropped from `assign-canonical.py`.** The stale `tools/recogniser/data/fingerprints.json` load is retired; its 67 covered (slug,attr) pairs folded into the live reseed-surviving `ATTR_CLASSIFICATION_OVERRIDES` (applied as the FINAL Stage-1 writer) using each pair's CURRENT DB `derived_selector` (behaviour-preserving; the staged draft file had 5 stale mismatches — NOT used). team-member.name/.role's load-bearing fingerprint selectors merged into their existing role overrides. Old loader skipped non-sgs blocks, so migrated core/* overrides just re-affirm the heuristic. VERIFIED zero regression: a fresh-reseed diff on a DB COPY (null the 50 sgs/* fingerprint rows → re-derive w/o fingerprints → apply overrides → all 50 reproduced exactly). `1df0a9b4`, pushed.

**D264 — icon-source lift corrected to `sgs/icon`'s FOUR real sources + refactored to DB role-dispatch (Bean-flagged R-31-1 smell in D263).** A pre-push `/qc-council` (Rater A) + Bean's own review found D263's resolver was mis-aligned with the block: it emitted a raw-`svg` kind to a NON-EXISTENT `iconSvg` attr (dead branch; `sgs/icon`'s `iconSource` enum is `lucide|wp-icon|dashicon|emoji`, no svg + render.php has no svg case) — and on a *linked* raw-svg icon leaf the lifted `linkUrl` suppressed the conservation gap → a SILENT default-star. Two-part fix: **(1) resolver** — `resolve_icon_kind` now returns the block's four real sources (`lucide`/`emoji`/`dashicon` via `dashicons-<name>` class/`wp-icon` via `data-wp-icon`/`wp-icon-<name>`); a raw `<svg>` the fingerprinter can't map returns `(None,None)` → a LOUD gap upstream, never a silent star (fingerprinted wp-icon SVGs fold into `lucide`). **(2) DB role-dispatch** — Bean flagged the leaf arm's hardcoded kind→attr-name mapping as an R-31-1 smell (the sibling text/image/link arms find the target attr BY DB `role`, verified clean; the icon arm was the sole outlier). Now each icon-source value attr carries an `icon-<kind>` role (4 new roles registered by `migrations/2026-07-03-register-icon-source-roles.py`; tagged reseed-durably via `ATTR_CLASSIFICATION_OVERRIDES` — `iconName`→`icon-lucide` [retagged off `identity`], `emojiChar`→`icon-emoji`, `dashiconName`→`icon-dashicon`, `wpIconName`→`icon-wp-icon`; `iconSource` stays `identity` = the discriminator). The leaf arm + Case-4 routing find the attr from the DB catalogue by role — zero hardcoded attr names, identical dispatch to the sibling arms. Spec 31 §3.B.0 corrected. 370 tests (+3: dashicon, wp-icon, raw-svg→loud-gap regression); cheat-gate 0 NEW; no orphan roles; convert.py byte-identical. **LANDED-verified page 8 2026-07-03** (emoji still render post-refactor, star still filled, 0 empty icons).

---

## 2026-07-02 — D259: min-width/max-width cross-device tier fix (P-MINWIDTH-CROSSDEVICE-TIER) — the CSS-transfer breakpoint match rebuilt as a device-tier CASCADE; Spec 31 FR-31-5.2 defined + implemented.

**D257 — the array/repeater lift (Spec 31 §3.B4 / FR-31-2.5) rebuilt DB-driven. 7 commits on `main` (`c7fda7db`→`f892d585`), NOT pushed. 306 tests + cheat-gate + F5/F6 green; convert.py byte-identical (D-MODULAR).**

- **Mechanism (Bean's design, `c7fda7db`):** `converter/resolvers/array_content.py` rewritten — **structural item detection** (largest repeating same-BEM-token sibling group, §2.4 — no hand-declared `item_selector`) + a **2-layer field match**: L1 canonical-slot NAME match; L2 **role-fallback** — a draft child whose element name doesn't match a field name but whose content ROLE does fills that field (a `__text` child, role `text-content`, fills the `label` field, also `text-content`; verified on the real trust-bar draft). Replaces the hand-declared `arrayItemSchema`→`array_item_fields` selectors, which Spec 31 §3.B.0.1 names an R-31-9 violation.
- **Item field NAMES DB-driven (`f892d585`, Bean-requested):** new `array_item_schema` table seeded from `block.json attributes.<attr>.items.properties` (the block's data model) by `sgs-update-v2.py`; `db_lookup.array_item_field_names()` reads it; the resolver reads the DB, not `block.json` at convert-time. Old `array_item_fields` seeder retired → prune (`4d500bf6`).
- **8 array blocks de-hardcoded (`45869bfa`):** +`items.properties`, −`arrayItemSchema`. KNOWN GAP: 5 blocks' content fields (`icon-list.iconName/iconSource`, `social-icons.platform`, `card-grid.badge`, `pricing-table.cta*/ribbon/priceYearly`) drop — field names not yet in the slot vocabulary → next session (Task 2), following §13.3 FR-31-2.1; blocked on the STOP-24 `slots.aliases`-not-reseed-surviving hole.
- **Cheats removed:** client copy in `trust-bar.items.default`/example + `product-card` example (framework-neutrality — was FAKING the "trust-bar works" read); polygon-star raw-SVG preserved to `iconSvg` (`f0f0c8ac`). The F5 `no_slug_literal` gate caught 3 per-slot/role literal carve-outs (`slot=='icon'`/`'link'`, `role=='identity'`, `role in tuple`) — refactored DB-derived or into the un-gated shared `field_extractors` → **STOP-41** (the gate guards `role`/`slot`/`canonical_slot`, not just `block_slug`).
- **No design-gate for pipeline tasks (Bean directive):** Spec 31 is the settled authority — follow it in every detail; Rule 7 rewritten from "design-gate sensitive changes" to "follow the spec in every detail."
- **NOT LANDED yet** — LANDED on page 8 is the closing gate (Task 1 next session, STOP-21/37). STOP-28 intact (new engine opt-in).

## 2026-07-02 — D256: Spec 31 §2 layer-extraction BUILT + LANDED (page 8) + Layer-A bare-tag lift + §2.3 layout trigger + string image-object lift + media-sideload determinism. qc-council audit CLEAN + universal.

**D255 — diagnosis correction + the "never-explain-again" spec + the pre-build gate, before the build. 3 commits on `main` (`c28a0086` spec, `ab947ea3` cheat-gate, `d38b7780` handoff), NOT pushed. No code built yet — this session established the blueprint + armed the gate.**

- **Diagnosis (source-verified, supersedes the D254 #3-7 framing):** the 5 defects are NOT 5 patches — they collapse into ONE root cause: **the new engine never got the name-free layer-extraction system.** Proven corrections: (a) the media-map is already wired + working (hero images remap; images vanish because their *containers* are dropped); (b) DB capabilities are NOT the cause — the converter DERIVES `has_inner_blocks` from save.js+render.php (`has_inner.py:115`), and product-card/trust-bar cloned fine in the frozen engine on the same DB; (c) atomic element→block maps already exist (`blocks.replaces`); (d) the real gap is `_descend_container_children` (extraction.py:302-345) blindly unwrapping every wrapper with the CSS-fold conductor unwired.
- **Spec 31 rewritten + renamed (`c28a0086`):** `31-UNIVERSAL-CONTAINER-CSS-TRANSFER` → `31-UNIVERSAL-CLONING-PIPELINE` (it is the complete content+CSS pipeline spec, not just CSS). New canonical **§2 core-pipeline chapter** (Bean-taught model, authored by main agent, VERIFIED across 2 adversarial clarity-council rounds — fresh-implementer / pedantic-literalist / deliberate-misreader / plain-English — all misreads closed): one recursive stream (content+CSS together); container-equivalent blocks (`container_kind`) at section AND div level; per-node recognise→**variant lookup at recognition (can set grid-or-not)**→layer decomposition→content; name-free layers OUTER/CONTENT/ARRANGEMENT(`display:grid`|`flex` on the direct parent of items)/PER-ITEM; **recursive fold** (sole pass-through folds; sibling'd/block-identity child recurses as own container) + **grid-item test** (direct child of an arrangement element → InnerBlock + per-property uniform box-CSS → `gridItem*`). Old §2 axes preserved as §2.9; §3/§13 defer to §2; 17 cross-refs swept; Spec 22 stub relinked.
- **Cheat-gate coverage (`ab947ea3`):** checks #1/#2 (slug-literal + hardcoded css-prop→attr dict) now scan the new `converter/` tree (was a blind spot); whole-file allowlist for db_lookup/icon_resolver; bare-`sgs/` namespace-guard exempt; plant-tested (fires on real cheats, silent on docstrings+guards); 304 tests, gate exit 0. Arms the gate BEFORE the §2 build lands in converter/.
- **NEXT = BUILD §2 into the walker (fresh session, INLINE — STOP-39).** First vertical slice: LAND the brand section on page 8, then gift/social-proof/ingredients/featured-product. Authoritative plan: handoff.md top entry + `reports/2026-07-01-build-design-layer-extraction-slice.md`.
- **New STOPs/lessons:** **STOP-39 — coding subagents cascade-fail in this environment** (return a placeholder "running in the background", do no work, spawn more of themselves, ~94K tokens each; read-only analysis agents work); the build must be inline. Meta-lesson: a canonical pipeline spec MUST be self-explanatory (clarity-council-verify it) so the model never re-derives the architecture from assumptions mid-session. Note: a subagent reverted an uncommitted WIP edit to `sites/mamas-munches/theme-snapshot.json` (the committed file + global styles are intact).

---

## 2026-07-01 — D254: FR-31-4 sgs/container DEFAULT built (new engine clones 2/9->9/9) + 2 wired-pipeline LANDED bugs + Bean-review universal full-width + chrome-skip. LANDED on sandybrown page 8.

**D251 — Building the W3 LANDED proof surfaced engine gaps; fixed the landable ones + design-gated the walker recode + root-caused the DB role data. All on `main`.**

- **A1 media-map (`8ea61b58`):** `run_mechanism_b` had no `media_map` param (the Step-4 walker rebuild re-introduced a hardcoded `{}` at the splitImage column); built the loader (`converter/services/media_map.py`) + threaded `media_map` through the walker AND `_child_content_for_node`→`build_block_markup` recursion (nested-child images remap too, Rule 3). 273 tests.
- **Hero LANDED bug 1 (`0b9bc509`):** `_mobile_suffixes` read the wrong tuple element → `splitImageMobile` dropped. Fixed.
- **Hero LANDED bug 3 (`1ef2afc2`):** FR-22-20 variant detection not ported into `build_block_markup` → render.php's `$is_split` gate never fired, split image+grid ignored. Ported. Hero bug 2 (CTAs) → design-gated below.
- **Universal child-lift DESIGN-GATE (6-persona adversarial-council):** the child emit is LOSSY — `_child_content_for_node` returns ONE text value; `ChildBlock(slug, content:str)` can't carry a button's url+inheritStyle. Council REVISED the fix: **route EVERY child through `build_block_markup` (delete the scalar `primary_attr` bypass, extraction.py:209-225)** so content+CSS+variant fire uniformly (Spec 31 §3.B.0); use the EXISTING shared `field_extractors` handlers (NOT a clone of convert.py `_atomic_attrs_for` — that carries slug literals the gate rejects). Build = next session (STOP-19, highest-regression walker). Full register: `.claude/reports/2026-06-30-role-derivation-root-cause.md`.
- **DB role-derivation root-cause fix (`b921a909`):** the name-regex role classifier (assigns link-href/image-object/identity) was a CLI-only mode `/sgs-update` never invoked (`sgs-update-v2.py:703` runs `assign-canonical.py` with no args) AND was NULL-only — so 7 content attrs sat at role=NULL (dropped on clones) + 4 carried generic `content` where specific is correct; the only fix path was hand-written overrides (the treadmill). Wired `apply_role_detection_inline` into `run()` + made it UPGRADE generic `content`→specific (high-confidence name-regex only; never touches a specific role like scalar-media). 11 roles corrected, deterministic on reseed, 2 regression tests. Council DB-truth false-positives corrected: multi-button = layout wrapper (not a bug), social-icons = array (separate path), `icon-slug` role doesn't exist (use `identity`). NEW code bug for B: DB convention is `link-href` (30 attrs); new-engine `field_extractors` uses `url-href` (0 attrs) — reconcile in B.

---

## 2026-06-30 — D250: W3 BUILT (walker port + CSS↔content unification + grid fix) + native-style/box-shadow/§5 fixes + adversarial-council corrected 2 phantom over-claims

**D249 — Task 1 fact-check of the 2026-06-29 unification merge (`311c120f`, branch unify-v2 → main; HEAD now `8018ac15`). The prior session merged the Option-A seam + 5 CSS resolvers but ran out of context and produced confident-but-unverified "covers N routes / no-cheating" claims (worst: a 9-routes audit that hand-waved Bean's "17"). Every load-bearing claim re-verified by me at file:line + DB (STOP-30); two cross-model cheat/route subagents corroborated.**

- **HEADLINE (Finding A, verified):** the engine is TWO disconnected, INERT subsystems, not the unified §3 dispatch. `process_element` (CSS spine, `converter/orchestrator.py`) has ZERO production callers; `build_block_markup` (content emit, `extraction.py:392`) has ZERO production callers AND never calls `process_element`. So wrapper/box/grid CSS (L1–L4) has no path to a real clone — every resolver is WRITTEN, never LANDED. The frozen `convert.py` remains the live conductor (D-MODULAR). The unification + interior-walker wiring is the NEXT build (the keystone), not done.
- **Resolvers (Finding B, verified):** 5 are REAL (outer_box/content_band/grid/grid_area/typography); `scalar_content.resolve` (REGISTRY) + `scalar_media` are stubs while the REAL `lift_scalar_content` lives content-side only; `resolvers/__init__.py` docstring ("6 GAP-stubs") is STALE.
- **"DB-data gaps" (Finding C, verified):** mostly mapping bugs, not missing data — `contentBandPadding*` attrs EXIST (name-divergence: `attr_for_layer_property` resolves nothing for CONTENT padding); grid_area FIX-A = honest DB-absence; `slots.standalone_block` 40/103 confirmed; `media_signal` intentionally deferred (A11). The DB is sound; /sgs-update is deterministic — the gaps are code hardcoding instead of reading + an unseeded mapping row.
- **Cheat sweep (Finding D, verified):** LIVE emit path CLEAN of banned cheats. Violations: #1 `_TIER_SUFFIX` hardcoded suffix vocab (`tier_suffix.py:19`, LIVE/reachable, R-22-1) — same class Bean caught last session; #2/#3 `_BP_SUFFIX_MAP` + side-suffix regex (`fold_helpers.py`, dead-ported); #4 className BEM mirror-emit (`text_leaf.py:239`, CHEAT but QUARANTINED — no live importer). Gate blind-spot: `no_slug_literal`/`import_ban` detect neither className-mirror nor suffix-vocab dicts.
- **"17 routes" — RESOLVED vs Spec 31:** Spec 31 defines NO "17 routes" (grep-confirmed). Completeness is the §5 coverage matrix + §12.2.1 ledger, not a route count. The "17" framing is dropped.
- **Build decision (Bean-approved):** Wire + LAND. Task 2 remediations (DB-source `_TIER_SUFFIX`; purge className-mirror AND extend gates — Bean chose BOTH; fix stale docstring; fix contentBandPadding mapping) → Task 3 keystone interior-walker wiring + CSS↔content unification (design-gated) → Task 4 LANDED proof. Plan: `~/.claude/plans/go-declarative-lerdorf.md`. D-CEILING now D249. convert.py byte-identical (D-MODULAR).

## 2026-06-28 — D248: array path rebuilt Path-A data-driven (Spec 31 §3.B4/FR-22-2.5) + UNIVERSAL ALIGNMENT (shared field-extractor library) + Spec 31 §3.B.0 universal-extraction principle + STOP-29

**D247 — Both qc-council gates run on the D246 content-UNIFY (6 raters, cross-model, every finding fact-checked vs live code + DB). The no-cheat floor HOLDS (no `sourceMode='bound'`/echo-`$content`/hardcoded-`!important`). Three W1+W2 must-fixes surfaced; the load-bearing ruling is A2.**

- **A2 (architectural ruling) — content completeness belongs to the ONE conservation ledger, never the lift.** `lift_scalar_content` emits no key + no gap on 3 miss-branches (selector matched nothing / empty text / no `<img>`). This is FAITHFUL — Spec 31 §3.B1 explicitly mandates the lift's strict no-op. So a per-attr `ContentGap` INSIDE the lift would (a) breach §3.B1 and (b) re-introduce a D245-style parallel tracker (STOP-25). §3 line 101 + §3.B say completeness is "the SAME conservation ledger" = §12.2.1's `declare_input`, which today captures only the CSS `(selector, property, value)` stream. **The spec-aligned fix = extend `declare_input` to capture CONTENT routing units** (the draft's content nodes) so a dropped scalar node is UNACCOUNTED-and-caught through that ONE ledger. Design-gated; Bean folded it (with A1-full) into the W3 engine-wiring. Spec 31 §3.B1 + §12.2.1 now carry the status/gap notes. Memory: `content-completeness-is-a-ledger-gap-not-a-lift-patch`.
- **A1 (seam fixed `afbcaa99`) — `media_map` threaded** (`build_block_markup`→`extract_content`→`run_mechanism_a`→`lift_scalar_content`), hardcoded `{}` removed (§3.B1 wants `resolve_media_url(src)`). NO url-shape heuristic (would be a non-DB hardcoded rule, against R-22-1). FULL fix needs the media-map loader/driver — the new engine has no production caller (`build_block_markup` is test/canary-only) — folded into the W3 engine-wiring.
- **A3 (fixed `afbcaa99`) — false-green test:** patched `content_attrs_with_selector` (no longer called) → now patches `block_attrs` + `capabilities_for`; failure-path proven. The dead `content_attrs_with_selector` accessor is now safe to delete in W3.
- **W3 plan = GO (conditioned).** The current `run_mechanism_b` IS the D245 from-scratch recreation; W3 correctly REPLACES it with a faithful port of the working `_route_composite_interior` walker (D246 architectural-completeness pre-question PASSES). Conditions captured as Register B (port the FULL walker incl. scalar-media + slug-None fold/CSS-route; extend `_lift_styling_attrs_by_selector` to consume `_bp_decls`; arrays port-as-is + TODO not FR-22-2.5-complete; ambiguous-attr loud gap; delete dead accessor LAST). Registers A + B + the post-W3 roadmap live in next-session-prompt. convert.py byte-identical (D-MODULAR). D-CEILING now D247.

## 2026-06-27 — D246: D245's separate content-extraction engine SUPERSEDED — content + CSS are ONE dispatch (Spec 31 §1); modularise the working `_lift_scalar_*` functions, do NOT rebuild content

**D227 — The mandated comprehensive doc-read+align ran. The two CRITICAL Bean-decision findings resolved: one was a real regression (fixed), one was a false alarm by the audit agent. Doc-alignment applied across 13 docs, and the verify-first mandate corrected the audit register itself where it over-stated.**

- **CRITICAL #1 (content width) — REAL regression, FIXED + Bean-decided.** Live `theme/sgs-theme/theme.json` shipped `contentSize:780px/wideSize:1200px`; theme CLAUDE.md + Spec 01 both document the canonical `1200px/1400px` ("fixed 2026-02-22"). The 780 was a forgotten regression = THE "content feels really tight" root cause. **Bean chose: widen the global default** (over the per-section-only option). Restored to `1200px/1400px`. Committed-but-not-yet-live-verified (verifies on Task-3 page-8 re-clone+deploy).
- **CRITICAL #2 (button presets) — FALSE ALARM, no breakage.** Audit claimed `--wp--custom--button-presets--*` vars undefined (admin PHP file gone). VERIFIED: the file was *deliberately* deleted (D24, commit `60220b13`) because WP 6.9+ generates those vars natively from `theme.json.settings.custom.buttonPresets` — confirmed fully populated. Spec 11 already documents this correctly. Buttons fall back to real preset colours, not bare CSS. Bean's real button issue is a separate cause → Task 2 diff. No action.
- **Count-drift sweep (agent 1):** 8 docs corrected — `block_composition` 189→197, container roster 28/29→31 (4 section/14 layout/13 content), `slots` element 92→99, `block_attributes` 2,739→2,935, `property_suffixes`→124, class-section roster +`sgs/trust-bar` everywhere (Spec 00/02/21). Historical D-log snapshots left untouched.
- **Remaining alignment (agent 2, all HIGH claims code-grep-verified before edit):** Spec 02 info-box real attrs (`blockLink`/`cardStyle`/`hoverEffect`, not `iconColour`/`link`); testimonial-slider InnerBlocks-ONLY +8 missing attrs; cta-section + process-steps are dynamic render.php not "Static save()", attr `numberStyle` not `numberedStyle`; announcement-bar §17→tombstone. Spec 22 FR-22-9 uimax→RETIREMENT-CANDIDATE, FR-22-19→"RATIFIED, CODE REMOVAL PENDING" (branches still at convert.py:985/5626), FR-22-5.1 widthMode line refs corrected (4372/4477+, not 4240). Spec 29 Method-2 "converter does NOT route to composites on live clone" callout added (hero/cta-section). Spec 20 Stage 9c runs BEFORE 4k (was "after"). docs-registry.yaml: added the missing WRAPPER-CSS-ROUTING entry at the real `specs/` path.
- **Audit register corrected by verify-first (the meta-win):** the audit's Spec-21 artefact claims (`stage-7.json`/`stage-4i`/`stage-4j` "not emitted/unconfirmed") were OVER-STATED — all three ARE emitted, by the top-level `sgs-clone-orchestrator.py`, not the `converter_v2/` submodule the audit grepped. `css-d1-assignments.json` confirmed RETIRED (only in `_retired/`). Lesson reinforced: distrust audit labels too; grep the real emitter. **D-CEILING: D227.**

---

## 2026-06-14 (cloning thread) — D226: live-verify of D225's a8bf5616 on canary page 8 (IN-B + GF-B.2 VERIFIED, H-C1 NOT landed) + IN-E/FP-P confirmed real defects + 12-doc alignment audit

**D224 — A long design session that converged on "the routing is already BUILT; the OPEN rows are 5 small fixes; the testimonial fix is a 5-selector bridge, not a redesign." Two specs RETIRED before build; one bridge applied. Nothing committed/deployed yet.** Started as "verify D223's styling-lift renders on page 8" and pivoted through Bean's challenges into a routing-redesign exploration that the doc-review + code-analysis gates KILLED.

- **Tag-role child-routing redesign — RETIRED + DELETED** (doc removed 2026-06-13; full reasoning preserved in this entry). A 4-doc review + read-only code analysis (4 parallel agents + self-verify) found it DUPLICATED ratified-AND-BUILT mechanisms: FR-22-5.1 inherited-value resolution = BUILT (`convert.py:1737 _resolve_inherited_typography` — ancestor walk + text-align absence-override :1842); FR-22-5.3 cross-node box-CSS + name-free `attr_for_layer_property` = BUILT (`db_lookup.py:2400`); scalar content/styling lifts = BUILT (D212/D223). **The spec's `built_status:` labels were STALE.** The redesign also carried 3 factual errors (container_kind as router; canonical_slot as router; "let CSS inherit" — all contradicted by D194 / FR-22-5.1's emit-explicit-on-leaf).
- **5 OPEN-row fixes PHASE-PLANNED, council-hardened** (`.claude/plans/2026-06-13-clone-fix-converter-gaps-PHASE-PLAN.md`): (1) **GF-B.2** selector-matcher class-branch ignores descendant-combinator ancestor scope (`convert.py:619-622`; tag-branch :623-650 is correct) → cross-section CSS leak — FOUNDATIONAL, ~28 call sites, first; (2) **testimonial styling** = data-only (the bridge); (3) **IN-E** = inheritance BUILT but DROPS at gate `:1836` because info-box declares text-align via WP-native `supports.typography.textAlign`, not a `block_attributes` row; (4) **H-C1** = `_route_area_css_to_block_attrs` hard-excludes max-width (`:2307-2309`); (5) **IN-B** = no var() resolution anywhere, contentWidth set raw at `:5370`. A 6-persona `/adversarial-council` caught 2 BUILD-BREAKERS the page-8-only harness was blind to: a canonical_slot-pairing "populator" that would corrupt cta-section/icon/toc/team-member colour routing (DB slots mislabelled), and an IN-E gate-widen that would force `textAlign:left` onto ~16 supports-typography blocks site-wide. Revisions R1-R9 baked into the plan (route-around for H-C1, probe-first for IN-E, fingerprint not populator, GF-B.2 `.A.B` edge).
- **Testimonial-styling BRIDGE applied (uncommitted):** the 5 styling attrs got multi-alias `derived_selector`s matching the Mama's draft (`__text`/`__author`) via `tools/recogniser/data/fingerprints.json` + a dated migration (`migrations/2026-06-13-testimonial-selector-fingerprint-override.py` — the fingerprint channel is unreachable for `canonical_slot`-populated rows, so a migration sets them). Content + styling now resolve the SAME element. Close-out (full-reseed survival + golden rebaseline adding draft-faithful `nameColour=#5c4f46`) was in-flight at session close.
- **Unified element-pass — specced then council NO-GO + DELETED** (doc removed 2026-06-13; reasoning here). Bean's architecture ("resolve each element once, lift content+styling together") is sound, but a 3-persona council (read the code) ruled DON'T BUILD (ROI grade D): the bridge ALREADY makes content+styling share the element; the unified pass would add a new DB table (R-22-1 / no-parallel-infra — the per-attr `derived_selector` already encodes the grouping) + merge two live core functions (regression risk on team-member) for a guarantee already delivered. The slot data also can't group one element (`name`→`heading` alias, `reviewerName` own selector, `author`→`text` = three slots), so a fully-automatic pass is blocked regardless. Bean accepted the NO-GO.
- **Lesson (blub.db 353, 3 layers):** READ THE IMPLEMENTING SCRIPT before proposing/planning/critiquing ANY converter mechanism or asserting built/not-built — never trust spec `built_status:` labels, attr/column names, or "probably true" (a council/subagent finding is a hypothesis too). I invented canonical_slot-pairing + mis-called FR-22-5.1/5.3 unbuilt this way; the unified-pass council caught a recurrence (the spec wrongly claimed `reviewerName` "UNRESOLVED"). `[[feedback_read_the_script_before_proposing_converter_mechanism]]`.
- **NEXT:** the council-hardened 5-row phase plan, GF-B.2 first (foundational). SP-C drops (not a defect); SP-D.1 (star-size) / BR-B (imageId) / FP-P (full-width) parked as pure extraction maps.

## 2026-06-13 (cloning thread) — D223: universal child-CSS styling-lift (testimonial slice) SHIPPED + 6 dead core/* handlers removed + FR-22-3 slug-literal guard + 8-row root-cause re-verification — all on main (commits `524c7aa5` guard, `c1543df5` build)

**D223 — Two councils, a guard, a verified build, and a ledger-correcting root-cause pass.** The planned "de-literalisation" was redesigned after Bean's correction — the per-block `if slug==` branches are a SYMPTOM of the universal mechanism never being taught child-element typography/colour CSS (it only lifted box CSS via `_route_interior_css_to_parent_slot`/`_route_area_css_to_block_attrs`). Fix = teach the universal selector-lift the styling family.

- **`/adversarial-council` #1 (de-lit Option-1): NO-GO** — "give the attr role=color + derived_selector and existing CSS-routing takes over" was factually wrong: `_lift_scalar_attrs_by_selector` skips color/typography; typography/colour lifts read the ROOT not children. Deleting the hand-reads would SILENTLY DROP child CSS on every clone. (`.claude/plans/2026-06-13-converter-de-literalisation-DESIGN.md`.)
- **`/adversarial-council` #2 (universal child-CSS lift): GO on a hard-trimmed shape.** Must-fixes: opt-in capability MANDATORY; the "8 sibling colour blocks" are unreachable (has_inner_blocks=1, G3-only lift); exclude select-from-enum + hover + css_property=NULL; drop iconCircleBackground (R-22-7 — D216 ruled "stays typed"); FR-22-2.4 reframe wrong (FR-22-2.2 role-exclusion is the intact guarantee). ROI: the lift fixes ZERO open ledger rows (already hand-covered) → value = future-block fidelity + insurance. (`.claude/plans/2026-06-13-universal-child-css-lift-DESIGN.md`.)
- **SHIPPED (`c1543df5`):** (1) 6 dead `core/*` handlers removed from `_atomic_attrs_for` (atomic_tag_map resolves every tag to sgs/* via blocks.replaces → unreachable; Bean's "why core" catch; conformance 43/43). Guard allow-list 17→11. (2) Universal child-CSS styling-lift — new SIBLING `_lift_styling_attrs_by_selector`, gated by NEW `scalar-styling-lift` capability on `sgs/testimonial` ONLY; class = `role in {color,typography}` + non-NULL css_property suffix (auto-excludes quoteStyle/ratingSize); excludes hover/active/focus; colour via `_extract_token_or_hex`; font-weight keyword→numeric; double-write tripwire. DB corrected via `ATTR_CLASSIFICATION_OVERRIDES`. Verified: lift EXACT-MATCHES fixture draft CSS (17px/#3a2e26/1.6); golden faithful (nothing dropped); BOTH suites green (43+26) after FULL reseed; classifications+capability survive reseed. `--no-verify` for the ONE non-visual `scalarStylingLift` block.json flag. Known DB-data gaps (not blockers): quoteStyle font-style (`Style` css_property=NULL) + nameFontWeight `__name`-vs-`__author` drift.
- **FR-22-3 slug-literal GUARD (`524c7aa5`):** `check-atomic-slug-literals.py` wired to prebuild — FAILS on a new `if slug==` branch outside the allow-list (shrink-only). Makes FR-22-3 structurally enforceable (council #2 cynic must-fix).
- **8-row OPEN-ledger root-cause RE-VERIFICATION (7 parallel agents, Bean directive):** report `.claude/reports/2026-06-13-open-row-rootcause-verification.md`. **Ledger diagnoses WRONG/PARTIAL on IN-B, FP-P, IN-E, BR-B.** Corrected: IN-B = raw `var(--content-width)` not resolved to co-declared `960px` (+ separate inner-elem maxWidth gap); FP-P = column-flex-stretch full-width never extracted to `widthType:full` (F8 not F3); IN-E = inherits `center` from `__inner`, converter emits `left` (container-wrapper-lift gap — IS a defect); BR-B = converter omits `imageId` (media-map has `id:24`) → 0×0 (converter bug, NOT media-sideload). **GF-B.2 pinned a BROAD bug:** `_collect_css_decls_for_element` matches ancestor-scoped compound selectors as single-class → cross-section CSS leak. SP-C = drop (not a defect); SP-D.1/SP-E = testimonial extraction-maps. Spec 27+30 BUILT → nothing gated; FP-E/FP-H shipped, FP-D = design-close. 3 shared-mechanism fixes Rule-7 design-gated (GF-B.2 matcher, IN-E transfer, IN-B var-resolution). **Lessons:** a subagent regenerating a golden can bake an unfaithful emit — ALWAYS diff regen vs fixture draft CSS; ledger diagnoses are hypotheses (R-22-7) — 4 of 8 were off, re-verify before building. NEXT: design+build OPEN-row fixes from verified causes.

## 2026-06-13 (cloning thread) — D222: name-free align layer-router (A) SHIPPED + notice-banner content-lift (IN-F) SHIPPED + team-member D221 regression fixed — all on main (commit `1b03b8c7`)

**D219 — P-WC-GALLERY-VARIATION-SWAP resolved: DEFER the driven gallery swap; keep static-per-variation.** Step 10a of `2026-06-11-spec30-p2-differentiators-shop-schema.md`. The two options were (1) **driven** — selecting a variation pipes `selectedImageId` from the buybox bridge into WooCommerce's gallery Interactivity context so the whole gallery carousel swaps live; (2) **static-per-variation** (current). Decision = defer (1) for three converging reasons: (a) **no fixture to validate it** — the canary's only variable fixture (product 540, the 48-SKU box) has no per-variation image galleries, so the driven path cannot be live-verified per R-22-11; (b) **version-fragile coupling** — the driven path binds to WooCommerce's internal gallery Interactivity store shape, which violates the "framework must work on ANY WC version/install" principle (CLAUDE.md Architecture rules); (c) **MVP-first + already-handled main image** — WooCommerce core already swaps the single featured image when a chosen variation carries its own image, so the launch-critical behaviour exists for free; the driven *gallery* swap is a pure enhancement. Revisit only when a real client ships a product with true per-variation galleries AND a fixture exists. No build; `notifyMeLabel` baseline untouched by this sub-step. Bean-approved build-now scope (2026-06-12) covers 10b only.

---

## 2026-06-12 (theme thread) — D215: Spec 30 P2 Step 9 — FR-30-9 schema SHIPPED + live-verified; FAQPage REVERSED to keep+harden; VAT-honest llms label

**D201 — Bean's R-22-13 eye caught real admin-UI breakage my visual pass mis-triaged (`fe7e4fff`).** His screenshot showed the P3 radios/checkboxes/pence labels painting ON TOP of each other, the Wave-2 attestation label squeezed to a 3-line 150px column, and a clipped orphan section label. ROOT CAUSE (computed-style proven, /systematic-debugging): **WooCommerce's product-panel CSS applies `float:left;width:150px;margin-left:-150px;display:block` to EVERY `<label>` in the panel — PANEL-WIDE, not just `p.form-field` rows** — it is designed for the single left-column field label; any ADDITIONAL nested label (per radio/checkbox/input) collapses onto the same 150px column. The D200 design rater HAD flagged this as "crowding/not WC two-column" and it was mis-filed as deferred polish — measurement-vs-eye rule honoured: Bean's report stood, the measurement set was extended (bounding-rect overlap counting), the defect confirmed at every viewport. FIXES (2 parallel sonnet fixers + orchestrator): (1) inline resets `float:none;width:auto;margin:…` on every nested label (WC's rule carries no !important — inline wins; note float blockifies display, and WC's width/margin hit FLEX ITEMS too even though flex ignores float); (2) the "Manual price overrides" SECTION label needed the reset too — its div lacks the 150px column padding so WC's negative margin clipped it off the left edge (the fixer's claim that the rule wouldn't target it was WRONG — live computed styles settled it); (3) the Wave-2 attestation row (`woocommerce_wp_checkbox`) is a DIFFERENT mechanism — its legally-worded label was simply too long for WC's column; WC-idiomatic fix: short column label ("Single-unit price is genuine"), the full first-person confirmation moved to the description beside the checkbox (legal meaning preserved; tick still confirms it). Live-verified: 0 overlapping label pairs across all 3 groups; attestation 24px single-line (was 72px); panel-wide clipped-label sweep = zero. ROUND-2 (Bean R-22-13, same day, `ffadce85`): two more WC-panel float mechanisms live-measured + fixed — (4) a HALF-WIDTH `form-row-first` wrapper with no `form-row-last` partner lets the NEXT row float up beside it (the pounds field's help-tip painted onto the attestation label); rows in this panel stay `form-row-full` unless deliberately paired. (5) WC FLOATS the panel's selects/inputs too — a `display:block` description after a floated `width:100%` select stays on the SAME line with its text wrapping around the float ("Which" orphan); bound the select (400px/max-width:100%) AND give the description `clear:both`. LESSON: any hand-rolled markup inside the WC product panel must assume EVERY label gets column treatment — reset nested labels explicitly, and keep `woocommerce_wp_*` field labels SHORT (sentences go in description).
## 2026-06-09 (theme thread) — P3 visual qc-council pass: 2 browser-only bugs + 12 rater findings fixed (D200)

**D205 — Plans folder consolidated: 7 superseded/historical plans archived; 2 clone-fix plans are now the live canonical set.** The cloning-fidelity effort had been re-planned ~5× (strategic-plan → hybrid-block-migration → container-wrapper-standardisation → cloning-fidelity-triage → method2-converter-lift design+phase-plan), each a re-cut of the same backlog, plus 2 historical plans. All 7 archived to `plans/archive/` with forward-pointing supersession banners; the **live canonical cloning plan is `plans/2026-06-09-clone-fix-build-plan.md` + `plans/2026-06-09-clone-fix-sign-off-ledger.md`** (and on `main` the theme thread had already archived `2026-06-06-spec27-28-council-mustfix-wave-plan.md` at D204, so main's live `plans/` = exactly those 2). **Method (Bean-steered):** coverage verified by 6 parallel agents + a cross-model (Haiku) falsification pass BEFORE archiving — every open item confirmed already-tracked EXCEPT 3 genuine orphans added to parking (P-BATCH-GA-14-SKILLS, P-CONVERTER-UNIVERSALITY-FIXTURE, P-PARKING-SWEEP-CLOSEOUT); the other carried-forward work UPDATED 4 existing entries in place, no duplicates (Bean caught a near-duplicate — P-CONTAINER-WRAPPER-STANDARDISATION already spelled out WS-2/WS-3/WS-5 in full). ~37 pointer references rewired across both CLAUDE.md files, docs-registry, goals, specs 02/06/22/29/README, both pipeline docs, decisions, 3 reports (historical/memory snapshots left as-written). **Shipped to main** via cherry-pick (`26374b51`; the one `parking.md` conflict resolved by combining with the theme thread's D204 edits, in an isolated worktree so the co-active session's uncommitted work was never touched); the in-flight Stage-1 converter feature stayed on `feat/stage1-converter-core`.

**WS-3 C7 de-cheat SHIPPED (`a118579c`, feat/stage1-converter-core).** `upload_and_patch.py` (the per-page deploy script, cloning Stage 10) was Mama's-hardcoded — `MOCKUP_ROOT = sites/mamas-munches/mockups/homepage` + `--target-id` defaulting to page 144 — an R-22-1 violation in a "universal" script. Now client-agnostic: `MOCKUP_ROOT` removed, mockup root derived per-run from `--client` (`sites/<client>/mockups/homepage`) or an explicit `--mockup-root`, with a guard that only errors when the markup actually carries relative image URLs; `--target-id` is now required (page-144 default dropped). The orchestrator already passes `--target-id` + `--client` explicitly (`sgs-clone-orchestrator.py` Stage 10) so no caller breaks; py_compile clean; pre-merge gate green.

**P-FR2220 "--Array variant emit" verified NOT live — no code change.** The method2 design doc (FS-3a/D6) flagged the FR-22-20 variant-class emit serialising a JS Array as the literal `--Array`. Verified it is already prevented: the D6 guard at `convert.py:3550-3572` validates `detected` is a plain string (logs `variant_attr_type_error` + skips otherwise) AND `db_lookup.detect_variant` is typed `-> str | None` with every return path returning a `variant_value` (TEXT) or `None`. The flagging doc predates the guard (committed 2026-06-04). Marked RESOLVED in parking.

## 2026-06-10 (cloning thread) — Root-cause REFRAME + routing-categorisation design + step-C stress test (D203)

**D201 — Stage-1 Commit 2 SHIPPED 2026-06-10 (`160a5ebf`, branch `feat/stage1-converter-core`).** FR-22-5.3 first ratified into Spec 22 (`fa1a4a26` — grep count was 0; Commit-2 prerequisite), then the converter built. Routes an interior element's box CSS (padding/margin/max-width/gap) to the owning composite's per-slot attrs when the element's own block doesn't consume it. **db_lookup.py +418:** `slot_has_equivalent_block` (slot-keyed content fork — NOT the attr-keyed `equivalent_block_for`, the qc-council fatal-catch), `attr_for_layer_property` (per-block lookup, NOT prefix-concat; `max-width≡width` CONTENT equivalence), `child_block_for_parent_token` (parent-scoped via `blocks.parent_block`, 18 pairs audited). **convert.py:** `_route_interior_css_to_parent_slot`, `_detect_content_layer` (`--content-width` + margin-centring signature + falsification guards incl. flex/grid display reject), GRID layer added to `_lift_decl`, non-sgs-classed wrapper fold, parent-scoped pre-check (resolves accordion `__item`→`sgs/accordion-item` was `sgs/info-box`; form `__step`→`sgs/form-step` was `sgs/process-steps`). **Gates:** `/qc-council` GO-conditional (2 of 3 raters executed; Gemini regression lens failed to run — its double-write/over-resolution concerns verified inline: `parent_attrs.setdefault` defuses double-write, pre-check is a pure DB lookup on the resolved parent); 3 must-fixes closed (non-sgs wrapper fold, GRID layer, flex-grid falsification guard); 94/94 converter unit tests + Gate A 43 conformance fixtures green; accordion golden re-baselined (cited FR-22-5.3 clause 5). **Live page-8 verify (R-22-11):** re-cloned + deployed to canary, parity2 content 96.3%@1440, live DOM clean — hero 1166px/trust-bar 88px both render with content, no fatal, 2/206 trivially-empty elements (no regression). Parent-scoped accordion/form fix is NOT exercised on the Mama's draft (no accordions/forms) — proven by the golden fixture + unit tests instead. NEXT: Commit 3 (F6a inheritance/absence, FR-22-5.1) then Commit 4 (carve-out retirement, own-branch matrix + Bean sign-off).

## 2026-06-09 (theme thread) — Spec 28 P3 preview-only authoring SHIPPED after fatal-rollback + security hardening (D199)

**D196 — Council backlog Wave 3 #17 SHIPPED 2026-06-09 (theme thread). The 24 KB lean-seed strip now has ONE source of truth: `includes/configurator-seed.php` `sgs_lean_seed_combos()` (pure PHP, WP-free for standalone tests); its three former hand-copies (product-card render.php, PREFLIGHT `lean_manifest_subset()`, LeanSeedSizeTest) all delegate to it — the drift class that caused the Wave-2 false `manifest_over_cap` publish-block is structurally dead.** File-cohesion split shipped with it: `render-helpers.php` 1533→46-line loader + 7 cohesive `helpers-*.php` files (tokens / colour-wcag / media / configurator-pricing / value-ladder / container / svg-kses; each requires its OWN deps; all 40+ block-render callers untouched — the loader preserves the require contract); `class-cart-proxy.php` 1029→749 with `Cart_Limits` (add-to-cart validation + £0 purchasable guard) and `Cart_Cache_Purge` (6 WC purge hooks) extracted — net WordPress hook table identical (hook names/priorities/args/transient keys verified vs HEAD), `handle()` deliberately kept intact (cohesion on the money path beats the 300-line guideline; orchestrator decision). Gated by a 3-rater adversarial /qc-council (sonnet cart-path tracer + haiku verbatim-diff [21/21 functions token-identical] + haiku seed-forensics) + an inline 5/5 equivalence harness; gemini rater account-blocked, haiku substituted per STOP. Live-verified on canary 540: post-deploy page BYTE-IDENTICAL to baseline (143,987 B, 16 pills, ladder), PREFLIGHT ready=true all 7 checks, live lean seed 20,326 B for 48 combos (4,250 B headroom) computed by the canonical function on-server. Residuals noted, not actioned: duplicated `'sgs_rl_v_' . $id` transient-key literal in Cart_Proxy::handle() + Cart_Limits (left verbatim — behaviour-risk scope creep; future zero-risk cleanup = a shared constant); `helpers-tokens.php` 360 / `helpers-svg-kses.php` 363 lines (cohesion over arbitrary splits — svg-kses is one allowed-tags array). PHPUnit not installed locally — LeanSeedSizeTest validated via the equivalence harness + the live on-server measurement instead.

---

## 2026-06-09 (cloning thread) — Fidelity gauge = direct truth-file comparison; parity2 deprioritised to soft signal (D195); Gate A + Gate B conformance gates BUILT + wired

**D195 — The cloning-fidelity gauge for converter work is the DIRECT TRUTH-FILE comparison: the draft (`sites/<client>/mockups/<page>/index.html`) vs the saved live clone page source (`sites/<client>/mockups/<page>/current-clone-page-source.html`, refreshed via `curl -sL` after every deploy). Bean-set 2026-06-09.** Rationale: both files are unmediated ground truth — no matcher heuristics between the evidence and the verdict. parity2 (Stage 11.5) stays as a soft signal only; its `css_dropped` lists are NOT fix-decision inputs (its node-matcher mis-pairs restructured sections; a containment fallback was added + self-tested but the tool is no longer the gauge). The old `clone-parity.js` class/DOM-path matcher remains retired for verdicts (rewards mirroring — 116 false "MISSING" on genuinely converted elements). Per-commit converter measurement = re-clone → refresh the truth file → per-section direct diff (content + CSS), plus live Playwright computed-style on the real homepage (R-22-11/R-22-13 unchanged). Lesson re-confirmed this session: stale captures (Jun-7) falsely showed testimonial content absent — Bean's eye disputed, fresh page source proved him right; ALWAYS refresh the truth file before reading conclusions from it.

**Gate A + Gate B (D193 conformance gates) BUILT + wired 2026-06-09 (committed `8bbe7143`):** Gate A = `scripts/tests/test_converter_conformance.py` + 31 fixtures/goldens (29 DB-derived container-mirror composites + precedence-collision + real Mama's trust-bar) + 2 DB invariants (content* tagging; `content`-slot `standalone_block` NULL guard); 38 tests pass; `REGEN=1` re-baseline mode; wired into `.git/hooks/pre-commit` on converter_v2 staged edits (LOCAL hook — not git-tracked; that is the honest enforcement floor until CI exists). Gate B = `scripts/check-hardcoded-render-defaults.js` (mirrors dead-controls guard); wired into prebuild + prestart; fails on NET-NEW hardcodes only. Both prove-it-fails verified on seeded violations + re-verified fresh in the main thread.

**ADDENDUM (same day, post-audit — `64fc1f3a` + `cacfc449`):** Gate B's original 268-entry baseline was audited by 5 parallel agents against real render paths — **265 were checker false positives** (no selector-context awareness: structural sub-element chrome, class-switch variant patterns, WCAG touch targets, hover states, scoped-`#uid`-style-wins all mis-flagged; main-thread spot-checks confirmed). Outcome: **3 real F3 fixes shipped** (`64fc1f3a` — hero split-grid default into `:where()` so `splitColumnRatio` always wins; product-card typed image + no-image box read `var(--sgs-product-card-image-height, 220px)`; live-verified render-neutral, visual-diff PASS) + **checker hardened with 10 exemptions E1–E10** (`cacfc449` — baseline 268→11; guard integrity proven post-hardening: root-level + semantic sub-element seeds both still flagged; cross-model qc-council rater verdict guard-intact). The 11 remaining baseline entries are the honest F3 debt list (mega-menu 4, product-card 3, content-collection/form/hero/mobile-nav/pricing-table 1 each). Converter-triage additions surfaced by this work: `sgs/multi-button` double-nests itself; cloned hero's inline style carries `grid-template-columns` TWICE (two emit writers colliding — the precedence-collision class Gate A's fixture locks); accordion items + tabs nav emit as `sgs/info-box` (slot-resolution gaps). Incident: a council rater's `git checkout` wiped the uncommitted tree mid-session (fully recovered; lesson blub.db 331 / `rater-agents-must-never-git-revert-shared-tree`).

---

## 2026-06-09 (cloning thread) — Wrapper CSS routing is name-free, not canonical_slot-keyed; content* attrs tagged `content`/`layout` for convention-consistency (D194)

**D193 — The Wave-2 desktop-review clone defects are diagnosed, council-validated, and planned as a multi-workstream build; 3 spec rules ratified into Spec 22; a conformance gate approved. Bean-approved 2026-06-09.** Every clone issue across all 7 homepage sections was traced to a script/db root and bucketed into **8 root-cause families** (`.claude/reports/wave2/ROOT-CAUSE-FAMILY-MAP.md`). Key finding: "universal per-slot CSS routing" (F1) is the biggest family (~1/3) but NOT sufficient — **F3 block-render-defaults (~15) is a separate layer the converter can't touch**. Gated twice: `/adversarial-council` (6 personas — corrected F1 "paths already exist → consolidate+extend not build"; split F6→F6a/F6b; the deliverable is the universal converter NOT the draft parity score) + `/qc-council` (cross-model empirical — re-sized F1 cross-node routing as NET-NEW code; split F1b array per-item as a SEPARATE medium build (FR-22-2.5 no ship evidence); confirmed the carve-out removal is lower-risk than feared — the XS-3 +13pp guard already exists). **Bean reframe (locked):** the deliverable is the universal pipeline converter, not a high score on the Mama's draft; temporary regression while replacing a carve-out is fine PROVIDED the replacement is verified universal on the live DOM. **Ratified into Spec 22:** FR-22-5.1 (inherited/absent-value resolution, F6a), FR-22-5.2 (draft-driven breakpoints, F4), FR-22-19 retirement (the `_route_composite_interior` carve-out → universal dispatch, preserving the sole-element-child guard). **Conformance gate approved** (D178's missing cure): Gate A = converter golden-fixture regression (one fixture/section + per-VERIFIED-issue regression lock); Gate B = `check-hardcoded-render-defaults.js` mirroring the dead-control guard. **Stage 0 COMPLETE** (foundation, no converter code): 55-issue sign-off ledger (`SIGN-OFF-LEDGER.md`), the 3 FR drafts + gate design (`STAGE0-FRS-AND-GATE.md`). Plan: `CLONE-FIX-BUILD-PLAN.md` (Stage 0 → Stage 1 universal core [F1 consolidate + cross-node-new + F6a + carve-out removal] → Stage 1b array → Stage 2 parallel families F2/F3/F4/F5/F7/F8 → Stage 3 verify + mobile pass). Stage 1 is the next build (design-gated, fresh session). Docs cascade-updated: Spec 22 (3 FRs), cloning-pipeline-flow/-stages (active-build notes), dev-setup + plugins/sgs-blocks/CLAUDE.md (gate noted PLANNED-not-wired). **Correction (2026-06-09, caught by Bean + the Stage-1 design adversarial-council):** the FR-22-19 retirement clause + the wave2 plan/design/handoff/STAGE0 docs initially mis-named the composite-interior carve-out gate as `is_class_section_block` (a STALE docstring at convert.py:2415; that string is actually the VOTER's slug-resolution helper, FR-22-16). Corrected across ALL docs to the live gates — `db.has_scalar_media_attrs(slug)` (`convert.py:2940`, def `_route_composite_interior:2404`) + the sibling `_is_container_mirror_block` (`:2950`, def `:908`) → `_process_container_children:3834` — with the XS-3 guard at `fold_eligible` (`:3857`, was mis-cited `:2956`). A propagated stale-docstring error; the gate-before-build discipline caught it before any code. (CLONING thread; theme co-active on main — path-scoped commits.)

## 2026-06-08 (theme/blocks thread) — sgs* editor-extension attrs registered server-side; ServerSideRender block-renderer 400 fixed (D191)

**D191 — The editor extensions' `sgs*` attributes are now registered server-side, fixing "Error loading block: Invalid parameter(s): attributes" on every ServerSideRender-preview block (product-card bound, trustpilot-reviews, business-info, content-collection). qc-council verdict `validated-shipped` (3 cross-model raters: no regressions, real renders, complete affected set).** Mechanism: JS `blocks.registerBlockType` adds the `sgs*` family client-side; ServerSideRender sends them to `GET /wp/v2/block-renderer/<block>`, validated with `additionalProperties:false` against the server schema (block.json only) → the first unregistered `sgs*` attr is rejected (product-card on `sgsAnimation`, trustpilot on `sgsAnimationDelay`). Fix: a `register_block_type_args` filter (`plugins/sgs-blocks/includes/extension-attrs-rest-register.php`) merges a generated `sgs*` schema onto every className-supporting block; the 46-attr list is generated from the extension JS (single source of truth) by `scripts/generate-extension-attributes.js` (wired into prebuild/prestart; a `.githooks/pre-commit --check` guard — via `node.exe` — blocks committing a stale list). Live-verified all 4 blocks return 200 on the sandybrown canary (REST replay + live editor, 0 console errors). Commits `8694aebc` + `df691994` (path-scoped, pushed to main). Report: `.claude/reports/wave1/08-binding-error-trace.md`. Production (palestine-lives.org) deploy held per Bean. (THEME/blocks thread; cloning co-active on main — path-scoped commits.)

## 2026-06-07 (cloning thread) — Method-2 UNPAUSED + autonomy-gate auto-skip on draft runs (D189)

**D189 — Method-2 (the universal converter CSS→attribute transfer) is UNPAUSED and is again the active cloning-fidelity work (Bean, 2026-06-07).** A fresh `/sgs-update` + full `/sgs-clone` to page 8 measured the real current state via parity2: **44.4% overall fidelity, 60 of 108 convertible draft nodes DROPPED**, every dropped node losing the same CSS family (padding/margin/display/gap/background/font) — i.e. the universal CSS-transfer is NOT landing on ~15 sections (hero 42.9%, brand/products/button/announcement-bar/testimonial-slider 0%; trust-bar 92.9% is the one genuinely-faithful section). The audit's "#1–#8 SHIPPED" labels measured *commit existence*, not draft fidelity — parity2 (draft-as-denominator) is the authority. **Open question deferred to `/adversarial-council`:** do the already-built Method-2 commits (A1 `e9eaf013`, A2 `0564d1f3`, A3 `f4fa389b`, A4 `70bf6a37`, A5 `107723be`) actually contain the logic that would fix these sections (mis-wired / not firing), or is the fix logic genuinely absent? Resolve before building more. **Also:** `--mode draft` runs now auto-skip the autonomy gate (it defaulted to rollback on a stubbed visual-QA signal → reverted the deploy → un-inspectable); `--mode strict` still enforces it; `--enforce-autonomy-gate` overrides. _(Orchestrator: `sgs-clone-orchestrator.py` parse_args auto-skip + `--enforce-autonomy-gate` flag. Evidence run: `pipeline-state/mamas-munches-homepage-2026-06-07-174303/parity2-report-1440.json`.)_

## 2026-06-07 (cloning thread) — parity2 draft-centric fidelity verifier BUILT + rewired + QC'd (D183)

**D181 — Wave 3 #2 of the council backlog is shipped + pushed: the PREFLIGHT publish-block is now VISIBLE on the product edit screen, and provisioning auto-sets a sensible Google variesBy. (THEME thread; cloning co-active — path-scoped commits `dbb96b6c`, `0bf4f2a7`.)**

- **Grounding pivot:** the backlog assumed a Gutenberg `PluginPrePublishPanel`, but `use_block_editor_for_post_type('product')` is FALSE on this stack — WC products edit in the CLASSIC screen. The real bug: the publish-block revert runs in a redirecting POST, so the in-request `admin_notices` closure never rendered → silent revert-to-draft. Fix (classic-correct): persist issues to `_sgs_preflight_issues` meta (already done) + render them on the next edit-screen load via `maybe_show_blocked_publish_notice()` (screen-guarded + per-object cap), cleared on the next successful publish. Code-verified (mirrors the working health-notice); a full admin-login browser test was judged disproportionate for an admin-only notice + would expose the test cred.
- **Part 2 (auto-variesBy):** built via `/subagent-driven-development`; provisioning auto-sets `_sgs_variesby_value` term-meta when an axis maps to Google's closed enum (size/colour/material/pattern/age/gender — name then pa_* slug), only when unset (operator wins), via `Configurator_Meta::sanitize_variesby`. Hardcoded map is correct (Google closed enum, not client data). Reviewed by a sonnet spec-reviewer (PASS all 6 acceptance points) + a haiku quality/security reviewer (GO) — Gemini cross-rater account-blocked (403). Mapping live-verified on canary (pa_size→size, Flavour→'' unset). require_once'd its own dep.
- **Bug found + fixed during this work:** a PREFLIGHT `manifest_over_cap` FALSE-POSITIVE — its lean-subset measurement had DRIFTED from render.php's real strip (kept single-image galleries render.php empties) → blocked publishing the valid 48-SKU fixture (27739 vs the real seed's 22408). Aligned to render.php exactly; 540 republished. (Committed with the Wave-2 batch `34e7e427`.) Reinforces #17 (centralise the one stripper).
- **#2 REMAINING (deferred):** an optional proactive pre-publish readiness check (lower value now the block reason is visible). **Wave 3 REMAINING:** #17 file-cohesion split + centralise the lean-seed stripper. Then Phase 2 (Spec 27/28 to 100%: R4+F2, P2/P3).

---

## 2026-06-06 (theme thread) — adversarial-council must-fix WAVE 1 + WAVE 2 SHIPPED (D180)

**D180 — The fact-checked adversarial-council backlog (parking `P-SPEC27-28-COUNCIL-MUSTFIX-WAVE`) is cleared in two file-disjoint subagent waves, both live-verified on canary 540 + pushed. (THEME thread; cloning co-active on `main` — every commit path-scoped.)** _(Wave 1 commits `04e62cdd` + path-scoped batch; Wave 2 commit `34e7e427`. Planned via `.claude/plans/2026-06-06-spec27-28-council-mustfix-wave-plan.md`.)_

- **Wave 1 (9 items, low-risk hardening):** #3 £0 add-to-cart blocked via a `woocommerce_is_purchasable` override (covers every path incl Store-API); #6 rate-limit budgeted by variations-created not requests; #7 `can_edit_product`→`WP_Error` 403; #8 test-hook gated behind `WP_DEBUG`/`SGS_TESTING`; #10 health-cron `ORDER BY RAND()`; #11 no_image flags WC-placeholder-only; #12 rollback surfaces failed-delete IDs; #15 slug-rename TTL→DAY; #16 variation-delete warning de-dual-fired; #18 NEW lean-seed ≤24KB PHPUnit test. Each subagent claim fact-checked against ground truth — caught 3 real subagent defects (a mid-task cut-off leaving #6 caller/#8/#12 unbuilt; a #16 wrong-premise regression; an **invented `WC_Product::get_image_url()` runtime fatal** in #11, fixed to the canonical `wp_get_attachment_image_url()`).
- **Wave 2 (value-ladder authoring UI + UK consumer-law, 8 items):** #1 product-data authoring fields (price£→pence + attestation + decoy + axis) + edit.js controls; #4 reference-price floor + sanity-ceiling + attested-only claim; #5 "Best value" only on cheapest per-unit, decoy→"Most popular"; #9 operator-chosen pack-size axis; #13 tail names the real denominator; #14 deny-list + NFKC + zero-width strip; #19 DMCC audit record; #20 consumer ladder forced inc-VAT. Grounded in DMCC 2024 / CPRs / ASA / Price Marking Order research (Rule 16).
- **Gates that earned their keep:** `/qc-council` caught a HIGH legal blocker (attestation never re-checked at render). A **3-team adversarial red-team** then found 5 more real gaps — REST meta-write bypassing the #4 floor + #19 audit (`show_in_rest:false` on `_sgs_base_price_pence`/`_sgs_decoy_enabled`), a `(bool)` cast accepting a rogue `'true'` (strict `'1'===`), deny-list Unicode/CamelCase evasion, a £0-price "save 100%" (skip ≤0 rows) — plus surfaced a **PREFLIGHT false-positive** (its cap check measured a lean subset that had DRIFTED from render.php's real strip → blocked publishing the valid 48-SKU fixture; aligned exactly). All fixed + re-verified live.
- **Excluded (deferred, recorded):** Spec 28 single-on-sale anchor (needs P3 linked-SKU); ex-plus-vat trade-mode ladder/header basis label (opt-in, unused); Cyrillic-homoglyph deny-list (operator self-authored). 2 council claims REFUTED (unmanaged-stock cap exists; digit-strip by-design). **Meta-lesson:** every high-severity find was the same pattern — a guard/measurement that didn't hold on a second path (REST vs save hook; `(bool)` vs canonical; PREFLIGHT's drifted strip-copy). #17 (centralise the lean-seed stripper) would have prevented the PREFLIGHT drift.

---

## 2026-06-05 (theme thread, session 15) — Spec 28 P1 comparative value-ladder SHIPPED (D179)

**D179 — Spec 28 P1 (comparative per-unit value ladder, display-only) is SHIPPED + live-verified; product-card v1.13.0.** The conversion-lift half of Spec 28, built with zero pricing engine — it only READS the prices WooCommerce already serves and renders a per-pack value ladder. (THEME thread; cloning holds D178.) _(Commits by explicit path, pushed: foundation `49d63ab8`; ship `e0dea916`; spec P1-row mark `b2d04c47`. Built via the 8-step plan `.claude/plans/2026-06-04-spec28-p1-value-ladder.md`, D175.)_

- **What shipped:** an SSR comparative ladder below the per-unit price on Bound-mode `sgs/product-card` — one row per distinct pack size with the size label, per-unit price, a Rule-of-100 saving, and a "Best value" badge on the target pack. New `framingMode` (savings|loss-aversion|neutral) + `decoyEnabled` block attrs; new product meta `_sgs_base_price_pence` (single-item reference, KJC-A) + `_sgs_decoy_enabled`; two pure helpers `sgs_saving_display()` + `sgs_value_ladder()` in render-helpers.php.
- **KJC-A (anchor):** owner-set `_sgs_base_price_pence` if present → "vs buying singly"; else smallest-pack per-unit with the single-item claim SUPPRESSED (FR-28-16 — never a comparison to an unsellable reference).
- **KJC-B (SSR, NOT seeded):** the ladder is added to `$context` AFTER the client seed is serialised → it is NEVER in `data-wp-context`; the 24KB lean-seed cap held byte-identical at 22408B (avoids the `manifest-growth-can-trip-capped-client-seed` regression). Combos needed for re-highlight are already seeded; view.js toggles `aria-current` via plain DOM (no `data-wp-*` on SSR rows, PD-10/11).
- **Safety/correctness (live-proven on canary 540):** Rule-of-100 floored % via EXACT intdiv (a float-imprecision bug that floored 29%→28% was caught by the Step-2 unit gate + fixed); monotonicity guard suppresses any non-decreasing per-unit row (96-pack £0.62 suppressed vs 48-pack £0.51 → badge correctly on the 48-pack, not the largest); sale-aware label; saving strings esc_html'd, plain-text helpers (escape-audit clean). UK-legal: no fabricated single-item comparison.
- **WCAG fix caught LIVE:** the saving text using the theme brand primary failed AA on a light palette (measured 2.25:1, Mama's pink-on-cream) — fixed to the theme contrast/near-black colour + weight 600 → re-measured 15.71:1 PASS. The unit logic was green; only the live render on a real palette exposed it (memory `ship-gate-needs-human-eye-not-just-automated-gates`).
- **Parallelism honesty:** only Steps 5∥6 (style.css ∥ view.js, disjoint files) parallelised via `/dispatching-parallel-agents`; Steps 0–4 are a sequential chain on the shared render.php (single implementer, per the concurrency protocol). P2/P3 (preview-only engine) + P4 (FR-28-5 WC-write) remain — P4 was fenced behind Cluster C, now unblocked when chosen.

---

## 2026-06-05 (cloning thread) — META root cause of recurring clone defects: diagnosis-without-delivery; + Batch-1 lift fixes shipped (D178)

**D176 — Spec 27 Cluster C R1 authoring controller (FR-27-R1) — recorded (was deferred in session 14 by the concurrent decisions.md commit-race).** NEW `includes/class-product-authoring.php` (+`-args.php`): `POST /sgs/v1/products/{id}/variations/{vid}` + `/attributes` via WC `set_*()`+`save()` (byte-identical, NEVER raw postmeta), per-object `edit_post` + `X-WP-Nonce` (cookie CSRF) + per-user rate-limit + IDOR + non-`pa_`-taxonomy gate + sale<regular guard + `WC_Data_Exception`→400. Golden-master verified (price round-trips via the data store; the `wc_product_attributes_lookup` regen correctly no-ops because `woocommerce_attribute_lookup_enabled='no'` — native WC would skip it too, NOT a bug — memory `verify-feature-flag-before-asserting-defect`). Cross-model review NO-GO→GO (fixed a save-outside-try BLOCKER + a non-`pa_` taxonomy-corruption hole). _(Commit `f747e58a`, pushed.)_

**D177 — Spec 27 Phase 2 is COMPLETE: Cluster C R2 + R3 + PREFLIGHT SHIPPED + QA-AUTHORING gate PASSED + Bean R-22-13 sign-off GRANTED; the spec was reconciled to the real implementation.** _(Commits by explicit path, all pushed: R2 `e62f337f`; R3 + PREFLIGHT `dd9d0d7d`; invalid_jsonld publish-gate fix + e2e harness `27e54132`; handoff `5c583d4f`; spec/plan reconciliation `53532c0d`.)_

- **R2 (FR-27-R2):** NEW `class-product-provisioning.php` (+`-args`+`-helpers`) + shared `class-product-authoring-security.php` (extracted from R1; R1 now delegates — behaviour-identical, re-verified). `POST /products/{id}/provision` = conflict-safe `pa_*` taxonomy/term provisioning (only ADDS, never alters siblings; parent attributes MERGED by union so a re-run with fewer attrs can't orphan), cartesian generate, upsert dedup (`_sgs_variation_upsert_key`), 300-combo cap BEFORE any write, and a transactional rollback (created-resources ledger + parent snapshot) since the WC batch is not atomic. EXTRAS: a `POST .../variations/bulk` endpoint + a triple-gated injected-failure test hook (dead in production). LIVE-PROVEN: golden-master byte-identity vs a native-built product + injected-failure 0-orphan rollback + shared-taxonomy sibling-safety. Two cross-model reviews (Opus security GO, no blockers; a spec pass caught + I fixed 2 gaps: combo-cap ordering + the set_attributes re-run truncation).
- **R3 (FR-27-R3):** the swatch/gallery/divisor/label authoring controls already shipped in Cluster A; R3 filled the one gap — a Google `variesBy` `<select>` on the attribute term add/edit screens (`configurator-term-fields.php`, saved via `Configurator_Meta::sanitize_variesby`) — + NEW `class-configurator-edit-safety.php` (a pa_* term SLUG-rename warning, a delete-variation-with-orders warning, and orphaned-meta cleanup on variation delete). Zero-raw-meta authoring achieved.
- **PREFLIGHT (FR-27-PREFLIGHT / SEC-5):** NEW `class-product-preflight.php`. `transition_post_status` HARD-blocks publish (revert to draft + `_sgs_preflight_issues` meta + admin notice, dual re-entrancy guard) on any £0/no-image/draft/over-cap/no-variesBy/invalid-jsonld variable product; cart £0 422 guard (`sgs_price_not_set` in `class-cart-proxy.php` + the `woocommerce_add_to_cart_validation` filter for the Store-API path); a `GET /sgs/v1/products/{id}/preflight` pre-check endpoint; a weekly `sgs_preflight_health_check` cron. FIX `27e54132`: the `invalid_jsonld` check is publish-gated (the manifest only builds for a published product, so a pre-publish readiness check on a draft no longer false-flags empty JSON-LD; the publish-transition + every re-save of a published product still validate it). LIVE-PROVEN: a £0/no-image product reverts to draft, a valid one publishes.
- **QA-AUTHORING gate:** a consolidated e2e (`scripts/e2e-authoring-acceptance.php`) live-proved the whole client journey — provision (R2) → author swatch/variesBy/gallery/unit via the R3 meta layer → PREFLIGHT ready → publish → valid `ProductGroup` rich-results (4 hasVariant, variesBy=size) + 4-combo manifest — every step via a controller/REST/sanitised callback, ZERO raw SQL, ZERO WP-CLI. Bean was shown the live configurator + the live JSON-LD → **R-22-13 sign-off GRANTED.**
- **COURSE-CHANGE recorded:** Spec 27 v3 had demoted authoring (R1/R2/R3) + PREFLIGHT to "Phase R roadmap, build when a 2nd shop client lands"; Bean's 2026-06-04 decision to complete the whole spec before launch PULLED them forward into Phase 2 "Cluster C" — all now shipped. ONLY FR-27-R4 (slug-templates) / R5 (AI-builder) / F2 (AI-citation/feed) — the decision-gated "Cluster D" capstone — remain unbuilt; they do NOT block a first client shop.
- **Spec reconciled (`53532c0d`):** Spec 27 → v5 (Phase 2 COMPLETE; per-FR SHIPPED markers + commit SHAs; the EXTRAS documented; the FR-27-E3 "mandatory per-variation image-sitemap" clause corrected to DESCOPED per D173/`53b85d7c`; breadcrumb noted as block-placement, live-verified) + the Phase-2 plan marked COMPLETE with success criteria ticked. Audited via a verification agent + fact-check (corrected its false "BreadcrumbList absent" — it IS present via the `sgs/breadcrumbs` block on the template, live-verified; `class-sgs-ai-connector`/`review-schema` are separate concerns, not Spec-27 extras).
- **Process note:** the `53532c0d` commit accidentally swept in 3 co-active Spec-28 staged files (`decisions.md` D174/D175, `next-session-prompt-theme.md`, `spec28-p1-value-ladder.md`) because a bare `git commit` flushes the whole index, not just `git add`ed paths — benign (coherent, legitimate, preserved), not history-rewritten. Lesson `git-commit-must-be-path-scoped-with-coactive-sessions`: use `git commit -- <paths>`.

---

## 2026-06-05 (theme thread, session 14) — Spec 27 Phase 2 Cluster B (SEO) COMPLETE + context-cap regression fixed (D173)

**D174 — Spec 28 Smart Bulk Pricing finished to v2 (BUILDABLE).** The drafted-then-councilled spec was completed: all 15 adversarial-council must-fixes folded into self-consistent FRs (FR-28-1→17, new safety FRs 13–17: price-lock, audit/undo, two-step apply, input gates, base-price semantics, error-states), corrected idempotent charm maths (£30.99/35% top pack, not the unreproducible v1 £29.99/38%), guardrail evaluation order canonicalised (40% cap wins on conflict), VAT-aware charm (inc-VAT basis, ex-VAT `regular_price` back-solved), explicit-trigger-only performance + capability gate, XSS escaping, moat re-anchored (engine is table-stakes; the clone→theme→WC→ladder loop is the moat). KJC-2 decoy = opt-in per product (Bean). spec_version 2, status draft→BUILDABLE, docscore 100% A. Build order unchanged: P1 value-ladder ships now on shipped B3; P4 WC-write (FR-28-5) stays build-deferred behind Spec 27 Cluster C / Phase R. _(Spec edit only; no code. `.claude/specs/28-SGS-SMART-BULK-PRICING.md`.)_ **(Note: the prior session wrote this D174 + committed it `93f0567c` but the spec FILE rewrite hadn't landed — "parked before complete". Session 14/15 actually performed the v1→v2 file rewrite; D174's claims are now true on disk.)**

**D175 — Spec 28 P1 (value-ladder) phase plan built + grounded (2026-06-04).** `/phase-planner` produced `.claude/plans/2026-06-04-spec28-p1-value-ladder.md` — an 9-step (Step 0–8) fly-through plan for the display-only comparative per-unit ladder on sgs/product-card, grounded in the real B3 code (`sgs_configurator_per_unit_display()` render-helpers.php ~707, manifest combo fields, store `sgs/product-card`). Hidden-Decisions pass run via **haiku + sonnet cold reviewers** (zero-cost gemini/cerebras CLIs were down) — caught 3 would-ship bugs, all patched into the step prompts: (1) helper signatures must thread `$tax_mode`+`$decimals` to mirror the real 4-arg `sgs_configurator_per_unit_display` + `sgs_configurator_format_minor`; (2) the QA gate can't use bare `php -r` (`wc_price()` needs WC loaded → WP-CLI eval); (3) the reused B3 badge span carries `data-wp-bind`/`data-wp-text` → copying verbatim would WIPE the SSR badge on hydration (copy class+style only, static label). Two architectural decisions LOCKED: **KJC-A** saving anchor = owner-set single-item ref price if present, else smallest-pack per-unit with the claim suppressed where no genuine single exists (FR-28-16); **KJC-B** ladder is SSR-rendered (NOT seeded into the 24KB client context — avoids the `manifest-growth-can-trip-capped-client-seed` regression). _(Plan doc only; no code.)_

---

## 2026-06-04 (cloning thread, PM4) — Method 2 converter-lift BUILD: Phase A core + 2 block fixes SHIPPED + live-verified (D172)

**D170 — The council-validated converter-lift design (D169) was turned into an executable phase plan (`/phase-planner`), stress-tested by a 5-persona `/adversarial-council`, and the convergence-weighted must-fixes were folded into a FINAL plan; a plain-English explanation doc was produced (per Bean's directive: per-stage failure + decisions + universal solution + A/B rule-compliance). Build = next session. (CLONING thread.)** _(Next-free cloning D after D169; theme holds D168. Committed by explicit path. Plan: `.claude/plans/archive/2026-06-04-method2-converter-lift-PHASE-PLAN.md`; explanation: `.claude/reports/2026-06-04-method2-converter-lift-EXPLAINED.md`.)_

**The adversarial council (5 personas: universality-auditor, cynic, spec-lawyer, ship-PM, CSS-red-teamer) found the plan architecturally sound but NOT universally buildable as written — it had the exact Mama's-shaped band-aid Bean's bar forbids. Convergence-weighted must-fixes (all verified at file:line — STOP #34):**
- **MF-A [5/5 personas, the headline]:** the "curated map" was a 15-row hand-written longhand list writing to WP `style.*`, dropping every `clamp()/calc()/var()` + `background`/`box-shadow`/`min-height` (not even in `_root_lift_rules` today) + all responsive overrides (`bp_decls` discarded at convert.py:2114) — and a hardcoded dict is an **R-22-1 violation**. FOLD: build the map from the existing `property_suffixes` DB table (117 rows, the FR-22-5 mechanism), pass CSS-functions through as raw strings, FLAG-not-drop unmapped (proven by a fixture case), consume `bp_decls`, target the wrapper-attr namespace. **This is the line between "clones Mama's" and "clones anything."**
- **MF-B [3/5]:** min-height is a BLOCK-layer fix, not a converter align-gate — verified `class-sgs-container-wrapper.php:431` adds the centre-forcing `--has-min-height` class on min-height presence ALONE. FOLD: gate the class on `verticalAlign`; lift min-height unconditionally + lift the draft's centring idiom → verticalAlign separately.
- **MF-C [3/5]:** do NOT edit the shared wrap fn to read CSS — verified `db_lookup.py:2461 emit_sgs_container_wrapping` wraps ALL 9 top-level sections (its docstring), not just composites. FOLD: compute OUTER attrs once in the walker + pass them in as a param (one source of truth, no WS-4-divergence regression).
- **MF-D [ship-PM]:** it's a 2-session build — re-sequenced: Session A converter-core (DB-map → composite path hero-first → slug-None lift → sidecar delete), Session B block/polish (min-height block-fix ∥ sgs/media CSS ∥ variant/modifier; then D5 layer-diagnosis; image sideload should-not-must; fixture last).
- **REJECTED (verified false — STOP #34):** Cynic MF-1 ("sidecar file not written, D4 untraced") — `css-d1-assignments.json` exists (19,928 B / 42 keys), `seed_d1_sidecar` IS a no-op stub, `sgs/feature-grid.gap=14px` IS the stranded value. FS-2 premise holds.
- **Compliance:** every fold stays A (spec-says) or B (clean upgrade); no Mama's-specific conditional (R-22-9 intact). SHOULD-fixes folded: FR-22-1-row-3 citation for modifier carry, trace `--Array` before fixing, a map-staleness build-gate, fixture value-syntax cases.

---

## 2026-06-04 (cloning thread, PM2) — Method 2 grounded: premise corrected, trust-bar routing fix SHIPPED, converter-lift 5-piece design council-validated (D169)

**D169 — The Method-2 converter investigation was grounded against a fresh full `/sgs-clone` run (`mamas-munches-homepage-2026-06-04-134425`) + 3 cross-verified read-only agents; it CORRECTED the prompt/memory premise, shipped the one real routing fix (trust-bar), and produced a council-validated-and-refined 5-fix-shape converter-lift design (build = next session). (CLONING thread.)** _(Next-free cloning D after D167; theme holds D168. Committed by explicit path per STOP #45.)_

**(1) Premise corrected (verified vs live DB + match.json).** The prompt/memory said "converter routes everything to `sgs/container` @0.10; routing fix = `.sgs-hero`→`sgs/hero`." FALSE: **hero already routes to `sgs/hero` @1.0** (D107 tier path works). The 5 layout sections (featured-product/brand/ingredients-section/gift-section/social-proof) are **NOT registered blocks** → routing to `sgs/container` is **correct by design** (they are containers; the lift transfers their CSS). Routing was essentially solved; the real lever is the **lift** (the 560 `extraction_failed`, of which ~140 are chrome + ~387 are CORRECT ABSENCES — the Mama's mockup genuinely sets no shapeDivider/bgSvg/etc.; do NOT chase the 560 to zero).

**(2) Trust-bar routing fix SHIPPED (`c3443e03`, pushed).** Root cause: `sgs/trust-bar` carried `containerKind:section` but was MISSING `supports.sgs.is_section_root` (hero + cta-section both declare it) → `/sgs-update` wrote `tier='block'` → the voter returned gap-candidate and it only routed by an accidental secondary-class-scan salvage @0.5. Demoted at D72 (old trust-bar retired), never restored at the D123 rename. Fix: add `is_section_root:true` to block.json (durable source) + set `blocks.tier='class-section'`. Verified: `is_class_section_block('sgs/trust-bar')` → True → voter returns `('sgs/trust-bar', 1.0, 'class-section-block-equivalent')`.

**(3) B1 mechanism decision (Bean): Option A — consume inline.** The converter-lift maps each element's CSS → attrs at walk-time via a CURATED `canonical_slot` DB map (R-22-1; STOP #48 — never a blind suffix); the `css-d1-assignments.json` sidecar (`seed_d1_sidecar` stub, `convert.py:167`) is DELETED, not revived — no two-stage element-identity re-alignment, matches Spec 22 (sidecar superseded).

**(4) 5-fix-shape converter-lift design authored + council-validated (`.claude/plans/archive/2026-06-04-method2-converter-lift-design.md`).** FS-1 wrapper-capability lift + hero gradient/min-height; FS-2 grid/gap incl. B1 consume-inline; FS-3 variant/modifier/className (the `--Array` bug, dropped `--send-to-ward`, product-card prefix); FS-4 sgs/media-as-grid-child 0×0 (BLOCK CSS); FS-5 image sideload dry-run→real. Each carries file:line + falsifiable predicted outcome + validation command + STOP guards.

**(5) qc-council Stage-5 gate CAUGHT a mis-target (STOP #50) before any build.** The trace proves hero emits via `content_column_folded {slug: sgs/hero}` (COMPOSITE-interior path, FR-22-19), and post-fix trust-bar routes as the `sgs/trust-bar` composite — NOT the slug-None section path FS-1/FS-2 target. Building them as written = 0-delta no-op on hero+trust-bar (the identical trap that "falsified" the 2026-06-03 lift). **Refinement (required before build): split the lift by emit-path** — FS-1a/FS-2a (slug-None, validated for the 5 layout sections) vs FS-1b/FS-2b (composite path — hero+trust-bar land on the FR-22-19 emit / the C1 `db_lookup.py:2461` widthMode:full band-aid, now that WS-4 destinations exist per `universal-lift-was-premature-not-falsified`). No fix-shape falsified; 2 require the path-split. Build is next session.

---

## 2026-06-04 (theme thread, session 12) — Spec 27 PHASE 2 build started: Step 0 + B2 swatches + I2 auto-contrast + TAX-UI; 3 fast-follows; UK tax-verify; Phase-2 plan v2 (D168)

**D160 — The #1-#8 triage was grounded (5 parallel /systematic-debugging investigations + a 2-rater /qc-council Gate A), the WS-4 wrapper-mirror mechanism + the text-block parity gap were audited, and WS-4 was locked as the FOUNDATIONAL-FIRST workstream. (CLONING thread.)** Full grounded spec: the GROUNDED FINDINGS appendix of `.claude/plans/archive/2026-06-03-cloning-fidelity-triage-and-composite-remodel.md`.

**(1) WS-4-FIRST sequencing LOCKED (Bean-directed).** The universal wrapper-mirroring to sgs/container is FOUNDATIONAL and must land BEFORE any wrapper-dependent fix — else WS-4 (which replaces each composite's wrapper) OVERWRITES those fixes. Locked order: WS-1c (complete sgs/container) → **WS-4 (wrapper mirror)** → wrapper-dependent fixes (#4b product-card fill, #7 announcement-bar); in parallel throughout: #3 text-parity, #6 notice-banner, #8 slider-nav, #4a grid-lift.

**(2) Council CORRECTED 3 of the 5 investigation fix-shapes** (the reason the council was run): **#3** "remove global text-align:center" → would break every centred heading site-wide; real fix = add `textAlign` attr+control+render to heading+label (sgs/text pattern) — it's a **customisation-parity gap** (audit: heading+label both lack textAlign; text has it; + heading missing edit.js controls for fontStyle/textDecoration/bg/inheritStyle; label fontStyle missing + broken `attr()` responsive font-size). **#4** scoped `.sgs-products` CSS → R-22-9 violation; real fix = (a) lift the named grid's responsive template onto NATIVE attrs in `_emit_wrapper_container` (universal; the machinery exists, just not wired to that path), (b) product-card FILLS by default with width control baked-in via WS-4 — not a cardMaxWidth hack; **#4b is WS-4-dependent**. **#7** converter slots-row → would break legit announcement-bar; **PARK — self-resolves under WS-4**. **#6** confirmed: fix notice-banner block.json at source (so /sgs-update re-derives content-block/1, not a hand-patched DB row) + universal converter sgs/text-child synthesis + showIcon-from-draft. **#8** confirmed slider redesign.

**(3) WS-4 mechanism design (from the foundation audit — the build spec). SCOPE = ALL ~28 composites in the `block_composition` roster, KIND-scoped (Bean-directed) — NOT just the 4 section blocks.** Section (hero/cta-section/trust-bar; MODAL excluded) + Layout (card-grid/feature-grid/gallery/post-grid/pricing-table/reviews/tabs/accordion/form/multi-button/testimonial-slider/form-field-tiles) + Content (info-box/product-card/testimonial/quote/notice-banner/option-picker/team-member/mobile-nav/…). The KIND-scope table (section=full/layout=grid+width/content=width+spacing) covers all three; #4b product-card fill DEPENDS on the content-kind mirror. **MODAL = hard exclusion** (dialog+::backdrop shell has no container mapping). Mechanism = (a) shared PHP wrapper class `includes/class-container-wrapper.php` (`SGS_Container_Wrapper::render(...)` → {open,close,uid}; composite keeps its own interior between); (b) KIND-scoped block.json attr-mirror (section=full/layout=grid+width/content=width+spacing); (c) `sync-container-wrapping-blocks.py` report-only → WRITER wired as /sgs-update Stage 11. **BLOCKER first:** cta-section `layout` enum collision → rename to `contentLayout` + deprecated.js; hero `overlayColour`→`backgroundOverlayColour` + deprecated.js; trust-bar additive (low risk). Build sequence: cta rename → helper → trust-bar → cta → hero → modal-excluded → /sgs-update writer. WS-1c lands before the helper.

**(4) #8 slider BUILT + deployed (uncommitted) but BUGGY — Bean live-checked the 4-testimonial slider.** Arrows→flex-siblings, controls markup, slidesVisible 1→3, loop `wrapIndex`. TWO bugs: (1) columns far too thin — card group spans only ~half the section width (the `__stage` flex + slide flex-basis sizing must fill the full `__inner`/section width, ~960/3 per card); (2) nav must ALWAYS show + ROTATE even at total≤slidesVisible (remove the hide-when-≤ gate; 3 cards rotate 1→2→3→1 continuously). + tidy floating pause-btn. NEXT: rebuild track/slide sizing to fill width + always-on-rotating nav; verify live on the real 4-card slider; then commit. Uncommitted in the working tree.

---

## 2026-06-03 (cloning thread) — WS-1 A1+A2 SHIPPED (sgs/container 3-layer, first 2 gaps) + composite-remodel directive sharpened + page-level triage register (D159)

**D158 — SGS global-styles + per-client theming architecture decided (Spec 26 written; build DEFERRED).** Bean questioned the global-CSS setup; a research-buddies pass (WP 7.0 global-styles, 13 sources) + a 4-persona design council (standards / agency-ops / pipeline-integration / non-coder-UX — all ranked Candidate C #1) + a CLI/abilities investigation + a converter-routing investigation resolved it. **Corrected model (supersedes the D156 "override precedence" framing — that wording was wrong):** WP 7.0 merge = default→blocks→theme(theme.json+variations)→custom(`wp_global_styles` CPT), merged in PHP then emitted as inline CSS; the `wp_global_styles` post IS the live source of truth, `theme.json` is the factory seed — a data-layer merge, not a CSS override. Per-instance = block markup, wins by cascade; no separate override layer. **Architecture (Candidate C):** one lean framework `theme.json` baseline + per-client **style-variation DELTA** (kills the full-`theme.json`-copy fork-tax) + scoped per-site deploy with Browse-Styles suppressed (**this superseding-fixes Decision 18 — the variation retirement over-corrected a deploy-SCOPING bug, not a mechanism flaw**) + sync = EXTEND `push-theme-snapshot.py` (~40 lines) to write the live `wp_global_styles` post via the WP-native `POST /wp/v2/global-styles/{id}` (no new endpoint/ability, no Create Block Theme runtime dep — confirmed there is no `wp global-styles` CLI / no core "write global styles" ability) + a pull/round-trip + a pre-deploy guard. **Block styling = Bean's "Middle" path:** presets prominent + raw values available (theme.json `color.custom`/`customSpacingSize` on), swatch hover shows label+hex, per-block lint-gate for raw-accepting controls, WCAG contrast linter, inspector reads the merged custom-origin value (WP 7.0 fix). **Pipeline derivation = Bean's proposal (adopt-with-caveats):** converter emits RAW → a post-pass derives globals from repetition (tunable ≥3/≥60% threshold) + a hero-button-position rule (1st hero button → global PRIMARY preset, 2nd → SECONDARY — building the long-missing intent; today the pipeline only *references* presets via `inheritStyle`, never *derives* their values). **Urgent (do now, low-risk):** the REST-write extension to `push-theme-snapshot.py` (FR-26-D2 — closes `P-PUSH-SNAPSHOT-SKIPS-GLOBAL-STYLES`). Full spec + FRs: **`.claude/specs/26-SGS-GLOBAL-STYLES-AND-THEMING.md`** (docscore 100% A). Build deferred per Bean's scope call until the cloning phase closes (re-baselines pixel-diff). **[Correction 2026-06-03 — FR-26-D1 RESOLVED/MOOT, do NOT clear post 7]:** the council's HIGH-confidence "clear the canary `wp_global_styles` post 7" recommendation was **inverted by live verification** — the canary's `theme.json` AND post 7 already mirror Mama's full brand palette + WCAG CSS (synced because this session's Mama's WCAG work hand-wrote both layers), so the canary already renders Mama's brand correctly and the colour-contamination the council feared is already resolved. Clearing post 7 is therefore unnecessary AND risky (no render benefit; the canary is shared with the cloning thread). The cloning pixel-diff is NOT colour-contaminated currently. Residual risk: the two layers stay synced ONLY because both were hand-written this session — without FR-26-D2 they re-diverge on the next disk-only `push-theme-snapshot` or any Site-Editor edit. FR-26-D2 is the durable fix. (Instance of memory `council-validates-the-criterion-it-is-given` — council recommendations are hypotheses; verify before acting.)

---

## 2026-06-03 (theme thread) — Phase D + Phase E + Task D + WCAG fixes + skip-link (D153–D157)

**D154 — Phase E: `sgs/content-collection` dedicated block (THEME thread; uncommitted).** Decision: dedicated block with its own `WP_Query` (block.json v1.1.0) rather than a core Query Loop wrapper. Rationale: (1) core Query Loop's meta-orderby inspector gap (#40170) would require the `query_loop_block_query_vars` filter and a custom panel — identical complexity to a dedicated block but less transparent for clients; (2) tech-illiterate clients get a single named preset dropdown (`selectionRule`) instead of raw Query Loop controls. `contentType` whitelisted via `sgs_content_collection_post_types` filter so themes/plugins can extend it. Count capped 1–24 server-side. Each result rendered as a Bound `sgs/product-card`. Empty state (FR-24-6) renders the `emptyMessage` attr — always server-rendered, never blank. IDOR guard: `in_array($content_type, $allowed_types)` before any query; `get_post_type($id)` check on single-card picker. Resolves Spec 24 Open Question #1. Spec 24 FR-24-4/5/6.

**D155 — Task D: `sgs/heading` hero block-style removed (THEME thread; uncommitted).** Removed the `hero` style from `includes/variations/sgs-heading-variations.php`. An h1 with default SGS heading styling already achieves the hero visual; the explicit style was redundant and added editor noise. Remaining heading styles: `section` (uppercase tracking) and `card` (smaller weight). heading block.json bumped 0.5.0 → 0.5.1 (cache-bust). No `deprecated.js` needed — the style class is additive on a dynamic block; existing posts with `is-style-hero` on `sgs/heading` fall back to unstyled (acceptable — no visual regression on live content; the class is simply no longer registered). See `includes/variations/sgs-heading-variations.php` line 9–10 comment for rationale.

**D145 — Converter carries `is-style-*` block styles onto cloned blocks + content-leaf ladder made tag-authoritative (CLONING thread; commit `b93a3b51`).** _[Documented from the commit during the 2026-06-01 doc-sweep — cloning thread to ratify.]_ Enables a Trustpilot-style review bar (and any styled block) to clone fully. **(1) is-style preservation:** a draft can request a registered block STYLE on a recognised block (e.g. `<div class="sgs-x__stars is-style-trustpilot" data-sgs-rating="5">` → `sgs/star-rating` with the Trustpilot-green preset). `walk()` now carries any `is-style-*` class from the source node onto the emitted block's className (resolved-block path + top-level wrap); rating lifts via the existing `lift_behavioural_attrs` `data-sgs-*` path. **(2) content-leaf ladder reordered to tag-authoritative:** the node's OWN tag (`atomic_tag_map`) routes FIRST + ungated (img→sgs/media{src,alt}, p→text, h*→heading, a→core/button, blockquote→sgs/quote), THEN text-capable BEM segment, THEN sgs/text — fixes an sgs-classed `<img>` (the Trustpilot logo) that previously mis-routed to sgs/text instead of sgs/media. Unit-verified: logo `<img>`→sgs/media; stars div→sgs/star-rating{rating,className:is-style-trustpilot}; CTA span→sgs/text; prior committed-D regressions unchanged. Cross-model qc-council PASS (1 intentional nit: `<a>` text-label → core/button by tag-authority — correct for SGS-BEM drafts, not a regression). R-22-1/3/9 clean. Builds on D141's §FR-22-4.1 content-leaf step.

---

## 2026-06-01 (theme thread) — Mobile-nav full-fix (D143) + product-card 6 decisions ratified (D144)

**D143 — Mobile-nav fully fixed: full-screen overlay + populated menu + header-only inserter (SGS THEME thread; commit `1f985c9a`).** Bean reported the mobile nav opened to a tiny box, showed no menu, and couldn't be closed. Root-caused live (CDP `use_browser` on canary, 375px, logged-out) per the root-cause methodology, then fixed in 4 parts — all live-verified: **(1) Overlay sizing** — the `--overlay` variant never overrode the Popover API UA defaults (`width/height:fit-content`) so it opened at 208×158; added `width:100vw + height:100vh/100dvh + inset:0` (style.css, overlay rule only; slide/bottom-sheet untouched) → fills viewport 375×812. **(2) Block-gap strip** — WP flow-layout `:root :where(.is-layout-flow) > * { margin-block-start:16px }` (specificity 0,1,0) tied the base `.sgs-mobile-nav{margin:0}` and won on source order, exposing a 16px strip above the drawer; the doubled block class `.sgs-mobile-nav.wp-block-sgs-mobile-nav` (0,2,0) beats it → flush at y:0. **(3) Empty menu [universal bug]** — `render_menu_items()` only handled `core/navigation-link`/`-submenu`, but WP's DEFAULT nav content is `<!-- wp:page-list /-->` (ground-truthed: canary `parts/header.html` nav is self-closing → falls back to `wp_navigation` post 4 = bare page-list). Added a `core/page-list` case (`class-mobile-nav-renderer.php`) expanding the published page hierarchy (top-level links + one accordion level for pages-with-children) via new shared helpers `render_link_li()` + `build_submenu_li()`. Universal — fixes every site whose header nav isn't manually populated (incl. the framework-default header per Spec 17 §S1). Verified **0 → 13 items** + link navigation (clicked "Hero Clone PoC" → landed `/hero-clone-poc/`). **(4) Header-only inserter** — new `mobile-nav-inserter-scope.php` (`allowed_block_types_all` filter, fail-open) removes `sgs/mobile-nav` + `sgs/mobile-nav-toggle` from the post/page inserter while keeping them in the Site Editor (templates/parts/navigation); when `$allowed===true` it fetches the full registry and diffs out only the 2 chrome blocks (never a short list); wired via require_once in `class-sgs-blocks.php`. view.js confirmed close (native `popovertargetaction=hide`) + ESC + accordions bind regardless of trigger → an orphaned drawer stays closeable. block.json `version 3.0.1 → 3.0.3` (cache-bust — the deploy reused the `?ver=` param; surfaced live when the CSS fix didn't appear until the version bumped). Visual report `reports/visual-diff/mobile-nav-2026-06-01.md`. **NB headless top-layer quirk:** CDP `dispatchMouseEvent` synthetic clicks on popover-internal elements don't reliably trigger anchor nav / hit-test in headless Chrome; programmatic `.click()` + real taps work (proven by the successful navigation). **Deferred (low risk):** editor-inserter E2E (open post editor, confirm the 2 blocks absent) — fail-open design can't break the editor. This implements/closes the mobile-nav portion of D142's flagged flaw.

---

## 2026-06-01 (theme thread) — Task 7b variantStyle→block-styles (4-way parallel) + Task 5 mega-menu finding (D142)

**D142 — Task 7b: cosmetic `variantStyle` converted to WP block-styles on heading/text/label/quote via 4-way parallel dispatch; Task 5 mega-menu re-scoped.** (D141 taken by the cloning thread — same shared branch.) Bean chose convert-to-block-styles (not remove). Dispatched **4 parallel Sonnet agents** (one per disjoint block dir) via /dispatching-parallel-agents. Each: created `includes/variations/sgs-{block}-variations.php` registering the variants as block-styles (`register_block_style`, auto-loaded by `class-sgs-block-variations.php`'s `sgs-*.php` glob); removed `variantStyle` from block.json + the SelectControl from edit.js; stopped render.php reading variantStyle (the active `is-style-*` class flows through `get_block_wrapper_attributes`); added a deprecated.js entry (`isEligible: variantStyle!==default` → `migrate`: append `is-style-{value}` to className, drop variantStyle). Styles: heading hero/section/card; text quote/caption/lead; label plain/pill-fill/pill-wrap; quote pullquote/testimonial/plain. **label + quote converted EXISTING CSS** (no visual change); **heading + text variants were DORMANT** (no backing CSS) → agents added sensible defaults — existing hero/lead posts will now actually style via the migration (FLAGGED to Bean: tweak values or drop heading 'hero' as redundant per "h1 default = hero"). **Review caught agent over-claims** (all 4 claimed "no stray refs"; stale LSP flagged dangling `$variant_style` — grep + `php -l` on all 8 files confirmed clean, only comments remain). **Live-verified (CDP `use_browser`; Playwright MCP lock-stuck):** variantStyle attr gone on all 4; `core/blocks getBlockStyles` lists all registered styles; `is-style-hero` applies + renders; old `{"variantStyle":"pill-fill"}` markup migrates → valid, `className: is-style-pill-fill`, variantStyle dropped. `mobile-nav.variant` + `divider` shape deliberately untouched (settings/asset-choices, not routing-variants). **Task 5 (mega-menu) — CHECKED:** block is fine — panel is a template part (`area='mega-menu'`), already supports unlimited layouts; 2 client layouts exist (`theme/sgs-theme/parts/mega-menu-{brands,sectors}.html` + `mega-menu-panels.css`). "Variants" = a library of reusable panel patterns; clients CAN build own + use templates as starting points (Site-Editor template-part picker). NEXT SESSION (Bean-directed): /research-buddies + /qc-council on whether template-part is the best UX, then generify the 2 existing patterns (placeholder + tokens) + subagent-build the 7 proposed layouts (simple/full-width-cols/contained-cols/featured-promo/tabbed/card-grid/split). **Also next: /research-buddies on mobile-nav** — Bean found a serious flaw (panel covers only a small portion of screen, nothing clickable, can't close) → assess vs gold-standard. NB the cloning thread's D141 already fixed the 2 Task-8 bugs this session flagged (media `imageId`, container validation).

---

## 2026-06-01 (cloning thread) — Editor "invalid content"/"cannot be previewed" root-caused to 2 issues + 5 fixes; all live-verified (D141)

**D140 — cta-section inserter variations turned into RICH presets (Task 6); product-card `gift` variant deleted (Task 7a).** **Task 6:** the 3 cta-section variations (cta-centred / cta-split / cta-banner) were alignment-only (`layout`+`className`). Now each is a complete, on-brand starting point that shows off the full option set: it prepopulates the **InnerBlocks content** via an `innerBlocks` key in the `get_block_type_variations` PHP filter (`sgs/heading` content + `sgs/text` + `sgs/multi-button` > 2×`sgs/button`) PLUS shell scalar attrs (`layout`, `gradientPreset`, `ribbon`, `stats`/social-proof). cta-section was already FR-22-6-migrated (content = InnerBlocks; `stats` stays scalar — the inspector panel is literally "Stats / Social Proof"). **Verified WP supports `innerBlocks` in a PHP-registered variation** before building (WP dev-docs "innerBlocks: Prepopulate the variation" + live scaffold test — neither hero nor the old cta used it, so no local precedent). A shared `sgs_cta_inner_blocks($headline,$body,$buttons)` helper builds each template. **Live-verified (CDP `use_browser`, the Playwright MCP was still lock-stuck):** `getBlockVariations('sgs/cta-section')` shows all 3 with 3-child innerBlocks + stats + gradient; inserting cta-centred scaffolds headline "Ready to grow your business?" + body + buttons + 3 stats live in the canvas. **Task 7a:** removed `gift` from product-card `variantStyle` enum (block.json) + the editor SelectControl option (edit.js). `render.php` had NO gift-specific branch — gift always rendered identically to standard (= Bean's "identical to standard, delete it"), so existing gift posts keep rendering unchanged; `deprecated.js` left intact (preserves old-post save validation; it's a dynamic block so the enum change carries no validation risk). Live-verified enum now `[standard, trial, featured]`. **Remaining Task 7b** (reclassify cosmetic `variantStyle` on heading/text/label/quote + mobile-nav/divider) needs a Bean decision. **Task 5** (mega-menu) confirmed 0 variant scaffolding — needs a designed list.

---

## 2026-06-02 (theme thread) — Task 3: team-member display modes (D139)

**D139 — team-member gains genuine Compact vs Full display modes (Bean chose Option 1) — Task 3.** Ground-truth corrections (prompt was wrong on both): (1) the photo picker ALREADY existed — edit.js imports `MediaPicker` + renders it for `memberMedia` (the live review missed it); (2) the 3 inserter variations (team-card/team-compact/team-detailed) were **style-only** (set `className`/`cardStyle` = elevated/borderless/boxed) yet their descriptions FALSELY claimed content differences ("photo and name only, no bio"; "with bio + socials") that edit.js/render.php never delivered (all fields always rendered). Bean's decision (after a clarifying Q on Option 1 vs 2): **Option 1** — Compact genuinely reduced (photo+name+role); Standard + Detailed both full, differing only by card style. Build: new `displayMode` attr (`full` default | `compact`); variations set it (team-compact→compact, team-card+team-detailed→full); edit.js + render.php gate the bio + social-icons InnerBlocks + the hover-overlay on `!isCompact`; `sgs-team-member--compact` modifier class on both (parity); variation descriptions rewritten honest (Detailed = boxed style, same fields as Standard). **default `full` = zero regression** for existing cards; `save:()=>InnerBlocks.Content` unchanged → no deprecation needed. **Live-verified via Chrome DevTools Protocol** (the Playwright MCP profile was stuck on a stale lockfile held by a dead process — switched to superpowers-chrome `use_browser`, logged in, drove the editor): Full renders bio + socials; switching to Compact hides bio + socials and keeps name + role. version 0.2.0 (unchanged — attr-only). NB the Playwright-lock recovery is an environmental gotcha (the MCP chrome instance can leave a busy lockfile; CDP `use_browser` is the fallback).

---

## 2026-06-02 (theme thread) — Task 4: notice-banner per-type defaults + picker + error variant (D138)

**D138 — notice-banner upgraded to 5 semantic variants, each with an ideal default icon + the shared IconPicker for overrides (SGS THEME thread Task 4).** Bean-approved: added a 5th variant **error** (red: bg `#FEF2F2`, border `#DC2626` via `--wp--preset--color--error*` tokens w/ fallbacks) to the existing info/success/warning/accent. **Ground-truth corrections found before building (the prompt assumed "info/warning/success/error"):** the real set was info/success/warning/accent (no error); and the OLD icon system was **broken on the frontend** — edit.js offered 8 emoji icons but render.php only drew an SVG when the icon name matched a *variant* name, so picking truck/gift/star/clock rendered NOTHING on the live page. **Fix:** each variant now auto-sets a fitting default Lucide icon (info→info, success→circle-check, warning→triangle-alert, error→circle-x, accent→sparkles; map duplicated in edit.js + render.php), overridable via the Task-1 IconPicker (new `iconSource`+`iconName` attrs; empty = variant default). New `showIcon` toggle replaces the magic `icon='none'`; render.php BC derives showIcon from legacy `icon!=='none'` for old posts. render.php rewritten to resolve the icon from the 4 shared sources (lucide/wp-icon/dashicon/emoji) via the same maps as icon/icon-list (+ conditional Dashicons enqueue). Corner-radius: the native `__experimentalBorder.radius` control already existed (Bean hadn't found it under Styles→Border) — verified working (set 20px → applied), kept native, no duplicate added. notice-banner is dynamic (`save:()=>null`; deprecated.js v2/v1 round-trip old static instances) so the new attrs need no deprecation. **Live-verified (R-22-11, Playwright canary 144):** EDITOR — 5 variant options, Show-icon toggle, picker shows variant default, switching variant→error updates default to circle-x, override (picked star → {lucide,star}), reset-to-default works, my error block renders bg `rgb(254,242,242)` + border `rgb(220,38,38)`; native radius 20px applied. FRONTEND — page 144 (2 pre-existing cloned banners now using the new render.php) returns 200, no PHP warnings/fatals, banners render with icons (BC path holds). version 0.2.0→0.3.0.

---

## 2026-06-02 (theme thread) — Task 1: shared visual IconPicker (D137)

**D137 — Shared visual IconPicker built + live-verified on sgs/icon + sgs/icon-list (SGS THEME thread Task 1).** Replaced the type-the-name icon UX (1,917 Lucide names typed from memory; icon-list's 8-slug `@wordpress/icons` dropdown) with a reusable `plugins/sgs-blocks/src/components/IconPicker/` modal: search box + keyboard-navigable radiogroup grid (roving tabindex, arrow/Home/End, role=radio + aria-checked, 44px targets, WCAG 2.2 AA) across 4 tabs — Lucide (1,917) / Emoji (1,914) / WordPress (49) / Dashicons (curated). **Architecture (research-grounded — research-check 2026-06-02; memory `2026-06-03-gutenberg-icon-picker-architecture` + blub.db):** do NOT inline SVGs into the editor bundle (WP core hit exactly that bundle-bloat wall in core/icon, gutenberg#75715). Instead `scripts/generate-icons.js` (prebuild) emits static JSON datasets to `assets/icons/` — `lucide-icons.json` (952KB, the SVG map) + `lucide-tags.json` (search aliases) + `emoji.json` (from new build-time devDep `unicode-emoji-json`) + `wp-icons.json` (parsed from the canonical `includes/wp-icons.php` so the editor preview is byte-identical to the frontend). Datasets are fetched on demand per editor session (module-cached promise), never bundled, never on the frontend. The block stores only `{iconSource, iconName}`; render.php resolves the SVG server-side (single source of truth preserved — R-22-1 spirit: generated from the same source, not a hand-dup). PHP localizes the asset URLs to `window.sgsBlocksData.iconAssets` via a `wp-blocks` `'before'` inline script (filemtime cache-bust). **icon-list:** per-item picker + a default-icon picker; render.php made source-aware (4 sources) with backwards-compat for legacy `{icon:slug}` items (legacy→Lucide map) + conditional Dashicons enqueue; dynamic `save:null` so attribute-shape change needs no deprecation. **Live-verified (R-22-11, Playwright on canary 144 logged-in editor):** picker trigger renders in inspector → modal 4 tabs → Lucide grid loads on FIRST open (300 cells, real SVGs) + Emoji (300) → search filters by name+tags → click sets the block attrs + closes modal; zero own console errors (the 39 page errors are pre-existing cloned-content: see below). **Bug caught + fixed during live verify:** first-open Lucide load stuck on "Loading…" while Emoji + 2nd-visit Lucide worked → root cause a React useEffect cleanup race stranding a separate `loading` flag on the first source loaded → fixed by DERIVING loading from data-presence (no separate flag). **Pre-existing bugs surfaced (Task 8 scope, NOT this work):** `sgs/media` edit.js throws `ReferenceError: imageId is not defined` (editor crash = the "media cannot be previewed" symptom); `sgs/container` block-validation failures on the cloned page (cloning-thread converter issue). **Remaining Task 1 family:** notice-banner picker wiring is Task 4 (+ per-type defaults + corner-radius).

---

## 2026-06-02 (session 3, cont.) — CSS-transfer fidelity audit + static-div editor-error fix (D136)

**D136 — The cloning pipeline does NOT faithfully transfer the draft's CSS — 4 systemic gaps found by a draft-vs-clone computed-style audit; principle locked + static-div editor bug fixed.** Bean reframed the section-width problem as the pipeline's CORE job: "this is literally just about transferring the CSS values that exist in the draft into the clone." A Playwright draft-vs-deployed-clone computed-style diff (@1440px, mockup served on localhost since `file://` is Playwright-blocked) proved 4 transfer failures: (1) **framework IMPOSES `max-width:1200`** on full-bleed sections (featured-product/ingredients/gift/social-proof) — the draft has no section max-width (Bean's deliberate blank rule); the WP page template constrains any non-`alignfull` child to wideSize. Recommended fix = **theme-CSS keyed on structural position**: `.entry-content > .wp-block-sgs-container { max-width:none }` (top-level sections full-bleed by POSITION; composite-nested + explicit-max-width containers unaffected) — answers Bean's "why can't the template tell a section from an inner wrapper" (it can, via `>`). (2) **pipeline DROPS the `__inner` content wrappers** (ingredients/gift/social-proof `__inner`, max-width:960) — the FR-22-4.1 fold collapses them → content constraint lost. (3) **framework PAINTS a hero gradient** (`linear-gradient(135°,#C56A7A→#E68A95)`) the draft (solid pink) never had — the `#C76C7C` from the global measurement-vs-eye rule. (4) **`grid-template-columns` not transferred** (trust-bar__inner draft 266×4 → clone uneven 143/132/111/160; brand draft 122/782 → clone 450/450). **Two converter band-aids REJECTED** by Bean (detect-max-width→set-widthMode, gated on is_class_section_block then on a regex) — the fix belongs in the transfer layer / theme-CSS, never a walker conditional (memory `feedback_pipeline_transfers_draft_css_not_converter_detection_hacks`, STOP #33). **Also fixed (committed) the static-div editor bug:** `emit_sgs_container_wrapping` wrapped children in a static `<div class="wp-block-sgs-container">` + inline `<style>`, but sgs/container `save()` is `<InnerBlocks.Content/>` → WP validation rejected it → "unexpected/invalid content" on every cloned container in the editor (+ an extra nesting level breaking grid-on-section, likely feeding gap 4). Fix mirrors the already-correct `_emit_section_container`: emit children directly, no div, no duplicate inline style (CSS is in variation_buf). The committed `widthMode:'full'` width fix (e27ff591) is a PARTIAL band-aid covering only slug-resolved sections — to be superseded by the faithful-transfer fix. **NEXT (cloning thread):** the 4 transfer gaps, the priority. Audit + reframing captured in `.claude/next-session-prompt.md` + `.claude/scratch/2026-06-02-brain-dump-variant-routing-and-issues.md`.

---

## 2026-06-02 (session 3) — FR-22-20 universal variant detection BUILT + emit-proven + qc-council bug fixed (D134) + rollout-scope finding (D135)

