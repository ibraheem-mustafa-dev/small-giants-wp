# state.md archive — superseded snapshots (autoload-exempt, git-blame continuity)


## Archived 2026-06-06 (full pre-restructure state.md — gutted per doc-council finding #1: stale-stamped, 33KB over the 24576 cap, frozen at D167, working_tree-lie, page-144-vs-8, 12 stacked _step_* keys)

```
---
doc_type: state
project: small-giants-wp
project_id: 14
current_phase: "TWO active threads on main. CLONING (D167): WS-4 composite-wrapper mirror is BLOCK-SIDE COMPLETE — the 29-block container roster (4 section / 14 layout / 11 content) mirrors sgs/container via the shared helper (hero + product-card mirrored, mobile-nav + modal EXCLUDED via containerMirror:false, content-collection registered as 29th, cta-section conforming). /sgs-update reconciled (block_attributes 2110→2739, roster 29, 0 orphans). Architecture DEFINITIVELY resolved (blub.db 312): ONE unified procedure (Spec 22 §FR-22-21) converts containers + composites identically; docs-are-truth-not-code; KIND-scoped FULL mirror, NO per-block trim. NEXT = Method 2 — the SEPARATE converter work (the mirror does NOT fix page-clone fidelity; the converter routes mockup classes to sgs/container @conf 0.10, NOT the composite BLOCK): routing fix (.sgs-hero→sgs/hero before the container fallback) + converter-lift + #6/#4a + image sideload; then /sgs-update Stage-11 auto-apply + full doc reconciliation + WS-2/WS-3 de-cheat. THEME (D165): Spec 27 Phase 1 SHIPPED; Phase 2 is the next theme chapter. Plans: .claude/plans/2026-06-02-container-wrapper-standardisation.md + 2026-06-03-cloning-fidelity-triage-and-composite-remodel.md; canonical procedure Spec 22 §FR-22-21; cloning D-ceiling D167. Entry points: .claude/next-session-prompt.md (cloning, 2026-06-04 PM opener — METHOD 2) + .claude/next-session-prompt-theme.md (theme)."
current_subphase: "CLONING: the WS-4 block-side mirror is DONE; the next build priority is the SEPARATE converter 'Method 2' work. Run /sgs-clone on Mama's page 144, read the debug artefacts in artefact-order (leftover-buckets.json → trace.jsonl → stage-2.json/extract.json), diff draft-vs-clone rendered HTML via /browsing, and use /dispatching-parallel-agents + /brainstorming + /qc-council to architect ONE coherent converter design: routing fix (.sgs-hero→sgs/hero before the sgs/container fallback) + the converter-lift (transfer draft CSS onto the now-mirrored composite wrapper attrs; post-WS-4; curated canonical_slot precision per STOP #48) + #6 notice-banner sgs/text-child synthesis + #4a grid-lift + image sideload (#5). CRITICAL: the block-side mirror does NOT fix page-clone fidelity — re-running /sgs-clone WILL show sgs/container everywhere (converter emits containers, not composites — memory composite-mirror-is-separate-from-cloning-fidelity); that is EXPECTED, not a WS-4 failure. THEME: Spec 27 Phase 1 shipped (D165)."
last_updated: 2026-06-06
current_subphase_step_17_council_backlog_shipped: "THEME (2026-06-06, session 17, D180/D181): the adversarial-council must-fix backlog is CLEARED — Wave 1 (9 security/hardening: £0 is_purchasable, rate-limit-by-units, WP_Error-403, WP_DEBUG-gated test-hook, cron-RAND, placeholder no_image, rollback return-check, edit-safety TTL/de-dual-fire, lean-seed ≤24KB test; 04e62cdd+batch) + Wave 2 (8 value-ladder authoring-UI + UK consumer-law: authoring fields+controls, reference-price floor+ceiling+attestation, Best-value→Most-popular decoy, operator pack-size axis, sale-tail denominator, deny-list+NFKC, DMCC audit, inc-VAT ladder; 34e7e427) + a 3-team adversarial red-team (6 more gaps fixed: REST-meta legal-control bypass→show_in_rest:false, (bool)-attestation→strict '1'===, £0 save-100% skip, deny-list Unicode evasion, decoy badge, + a PREFLIGHT lean-subset-drift false-positive) + Wave 3 #2 (PREFLIGHT publish-block VISIBLE on the classic edit screen dbb96b6c + auto-set Google variesBy at provisioning 0bf4f2a7, via /subagent-driven-development). ALL live-verified canary 540, R-22-13 signed. /sgs-update clean (0 new blocks/attrs). REMAINING: Wave 3 #17 (file-split + CENTRALISE the lean-seed stripper — it's hand-copied in render.php/PREFLIGHT/the test + drifted, causing the false-positive) → Phase 2 (Spec 27 R4+F2; Spec 28 P2/P3; P4/R5 gated). The real first-shop blocker is the converter (cloning). See next-session-prompt-theme.md."
current_subphase_step_9_clusterC_complete_phase2_done: "THEME (2026-06-05, session 15, D176/D177): SPEC 27 PHASE 2 IS COMPLETE — Cluster C SHIPPED + QA-AUTHORING gate PASSED + Bean R-22-13 sign-off GRANTED. R2 provisioning/cartesian/transactional-rollback (NEW class-product-provisioning.php +-args +-helpers + shared class-product-authoring-security.php; conflict-safe pa_* provisioning, parent attrs merged-by-union, upsert dedup, 300-combo cap pre-write, ledger+snapshot rollback; EXTRAS bulk_update endpoint + triple-gated injected-failure hook; golden-master byte-identity + 0-orphan rollback + sibling-safety LIVE-PROVEN; e62f337f). R3 authoring/edit-safety (variesBy term-screen select filled the one gap; NEW class-configurator-edit-safety.php slug-rename + delete-with-orders warnings + orphan-meta cleanup; dd9d0d7d). PREFLIGHT hard go-live gate (NEW class-product-preflight.php transition_post_status revert-to-draft on £0/no-image/draft/over-cap/no-variesBy/invalid-jsonld + cart £0 422 + GET /preflight pre-check + weekly health cron; invalid_jsonld publish-gated 27e54132; live-proven bad→draft / valid→publish; dd9d0d7d). QA-AUTHORING e2e (scripts/e2e-authoring-acceptance.php): provision→author→publish→valid ProductGroup rich-results, ZERO raw-meta/WP-CLI — PASS. COURSE-CHANGE: authoring+PREFLIGHT pulled forward from 'Phase R roadmap' into Phase 2 Cluster C (Bean: complete whole spec before launch). Spec 27 reconciled to v5 + Phase-2 plan marked COMPLETE (53532c0d); FR-27-E3 image-sitemap clause corrected to DESCOPED. ONLY FR-27-R4/R5/F2 ('Cluster D' capstone, decision-gated) remain — do NOT block a first client shop. A first client shop is now complete, sellable, discoverable, AND client-authorable. NEXT THEME = Bean decision: first real client shop on this stack vs Cluster D; + Spec 28 P1 value-ladder (rides shipped B3). See next-session-prompt-theme.md."
current_subphase_step_8_clusterB_seo_clusterC_r1: "THEME (2026-06-05, session 14, D173): SPEC 27 PHASE 2 CLUSTER B (SEO) COMPLETE + CLUSTER C R1 SHIPPED. Cluster B live-verified on canary 540/589: E1 ProductGroup JSON-LD (NEW class-product-schema.php + manifest v6 incMinor/sku/gtin/saleEndDate/variesBy; SEC-1 manifest-only, SEC-2 inc-VAT, HEX-escaped, <=16KB low+high-anchor trim, SEC-8 enum+additionalProperty; seo-schema Rich Results 0 ERRORS; 1 cross-model BLOCKER fixed; 6ef7e7c6) -> E2 canonical (core already strips ?attribute_*->parent; NEW class-product-canonical.php indexVariationUrl OPT-IN override SEC-7/IDOR/SEC-9; ba96a4ff) -> E3 OG (inc-VAT SEC-2/9) + NEW class-product-sitemap.php (lastmod=MAX(parent,variations) SEC-6 + transient/purge + SEC-9) + breadcrumb placed page 589 (325b521f) -> F1 no-JS PASS. REGRESSION self-caught+fixed (3a1e95df): E1 manifest growth tripped product-card 24KB cap -> static fallback (pills gone) -> render.php seeds LEAN combo subset (strip schema-only fields + <2-img galleries; view.js falls back to imageUrl). Image-sitemap DESCOPED (Bean). CLUSTER C R1 SHIPPED (f747e58a): NEW class-product-authoring.php(+args) POST /sgs/v1/products/{id}/variations/{vid}+/attributes via WC set_*/save (byte-identical, never raw postmeta), per-object edit_post+nonce+rate-limit+IDOR+WC_Data_Exception-400; golden-master verified (price round-trips; lookup-regen correctly no-ops, woocommerce_attribute_lookup_enabled=no); cross-model review NO-GO->GO (save-outside-try BLOCKER + non-pa_-taxonomy hole + sale<regular all fixed). NEXT THEME = Cluster C R2 (provisioning + cartesian + upsert dedup + transactional rollback, heaviest) -> R3 authoring UI -> PREFLIGHT. D-numbers: theme D173; cloning D172; Spec 28 PARALLEL session took D174/D175 -> R1 needs a D176 entry (deferred, concurrent decisions.md commit-race). See next-session-prompt-theme.md."
current_subphase_step_7_clusterA_complete: "THEME (2026-06-04, session 13, D171): SPEC 27 PHASE 2 CLUSTER A COMPLETE — Bean R-22-13 sign-off GRANTED. B3 per-unit+sgs/label-badge+on-sale-Sale-badge+auto-contrast (ceb4e04a/5fe7cfd5) + A4 per-variation gallery+thumbnail-strip(delegated/imperative-rebuild)+prefetch+fixed-220px-box+editable-imageHeight-attr (77dccc9f/48fc54b7/5fe7cfd5) + C2 3-state sold-out-vs-unavailable + Step-7 demand-analytics REST(/sgs/v1/demand/attempt, SHA-256, ZERO PII)+admin (771f43ad). ESCAPE-AUDIT(fixed gallery-save BLOCKER + image-type sanitiser + SHA-256, 28607ac4) + QA-VIS(axe-0, 3-bp, self-review caught a clipped-thumbnail bug→fixed) PASSED. Label/badge→sgs/label convention registered (07975d7e). Spec 28 Smart Bulk Pricing drafted + 6-persona adversarial-council CONDITIONAL-GO + 4-phase re-scope (5cee5e5f). product-card 1.12.0. NEXT = Cluster B SEO (E1/E2/E3/F1) → Cluster C authoring + PREFLIGHT. Backlog: Spec 28 P1 value-ladder (rides shipped B3). See next-session-prompt-theme.md."
current_subphase_step_0_ws4_complete: "CLONING (2026-06-04 PM, D167): WS-4 composite-mirror BLOCK-SIDE COMPLETE across the whole roster. hero MIRRORED+live-verified (bacbde57, section KIND, double-emit guard, overlayColour→backgroundOverlayColour rename, split via extra_styles+wrap_inner:false); product-card MIRRORED (f68bdc6f + perf 82fd3b45, content KIND, all 5 source-mode branches, live wc-configurator preserved — verified page 589: interactive+context+44 pickers+sgs-container, 0 fatals); mobile-nav EXCLUDED (391e6cb1, containerMirror:false content KIND — Popover shell like modal); content-collection REGISTERED (40a9e03d, 29th roster block layout KIND + scanner widthMode fix); cta-section AUDITED conforming. Roster 29 (4/14/11). Architecture DEFINITIVELY resolved via 4-agent doc audit: ONE unified procedure converts containers+composites identically (§FR-22-21), docs-are-truth-not-code, KIND-scoped FULL mirror REQUIRED (no per-block trim — trust-bar is a grid-using section) — blub.db 312. /sgs-update reconciled: block_attributes 2110→2739, 0 orphans, 02-SGS-BLOCKS-REFERENCE regenerated. REMAINING (next session): Method 2 (run /sgs-clone + draft-vs-clone diff + parallel agents + /brainstorming + /qc-council to architect the CONVERTER work — mirror does NOT fix page-clone fidelity, converter routes to sgs/container @conf 0.10 not the composite BLOCK) → routing fix (.sgs-hero→sgs/hero) + converter-lift (universal-lift-was-premature-not-falsified) + #6/#4a + image sideload; /sgs-update Stage-11 auto-apply upgrade; full doc reconciliation (spec/flow/stages mark-complete + concise rewrite); WS-2/WS-3 de-cheat. See next-session-prompt.md (2026-06-04 PM opener)."
current_subphase_step_0_ws4_done: "CLONING (2026-06-04, D166): WS-4 composite-mirror BUILT — 25 composites mirror sgs/container via the shared helper (element route), all live-validated + committed: foundation 64950efa (byte-identical container refactor + helper + ContainerWrapperControls + propagation writer + Stage-11 report-only), section a18e6188/a0297c04, content 84a86b96/6634d2e2, layout grids 54032e86 (×9), interactive 0ad389b0 (×8); recipe canonical a4295130 (Spec 22 §FR-22-21.1/.2). Reliable small-batch fan-out proven (small batches + undefined-var self-check + orchestrator live test-page verify — fixed the 2026-06-03 5-way-overload failure mode). REMAINING (next session, Bean-directed): hero (inline w/ Bean — hardest), mobile-nav, product-card (coordinate theme); then /qc-council all, Stage-11 auto-apply upgrade, run /sgs-update, /systematic-debugging + /adversarial-council (the .sgs-hero→sgs/hero routing fix + universal-setup hole-poke), /handoff. Full plan: next-session-prompt.md (2026-06-04 opener). Mirror is SEPARATE from page-clone fidelity (converter emits containers not composites @conf 0.10 — memory composite-mirror-is-separate-from-cloning-fidelity)."
current_subphase_step_0_ws4_pm: "CLONING (2026-06-03 LATE-PM, D163): NEXT OPENER = WS-4 composite-wrapper mirror across ALL ~29 composites, KIND-scoped (NOT 4) — Bean's core directive, FOUNDATIONAL. 3 block fixes BUILT-uncommitted (need Gate-B visual-diff): #3 heading/label text-parity, #8 reviews-slider, WS-1c A3+A4 container; + composite-diff scanner extension. DEDUP AUDIT (Bean-inserted; 4-rater council overturned 3 of 4 merge guesses): NO block merges — overlap is plumbing-level → shared helpers + container-mirror; content-collection=register (29th, layout); #6=notice-banner option-a; bloat is a mirror problem not a merge problem. GENERIC CONVERTER-LIFT FALSIFIED by /qc-council (wrong path — real min-height is hero composite-interior; unsafe blind fingerprint; --has-min-height render-trap) — A6→WS-4 lift-only-gated, A5→curated _root_lift_rules + hero-composite-interior fix. Full orchestration plan (Opus-orchestrator role + skills/QC/subagent tables + per-task WS-4 blocks) in next-session-prompt.md. STOP #47-50 added."
current_subphase_step_0_ws1: "CLONING (2026-06-03, D159): WS-1 A1+A2 SHIPPED (commit 2f86d9e6, pushed) — the container/wrapper standardisation BUILD has begun. sgs/container gained `contentWidth` (guarded `__inner` render wrapper) + the converter now transfers each slug-None section's own max-width→widthMode (absent→full/alignfull→escapes the WP 1200 cap; present→custom) + lifts the folded `__inner` max-width→contentWidth. Live-DOM verified @1440 (R-22-11): the 4 target sections 1200/dropped→1425 full-bleed/content 1040|960; brand 1000 unchanged. 3-rater /qc-council design-gate. Visual-diff report PASS. REMAINING WS-1c: A3 custom-width centring, A4 raw-px gap, A5 min-height, A6 gridItem*. NEXT: page-level triage register #1-#8 (D159.3 — NONE an A1/A2 regression; #3/#4 root-cause-verified pre-existing) + WS-4 composite-remodel (Bean's sharpened directive D159.2: remove each composite's drifted wrapper, replace with exact sgs/container mirror, /sgs-update auto-propagates on version bump)."
current_subphase_step_1: "CLONING (2026-06-02, D152): Task 1 4-block save-null editor-/qc'd LIVE (PASS); Workstream A SHIPPED (0d746073 — sync-container-wrapping-blocks.py rewrite + container_kind migration + roster --apply all 28; 4-rater qc-council fixed the UPDATE-only silent undercount). Container/wrapper system deep-analysed (4-branch + 6-step target/current compare + artefact-empirical: the fold deletes __inner + strands its max-width). Bean reframe: NO composite evades R-22-9 — composites must MIRROR sgs/container (block_composition propagation). 5-workstream standardisation plan + Spec 22 §FR-22-21 + flow/stages + parking written + 2-rater doc-council (1d846667). On main @ c468af7a (theme thread co-active)."
current_subphase_step_2: "CLONING (2026-06-03): editor errors FULLY FIXED+verified (D141); is-style carry+tag-authoritative leaf routing (D145); sgs/button REPLACES core/button + multi-button grouping — P-9 COMPLETE (D146); button PRESETS via slots.standalone_block_default_attrs column + modifier path (D147, qc-council Finding-5 boolean-attr fix); video/iframe→sgs/media (D147); sgs/star-rating Trustpilot styles (D147). CSS-TRANSFER FIDELITY (4-gap D136) remains THE next priority. Branch merged to main (Bean-directed); feat/fr22-4-1-universal-wrapper deleted."
current_subphase_step_3: "THEME-THREAD Wave 1 (2026-06-02): sgs/option-picker SHIPPED (D144); notice-banner FR-22-6 migration SHIPPED; mega-menu layout library SHIPPED (7 patterns); 4 design fixes; sgs/icon enhancements; product-card Phase B SHIPPED; sgs/cart SHIPPED (WooCommerce badge count v1). D148/D149. Block count 66→68. Wave merged to main (a8cb3ff9)."
current_subphase_step_5_phase1_shipped: "THEME (2026-06-04, session 11, D165): **SPEC 27 PHASE 1 SHIPPED — Bean R-22-13 sign-off GRANTED.** All 6 hardening units SHIPPED + live-verified (U9 a11y `<button>`+moat-evidence afb7a65a / U8 cache+the M-C1 staleness-gap root-caused-and-fixed f15fa067 / U10 perf INP152ms d5af2c67 / U1 per-site capability-flag+toggle+migration 9fc9fecb / U11 WC<9.8 read-only degradation a97f52b8 / U12 cloning+schema-compat tests Jest14-14 9545ee24). THEN Bean's ship-gate visual review caught 7 UX gaps, all fixed+live-verified: card width 380 / pink resting pills / hover (1752fbe6); image+title product-page links + near-black desc + visible add-to-cart feedback (d9eddb4d, research-buddies-backed); sgs/cart in header (f9e3ae0c, full sell-loop badge 0→1); graceful no-image placeholder (c1f3b1c2). Self-review /ui-ux-pro-max + /playwright 3-breakpoint = A-. product-card 1.6.6. FAST-FOLLOWS (parked, not blocking): P-CONFIGURATOR-JS-WEIGHT-DEQUEUE, M-C10 dual-tax, WCAG-public-marketing audit, P-JEST-BLOCK-EDIT-MOCK-ROT. NEXT THEME = Spec 27 PHASE 2 (swatches/per-unit/galleries/JSON-LD/SEO/PREFLIGHT) — separate chapter, do NOT pull forward. See next-session-prompt-theme.md."
current_subphase_step_6_phase2_started: "THEME (2026-06-04, session 12, D168): **SPEC 27 PHASE 2 BUILD STARTED — 3 of ~11 units SHIPPED + live-verified on canary 540.** Step 0 foundations (class-configurator-meta.php registry [SEC-4 digit-strip + SEC-8 variesBy enum + per-object auth] + configurator-head.php wp_head scaffold + seed-48-sku-fixture-v2.php; 9b3a1724) → B2 swatches + I2 build-time WCAG auto-contrast on sgs/option-picker (configurator-term-fields.php authoring UI; 12 chips/axe-0/44px; 6cdff8d0; additive ON TOP of cloning WS-4 mirror) → TAX-UI per-card taxDisplayMode auto|inc-suffix|ex-plus-vat (manifest exMinor/taxMinor/regularExMinor + translated pctDisplay; SSR==swap; qc-council 3-rater PASSED, SEC-2 cart-untouched; 395c76c9). PLUS 3 fast-follows (Jest mock+key fix b80907c6, JS-weight dequeue 2bbec95a, moat sheet c3eb2907), UK tax-verify + manifest tax-fingerprint cache-bust (9e26de74), Phase-2 plan v2 after adversarial-council (89b00fa5/a6ef3fe7/74ada53d). Phase-1 plan ARCHIVED (plans/archive/2026-06-03-spec27-phase1-configurator-plan-complete.md). SCOPE (D168): demand-analytics IN; AI-builder R5+F2 = framework CAPSTONE (build last). NEXT = Cluster A remaining (B3 per-unit+label, A4 gallery, C2 OOS-vs-nonexistent, Step7 demand-analytics) → ESCAPE-AUDIT + QA-VIS (axe+ui-ux-pro-max+Bean R-22-13) → Cluster B SEO (E1 JSON-LD reusing review-schema+manifest, E2 canonical, E3 breadcrumb-place+OG+sitemap, F1). Plan: .claude/plans/2026-06-04-spec27-phase2-display-seo-plan.md (v2). See next-session-prompt-theme.md. product-card 1.8.0 / option-picker 0.1.6."
current_subphase_step_4: "THEME (2026-06-04, session 10, D164): Spec 27 Phase-1 PILL-SWAP QUARTET SHIPPED + LIVE-VERIFIED on page 589/fixture 540 — the sell-loop is real, Mama's can sell a variant. U3 (manifest+SSR seed, 7f096976: Product_Manifest::build + 48-combo per-instance context ≤24KB + 2 pickers, M-C1/3/5/7/9) → U4 (multi-axis pill-swap store, 6b4af10a: price/sale/stock swap, 0 XHR on change; fixed a latent Phase-C colon-event wiring bug via data-wp-init bridge) → U7 (secure add-to-cart, c903e760: posts selected variation + display-name variation[] + nonce to /sgs/v1/cart/add-item, cart holds variation 565 @ £24.49) → U5 (availability, 3bbb21b6: both-directions grey-out + GET /sgs/v1/cart/availability endpoint; all 4 a11y gates PASS axe-core0/keyboard/SR/44px). 5 bugs caught by orchestrator live-testing; cross-model councils per unit; GATE 2 Bean-signed-off. NEXT (theme): the REST of Phase 1 to the single whole ship gate — U9 (WCAG sprint + evidence sheet + the <a>→<button> fix) → U10 (perf/INP≤200) → U8 (cache+tax staleness guard) → U11 (WC<9.8 degradation) → U1 (per-site capability flag) → U12 (cloning-compat tests). Opus=orchestrator + Sonnet build. See next-session-prompt-theme.md."
latest_commit: "82fd3b45 (cloning D167: product-card perf follow-up — compact data-wp-context; WS-4 block-side complete. Theme thread co-active interleaving spec27 commits — verify HEAD + commit by explicit path)"
working_tree: "Code + docs committed; lucide-icons.php auto-regen (never committed)"
github_branches: "main (clean — no feature branches; two agent worktrees local-only)"
spec_22_implementation:
  status: ARCHITECTURAL_CLEANUP_BATCH_SHIPPED_STREAM_A_MEASUREMENT_PENDING
  spec: .claude/specs/31-UNIVERSAL-CLONING-PIPELINE.md
  spec_version: "0.5+FR-22-15+FR-22-16+FR-22-19 (FR-22-19 added + SHIPPED 2026-06-01 D128-D132 — composite scalar-media routing: hero + testimonial-slider; new scalar-media role; gate has_scalar_media_attrs. FR-22-15 added 2026-05-29 D96; FR-22-2.2 amended D99; §4 data layer slots + roles tables, slot_synonyms + legacy_role_lookup retired D99)"
  phase_plan_active: .claude/plans/2026-05-28-phase-2-hybrid-block-migration.md
  phase_1_5_close_note: "Phase 1.5 CLOSED with just Fix 1 shipped (5731dc36 walker FR-22-3 #3 ordering). Diagnostic value complete."
  phase_plan_archived: .claude/plans/archive/2026-05-26-phase-1-spec-22-implementation-closed-2026-05-27.md (Phase 1.5 closed 2026-05-27 per D90)
  d_decisions_2026_05_29: "D93 svg-merge | D94 /sgs-update Stage 1 UPDATE-on-drift + Stage 10 v2 | D95 trust+cert merge | D96 block_capabilities FR-22-15 tiebreaker | D97 sgs/media video extension | D98 container ship-blockers | D99 slots/roles unification + INSERT OR REPLACE + kind_override (link-href bug closed) | D100 Stage 10 v3 retired-blocks delete + phpunit stub"
  docscore: "100% Grade A (last verified pre-D99; re-verify post-architectural changes)"
  acceptance_gate_phase_1: per-section ≤5% × 3 viewports for all 7 body sections + Bean visual sign-off (R-22-13 co-authoritative)
  acceptance_gate_phase_1_5: per-section ≤1% × 3 viewports (stretch — bridges via pixel-diff.py vertical-anchor fix + chrome cropping + font-load timing)
  empirical_baseline_post_fix_1: "mean_mismatch_percent: 58.6% (pipeline-state/mamas-munches-homepage-2026-05-27-193804/stage-11-pixel-diff.json) — canonical baseline for D93-D100 batch validation; NEXT SESSION runs the new measurement to compare"
  walker_loc: "convert.py NEW = 1873 LoC; retired _retired/convert_pre_spec22.py = 4803 LoC; 61% reduction"
  hero_clone_poc: "/hero-clone-poc/ (page 29) — visual parity proof; 54.5% pixel-diff is 60px chrome-bleed alignment artefact, not visual divergence"
  retired_in_session_2026_05_29: ["slot_synonyms table (D99)", "legacy_role_lookup table (D99)", "sgs/svg-background block (D93)", "sgs/certification-bar block (D95)", "sgs/back-to-top + sgs/data-display + sgs/icon-block + sgs/reading-progress (D100 Stage 10 v3)"]
  db_state_post_d99:
    slots_element_rows: 92  # post-D111: 89 base + inner passthrough + testimonial/testimonial-slider re-inserted at element scope (12 dead section rows deleted)
    slots_section_rows: 4   # post-D111: keepers are core/group, hero, cta, cta-section
    roles_rows: 21  # D99 base 20 + scalar-media added 2026-06-01 D128 (§FR-22-19)
    property_suffixes_kind_override_populated: 17
    blocks_table_after_d100_cleanup: 66  # +2 added theme-thread 2026-06-02 (sgs/option-picker D144 Phase A + sgs/cart D149-adjacent) = 68 total post-wave-1
db_architecture_2026_05_29:
  hard_link_finding: ".claude/skills/sgs-wp-engine/sgs-framework.db and .agents/skills/sgs-wp-engine/sgs-framework.db share inode (NTFS junction point); same physical file. Prior 'mirror-DB divergence' framing structurally impossible. See memory/feedback_dbs_are_junction_not_mirror.md."
  real_two_dbs: "sgs-framework.db (~/.agents/skills/sgs-wp-engine/) + ui-ux-pro-max.db (~/.agents/skills/ui-ux-pro-max/scripts/) — different physical files. Cross-skill sync via /sgs-update Stage 8."
session_b_records:
  qc_council_report: .claude/reports/2026-05-22-session-B-qc-council.md
  session_summary: .claude/memory/session-summary-2026-05-22-session-B.md
  property_coverage_audit: .claude/reports/phase-5b-button-property-coverage.md
architecture_programme:
  staging_doc: .claude/plans/2026-05-21-architecture-staging.md
  phase_0_status: SHIPPED (commit aec54882, 2026-05-21)
  phase_0_5_status: SHIPPED (commit 6eaadbc2, 2026-05-21)
  phase_1_status: SHIPPED (commit 8c56ab6 in ~/.agents/skills repo, 2026-05-22)
  phase_1.5_status: SHIPPED (commit cc541e94, 2026-05-22)
  phase_2_status: SHIPPED (commit aca7c98 in skills repo, 2026-05-22)
  phase_3_status: SHIPPED (commit 79158da5, 2026-05-22)
  phase_4_status: "SHIPPED (commits 39d32799→99081252, 2026-05-22). POST-SHIPMENT UPDATE 2026-05-29 (D99): Stage 5 + Stage 7 queries updated to new slots table."
  phase_5a_status: SHIPPED-IN-SESSION-B (commit 43a93df9, 2026-05-21)
  phase_5b_status: SHIPPED-IN-SESSION-B (commit 60220b13, 2026-05-21)
  phase_5b_paint_fix_status: SHIPPED-IN-SESSION-B (commit 0ef032fe, 2026-05-22)
  phase_6_status: "SHIPPED-IN-SESSION-B (commit d307c8b0, 2026-05-22) with two non-blocking partials parked"
  phase_7_status: SHIPPED (commits da19374c + b26abf56, 2026-05-22)
wp_7_0_upgrade_status: "SHIPPED 2026-05-22 (Session B Hostinger op). sandybrown core 6.9.4 → 7.0."
blockers:
  - "NONE block the next session. The WS-4 block-side mirror is complete + verified; Method 2 (the converter work) is ready to start."
  - "The block-side mirror does NOT fix page-clone fidelity — that is the Method-2 converter work (routing fix + converter-lift), NOT a blocker (memory composite-mirror-is-separate-from-cloning-fidelity)."
  - "Known-open, NOT blocking Method 2: P-TEAM-MEMBER-SCHEMA-ORG-SAMEAS-RESTORATION (OPEN, SEO bucket — team-member sameAs JSON-LD lost in the InnerBlocks refactor); sgs/media.videoUrl canonical_slot backfill (low priority); seed-slot-synonyms.py references the retired slot_synonyms table (verify before invoking — this session used seed-composition-roles.py)."
---

# small-giants-wp — State Snapshot

## Human Summary

**CLONING UPDATE (2026-06-04 PM4, D172):** Method 2 converter-lift **CORE BUILT + live-verified** — **A1** DB-driven lift helper (`e9eaf013`), **A2+MF-B** composite-path lift + min-height centre-gate (hero renders `minHeight:520`, no regression — `0564d1f3`), **A3** slug-None section lift (203 attrs, brand grid lifted — `f4fa389b`), **FS-4** sgs/media grid-child 0×0 fix (2/3 images render — `0cbe3daf`). Earlier: routing solved (hero `sgs/hero`@1.0; trust-bar fix `c3443e03`); design D169 → qc-council → phase plan → adversarial-council → universality must-fixes D170. **REMAINING (next session):** FS-5 image sideload (MUST — brand/hero/product images are 404/un-uploaded; FS-4 fixed the CSS but they need the upload; auth-complicated webroot-runner) · A5 D6/D7 (testimonial `--Array` bug + `--send-to-ward` modifier + className carry) · D5 product-card fill · A4 sidecar delete (cleanup) · universality fixture (MANDATORY) + QA Gate 2 + Bean R-22-13 (incl. hero `align-items:center` hero-block-default question). pixel-diff held 57.0% (informational FR-22-18 — structural/editable parity is the gain; big pixel move needs FS-5). Build map: `.claude/plans/2026-06-04-method2-converter-lift-PHASE-PLAN.md` + `.claude/reports/2026-06-04-method2-converter-lift-EXPLAINED.md`. See `next-session-prompt.md` PM4 opener.

Two threads on `main`. **Cloning thread (D167):** WS-4 composite-wrapper mirror is **BLOCK-SIDE COMPLETE** — the 29-block container roster (4 section / 14 layout / 11 content) mirrors `sgs/container` via the shared helper (hero + product-card mirrored; mobile-nav + modal EXCLUDED via `containerMirror:false`; content-collection registered as the 29th; cta-section conforming). Architecture DEFINITIVELY resolved (blub.db 312): ONE unified procedure (Spec 22 §FR-22-21) converts containers + composites identically; **the docs are the standard of truth, NOT the unbuilt converter code**; KIND-scoped FULL mirror with **NO per-block trim**. `/sgs-update` reconciled (block_attributes 2110→2739, 0 orphans). **The mirror is block-side ONLY — it does NOT fix page-clone fidelity.** The next build priority is the SEPARATE converter **"Method 2"** work: the converter still routes mockup classes to `sgs/container` (conf 0.10), not the composite BLOCK, so the cloned page needs the routing fix (`.sgs-hero`→`sgs/hero` before the container fallback) + the converter-lift + #6/#4a + image sideload, then `/sgs-update` Stage-11 auto-apply + WS-2/WS-3 de-cheat. **Theme thread (D165):** Spec 27 Phase 1 SHIPPED; Phase 2 is the next theme chapter. See `next-session-prompt.md` (cloning — Method 2 opener) + `next-session-prompt-theme.md` (theme) for per-thread entry points.

## State Snapshot

- **current_phase:** TWO THREADS ON MAIN. CLONING (D167): WS-4 composite-mirror BLOCK-SIDE COMPLETE (29-block roster mirrors sgs/container; hero+product-card mirrored, mobile-nav+modal excluded, content-collection 29th); NEXT = the SEPARATE converter "Method 2" work (mirror does NOT fix page-clone fidelity). THEME (D165): Spec 27 Phase 1 SHIPPED.
- **current_subphase:** CLONING: Method 2 — run /sgs-clone on Mama's 144, read artefacts, diff draft-vs-clone, architect the converter design (routing fix .sgs-hero→sgs/hero + converter-lift + #6/#4a + image sideload). THEME: Spec 27 Phase 2 (next chapter).
- **current_step:** CLONING → see `.claude/next-session-prompt.md` (2026-06-04 PM — Method 2). THEME → see `.claude/next-session-prompt-theme.md`. Block count: 69 sgs / 191 total (post-D167, incl. content-collection).
- **latest_commit:** 82fd3b45 (cloning D167 product-card perf) + the D167 doc-reconciliation commit. main HEAD MOVES — theme thread interleaves spec27 commits; verify `git log -1` and commit by explicit path.
- **working_tree:** Code + docs committed; lucide-icons.php auto-regen (never committed).
- **github_branches:** main (clean — no feature branches; two agent worktrees local-only, not on GitHub).
- **spec_22_status:** **ARCHITECTURAL_CLEANUP_BATCH_SHIPPED** — D93-D100 + D141/D145/D146/D147/D152 live on origin/main. FR-22-15 added (capability-aware tiebreaker); FR-22-2.2 amended (roles table); §4 data layer updated (slots 96 rows / roles 21 rows); slot_synonyms + legacy_role_lookup retired.
- **empirical_baseline:** pixel-diff is informational per FR-22-18 — latest canary run `mamas-munches-144-2026-06-02-224706`; cite run id + date, never a bare percentage as a gate.
- **session_commits_2026_05_29:** bcbafe09 (D93-D100 architectural batch, 99 files +5346/-2275) → e2df4eca (handoff docs).
- **blockers:** CSS-transfer fidelity (4-gap D136) is the next build priority; B1 decision (D1 sidecar revive vs DB-replace) needed before WS-2; WS-1 must complete before WS-4. None block session entry.

## What changed at the data layer (D99)

The biggest architectural change this session. Was:
- `slot_synonyms` (89 rows × 9 columns) + `legacy_role_lookup` (16 rows × 4 columns) — two tables, both slot→block mappings at different scopes
- `slot_synonyms.role_classification` populated by `_migrate_role_classification()` UPDATE-only migration — link-href missed because no slot_synonyms row had `role='link-href'` → walker gate returned 4 of 5 spec-defined content-bearing roles

Now:
- `slots` table — post-D111 (2026-05-30): 96 rows total (92 element + 4 section). 12 wrong/dead section-scope rows DELETED (ingredients-section, ingredients, gift-section, social-proof, testimonial section-scope, testimonial-slider section-scope, brand-story, featured-product, header, footer, site-header, site-footer); 2 rows re-inserted at element scope (testimonial → sgs/testimonial, testimonial-slider → sgs/testimonial-slider) plus `inner` passthrough element row added. Section keepers (4): core/group, hero, cta, cta-section.
- `blocks.tier` column (D107 2026-05-30) — populated from `supports.sgs.is_section_root` in block.json. Seeded rows: `sgs/hero`, `sgs/cta-section` (tier='class-section'). Voter at `per-section-convention-voter.py:295-305` queries this column to segregate section-root candidates from inner-div candidates.
- `block_composition` table (D108 2026-05-30, 189 rows) — block-shape catalogue (block_slug PK, wraps_block, composition_role enum [section-root|wrapper-shell|content-block|leaf], has_inner_blocks, accepts_allowed_blocks, container_kind). Walker consumption of `composition_role` + `container_kind` deferred (WS-3); data layer valid. Refined-trigger work parked at P-XS-3-TRIGGER-REFINEMENT.
- `roles` table (20 rows, columns: role_name PK + classification + description) — per-role catalogue, seeded by `_migrate_roles_table()` via INSERT OR REPLACE from `_ROLE_CLASSIFICATION_MAP` Python dict; all 5 content-bearing roles correctly populated
- `property_suffixes.kind_override` column (17 rows populated) — replaces `_KIND_BY_SUFFIX` hardcoded dict
- `html_tag_to_core_block` seed migration switched INSERT OR IGNORE → INSERT OR REPLACE (prevents seed/DB divergence)

Walker now queries `slots WHERE scope='element'` for BEM-element resolution + `roles WHERE classification='content-bearing'` for the gate. `per-section-convention-voter.py` queries `slots WHERE scope='section'` for --legacy mode mockup recognition.

Empirical effect: `equivalent_block_for('sgs/media', 'videoUrl')` now returns `'sgs/media'` (was `None`); video URLs would route as child blocks when walker encounters them in mockup DOM. Caveat: depends on Subagent D's new video attrs in block_attributes getting canonical_slot backfilled — that's a Stream A task carried to next session.

```


<!-- rotated 2026-07-05 by /handoff (D276 programme-complete rewrite) -->
---
doc_type: state
project: small-giants-wp
project_id: 14
last_updated: 2026-07-04-D275
note: "LEAN snapshot (single active workstream as of 2026-06-17 — the cloning CSS-transfer rebuild; theme/Spec-30 work COMPLETE + archived). Full history → memory/state-archive.md. This file holds ONLY the current pointer; detail lives in handoff.md + next-session-prompt.md (the SoT). Do NOT restate D-numbers / counts / commit hashes here — they drift. ≤24576 bytes."
---

# small-giants-wp — State Snapshot (lean)

## Human Summary
Cloning CSS-transfer foundation (Phase F) COMPLETE; modular-scaffold VERTICAL SLICE COMPLETE (D243); recognition / Method-2 BUILT + LANDED (D244). **Step-3's content / block-equivalent extraction stage (the child-shape fork, pipeline Stage 3-4) is now BUILT + LANDED + Bean-signed-off 2026-06-26 (D245):** a recognised composite's child CONTENT lifts to native attrs (was blank after D244) — a draft testimonial quote → genuine `build_block_markup()` emit → deployed to a live canary → quote element `innerText` reads the draft text anonymously (independent oracle), Bean signed off live (R-22-13). ONE universal stage (Spec 31 §12.0), built via the vertical-slice cadence (testimonial quote LANDED first, then generalise). Two DB-driven mechanisms, names no block: A = scalar-content-lift via `derived_selector`; B = child-block via `slot_has_equivalent_block`. Design-gate caught a project-threatening fatal fork error (v1 `equivalent_block_for(slug,slot)` → would recreate D212) across 2 council rounds; qc-council found+fixed 4 more build bugs; 316 tests green; `content_gap_check` gate built+wired (silent content-drops now commit-blocking); convert.py byte-identical (D-MODULAR). **D246 (2026-06-27) — the D245 content build is ARCHITECTURE-SUPERSEDED.** Reading the FULL Spec 31 (§1 "ONE lift path") showed content + CSS are ONE dispatch: the working `_lift_scalar_attrs_by_selector`/`_lift_scalar_media_from_img` functions collapse into the SAME dispatch as the CSS resolvers. D245 wrongly built a SEPARATE parallel content engine (extract_content/run_mechanism_a/b + new db_lookup columns + bespoke media-shaping) recreating those functions. **NEXT = redo content UNIFIED:** read the two working functions, modularise them into resolvers in the ONE `(block,layer,property,tier)→dispatch` table next to the CSS resolvers, rewrite Spec 31 §3 as the unified content+CSS routing (absorbing Spec 22's working content logic so 22 can be archived). Do NOT rebuild content; do NOT extend the separate engine. Recognition (Stage 2 / D244) is correct + unaffected. The D245 commits are kept as history. Design doc + the qc-council 7-gap NO-GO: see `.claude/plans/2026-06-26-stage3-child-shape-fork-design.md` v3 + Spec 31 §2 Axis-3 status note. D-ceiling D247 (D246 = the content-UNIFY correction; D247 = the qc-council + A1/A3-fix + A2-ruling record), branch `main`. Memory: `do-not-recreate-working-functionality-modularise-it`, `content-completeness-is-a-ledger-gap-not-a-lift-patch`. **D248 (2026-06-28) — Bean-directed array detour, NOT W3:** the array path was rebuilt Path-A data-driven (Spec 31 §3.B4) + the per-element role handlers promoted to ONE shared `field_extractors.py` (B1+B4 delegate; drift impossible); import-ban opened to `icon_resolver`; Spec 31 gained §3.B.0 (universal recognise→content→CSS principle) + a top-of-doc SCOPE-CONTRACT banner; STOP-29 captured (bind definition-of-done to the spec's full scope — the root-cause of repeated half-jobs). New engine still INERT in production. NEXT = W3 walker port + the CSS branch (§3.A) + array residuals + production-wiring. SoT = handoff.md + next-session-prompt.md. Memory: `bind-definition-of-done-to-full-spec-scope`.

## State Snapshot

## GROUND TRUTH FIRST — do not trust any doc's cached status; verify these every session
- **HEAD + tree:** `git log -1 --stat` + `git status` + `git branch --show-current`. NO doc's "latest_commit"/"working_tree" line is authoritative — verify against git.
- **D-number ceiling:** `grep -oE 'D[0-9]+' .claude/decisions.md | sort -V | tail -1` — `decisions.md` is the SOLE source of truth for the D-ceiling. Never assign a D without this check.
- **Framework counts (blocks/attrs/slots/roles):** query `/sgs-db` or `/wp-blocks` — the DB is authoritative; counts are NOT maintained in prose anywhere.
- **Commit discipline:** commit by EXPLICIT PATH (`git commit -- <paths>`), never `git add -A` — keeps unrelated in-flight edits out of a commit. `main` is the source of truth.

## ACTIVE WORKSTREAM — Cloning CSS-transfer rebuild (the ONLY active work)
- **SoT for current status:** `.claude/handoff.md` (latest session) + `.claude/next-session-prompt.md` (the operative opener, with the MANDATORY READING GATE). READ THOSE — do not rely on a summary here.
- **One-line where-we-are (2026-07-04 LATEST, D273-D274 — scope FACT-CHECKED, completion plan APPROVED, EXECUTION Steps 1-2 DONE):** **The converter completion plan is Bean-APPROVED (delete-last / 6-phase / FR-31-2.7 / SINGLE-session scope) and its first two execution steps shipped.** Fact-check vs the running scripts + a real clone corrected the D272 scope (A1 media-map FALSE — media live end-to-end, LANDED; **frozen fallback = ZERO Mama's sections**, measured; parity baseline content 77% / CSS 47-54); pseudo-element `::` parse bug FIXED (`3d7e7d42`); 12 old plan docs archived (residuals → `P-W3-ARCHIVE-RESIDUALS`); EXECUTION plan written (16 steps, gates A/B/C, solo-Sonnet orchestration per the corrected STOP-39); flow docs REGENERATED from live scripts (/qc + council-verified — FIVE false rater/agent claims caught by fact-checking, incl. **css_router D1 = DEAD output**); architecture LOCKED into Spec 31 as **FR-31-2.7 + FR-31-2.8** + full staleness sweep (the spec is session-start-read accurate). All pushed. **NEXT = EXECUTION Step 3 (Ctx destination contract) → Phase-2 core (lands product-card + the ONE cascade).** A2 ledger = the only remaining STOP-28 gate. D-CEILING: **D275** (the parallel product-card session). SoT = handoff.md top + next-session-prompt.md.
- **One-line where-we-are (2026-07-04, D275 — parallel session: product-card legacy InnerBlocks PURGED; page-8 cards emit clean but content-EMPTY until Phase 2):** **The product-card's legacy child-block machinery is deleted at the source** (block.json `allowedBlocks`, index.js `<InnerBlocks.Content/>` save, render.php FP-H `$content` bridge, edit.js legacy-bridge UI + warning — all gone; root cause proven: the old "removal" never landed in ANY file, and `has_inner.py` derives from the source markers so DB edits were irrelevant). DB resynced (`hib_updated=1`, derived=0), plugin deployed to sandybrown, page 8 re-cloned: cards emit ZERO children + render via the built-in path live, **but `productName`/`priceLarge`/`ctaText` are EMPTY** (`primary_content_attr → ambiguous`) — the element→attr routing is the FR-31-2.6 per-attr walk (completion-plan Phase 2 "lands product-card"), deliberately NOT shipped inline. **First brick BUILT + TDD-green (INERT):** `db_lookup.content_attr_for_element` (match-strength ranking, `test_content_attr_resolver.py` 3/3). 281 converter tests green. ~~UNCOMMITTED~~ **COMMITTED + PUSHED** (`291ed7ce`/`68cd32ba`/`7f58a95c` — verified 2026-07-04 by the D273-D274 session; only `test_extraction.py` modifications remain uncommitted). Do NOT read the empty page-8 cards as a regression — pre-D275 they were text-block soup, not correct either. D-CEILING: **D275**. SoT = handoff.md top + next-session-prompt.md.
- **One-line where-we-are (2026-07-04 LATER — converter REBUILD scoped; emit_shape foundation built):** The pipeline content-extraction is being rebuilt: kill Mechanism A/B + block-level `has_inner`, replace with ONE universal per-attr walk (single walker + total registry + pure priority-ranked handlers — Babel merged-visitor, research-locked) using the per-attr `emit_shape` fork (FR-31-2.6). `emit_shape` FOUNDATION BUILT + committed (column + `/sgs-update` seeder + `db_lookup.emit_shape_for` + `render_emits.render_reads_attr`) — additive, INERT, not wired. Full completion scope in `.claude/plans/2026-07-04-new-engine-to-parity-delete-converter-v2.md` (6 phases, DELETE-LAST: `converter_v2`/`convert.py` deleted only after the new engine reaches §2.7 parity). **⚠ The scope is DOCUMENTATION-DERIVED and UNVERIFIED — next session MUST fact-check it against the running scripts + a real clone FIRST (Bean: the "media loader not built" claim is WRONG, media loads).** SoT = handoff.md top + next-session-prompt.md.
- **One-line where-we-are (2026-07-04, D271 — SIDE session, block/converter infrastructure; does NOT touch the cloning thread):** **core→SGS routing consolidated to two git-tracked data files + all block deprecations removed plugin-wide.** Atomic bare-tag routing now points DIRECTLY at SGS blocks via `scripts/data/atomic-tag-map.json` (was a hardcoded dict); `replaces` moved out of all 23 block.json into `scripts/data/block-replacements.json` (33 mappings). **Bean-locked architecture: the DB tables are the RUNTIME source; the two data files are git-tracked rebuild-insurance seeds.** Also: 34 `deprecated.js` deleted + wiring stripped (no-deprecations policy in plugin CLAUDE.md); TOC render.php/edit.js now scan `sgs/heading` (pre-existing bug); composites reference SGS inner blocks not core; docs-registry old specs 20/21 → new spec 20. The audio/video one-time prompt is DELETED (this was its Task 4). 324 conformance + 28 atomic tests + /qc 6/6 + build clean; commit `1755a21f`, pushed. D-CEILING: **D271**. Cloning thread's next work is UNCHANGED (feature-grid/product-card per next-session-prompt.md). Memory: `db-is-runtime-source-data-file-is-rebuild-insurance`.
- **One-line where-we-are (2026-07-04, D270 — CLONING pipeline: feature-grid 4-col FIXED + LANDED):** **feature-grid renders through the shared container grid engine now — 4/4/2 desktop/tablet/mobile on page 8, Bean eye-confirmed.** Root cause proven on the LIVE node (STOP-43, Bean corrected my code-inference + was right): the wrapper ALREADY emitted `repeat(4,1fr)`; feature-grid's OWN render.php auto-flex `<style>` overrode it by specificity. Fix = block delegates to the shared engine when an explicit `gridTemplateColumns` is present (grid=default, auto-flex opt-in); wrapper suppresses the tablet/mobile count shorthand when a base template governs (D228 family). NO converter change. All 6 page grids regression-scanned (no regression); independent diff review clean; 374 tests + cheat-gate green; convert.py byte-identical. Commits `9a437113`+`be8e721e`+`409a47fc`, pushed. **NEXT (last cloning fidelity task in the main prompt) = product-card typed-mode Layer-B structure (price 18px Inter → draft 28px Fraunces bold).** D-CEILING: **D270**. Memory: `block-own-render-can-override-shared-wrapper`. SoT = handoff.md top + next-session-prompt.md.
- **One-line where-we-are (2026-07-04, D268-D269 — dedicated sgs/audio block + sgs/media audio-removal + branded video player; a SEPARATE workstream from the cloning pipeline):** **`sgs/audio` block SHIPPED + /qc PASS (D268, `596b9943`); sgs/media audio-mode REMOVED + branded video player SHIPPED + /qc-council GO (D269, `e8bebd39`); both LANDED on sandybrown + pushed.** sgs/audio = a native `<audio>` player upgraded by a Web Audio `view.js` to one of 7 styles (minimal/waveform/spectrum/radial/oscilloscope/gradient-pulse/hidden) + AudioObject schema + perf (visibility-gated RAF, first-play graph, reduced-motion); replaces `core/audio` (removed from sgs/media). sgs/media video mode re-skinned with a themed player (centre-play + hover control bar: scrub/time/mute/volume/fullscreen, keyboard, iframes untouched); `replaces=[core/image,core/video]`, v1.5.0. Two QC bugs found+fixed live: sgs/audio `crossorigin` broke non-CORS playback; media wrapper keydown stole arrows from the sliders. 374 tests + cheat-gate green; npm build + WPCS clean. Docs: decisions.md D268/D269, `reports/visual-diff/{audio-2026-07-03,media-2026-07-04}.md`, one-time prompt `next-session-prompt-audio-video-blocks.md` (Tasks 1-3 DONE; Task 4 = finish the core→sgs table + a routine `/sgs-update` reseed remain — then DELETE that prompt). This did NOT touch the cloning pipeline (that thread's next work is still feature-grid/product-card per the main prompt). D-CEILING: **D269**. SoT = handoff.md top + the two prompts.
- **One-line where-we-are (2026-07-03 EVEN-EVEN-LATER, D267 — ran PARALLEL to the D264-266 session):** **Universal L2 interior band-CSS fold BUILT + LANDED on page 8 (contentWidth + textAlign) — COMPLETES the D264-266 prompt's "Task 1 — L2 content-width", the RIGHT way.** Proven on the real draft (STOP-43): the "4 composites lack contentWidth / run the mirror sync" premise was WRONG — the 4 default-container sections already fold contentWidth; only trust-bar (a composite, has_inner=0/array) dropped it, because NO path folds a composite's sole pass-through inner. Fix (ONE mechanism, R-31-9, Spec 31 §2.4/§13.4 FR-31-5.3): shared `_sole_passthrough_child` + `build_block_markup` step-3c composite fold via `route_interior_css_to_parent_slot`; default fold switched to the same router (BEM-less fallback both paths); +co-declared var() resolution +text-align→native textAlign. Render-side: `SGS_Container_Wrapper` emits `has-text-align-*` explicitly (WP core doesn't for the dynamic composite — WRITTEN≠LANDED caught it, STOP-44). `containerKind` declared in info-box/feature-grid block.json. LANDED page 8: trust-bar 1100 / featured 1040 / ingredients·gift·social 960 + ingredients fully centred, Bean eye-confirmed. qc-council 3-rater GO. 374 tests + cheat-gate green; convert.py byte-identical. Commits `a438bb41`/`5205f170`/`516a5790`, all pushed. **NEXT = feature-grid 4-col (investigate the premise — the "auto-flex" claim is probably wrong, Bean) + product-card structure.** D-CEILING: **D267**. SoT = handoff.md top + next-session-prompt.md.
- **One-line where-we-are (2026-07-03 EVEN LATER, D266):** **Task 4 re-clone LANDED all 4 held fixes on page 8 (emoji+star+16px+min-width) + icon-source correction (D264, DB role-dispatch, pushed) + core→sgs replacement mapping many-core→one-sgs (D265, 25 mappings, pushed) + 2 block features (D266: sgs/divider gap variant, sgs/media audio mode + tag hooks — LANDED on sandybrown, pushed).** All pushed; branch `main` at `38c27ca2`; 370 tests + cheat-gate green; convert.py byte-identical; npm build clean. **Audio-player design: artifact published (6 styles) + Bean decided a DEDICATED `sgs/audio` block (7 variants + Web Audio visualiser) + re-skin sgs/media video player + finish core→sgs table — split into the one-time `next-session-prompt-audio-video-blocks.md`.** The MAIN `next-session-prompt.md` now carries ONLY the remaining CLONING work (L2 content-width universal, feature-grid variant, product-card structure). SoT = handoff.md top + the two prompts. D-CEILING: **D266**.
- **One-line where-we-are (2026-07-03 LATER, D263):** **Tasks 0-3 done — Gate A unblocked + D259 committed/pushed; theme font 16px LANDED; trust-bar star fill control + universal icon-content lift built; fingerprint migration done (zero-regression proven).** Task 0: Gate A was blocked by a long-standing `composition_role='leaf'` misclassification of 5 typed-array blocks (not by D259) — reclassified to `content-block` (completes D150), Gate A 43/43 no-regen; + `seed-composition-roles.py` wired into `/sgs-update` so it's reseed-durable (planted-revert proven). Task 1: `theme-snapshot.json` base 18→16px LANDED (18px mismatches→1). Task 2: trust-bar per-icon `fillStyle`/`fillColour` client control + converter `is_filled_glyph`. Task 3a: fingerprints.json→`ATTR_CLASSIFICATION_OVERRIDES` (behaviour-preserving, fresh-reseed copy-DB diff = 0 regression). Task 3b: universal icon-content lift (shared `resolve_icon_kind` emoji/lucide/svg + `run_mechanism_leaf` icon arm, Spec 31 §3.B.0) — the real info-box emoji now lifts (premise verified empirically). 367 tests + cheat-gate + F6 green; convert.py byte-identical. **All pushed EXCEPT `31358f84` (info-box icon) — held for LANDED at the Task-4 re-clone.** NEXT = Task 4 re-clone → LANDED-verify emoji+star+theme+min-width together + parity scores, then push. D-CEILING: **D263**. SoT = handoff.md top + next-session-prompt.md.
- **One-line where-we-are (2026-07-03, D259):** **min-width/max-width cross-device tier CASCADE (FR-31-5.2) BUILT + LANDED on page 8 (trust-bar 375=2/768=4/1440=4, Bean-confirmed).** The live `collect_css_decls_for_element` matched `@media` by substring + silently dropped non-marker thresholds (`min-width:600`); rebuilt as a device-tier cascade (sample the CSS cascade at Desktop 1440/Tablet 800/Mobile 375; Desktop→base, Tablet/Mobile→suffixed; min/max symmetric; inverts mobile-first into SGS desktop-base; non-device thresholds → non-silent F-ii log). 320 tests (+9) + cheat-gate green; convert.py byte-identical. **Commit BLOCKED by Gate A (golden-fixture regen needed — intended emit change); held from push.** A full clone-vs-draft parity audit produced root-cause THEMES: (A) typography DEFAULTS (draft base 16px vs clone theme 18px → brand-quote + no-explicit-size text; inherited line-heights) = TOP next fix, theme-layer; (B) L2 content-width UNIVERSAL (`__inner` max-width drops; 4 composites lack `contentWidth` — mirror sync report-only); (C) feature-grid `layoutMode=auto-flex` ignores columns; (D) product-card structure. **Reliable-parity method LOCKED (CLAUDE.md rule 4a / STOP-42): computed values matched by CONTENT, never declaration-diff/wrapper-keying.** Bean doubts the pipeline drop-logs' counts (cumulative/input-side, not per-clone). SoT = handoff.md top + next-session-prompt.md. D-CEILING: **D259**.
- **One-line where-we-are (2026-07-02 LATER, D258):** **Array-item content lift COMPLETED for the 5 gap-blocks + LANDED on page 8.** Each dropping item field DECLARES its extraction `role` in `block.json items.properties.<field>.role` → seeded to a new `array_item_schema.role` column by the existing Stage-1 seeder (no parallel table); the resolver (`converter/resolvers/array_content.py`) prefers the declared role + gained L1b BEM-element-segment matching (pricing-table's 5 same-role text fields each find their own element; one `<a>__cta` feeds ctaText+ctaUrl by role) + L1c flat-item self-extraction (social-icons) + a root-inclusion fix in `_find_item_nodes` (direct-child-of-root items were undetected). **Name-heuristic seeder (peel-suffix+last-segment in assign-canonical.py) trialled + REJECTED** — ownership comes from the block CODE/declaration, never name-parsing (Spec 31 FR-31-2.1a; + FR-31-2.5a array recognition signature). **LANDED** on sandybrown page 8 via `SGS_NEW_ENGINE=1`: trust-bar renders its 4 real draft badges (icon+label), no phantom row, no default cheat. Commit `8375debb` + docs `95644742`, on `main`, NOT pushed (Bean page-8 eye sign-off pending). 311 tests + cheat-gate green; convert.py byte-identical (D-MODULAR). **2 open trust-bar gaps (both styling, parked):** P-MINWIDTH-CROSSDEVICE-TIER (min-width:X must emit every device tier ≥ X — 4-across drop, TOP next task) + P-RAWSVG-FILLED-VS-OUTLINE (star must render filled → a client-facing block control). Full backlog + D101 carry-forward in `parking.md`. SoT = handoff.md top entry + next-session-prompt.md. D-CEILING: **D258**.
- **One-line where-we-are (2026-07-02 LATE, D257):** **DB-recognition array field-lift + role-fallback BUILT + committed (Bean's design), replacing the hand-declared `arrayItemSchema` (R-31-9 violation, Spec 31 §3.B0.1).** `converter/resolvers/array_content.py` rewritten: structural item detection + 2-layer match (canonical-slot name, then ROLE-fallback — a `__text` child fills the `label` field via shared `text-content` role). Item field names now DB-driven (`array_item_schema` seeded from block.json `items.properties`). 8 array blocks de-hardcoded; client-copy cheats removed (trust-bar/product-card); polygon-star → `iconSvg`; the F5 `no_slug_literal` gate caught + fixed 3 per-slot/role literal carve-outs (STOP-41). 7 commits `c7fda7db`→`f892d585` on `main`, NOT pushed. 306 tests + cheat-gate + F5/F6 green; convert.py byte-identical (D-MODULAR). **NOT LANDED yet — next session: (1) LANDED-verify trust-bar on page 8; (2) complete the slot vocabulary so 5 gap-blocks' content fields resolve (icon-list/social-icons icons, card-grid badge, pricing-table cta/ribbon — needs the STOP-24 alias-reseed hole fixed).** No design-gate for pipeline tasks — follow Spec 31 in every detail (Bean). SoT = handoff.md top entry + next-session-prompt.md. D-CEILING: D257.
- **One-line where-we-are (2026-07-02, D256):** **Spec 31 §2 layer-extraction BUILT + LANDED on sandybrown page 8 + Layer-A bare-tag lift + §2.3 layout trigger + string image lift + media-sideload determinism.** New engine (`SGS_NEW_ENGINE=1`) clones the Mama's homepage with faithful content + grids that RENDER; qc-council-audited CLEAN + universal (all 5 fixes; cheat-gate 0 NEW; R-31-1..15 followed). Bean re-review: ingredients grid + social-proof flex-row FIXED, images load. STILL BROKEN (next session, `/systematic-debugging`): (1) products/gift/ingredients don't reach DESKTOP column counts — ONE root cause, `min-width:X` mapped to one tier / dropped (must emit every device tier ≥ X + §3 F-ii passthrough for 600/640); (2) product-card typed-mode = plain text → Bean-specced Layer-B rebuild to feature-parity with bound mode (Task 1); (3) trust-bar spurious all-caps 1st row; (4) ingredient `__icon` emoji unlifted. 4 commits `87816090`→`db501007` + D254 set, NOT pushed. 326 tests + cheat-gate green; convert.py byte-identical (D-MODULAR). SoT = handoff.md top entry + next-session-prompt.md. D-CEILING: D256.
- **One-line where-we-are (2026-07-01 LATE, D255):** **Bean-review defects #3-7 collapse into ONE root cause — the new engine never got the name-free layer-extraction system. That system is now the canonical, clarity-council-verified Spec 31 §2 (committed `c28a0086`); the cheat-gate is armed on `converter/` (committed `ab947ea3`). NO code built yet — the blueprint + gate are the deliverable.** Diagnosis corrections: media-map already works (containers are dropped, not the map); DB caps not the cause (`has_inner_blocks` derived from save.js+render); atomic maps already in `blocks.replaces`; the gap = `_descend_container_children` blindly unwraps + the CSS-fold conductor is unwired. **NEXT = BUILD §2 into the walker, INLINE (STOP-39 — coding subagents cascade-fail here), first slice LAND brand.** SoT = handoff.md top entry + `reports/2026-07-01-build-design-layer-extraction-slice.md`. 3 commits (`c28a0086`,`ab947ea3`,`d38b7780`) + the 9 D254 commits, all NOT pushed. 304 tests + cheat-gate green. D-CEILING: D255.
- **One-line where-we-are (2026-07-01, D254):** **FR-31-4 sgs/container DEFAULT BUILT + LANDED — new engine clones a FULL homepage (2/9->9/9) on sandybrown page 8.** The new engine now defaults a slug-None class-section to `sgs/container`+recurse-descend children (recognise_section + run_container_default; DB-derived container slug, no literal). 2 wired-pipeline LANDED bugs found by the canary deploy + fixed (emit_block_markup newline-separate + self-close empty dynamic blocks — the pipeline dropped content that unit tests passed, STOP-21). Bean page-8 eye-review (R-31-13) surfaced 7 defects: **#1 full-width (made UNIVERSAL across container+composites, gated on block_supports.align per Spec 31 §3 step-7) + #2 chrome-skip header/footer FIXED**; **#3-7 OPEN** (A1 media-map [priority], hero split, trust-bar extra column, product-card, ingredient cards — the next session's primary tasks in `next-session-prompt.md`). 6 commits `c2105981`->`7d694a54` on main, **NOT pushed** (pending Bean sign-off). 299 tests + cheat-gate green; convert.py byte-identical (D-MODULAR); STOP-28 intact (new engine still opt-in). D-CEILING: D254.
- **One-line where-we-are (2026-06-30, D253):** **W3 keystone LANDED-IN-ENGINE + new engine WIRED to /sgs-clone + Spec 22 MERGED into Spec 31.** D252 (HEAD `df9798a9`/`798febc7`): the universal child-lift + the recognition has_inner fix make the real hero CTAs lift {label,url,inheritStyle}; the new engine is reachable via `SGS_NEW_ENGINE=1` (default-off, frozen convert.py still runs live clones). The FULL-HOMEPAGE run mapped 2/9 sections cloning via the new engine, 7/9 honestly gapping — surfacing the **DEFAULT-IS-CONTAINER deviation** (recognition 4th branch FAILS LOUD instead of defaulting a slug-None section to sgs/container+recurse, FR-31-4) as the #1 engine unblock. D253 (HEAD `bb7b1e99`/`1e41c1df`): Spec 22 absorbed into **Spec 31 §13** (R-31-1..15 + carry-forward FRs + appendices) + archived; renumbered R-22->R-31/FR-22->FR-31 in 69 active files (frozen convert.py + archives keep 22; mapping R-22-N==R-31-N); 4-rater QC-council passed + 9 defects fixed + 3 absorption gaps backfilled. **Spec 31 is now THE single pipeline spec.** 286 tests + cheat-gate green; convert.py byte-identical. **NEXT: (1) sgs/container DEFAULT fix (FR-31-4) -> unblocks 7/9 sections; (2) canary LANDED test via SGS_NEW_ENGINE=1 + Bean eye.** D-CEILING: D253.
- **One-line where-we-are (2026-06-30, D251):** **W3-remainder progressed (HEAD `b921a909`).** A1 media-map + hero LANDED bugs 1&3 FIXED; the DB role-derivation root cause FIXED (role classifier was never wired into `/sgs-update` + was NULL-only → wired + generic-`content`→specific upgrade, 11 roles corrected, deterministic). Hero bug 2 (CTA child-lift) is the KEYSTONE LEFT — a 6-persona council design-gated it: **route every child through `build_block_markup` (delete the scalar bypass) + reuse shared `field_extractors` handlers** (Spec 31 §3.B.0). B = fresh-session (STOP-19, highest-regression walker). Register + role facts: `.claude/reports/2026-06-30-role-derivation-root-cause.md`. 276 tests + 2 new, F5/F6 green. **NEXT: B child-lift → re-run LANDED → A2/§5/!important/dead-code.** D-CEILING: D251.
- **One-line where-we-are (2026-06-30, D250):** **W3 Steps 1–7 BUILT (HEAD `1b3d108c`).** `run_mechanism_b` is the faithful `_route_composite_interior` walker port (Step 4); `build_block_markup._build_css_attrs` (Step 7 KEYSTONE) drives BOTH `process_element` (CSS — incl. new `root_supports.py` native-`style.*` lift + `outer_box` background-*/box-shadow) AND `extract_content` into ONE emit — Finding A FIXED, the two inert halves connect. Fixes: Fix1 native style.*, Fix2 box-shadow→shadow preset token-snap, Fix3 §5 seeds, grid-template-* pre-layer routing, validate.py enum bug, mega-menu !important→specificity, Check #3 selector-aware (118→75 baseline). A `/adversarial-council` corrected 2 phantom over-claims (four-channel-check rule, blub #373). **BUT all WRITTEN, NOTHING LANDED — new engine still INERT in prod (frozen `convert.py` runs live clones, STOP-28).** **NEXT: LANDED proof (Step 10, STOP-21) + A1 media-map + A2 ledger (Steps 8–9) + §5 lift-path + base-selector !important sweep.** 267 tests, 6 gates green, convert.py byte-identical. D-CEILING: D250.
- **(superseded) One-line where-we-are (2026-06-28, D247):** **D246 content-UNIFY — W1+W2 DONE+LANDED (scalar lift modularised, `a4c0de86`/`57209f48`); BOTH `/qc-council` gates RUN (D247): no cheats; A3 false-green test + A1 media_map seam FIXED (`afbcaa99`); A2 content-completeness RULED a §12.2.1 conservation-ledger gap (NOT a lift patch — §3.B1 strict no-op binding), folded with A1-full into the W3 engine-wiring. NEXT = BUILD W3 on Bean sign-off — port the FULL `convert.py` walker `_route_composite_interior` into `run_mechanism_b` per Register B (scalar-media + slug-None fold/CSS-route, styling-lift `_bp_decls`, arrays port-as-is, delete dead accessor last). Registers A/B + post-W3 roadmap in next-session-prompt. convert.py byte-identical (D-MODULAR). D-CEILING: D247.** Prior milestone — **Stage 2 (D244, commits `81971f00`→`712f037d`):** fresh `converter/recognition.py` 4-branch (named→atomic→scalar→unrecognised), name-free DB-driven; `recognise()` routes `.sgs-hero`→`sgs/hero`+`variant=split` (BEM modifier ↔ `variant_slots.variant_value`); `has_inner` derived FRESH from save.js (Spec 31 §12.7, not the cached column); `GapOrigin.UNRECOGNISED` loud-fail; `no_slug_literal` hardened (slug-arg + variant bare-string + AnnAssign, 4 gaps closed) + `coverage --check`. **LANDED-proven** live: all 10 variants (hero ×4, testimonial ×6-with-content) render their own `--variant` class, never a container fallback. 75 converter + 603 scoped-regression green; multi-model review fixed; convert.py byte-identical (D-MODULAR). Bean signed off (R-22-13). **D-CEILING: D244.** **DEFERRED (A14):** scalar branch data-limited (`slots.standalone_block` 40/103 → seed via `/sgs-update`); content-requiring blocks' full visual LANDED is Stage-4f; golden-source gate (A8) + fixture-corpus conservation runnable (need the full walk); the prior slice's 5 stub resolvers' real logic (per-stage). **(P-F5-RESIDUALS:** css_router D1 media-axis still rebuild-MF-2-owned.)
- **Canonical spec:** `.claude/specs/31-UNIVERSAL-CLONING-PIPELINE.md` — **read §2 (THE CORE PIPELINE MECHANISM) FIRST** (the clarity-verified blueprint for the build, D255); §12 = build-state/sequencing; §13 = binding rules/walker/content-fork (Spec 22 absorbed + archived, D253). Reading order: `docs-registry.yaml` `cold_start_reading_order`.

## DONE / ARCHIVED — theme + WooCommerce layer (Spec 30, no longer an active thread)
- **Spec 30 COMPLETE + MERGED (D220, 2026-06-12) — SGS is a sellable shop.** This work is finished; it is NOT a parallel thread anymore. Session docs archived to `memory/handoff-theme-2026-06-12.md` + `next-session-prompt-theme-2026-06-12.md`. Deferred roadmap items (B2B ex-VAT, subscriptions/build-a-box, F5/F10) live in `parking.md` — pick up via a fresh `/handoff` only if/when a theme task is explicitly started. Specs: 30 (COMPLETE) · 27 (product/configurator master) · 28 (bulk pricing, COMPLETE) · 26 (global styles, build-deferred).

## DOC DISCIPLINE
- `decisions.md` / `parking.md` / `mistakes.md` are append-only — append, never clobber; D-ceiling check (above) MANDATORY before a new D.
- `/handoff` writes `handoff.md` + `next-session-prompt.md` + this block.

## BLOCKERS
- None block the next session. Known-open items live in `handoff.md` "Known Issues".

<!-- Caps: this file ≤24576 bytes, ≤60 lines of body. History → memory/state-archive.md. If a fact here also lives in another doc, that other doc is the SoT and this is a POINTER — do not duplicate detail. -->


---

## Archived 2026-07-17 (P4 — LEDGER collapse: state.md + handoff.md + next-session-prompt.md collapsed to LEDGER.md; full state.md history stack D282->D342 at collapse time, appended below the earlier 2026-06-06 archive)

---
doc_type: state
project: small-giants-wp
project_id: 14
last_updated: 2026-07-16-D341-D342
note: "LEAN snapshot. Full history -> memory/state-archive.md. This file holds ONLY the current pointer; detail lives in handoff.md + next-session-prompt.md (the SoT). Do NOT restate D-numbers / counts / commit hashes here - they drift. <=24576 bytes."
---

# small-giants-wp — State Snapshot (lean)

## Human Summary
**LATEST (D341/D342, 2026-07-16) — Phase 2 nav/logo fixes SHIPPED + qc-council GO + 3-branch consolidation to `main` + dual-site deploy, both LIVE-VERIFIED.** On `main` **`a693e0e8`**, theme **1.5.38**. Two fixes: `sgs/responsive-logo` dropped the unshippable `auto` logo-switch (clashed with adaptive-nav's "More" overflow) for an operator `custom` breakpoint (`logoSwitchCustomPx`, default 1024); `sgs/adaptive-nav` collapse tier now reads `SGS_Breakpoints` (was hardcoded 768/1024/1280) with default `collapseTier` `tablet` — fixes the burger missing the 768-1023 band. qc-council ship gate GO. Consolidated `feat/adaptive-nav-dialog-drawer` (Spec 34 drawer + uimax thread + D338 remnants + these 2 fixes) + `feat/core-block-migration` (Track C, ~395 core blocks) + origin's docs commit into `main` via an isolated worktree; deployed canary then production, both PASS. **Live-verified:** tablet burger both sites; rebuilt `sgs/nav-menu` drawer live (old stale drawer gone); cart guard CONFIRMED on Indus (no WooCommerce, cart absent); drawer colours correct (old "dark pink" complaint was the pre-rebuild drawer); **NEW finding** — pre-existing 9px overflow on Indus (`P-INDUS-BRANDSTRIP-OVERFLOW-9PX`), and resting drawer link text wants the `text` token not `#000`. Track B (Indus content) stayed unmerged/paused. SoT = handoff.md + next-session-prompt.md.

## Human Summary (prior — D335, 2026-07-14) — §S9 pre-sign-off QA (both clients) + a11y/social polish SHIPPED; Bean set 3 NEW gates before sign-off. On `main` `ddb9b2fe`, D-ceiling **D335**, plugin+theme **1.5.22**. Bean did NOT sign off — he set a QA gate first (live DOM+CSS, a11y/WCAG, aesthetics/UX via `/ui-ux-pro-max`, out-of-box defaults). **QA (live, sandybrown/Mama's + palestine-lives/Indus) found real MINOR items — all fixed + shipped:** Indus drawer socials/email dark-on-teal (1.4.3), header utility icons 16×16 (2.5.8), toggle missing `aria-controls` (4.1.2), duplicate "© 2026 © 2026". (A footer "invisible text" was a FALSE alarm — measurement-vs-eye: getComputedStyle read an outer `<p>`; the text was visible gold ~7:1; pixel-confirmed.) **Polish SHIPPED (3 parallel Sonnet branches, `ddb9b2fe`, live-verified):** `sgs/social-icons` `source=manual|site-info` (pulls the 7 socials from Site Info — "2 ways") + 44px floor; `sgs/business-info` 44px + invisible-on-dark fix (defaults emptied → `var(--x,currentColor)` fallback fires; override still wins) + duplicate-© dedup; `sgs/mobile-nav-toggle` aria-controls; framework drawer — removed the REDUNDANT placed socials block (mobile-nav renders its own styled socials → drawer now one clean set + visible email, verified live on Indus). **HIDDEN PARALLEL SYSTEM found:** mobile-nav `render_socials_zone()` reads `sgs_social_*` options, NOT Site Info (2 social stores). **BEAN set 3 gates before sign-off:** (1) **[DESIGN-GATE FIRST] remove `sgs/mobile-nav`; `sgs/adaptive-nav` handles nav GLOBALLY** — high-blast-radius (adaptive-nav must absorb the FR-S9-5 drawer a11y contract); Rule 7 design-gate before building. (2) **"all business info optional, one source (Site Info), never hardcoded"** — unify the `sgs_social_*` store + audit. (3) **Site-Editor visual-BUILDER UX analysis** (same depth as frontend). §S9 sign-off PENDING (1) + (3) + re-QA. SoT = handoff.md + next-session-prompt.md.

**Human Summary (prior — D334, 2026-07-14) — the 5 deferred §S9 must-fixes CLEARED + Bean-added business schema (LocalBusiness) + repo-wide doc reconcile.** On `main` (`776a4c35` code + `01a038f3` docs), plugin 0.1.8. **Task 1 uid determinism** (site-header/-row/footer-row `wp_unique_id`→`md5(attributes)`, proven live: footer uid shifted across pages; qc-council-gated; verified identical uid on 6 pages, 0 visual change). **Task 3 2nd-client** — §S9 live-verified on palestine-lives (Indus) with its own data, 0 header/footer overflow (R-31-9 now real). **Task 4 wrapper golden test** — 36/36. **Task 6 business schema (Bean-added)** — central Org emitter upgrades Organization→LocalBusiness on a complete address (gated), emits OpeningHoursSpecification (defensive parser: am/pm-aware, omits split/closed days); non-WC address fallback parses multi-line into structured PostalAddress; LIVE: Mama's stays Organization (no address), **Indus is a valid LocalBusiness with real Birmingham address + hours** (split-Friday safely omitted); SiteNavigationElement confirmed inert. **Task 5 mega-menu** — Bean chose accordion + no-AJAX. **Task 2 repo-wide doc reconcile** — §S9 spec + architecture/01-theme/02/29/31/32/00 + 7 plans archived. SoT = handoff.md + next-session-prompt.md.

## Human Summary (prior — D333, 2026-07-14) — §S9 pre-sign-off `/adversarial-council` + fact-check + 5 quick must-fixes SHIPPED. §S9 sign-off PENDING 5 deferred fixes.** On `main` `da1415e0`, D-ceiling D333, plugin 0.1.8. **§S9 is 11/11 BUILT** (FR-S9-8 landed D331; button/rows D332). Ran a 6-persona blind `/adversarial-council` on the whole system (grades C+/C-/D+/C+/B+/B-, verdict NO-GO-as-is → GO-CONDITIONAL). **Bean distrusted the reports + directed a FACT-CHECK of every finding vs live code FIRST** (correct — council fix-shapes are hypotheses): 7 real, several framings corrected (FR-S9-6 IS built D328; the FSE Site Editor IS a drag-drop builder; 3rd parallel system retired D330; core blocks WERE replaced in the live part — gap is only the block insert template). **5 quick must-fixes shipped** (3 parallel Sonnet subagents, integrated + live-verified): (1) SECURITY — `sgs/mobile-nav` drawerGradient prefix-only-check CSS-injection (Author→phishing overlay) fixed with a shared `sgs_css_gradient_value()`, malicious payload rejected live; (2) WCAG 1.4.3 — transparent header auto-upgrades contrastSafe none→scrim; (3) insert TEMPLATE core/navigation→sgs/adaptive-nav (+drop woocommerce/mini-cart) so a fresh insert no longer re-arms the WC injection; (4) Site Info placeholder now editor-only, 0 frontend leak live; (5) "Text shadow (not WCAG-safe)" relabel. **5 DEFERRED must-fixes = next session (the rest of the sign-off gate):** uid determinism (rows use wp_unique_id not md5(attributes)); `/doc-audit` spec reconcile (FR-S9-4 built-note, "all four verified" inaccurate, FR-S9-9 taxonomy); **palestine-lives.org 2nd-client verify** (Indus sandbox, login `.claude/secrets/palestine-lives.env`); shared-wrapper golden test (1671 lines, 28 deps, 0 tests); mega-menu drill-down-vs-accordion reconcile. THEN Bean's "§S9 totally covered" sign-off → Spec 33 Part 2. SoT = handoff.md + next-session-prompt.md.

**Human Summary (prior — D331, 2026-07-14) — FR-S9-8 BUILT + LIVE (per-device content adaptation) → Spec 17 §S9 now 11/11 (ALL FRs built).** The LAST §S9 build. On `main`, D-ceiling D331, plugin 0.1.3→0.1.4, theme 1.5.17→1.5.19. Delivered by EXTENDING existing infra (extend-don't-rebuild). Three Bean decisions: nav iconOnly → descoped to contact + Call-button; Indus verify → sandybrown + by-construction; visibility breakpoints → align to 767/1023 now. **(a) Per-tier visibility** — routed `device-visibility.php`'s media queries through the canonical `SGS_Breakpoints` (600/1024 → **767/1023**, R-31-1 single-source, generated inline; help text updated). **UNIVERSAL BUG FIXED** (proven live + server probe, not inferred): the `render_block` visibility filter grabbed the leading scoped `<style>` (on any no-inline block) which the D311 CSS collector then lifts to `<head>` → hide class vanished; fixed to skip leading `<style>`/`<script>`. **(b) `showLabel`/`iconOnly`** on `sgs/business-info` — icon-only phone/email keep working `tel:`/`mailto:` + accessible name (clipped sr-label span). **(c) Move-to-drawer** — `sgs/mobile-nav` `ALLOWED_BLOCKS` widened for `sgs/business-info`; place-then-toggle (drawer copy renders only there, header copy hidden per-tier). **(d) Indus slim-bar** — `parts/header.html` + pattern: desktop icon-strip → ≤1024 logo + prominent "Call" button (business-info phone via `.sgs-header-call`, reads Site Info → `tel:`, 44px), email/social to drawer, footer 3→1 at 768. **Binding pivot:** `sgs/site-info` binding on `sgs/button` url did NOT resolve on WP 7.0 (custom-block bindings) → business-info-as-button (proven, R-31-1-clean). **Live-verified all tiers 320–1440:** icon-strip/nav/cart desktop, slim-bar ≤1024, drawer email/social present + absent from header, footer collapse, breakpoint bounds, sr-label clipped, 0 console errors, no header/footer overflow (1024 overflow = accepted testimonial slider). 2nd-client by-construction (no live indus). `/sgs-update` registered showLabel + widened allowed_blocks; reports `business-info-2026-07-14.md` + `mobile-nav-2026-07-14.md`. **§S9 = 11/11 BUILT. NEXT (Bean directive):** `/adversarial-council` on the WHOLE §S9 as built → Bean's "totally covered" sign-off (HARD gate) → Spec 33 Part 2. SoT = handoff.md + next-session-prompt.md.

**Human Summary (prior — D330, 2026-07-14) — FR-S9-9 SHIPPED (3 header behaviours as no-code block controls) + theme-side header-mode system RETIRED + FR-S9-10 live-confirmed → Spec 17 §S9 now 10/11, ONLY FR-S9-8 remains.** Commits `ba21fccf` (FR-S9-10) + `7a054e11` (FR-S9-9 + retirement) on `main`, pushed. D-ceiling D330. **FR-S9-9:** sticky / transparent-at-rest→solid-on-scroll / shrink-on-scroll + a contrast-safe fallback (scrim/shadow/force-solid) as INDEPENDENT no-code toggles on the `sgs/site-header` block inspector (a "Header behaviour" ToolsPanel). A bridge `Sgs_Header_Behaviours::resolve_active_header_behaviour()` reads the block attrs off the active header template part (via `SGS_Nav_Menu_Source::get_header_content()` — an independent PARSE, sidesteps the body_class-before-header ordering hazard) at body_class time → emits `sgs-header-behaviour-{flag}` classes consumed by header-behaviours.css + view.js. **A qc-council (2 cross-model raters) NO-GO'd the initial scope** — it surfaced a THIRD, fully-built parallel header-behaviour system (theme-side `inc/class-header-behaviour.php` + `header-behaviour.js` w/ its OWN --sgs-header-height publisher + `header-modes.css` + `_sgs_header_mode` post-meta + Settings page, silent only because `sgs_header_mode=static`) — so **Task 2b's theme-system retirement was merged into this build** (files deleted, functions.php de-registered, mode CSS stripped from `core-blocks-critical.css`; proven dormant first; php -l clean; 0 refs). Council corrections applied: scroll-driven shrink (never writes --sgs-header-height from CSS → no publisher race), scrim/force-solid as WCAG modes (text-shadow cosmetic-only), scrim ::before pointer-events:none + z-index, own is-header-shrunk class, literal attr keys for the dead-control guard. **Live-verified (sandybrown, full cache clear incl. Hostinger CDN, plugin 0.1.3):** sticky pinned top:0 + single --sgs-header-height:121px + scroll-padding-top:121px; transparent absolute rgba(0,0,0,0)→rgb(251,243,220) on scroll; shrink is-header-shrunk + padding reduced; scrim ::before gradient pointer-events:none inset:0; old theme header JS/CSS gone (0); 0 console errors; no overflow; no inline style. Shipped default headerSticky:true. Build gates green (dead-control 0, control-ux, conformance, box-family); HeaderBehavioursTest.php → 13 multi-flag tests; `/sgs-update --stage 1` registered 4 new attrs. Plugin 0.1.2→0.1.3, theme 1.5.15→1.5.16. **FR-S9-10:** a phone set ONCE in `sgs_site_info` renders in BOTH header + footer live + Org schema; blocks literal-free; ≥2-client universality by-construction (no live indus deployment). **§S9 = 10/11 DONE. FR-S9-8 (per-device content adaptation) is the SINGLE remaining build** → its own `/strategic-plan` next session, THEN a `/adversarial-council` on the WHOLE §S9 as built (Bean-directed, pre-sign-off), THEN Bean's "totally covered" sign-off gates Spec 33 Part 2 (which Bean wants ASAP). **Task 2b FULLY COMPLETE (commit `87dd869d`):** both legacy header/footer paths retired (theme-side header-mode + plugin-side Customiser) + 3 inert alt-header stubs deleted — the Site Editor is the one home. Only parked: `P-RECOGNISER-HEADER-BEHAVIOUR-MULTIFLAG` (→ Spec 33 Part 2). SoT = handoff.md + next-session-prompt.md.

**Human Summary (prior — D328, 2026-07-14) — FR-S9-6 CLOSED for all 3 blocks + Spec 17 §S9 coverage audit (8/11 FRs done).** Commits `3ea21bdf` (Tasks 1+2) + `0fe3a5fe` (audit) on `main`, pushed. **Task 1:** migrated the live header/footer/nav pattern instances (file-based FSE patterns, NOT DB template parts) from flat→object shape — which FIXED a real live fidelity bug: the flat strings were coerced to block.json DEFAULTS by WP `prepare_attributes_for_render`, so the footer columns rendered equal thirds not the draft's `2fr 1fr 1fr` (draft `sites/mamas-munches/mockups/homepage/index.html:711`) + the bottom-bar gap was 48px not the authored 8px. Both fixed live (`592 296 296px` + 8px). **Task 2 (qc-council-gated, 2 cross-model raters, 3 fixes baked in):** padding/margin/maxWidth/contentWidth + adaptive-nav linkFontSize migrated to the object model. Wrapper Change 1 (contentWidth token transform + maxWidth sanitiser in the object-emit, gated on `$object_model` → 50+ flag-off blocks byte-identical). 3 block.json: dropped `supports.spacing` (one system — the wrapper reads `style.spacing` UNCONDITIONALLY so keeping it double-emits) + the dead flat tier orphans; added object box/width attrs. Footer bottom row `style.spacing`→object desktop tier (rater-B R2, `var:preset|N`→`var()` — removing supports had zeroed the padding, live-caught). New shared editor `ResponsiveBoxControls`. **Live-verified 1440/375 (full cache clear):** padding 24 + margin 32 + inner gap 8 inherit to mobile (null-inherit cascade proven); columns 2fr 1fr 1fr→1fr; nav gap 28 + link 15; no overflow; 0 console errors. Build gates green (dead-control 0, control-ux PASS); `/sgs-update` re-registered (6 attr rows); theme 1.5.12→1.5.14. **Task 4 — Spec 17 §S9 coverage audit (`.claude/reports/2026-07-14-spec17-s9-coverage-audit.md`): 8/11 FRs DONE + live (FR-S9-1..7 + FR-S9-11), 3 OPEN builds — FR-S9-8 (per-device content adaptation), FR-S9-9 (transparent-on-scroll toggle), FR-S9-10 (global defaults + shared Site Info). Each has infra but needs block wiring. Spec 17 §S9 is NOT yet "totally covered" — these 3 are the gate before Spec 33 Part 2.** Pre-existing finding (out of §S9 scope): the wrapper has NO `style.border` emit (footer divider never rendered). SoT = handoff.md + next-session-prompt.md.

## Human Summary (prior — D327, 2026-07-14) — FR-S9-6 shared {desktop,tablet,mobile} responsive-override ENGINE built + wrapper opt-in branch + ALL 3 row/nav blocks wired + PROVEN LIVE.** Commits `4b2e28f7` (engine) → `665b5b12` (wrapper) → `c3497724` (editor+header) → `46c5f142` (docs) → `837fd63f` (footer+nav+graceful gate), all on `main`, pushed. **The engine** (`includes/class-sgs-breakpoints.php` + object-model resolver/emitter `sgs_emit_responsive_css()` in `helpers-responsive.php`): one shared breakpoint source (R-31-1, 1023/767), per-side null-inherit cascade + tier-diff emission (a tier's rule ONLY where it diverges) + `@container`+`@media` together + canonicalisation oracle (write-time uid stability, STOP-NO-KSORT). **34/34 unit tests.** **qc-council (3 cross-model raters) pre-build caught 3 flaws** (block-private = fork-not-mirror → wrapper-owned opt-in; container-type-on-root is void → two-layer inner split; uniform is_array guard breaks columns → per-site defaults). **Wrapper opt-in branch** (`SGS_Container_Wrapper`, gated `$opts['responsive_model']='object']`): flag-off path byte-identical for 50+ blocks by construction; container-type on outer, flex+gap routed to `.sgs-container__inner` (descendant) so @container works; wrapper-owned object-emit; **graceful `$object_grid` gate** so flat-stored instances render via legacy until re-saved (D270). **Editor:** SGS-owned `ResponsiveOverride` (tablist/44px/inherited-icon+aria/reset) + JS breakpoint mirror + `makeResponsive` canonical-order. **ALL 3 PROVEN LIVE (sandybrown, caches cleared, 1440/375):** header inner-flex gap 16px (+40px @375 with a mobile override, tier-diff proven); nav `<ul>` gap 28px (block-owned, via shared emitter on its own list); footer grid 3 cols→1 col + gap 48→32px; no overflow; 0 console errors. Reports `reports/visual-diff/{site-header-row-2026-07-13,site-footer-row-2026-07-14,adaptive-nav-2026-07-14}.md`. **DEFERRED (non-blocking, documented):** re-save footer/nav instances to switch legacy→object path (renders identical today); migrate box props padding/margin + nav linkFontSize to the object model (engine already supports); `/sgs-update` re-register + broader `/qc`+`/a11y-audit` pass. Plan `.claude/plans/go-rippling-cascade.md`. SoT = handoff.md + next-session-prompt.md.

## Human Summary (prior — D326, 2026-07-13) — P2 NAV SHIPPED + LIVE-VERIFIED: `sgs/adaptive-nav` replaces `core/navigation`, so the stray WooCommerce mini-cart + customer-account injection is GONE by construction (the D324 root-cause, fixed architecturally — no filter).** Commit `fbd93308` on `main`, pushed. Built the SGS nav block: a shared `SGS_Nav_Menu_Source` resolves ONE `wp_navigation` menu that drives BOTH the desktop bar AND the `sgs/mobile-nav` drawer (drawer's `get_nav_blocks()` delegates to it — one source, no duplication). `SGS_Adaptive_Nav_Renderer` server-renders every link as a real `<a href>` (crawlable + AI-visible; **no AJAX lazy-load**); submenus → CSS-hidden mega-panels using the ARIA APG **disclosure** pattern (not `role=menu`); NO `SiteNavigationElement` schema (research-backed — useless). render.php delegates the outer frame to `SGS_Container_Wrapper` (layout KIND) + **owns the collapse tier** (scoped rules for the bar + header burger, replacing the two fixed 768/782px hacks removed this session). view.js: overflow → "More" disclosure + Cluster never-overflow base. **LIVE-VERIFIED on sandybrown (caches cleared, theme 1.5.12):** WC mini-cart + customer-account GONE, `core/navigation` gone, 7 crawlable links + submenu, collapse tier correct 320-1440, **header never overflows** (all page overflow = the accepted testimonial slider), cart 44px, drawer opens (P0 re-parent intact) from the SAME source, mega-panel disclosure works, **0 console errors**. Report `reports/visual-diff/adaptive-nav-2026-07-13.md` (PASS). 3 live-QC bugs caught + fixed (view.js insertBefore + overflow-measurement + `display:contents` header-expansion). **DEFERRED → P2b (`P-ADAPTIVE-NAV-P2B`):** drawer accordion→drill-down animation; `sgs/mega-menu` role=menu→disclosure alignment; FR-S9-6 responsive-override model (adopt the D325 footer track's shared engine). **Parallel Track B shipped D325 today: `sgs/site-footer` + site-info-binding fixes + Org schema + cart hide-when-empty.** **CURRENT FRONT: P2b polish OR the FR-S9-6 shared responsive-override engine (analyse the footer track's engine, adopt across all 3 header blocks + adaptive-nav) OR P4 per-device adaptation / Site-Editor header-builder editor-UX design-gate.** SoT = handoff.md + next-session-prompt.md.

## Human Summary (prior — D324, 2026-07-13) — Header P1 SHIPPED + LIVE-VERIFIED (sub-400px WCAG overflow emergency FIXED); WooCommerce Block-Hooks injection root-caused; architecture-fix-over-workaround.** Commits `a1433f82` (v1) + `c575a41d` (v2) on `main`, pushed. Built + shipped two new blocks: `sgs/site-header` (section KIND) + `sgs/site-header-row` (layout KIND, never-overflow cluster row), both delegating to `SGS_Container_Wrapper` (composite-mirror). 3 optional rows, empty-row zero-output. Live-verified on sandybrown 320/375/414/768/1440: `scrollWidth<=innerWidth`, 0 overflow, no inline style, toggle+cart 44×44. **The sub-400px WCAG 2.2 SC 1.4.10 header overflow emergency is FIXED.** v2 swapped to SGS-native `sgs/cart` + `sgs/responsive-logo`, grouped cart+burger, designed collapse-to-drawer (nav hidden <768, matching mamas-munches draft). `sgs_header` CPT gained a `template` key (was absent). **Root-caused (proven in WC source, not inferred):** WooCommerce auto-injects `woocommerce/mini-cart`/`customer-account` after ANY `core/navigation` via the WP Block Hooks API (`hooked_block_types` filter anchored to `core/navigation`) — this is why a stray cart/account kept appearing. A suppressor filter was built, validated, then REVERTED per Bean in favour of the architecture fix: P2 `sgs/adaptive-nav` replaces `core/navigation`, so WooCommerce has nothing to anchor to. Full detail: `.claude/plans/2026-07-13-header-builder-remaining-work.md`. **CURRENT FRONT: P2 — `sgs/adaptive-nav`** (the header nav block; also the root fix for the WC Block-Hooks injection — once it ships, remove the interim nav-visibility CSS hack). SoT = handoff.md + next-session-prompt.md.

## Human Summary (prior — D323, 2026-07-13)
**Header/Footer/Nav SYSTEM design-gate APPROVED + P0 drawer-fix SHIPPED + 6-doc rollout.** Commits `24e5d167` (P0) + `4f88ee5a` (docs) on `main`, pushed; design-gate `220f4200`. The emergency header-overflow-below-384px task grew (Bean-directed) into designing header/footer/nav as a proper SYSTEM: 3 specialised container blocks `sgs/site-header`+`sgs/site-footer` (section KIND, delegate to `SGS_Container_Wrapper`) + `sgs/adaptive-nav` (one menu → burger, 4 tiers incl custom-px, mega-menu drill-down), living INSIDE the template parts (rule EVOLVED: container-in-part permitted like card-grid; monolithic header/footer block still forbidden — the `no-header-footer-block.py` hook already permits `site-*` names, no code change). 3 named rows, per-breakpoint override (`{desktop,tablet,mobile}` null=inherit, new-blocks-only, wider surface + container queries), never-overflow Cluster+`clamp()`, GOV.UK-grade drawer a11y, per-device content adaptation, global-defaults+Site-Info access (FR-S9-10). Grounded in 5 documented systems (Bricks/Elementor/Blocksy/Material 3/GOV.UK, read holistically) + Indus reference + a research-council. **P0 SHIPPED + LIVE-VERIFIED:** the unclickable mobile-nav drawer (`view.js` set `inert` on `.wp-site-blocks`, the drawer's own ancestor → froze itself; fix = re-parent drawer to `<body>`; 15/15 links reachable live, background still frozen, ESC works). **6 docs updated additively** (Spec 17 heavy §S9 11 FRs; Spec 02 main; Spec 01; Spec 00; Spec 33; architecture.md) + a subagent cross-check/fact-check (6/6 ground-truth passed; 3 build-pending-status drifts fixed). **REMAINING (build-pending, NOT started):** P1 `sgs/site-header` (fixes the STILL-LIVE header overflow), P2 `sgs/adaptive-nav`, P3 `sgs/site-footer`, P4 per-device content, P5 pipeline Part 2. SoT = handoff.md + next-session-prompt.md.

## Human Summary (prior — D320/D321/D322, 2026-07-13) — Spec 33 Part 1 COMPLETE: all 13 FRs shipped + live-verified on Mama's.** Commits `03d98c97` (D320) → `f8e8ab1e` (D321) → `3e03e810` (D322) on `main`, pushed. The draft global-styles extractor's entire follow-up set landed: **FR-33-12 (D320)** fail-closed freshness gate — `/sgs-clone` HALTs unless the canonical `theme-snapshot.json` carries an `_sgsExtractor.draft_css_sha256` matching the current draft (a code-review caught the first design tying it to the generated file; fixed by embedding the key IN the canonical snapshot); new `scripts/shared_utils.py` single-source hash. **FR-33-5 + FR-33-6 (D321)** — Pass B advisory palette derivation (`derive.py`, token-less drafts → derived-by-usage-context, `advisory:true`, never auto-live, stripped by push unless `--include-advisory`) + dark-theme/preview-shell safety (`_theme_background` = widest content ancestor; dark discarded only on a positive shell signal; `measure.js` now records marker paths — a qc-council rater caught it had none). **FR-33-13 + component-CSS migration (D322)** — snapshot reserves `settings.custom.header`/`.footer` (+ reconciliation note; Spec 17 uses Customiser/JS-var so Part 2 decides tokenise-vs-Customiser); the transitional `styles.css` (2273→117 chars) migrated (dead is-style/hero-cta/page-hack rules DROPPED per live-DOM confirmation, focus-visible → theme `utilities.css`, product-card client vars KEPT); the button now consumes the open-bag `hover-transform` token (FR-33-4 render-side closure — the council spec-lawyer reversed an earlier plan to DROP it). **Deployed + LIVE-VERIFIED clean (sandybrown page 8):** focus-visible `solid` from `utilities.css?ver=1.5.8`, `--sgs-op-sel-text`=#3A2E26, buttons unchanged, hover-transform inert, 0 console errors, visual intact 1440+375; LiteSpeed v7.8.1 confirmed active. 26 extractor tests green; determinism byte-identical; frozen hex-helper golden intact. **Spec 33 → v1.1.0 COMPLETE.** DEFERRED (explicitly scoped): other-5-client rollout behind per-client reclone (FR-33-11); Part 2 = header/footer clone (Spec 17). **⛔ IMMEDIATE NEXT FRONT (Bean-inserted 2026-07-13, EMERGENCY):** the site header overflows the viewport below 384px (header row doesn't wrap/collapse; account + cart icons escape bounds; WCAG 2.2 Reflow failure). The NEXT session is ONLY this fix (`theme/sgs-theme/parts/header.html` + `assets/css/*`; breakpoints stop at 599/782px, nothing <384px). The Spec 33 rollout + Part 2 move to the session AFTER. SoT = handoff.md + next-session-prompt.md.

## Human Summary (prior — D319, 2026-07-13)
**extractor palette made ADDITIVE; the D318 pink regression is fixed.** Commit `067d7bbe` on `main`, pushed. Bean caught live: after the D318 deploy, `surface-pink` sections (hero band, trust bar, gift section) + gift-card labels went CREAM. **Cause (proven on live DOM):** the D318 palette generator renamed `surface-pink`→`custom-surface-pink`, dropped declared tokens (`success`/`cookie-brown`/`accent-dark`), and its palette was a SUBSET missing hand-added colours the draft can't provide (`surface-peach`/warm-axis/`footer-bg`/aliases) — so already-cloned blocks' `var(--wp--preset--color--surface-pink)` went UNDEFINED → transparent → cream. This VIOLATED the extractor's own FR-33-11. **Fix (Bean-directed "add the customs, don't ruin the palette; reclone fixes the new setup"):** `palette.py` keeps each colour's OWN draft-token name as the slug (`surface-pink`, never `custom-`/dropped) + emits EVERY declared solid `:root` colour; `extract.py --merge-onto <existing>` additively merges generated globals/base onto an existing snapshot (generated wins on shared slugs, existing extras + component CSS preserved). **LANDED live:** all pink restored (`rgb(245,194,200)`), 21 slugs defined, base 16px, h2 lh 1.2, primary coral, secondary transparent, 0 console errors; 18 tests (2 new guards). **No reclone needed to un-break** (additive preserved every slug name); a full reclone is folded into Phase 5 (the FR-33-12 ordering gate). Also archived 3 complete plans (new-engine-to-parity + css-property-column + preset-sync). SoT = handoff.md + next-session-prompt.md.

## Human Summary (prior — D318, 2026-07-13)
**Spec 33 Part 1 BUILT + D303 killed live on Mama's.** Commit `e0a73b04` on `main`, pushed. Executed Phases 1-4 of `.claude/plans/go-parallel-blum.md` (Bean-chosen checkpoint: drive to D303-dead-live, commit). **The extractor** (`plugins/sgs-blocks/scripts/theme-extractor/`, hybrid Node `measure.js` + Python) renders a draft, MEASURES computed values, and generates the theme's global styles so blocks inherit the correct base by construction. **Iron law shipped:** the emitted value is ALWAYS the computed value on a real rendered node; declared CSS supplies only the name/role. **D303 dead** (FR-33-3): base body = the longest main-content `<p>` (16px); heading line-height = the MODE ratio across non-chrome headings (1.2 — the hero's 1.15 is an outlier excluded by construction). **Palette** (FR-33-2): role by usage-context, `:hover` selectors excluded from identity, two-pass slug assignment → primary=coral #E68A95 (not the near-white footer link). **16 tests, byte-identical re-run, frozen `build_draft_root_colour_map` unchanged (golden), schema-validate, no converter regression.** **Deploy safety** (FR-33-11): `push-theme-snapshot.py` gained default-on `--backup`, `--rollback`, drift-warn. **PROVEN LIVE (sandybrown page 8, Bean chose full cutover):** base 16px/1.6, brand quote 16px, h2/h3 1.2, hero h1 1.15 faithful, buttons faithful; all baseline theme.json keys preserved. **Two bugs caught via LIVE measurement (STOP-VERIFY-COLOUR):** (1) an early merge-from-partial would have stripped 136 baseline keys — fixed by merging generated-full + carried-forward component CSS; (2) `presets.py` dropped alpha, turning transparent secondary/outline buttons opaque BLACK — fixed + regression test. **Spec 33 → v1.0.0 (built).** FOLLOW-UP (Phases 5-6): FR-33-12 orchestrator ordering gate, FR-33-5 Pass B advisory, FR-33-6 dark-theme, FR-33-13 namespace + parking re-point, migrate transitional component `styles.css` out of the snapshot. The full pipeline reclone was NOT run (D303 proof holds on the deployed snapshot + existing page). SoT = handoff.md + next-session-prompt.md.

## Human Summary (prior — D317, 2026-07-13)
**next front chosen + Part 1 designed to a build-ready spec.** Bean picked the **header/footer setup pipeline** (2 parts: Part 1 = a universal draft→theme global-styles/token extractor that kills the D303 inherited-base drift by construction; Part 2 = header/footer clone, Spec 17, later). Grounded in WP docs + a focused research pass + a full survey of ALL `sites/` drafts (3 real design systems + a token-less scrape) → wrote **Spec 33 v0.1**, ran a 6-persona `/adversarial-council` (GO-conditional — caught that FR-33-1 "declared wins" would RE-SHIP D303, the undefined role algorithm, Pass-B palette inversion, the 6-live-snapshot blast radius), then reshaped to **v0.2** (`e77eec79`, on `main`): **COMPLETE spine** (all declared value types, Bean's call) tiered by **PROVENANCE** (declared auto after computed-validation / derived advisory-gated) + every convergent fix + all 4 open questions resolved into FRs. NO code built — design + design-gate only. **NEXT: BUILD Spec 33 v0.2, prove on Mama's only** (next-session-prompt.md is the build orchestration). SoT = handoff.md + next-session-prompt.md.

## Human Summary (prior — D315/D316, 2026-07-12)
**parity tool BUILT to Spec 20 v1.1.0 (trustworthy) + universal pill tick-gap fix + Task-2 dead-control finding.** Commits `331f9523` (parity rebuild), `b2c2db29` (pill fix), `ed772825` (Task-2 parking) — all on `main`, pushed. **Task 1:** rebuilt `computed-parity.js` (Stage 11.6) so its % tracks VISIBLE fidelity — font-family primary-only, blocklist `interactivity`/non-control `appearance`, sub-visible representational twins → unscored `sub_visible[]` bucket (each gated by a per-pair INVISIBILITY PREDICATE, the qc-council's load-bearing correction — never a blanket label-exclude), tag scored separately (FR-20-9), class names context-only (FR-20-10), force-load lazy content (FR-20-11). `feature-dev:code-reviewer` caught 3 real bugs, ALL unsafe hide-a-gap (inline `clientHeight=0`, native-`<button>` UA-bg appearance, margin-absorbed direction) — fixed + guarded. 11/11 fixtures, draft-agnostic. **Baseline 77% → 88% raw; validated vs the independent D314 ledger** (82% of misses = the ledger's accepted+out-of-contract dispositions → ~98% dispositioned, tag divergences exact, content 100%). The raw 88% is honest + lower ONLY because it's page-agnostic (FR-20-2) — pairs with Bean's eye (FR-20-7/R-31-13), never closes alone. **Bean signed off.** **D316 pill fix:** the selected pill's ✓ tick was an absolute overlay with no gap / overlapping the value → now an in-flow tick + `margin-right:0.3em`, shown only when selected (unselected stays draft-tight); product-card pack pills render via `render_block('sgs/option-picker')` so one `style.css` change is universal; LANDED 1440+375 (`✓ 8-pack`, no overlap); version 0.1.9→0.1.10 cache-bust; visual-diff report. **Task 2 (D314 deferred C-type sweep):** all 7 mobile-nav/trust-bar null-role attrs proven DEAD render paths (nothing consumes them) — seeded NONE (a role-seed → render no-op, STOP-44); parked `P-DEAD-NULL-ROLE-CONTROLS` (block-quality debt). **NEXT:** Bean picks from the menu (header/footer setup pipeline / more parity sweeps / block-quality). SoT = handoff.md + next-session-prompt.md.

## Human Summary (prior — D312/D313, 2026-07-12)
**P-STYLE-TAG-CONSOLIDATION SHIPPED + LANDED.** Commits `9dfcaa6e` (Spec 32 v1.2 / FR-32-11 + design), `72c0387a` (collector), `c30dd5e2` (final modes + settings), `c85bb91c` (docs) — all on `main`, pushed. **The ~100 per-block scoped `<style>` tags (~33KB, page 8) are consolidated into ONE head stylesheet.** Collector = a single `render_block` chokepoint (lifts every `sgs/*` block's `<style>` from rendered HTML into one buffer, dedup by content hash, D303 residual order preserved) — dissolves the 6-emit-shapes risk the `/qc-council` (3 raters, GO-WITH-FIXES) found. Delivery = ONE output buffer injecting into `<head>` every render (self-consistent under full-page caching — no pointer, no cold/warm, no freeze). **Two operator modes (SGS → CSS Output settings, `sgs_css_output_mode`, default `file`):** `file` = cached content-hashed external `<link>` (immutable cache header, atomic write, epoch invalidation + `litespeed_purge_all` + GC; an optimisation plugin can defer it — settings page lists LiteSpeed/Autoptimize/WP Rocket/Perfmatters + exact setting); `head` = inline `<style>` (the draft's model), self-contained. **Key build finding (Bean-directed test):** first VERIFIED no LiteSpeed Cache plugin was installed; then reproduced the initially-designed generate-then-serve model FAILING LIVE under LiteSpeed page cache (froze the cold inline response — the council's prediction) → replaced with the unified buffer. Research (web.dev 14KB rule + "LiteSpeed optimises external not inline") corrected a hasty inline-in-head lean. **LANDED (page 8, both modes, incl. under LiteSpeed):** 1 head style/link, 0 body `<style>`, correct cascade + computed values 375/768/1440, D303 intact, editor parity (block-renderer REST keeps inline), 0 console errors, settings page renders. Render-side PHP only (no converter/JS; conformance goldens unaffected); no version bump. **Then D313 (same day):** full DOM audit (0 body SGS `<style>`, 1 head `<link>`, zero SGS-block forbidden inline declarations) + Lighthouse (CWV green LCP 274ms/CLS 0.06, BP 100, SEO 100, a11y 95→96). **Page-8 a11y fixed at the DRAFT source + re-cloned** (`904fe02e`, Bean-locked "fix the mockup not the clone"): gift-tag contrast 2.34→8.4 (`--primary-dark`→`--text` charcoal), ingredient headings h4→h3. Ghost button + Trustpilot left faithful-to-draft. **NEXT (Bean-directed):** (1) exhaustive draft-vs-live DOM diff to PROVE the 100% clone (assume nothing); (2) rebuild the computed-parity tool (`scripts/parity/computed-parity.js`, Stage 11.6) to be universally trustworthy — match tags/classes/elements/content/CSS, no cheating, validate vs a manual ledger. **Note:** LiteSpeed Cache now active on sandybrown (installed to test); CSS async/CCSS optional (QUIC.cloud). SoT = handoff.md + next-session-prompt.md.

## Human Summary (prior — D311, 2026-07-12)
**page-8 Fix 4 (labels) SHIPPED + LANDED.** Commit `cef1fca9` on `main` (pushed). The gift-card `sgs/label` capsules rendered with NO padding (a tight chip) because `label/render.php` gated padding behind the `is-style-pill-*` class — a cloned label has no pill class, so the padding the converter already transferred was dropped; and the product-card trial tag ("New? Start here") hugged (`width:max-content`) instead of stretching like the CTA. **Fix (Bean chose FULL MIRROR, attributes over variants):** new shared `includes/helpers-box.php :: sgs_label_box_css_rule()` emits a label-style box (padding/radius/bg/display/fullWidth) as SCOPED class-level CSS — BOTH `sgs/label` and the product-card trial tag call it → byte-identical (composite-mirror R-31-9 / Spec 31 §13.6 D294). Label: padding+custom-bg ungated to value-presence + display model (fullWidth→block+100%, box→inline-block, bare→block), suppressed when an `is-style-*` class is present; defaults `padding {}`/`borderRadius 0`/`backgroundColour ""` + new `fullWidth`. Product-card: trial tag via the helper (`tagFullWidth` default true) + new client-editable `tag*` box attrs; `width:max-content` moved base→`--featured` (protects the featured overlay); static pill chrome kept (borrowed by badges). **`/qc-council` (2 cross-model raters) BOTH returned RESHAPE pre-build — caught the featured-badge break, the pill-fill display collapse, and dropped a speculative fullWidth heuristic.** **LANDED live 375/768/1440:** gift capsules padded hug (4/10, surface-pink, radius 6); trial full-width; 4 eyebrows stay bare (Bean's #1 regression concern holds). Unit suite 449 pass; conformance unchanged (15 pre-existing red, 0 new); `/sgs-update --stage 1` seeded 6 attr rows; no version bump (render-side lands fresh). **NEXT:** Fix 9 (inline-styles / Spec-32 investigation, read-only — enumerate + classify live inline `style="…"` declarations, Bean picks scope). **Open (deferred):** per-element geometry transfer for the tag (colour/typography only routed today); 15 pre-existing stale goldens. SoT = handoff.md + next-session-prompt.md.

## Human Summary (prior — D310, 2026-07-11)
**page-8 Fix 2 (product-card CTA) mirrors sgs/button style + colour, LANDED + hover-verified.** Commits `6fdf54d7` (converter mirror + colour + editor merge), `73c60b25` (decisions), `ab8c2a64` (hover + secondary-border fix) — all on `main`. The cloned trial CTA rendered `--primary` (draft wants `--secondary`) + featured CTA had near-white text on pink (WCAG fail). **The mirror fix (Bean-directed "mirror how sgs/button clones"):** the nested CTA's BEM `--modifier` lifts onto `ctaStyle` via the SAME preset-modifier mechanism as `sgs/button` `inheritStyle` (new shared `db_lookup.preset_style_for_element` + `style_preset_attrs_for_identity`, DB-driven, no literal, Spec 31 §13.5); ALL 6 `ctaColour*` preset defaults emptied AND the product-card's OWN divergent `.product-card .sgs-button--primary/secondary` rest+`:hover` `style.css` rules removed → the CTA inherits the shared button-preset channel entirely (the client `buttonPresets` are faithful to the draft). Editor merged to one `ctaStyle` picker (removed `ctaPreset`). **Two-pass verify — Bean caught the first miss** (resting-contrast-only shipped a wrong hover + secondary border): fixed + LIVE-verified REST + HOVER (Playwright `.hover()`) — trial rest border `#E68A95`, both hovers dark bg + white text + dark border, exact draft match. **Cache-bust:** bumped version 1.16.11→1.16.12 (a `style.css`-only change is served stale to a cached browser without a `?ver` change; render-side inline `<style>` lands fresh). Suite 448 pass. **NEXT:** Fix 4 (labels — attribute-driven pill + `fullWidth` + THE MIRROR: the trial product-card's nested "New? Start here" tag must be an exact functional mirror of the standalone `sgs/label` block used in the gift section — same pattern as Fix 2's CTA↔button), Fix 9 (inline-styles/Spec-32 investigation). **Open:** cache-bust policy (switch block styles to filemtime `?ver`?); MEMORY.md ~24.7KB (compact). SoT = handoff.md + next-session-prompt.md.

## Human Summary (prior — D309, 2026-07-11)
**page-8 Fix 5 (universal hover) LANDED + Part F editor controls + a team-member duplicate-control cleanup.** Commits `254f5b97` (hover-attr naming standardisation — 70 declarations across 17 blocks moved prefix→suffix `{base}Hover` to match button + the tier grammar), `018a8276` (D309 converter mechanism), `cb1a782e` (docs), `27e0f517` (Part F + dedup) — all on `main`, pushed. **The fix:** a cloned draft `:hover` declaration now transfers to the block's `{attr}Hover` companion. Cause proven on the REAL node (the CSS collector silently dropped every `:hover` rule — matches neither the class-compound nor tag-equality branch). Bean reshaped it twice mid-design (both improvements): (a) hover is a MODIFIER APPEND like a responsive tier — the converter resolves the base attr then re-appends `Hover` from `modifier_suffixes(kind='state')`, NO `property_suffixes` derivation; (b) standardise the hover naming FIRST so routing needs zero per-convention code. `collect_state_decls_for_element` reuses the base matcher on a per-state pseudo-STRIPPED rules copy — state can't leak into base by construction (the qc-council fall-through trap made impossible). Both CSS families carry it; `text-decoration`+`text-transform` brought into the typed-lift scope. **LANDED live** (page 8, CDN+OPcache cleared): announcement "Find out more" = `text-decoration:none` rest, `underline` on hover+focus-visible. Suite 448 pass. **Part F:** `TypographyControls` gained opt-in text-decoration/transform/letter-spacing controls (enabled on sgs/text) + an opt-in hover section (gate-safe). Button NOT wired to the helper (Bean-confirmed; preserves the WCAG focus-visible rule). **team-member:** 8 redundant `sgsHover*` block.json declarations removed (centrally injected). **NEXT:** Fix 2 (product-card CTA composite-mirror), Fix 4 (labels attribute-driven pill, Option B), Fix 9 (inline-styles/Spec-32 investigation, Bean-raised). **DB:** a `/sgs-update --stage 10` prune of orphaned prefix-hover + team-member rows is pending (non-blocking). SoT = handoff.md + next-session-prompt.md.

## Human Summary (prior — D307 + D308, 2026-07-11)
**D307 + D308, 2026-07-11 — the page-8 discrepancy programme: 5 more items fixed + LANDED live.** **D307** (`54ac3a44`, on `main`) shipped 4 CSS-layer fixes, each cause proven on the LIVE DOM first (the register's mechanism was WRONG on 3 of 4 — always verify before building): **emoji size** (sgs/icon sized its container off `--sgs-icon-size` but had no rule for the emoji glyph → added `.sgs-icon__emoji{font-size:var(--sgs-icon-size)}`, universal); **universal margin reset** (the converter's CSS matcher silently DROPS the draft's global `*{margin:0}` reset, so cloned `<p>`/`<h*>` leak browser-default margins — Bean chose a THEME reset, researched via `/research-check` extended: a bare `p{margin:0}` breaks WP block-gap [gutenberg#53717], so shipped as `@layer reset{}` in `core-blocks-critical.css` which yields to block-gap while beating the UA default; theme 1.5.6→1.5.7; block-gap verified preserved on authored content); **option-picker tick** (absolutely-positioned so unselected pills reserve no space, no shift on select); **trustpilot height** (folded into the margin reset — bar padding was already correct; extra height was the leaked `<p>` item margins). **D308** (`8b45a417`, on branch `feat/text-box-and-universal-hover`, NOT yet merged) shipped **Fix 6 — sgs/text disclaimer box**: sgs/text ALREADY had bg/border/radius/max-width attrs + no-inline render (register's "extend the block" premise was wrong); the gap was the converter OUTER resolver missing the camelCase attrs (name-build capitalises → `BackgroundColour` ≠ `backgroundColour`); fixed with an additive OUTER fallback to `attr_for_property` + a border-width box-object merge + a colour-slug validation guard (code-review caught a D306-class blocker: raw `var(--border)`→undefined→dark; now validates against the real palette → `border-subtle`). LANDED: disclaimer = white bg, 1px #E8D5C0 border, 10px radius, 16/20 padding, 620 max-width at 375+1440. Converter suite 445 pass. **NEXT (page-8 remainder):** Fix 5 (universal hover — Bean-directed: modify the EXISTING collector to bucket `:hover`, universal across BOTH CSS families; deterministic `{suffix}Hover` DB derivation; helper hover + editor controls; wire button to the shared helper), Fix 2 (product-card CTA = full button capability), Fix 4 (labels pill-box). SoT = handoff.md + next-session-prompt.md.

## Human Summary (prior — D306, 2026-07-11)
**D306, 2026-07-11.** Commit `0908ff92` pushed to `main`; build green; converter suite 440 pass. **The page-8 discrepancy programme: full evidence-based diagnosis of Bean's 22 items → 3 universal causes, and the two biggest FIXED + LANDED.** **Cause 1 — 8 black borders:** root cause is WORDPRESS CORE (not our converter — Bean's architecture worry answered) — WP 7.0.1 style-engine drops a `var:preset|color|` value for shorthand `border-color` (its definition lacks `css_vars`); the converter emits border-colour correctly, WP drops it at render. Fix: `root_supports.py` emits `style.border.color` as a direct `var(--wp--preset--color--{slug})`. LANDED: #E8D5C0 / #F5D050 accurate. **Cause 2a — unequal cards + brand button:** flipped `container.verticalAlign` default `start`→`''` + wrapper fallback → CSS-initial stretch (FR-31-5.1); LANDED cards 495/495, brand button full-width. **9 items remain, all scoped** (register `.claude/reports/2026-07-11-page8-discrepancy-diagnosis.md` "PRECISE FIX SCOPES"). **Bean's 3 architecture Qs answered with DB/code ground truth:** Q1 gift-label padding is pill-gated (not a default); Q2 base typography IS universal, only HOVER typography missing (button-only); Q3 `iconSize.css_property=None` unseeded → seed `='font-size'`. **NEXT:** execute the 9 remaining fixes (next-session-prompt.md Tasks 1–5) + the separate inline-styles/Spec-32 investigation (Task 6). SoT = handoff.md + next-session-prompt.md.

## Human Summary (prior — D304+D305, 2026-07-11)
**LATEST (D304+D305, 2026-07-11).** Commits `6f52fb91` (D304 scoped-selector gate), `a042df37` (Task-2 reframe + parking merge), `fc05fe70` (multi-button flex-wrap), `b8789ae7` (hero gap), `272df589` (D305 heading text-wrap) — all pushed to `main`; build green; converter suite 440 pass. **D304:** the multi-button scoped-selector bug-class is NOT present across the roster (live audit, 0 dead); shipped `audit-scoped-selector-live.js` (plant-tested, wired into `build-deploy.py --audit-scoped-page 8`); a static analyser gave 26 FALSE POSITIVES → dropped. **D305 + 2 siblings:** three D228 "framework default overriding the draft's absent value" fixes — multi-button `flex-wrap` `wrap`→`nowrap` (buttons stacked at 800px), hero `.sgs-hero--split` 16px gap removed (column 5px too narrow), heading `text-wrap:balance`→draft-greedy for CLONED headings (new `textWrap` attr + converter step 3a3, authored keep balance). **Task 2 REFRAMED** (Bean): the typography ancestor-walk is superseded by a draft→theme-token extractor (header/footer setup pipeline) — `P-DRAFT-TOKEN-EXTRACTION-SETUP-PIPELINE`. **NEXT:** re-clone page 8 (lands D305) → the **page-8 discrepancy programme** (`P-PAGE8-DISCREPANCY-REGISTER`: universal black-border cause, card equal-height, button fidelity, D228 component-default sweep, inline-styles/Spec-32). SoT = handoff.md + next-session-prompt.md.

## Human Summary (prior — D303, 2026-07-11)
**LATEST (D303, 2026-07-11) — pipeline typography-fidelity: BOTH next-session Tasks LANDED + verified live.** Commits `83d133aa` (M1 core) + `da58ea48` (M2 snapshot) + `0b6e7366`/`437687f5` (docs), all pushed to `main`. **M1 — universal residual render-precedence** (the hero H1 rendered 52px not the draft's 58px @≥1280): a 4-part theme-wide architecture — BOUND each `sgsCustomCss` residual to the device tier its threshold falls inside (symmetric min/max, so a `min-width:600` residual = 600–767 can't bleed into tablet); MATCH the block's class specificity (`.uid.uid` 0,2,0) + win by SOURCE ORDER (appended last), never an ID/`!important` (WP-6.6 `:root :where()` + Kadence/Spectra/GenerateBlocks idiom, research-backed + Bean design-gated); NORMALISE the 7 blocks that self-rolled at `#uid` to class-level + add `$uid` to their class list; F3 gate E8 now recognises class-scoped overrides. Spec 31 FR-31-5.2/22.3 + Spec 32 §6.1(b) amended. **Verified live 375/768/1440:** H1 34/52/58, bounded residuals dormant outside band, 7 blocks render correctly. **STOP-21 caught live:** multi-button rendered `display:block` on a GREEN build (uid was an id not a class) — fixed. **Task 2 pills** already resolved by D301/D302 + reclone (verified). **M2 (section-heading letter-spacing)** fixed THEME-SIDE — removed the arbitrary `h1 -0.022em`/`h2 -0.015em` from the Mama's snapshot (copied into all 6 client snapshots, not from any draft) → h2s render `normal` (draft-faithful), hero h1 keeps −1px. **NEXT (Bean-directed, 2 tracks):** (B) audit + structural gate for the multi-button bug-CLASS (scoped rule whose class the element doesn't carry = silent no-op) `P-SCOPED-SELECTOR-MATCH-AUDIT-AND-GATE`; (A) the general effective-value typography-lift MECHANISM `P-EFFECTIVE-VALUE-TYPOGRAPHY-LIFT`. SoT = handoff.md + next-session-prompt.md.

## Human Summary (prior — D300+D301, 2026-07-10)
**INTEGRATION session: all no-inline tracks merged to main + 6 universal pill-cloning converter bugs fixed.** The split-edit/serial-land rollout was integrated: 6 branches (Tracks A–E + D299 option-picker) merged to `main`; the 2 collisions resolved (option-picker → D299 superset; product-card → D299 draft-fidelity + Track E no-inline, brand-purged `#e68a95`→teal). **box_family made fully DECLARATIVE** (Bean "stop hardcoding into sgs-update"): 40 blocks declare `supports.sgs.boxFamilies` in block.json, sgs-update derives the column, 137 dict rows removed, 0 regressions (live `applied=363, 0 missing`). **hero dark-pink bug FIXED** (Bean-reported — the default gradient overpainting the transferred flat pink; `has-background` now added for custom bg; verified live == trust-bar). **Bean's 3 priorities verified live:** hero responsive padding matches draft at 375/768/1440; custom hex colours + custom px padding render zero-inline (NOT preset-locked); pill-cloning fixed to 7/8 draft-accurate via **6 universal styling-lift bugs** (BgColour suffix, compound-selector matching, background+border shorthand, border-color resolution, rgba preservation) + the **anti-cheat refactor: colour lift routes by DB `role='color'`, not a hardcoded property-name list** (D301). **NOT YET DONE (this rollout's outcome is NOT fully hit):** the ~35-block harness LAND, ~35 visual-diff reports, wiring the 2 gates zero-tolerance, the P-F3 gate fix, the `colourPreset` pill-fill fix, container inline-gap check, spec reconciliation to declarative box_family. See next-session-prompt.md. SoT = handoff.md + next-session-prompt.md.

## Human Summary (prior — D298)
**LATEST (D298, 2026-07-10) — no-inline Wave 2: all 10 blocks LANDED (3 dispatch rounds).** Commits `d8923f4c` (R1: collapsible-text, table-of-contents, decorative-image, countdown-timer), `eeb17be4` (R2: icon-list, brand-strip, timeline, process-steps), `60d55b23` (R3: mega-menu, mobile-nav). All 10 migrated to the no-inline contract + LANDED via the reusable harness (`scripts/no-inline-land-verify.js`, asymmetric instances on page 1356, zero-inline + computed box at 375/768/1440); orchestrated Opus + solo-Sonnet-per-block; every block re-gated by me (STOP-16: build gates + box-family-guard 0 + suite 440 pass); visual-diff reports at repo-root. **Roster now 25/59; ~34 remain.** Round bonuses: ToC's long-standing "broken" bug fixed (custom attr named `style` collided with WP's reserved `style` → `tocStyle`); several `view.js` re-inline traps closed (decorative-image parallax, brand-strip marquee, mobile-nav swipe). navs are KEEP-STRUCTURE (mobile-nav content-KIND but a genuine 7-zone drawer → not block-private). **F3 finding (Bean-decided, deferred):** 3 nav baseline rows traced to structural CSS / a viewport safety clamp (MIS-TAGGED, not dead-control debt) — left baselined, gate-precision fix tracked `P-F3-NAV-MISTAG-GATE`. **NEXT = Wave 3** (content-KIND composites → block-private: info-box, testimonial, team-member, product-faq-item, notice-banner, option-picker, product-faq). See next-session-prompt.md. SoT = handoff.md + next-session-prompt.md.

## Human Summary (prior — D296, 2026-07-09)
**LATEST (D296, 2026-07-09) — the no-inline styling rollout advanced through quote/media/hero + a universal grid-scoping; the hard architecture is settled, a ~52-block roster remains (phased waves).** This session: **Task 1 (`13fd1634`)** quote + media LANDED block-private (zero inline, box-objects) + the shared wrapper max-width/contentWidth/band inline→scoped; **a qc-council (unanimous) settled the pattern selector — content-KIND composites that use only box+width go BLOCK-PRIVATE (like quote), section/layout composites KEEP the wrapper (like hero) — D294**; **Task 2 (`74d164c6`)** hero (the biggest block) LANDED — 6 per-area box-object families + F3-drain, keeps the wrapper, blub-255 2-rater panel SHIP; **grid-scoping (`d65e7d10`, D296)** moved the shared wrapper's grid/flex CSS inline→scoped (STOP-68 CLOSED — 0 inline grid across all 95 live SGS elements, grids intact). **7/59 styling-support blocks now migrated** (button/container/heading/text/quote/media/hero); **~52 remain** — a phased, multi-session waved programme (Bean-corrected: the prompt's "~26" was a narrow slice). **NEXT SESSION = run a roster wave** (Wave 1 single-element leaf blocks → Wave 4 composites → Task 4 wire the 2 gates zero-tolerance). See `.claude/next-session-prompt.md` (the wave plan + MANDATORY READING GATE + STOP catalogue incl. STOP-68). SoT = handoff.md + next-session-prompt.md.

## Human Summary (prior — D293, superseded by D294-296)
**LATEST (D293) — the no-inline styling PILOT is COMPLETE + the converter is hardened; the universal rollout is fully specified + underway (4/50 blocks LANDED).** The box-object no-inline mechanism (KEEP supports + `__experimentalSkipSerialization` + `wp_style_engine_get_styles(#uid)` scoped `<style>`; per-side/corner props → named OBJECT attrs via WP `BoxControl`, gated by the DB `box_family` column + AST gate) is now PROVEN LIVE on **button, container, heading, text**. Commits: `9f281337` button (all 9 acceptance tests LANDED, incl. the asymmetric tier accumulator + security hardening), `ca8b1787` converter (a 3-rater `/qc-council` found + fixed 3 silent-loss paths in the shared merge spine — root_supports overwrite, destination-fold dict/scalar collision, aliasing; +8 tests, 436 pass), `3e266090` heading+text (full 100% no-inline + 100% box-group), `0b2199ce` heading wrapper-removal (`<h{level}>` is the root, no dead div). **Bean LOCKED the per-block bar:** 100% no-inline + 100% box-group (incl. Tablet/Mobile tiers) + no useless wrapper + F3-hardcode drained + device-tiers-only (1023/767; custom→`sgsCustomCss`) + Spec 31/32 — captured in `.claude/plans/2026-07-09-per-block-no-inline-migration-contract.md` (HOW) + `.claude/plans/block-migration-DONE-checklist.md` (11 end conditions). No version bumps / deprecations (pre-production, overrides STOP-57). **NEXT SESSION = continue the rollout** (quote → media → hero → the ~26 block-private blocks + F3-drain, then wire the 2 gates zero-tolerance) — each block to the DONE checklist, LANDED-verified. See `.claude/next-session-prompt.md`. SoT = handoff.md + next-session-prompt.md.

## Human Summary (prior — D291, superseded by D292's proven mechanism)
**D291 — button styling re-architecture + the universal fix specced & council-validated.** The button was rebuilt to **Spec 32** (Component Styling Token Contract, NEW): semantic `.sgs-button--{preset}` class consuming `--wp--custom--button-presets--*` tokens, hover in `:hover`, no inline colour — **hover/preset-colours/structure Bean-confirmed working live**. But QC caught that cloned buttons STILL carry inline `padding-*`/`margin-*` — ROOT CAUSE PROVEN: **WordPress-native block-supports serialization** (`supports.spacing/color` + converter routing to `style.*` → `get_block_wrapper_attributes()` inlines). This is the SAME Thread-2 problem as the L4 per-area routing below (route CSS → the correct canonical block attribute; Spec 31 §46/§58's designed D1 router is absent). **qc-council validated FS-A** (route base CSS → canonical typed attrs, fallback-safe/class+token/per-block); detection-script GO, auto-fix NO-GO. **Bean's reframe (verified):** container has tier attrs but no base attr; the responsive helper already does the base+tier cascade — so the fix = add the canonical base attr the tiers imply + a naming standard. **NEXT SESSION = the two-thread styling programme** (`.claude/plans/2026-07-09-styling-two-thread-programme.md` — start with its ▶ SESSION OPENING SEQUENCE). RC2 (button enum `custom` fix) staged, deploys with the Thread-2 build. This programme SUBSUMES the L4 cascade thread below — they converge.

## Human Summary (prior — D290, L4 cascade; converges into D291 programme)
**L4 per-area extraction SHIPPED + LANDED (D290, `f96bf871`, 2026-07-07) — the hero content padding is FIXED.** `.sgs-hero__content` padding lands at every tier on page 8 (28/20/40 @375, 56/48 @1024 — was cramped-left, 72/64 @1280 — class-scoped residual painted). Wired as `assembly` step 3d (universal, DB-gated, recursive), + a council-caught cta-section band-alias fix. MF-5 resolved. **Remaining fronts (all lower priority now the headline defect is fixed):** (a) D289 residual ID-precedence — an ID-scoped block rule (typography helper `#sgs-hdg`) still isn't overridden by its residual (`P-RESIDUAL-RENDER-PRECEDENCE`, STOP-64); (b) per-element extraction tidy-ups (notice-banner child-owned text; sgsCustomCss marker pairs — `P-L4-PER-ELEMENT-EXTRACTION-FOLLOWUPS`); (c) the parity tool is broken (font-family normalisation + clone-only over-count — use per-section visual diff, not the number; Bean deprioritised the fix); (d) the 4 pre-existing golden failures (other threads). SoT = handoff.md + next-session-prompt.md.

<!-- prior D289/D288 front (SUPERSEDED — L4 now done) -->
**Superseded pointer (was NEXT at D289 close):** the container L1-L4 cascade / L4 wiring — NOW DONE (D290). D289 SHIPPED + LANDED (page 8, `9a22b6f2`): the **universal responsive breakpoint router** — a draft `@media` rule routes by breakpoint CLASS for every element of every block (device-tier 768/1024 → the block's tier attrs; non-device 600/640/1280 → the block's `sgsCustomCss` Additional-CSS). Proven: hero h1 52px at 768/1024 (was 58); ingredients/gift/social non-device breakpoints captured (were dropped). The visible hero content PADDING still doesn't land — root cause PROVEN: the **L4 per-area extraction is UNWIRED for composites** (nothing sets `ctx.area_name` → `grid_area` resolver + `attr_for_area_property` never fire → the wrapper's box-CSS is never collected). Bean scoped D289 to the router (Task A) only and deferred L1-L4 to next session (needs L1-3 working first + must work for ALL block types). ALSO folded into L4: the residual render-precedence limitation (STOP-64 — wrapper-class residual can't override an ID-scoped block rule; once L4 routes the class-scoped padding, the residual wins). SoT = handoff.md (2026-07-07 D289) + next-session-prompt.md + parking `P-HERO-SUB-MAXWIDTH-NESTED-CHILD`.

<!-- parallel thread (D284/D285) -->
**THE PRODUCT-CARD FRONT IS DONE (D284, 2026-07-06).** Two commits, both LANDED on page 8 + pushed: (1) the typed/cloned product-card's **option-picker now clones** — root-caused 3 gaps on the real Featured node (no array-content-lift capability, no packSizes item-schema, and the array lifter refused a plain-text LEAF item's own text); fixed with a new UNIVERSAL match layer **L1d** in `converter/resolvers/array_content.py` (leaf item lifts its own text; leaf-guarded) + block.json capability/schema + typed render emits the self-contained `sgs/option-picker` via render_block; content parity 96→100. (2) **typed-element un-hardcoding** — typed CTA now attr-driven (cta* via sgs_button_element_style_css + marker class), descColour/priceNoteColour/desc-line-height controls added, and the hardcoded **'Fraunces'** price font (a client font in a framework block — Bean caught it) swapped for `var(--wp--preset--font-family--display)`; editor preview mirrors the frontend picker. Gates 0 net-new; 2 cross-model /qc-council raters GO on Task 0. **Prompt Tasks 1+2 (CSS-property column seeding + capability-roster rollout) NOT done** — deliberately scoped out; both fully mapped for next session. 4 items parked. **NEXT = Task 1+2 (`prompts/2026-07-06-block-fixes-testimonial-button.md`) OR the untouched container L1-L4 cascade thread (`next-session-prompt.md`).**

<!-- prior (D282) -->
**THE PAGE-8 QC BATCH IS DONE (D282, 2026-07-06).** D2 is NO LONGER DEPLOYED into the page (debug log only, `SGS_EMIT_D2_PAGE=1` restores) — so the live page shows the HONEST gap set (parity 80/81/81 D2-masked → 69/70/70 honest), which surfaced Bean's visible defects. Diagnosis-first (3 parallel read-only investigators, every root cause main-session-verified vs code+DB+live DOM) fixed 3: **#1 container gaps** — the L3 gap-gate (`root_supports.py`) was consuming `gap` into a dead `style.spacing.blockGap` leaf; fixed to check `blockGap` specifically, so gap reaches the wrapper-rendered `gap` attr (grids now show 16/60/14px; parity 70/71/71; Phase-4 red→green test + 2 partition tests corrected); **#4 hero image** (hardcoded split padding moved off the outer grid → image flush); **#9 testimonial** (hardcoded cream slide bg → transparent). Also **de-duped the hero** (splitGap was the ONLY true attr dupe on a 30-block audit — removed + its hardcoded 24px gap default) and **cleaned dead schema** (customWidth orphan off 27 composites +53 DB rows; dead blockGap off accordion/product-faq). Recurring cheat = D228 hardcoded defaults. Proven-open: **L4 per-area extraction** (composite grid-area box-CSS not fed to the wired area resolver → hero contentPadding "no value extracted") + **L3 universal reach** (Bean: universal = ALL L3 landing across examples, not one rule). **NEXT = the container L1-L4 cascade deep-dive (Bean's slow understand-first agenda: map → group L1-L4 → prove universal → explain simply → layer-by-layer test).** See next-session-prompt.md.

## State Snapshot

## GROUND TRUTH FIRST — do not trust any doc's cached status; verify these every session
- **HEAD + tree:** `git log -1 --stat` + `git status` + `git branch --show-current`. NO doc's cached line is authoritative.
- **D-number ceiling:** `grep -oE 'D[0-9]+' .claude/decisions.md | sort -V | tail -1` — decisions.md is the SOLE source of truth.
- **Framework counts:** query `/sgs-db` or `/wp-blocks` — the DB is authoritative; counts are NOT maintained in prose.
- **Commit discipline:** commit by EXPLICIT PATH, never `git add -A`. `main` is the source of truth.

## PARALLEL / DEFERRED programme — the no-inline styling ROSTER (~52 blocks, phased waves)
- **NOT the current front (D306 NEXT = the page-8 discrepancy fixes — see the D306 summary above + next-session-prompt.md).** This is a real parallel rollout, resumed after the page-8 programme closes. Roster count drifts across summaries (7/59 → 25/59 → this section's 15/59) — treat as approximate; the DB + the DONE-checklist are authoritative, not this line. The hard architecture (mechanism, D294 pattern selector, grid-scoping) is SETTLED — the rest is templated repetition. **Reusable LANDED harness:** `plugins/sgs-blocks/scripts/no-inline-land-verify.js` + a per-wave manifest — the cheap #10 (LANDED) gate for every future wave.
- **SoT for current status + the wave plan:** `.claude/next-session-prompt.md` (Wave 1 leaf blocks → Wave 2 more leaf + F3 → Wave 3 content-KIND composites BLOCK-PRIVATE → Wave 4 section/layout composites KEEP-WRAPPER → Task 4 wire gates) + `.claude/handoff.md`. READ THOSE + the MANDATORY READING GATE + STOP catalogue (incl. STOP-68).
- **Pattern selector (D294):** content-KIND composites that use only box+width → block-private (like quote); section/layout composites → keep `SGS_Container_Wrapper` (like hero), which is now fully scoped (spacing/max-width/contentWidth/band/GRID — D292/D294/D296).
- **Per-block recipe (PROVEN this session):** solo Sonnet migrates block files (its dir only) → I add box_family seeds centrally in `sgs-update-v2.py` → build + gates + deploy + `/sgs-update` (--stage 1 seeds / --stage 10 prune, STOP-66) + purge → LANDED (re-clone page 8 if on it, else asymmetric on page 1356) → visual-diff report at repo-root → commit path-scoped.
- **Canonical spec:** `.claude/specs/31-UNIVERSAL-CLONING-PIPELINE.md` (§3.A CSS routing, §13.4 FR-31-5.2, §13.6 FR-31-21.1 composite-mirror + the D294 clarification, FR-31-22 box-object) + Spec 32 §6.1 (box-object contract) + the DONE-checklist + migration-contract plans.
- **The cloning pipeline (L1-L4 cascade / L4 per-area extraction) is DONE (D290) — no longer the active thread.** *(D297 nuance: L4 per-area RENDERING was done at D290, but the CLONING side for a block that migrated its per-area family to a box-OBJECT was still flat until D297 — now object-aware in `fold_helpers.route_area_css_to_block_attrs` + `grid_area.py`. If a FUTURE wave migrates another composite's per-area family to objects (mediaPadding/imagePadding/etc.), the same box-object routing already covers it universally — no per-block change.)*

## DONE / ARCHIVED — theme + WooCommerce layer (Spec 30, not an active thread)
- Spec 30 COMPLETE + MERGED (D220). Deferred roadmap items live in parking.md.

## DOC DISCIPLINE
- decisions.md / parking.md / mistakes.md are append-only; D-ceiling check MANDATORY before a new D.
- /handoff writes handoff.md + next-session-prompt.md + this block.

## BLOCKERS
- None block the next session. Known-open items live in handoff.md "Known Issues".

<!-- Caps: this file <=24576 bytes, <=60 lines of body. History -> memory/state-archive.md. -->
