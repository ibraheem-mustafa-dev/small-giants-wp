# Wave 3C, lane C: the independent units (U-12, U-15, U-17)

Paste everything below the line into a fresh session. It runs alongside the lane A session (the menu and drawer units), on files lane A never touches. Delete this file in the same commit that closes lane C.

---

Invoke /autopilot before doing anything else.

Resume the merged Spec 36+37 track, small-giants-wp on main, as **Wave 3C lane C**: the three units that share no files with the menu and drawer chain, run while another session works lane A (U-9+U-11 and the rest of the nav chain) in the same worktree. Lane C is:

1. **U-17, the Lottie player** (DEC-13, Bean's answer 1h): a Tier H admission through Spec 38 §1.2a with its own D-numbered decision entry and the Spec 38 §1.2a membership amendment in its own commit; `lottie-web` 5.13.0 light build (SVG renderer, npm-bundled, never a CDN, loaded lazily behind a swappable adapter, never under reduced motion); on `sgs/responsive-logo` an `animationSubstrate` value (`svg-draw` or `lottie`), `lottieSource` (a media-library .json, validated at upload, fail closed) and trigger and loop attributes, with the existing logo `<picture>` as the poster; a shared `supports.sgs.lottie` helper so other media slots can adopt it. Part (iv) is granted: a page that uses Lottie gets a named 60 KB allowance for the player alone; pages without it ship nothing extra. Research and integration shape: `.claude/reports/2026-09-21-lottie-player-research.md`. This is a shared mechanism, so it is design-gated.
2. **U-12, eight header furniture blocks** (M-18), in priority order: local-time clock, language switch, back-to-top, account or log-in link, then store selector, wishlist, theme toggle, sound mute. Plus the two `headerEssential` flags on `product-search/block.json` and `filter-search/block.json` (main thread edits those two). Accessibility per plan §5 step 7: `lang` on each language option; back-to-top moves focus to the target.
3. **U-15, the self-changing header message** (M-07) on `sgs/notice-banner`: rotate, random and live clock. WCAG 2.2.2: a pause control for the rotating message, and no `aria-live` on a live clock. Bean judges it by eye.

**Read first:** `.claude/LEDGER.md` (the Spec 36+37 merged track section); `.claude/plans/2026-09-21-wave-3c-implementation-plan.md` §1 (esp. 1c and 1h), §4 (the U-12, U-15 rows and the parallel dispatch rules under the table), §5 (the per-unit loop) and §9; families M-18 and M-07 in `.claude/reports/reference-requirements/families-master.json` with their reference rows; Spec 38 §1 and §1.2a before U-17's design; `plugins/sgs-blocks/CLAUDE.md` (block pattern, Block Customisation Standard, colour and typography controls). For each new block, query the framework DB first for an existing block or attribute that already does the job (`python ~/.claude/skills/sgs-wp-engine/scripts/sgs-db.py`); never build a duplicate.

**Order and gates:**
- One short design note per unit in `.claude/reports/` with the exit-cell table, attribute names and defaults fixed (names checked free in the DB). U-17's note goes through /qc-council (two models picked by /delegate) and then my sign-off before any build. U-12 and U-15 need no design sign-off from me; U-15's result gets my eye.
- Bundle your questions to me: at most one message with every open decision for lane C, ranked, each with one recommendation.
- Start U-12 first (no sign-off needed) while U-17's council runs.

**Build (token discipline):** keep the main thread to design, decisions and reading every diff. One Sonnet builder per new block directory for U-12 (the plan's parallel dispatch rules, verbatim: each agent writes ONLY its own `src/blocks/<new-block>/`; no `git add`, commit, stash, `npm run build` or deploy; no edits outside its directory; no cleanup of files it did not create; tests in its own directory). One Sonnet builder for `src/blocks/notice-banner/`, and one for U-17's player and helper files. Haiku for mechanical checks. Pick every model through /delegate. Every block ships its inspector controls in the same commit (SgsColourPanel, TypographyControls, ResponsiveControl, no inline style, Spec 32), PHP standalone tests with a negative control, and a clean `npm run build`.

**Stay out of lane A's files:** never edit `nav-menu-markup.php`, `nav-menu-submenu-css.php`, `src/blocks/{nav-bar-menu,nav-drawer,nav-drawer-menu,mega-panel,site-header,site-header-row,site-footer,site-footer-row}/`, `src/shared/nav-interactivity/`, `src/header-behaviours/` or `includes/class-sgs-header-behaviours.php`. If a lane C unit genuinely needs one of them, stop and tell me.

**Shared-worktree rules:** before every commit run `git status --short` and commit ONLY your own paths with an explicit pathspec (never `git add -A`, never a glob, never stash); the other session commits alongside you. Before any `[gates-ok]` bypass, name each failing gate and the file it points at, and that file's owner (`git log -1 -- <file>`); only another session's failures may be bypassed. Run the full `npm run build` before calling a unit done. Push after each unit.

**Deploy and verify:** deploy only with `python plugins/sgs-blocks/scripts/build-deploy.py --target sandybrown`. Lane A deploys to sandybrown too: before deploying, check no lane A deploy is running (ask me if unsure) and never deploy over lane A's uncommitted work (the dirty-tree gate refuses; do not pass `--allow-dirty`). Verify on a lane C fixture page of your own (for example `/qa-furniture/`), never on lane A's `qa-hdr-*` pages or the active test header 3777 and drawer 3778. One headed Chrome window, real pointer input, tell me before opening it. A report per unit in `reports/visual-diff/<block>-<date>.md` with `source_sha` from `python plugins/sgs-blocks/scripts/visual-report-sha.py <block>`.

**Docs in the same push:** the plan's §4 rows for U-12, U-15, U-17 (mark done, name any residue); `.claude/verify/merged-spec36-37-track.md`; `families-master.json` coverage for M-18 and M-07; a `decisions.md` entry (U-17 has its own); LEDGER (re-read it from disk immediately before editing, since lane A writes it too, and replace only the lane C lines). Then `python .claude/hooks/handoff-preflight.py --check` and `python plugins/sgs-blocks/scripts/lints/lint-spec-drift.py --check`.

**Done when:** all eight furniture blocks, the self-changing message and the Lottie player are built with editor controls, tested, deployed, verified live and documented, or each leftover is named with its reason in the unit's plan row.
