# Next session — recovery residuals, the held commit, and the root-filter NO-GO

**Written 2026-08-29.** Supersedes `2026-08-28-rollout-detectors-and-the-null-element-tail.md`
for the client-controls track. Invoke `/autopilot` first. Bean is QC-only: batch every open
question into one message at the start, then work without interrupting him.

---

## READ THIS BEFORE ANYTHING ELSE

**Five tracks share `main`.** Commit with explicit paths (`git commit -- <paths>`), never
`git add -A`, and re-check the branch in the same command. `git commit --amend` flushes the
WHOLE index regardless of your original pathspec.

⛔ **NEVER `git stash` on this worktree.** On 2026-08-28 a peer's stash swept every session's
uncommitted work; the ref was then dropped by someone else. 553 lines survived only as a
dangling commit. If you think you need a stash, you need a commit or a worktree.

⛔ **Two commit-gate layers, different bypasses.** `[gates-ok:<reason>]` in the commit MESSAGE
clears the session hook. The git-native `.githooks/pre-commit` is separate and needs
`--no-verify`. Identical-looking output, different mechanisms. Do not reach for either without
Bean; `--no-verify` also discards gitleaks, F5, F6 and cheat-gate, which are usually passing.

⛔ **The visual-diff gate takes a SCOPED bypass, not `--no-verify`:**
`SGS_VISUAL_GATE_SKIP=<block> SGS_VISUAL_GATE_REASON="..."`. A skip is a DEBT, logged to
`reports/visual-diff/manual-skips.log`. Pay it with a real report or it becomes hero's.

⛔ **Long prose through a `cat <<EOF` heredoc FAILS in the Bash tool.** Use the Write tool.

---

## ⛔ THE FOUR PITFALLS THAT COST REAL WORK ON 2026-08-28

Not general advice. Each produced a confident wrong answer or destroyed work.

### 1. A false completion claim is how work gets lost — not the stash

At 20:29Z the prior session told Bean the classifier was "fixed and **committed**". `git log`
still showed a commit from five days earlier. Nothing protected the file, so the stash took it.
**Before writing "committed" anywhere, run `git log -1 -- <path>` and read the date.** The
stash was the proximate cause; the false claim was the root cause.

### 2. FOUR too-narrow greps each produced a confident FALSE answer

Every one would have caused a wrong action. This was the dominant failure mode of the day:

| Grep | False answer it gave | Truth |
|---|---|---|
| `gridTemplateColumns` in `render.php`/`edit.js` → 0 | "dead on 10 blocks, delete it" | The **shared wrapper** reads it. Live, client-reachable. Nearly deleted from 9 blocks. |
| flat key against nested JSON | every lookup `None`; negative control "passed" | `css_element` is nested under `fields`. |
| `import.*SgsBorderControl` → 0 in 4 files | "peer shipped undefined refs" | Multi-line import lists. Imports were present. |
| `find -name "style.css"` on the server | "not deployed" | webpack emits `style-index.css`. |

**A grep returning 0 is a hypothesis, not a finding.** Before acting on absence, add a
POSITIVE CONTROL — grep something you know exists, in the same file, the same way. If the
control also returns 0, your pattern is wrong, not the world.

### 3. A test can pass for the wrong reason — build the control first

`extract-signatures.py --self-test` returned 8/8 PASS on the file with Cause A/B **removed**.
It passes with or without the feature, so it could not gate the recovery. Separately, a
fidelity check "passed" only because a broken lookup returned `None` for everything, including
the negative control. **A negative control that passes while every positive control fails is
not a control — it is the same bug twice.**

### 4. `--dry-run` before any shared-DB write, and read what it says

`sgs-update-v2.py --stage 1 --dry-run` revealed the ordering trap: the derived layer had been
regenerated by an already-reverted classifier, so reseeding would have written poisoned values
into the DB five tracks share. **Order: restore the generator → regenerate the derived layer →
reseed → commit.** Getting it wrong bakes in a regression that every gate passes.

---

## What landed 2026-08-28 (do NOT redo)

Three commits, pushed: `297e98855` (Cause A/B classifier recovered, 553 lines, also preserved
as tag `recovered-cause-ab`), `1cb7961a1` (form six-track grid), `4f5d7eca6` (its visual-diff
report, live-verified). Uncommitted but applied: a schema guard on TIER 3.20, the card-grid and
nav-drawer routing fixes, and 8 pruned ghost rows.

**Measured, not claimed:** classifier `resolved` 503 → **509**; F6 gate **12 findings → 0 NEW**;
`node_modules` **0 → 972** (no track could build); full build green, all 69 gates.

---

## Task 1 — Land the held commit (the hold IS discharged)

Bean's instruction was *"hold this commit, chase down card-grid/nav-menu owners first."* Both
owners are now resolved, so the condition is met.

⛔ **But `gate:full` still fails on two committed converter tests, and the held work causes
both.** Do not land until Task 2 decides them.

**When landing:** ~30 files. Explicit paths, EXCLUDING the motion track's three
`fx-generative-background.*` files and the regenerated `consistency/` artefacts (clone track).
Then write the D-entry and update the LEDGER. Note `git commit -- <path>` fails on UNTRACKED
files — `git add` the specific path first.

## Task 2 — Two failing tests: one mechanical, one a decision

`test_per_element_state_lift.py::...five_stranded` — a stale exact-set snapshot. The held work
legitimately ADDS three routings (`grayscaleHover → filter/image/hover` on card-grid, gallery,
team-member). Update the expected set. Mechanical.

`test_state_value_lift.py::test_card_grid_hover_grayscale_lifts_as_boolean` — **a real
decision; the council says this test asserts a defect.** See Task 3.

## Task 3 — Root-filter capability: council returned NO-GO. Read before re-opening.

Brief: `.claude/reports/2026-08-28-root-filter-capability-design-gate.md`. Six personas, five
reported. Grades D, D+, D+, D+, C+. **Do not build Shapes A, B or C.** Convergent findings:

- **The defect is FIVE divergences, not one.** `grayscaleHover` renders **grey at rest, colour
  on hover** (`card-grid/style.css:274,279`); the draft asks the **inverse**. Also wrong
  element, wrong trigger (`__item:hover`), wrong amount (always 100%), no reduced-motion.
  Identical across all four blocks. The DB's `css_state='hover'` is a **false declaration**.
- **A live Rule 4 silent drop exists TODAY, independent of any shape.**
  `state_value_lift.py:139` does `return parsed > 0`, so `grayscale(0.4)` renders as 100% —
  emitted as a `Write`, so no gap row. The module already gaps out-of-range values and
  anisotropic `scale()` four lines away. **Fix this first: ~90 min, forecloses nothing.**
- **Demand is ZERO.** All five client mockups scanned: exactly one filter rule
  (`.sgs-footer__logo`, `brightness(0) invert(1)`) — an element, in the footer (excluded from
  fidelity), which none of the shapes would carry.
- **Passthrough already exists and the brief omitted it.** `excluded_properties` rows say the
  property is "still cloned via passthrough `<style>`"; `ResidualBand → sgsCustomCss` is
  "never snapped, never dropped". Evaluate this BEFORE building anything.
- **Shape C is disqualified.** `filter` makes an element the containing block for
  `position: fixed` descendants, and `container/style.css:215` + `hero/style.css:113` use fixed
  as the load-bearing technique. It breaks the container's own parallax, silently.
- **Shape A is inert as written.** Resolver domain is `_BASE_ELEMENTS = ("", "root", "self")`
  (`db_lookup.py:1699`). `wrapper`/`grid` are NOT in it — declaring there ships a dead attribute.
- **No client can reach the existing setting.** ZERO `edit.js` files reference `grayscaleHover`.
  On `team-member`/`info-box` TWO attrs drive one class (`sgsHoverGrayscale` visible,
  `grayscaleHover` invisible). A new capability must not repeat this.

⛔ **`hover-effects` is NOT available as an owner** — `decisions.md:10074/10076/10078`: being
removed, STOP against repairing, zero usage across 194 canary pages. But it is **not gone**:
`includes/hover-effects.php:37` still registers `render_block` on every render.

## Task 4 — Carried, unchanged

- **Two live production bugs** (the only client-visible items): `testimonial.quoteColourHover`
  paints the whole card AND only renders if `borderColourHover` is set;
  `process-steps.numberBackgroundHover` paints the entire step grid.
- **Batch-A findings are UNVERIFIED** — that agent was killed having traced nothing, yet
  detailed findings were reported from it 90 seconds later; the council caught one as false.
- **CHECK A**: re-measure. The tree raises the ceiling 206 → 207 — justify or revert.
- **Hero owes a visual-diff report.** **Spec 39 still does not exist** and paces the migration.
- **Rule 36** is advisory with backlog 0 — flip it blocking to ratchet.

---

## Known instrument faults — do not rediscover

⚠ `computed-parity.js` measures **resting styles only** — zero occurrences of "hover". A
correct hover fix goes green whether or not the hover works.
⚠ `inspector-scan/run.js --json` non-deterministically drops the last 2-3 rules on a large
single `console.log` write. Re-run before trusting it.
⚠ SGS block CSS is lifted into `uploads/sgs-css/` — grepping page HTML proves nothing. Measure
`getComputedStyle` in a real browser.
⚠ The element manifest (`states.hover.attrMap`) is **converter-facing only**: 0 PHP consumers,
0 JS, 15 Python. Declaring an entry renders NOTHING.
⚠ A deploy can report `[ABORTED] ... Nothing was uploaded` while its wrapper reports exit 0.
Read the output, never the exit code.
⚠ `wp-cli` over SSH needs `--path=`. Long base64 breaks over SSH — pipe on stdin.
⚠ Removing a git worktree with `--force` without unlinking the junction empties the SHARED
`node_modules` (962 → 0), silently breaking every track's build.
⚠ Another session's build can ship YOUR uncommitted work — one shared worktree, both directions.
