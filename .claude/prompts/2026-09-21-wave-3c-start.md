Invoke /autopilot before doing anything else.

You are continuing the merged Spec 36 + Spec 37 nav, header, footer and drawer track for Small Giants Studio (small-giants-wp, branch main). Waves 1 to 3B are done and Bean has answered every open question (be token-efficient: he is on a time crunch): 13 reference sites were captured, clustered into 46 capability families, adversarially reviewed and signed by Bean. Your job is Wave 3C: build the families. Scope is full: nothing is parked, deferred or dropped.

Read, in this order, in full:
1. `.claude/plans/2026-09-21-wave-3c-implementation-plan.md` is the plan. Start with its "Read this first" block, then section 1 (Bean's answers), then section 9 (commands, fixtures, gotchas and the lessons that each cost time).
2. `.claude/reports/reference-requirements/FAMILIES-MASTER.md` (the 46 families and the decisions) and `families-master.json` (the same data as JSON: `families`, `units`, `decisions`, `engineering_notes`, `lanes`, `verification`, `meta`). The plan's section 4 supersedes FAMILIES-MASTER's unit table.
3. `.claude/plans/2026-07-29-merged-spec36-37-track-strategic-plan.md`, sections "Checkpoint protocol", "Wave 3B", "Wave 3C" and the Gate 3C definition.
4. `.claude/reports/reference-requirements/CAPTURE-PROTOCOL.md` and the per-reference `<ref>.md` and `<ref>.json` for whichever unit you are building.
5. Spec 36 (`.claude/specs/36-SGS-NAVIGATION-SYSTEM.md`) in full before U-1, FR-36-6 (the drawer) before touching the drawer model, and Spec 37 (`.claude/specs/37-HEADER-FOOTER-BUILDER.md`) sections 1 and 2 before any drawer unit. Spec 37 section 1 carries the boundary rule: a change that crosses the line edits BOTH specs in the same commit.

Then Step 0 in the plan's section 3 (0a and 0b before U-1; 0c before Wave 4; 0d before U-16; 0e optional), then the units in the order in section 4, each through the per-unit loop in section 5. U-12 (eight furniture blocks, one agent per block in its own new directory) and U-15 run in parallel with the nav chain from unit 1; every other unit runs one at a time because they share files.

Rules that apply: commit to main with explicit pathspecs, never a PR, never `git stash`, never `git add -A`, no attribution lines in commits; push after every unit; deploy only through `plugins/sgs-blocks/scripts/build-deploy.py --target sandybrown|indus-test|eye-care-test`, never with `--allow-dirty`; verify in a REAL headed Chrome in ONE window and wait for each page to load fully; measure inside a real `sgs/site-header` fixture (`qa-hdr-*` pages), never a loose block; prove the cause before a fix; design-gate and QC-council every shared-mechanism change before building it; a reference is never trimmed to fit the framework; UK English; no em-dashes in anything written in Bean's voice; ADHD-friendly reports (Problem, Effect, Solution; menu plus one recommendation; concise). Never treat a background-task notification as Bean's answer.

First action (under 5 minutes): read the plan, run `git status`, then tell Bean in three lines: that his section 1 answers are recorded and nothing is open, whether the dirty worktree blocks a deploy, and that Step 0a is ready to run. Step 0a writes to the shared framework DB and the project starts in plan mode, so do not run it until Bean says go; then run it in the background and poll its exit.
