Invoke /autopilot before doing anything else.

You are resuming Wave 3C, unit U-1 (nav surfaces), of the merged Spec 36 + 37 track (small-giants-wp, branch main). Bean is on a time crunch: be token-efficient, add no reviews beyond what is listed here, use headed Chrome in ONE window, wait for pages to load, and commit with explicit pathspecs (never `git add -A`, never stash, no attribution lines, never a PR). Read `.claude/plans/2026-09-21-wave-3c-implementation-plan.md` (Read-first block, section 1, section 9), `.claude/reports/2026-09-21-u1-design.md` (the council-changed design), and `.claude/reports/2026-09-21-u1-c4-surface-ground-design.md` (commit 4 design, council changes, the 4e ruling). Deploy only with `python plugins/sgs-blocks/scripts/build-deploy.py --target sandybrown --blocks-only --skip-build` after a webpack build via the PowerShell tool.

## UPDATE (end of the 4f session): 4b and 4f-1 are DONE, DEPLOYED and LIVE-VERIFIED. Start at 4c.

State: origin/main includes `c279c4aab` (JS control), `1d5b3ac39` (new theme presets, rename, hover and container selects, composers accept a preset's strict color-mix, theme 1.5.91) and `fbbb5df3d` (the live reports). Deployed to sandybrown with `python plugins/sgs-blocks/scripts/build-deploy.py --target sandybrown --skip-build` (plugin AND theme, exit 0, 472 s). The other session's converter files were committed before the deploy, so the dirty gate did not block.

DONE this stretch:
- Step 5: `src/utils/shadow-model.js` (parse, serialise, generate, recognise; `scripts/tests/test-shadow-model.mjs` round-trips 90 builder combinations and caught a real defect: Glow at elevation 6 exceeded the 100px blur cap). `src/components/ShadowControl.js` (top level, keeps `shadowAttrName` and `shadowAttrKeys`) plus `src/components/shadow-control/*` (Simple, Layers, Raw CSS, hover-colour-only, colour and opacity, preview helpers, `useShadowPresets.js`). Shape and colour are written in one `setAttributes` when the mount uses `attrNames` (the explicit-props mounts still write two calls). `resolveShadowPreview` is deleted; `resolveShadowPreviewComposed` delegates to the composer; container, hero and trust-bar now pass their colour.
- Step 6: `scripts/migrate-shadow-presets.py` (survey, fix, check, self-test; two gates in `gates.json`) wrote the 11 presets and `settings.custom.shadowColour` (`var(--wp--preset--color--text)`) into `theme.json`, renamed subtle to whisper and raised to soft in 17 files, and collapsed dark mode to `--wp--custom--shadow-colour: #000000`. `includes/hover-effects.php` now checks that a hover shadow is a slug (`is_hover_shadow_slug`) and the hover and container selects read the theme's presets. Both composers accept exactly one strict `color-mix(in srgb, <site|palette var|hex> N%, transparent)` per colour token (needed because presets carry it), with shared cases and a filter test.
- Step 7: the existing gates covered the new files (helper parity, dead controls, editor render parity, inspector scan passed in the gate run).
- Step 8: live-verified in one headed Chrome window; reports `reports/visual-diff/mega-panel-2026-09-21.md` (4b, F5, forced-colours rule in the live lifted CSS) and `reports/visual-diff/container-2026-09-21.md` (editor round trip). Mega post 1745 was restored byte for byte.

NOT proven, tell Bean: (a) forced-colours emulation on the live page was not run (the chrome-devtools tool cannot set it; the nested rule was proved under emulation on a local page earlier, and the live lifted CSS was read and the CSSOM parsed it); (b) no live menu sets a submenu shadow, so the drop-shadow chain is tested by PHP only; (c) old browsers without CSS nesting are reasoned, not run.

Bean's rulings on the residuals (54979b044, pushed, NOT yet deployed): (1) client snapshots: DONE. `scripts/sync-snapshot-shadow-presets.py` (survey, fix, check, self-test, two gates) makes every `sites/*/theme-snapshot.json` carry the framework's `settings.shadow` and `settings.custom.shadowColour`; Mama's snapshot is synced in the WORKING TREE only, because another session has uncommitted extractor changes in that file (commit it with theirs, do not commit it alone). (2) explicit-props mounts: only `GridItemDefaultsPanel` was one (the earlier claim of about 20 was wrong: every other mount already used `attrNames` and writes once); it now uses `attrNames`. (3) dark mode: Bean's point was right and measured on the canary (dark-mode.css injected, `data-theme=dark`, cards #1E1E1E on #121212): a black shadow is invisible, the same as no shadow; a light shadow separates the card. So dark mode now sets NOTHING for shadows: the site shadow colour (`custom.shadowColour` = `var(--wp--preset--color--text)`) is light there and every preset becomes a soft glow (theme 1.5.92). At the preset opacities the Lifted glow is faint but visible and Floating is clear; if Bean wants it stronger, add a dark-only strength multiplier (needs the composers' strict preset color-mix to accept a calc) or raise the opacities. Check by eye on a real dark site after the next deploy (the Mama's canary loads no dark-mode.css, so it needs the injection used here). (4) trust-bar swatch: `scripts/dedupe-shadow-colour-rows.py` (survey, fix, check, self-test, two gates) removed the duplicate shadow colour row from 11 Colour panels (before-after, brand-strip, button, cta-section, info-box, media, quote, team-member, testimonial, trust-bar x2); the Shadow control is the only writer. STILL OPEN: the canary's scratch page 2526 stores `shadow: subtle`; converter fixtures and `design_tokens` shadow rows name the old presets until the reseed. DEPLOY IS BLOCKED (checked): another session has uncommitted `src/blocks/google-reviews/block.json` (about 1,800 lines) and new `src/blocks/google-reviews/components/*.js`, and the current `build/` was built with them, so do NOT deploy that build. When they commit, rebuild (`npx wp-scripts build --experimental-modules --webpack-copy-php`, then `npm run postbuild`, because four unrelated prebuild gates are red) and deploy, then verify the dedupe live in the editor (open a trust-bar, check the Colour panel has no shadow rows and the Shadow control still writes) and check dark mode.

Pre-commit and build traps this stretch: four prebuild gates fail for reasons that are not this track (`dbschema-check_schema_drift`, `check-box-family-guard` on another session's `converter/services/render_emits.py`, `check-doc-citations`, `migrate-orchestrator-rename`), so `npm run build` stops before webpack: run `npx wp-scripts build --experimental-modules --webpack-copy-php` then `npm run postbuild` directly (the `prebuild` generators already ran). The pre-commit F5 gate needs `SGS_F5_SKIP=db-consistency/run.py SGS_F5_SKIP_REASON="..."` while F6 check 8 (`sgs/google-reviews.starColour`, another session) is red, plus a `[gates-ok:...]` token in the message. The Bash tool mangles backslashes and newlines in heredocs again: write scripts with the Write tool. Node needs `.js` extensions on imports in files the `.mjs` tests load.

Then 4c, 4d, 4e, the council, commit 6, the reseed, LEDGER and docs, exactly as below.

Traps found this session: the pre-commit hook chain (F5/F6 db-consistency, cheat gate) takes over a minute and F6 has no baseline and no skip except a `[gates-ok:...]` token for NOT-your-work violations (this one WAS ours, so it was fixed properly); a fix to `attr-classification-overrides.json` only takes effect after `sgs-update-v2.py --stage 1`; the tree must stay clean of your own edits during a deploy; `git pull --rebase` refuses on a dirty tree and stash is banned, so push directly or merge `origin/main`; `wp_style_engine_get_styles` drops `color-mix()`, so shadows with opacity must go through our own scoped rule (4a's header fill is the precedent).

## Bean's instruction for this resume (in this order)

1. Finish 4b properly (below), then do 4c, 4d, 4e.
2. Run a `/qc-council` over the whole U-1 build (4a to 4e), two different models chosen by `/delegate` (one code-path verifier, one adversarial reader), apply its changes.
3. Build and commit commit 6 (item hover paint: `itemOpacity`, `itemOpacityHover`, `panelCardLift`, `itemPaddingShiftHover`; design in `2026-09-21-u1-design.md` section 3 item 6; M-21 cells in section 2).
4. Then run the DB reseed (`python plugins/sgs-blocks/scripts/sgs-update-v2.py`, 20 to 40 minutes, run it in the background and poll its exit), update `.claude/LEDGER.md`, and do the full-scale doc updates (Spec 36, Spec 37, the plan and verify docs, decisions.md entry, U-1 exit cells, `families-master.json` coverage_status for M-09, M-13, M-43, M-21, Spec 19/dev-setup if any command or fixture changed).

## State at the pause (all on origin/main)

| Commit | What | State |
|---|---|---|
| 677148215 (+ report d0bff2eb9) | 1: mega close delay reads `submenuCloseGrace` | done, live-verified |
| 6eb948176 (+ c7f1a8043) | 2: force-solid paints the resting background through the merge | done, live-verified |
| e8c70192c, 3fa812e8d (+ 0aa26468b) | 3, 3b: per-tier header `zIndex`, drawer stacking derives from it | done, live-verified |
| be1be25da (+ 3e221b698) | 5: `submenuIntentDelay`, `submenuOpenOn` (hover or click) | done, live-verified with real pointer input |
| bf2200abd, 60d8905f0 (+ 91fb3d550) | 4a: shared surface ground helper, wrapper hook, header `backdropBlur` renamed `surfaceBlur`, saturate, opacity | done, live-verified (blur, saturate, 86 percent fill) |
| 5abaa8981 | 4b: mega panel `bgBlur` replaced by `surfaceBlur`/`surfaceSaturate`/`surfaceOpacity`; `shadow` (default preset `floating`, empty means none) and `shadowColour` are its single box-shadow writer; roster JSON regenerated | **committed and pushed, NOT deployed, NOT verified live** |

## 4b: DONE

Deployed and verified live (see the mega-panel report). Nothing left except the non-modal drawer half of the F5 check, which waits for 4c: on a blurred header, open the drawer with `modality: non-modal` and check position, viewport and `elementFromPoint`.

## 4c, 4d, 4e (from the commit-4 design note)

- 4c `sgs/nav-drawer`: keep `surfaceBlur` and `surfaceOpacity`; emit both through `sgs_surface_backdrop_decls` / `sgs_surface_fill_alpha` in `nav-drawer/render.php`; add `surfaceSaturate`; add `shadow` and `shadowColour` (one writer); mount `SurfaceGroundControls` and `ShadowControl` in its inspector; canvas preview in `nav-drawer/edit.js`; use `sgs_css_single_length_value` for blur (the council found the drawer's old check accepted a two-value blur).
- 4d `surfaceFadeEdge` on `sgs/site-header` (`none | top | bottom`, a `mask-image` fade of that edge to transparent; one reference, fantasy). Built last, small.
- 4e fan-out through the shared background panel (Bean's ruling): mount `SurfaceGroundControls` inside `container/components/BackgroundPanel.js` when the block declares `surfaceBlur`; add `surfaceBlur` and `surfaceSaturate` (opacity only where the block passes its own fill to `sgs_surface_fill_alpha`) to every wrapper block that mounts the panel (container, header, footer, hero, cta-section, trust-bar, physics-canvas; `multi-button` only if it renders through `SGS_Container_Wrapper`, check first). More than three blocks: ship it as a survey, fix and check script (precedent `scripts/fanout-overlay-sibling-attrs.py`), a canvas preview per block (the `check-editor-render-parity` gate enforces it), and note that `trust-bar` and the wrapper are currently being edited by another session.

## Not done and to tell Bean

- `sgs/mega-panel::borderRadius` (string) has NOT been moved to a tier object: no exit cell needs it and it carries a silent-coercion hazard and a converter DB routing row. Ask Bean whether to do it.
- Halcyon and Indus mega shadows are two-layer; `shadow` composes one layer, so their second layer is approximated (record as a divergence in the clone report).
- The mega panel's default shadow changed from a hardcoded two-layer literal to the `floating` theme preset.

## Facts that cost time this session (do not rediscover)

- The Bash tool halves backslashes in heredocs and mangles `\n` inside them: write patch scripts to the scratchpad with the Write tool, then run them; never put `\n` escapes in a heredoc.
- `wp_style_engine_get_styles` silently DROPS a `color-mix()` value (measured live). A translucent fill must be written as its own scoped rule, as `site-header/render.php` now does.
- The pre-deploy stored-content audit aborts a deploy if stored content still uses a renamed attribute; rebuild the fixture (`python plugins/sgs-blocks/scripts/nav-qa/build-header-fixtures.py --only <slug>`) and re-run.
- Gate failures that are NOT this work and are disclosed in every commit message: `migrate-orchestrator-rename` (scans the gitignored scratch copy `pipeline-state/_p1p2/headtree`), `dbschema-check_schema_drift` (an empty table `should_not_be_writable` left in the shared framework DB), `wp-hooks validate` (3 unknown hooks), and the pre-commit F6 db-consistency `variant_slots` for `sgs/trust-bar` (bypass with `SGS_F5_SKIP=db-consistency/run.py SGS_F5_SKIP_REASON="..."`; the reseed refreshes it). After any block.json attribute change run `python plugins/sgs-blocks/scripts/consistency/build-roster.py` and commit `scripts/consistency/roster.json` and `attr-role-map.json`, or the inspector-scan pre-commit gate blocks.
- The visual-diff commit gate: commit with `SGS_VISUAL_GATE_SKIP=<block>[,<block>]` and a truthful `SGS_VISUAL_GATE_REASON`, then commit the live report straight after. The commit hooks take over a minute: run the commit in the background and confirm with `git log -1`.
- Live testing: sandybrown fixtures are pages built by `plugins/sgs-blocks/scripts/nav-qa/build-header-fixtures.py` (slugs `qa-hdr-*`): `qa-hdr-force-solid` 3790, `qa-hdr-z-index` 3799, `qa-hdr-open-click` 3808, `qa-hdr-open-delay` 3817, `qa-hdr-surface` 3826, pill 3734. Test header #3777 and test drawer #3778 are the active ones on sandybrown; Mama's header #3648 and default drawer #3593 can be re-activated with `wp sgs header set-active` / `wp sgs drawer set-active` (`--user=1`, over `ssh -i ~/.ssh/id_ed25519 -p 65002 u945238940@141.136.39.73`, path `domains/sandybrown-nightingale-600381.hostingersite.com/public_html`).
- Real pointer input is available through the chrome-devtools `hover`, `click` and `press_key` tools; read timings from an in-page MutationObserver, not from tool latency.

First action on resume: read the three docs above, run `git status --short plugins/sgs-blocks`, tell Bean in three lines where 4b stands (deployable or blocked by the other session), and continue.
