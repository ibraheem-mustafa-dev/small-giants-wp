---
doc_type: next-session-prompt
project: small-giants-wp
thread: cloning-pipeline
generated: 2026-06-28
primary_goal: "GATE-THEN-BUILD W3 (the child-block / CSS-on-content / arrays half of the D246 content-UNIFY). W1+W2 are DONE + LANDED: the scalar content path now runs on the MODULARISED working `_lift_scalar_attrs_by_selector` (not the D245 recreation), wired into the live path, D245's from-scratch scalar logic retired, re-LANDED live (quote+name+avatar). BEFORE any W3 build, run TWO `/qc-council` gates (Bean's explicit instruction): (1) on the W1+W2 BUILT code — did the modularisation add any cheats / break any rule (silent content drop? thin-wrapper behaviour drift? dead-path still reachable?); (2) on the W3 functions + PLAN — repurposing `run_mechanism_b` + the `convert.py` single-recursive walker / `_route_composite_interior` (FR-22-3, NOT yet read) + `_lift_styling_attrs_by_selector` (B2) + arrays (B4, FR-22-2.5, PARTIAL even in convert.py) + deleting the now-dead `content_attrs_with_selector` — validate it MODULARISES the working walker, does NOT recreate it, and fits Spec 31 §1's ONE dispatch. Only after BOTH councils GO + Bean sign-off, build W3 starting with the walker read-and-port. convert.py stays byte-identical (D-MODULAR). D-ceiling D246."
---

# Next session — GATE W1+W2 + the W3 plan with `/qc-council`, THEN build W3 (D246 content-UNIFY, child-block half)

You are the rebuild orchestrator (always Opus). You plan, delegate to subagents, QC, live-verify, commit; subagents implement assigned files only. The cloning pipeline must CONVERT any SGS-BEM draft into NATIVE SGS blocks driven by attributes, faithful on the real homepage, with zero cheats and zero silent content drops.

## ⚠️ WHERE WE ARE (plain English, 2026-06-28, D246) — read before anything
D246 corrected an architecture mistake: D245 built content as a SEPARATE engine that RECREATED two working `convert.py` functions; Spec 31 §1 "ONE lift path" says content + CSS are ONE dispatch, so the rebuild must MODULARISE the working functions, not rebuild them. **This session executed the first half of that correction and it is DONE + VERIFIED + LANDED:**
- **§3 unified** — Spec 31 §3 rewritten as the ONE content+CSS routing algorithm (`661d3357`).
- **W1 (`a4c0de86`)** — modularised the working scalar-content lift (`_lift_scalar_attrs_by_selector` → `converter/resolvers/scalar_content.py:lift_scalar_content`; 6 helpers → `converter/services/lift_helpers.py`), QC'd behaviour-identical, no convert.py import, no slug literals.
- **W2 (`57209f48`)** — wired it into the live path (`run_mechanism_a` is now a thin wrapper over `lift_scalar_content`), retired D245's from-scratch scalar logic (`content_attrs_with_selector` now unused), RE-LANDED live (quote+name+avatar render via the modularised path). 318 tests + gates green; convert.py byte-identical.

**The scalar path is corrected + proven. W3 is the larger, qualitatively-different remainder — and Bean banked deliberately so the walker-port gets fresh care.**

## ✅ BOTH `/qc-council` GATES RUN (2026-06-28) — results below (Tasks 1+2 DONE; fix the registers then build W3)
The two gates Bean asked for were RUN this session (6 raters, cross-model, all findings fact-checked against the live code + DB per STOP-15). No `sourceMode='bound'` / echo-`$content` / hardcoded-`!important` cheat exists anywhere — the no-cheat floor HOLDS. But real must-fixes surfaced:

### Register A — W1+W2 BUILT-CODE must-fixes (fix BEFORE W3; small)
- **A1 — `media_map={}` silent wrong-URL regression (HIGH).** `extraction.py:96` hardcodes `media_map={}`; the new engine has NO media-map loader. DB-confirmed reachable: `sgs/testimonial` (avatarMedia/orgLogo/workMedia) + `sgs/team-member` (memberMedia/photo) are `image-object`+`scalar-content-lift`, so they lift via `scalar_media_from_img(img,{})` → `resolve_media_url` returns the RAW mockup `src` (404s live) instead of the WP URL the frozen path wrote. It LANDS an `<img>` (id=0 renders from url) so it passed the W2 smoke — but with the WRONG url (Rule 4 silent + Rule 5 WRITTEN≠LANDED-correctly). FIX: thread the run's real media-map through `extract_content`→`run_mechanism_a`→`lift_scalar_content`; OR (until a media-map loader wave lands) emit a tracked `ContentGap` for every image-object lift so it is NOT silent.
- **A2 — silent-drop hole for content attrs (HIGH, Rule 4).** `lift_scalar_content` emits NO key AND NO gap on 3 branches for an attr that HAS role+selector: selector matched nothing (scalar_content.py:121-122), `rich_text_content` empty (124-127), no `<img>` found (132-134). `expected_content_gaps` explicitly skips any attr with a role OR selector (extraction.py:192-193), and the F2 ledger tracks CSS declarations not content nodes — so these drops are invisible to every gate (false-green F5). NOTE: this matches the ORIGINAL working function's no-op-on-absent behaviour (so it is faithful, not a port-regression) — but the W2 commit's claim that "the F2 ledger covers completeness" is WRONG for content. FIX (Bean decision): either re-add a per-attr `ContentGap` for role+selector attrs that fail to lift (the coverage the D245 path had), OR build a real content-completeness channel into the F2/draft-derived ledger. Until one lands, scalar content completeness is UNGATED.
- **A3 — false-green test (MEDIUM, STOP-16/23).** `test_extraction.py` monkeypatches `content_attrs_with_selector` — a function `run_mechanism_a` no longer calls → the test passes vacuously. FIX: patch `db_lookup.block_attrs` + `capabilities_for` instead. (Remove this monkeypatch BEFORE deleting the dead accessor in W3 — see B-order.)
- **CLEAN:** the `_CONTENT_NAME_RE`/`_STYLING_RE` regexes (GAP-only, not routing) + the rich-text/href frozensets (sanitisation) are legitimate named-constant exceptions; the `scalar-content-lift` capability gate is correct DB-driven opt-in (no carve-out); the emit path writes render-read attr keys (no input≠output trap). W1's 6 helper ports are line-faithful.

### Register B — W3 PLAN must-fixes (GO, conditioned — the plan correctly REPLACES a recreation with a faithful port; D246 architectural-completeness pre-question PASSES)
The current `run_mechanism_b` IS the D245 from-scratch recreation (flat, no recursion, uses `extract_payload` not the working `rich_text_content`, no scalar-media/fold branches) — W3 is RIGHT to replace it with a faithful port of the working `_route_composite_interior`. Conditions:
- **B1 — port the FULL walker, not a thinned one (HIGH, Rule 4/6).** `_route_composite_interior` (convert.py:4124) does THREE things the current B drops: (i) scalar-media column lift → `{attr}`/`{attr}Mobile` (the hero `split` `splitImage`/`splitImageMobile` case); (ii) slug-None content-column FOLD + cross-node CSS routing (`_route_interior_css_to_parent_slot` — the hero `__content` 0%-transfer fix). Port a thinned walker → hero split image + content CSS EVAPORATE (D212/MF-1 shape repeats). Port all three branches, DB-keyed.
- **B2 — `_lift_styling_attrs_by_selector` (convert.py:3903) discards `_bp_decls` (line 4025) → responsive typography/colour silently dropped.** Porting verbatim ships the base-only drop (an inherited bug to FIX, not preserve). The port must consume `_bp_decls` into `{attr}Tablet`/`{attr}Mobile`, AND actually wire the styling-lift into the dispatch (it isn't called by Mechanism A today). Its 5 helpers (`_collect_css_decls_for_element`/`_extract_token_or_hex`/`_split_value_unit`/`_css_value_to_attr`/`_css_selector_has_class`) are NOT yet in the new tree.
- **B3 — arrays (FR-22-2.5): port behaviour-identical with TODO markers; do NOT complete FR-22-2.5 in W3 (STOP-18).** convert.py's array path uses per-block slug literals (`sgs/quote`/`sgs/icon-list`/`sgs/option-picker` — R-22-1 violations); `array_item_slot_for` exists but is never wired. Completing it is scope-creep needing its own design-gate. Port-as-is + `# TODO FR-22-2.5` so no NEW R-22-1 violation, incompleteness documented not silent.
- **B4 — `build_block_markup` ambiguous-attr fallback (MEDIUM).** `emit_block_markup(cb.slug, {}, cb.content)` (extraction.py:292) when `primary_content_attr` is None (ambiguous multi-attr) dumps bare inner-HTML a typed render.php may ignore → latent silent drop. Emit a tracked `ContentGap` (loud) when ambiguous instead.
- **B-order — delete `content_attrs_with_selector` LAST.** Safe order: (1) port styling 5-helpers; (2) port `_lift_styling_attrs_by_selector` + extend for bp_decls + wire to dispatch; (3) port the full walker into `run_mechanism_b`; (4) port arrays as-is + TODO; (5) remove the dead test monkeypatch (A3); (6) THEN delete `content_attrs_with_selector` (grep-confirm 0 readers). Deleting it before the monkeypatch cleanup breaks the suite. `payload.py` + `content_select.py` are clean pure helpers — safe to carry, BUT do NOT keep `content_children` as the walker if it diverges from `_route_composite_interior` (the D246 trap).

**Your job this session: fix Register A (small, before W3) → build W3 honouring Register B (only on Bean sign-off) → continue the post-W3 roadmap below.**

## ⛔⛔ READ THIS FIRST — MANDATORY READING GATE (Bean directive: verify against ground truth, never guess; read WHOLE docs/files, not greps)
Tick each in your first message before any council or build action:
1. ☐ **`.claude/handoff.md` (2026-06-28)** — the W1/W2 done-state + the W3 4-piece breakdown + Bean's two-`/qc-council` instruction. This is the operative summary.
2. ☐ **The W1+W2 BUILT code (the subject of gate 1):** `converter/services/lift_helpers.py` (the 6 ported helpers) + `converter/resolvers/scalar_content.py` (`lift_scalar_content` — the modularised `_lift_scalar_attrs_by_selector`; note the `resolve()` stub + the line-12 note that the unified-dispatch WIRING is a separate follow-on) + `converter/services/extraction.py` (`run_mechanism_a` thin wrapper; the retired from-scratch path in the diff of `57209f48`).
3. ☐ **`.claude/specs/31-UNIVERSAL-CONTAINER-CSS-TRANSFER.md`** — §1 "ONE lift path" (the COMPLETE function inventory — read in FULL, it names which functions already work incl. the content + walker functions) + §3 (the JUST-rewritten unified content+CSS routing) + §2 Axis-3 (child-routing: parent_block/accepts_allowed_blocks/standalone_block) + §12.0/§12.6 (modularise into per-resolver files behind ONE dispatch).
4. ☐ **The W3 port-SOURCE in `convert.py` (read-to-port is SANCTIONED for working functions — STOP-22 carve-out):** `_route_composite_interior` + the single-recursive walker (FR-22-3) + `_lift_styling_attrs_by_selector` (B2) + its 5-helper closure + the array path (B4, FR-22-2.5 — note it's PARTIAL even here). You have NOT read the walker yet — this is the meaty read.
5. ☐ **`.claude/decisions.md` (head → D246)** — D246 = the architecture correction; D245 = the superseded separate engine (kept as history); D244 = recognition (correct + unaffected); D229 = D-MODULAR (convert.py frozen). Verify D-ceiling: `grep -oE 'D[0-9]+' .claude/decisions.md | sort -V | tail -1` → D246.
6. ☐ **`.claude/specs/22-...`** — FR-22-2/2.1/2.2 (child-block-vs-scalar fork) + FR-22-3 (the single-recursive walker — the W3 architecture) + §FR-22-5.3 (the SLOT-KEYED predicate `slot_has_equivalent_block`, NOT `equivalent_block_for(block,slot)` — the v1 "fatal catch") + R-22-* binding rules.
7. ☐ **`.claude/state.md` + `.claude/parking.md`** + the foundation modules (`ledger/` F2, `oracle/` F3, `cheat-gate/`, `excluded-gate/`, `coverage-matrix/`, `db-consistency/` F6) + `orchestrator/converter_v2/db_lookup.py` (the vetted accessor).
8. ☐ `pipeline-state/<latest-run>/{leftover-buckets,extract,trace}.json` — raw artefacts before ANY converter-quality conjecture.

## ⛔ THE 7 NON-NEGOTIABLE RULES (Bean-set; gate every change AND every council)
1. **CONVERT, don't mirror** — output = native SGS blocks driven by attributes; NOT a div-by-div copy of draft classes/DOM.
2. **NO CHEATS** — no `sourceMode='bound'` emit, no echo-`$content` passthrough, no hardcoded `!important`/default overriding faithful draft CSS. Only the live WC configurator `wc-product`/`sgs-cpt` is legitimate.
3. **UNIVERSAL, no carve-outs** — a fix applies to every qualifying block/case; no per-block/per-slot exception. Over-broad universality is ALSO a break.
4. **NO SKIPPING** — every draft content node + CSS declaration transfers, OR is EXCLUDED-with-reason, OR is a tracked GAP (ContentGap visible to F5). Zero silent drops.
5. **VERIFY ON THE REAL HOMEPAGE** — live computed-style/innerText + draft-vs-clone. Emit-green ≠ rendered. WRITTEN (attr set) ≠ LANDED (renders on the page).
6. **RESPONSIVE VALUES IN BLOCK ATTRIBUTES, never inline CSS.**
7. **DESIGN-GATE sensitive/high-blast-radius changes** (shared wrapper, walker, converter, seeding, ledger, oracle, gates, EACH rebuild stage) via `/adversarial-council` or `/qc-council` + Bean approval BEFORE building. The W3 walker-port is EXACTLY this — a new stage's port needs its own design-gate (this session's gate-2).

## First action (~5 min, zero deps)
Reading gate (above) + pre-flight ritual. Re-confirm ground truth: `git branch --show-current` (→ main), `git status` (lucide/phase4/theme-handoff dirty files are NOT yours), `grep -oE 'D[0-9]+' .claude/decisions.md | sort -V | tail -1` (→ D246), `git log --oneline -4` (W2 `57209f48` is the thread HEAD). Then read the W1+W2 built code (item 2) + Spec 31 §1/§3 in FULL — so you can run gate-1 from ground truth, not from this prompt's summary.

## Tasks

### Task 1 — `/qc-council` on the W1+W2 BUILT code (cheats / rule-breaks) — Bean's gate 1
**What:** Multi-rater review of the SHIPPED `a4c0de86` + `57209f48` (lift_helpers.py + scalar_content.py + extraction.py diff) against the 7 rules + R-22-* + the STOP catalogue. The council MUST be asked the concrete failure questions, not "is it good":
- Does the modularised `lift_scalar_content` drop any draft content the original `_lift_scalar_attrs_by_selector` lifted? (behaviour-identical port, or silent regression? — Rule 4.)
- Is `run_mechanism_a`'s thin-wrapper completeness (F2 ledger + `expected_content_gaps`) actually equivalent to the retired per-attr `ContentGap` coverage, or did retiring the from-scratch path open a silent-drop hole? (STOP-17 — coverage join on full unit identity.)
- Is any of the dead from-scratch scalar path (`content_attrs_with_selector` + bespoke object-shaping) still reachable / half-wired? (dead-but-live cheat.)
- Any slug literal / hardcoded dict / convert.py import that crept in? (R-22-1, R-22-2.)
- Input-class ≠ output-class trap (draft `__author` → render `__name`) — does render.php read the attr the modularised lift writes? (STOP-23, WRITTEN≠LANDED.)
**Why:** STOP-23 — a pre-commit qc-council on BUILT converter code catches what self-tests miss (blub 255). W1/W2 were QC'd for behaviour-identity but not adversarially council'd as built code.
**Orchestration:** `/qc-council` on the built code → FACT-CHECK every council finding against ground truth (the live code file:line + the DB + a draft-vs-clone re-LAND if a drop is alleged — STOP-15: a council finding is a HYPOTHESIS). Any TRUE finding → fix + re-verify + path-scoped commit BEFORE W3. **/qc gate:** council findings all resolved (fixed or fact-checked-false-with-evidence).

### Task 2 — `/qc-council` on the W3 functions + PLAN (before building) — Bean's gate 2
**What:** Review the W3 plan + the functions it will repurpose, to catch cheats / rule-breaks in the REPURPOSING before a line is built. W3's four pieces: (a) the child-block path `run_mechanism_b` ← port the `convert.py` single-recursive walker / `_route_composite_interior` (FR-22-3); (b) `_lift_styling_attrs_by_selector` (B2) + its 5-helper closure; (c) arrays (B4, FR-22-2.5 — PARTIAL even in convert.py); (d) delete the dead `content_attrs_with_selector`. The council MUST be asked the D246 architectural-completeness pre-question (MANDATORY on every rebuild-stage design): **"Does this W3 plan RECREATE something Spec 31 §1's one-dispatch inventory already names as working (a new module / column / mechanism for what a §1 function already provides)? PASS = re-houses/modularises the working walker; FAIL = recreates it."** A STOP-25 FAIL from any rater = NO-GO. Also gate: does the array path's PARTIAL convert.py state mean W3 ports a known-incomplete function (decide by fixture-set evidence, STOP-18, not habit)? Is the `content_attrs_with_selector` deletion provably safe (grep every reader first — STOP-11, schema≠usage)?
**Why:** The walker-port is the highest-blast-radius piece of the whole content-UNIFY (Rule 7 design-gate). Gating the PLAN before the build is the cheapest place to catch a recreation.
**Orchestration:** `/qc-council` (or `/adversarial-council` for the design-gate) on the W3 plan → FACT-CHECK findings against Spec 31 §1 + FR-22-3 + the live DB + the actual convert.py walker (read it first — item 4). Produce a GO/NO-GO + a must-fix register. **Depends on:** Task 1 clean. **/qc gate:** council GO + **Bean sign-off** before any W3 code.

### Task 3 — Build W3 (modularise the walker) + LANDED — only on Task-1-clean + Task-2-GO + Bean sign-off
**What:** Implement the gated W3 plan: port the `convert.py` walker/`_route_composite_interior` faithfully into `run_mechanism_b` in the one dispatch; modularise `_lift_styling_attrs_by_selector` + its 5 helpers; handle arrays per the Task-2 decision; remove the dead `content_attrs_with_selector`. Gate per Spec 31 §12.6 step-3 ritual (ledger+oracle on the multi-shape fixture set + draft-vs-clone LANDED + Bean sign-off). Continue stage-by-stage toward §8's convert.py decommission trigger.
**Orchestration:** read-to-port the walker FIRST (sanctioned, STOP-22 carve-out) → `/subagent-driven-development` → re-run gates from canonical cwd (STOP-16) → `/qc-council` on the BUILT code (STOP-23) → deploy → live LANDED (STOP-21 recipe) → Bean sign-off → path-scoped commit per increment. **Depends on:** Task 2 GO.

## Dependency graph
```
READING GATE (whole Spec 31 §1/§3 + the W1/W2 built code + the convert.py walker, holistic) + ritual
  → Task 1 (/qc-council on W1+W2 BUILT code) → fact-check findings → fix any TRUE → commit
      → Task 2 (/qc-council on the W3 plan + functions) → fact-check vs Spec 31 §1/FR-22-3/DB/walker → GO/NO-GO + Bean sign-off
          → Task 3 (build W3 — port the walker; SDD → gates STOP-16 → /qc-council on BUILT → deploy → LANDED → sign-off)
              → continue stage-by-stage to convert.py decommission (Spec 31 §8)
                → commit path-scoped per increment (D-ceiling check before any new D → D246)
```

## POST-W3 ROADMAP — the rest of the D246 content-UNIFY (so nothing is dropped)
After W3 lands + LANDS, the D246 content-UNIFY continues stage-by-stage (Spec 31 §12.6 ritual) — it is ONE continuous programme, not separate projects:
1. **Generalise the mechanisms to every content block** — testimonial's other slots (name/avatar/rating/date/org), `sgs/team-member` (Mechanism A), then the other `has_inner_blocks=1` composites (cta-section, info-box, card-grid…) via Mechanism B, each with its `deprecated.js` where the save-shape changes. Each earns its OWN draft-vs-clone LANDED proof (A14, never bank from a prior slot). Seed unmapped scalar slots via `/sgs-update`.
2. **WIRE the modularised resolvers into the UNIFIED dispatch table** (currently `scalar_content.py:156` `resolve()` is a `GapOrigin.UNIMPLEMENTED_STUB` — the dispatch-table integration is the explicitly-noted "SEPARATE follow-on wave"). Seat `lift_scalar_content` + the W3 walker + the styling-lift as resolvers in the `(block, layer, property/role, tier) → resolver` table NEXT TO `outer_box`/`grid` — this is what makes content + CSS literally ONE dispatch (Spec 31 §3). Until this, `run_mechanism_a/b` + `extract_content` are still a parallel glue layer to retire.
3. **Finish Spec 31 §3 as the fully-unified content+CSS routing** — fold the Axis-3 child-routing (parent_block → slots.standalone_block → accepts_allowed_blocks) into the same §3; absorb Spec 22's working content logic (§FR-22-2/2.5) so Spec 22 can be archived.
4. **Full multi-shape fixture-set ledger+oracle gate** (Spec 31 §12.0 universal) — zero UNACCOUNTED + zero WRITTEN-not-LANDED across all block-shapes + all stages.
5. **convert.py decommission (Spec 31 §8 trigger)** — DELETE the frozen convert.py once the fixture set is fully TRANSFER-and-LAND green. This is the final proof the rebuild is complete.

**Parallel / parked (NOT blocking D246 closure — separate threads in `parking.md`):** P-CLONE-FIDELITY-FULL-ALIGNMENT (Bean P-1: HTML diff clone-vs-draft, 8 defect families) · P-DOC-ALIGNMENT-12-DOCS (drift sweep) · P-BLOCKJSON-SELECTOR-AUTOSEED (move hardcoded selector dicts to block.json + `/sgs-update`) · P-SPEC22-DESIGN-GATE-PHASES (array→child wiring, draft breakpoints, D1 layout sidecar).

## Methodology guardrails / STOP catalogue (carried forward + extended — do NOT subtract)
- **STOP-1 — READ before you conjecture.** Verify every claim (yours, a subagent's, a doc's, a metric's) against ground truth — live code (file:line), the DB, decisions.md, the raw run artefacts. The reading gate is non-skippable. (blub 353.)
- **STOP-2 — Subagents RETURN data / implement assigned files; NEVER write shared files.** Cold prompts say "do NOT write/move/create any shared doc." Opus orchestrates all shared-file writes; commit valuable artefacts BEFORE dispatching. (`feedback_subagents_must_not_write_shared_files`.)
- **STOP-3 — The ledger/oracle input is the DRAFT/rendered-clone, NOT the converter's recognised set.** (Spec 31 §12.2.1.)
- **STOP-4 — WRITTEN ≠ LANDED.** "An attr was emitted" is a progress signal, never a gate. (Spec 31 §12.2.2.)
- **STOP-5 — Stage-by-stage is the BUILD ORDER; the ledger+oracle is the cross-stage TEST.** (Spec 31 §12.1/§12.6.)
- **STOP-6 — A gate that EXISTS but isn't WIRED-TO-SOMETHING-THAT-RUNS protects nothing.** The F5 gates run on every CC `git commit` (`.claude/hooks/f5-commit-gate.py` `_GATES`). `content_gap_check` is wired there. Before claiming "enforced", grep the wiring + confirm it RUNS.
- **STOP-7 — Hardcoded wrapper/converter defaults are CHEATS to remove, not blockers** (R-22-1).
- **STOP-8 — Device-tier ≠ arbitrary visual breakpoints.** Device-tier = {767/768, 1023/1024, 1024}; a single-rule visual breakpoint (600/640/781) is legitimate + must NOT be snapped to a tier.
- **STOP-9 — Composites are NEVER a separate system; variant grids are DB-defined** (`variant_slots` + `blocks.variant_attr`; query, don't guess).
- **STOP-10 — Empty cloned section = usually a cv2 soft-fail, NOT a recognition miss.** Read extract.json `status` + trace.jsonl FIRST; gate on `innerText.length>0`. A content-gated block (testimonial) renders EMPTY without content BY DESIGN — Stage 3 is what fills it.
- **STOP-11 — SCHEMA enumeration ≠ USAGE enumeration.** Knowing an attr/column/function EXISTS is necessary but NOT sufficient before reusing/renaming/retiring; grep the real signature + how it's WRITTEN and READ first. (The `content_attrs_with_selector` deletion in W3 needs this — grep every reader before removing.)
- **STOP-12 — A cross-environment verifier must be apples-to-apples; reusing infra ≠ inheriting its assumptions** (D233). The LANDED probe renders the new-engine output in the SAME live WP env it measures.
- **STOP-13 — A cross-FAMILY rater catches what same-family review misses; if its tooling is broken, stand in with a structured branch trace** (D234; Gemini cross-family path is tool-blocked in the Windows harness).
- **STOP-14 — Before ARMING any gate, run it in report mode against CURRENT output + baseline today's legacy violations (key by identity, not line); fail only on NEW** (D236).
- **STOP-15 — Validate routing/variant claims against pipeline-PRODUCIBLE inputs, not synthetic dicts; run an adversarial-council AFTER a qc-council; then FACT-CHECK the council against ground truth** (D237/D242 — a council finding is a HYPOTHESIS). **Applies to BOTH councils this session: fact-check every W1/W2 and W3 finding against the live code/DB/draft before acting.**
- **STOP-16 — A subagent's "N tests pass / gate green" is a HYPOTHESIS.** Re-run the suite + the gate's `--check` YOURSELF from the project's CANONICAL cwd (`plugins/sgs-blocks/scripts`, `--import-mode=importlib`); prove the FAILURE path (plant a violation → exit 1); inspect the committed baseline for stale plants.
- **STOP-17 — A coverage/no-drop/diff gate's set-difference join must key BOTH sides on the FULL unit identity incl. responsive tier/media; red-team a BUILT gate before trusting it** (D240). (Gate-1 must check the W2 thin-wrapper completeness didn't open a join hole.)
- **STOP-18 — Don't defer residuals out of habit; fact-check each against ground truth first** (D241). Label each DONE-NOW / DISMISSED-with-evidence / DEFERRED-with-cited-blocker. (Arrays B4/FR-22-2.5 is PARTIAL even in convert.py — decide by fixture-set evidence in gate-2, not habit.)
- **STOP-19 — A path-scoped `git commit -- $(...)` can DROP the source-path deletion of a `git mv`.** After any rename commit, verify `git ls-tree -r HEAD --name-only | grep <oldpath>`; commit by an explicit path list naming BOTH sides of every rename. (blub.db 364.)
- **STOP-20 — Restructure a multi-file rebuild as a VERTICAL SLICE (one real output LANDED), not a horizontal scaffold of empty stubs; make LANDED the headline signal; DOUBLE-VERIFY a design before build then FACT-CHECK the verifiers** (D242).
- **STOP-21 — A new-engine resolver is only LANDED-proven by deploying its GENUINE output to a live page + computed-style/innerText + verdict — NOT by new-vs-frozen attr equivalence** (D243). Recipe: build markup via the engine → `orchestrator.emit_block_markup()` → REST-create a FRESH canary page (guard-safe; the wp-content-guard blocks post_content REWRITES, not REST page CREATE) → anonymous Chrome-DevTools/Playwright `getComputedStyle`/innerText (no admin bar) → require the OUTPUT marker + non-default. Delete the test page + 404-confirm. Creds: `.claude/secrets/sandybrown.env` (grep/cut, never `source`).
- **STOP-22 — The frozen `convert.py` is NEVER the design reference or the fact-check oracle for a rebuild stage — WITH ONE CARVE-OUT (D246).** The authority is Spec 31/22 + the modern DB tables + the draft. `convert.py` is consulted for exactly two purposes: (1) to NAME the bug being killed; (2) to **READ-TO-PORT a function that Spec 31 §1's "ONE lift path" inventory names as WORKING + in-scope for modularisation.** The W3 walker (`_route_composite_interior` / FR-22-3 single-recursive walker), `_lift_styling_attrs_by_selector`, and the working scalar functions ARE the port source — reading them in full to modularise faithfully is MANDATORY before designing the replacement, not forbidden. Tell council/qc raters to fact-check against Spec+DB+draft, never the BROKEN engine logic — but the working functions ARE the port source. (`feedback_rebuild_stage_authority_is_spec_and_db.md`, `do-not-recreate-working-functionality-modularise-it`.)
- **STOP-23 — Run a pre-commit `/qc-council` on the BUILT converter code, not just the design.** Self-tests exercise the paths you thought of; the multi-model qc exercises the ones you didn't (blub 255). Input class ≠ output class is a live trap (draft `__author` → render `__name`): verify render.php reads the attr you write AND paints the element you check (WRITTEN-not-LANDED). **This session's Task 1 IS a STOP-23 gate on W1/W2.**
- **STOP-24 (D245) — A DB change to a column that `/sgs-update` assign-canonical RE-DERIVES (role/canonical_slot/derived_selector) must use the reseed-surviving `ATTR_CLASSIFICATION_OVERRIDES` channel (sgs-update-v2.py), not a bare migration** (a migration writes the row but the next reseed overwrites it). Pair the override (reproducible source) with a dated migration (immediate effect).
- **STOP-25 (D246) — A rebuild's "fresh/modular" = RE-HOUSE the existing WORKING logic into smaller files behind one dispatch; NEVER recreate it with new logic + new DB columns.** Before building, READ THE WHOLE spec/file holistically (not greps), enumerate what ALREADY exists+works (Spec 31 §1 lists the working lift functions incl. content + the walker), and PORT working code rather than re-derive it. A council "X is missing from the spec" finding is a HYPOTHESIS about the spec TEXT — ask "is X actually missing, or elsewhere in the same system I haven't unified?" before adding a parallel mechanism. STOP-22 guards against importing BROKEN assumptions — it does NOT forbid modularising the parts that WORK. (D245's exact failure: a separate content engine recreating the working functions — `do-not-recreate-working-functionality-modularise-it`.)
- **STOP-26 (D246) — Before designing ANY rebuild stage, read the WHOLE target spec holistically (not greps), and state Spec 31 §1's COMPLETE function inventory in plain English BEFORE the first design sketch.** §1 "ONE lift path" must be read in full, first — it is the SYSTEM MAP naming what already exists + works (incl. the walker + content functions) vs what is genuinely absent. A council "X is missing" finding is a HYPOTHESIS about the spec TEXT — verify against the §1 inventory BEFORE adding any parallel mechanism. (D245 built a separate content engine because a grep-section read missed §1's listing of the working functions.)
- **Commit discipline:** path-scoped (`git commit -m "msg" -- <paths>`, `-m` before `--`; pre-existing lucide-icons/phase4/theme-handoff dirty files are NOT yours; exclude `__pycache__`). Bare commits are blocked by the path-scoped-commit gate. Subagents NEVER `git checkout/restore/stash/reset/clean/mv` the shared tree. **Bean's "are you sure?"/"why?" = a mandate to GROUND in ground truth, not reassure.**
- **Generalisation discipline (carried):** the mechanism is ALREADY universal — a new slot/block is usually a PROOF (richer fixture + LANDED), not new code; A14 — each earns its own draft-vs-clone LANDED, never bank from a prior slot. A new attr-SHAPE / a new STAGE (the W3 walker) is the exception — it needs its own design-gate. DB changes via the override channel + dated migration + full reseed; run BOTH conformance suites; de-hardcode counts to `/sgs-db` pointers.

## Pre-flight self-attestation ritual (answer in your first message)
1. Have I completed the MANDATORY READING GATE — handoff.md + the W1/W2 BUILT code (lift_helpers.py + scalar_content.py + the extraction.py diff) + Spec 31 §1 (the FULL function inventory) + §3 (the unified routing) + §2 Axis-3 + the convert.py walker (`_route_composite_interior` / FR-22-3) + decisions.md D246/D245/D244 + Spec 22 (FR-22-2/2.1/2.2 + FR-22-3 + §FR-22-5.3) + state.md + parking.md + the foundation modules? (Quote one specific thing — e.g. the `run_mechanism_a` thin-wrapper body, or a §1 inventory line naming the walker — to prove it.)
2. What branch + D-ceiling? (`git branch --show-current` → main; D-ceiling → D246.) Anything uncommitted that's MINE? (lucide/phase4/theme-handoff are NOT mine.)
3. Am I running the TWO GATES BEFORE any W3 build (Task 1 on W1+W2 built code, Task 2 on the W3 plan), and FACT-CHECKING every council finding against ground truth (STOP-15) before acting on it?
4. For any subagent: did I tell it "implement only your assigned files / RETURN findings; do NOT write shared docs or touch the shared git tree"? Am I verifying its test/gate claims myself from the canonical cwd + proving the FAILURE path (STOP-16) + running a pre-commit `/qc-council` on the BUILT code (STOP-23)?
5. For W3, am I PORTING the working walker faithfully (read-to-port sanctioned, STOP-22 carve-out) — NOT recreating it (STOP-25) — and is the council asked the D246 architectural-completeness pre-question? Will I gate on draft-vs-clone LANDED (computed-style/innerText, input-class≠output-class checked, STOP-21/23) + Bean sign-off — not emit-green, not a passing self-test alone (Rules 4/5, A14)?

## Skills to Invoke
| Skill | When |
|-------|------|
| `/qc-council` | **Gate 1 (W1+W2 built code) + Gate 2 (W3 plan) — this session's headline tasks**; pre-commit qc-council on any W3 BUILT increment (STOP-23, blub 255). FACT-CHECK the council against ground truth (STOP-15). **MANDATORY architectural-completeness pre-question on the W3 design (D246): "Does this RECREATE something Spec 31 §1's one-dispatch inventory already names as working? PASS = re-houses/modularises the walker; FAIL = recreates it." A STOP-25 FAIL from any rater = NO-GO.** |
| `/adversarial-council` | the W3 walker-port design-gate (Rule 7) — a new STAGE needs its own gate |
| `/brainstorming` | the W3 walker-port design BEFORE build |
| `/strategic-plan` + `/phase-planner` | sequence the W3 build increments |
| `/gap-analysis` | grade the W3 design/output before applying |
| `/lifecycle` | before any skill/agent/pipeline change |
| `/research` (+ `/library-docs`) | any unfamiliar mechanism; don't guess |
| `/subagent-driven-development` · `/dispatching-parallel-agents` · `/subagent-prompt` | orchestrate per-increment W3 implementation |
| `/systematic-debugging` · `/verify-loop` | root-cause gate + 2-attestation per load-bearing claim |
| `/qc-inline` | quick inline QC of a small increment |
| `/sgs-clone` · `/sgs-db` · `/wp-blocks` · `/sgs-wp-engine` · `/sgs-update` | run the pipeline / verify DB + block ground truth (esp. `slot_has_equivalent_block`, `parent_block`/`accepts_allowed_blocks`, the override channel) |
| `/capture-lesson` · `/handoff` | new rules / session close |

## MCP Servers & Tools
| Tool | For |
|------|-----|
| Chrome-DevTools / Playwright | the draft-vs-clone LANDED check — live computed-style/innerText on the deployed canary (STOP-21 recipe) — creds `.claude/secrets/sandybrown.env` (grep/cut, never `source`) |
| `/sgs-db` (`python ~/.claude/skills/sgs-wp-engine/scripts/sgs-db.py`) | verify any DB/table/count claim — esp. `block_attributes` (role/derived_selector/attr_type), `block_capabilities`, `block_composition` (has_inner_blocks), `slots` (standalone_block), `variant_slots` |
| `/wp-blocks` (`python ~/.claude/hooks/wp-blocks.py dump`) | verify a block/attr/schema claim before calling it missing |

## Agents to Delegate To
| Agent | When |
|-------|------|
| `wp-sgs-developer` | heavy SGS build — resolver files, services, render.php, migrations (W3) |
| general-purpose (sonnet) | per-file implementers (under `/subagent-driven-development`) — implement assigned files, no shared-tree writes |
| general-purpose (opus) | cross-model adversarial/correctness review of a BUILT increment before commit (STOP-23) |
| `code-reviewer` | spec + quality review per SDD task |

## Guardrails
The W3 walker-port is a NEW STAGE → its own `/brainstorming` → design-gate (Rule 7) BEFORE build. Build by PORTING the working `convert.py` walker faithfully (read-to-port sanctioned, STOP-22 carve-out) — NEVER recreating it (STOP-25); convert.py stays FROZEN (D-MODULAR) as the port source only, never a comparison oracle. Each increment earns its OWN draft-vs-clone LANDED proof (A14). Run a pre-commit `/qc-council` on the BUILT code (STOP-23). DB changes that touch reseed-derived columns use the `ATTR_CLASSIFICATION_OVERRIDES` channel + a dated migration (STOP-24). Deploy/measure via the STOP-21 recipe; input class ≠ output class. Path-scoped commits (`-m` before `--`, exclude `__pycache__`); verify HEAD after any rename (STOP-19); D-ceiling check before any new D (→ D246).
