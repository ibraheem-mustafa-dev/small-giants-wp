---
doc_type: next-session-prompt
project: small-giants-wp
thread: sgs-theme
session_tag: small-giants-wp-2026-06-12-theme-spec30-p2-shop-layer-COMPLETE-D214-next-step9-schema
generated: 2026-06-12
primary_goal: "SGS-THEME THREAD. Spec 30 P2 SHOP LAYER COMPLETE + merged to main (141ddd71). FR-30-3 archive (D213) + FR-30-5 product search + FR-30-6 searchable filter all live-verified on canary /shop/ (QA Gate B filter + Gate C search 7/7). This session also: sgs/product-search displayMode inline+icon variants (icon = native <details>, no-JS-safe); 3 opt-in header search patterns (search kept OUT of the framework default header); 5 architecture docs synced to D191-D214. NEXT = Step 9 FR-30-9 schema (net-new Organization+WebSite JSON-LD + cart/checkout/account noindex + returnPolicyCountry + drop FAQPage; design-gate FIRST per the draft-leak history) → Step 10 parked follow-ups → Step 11 go-live → Step 12 phase close. Opus orchestrator, sonnet dispatch, /qc-council + /adversarial-council before any schema/SGS-block commit, plain English to Bean."
---

# Next Session — SGS THEME thread — Spec 30 P2: shop layer DONE → execute Steps 9–12 (schema + go-live + close)

> ## SESSION CLOSE (2026-06-12, theme thread — READ FIRST) — SHOP LAYER COMPLETE + MERGED to main (D214)
> **Spec 30 P2 shop layer is closed + merged to main (`141ddd71`).** FR-30-3 archive (D213) + **FR-30-5 product search** + **FR-30-6 searchable filter** are all live-verified on canary `/shop/`. QA Gate C (search, security-critical) passed 7/7: draft product 1017 never leaks, >30/IP/min → 429 + Retry-After, response shape `{id,title,permalink,thumbnail}` only, XSS-inert (server `wp_strip_all_tags` + client `textContent`), no-JS GET form, axe-0 keyboard combobox. QA Gate B filter half passed by seeding the real `pa_flavour` block across the 16/15 boundary (then restoring the canary). Plus this session: `sgs/product-search` gained a **`displayMode`** attr with `inline` (always-visible bar) + `icon` (native `<details>` disclosure, expand-on-click, no-JS-safe) variants; search is **kept OUT of the framework default header** and ships as **3 opt-in header patterns** (`sgs/header-search-bar-above`/`-below`/`-icon`, category `sgs-headers`, Block Types `core/template-part/header`); the 5 hand-written architecture docs (specs 01/02/17 + plugin/theme CLAUDE.md) were synced to D191–D214 (the auto-gen `02-SGS-BLOCKS-REFERENCE.md` is regenerated by `/sgs-update` — never hand-edit it). Merged via the co-active-safe temp-worktree cherry-pick (9 theme commits onto origin/main; the cloning thread's WIP on the feat branch left untouched). Commits: `141ddd71` (main tip) — search/filter/variants/patterns/docs.
>
> **RESUME at Step 9** of `.claude/plans/2026-06-11-spec30-p2-differentiators-shop-schema.md` (the plan's `## PROGRESS` marker is updated + it carries a `## Ground truth` block — READ BOTH). Order: **Step 9 FR-30-9 schema** (design-gate FIRST: `/adversarial-council` the draft-leak red-team) → Step 10 parked P1 follow-ups → Step 11 go-live checklist → Step 12 phase close + `/qc-council` + Bean R-22-13 + merge to main (temp-worktree). After Step 12, Spec 30 is COMPLETE and SGS is a sellable shop. FR-30-12 product-page clone stays UNGATED (cloning thread's call).
>
> **⚙️ BINDING ORCHESTRATION MODEL (Bean directive 2026-06-11):** Opus main agent does ONLY plan / delegate / QC / live-test / commit. ALL block/PHP/JS/CSS implementation DELEGATED to sonnet subagents (return uncommitted diffs, NO commit/deploy authority). Main agent QCs (`/qc-council` + `/adversarial-council`, fact-check EVERY rater claim against live code/axe/DOM), deploys + live-verifies on canary HIMSELF, then commits. SendMessage corrections back; never retype a subagent's file. (Narrow exception: trivial config tweaks — version bumps, a single attribute, a one-line template placement — done inline during live-test tuning.)
>
> **CO-ACTIVE TREE WARNING (live):** the cloning thread shares the primary worktree + `main`. The feat branch `feat/spec30-p2-shop-schema` carries the cloning thread's WIP commits (their own to merge). Commit by EXPLICIT PATH (`git commit -- <paths>`), NEVER `git add -A`/bare commit. Merge to main via an ISOLATED temp worktree (`git worktree add --detach <tmp> origin/main` → cherry-pick the theme commits → push HEAD:main → `git worktree prune`); NEVER `git checkout main` in the primary worktree. Never `git revert/restore/stash/clean` the shared tree. After pushing, fast-forward local main with `git fetch origin main:main` + verify `git merge-base --is-ancestor main origin/main`.

## READ BEFORE ANYTHING ELSE — warm-start + STOP catalogue (carried forward + extended; do NOT subtract)
- **STOP — verify the branch before EVERY commit.** `git branch --show-current` + `git log origin/main --oneline -5`. `main` is shared with the co-active cloning thread (it holds the tree + commits WIP to the feat branch). Commit path-scoped (`git commit -m "..." -- <paths>`); merge to main via temp-worktree cherry-pick, never disrupt the co-active tree. Leave never-stage artefacts untouched: `lucide-icons.php`, `sgs-framework.db`, `theme-snapshot.json`, `.parity-golden.json`, phase reports, build/, and the cloning thread's `testimonial*`/`convert.py`/`db_lookup.py`/conformance fixtures.
- **STOP — new block-theme `patterns/*.php` won't register until the theme `style.css` `Version:` BUMPS.** WP caches the pattern-file list against the theme version; a valid new pattern file is silently MISSING from `WP_Block_Patterns_Registry`/the Replace picker until you bump `Version:` (+ `wp transient delete --all` + `wp cache flush` on the live site). If a fresh pattern won't appear, check the served theme version FIRST. Direct sibling of the theme-CSS cache-bust rule. (Found D214, 2026-06-12.)
- **STOP — for an expand-on-click control in a WP block, use the native `<details>`/`<summary>` disclosure, not a JS-only toggle.** It works with JS off, is keyboard- + screen-reader-accessible for free, and JS only ENHANCES (focus the field on `toggle` open; Escape/outside-click closes). The `sgs/product-search` `displayMode=icon` variant is the reference. (D214.)
- **STOP — WC filter/collection markup must match WooCommerce's CANONICAL `templates/blockified/archive-product.html`.** Hand-authored WC block markup is the trap: each `product-filter-*` parent MUST nest its inner control block (price→price-slider, attribute→chips, status/rating→checkbox-list) — self-closing = empty panels; `displayStyle` = the FULL block name (`woocommerce/product-filter-chips`), not `"chips"`. The `product-collection` MUST have `tagName:"div"` + an explicit wrapper `<div class="wp-block-woocommerce-product-collection">` + `queryId:0`/`isProductCollectionBlock:true` or WC never attaches `data-wp-router-region` and instant filtering silently dies. When a WC block "renders empty/half", read WC's own block.json `ancestor` chain + its canonical template FIRST. (Found D213.)
- **STOP — there is NO global `.btn`/`.btn-primary` in the theme.** Those styles live ONLY in `product-card/style.css` scoped to `.product-card`. A bare `btn btn-primary` outside a card matches zero rules. Reuse the design TOKENS (`--wp--preset--color--primary` + `--sgs-product-card-btn-text`) or extract a global button utility (parked P-NO-GLOBAL-BUTTON-COMPONENT).
- **STOP — deploy `*.asset.php` with ANY viewScriptModule JS change; scp the WHOLE block set; opcache-reset; verify the served `?ver`.** WP reads each block's `style.css`, not `style-index.css`. The plugin build output is `plugins/sgs-blocks/build/blocks/<block>/` (NOT repo-root `build/`).
- **STOP — bump block.json version with ANY block style.css change; bump theme `style.css` Version with ANY theme `assets/css/*.css` change OR new `patterns/*.php`** (TWO cache-bust mechanisms — Hostinger CDN caches 7 days on the ?ver URL). A probe contradicting a fresh deploy → check the served ?ver FIRST.
- **STOP — typography controls use the shared `TypographyControls` + `sgs_typography_css_rule()` (D209), NEVER bespoke blank-box font controls.**
- **STOP — a guard on ONE write path is not a guard; enumerate every path. `show_in_rest:false` on PHP-authored metas; strict `'1'===(string)$v` casts, never `(bool)`.**
- **STOP — REST/one-shot gates CANNOT see admin/editor/shop visual defects; a Playwright/chrome-devtools pass at 375/768/1440 is MANDATORY for any new admin/editor/shop UI.**
- **STOP — clean up superseded controls when changing a block** (ONE control per setting; audit duplicate/dead/render-without-control/vestigial; the dead-control guard is BLIND to render-without-control).
- **STOP — WC products edit in the CLASSIC screen, not Gutenberg** (`use_block_editor_for_post_type('product')` FALSE — no PluginPrePublishPanel for products).
- **STOP — a file-scope `extends \WC_*` class fatals the WHOLE site if required before WC loads; require inside `woocommerce_loaded` + a parse-time `class_exists` guard. After bootstrap-wiring deploys: curl the front page FIRST.**
- **STOP — a CPT capability map with SINGULAR meta-caps breaks the mapped capability SITE-WIDE; use plural primitives; probe the live admin after any CPT-registration change.**
- **STOP — canary live styles come from the `wp_global_styles` DB post (ID 7), not theme.json on disk.**
- **STOP — public product/text/XML endpoints: enumerate WC visibility states (`visibility=>'catalog'`/`'search'`), read raw `$post->post_password`, entity-decode display strings, single-flight lock + item cap, `?cb=` CDN-bust when verifying.** (The shipped `class-product-search-rest.php` is the reference for the search variant of this.)
- **STOP — schema/OG/feed price ALWAYS inc-VAT + from the MANIFEST; never `wc_get_price_to_display`/`get_children` in schema/feed emitters; FAQPage is DEAD (drop it, Google removed the appearance 2026-05-07); ONE Product node per PDP.**
- **STOP — NO static fake reviews (UK DMCC Act in force 2026-04-06, displaying trader liable); Trustpilot/verified-buyer only. The FR-30-9 PDP aggregateRating gate reads WC-NATIVE review count but the displayed reviews are TRUSTPILOT — pick a DMCC-honest aggregateRating source (it won't emit despite 5 live Trustpilot reviews), or omit (never stub).**
- **STOP — passing automated gates ≠ DONE (design + operational + legal); expect Bean's R-22-13 eye to catch more.**
- **STOP — fact-check EVERY subagent/rater claim against live ground truth (code + axe + live DOM) before acting; rater findings are HYPOTHESES.**
- **STOP — build via PowerShell (`npm run build`), NOT Bash (broken node wrapper); WP guard-blocked ops via token-gated webroot one-shot (native PHP, quoted string literals); `POST /wp/v2/pages` is NOT guard-blocked; the creds file `.claude/secrets/sandybrown.env` has an unquoted `)` — parse with grep/cut, never `source`.**
- **STOP — dispatched agents have NO commit/deploy authority; they return uncommitted; the orchestrator reviews + live-verifies + deploys + commits. A rater must NEVER `git checkout/restore/stash/clean` the shared tree.**
- **STOP — WC 10 ships `woocommerce_coming_soon=yes` by default: it masks ALL store pages behind a Coming Soon template for guests. Check the option FIRST when store pages look wrong (canary is set to `no`). FR-30-13 go-live item.**
- **STOP — WC fixture = product 540 (`mamas-test-box-48-sku-fixture`, published 48-SKU variable); product 1017 is a DRAFT ("P4 pricing write test"). Verify a fixture's post_status before writing tests against it.**
- **STOP — a `wp:html` block CANNOT wrap a following WP block** (self-contained; the next block renders as a SIBLING). To wrap WP blocks in a semantic element use `wp:group {"tagName":"aside"|"section"}`. After building any block-theme layout, live-probe `element.children`.
- **STOP — theme `contentSize` 780px / `wideSize` 1200px** (theme.json L297-298). A wide shop layout needs `align:wide` on the DIRECT child of the constrained `<main>`.
- **STOP — the SGS visual-diff pre-commit gate** (`.git/hooks/pre-commit`) blocks any commit touching `plugins/sgs-blocks/src/blocks/<block>/` until `reports/visual-diff/<block>-<TODAY>.md` exists with `verdict: PASS` AND `first_paint_capture_passed: true`. Live-verify the block's RENDERED output FIRST, then write the report.
- **STOP — `sgs/product-card` has a loop-context mode** (`usesContext: postId`): inside a core `woocommerce/product-template` with `sourceMode=wc-product` + no explicit `productId` it binds to the loop's current product (FR-30-3 Option C).
- **STOP — on Playwright MCP "Browser is already in use" (co-active thread holds it), switch to the chrome-devtools MCP / a self-contained Playwright CLI script** (own browser, no MCP lock) — Bean's stated preference. The `axe-core` 4.10 CDN inject + a node `.mjs` driven by local `plugins/sgs-blocks/node_modules/playwright` is the reference (run via PowerShell).

## Pre-flight self-attestation ritual (answer before the first action)
1. Which thread am I? (theme/blocks — NOT the cloning pipeline.)
2. What branch is the working tree on? (`git branch --show-current`.) Has origin/main moved? (`git log origin/main --oneline -5`.) Is the cloning thread's work in the index right now? (`git status` — if so, commit ONLY by explicit path.)
3. Have I read Spec 30 (FR-30-1..13) + D208/D209/D213/D214 + the plan's PROGRESS + Ground truth blocks end-to-end before proposing a build sequence?
4. What is the MEASURABLE acceptance for the task I'm about to start (FR criterion + Bean's eye), not "code shipped"?
5. Will this change be visible on a live URL? If so — deploy (build + scp whole block set + asset.php + opcache-reset + verify ?ver) BEFORE measuring.

## State recap (plain English)
The shop is done: a working archive with filters that update the grid instantly, a type-to-find narrower for long attribute lists, and a custom product search box (security-hardened — no draft/data leaks, rate-limited, XSS-safe). The search box comes in two looks (a full bar or a click-to-expand icon) and is offered as opt-in header layouts so only sites that want search get it. All merged to main, and the architecture docs are caught up. The remaining Spec 30 work is the **structured-data (schema) build** — telling Google/AI search engines about the business and products — plus a go-live checklist. The schema build is security-sensitive (this codebase leaked structured data once before), so it needs a red-team design gate before building. After that, Spec 30 is complete and SGS is a sellable shop framework.

## Skills to Invoke
| Skill | When |
|-------|------|
| `/brainstorming` | ALWAYS — any architectural/design call (the FR-30-9 schema scope split) |
| `/adversarial-council` | MANDATORY red-team on the FR-30-9 schema draft-leak pass (Step 9) — this codebase shipped a draft→public JSON-LD leak before |
| `/gap-analysis` | ALWAYS — grade any unit vs its FR acceptance before delivery |
| `/lifecycle` | ALWAYS — before any skill/agent/pipeline change |
| `/research` (+ `/library-docs`) | ALWAYS — JSON-LD per-page-type shapes, Organization/WebSite schema, WC return-policy fields, Rich Results changes |
| `/strategic-plan` + `/phase-planner` | only if Step 9 needs re-planning; the consolidated plan already covers 9–12 |
| `/sgs-wp-engine` + `/wp-plugin-development` + `/wp-rest-api` | the schema-emitter build (Organization/WebSite emitters, noindex `wp_head` hook) |
| `/qc-council` | MANDATORY before any schema/SGS-block commit (blub.db 255) |
| `/verify-loop` | 2-attestation on the draft-leak-as-guest claim |
| `/ui-ux-pro-max` + chrome-devtools/Playwright | any new visible surface at 375/768/1440 (Step 10 notify-me if built) |
| `/dispatching-parallel-agents` + `/subagent-driven-development` | Step 9 audit-vs-build split; implementer→review loops |
| `/sgs-update` | after any block add/change |
| `/delegate` | model per dispatch (sonnet default; haiku/gemini-flash = 2nd council family) |
| `/capture-lesson` | any new architectural rule |
| `/handoff` | session close |

## MCP Servers & Tools
| Tool | For |
|------|-----|
| chrome-devtools / Playwright (switch to chrome-devtools MCP or a self-contained Playwright CLI script if Playwright MCP "Browser already in use") | live verification + screenshots + 3-breakpoint + axe (inject axe-core 4.10 from CDN) |
| `/wp-blocks` (`python ~/.claude/hooks/wp-blocks.py dump`) | block schema before asserting capability |
| `/sgs-db` (`python ~/.claude/skills/sgs-wp-engine/scripts/sgs-db.py`) | block roster / attrs |
| WooCommerce Store/REST + `/wc/v3` (app-password Basic auth) | products + return-policy options + base-country (`returnPolicyCountry`) |
| SSH + token-gated webroot one-shot | guard-blocked WC ops + `wp eval`; `ssh -p 65002 u945238940@141.136.39.73`; creds `.claude/secrets/sandybrown.env` (grep/cut, never `source`); local JSON-LD validator |
| external Google Rich Results Test | FR-30-9 post-deploy verification (NOT the primary gate — the local validator + grep gates are) |

## Agents to Delegate To
| Agent | When |
|-------|------|
| general-purpose (sonnet) | the schema-emitter build (Organization/WebSite, noindex hook, returnPolicyCountry, FAQPage removal) — NO commit/deploy authority, return uncommitted |
| general-purpose (haiku / gemini-flash) | security / 2nd-council-family review on the draft-leak schema pass |
| design-reviewer | visual verify of any new visible surface (Step 10 notify-me) |

## Dependency graph
```
Step 9 FR-30-9 schema  (design-gate FIRST: /adversarial-council draft-leak red-team → sonnet build → main agent runs local JSON-LD validator + guest draft-leak probe + grep gates)
  ↓ /qc-council + /adversarial-council
Step 10 parked P1 follow-ups (gallery-swap decision + notify-me build-or-defer)  ‖  can run after Step 9
  ↓
Step 11 FR-30-13 go-live checklist doc
  ↓
Step 12 phase close — /qc-council full P2 diff + /sgs-update + Bean R-22-13 3-breakpoint sign-off + merge to main (temp-worktree cherry-pick)
```

## Task 1 — FR-30-9 schema (Step 9) — net-new, security-sensitive [THE PRIORITY]
**What:** AUDIT the shipped PDP `Product`/`Offer` + Shop `ItemList` (D204 guard lives in `is_publicly_listable()` in `class-product-item-list.php`, NOT the emitter), then BUILD net-new: `returnPolicyCountry` (source `get_option('woocommerce_default_country')`=GB) on PDP + Organization; a sitewide `Organization`+`WebSite` emitter (logo/sameAs/contactPoint/address; NO `SearchAction`); cart/checkout/account `noindex` via a `wp_head` hook (`is_cart()||is_checkout()||is_account_page()||is_wc_endpoint_url()`). REMOVE FAQPage: delete `sgs_emit_faq_page_jsonld()` add_action in `includes/product-faq-schema.php` AND its `require_once` in `product-faq/render.php` (or the block PHP-fatals). **Why:** complete structured data = SEO/AI-search ranking; the last Spec 30 build pillar. **Estimated:** 75 min.
**Orchestration:** Step 9a INLINE Opus design gate (`/adversarial-council` red-team the draft→public-JSON-LD leak — this codebase shipped exactly that before). Split audit-vs-build → delegate the build to sonnet. Main agent runs the local JSON-LD validator + a **guest** draft-product probe (must emit ZERO JSON-LD) + grep gates (0 SearchAction, 0 FAQPage) + commits.
**Decide:** the PDP `aggregateRating` source — `build_aggregate_rating()` reads WC-native review count but displayed reviews are TRUSTPILOT (won't emit despite 5 live reviews). Pick a DMCC-honest source or omit; never stub.
**Acceptance:** local validator 0 errors on PDP/shop/sitewide shapes; draft/scheduled product as guest → 0 JSON-LD (live-probed); cart/checkout/account `noindex`; grep 0 SearchAction + 0 rich-result FAQPage; `returnPolicyCountry` present in PDP + Organization. **/qc gate after:** `/adversarial-council` (draft-leak) + `/qc-council`.

## Task 2 — Step 10 parked P1 follow-ups (after Step 9, serialise on buybox)
**What:** (a) P-WC-GALLERY-VARIATION-SWAP — decide driven-into-Beta-gallery vs static-per-variation; record in decisions.md; build only if driven + a multi-image variation fixture exists. (b) P-WC-NOTIFY-ME-CAPTURE — build the PECR-guarded capture endpoint (sanitize_email+is_email+nonce+≤3/IP/hour+un-pre-ticked consent+privacy link, email+product_id+timestamp only) + wire the buybox notify-me UI, OR re-defer with a recorded reason. **Estimated:** 10 min (decision) / 40 min (notify-me build). **Orchestration:** delegate notify-me to sonnet with the PECR guard list; main agent live-tests consent+rate-limit + commits. **/qc gate:** /adversarial-council (PII/consent red-team).

## Task 3 — FR-30-13 go-live checklist (Step 11) + Step 12 phase close
**What:** versioned go-live doc (gateway LIVE-mode / return-policy populated / review source connected / per-unit denomination strings / product-data sku-gtin sweep / allergen statutory content / FR-30-11 responsive-audit script on the LIVE site / cookie-consent if any capture active / `woocommerce_coming_soon=no`), each item with a named probe/manual step. Then Step 12: `/qc-council` full P2 diff + `/sgs-update` + present 3-breakpoint screenshots of every new visible surface to Bean for R-22-13 sign-off + path-scoped commit + merge to main (temp-worktree); archive the plan at close. **Estimated:** 25 + 25 min.

## Parked / optional (Bean's choice, not blocking)
- P-NO-GLOBAL-BUTTON-COMPONENT (extract a global `.btn` theme utility); P-COLLAPSIBLE-TEXT-DEFAULT-COPY (per-client SEO-copy onboarding); trust-bar `gridItemPadding` cosmetic NIT.
- B2B ex-VAT price display (Indus Foods) — Spec 30 Open Q3, before Indus's shop build. Subscriptions/subscribe-and-save — Spec 30 Open Q4 (Bean's scope + extension-licence call).
- FR-30-12 product-page clone (UNGATED) — cloning thread's call.

## Methodology guardrails (do not skip)
- **Deploy before measure** — any change visible on a live URL needs build (PowerShell) + whole-block-set scp + `*.asset.php` + opcache-reset + ?ver check BEFORE any browser/axe test; else you measure stale output. For a NEW theme pattern, ALSO bump the theme `style.css` Version (pattern-file cache).
- **Root cause before instance fix** — ask "what's the class of failure?" When a WC block renders wrong, read WC's block.json `ancestor` + canonical template FIRST (D213). When a new pattern won't register, check the theme version (D214).
- **Outcome vs completion** — code shipped ≠ outcome achieved; don't mark a task done until its FR acceptance + Bean's eye pass.
- **Fact-check every rater claim** against live code/axe/DOM before acting — rater findings are hypotheses.
- **/qc-council (cross-model) BEFORE every commit** touching schema / REST / SGS-block logic (blub.db 255); `/adversarial-council` MANDATORY on the FR-30-9 draft-leak pass.
- **Verify a "registered" block/pattern actually RENDERS** — a registry/asset handle string in the page (e.g. `sgs-product-search-editor-script`) is NOT a rendered instance; live-probe for the real DOM before live-testing front-end behaviour (D214).
- **WCAG 2.2 AA, mobile-first, 44px targets, 4.5:1 contrast. UK English everywhere.**
