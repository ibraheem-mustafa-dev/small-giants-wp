# Defect Register + Cause Groups — 2026-07-05 diagnosis session (Bean-directed)

Run: `pipeline-state/mamas-munches-homepage-2026-07-05-051552` (fresh clone to page 8, deploy green, parity at baseline content 90 / CSS 67-69-76, A2 green at 5 keys).
Register: 311 rows (8 content drops, 287 element-x-property CSS mismatches, 16 unmatched draft elements), conservation-checked against the parity report (match + mismatch = meaningful props at every viewport, 0 unexplained). Full register JSON: session scratchpad `defect-register.json`; cluster extracts `C1..C8-*.json`.
Method: 8 parallel read-only investigators (one per mechanical cluster), every load-bearing claim fact-checked by main-session tracing (STOP-15). Two rater claims corrected by tracing: (1) the "single-winner resolver" prior is real but NOT the proximate CTA cause; (2) C8's "maxWidth never populated" — it IS emitted, but type-invalid.

## REAL DEFECTS (11 cause groups)

| # | Cause (plain English) | Evidence | Register rows | Fix-shape | Effort | Gain |
|---|---|---|---|---|---|---|
| CG-1 | **Text blocks can never receive a colour.** The CSS pass throws away every `color`/`background-color` declaration before dispatch, assuming the native WP lift consumed it — but `sgs/text` has `supports.color:false` and its own `textColour` attr, which nothing then populates. 0 of 22 text blocks on the page carry a colour. | `css_pass.py:126/132` (partition implements only half its own docstring); `sgs/text` block.json `color:false` + `textColour` attr; 0 `textColour` in extract.json | R045-R174 colour rows (14 elements ×3vp) + contributes disclaimer background/border (R102-114) | Partition only props the native lift ACTUALLY wrote; let the rest reach the typography resolver | 30 min | ~50 props/vp |
| CG-2 | **Headings: the block's inline style kills its own responsive rules, and every heading renders as `<h2>`.** Base font-size goes out as an inline style (specificity beats any @media rule), so the emitted Mobile/Tablet overrides are dead CSS. And no converter path writes `level` — the live page has ZERO `<h1>` (SEO + a11y defect); h1/h3/h4 all inherit the theme's h2 letter-spacing/line-height. | `heading/render.php:211` (inline) vs `:365-372` (dead media); `render.php:94` default h2; grep: no `level` write anywhere in converter/; live page `<h1` count = 0 | R015-017, R041/052/081/123/169, letter-spacing rows R042-170, h4 line-heights, derived line-heights | Emit base font-size via the scoped `<style>`/custom property, not inline; converter writes `level` from the source tag | 45 min | ~30 props/vp + h1 restored |
| CG-3 | **Theme fluid body font-size the draft never asked for.** The snapshot's flat 16px body becomes a WP fluid `clamp(14px…16px)` — mobile paragraphs render 14px, tablet 15px, where the draft is flat 16px. Single biggest row count (most font-size+line-height pairs at 375/768). | live CSS `body{font-size:clamp(14px,…,16px)}`; snapshot styles.typography fontSize=16px; draft declares no body font-size | R058-065, R181-182, majority of R199-279 | Disable fluid for the body font-size in the snapshot/theme.json generation (flat 16px) | 15 min | ~70-80 props at 375/768 (largest single gain) |
| CG-4 | **max-width silently discarded by a type bug.** The serialiser writes `maxWidth:"620px"` (string) into `sgs/text.maxWidth` which is declared `number` — WordPress drops the invalid value. Hero sub (420px), ingredients intro (540px), disclaimer (620px) all stretch full-width; the auto-centring margins collapse as knock-on. | `value_serialise.py:11-18` stub ignores attr_type; emits verified in extract.json; text block.json maxWidth type=number | R020(mw), R085, R116, R288-292 | Branch serialisation on attr_type (number → bare numeric + Unit companion) | 20 min | ~10 props/vp |
| CG-5 | **Buttons: four stacked defects.** (a) a bare `<a>` with no button class gets forced into the button slot and silently defaults to the full primary pill ("Find out more"); (b) the `outline` preset is colour-only — no size variant, so ghost buttons can't shrink; (c) lifted sizing lands on the WP wrapper div, not the visible `<a>`, and typography only applies when `inheritStyle==='custom'`; (d) multi-button emits only desktop `flexDirection` — no responsive gap/direction. | draft :976/:599; button style.css:69-80; render.php:168/182/228 + wrapper :609; multi-button emit `{"flexDirection":"row","layout":"flex"}` only | R145-168, R066-074, R191-198, R024/025, R070, R163 | (a) require a button-class/DB signal before the button slot; (c) route sizing to the block's flat attrs or read style.* in render.php; ungate typography; (b) size variants or rely on (c); (d) lift responsive flex attrs | 1-2 h | ~40 props/vp |
| CG-6 | **Labels/badges: converter never writes shape attrs; render.php burns schema defaults inline.** Every eyebrow label + gift badge gets the default 6px-radius chip look (radius/line-height/display), and pill padding is gated on a style-variant class the converter never sets. | label render.php:100-113 (default radius inline; pill gate on className); emitted label attrs carry no borderRadius/lineHeight/className | R009-013, R035-040, R075-080, R117-122, R128-132, R137-141 | Label resolver writes borderRadius/lineHeight from draft computed values + selects is-style-plain/pill-fill | 45 min | ~25 props/vp |
| CG-7 | **Product-card content (the A2 baseline).** (a) CTA: the draft anchor uses global `sgs-button sgs-button--primary` classes, so the card's element walk (BEM family gate, walk.py:229-235) never sees it — text AND href drop. The "single-winner resolver" prior is real but never reached; two more latent bugs behind it (walk.py:280 hardcodes text-content; role `content` has no extractor). (b) pills: `packSizes` declares no `items` schema in block.json, so the seeder seeds nothing and the array lift skips the attr before structural detection. | draft :861; DB derived_selector `.sgs-product-card__button`; array_item_schema 0 rows; block.json packSizes no items | R001-004, R008, R296-300, R306 (= the A2 baseline) | (a) DECISION: identity-anchored lift (parked FR-31-2 shape — recognise the sgs/button descendant, lift text+href) vs draft-side re-class; then fix the latent trio; (b) add items schema to block.json + reseed | 1 h + design gate | content 90→~97; A2 5→1 |
| CG-8 | **Image alt discarded.** The extractor builds `{url,id,alt}` correctly, then `walk.py:293-294` collapses to the URL for string-typed image attrs; the sibling `imageAlt` attrs exist on both blocks (render.php reads them) but have `role=NULL` — unwired. All 3 "dropped" images are actually LIVE with `alt=""` (a11y defect, not a content drop). | walk.py:291-294; DB imageAlt role=NULL; live page 3× `alt=""` with correct src | R005-007 (+R305/307/308 instrument side) | Companion-attr lift: route dict alt into the sibling imageAlt; seed its role | 30 min | content +3 units (with CG-12) |
| CG-9 | **Block-CSS hardcoded defaults fight the draft** (composite-mirror/R-31-9 flavoured, per block): trust-bar label `clamp(0.75rem,1.2vw,0.875rem)` is arithmetically pinned to 12px below ~1000px (draft 13px) + hardcoded `.sgs-trust-bar{text-align:center}` with NO textAlign support; heading `:where(){text-align:center}` default while the fold only fires on EXPLICIT draft text-align (left-aligned drafts can't win); product-card price-note hardcoded 14px (draft 13); label/button `inline-block`/`inline-flex` display defaults; testimonial quote margin fixed to a fluid spacing token; hero-CTA/ingredient gap defaults override transferred values. | trust-bar style.css:7-10 + :99; heading style.css:7-9; product-card style.css:1025; fold_helpers.py:475-486 | R019-190 text-align set, R026-034, R047-051, R039/079/121, R176/178/180, R192/198, R247/251 | Per block: expose the attr (textAlign support on trust-bar; label font-size var write-through), fix the broken clamp + wrong constants; make heading default left OR fold resolved (not just declared) text-align | 1-1.5 h | ~45 props/vp |
| CG-10 | **Nested-grid gap never dispatched + slider-vs-grid.** For grids nested 2 levels deep (brand content, gift cards, trustpilot row), the grid tracks transfer but `gap` never fires (zero trace entries — not even a logged GAP). Separately the testimonial section: draft is a static 3-col grid, clone renders the slider block's carousel — a real, possibly INTENTIONAL block-behaviour difference. | grid.py:42/110-131; no cross_node_gap trace entries; C3 confirmed wrapper mis-pairing carries the real grid-vs-carousel diff | R224/228/262/266/269/272/277/280/281/286; R282/284 | Trace + fix the fold depth for gap; DECISION: keep carousel or honour draft's static grid | 1 h | ~15 props/vp |
| CG-11 | **Testimonial italics unreachable.** The block HAS `quoteStyle` ('italic'), but the generic typography lift targets an attr literally named `fontStyle`, and the scoped per-element lift deliberately excludes `quoteStyle` (NULL css_property). No path writes it. | block.json:184-188; styling_content.py:78-80 docstring; 0 emits | R175/177/179 | Seed css_property for quoteStyle via the sanctioned channel, or map font-style→quoteStyle by canonical slot | 20 min | 3 props/vp |

## INSTRUMENT ARTEFACTS (CG-12 — fix computed-parity.js, not the page)

~55 register rows are the measurement tool's own defects (STOP-49): (a) empty-alt images invisible + unpairable (R305/307/308); (b) any `<footer>` ancestor = chrome, so testimonial `<footer>` attribution is excluded — authors ARE live (R301-303); (c) box-tier exact matching broken by a leading-space from the draft's ★ glyphs + first-write dedupe collision (R309/310; also mis-pairs the slider wrappers R281-293); (d) `<html>` treated as a content box (R304, R311); (e) dual keying + fuzzy fallback pairs the hero h1 against an ancestor (R181-187 duplicate of R015-017); (f) grid-template-rows compared as resolved pixels, unlike columns (R226/237/249/264); (g) WP alignfull root-padding counter-margins not blocklisted (R205/206/222/223/243/244/260/261/275/276); (h) `auto` vs `0px` min-height noise (R014/023/056/189/197/287). Fixing these RAISES the honest parity numbers (false negatives clear) — re-baseline after.

Skip-link (5th A2 key): draft's custom skip link intentionally superseded by WP core's — permanent chrome, stays baselined; the dead `.page-id-8 .sgs-header__skip-link` CSS can be cleaned opportunistically.

## Decisions Bean must make (Task 4 gate)

1. **CG-7 CTA shape:** identity-anchored lift (recommended — matches the parked FR-31-2 plan, universal, no draft edits) vs re-class the draft CTA.
2. **CG-10 testimonials:** keep the slider carousel (DECIDED by Bean 2026-07-05: KEEP).
3. **CG-3 theme fluid body font:** flat 16px for cloned sites (recommended) vs keep WP fluid typography.
4. **Order:** bulk pass recommended — CG-3, CG-1, CG-2, CG-4 first (biggest gains, smallest efforts), then CG-5/6/9, then CG-7/8/11, CG-12 instrument last + re-baseline.

---

# ADDENDUM (2026-07-05, round 2) — Bean's point-by-point review + 7 block-wide scans

Bean reviewed the cause groups and raised corrections, questions, and new tasks. 7 further read-only scans ran (CG-9 defaults / CG-2 specificity / CG-11 naming / pipeline-vs-instrument / tag settings / divider+quote / UX research), all main-session fact-checked. Verdicts:

## Verdicts on Bean's points

- **CG-10 universality (Bean's diagnosis):** direction right, mechanism different. The cascade DOES run per node (`_child_content_for_node` → `build_block_markup(is_root=False)` → full CSS pass, extraction.py:187-217) — there is no literal "section-only" gate. The observed nested-gap loss is the **CG-1 partition bug wearing another hat**: `gap` is in `_LIFT_CSS_PROPS` (root_supports.py:74 → WP `spacing.blockGap`), so it's stripped from EVERY node's stream; sgs/container declares NO blockGap support (DB verified), so the value vanishes with no gap logged. One partition fix (only strip what the native lift actually wrote) covers colour + background + border + gap = CG-1 + CG-10-gap + part of C7's disclaimer box.
- **text-align question:** Bean's memory is RIGHT — it's the WP-native `supports.typography.textAlign`. `sgs/container` declares it (verified: typography.textAlign=true), the user sets it via the block's typography panel, the pipeline folds band text-align into it (FR-31-5.1a, D267) and the wrapper emits `has-text-align-*` explicitly (STOP-44). Gaps to fix: trust-bar doesn't declare it; the fold only fires on EXPLICIT draft declarations (a left-aligned draft says nothing → block CSS default centre wins — pairs with the CG-9 heading/trust-bar deletes); `align-items` on inner wrappers → the container's `verticalAlign` attr (D172) — verify the fold routes it + the control is visible.
- **CG-4 visibility:** real but subtle at a glance — hero sub paragraph runs to the hero column width (~700px) instead of 420px (longer lines); disclaimer runs to band width (~960px) instead of 620px centred; ingredients intro 540px cap lost. Screenshot crops at fix time will show it.
- **CG-6 identity dispute:** RESOLVED with evidence — the gift "Gift idea"/"Most thoughtful" badges ARE `wp-block-sgs-label` (live markup verified; DB aliases "pill"→label slot in badge context). Bean was thinking of the PACK-SIZE pills, which alias "pill-group"→sgs/option-picker (design discussion deferred). Bundling the label defaults into CG-9: agreed.
- **CG-5-A (a→button feature):** confirmed as designed (atomic tag map). UX research verdict: keep the draft's plain-link look — the pill promotion breaks primary/secondary hierarchy; hover-reveal is a High-severity anti-pattern on touch (uimax ux-guidelines row 11); draft already meets WCAG 2.2 target size (min-height 44px). So: KEEP emitting sgs/button, but it must render faithfully (fix = don't default inheritStyle to primary when no modifier exists; lift the element's actual styles via the fixed channel).
- **CG-5-B (preset drift detection + build out presets):** design proposal drafted below (§Preset-sync). Preset build-out agreed as a block task.
- **CG-5-C (wrapper dispute):** Bean's belief corrected with evidence — single sgs/button DOES render a wrapper div (`render.php:691/709` echoes `<div {get_block_wrapper_attributes()}>`; live markup `class="sgs-button-wrapper wp-block-sgs-button"` on the solo Find-out-more). WP's style channel lands on that wrapper — that's the mis-channel bug.
- **CG-5-D:** Bean right in substance: the `.sgs-hero__ctas` div becomes `sgs/multi-button` (the actual slug; "button-group" doesn't exist) and its CSS doesn't transfer: `gap` = the partition strip (CG-1/10); mobile `flex-direction:column` = tier not emitted (only base `flexDirection:"row"` in the emit — verified). multi-button render.php itself is CLEAN (CG-2 scan: fully attribute-driven).
- **CG-7-A (Bean's fix-shape):** ALIGNS with my recommendation — Bean's description (bare/foreign node inside a composite → consult the equivalents table → parent's attr row → emit_shape child-vs-nested → route to that population path) IS the FR-31-2/2.6 identity-anchored walk, extended to elements that fail the BEM family gate. Recommendation stands, now Bean-shaped: when `_family_element` returns None but the node RECOGNISES to a block (sgs-button → sgs/button), resolve the parent attr whose identity (`equivalent_block_for`) matches that block (ctaText/ctaUrl anchor to sgs/button via canonical_slot=button) and lift per emit_shape. Universal (DB signals only), no draft edits. Plus the two latent fixes (walk.py:280 role passthrough; ctaUrl role→link-href) and the multi-attr-per-element loop.
- **"Parked plan" clarified:** the D276 EXECUTION plan completed the ENGINE; the card residuals were explicitly tracked OUTSIDE it in `parking.md` (P-GATE-A-CARD-RESIDUALS) with fix-shapes mapped to FR-31-2 — "parked plan" = that entry, not an unfinished build.
- **CG-11 (rule vs naming):** scan verdict — the rule is SOUND (11 of 12 NULL-css_property exclusions on testimonial are genuinely non-CSS). The failure is COMPOUND: rename `quoteStyle`→`quoteFontStyle` AND fix the `property_suffixes.FontStyle` row (role='select-from-enum', the only typography sibling misclassified — verified; without the row fix a reseed reintroduces the bug via sgs/quote's bodyFontStyle/attributionFontStyle). Bonus find: 4 testimonial attrs (nameFontWeight, orgColour, summaryColour, summaryFontSize) have NULL role+derived_selector — reseed backfill needed. Only ONE block has scalar-styling-lift capability today (testimonial), so no wider leak through this gate yet.
- **CG-12 pipeline parallel:** ONE confirmed shared bug — the A2 ledger's chrome exclusion counts nested header/footer/nav at ANY depth (`ledger/declare_input.py:236-247,316`), unlike the walker (correctly root-only, entry.py:222-240). Gate blind-spot: a real drop inside a card-local `<footer>` would be mis-bucketed "chrome-excluded" and A2 stays green. Fix: scope chrome to the fragment root. Bugs 1/3/4 have NO pipeline analogue (traced).

## Scan results (full tables in the agents' outputs; highlights)

- **CG-9 block-wide:** 32 verified instances. (a) DELETE: heading + trust-bar text-align. (b) match browser default: label inline-block, button inline-flex. (c) EXPOSE as attr: 18 (product-card's 8-selector font-size gap is the systemic cluster — font-weight/line-height var-driven, sibling font-size hardcoded), info-box/card-grid heading sizes, testimonial quote margin, icon shape padding. (d) FIX formula: trust-bar clamp. 6 sweep-tier follow-ups (process-steps, team-member, pricing-table, business-info, hero rules) need verification in the fix wave.
- **CG-2 block-wide:** BROKEN in 8 blocks — heading (font-size + wrapper margin/padding), text (typography + margins), quote (both slots), media (max-w/h + order), hero (7 property families incl. the CTA gap var), button (typography + padding + icon var), mobile-nav (5 vars). OK: feature-grid, multi-button, shared helpers, container wrapper. Fix-shape: ONE shared `sgs_responsive_css_rule()` helper (generalising helpers-typography.php) replacing every hand-rolled inline+media pair; `!important` tier pattern where WP native inline is unavoidable. CSS-custom-property bases are NOT safe (re-declaring a var in scoped CSS loses to an inline var).
- **Tag-setting scan:** live tag choosers on container (section/div/article/aside/main), button (a/button), text (p/span/div/blockquote/em/strong), label (span/p/div); DEAD htmlTag attrs on hero/trust-bar/cta-section (control shows, render hardcodes). Converter never emits any of them.
- **Divider:** sgs/divider is dead (zero converter emission, zero pattern usage; wrapper's 16-shape divider strictly richer). Recommend full removal; only reintroduce a minimal line/gap spacer if a real between-blocks need surfaces.
- **Quote editor-empty:** converter fell back to universal nesting (4 sgs/text children, parent attrs EMPTY); render.php has an InnerBlocks bridge ($content non-empty → echo), edit.js reads only body[]/attribution → editor shows empty. Root fix = converter populates body[]/attribution via the declared arrayContentLift path (investigate why the matcher didn't fire); optional editor InnerBlocks bridge as belt-and-braces. NOTE: the β-path also loses attribution semantics (4th sgs/text indistinguishable).
- **UX:** flat 16px body (16px is the a11y floor; fluid solves a heading-layout problem body copy doesn't have; Lighthouse flags <16px); plain-link Find-out-more (hierarchy + touch).

## Proposal — §Preset-sync (CG-5-B design sketch, for /brainstorming + qc-council before build)

When the draft styles an element that maps to a block PRESET (e.g. `.sgs-button--primary`), Stage 0.7/4 compares the draft's declared values for that preset class against the site's current preset definition (the `--wp--custom--button-presets--*` vars in the theme snapshot). On mismatch: write the draft's values INTO the per-client theme snapshot's preset vars (the existing per-client channel — sites/<client>/theme-snapshot.json → wp_global_styles), never into block CSS. That makes presets client-faithful once, keeps blocks universal, and every button using the preset inherits the correction. Needs: a preset-class→var-map fact (DB-seeded from block.json presets), a diff step, and a snapshot writer. Design-gate before build (shared-mechanism, Rule 7).

## FIX-WAVE RESOLUTION TABLE (end-of-session verification, 2026-07-05 — parity tool + Playwright at 375, live page 8)

| Cause group | Register rows | Status | Live evidence |
|---|---|---|---|
| CG-1/CG-10 partition (colour/gap/border strip) | ~50 rows | **RESOLVED** | muted p = rgb(107,92,80) draft-exact; colour rows 17→2 (residual 2 = card descriptions, see below); nested gaps emit |
| CG-2 responsive channels | ~40 rows | **RESOLVED** (channel) / **PARTIAL** (level) | h2 = 28px @375 (was 36); hero heading 34px @375 (was 58); Pattern A live. RESIDUAL: `level` converter write never dispatched — page still has 0 `<h1>` (SEO/a11y, next session) |
| CG-3 fluid body font | ~70 rows | **RESOLVED** | body + paragraphs = flat 16px @375 (was 14) |
| CG-4 maxWidth type bug | ~10 rows | **NOT BUILT** (approved but never dispatched — the serialiser fix fell out of the wave; still open) |
| CG-5 buttons | ~40 rows | **PARTIAL** | Find-out-more: transparent bg + coral text (UX-Q2 live); padding still 14/24 vs draft 6/0 (custom-mode padding channel = remaining CG-5-c work); CG-5-D held per Bean |
| CG-6/CG-9 labels + defaults | ~80 rows | **RESOLVED** (32-item sweep) | forced text-aligns deleted; gift badges coral; trust-bar label 13.9px vs draft 13 (clamp near-miss — expose-attr lift is the finish) |
| CG-7-A card CTA | 5 rows + A2 | **RESOLVED** | "Add to Cart — £10" + href live; A2 baseline 5→4 |
| CG-7-B pills / option-picker | 7 rows | **DEFERRED** (design discussion, per Bean) |
| CG-8 image alts | 6 rows | **RESOLVED** | 0 empty alts; content parity 90→96 |
| CG-11 quoteFontStyle | 3 rows | **RESOLVED** (emit carries italic post-rename; verify paint on next-clone testimonial pass) |
| CG-12 instruments | ~55 rows | **RESOLVED** | unmatched els 15→7; footer-attribution false-flags gone; A2 ledger root-scoped +4 tests |
| Quote editor-empty | — | **RESOLVED** | attribution in its own element; ONE content model; editor shows children |
| Palette `text` collision | 3 rows | **RESOLVED** | gift badges + find-out-more coral live |
| Tag settings / divider | — | **RESOLVED** (all removed; auto-derive a/button; seeder deletion-propagation fixed) |

**Commits (9, all pushed):** 2ce558aa · e8180d44 · 43c24194 · 33069003 · e1046bf4 · c283821c · a1c84562 · cd27dca8 · 9b6680c4.
**Honest instrument final:** content 96 / CSS 77-78-80 / unmatched 7 / A2 4 keys / 806 tests / gates green.
**Open for next session (6 residuals):** the D2 inline style block disposition (Bean STOP-52 doctrine: load-bearing D2 = cheat; debug-log-only + 100%-parity deletion gate — next session TASK 1) · heading `level` converter write (0 h1 on page) · CG-4 maxWidth serialiser · CG-5-c button padding channel + preset build-out · trust-bar label lift · CG-5-D multi-button responsive (Bean-held) · option-picker design · preset-sync build (design gated).

## New-task verdicts

- **Tag settings removal:** decision menu — the DEAD attrs (hero/trust-bar/cta-section) are unarguable deletions. For the LIVE ones: container's section/div/article chooser and text's p/blockquote/span and button's a/button carry SEMANTIC value (landmarks, quote semantics, link-vs-button behaviour — the converter/atomic map relies on tag semantics for recognition, and a/button changes actual behaviour). Recommend: delete the 3 dead attrs + delete container's UI control ONLY if Bean confirms clients never need landmark choice; keep button a/button (functional); keep text's tag (semantic); label's span/p/div is low-value — candidate for removal.
- **Quote:** fix converter array-lift path (primary) + attribution distinction; editor bridge optional.
- **Divider:** remove (no deprecation needed).
- **Report HTML block in page:** noted, untouched; Bean will supply the file for review after fixes.
