# QA Gate B — Working PDP + cart loop (FR-30-2 + FR-30-7 + FR-30-4) — 2026-06-11

All checks run LIVE on the sandybrown canary (R-22-11). Fixture: product 540 (48-SKU Size×Flavour variable).

## FR-30-7 — option-picker → cart bridge

| Check | Result | Evidence |
|---|---|---|
| Pills update price reactively | **PASS** | £9.99 → 24-pack £18.99 → (+chocolate) £18.99 → 96-pack £59.99 (3 distinct combos incl. multi-axis) |
| Add-to-cart posts EXACT variation via proxy | **PASS** | UI submit → `POST /sgs/v1/cart/add-item` body `{id:578, variation:[Size:96-pack, Flavour:chocolate]}` → 200; Store-API cart READ shows exactly that variation. Second add (12-pack vanilla) also exact |
| Foreign variation_id → 4xx | **PASS** | id 999999 → 400; REAL foreign variation 898 (product 897) with mismatched attrs → 400 "Attribute value mismatch for Size" |
| Simulated 503 → dismissible error + button re-enables | **PASS** | Error region shows "Service temporarily unavailable." (role=alert), add button re-enabled, dismiss (44×44) clears it |
| Availability greying | **PASS** | 96-pack selection greys 1 term; aria-label suffix operator-seeded (`soldOutLabel`/`unavailableLabel` ctx keys) |
| Zero direct /wc/store/v1 cart writes | **PASS** | grep of src/: 0 matches |
| Radiogroup semantics | **PASS** | Native radio inputs in fieldset (single tab stop / arrow keys native); axe 0 on PDP |
| Notify-me | **DEFERRED** (explicit) | No shipped guarded email-capture path exists (demand-analytics is PII-free counts only). Label control ships; capture deferred per FR-30-7 rule |
| Gallery swap on pill change | **FLAGGED to Bean** | Core product-gallery's variation-awareness binds to the core add-to-cart FORM (`callbacks.watchForChangesOnAddToCartForm`), which the buybox replaces. No public set-image API. Gallery static at P1; fixture has a single parent image anyway. Future options in STEP5-BRIDGE-DESIGN.md |

## FR-30-2 — composition

| Check | Result |
|---|---|
| Variable product renders full composition (pickers/price/stock/CTA) | **PASS** (540) |
| Simple product renders core path | **PASS** (513 zookies: 0 SGS pickers, core price + add-to-cart) |
| Single-variant product renders NO selector pills | **PASS** (fixture 1125 created: buybox present, 0 pills, £4.99 + button) |
| Unavailable option disabled-not-hidden | **PASS** (greyed + aria-disabled, label editable) |
| axe 0 on PDP | **PASS** at 375/768 (the @1440 `list` violation is the pre-existing core nav `ul>ul` page-list fallback — sitewide, predates P1, fix = real menu ref; flagged) |

## FR-30-4 — cart / checkout / Mini-Cart

| Check | Result |
|---|---|
| PDP add-to-cart OPENS the styled drawer | **PASS** — engine addition: success → `wc-blocks_added_to_cart` + programmatic mini-cart button click (WC's own openDrawer binding); verified live (closed → open with refreshed contents) |
| Drawer styled | **PASS** — 420px (`--sgs-mini-cart-drawer-width`), token background, brand buttons |
| COD test order completes end-to-end | **PASS** — **order 1124** (guest, 2 variations, COD) → Order Confirmation page |
| 375px zero horizontal overflow (all 4 page types) | **PASS** (fixed: mini-cart `hasHiddenPrice:true` + `.sgs-header-actions` mobile gap + 44px customer-account target) |
| axe 0 on cart + checkout | **PASS** at 375/768 (1440 = the same core nav issue) |

## FR-30-11 — committed script + budget

- Script: `scripts/wc-pages-responsive-audit.js` (committed; overflow + 44px targets + executed-JS + axe + screenshots, 4 pages × 3 viewports).
- Evidence: `wc-pages-responsive-audit.json` + `audit-{page}-{viewport}.png` ×12 in this directory.
- **Executed-JS (initiatorType=script only):** PDP 73.2KB · shop 74.3KB · cart/checkout 645.4KB. PDP/shop exceed the 50KB budget (WC interactivity runtime + gallery + ~10KB sgs engine); cart/checkout are WC's React bundles (core, unavoidable at P1). **Flagged for Bean** — budget ruling needed (WC pages arguably need their own budget line).
- Small targets remaining (warnings): theme nav/footer links 36×16 + sr-only skip-link false positive — pre-existing theme-wide, not introduced by P1.

## a11y/contrast fixes shipped this gate (scoped to WC surfaces; wp_global_styles untouched — clone parity safe)

1. Closed drawer `visibility:hidden` (axe aria-hidden-focus).
2. Breadcrumbs + cart/checkout headings + cross-sell titles + PDP title (`textColor:"text"` in templates) + shop query-title — all were brand-pink #e68a95 on cream #fbf3dc = 2.24:1.
3. Mini-cart price hidden in trigger (overflow); customer-account icon 44px (WC Block-Hooks auto-inserts it — discovered: WC hooks mini-cart+customer-account into block-theme headers; explains the Step-6 agent's "two carts" observation).

## Known-issue register for Bean (R-22-13)

1. Core nav `ul>ul` (page-list fallback) — axe `list` serious @1440, sitewide, pre-existing. Fix = assign a real navigation menu (operator action) — co-active thread parity made me defer.
2. Gallery static on pill change (above).
3. Success message ("Added to your basket.") renders in the same alert-styled region as errors AND the drawer now also opens — Bean to rule whether the message should be suppressed when the drawer opens.
4. PDP/shop executed-JS 73-74KB vs 50KB budget.
5. sgs/cart block (count badge) replaced in the header by core mini-cart — sgs/cart still exists for non-drawer use.
