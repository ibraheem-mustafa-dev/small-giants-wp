---
doc_type: gap-analysis-report
target: .claude/plans/2026-07-18-P2-builder-ux-design-gate.md (v3)
target_type: plan (design-gate)
date: 2026-07-18
grade: B+ (4.3 / 5)
qc: substituted — the 6-persona adversarial-council (Gate 2) + wp-sgs-developer analysis this session exceed qc-council's 3-rater validation; not re-run (lean-over-ceremony, memory `lean-beats-structural-theatre`)
---

# Gap-analysis — P2 builder-UX design-gate (v3)

**Grade: B+ (4.3/5)** — strong, ship-to-P3-ready. Research baseline: live code read in full, the
6-persona Gate-2 council, the wp-sgs-developer grounded analysis, competitor patterns (Kadence/Elementor
Elements). Self-preference counteracted (Claude authored the doc; litmus tests applied literally).

## Per-criterion (weighted)

| Criterion | Score | Evidence |
|---|---|---|
| Precision / buildability (w1.5) | 4.3 | v1's real gaps folded (concrete component APIs §4.2, single-named resolver + desktop guard §6, box-shape §6d); activation mechanism + binding fix now LOCKED to one option each. Residual softness: mega-menu shape legitimately TBD-P5 (named, not dropped). |
| Completeness vs DP1–DP6 + 3-way goal (w1.5) | 4.3 | All 6 DPs traced (§1). 3-way served: framework (blocks), self-service (CPT + starter templates + operator test), converter output (§9 incl. Site-Info + CPT emit). Starter library closes the DIY-moat gap the Competitor found. |
| Internal consistency post-CPT-pivot (w1.3) | 4.2 | 4 stale "Site Editor / sidebar" refs from the pivot were found + fixed this pass; doc now propagates CPT cleanly. Would have been 3.3 unfixed — the pivot's main risk, caught. |
| Risk identification (w1.2) | 4.4 | Council + §13 sweep + named pre-P3 checks (binding gap, device-sync confirm, D328 migration) — risks named with owners + mitigations. |
| Deferral discipline (w1.0) | 4.6 | Every deferral a named phase (mega-menu→P5, walker→its phase, template library→named goal, tri-state→P3). STOP-29 honoured explicitly. |
| Feasibility (w1.0) | 4.2 | Smallest-plausible estimates; slice additive/low-risk; CPT delta small (existing infra: CPTs + submenus + rules engine already built). |

**Weighted overall: 4.3 → B+.** Not A (4.5+) only because a couple of buildability details stay
open-by-design (mega-menu shape TBD-P5) and two live checks are pending (binding + device-sync) — both
legitimately P3-openers, not doc gaps.

## 6-lens (design-doc target)
- **End-result:** PASS — drives the P3 model + P4 panel build; prevents a wasted build (the whole point of a gate).
- **OC↔CC / blub persistence:** N/A — a plan doc, not a persistent system artefact.
- **Automation vs remember:** PASS — maintenance = the roadmap gates, not a manual-update rule.
- **ADHD veto:** PASS — reduces Bean's load (progressive disclosure + operator test + one findable CPT home); no net new checkpoints.
- **Values:** PASS — carries the USP (3-way requirement / converter-emittable moat), the next action (P3 + named checks), serves the framework goal.

## Recommendations (all optional; doc is sign-off-ready without them)
1. *(Applied this pass)* Fix 4 stale Site-Editor/sidebar refs + lock the activation mechanism + lock the binding fix. — DONE.
2. **B — mega-menu shape:** when P5 is scoped, decide the per-item mega-panel attribute shape (currently TBD-P5, correctly named). Not now.
3. **C — the two pre-P3 live checks** (CPT binding renders on frontend; `core/editor` deviceType on the CPT screen) are the first P3 tasks — verification cadence, not a doc fix.

## S-grade
Not awarded (it's a design-gate doc, not a shippable showpiece). The *design it describes* — a
converter-emittable header/footer builder with a starter-template picker generated by the clone pipeline
— is a genuine USP + marketing-showpiece candidate when BUILT (flag for a future S-screen post-P4).

## Verification cadence (not recommendations)
- Two live checks open P3 (binding + device-sync), same sandybrown session.
