---
doc_type: adversarial-council-report
project: small-giants-wp
track: TRACK 2 — Header/Footer/Nav (P2.5)
artefact: .claude/specs/36-SGS-NAVIGATION-SYSTEM.md v2.0 (the canonical nav mini-spec)
date: 2026-07-19
panel: 7 personas — Cynic, Competitor, Spec-Lawyer, Ship-PM, Support-Realist (general-purpose, committed worldviews) + wp-sgs-developer + project-manager (Bean-requested, code-grounded)
constraint: judged on own go-forward merits; retired docs/blocks (Spec 34, §S9 old nav FRs, Spec 02 §23, adaptive-nav/mega-menu/mobile-nav) OUT OF SCOPE
verdict: GO on the architecture — NOT build-ready as written. Close a Phase-3.5 revision (all paper edits; no redesign) before build.
---

# Spec 36 v2 — adversarial council synthesis (fact-checked)

## Grades (spread C+ → A-/B+, consistent)
| Persona | Grade | Dimension |
|---|---|---|
| project-manager | A−/B+ | deliverability (the CORE is deliverable) |
| wp-sgs-developer | B | buildability |
| Cynic | C+ | maintainability |
| Competitor | C+ | moat |
| Spec-Lawyer | C+ | unambiguity |
| Ship-PM | C+ | shippability |
| Support-Realist | C+ | non-coder usability |

## Convergent headline
**Unanimous: the CORE nav system (§0–13, the 07-18 base) is sound** — Ship-PM grades it B+/A- alone; every code-grounded critic verified the load-bearing mechanisms exist (Store-API cart, the shipped combobox, the Interactivity store prior art, `--sgs-header-height`, labelCollapse, Responsive-Visibility, native-menu mega-attach). **The drag is §14–17 — MY 07-19 additions** — bolted on WITHOUT phasing, with feature-lists that dwarf the acceptance gate, mislabelling real builds as "extends", with undefined failure states and a self-introduced ownership contradiction. **Every fix is a paper edit; none is a redesign.**

## MUST-FIX register (convergence-weighted)

### CONVERGENT (3+ voices) — the headline five
1. **Phase §14–17 + honest per-piece "v1 = X, rest = later" lines** *(Ship-PM MF-A, PM MF1, Cynic, Spec-Lawyer).* §7's phasing covers §0–13 only; the 5 utility pieces + 2 cross-cutting FRs + 10 fold-ins have NO phase assignment → a solo builder can't tell what to build after Phase 1. **Fix:** phasing map for §14–17; a v1-line per piece. PM's proposed map: Ph1 = cart badge fix + logo basics; Ph2 = search + social + business-info + mega + collapse; Ph3 = extras.
2. **Honest "build vs extend" for the utility pieces** *(Ship-PM MF-C, Cynic M1, wp-dev, + Bean's cart evidence).* **FACT-CHECKED, with a correction:** the CART is a real build (render.php is a badge+link only; the mini-drawer is unbuilt — "Phase 2" per its own block.json + the plugin build-table); "~90% ready" is wrong. BUT the SEARCH combobox IS genuinely shipped (`product-search` render.php:227/269 ships `role=combobox/listbox/aria-activedescendant` live) — so search is honestly ~extendable, not a rebuild. **Fix:** relabel cart as "substantial build on a badge shell"; keep search as a real extend (+ product-preview + display modes); per-piece built-vs-to-build split.
3. **Ownership boundary: fix the §1↔§14 contradiction + split FR-36-24** *(Cynic M1, Spec-Lawyer M5, PM MF2).* §1 still says "does NOT own the cart/search" while §14 gives them full FRs — a self-contradiction I introduced (§14 preamble expanded ownership; §1 wasn't updated). FR-36-24 conflates (a) whole-piece show/hide = Spec 17/row-level vs (b) per-tier settings on a piece's own attrs = that block's own job. **Fix:** update §1's ownership list; split FR-36-24 into the two ownership lines; add a structural gate (`lint-responsive-controls.py`) so no piece invents a parallel per-device control.
4. **Define failure states + pull minimal "Nav Health" into Phase 1/2** *(Support d, Cynic MISS-2/3, PM, Spec-Lawyer).* No client-facing go-live check until Phase 3; undefined states for Store-API-down (cart/search), silent mega degradation, a DRAFT (not just trashed) panel, a zero-drawer burger, partial Site-Info fill. **Fix:** define each failure state; surface FR-36-9a's error notices as a minimal editor "Nav Health" list in Phase 1/2 (the plumbing already exists per FR-36-12).
5. **The converter's multi-artefact emit needs its own sub-design** *(wp-dev MF-c, PM MF3, + the reconciliation converter critic).* Verified: 0 `nav_menu`/`wp_update_nav_menu_item` references in `converter/` today — it's single-page-PATCH. Writing a menu term + N menu-items + N CPT posts then back-wiring real IDs before the header part is walked is a NEW ordered multi-pass write. §16's idempotency fold-in assumes an orchestration that doesn't exist. **Fix:** FR-36-15 gets its own mini-design (pre-pass vs post-pass) before Phase-2 build.

### SINGLE-VOICE BUT VALID (fact-checked)
- **Fact-check error in MY spec (wp-dev MF-a):** §17 cites `class-product-templates-cpt.php:70` as proof `show_in_nav_menus` works — but that file sets it to **FALSE** (a CPT excluded from menus). The WP mechanism is sound, but the citation is wrong. **Fix:** correct the citation + run the actual spike before Phase 2.
- **`templateLock:"contentOnly"` is UNBUILT** (wp-dev, 0 hits) but cited as reused — flag it "first application in SGS", like ResponsiveTriStateControl.
- **Spec 33 Part 2 is stale** (PM MF3) — still points at the old blocks; repoint it + validate schema conformance BEFORE the Phase-1 converter proof (a hidden Phase-1 blocker). Add to companions + STOP-CATALOGUE.
- **Live-site cutover has no rollback** (PM MF4) — old nav is LIVE on Mama's + Indus; FR-36-18 deletes the old blocks with no revert. **Fix:** regression gate (old attrs representable in new) + 7-day dormant rollback + optional shadow-run.
- **Name D323 + D340 as fixes to carry forward** (wp-dev MF-d) — the drawer's body-reparent (transform-ancestor escape) + scrollbar-bounce compensation must be explicitly ported, not re-derived, or they regress.
- **FR-36-16 Indus mega count self-contradicts** (Spec-Lawyer M1) — "1 mega" asserted, then "1-vs-5 unresolved". **Fix:** state as TBD-via-arbitration; delete the confident "1".
- **§8 gates on Spec 34's elementFromPoint baseline** (Spec-Lawyer M9) — but this doc DELETES Spec 34. Carry the methodology into §8 verbatim before the purge (D101).
- **"R5 tension" (FR-36-24) is an undefined reference** (Spec-Lawyer M3) — my working shorthand leaked in; inline it or cite the 07-19 phase-1 report.
- **`NAV_BLOCK_NAMES` hardcoded + `sgs/nav` block-name vs store-namespace collision** (Cynic M3) — the block is `sgs/nav-menu`; `sgs/nav` is the store namespace. §17 wording conflates them. **Fix:** canonical slug = `sgs/nav-menu`; route detection off the DB, not a literal.
- **Mega transient cache stale-key + client-authored-CPT lifecycle** (Cynic M4) — key the cache on panel-modified + theme-snapshot version + global-styles hash; state how a client's hand-built mega survives a framework contract change under no-deprecations.
- **Mega click-target undefined** (Support a, wp-dev b) — resolve the mega by `object_id` (`get_post()`), never `$item->url`; force-publish the CPT on save; state what the top-level "Brands" link's href resolves to.
- **Menu bar/drawer override mechanism unspecified** (Spec-Lawyer M2) — pin the attribute (`menu:inherit-from-bar` default + explicit override).
- **Reposition the real moat** (Competitor MF-E) — "AI builds your whole nav from a sitemap" (the one un-copyable weapon) is buried in Phase 3; foreground it.

## Fact-check results (Bean-requested — what I verified before presenting)
| Claim | Verdict |
|---|---|
| Cart "extend, ~90% ready" | **OVERSTATED** — cart is a badge+link only; mini-drawer unbuilt. Real build. (Verified in render.php + block.json + plugin build-table.) |
| Search "extend" | **HONEST** — the ARIA combobox IS shipped live (`product-search`). Genuinely extendable. (Corrects Ship-PM's "cart AND search both dishonest".) |
| §17 `show_in_nav_menus` citation | **WRONG FILE** — cited a CPT set to FALSE. Mechanism sound; citation must be fixed + spiked. |
| `templateLock:contentOnly` "reused" | **UNBUILT** — 0 hits; it's a first application. |
| aria-current client-side "assumes LiteSpeed" (PM MF5) | **MOOT** — LiteSpeed IS confirmed active on the live sites (qc-council + dev-setup). The amend is correct, not speculative. |
| Cynic "structured-data-once will drift across two render paths" | **DOWNGRADED** — the mechanism EXISTS (SGS social/business-info blocks read one Site-Info option, placed in both header + footer). Needs the binding STATED, not a redesign. Not a fatal drift. |
| Converter multi-artefact emit is "new orchestration" | **CONFIRMED** — 0 nav_menu handling in converter today. |
| Spec 33 Part 2 stale | **CONFIRMED** — points at old blocks; a real pre-Phase-1 blocker. |

## GO / NO-GO
**GO on the architecture (unanimous). NOT build-ready as written.** Close a **Phase-3.5 revision** of Spec 36 v2 — all paper edits: (1) phase §14–17 + per-piece v1-lines; (2) relabel cart as a build / search as a real extend; (3) fix the §1↔§14 ownership contradiction + split FR-36-24 + add the responsive-control lint gate; (4) define failure states + minimal Nav Health in Phase 1/2; (5) give the converter emit its own sub-design + name the Spec 33 Part 2 repoint as a pre-Phase-1 blocker; (6) fix the fact-check errors (show_in_nav_menus citation, templateLock flag, R5 ref, NAV_BLOCK_NAMES/store collision, Indus count, Spec-34 baseline carry-forward); (7) add cutover rollback + name D323/D340. No architecture change.
