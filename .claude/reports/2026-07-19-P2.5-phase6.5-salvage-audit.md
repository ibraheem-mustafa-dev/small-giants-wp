# P2.5 Phase 6.5 — Code Salvage Audit (Spec 36 v2.1)

**Date:** 2026-07-19
**Method:** read-only, 4 parallel wp-sgs-developer (Sonnet) audits, feature-by-feature vs Spec 36 v2.1, every claim evidenced to `file:line`. NO deletions — deletions are Phase 6 (needs Bean's second go).
**Gate 0:** Spec 36 v2.1 SIGNED-OFF (2026-07-19).

## Headline
Spec 36's own §8a "Verified build notes" + §4 built-vs-to-build honesty held up under audit — **every cited `file:line` claim CONFIRMED** (minor corrections listed below). Nothing was over-sold. The thin-bar warnings were accurate: the cart is a badge-only shell, the mini-cart is unbuilt; search's combobox IS genuinely shipped.

## Live roster (from `/sgs-db`)
- **RETIRE (reference-only, salvage snippets):** `sgs/adaptive-nav`, `sgs/mega-menu`
- **Rebuild same slug (D270):** `sgs/nav-menu` (617-line render.php — a full rewrite, read-before-rebuild)
- **EXTEND:** `sgs/cart`, `sgs/product-search` (primary), `sgs/responsive-logo`, `sgs/social-icons`, `sgs/business-info`
- **Build-new (absent — correct):** `sgs/nav-drawer`, `sgs_mega_menu` CPT
- **Out of scope:** `sgs/filter-search` (attribute-chip filter nested in `woocommerce/product-filter-attribute`, NOT a header search)

---

## 1. Nav core — `adaptive-nav`, `mega-menu`, `nav-menu`

| Block | Verdict | Reuse | Discard |
|---|---|---|---|
| `sgs/adaptive-nav` | RETIRE, salvage snippets | drawer JS mechanics (view.js); collapse-tier breakpoint math (render.php:336-376, reads `SGS_Breakpoints`, no hardcoded 768/1024) | edit.js/save.js/block.json, composite wiring |
| `sgs/mega-menu` | RETIRE, salvage snippets | Interactivity-API keyboard-nav store (view.js:79-333); `repositionPanel` CSS-var-only overflow (view.js:31-77) | render.php scoped-CSS (attr-entangled), edit.js |
| `sgs/nav-menu` | Rebuild same slug (D270) | Already Spec-32-clean (no-inline, WCAG-computed hover/current-page). Read before rebuild; code itself discarded per D270 | — |

**Salvage snippets — PORT VERBATIM (highest value, FR-36-6/-7):**
| Snippet | Location | Note |
|---|---|---|
| **D323 body-reparent** | `adaptive-nav/view.js:108-117` (`reparentToBody`) | Drawer reparents to `<body>` to escape transformed/filtered ancestor. CONFIRMED. Port verbatim into `sgs/nav-drawer`. |
| **D340 scroll-lock + scrollbar-bounce** | `adaptive-nav/view.js:285-305` (`lockScroll`) + `312-324` (`unlockScroll`) | Compensates classic-scrollbar vanish mid-animation (frame capture showed anchor 753→768 @768px). CONFIRMED. Port as a pair. |
| Selective `inert`/`aria-hidden` freeze walk | `adaptive-nav/view.js:208-261` | Emergent focus-trap (FR-34-1), restores exact prior state. |
| Non-modal `<dialog>.show()` open/close/scrim/ESC skeleton | `adaptive-nav/view.js:119-192` | Drawer open/close orchestration — rename BEM selectors. |
| One-menu-source resolver | `includes/class-sgs-nav-menu-source.php` (whole, 210 lines) | Reuse as-is; **edit `NAV_BLOCK_NAMES` at L44** (currently `['sgs/adaptive-nav','core/navigation']`) to the new nav block slug or the resolver silently stops finding the menu. |

**Claim D correction (IMPORTANT):** do NOT port the disclosure/focus-trap/keyboard-nav layer from BOTH blocks — `adaptive-nav` and `mega-menu` already have two independent implementations that **disagree on `FOCUSABLE_SELECTOR` and Tab-handling**. Merge into ONE shared `store('sgs/nav')` utility (FR-36-7). Port the drawer/scroll-lock cluster verbatim; RE-DERIVE the focus/keyboard layer merged.

**Corrections:** nav-menu render.php is **617 lines** (spec said ~600). Its docblock (render.php:11-18) cites `SGS_Adaptive_Nav_Renderer::render_drawer_*` L328-640 which **no longer exists** (removed at FR-34-4, 2026-07-15) — stale provenance, doesn't affect rebuild.

---

## 2. Cart — `sgs/cart` (FR-36-19)

| FR-36-19 MUST feature | Status | Evidence |
|---|---|---|
| Live count badge | BUILT | `view.js:141-144` `refreshCart()`; badge `render.php:203-208` |
| Store-API hydration (NOT cart-fragments) | BUILT | `view.js:67-90` `GET /wc/store/v1/cart`; no `wc-cart-fragments` anywhere |
| Mini-cart preview (thumb/name/qty/line-price) | TO-BUILD | no per-item markup; `displayMode:'drawer'` enum is an inert stub |
| Inline qty-edit + remove | TO-BUILD | view.js only does `GET` — no POST/PUT/DELETE |
| View-Cart + Checkout CTAs | TO-BUILD | only the single trigger `<a>` |
| Subtotal + shipping | TO-BUILD | only reads `items_count` |
| Empty-cart state (in drawer) | PARTIAL (badge-level only) | `showZero`/`hideWhenEmpty` control the badge, not a drawer message |
| Keyboard + focus + ESC (drawer) | TO-BUILD | no drawer, no keydown listener |

**Verified claims:** all CONFIRMED. **Phase-1 one-line fix (the single verified gap):** add `role="status"` to the badge `<span>` at **`cart/render.php:203`** (already has `aria-live="polite" aria-atomic="true"`).
**Salvage:** keep the badge shell + WC-active gate + Store-API hydration + scoped-CSS. Build the entire mini-cart drawer fresh (Phase 2) — nothing to salvage there.

---

## 3. Search — `sgs/product-search` (FR-36-20)

| FR-36-20 feature | Status | Evidence |
|---|---|---|
| Full WAI-ARIA combobox (role/expanded/controls/activedescendant/listbox/option) | **BUILT** | `render.php:227-272` + `view.js:64-186` — every ARIA attr present, dynamic state correctly in JS |
| No-JS `<form role="search">` fallback (real results) | BUILT | `render.php:206-239` |
| Debounce | BUILT | `view.js:214` (300ms) |
| Keyboard nav (arrow/Enter/Esc, focus retention) | BUILT | `view.js:223-264` |
| Live result-count region (`role="status"`) | BUILT | `render.php:274-281` + `view.js` `announce()` |
| Result cap | PARTIAL | single `maxResults` attr, not device-tiered (spec wants ≤10 desktop / 4-8 mobile) |
| displayMode overlay (full-screen dialog) | PARTIAL (2 of 3) | `inline` + `icon-expand` built; `full-screen-overlay` (dialog) TO-BUILD |
| Product preview thumbnail | BUILT | `view.js:170-177` |
| Product preview price | TO-BUILD | no price element/payload field |
| Highlight matched substring | TO-BUILD | `view.js:181` raw `textContent` |
| Default-unscoped + scope option | TO-BUILD | hardcoded `post_type=product`, no scope attr |
| Recent searches | TO-BUILD | no storage |

**Verdict:** genuine EXTEND confirmed — the combobox core needs no rebuild. Layer the 6 additions on top without touching the ARIA/keyboard wiring.

---

## 4. Logo / Social / Business-info (FR-36-22/-21/-23) — all keep-and-extend

**`sgs/responsive-logo` (FR-36-22):** BUILT — link-to-home default, **separate per-device image files** (not resize-only), SVG upload with `wp_kses` sanitise. TO-BUILD — per-tier max-width/height (single desktop `width` today), sticky-header compact-mark swap (no scroll-state hook), left-align default. PARTIAL — functional alt lacks "home" suffix + doesn't distinctly drive the `<a>` aria-label. Phase-3 SHOULDs (transparent/dark variant, shrink-on-scroll, lockup, favicon-sync) all net-new.

**`sgs/social-icons` (FR-36-21):** BUILT — curated set, size/shape/spacing, keyboard+focus+44px, **site-info single-source toggle** (same store as business-info). PARTIAL — accessible name auto-generated + editable but bare platform name not "verb+platform"; glyph SVG lacks `aria-hidden`; `rel`/`target=_blank` hardcoded (no `nofollow` toggle, no override). TO-BUILD — custom-SVG platform slot, brand-colour mode, drag-to-reorder.

**`sgs/business-info` (FR-36-23):** strongest of the three. BUILT — mailto descriptive, **single Site-Info source of truth** (all fields via `Sgs_Site_Info::get()`), **block emits NO schema itself** (verified: 0 `LocalBusiness`/`ld+json` in render.php; owned by `class-org-website-schema.php`), `labelCollapse` reuse + 44px. PARTIAL — `tel:` strips to digits+plus but not true E.164; click-to-map is a separate `map` displayType, the `address` item itself isn't clickable (URL-building already exists at `render.php:341-342` to reuse).

---

## Spec FRs to mark "already built (salvaged)"
- **FR-36-7** — shared plumbing: D323/D340 + freeze exist (port verbatim); focus/keyboard layer merge-not-port.
- **FR-36-19** — cart badge + Store-API hydration BUILT; only `role="status"` + the whole mini-cart remain.
- **FR-36-20** — combobox, no-JS fallback, debounce, keyboard, live-count region all BUILT (genuine EXTEND).
- **FR-36-22** — per-device image upload + SVG sanitise + link-home BUILT.
- **FR-36-23** — single Site-Info source + schema-non-duplication + `labelCollapse` BUILT.
- **FR-36-25** (structured-data-once) — partially real already: `source:site-info` toggle live on social + business-info.

## Menu-source detection fix (build-time, R-31-1)
`SGS_Nav_Menu_Source::NAV_BLOCK_NAMES` (`class-sgs-nav-menu-source.php:44`) is a hardcoded PHP const — spec §8a says route it DB-first, not a const. Confirmed hardcoded. Address at build.
