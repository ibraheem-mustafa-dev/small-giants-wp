---
doc_type: report
title: Rule 21 (render-without-control) — full triage of all 211 findings
date: 2026-08-27
status: COMPLETE — every finding classified; no finding left unverdicted
method: .claude/THE-MIGRATION-METHOD.md Step 7b
---

# Rule 21 triage — 211 findings, all classified

**`21-render-without-control` means: the block paints something the client cannot change.**
It is the most client-facing of the scanner's rules and it states CLAUDE.md's own standard as a
number — *"no block feature is complete until it has full block-editor UI controls"*.

## Verdicts — the counts sum to exactly the measured FLAGGED population

| Verdict | Count | What it means |
|---|---:|---|
| **DETECTOR BUG — fixed** | **128** | The control exists and works; the scanner could not see it. Fixed in `7f3ad8ae4`. |
| **REAL** | **82** | Genuinely no control. The client is stuck with the `block.json` default. |
| **FALSE POSITIVE — known limit** | **1** | `sgs/nav-drawer.variantPreset` is client-settable via the block VARIATION picker (`variations.js`), which the rule does not model as a control surface. |
| **Total** | **211** | Matches the measured FLAGGED count before the fix. |

⚠ **Quote 211, never 222.** 222 is FLAGGED + BASELINED; the scanner's `--json` serialises both
while the exit code counts only FLAGGED.

## The 128, and why they were not defects

`ContainerWrapperControls` is a **table-driven dispatcher**: it reads `KIND_PANELS[ kind ]` and
INVOKES each panel as a function. The scanner's recursion followed only JSX tags, so it dead-ended
and reported every attribute those panels own as uncontrolled, across all 16 blocks mounting it.

Three earlier explanations were checked and **disproved** before this one — it is not
`core/components.js` (it indexes the facade correctly), not `exportBody` failing on a default
export (it returns a clean 816 chars), and the facade's six `contentWidth` mentions are all
**comments**, which the source cache strips. Full mechanism: rule 21's own `advisoryReason`.

Measured before/after, keyed by finding key: **211 -> 83, 128 cleared, ZERO new, and no other rule
moved** (all 24 checked). Guarded by fixture `control-via-dispatcher-table`, written and watched
FAILING first.

## The 82 REAL findings — the worklist

⛔ **This is NOT a build instruction.** Each REAL finding means a new client-facing control, which
is Step 3 of the method: settle the shape with Bean on ONE instance before building any of them.

Independently verified: for all 82, the attribute appears nowhere in that block's own control
surface (its `edit.js` and its own `components/`), which agrees with the rule.

### By block, most-affected first

| Block | Count | Attributes |
|---|---:|---|
| `sgs/site-footer` | 16 | `alignContent`, `flexDirection`, `flexWrap`, `shapeDividerBottom`, `shapeDividerBottomColour`, `shapeDividerBottomColourGradient`, `shapeDividerBottomFlip`, `shapeDividerBottomInvert`, `shapeDividerBottomScale`, `shapeDividerTop`, `shapeDividerTopColour`, `shapeDividerTopColourGradient`, `shapeDividerTopFlip`, `shapeDividerTopInvert`, `shapeDividerTopScale`, `tagName` |
| `sgs/hero` | 11 | `alignContent`, `borderColourHover`, `borderColourHoverGradient`, `flexDirection`, `flexWrap`, `gridAutoRows`, `gridTemplateRows`, `justifyContent`, `justifyItems`, `transitionDuration`, `transitionEasing` |
| `sgs/heading` | 8 | `boxShadow`, `boxShadowHover`, `customWidth`, `customWidthUnit`, `scaleHover`, `textWrap`, `transitionDuration`, `transitionEasing` |
| `sgs/text` | 8 | `boxShadow`, `boxShadowHover`, `customWidth`, `customWidthUnit`, `inheritStyle`, `scaleHover`, `transitionDuration`, `transitionEasing` |
| `sgs/card-grid` | 6 | `grayscaleHover`, `imageZoomHover`, `scaleHover`, `staggerDelay`, `transitionDuration`, `transitionEasing` |
| `sgs/media` | 6 | `captionFontSize`, `captionFontSizeUnit`, `heightUnit`, `maxHeightUnit`, `maxWidthUnit`, `order` |
| `sgs/team-member` | 5 | `grayscaleHover`, `imageZoomHover`, `scaleHover`, `transitionDuration`, `transitionEasing` |
| `sgs/cta-section` | 4 | `backgroundColour`, `backgroundColourHover`, `transitionDuration`, `transitionEasing` |
| `sgs/info-box` | 4 | `grayscaleHover`, `scaleHover`, `transitionDuration`, `transitionEasing` |
| `sgs/buybox` | 3 | `decoyEnabled`, `framingMode`, `showLadder` |
| `sgs/gallery` | 3 | `grayscaleHover`, `shadowHover`, `staggerDelay` |
| `sgs/form` | 2 | `rateLimit`, `requireLogin` |
| `sgs/form-field-file` | 1 | `uploadText` |
| `sgs/form-field-tiles` | 1 | `selectedStyle` |
| `sgs/icon-list` | 1 | `dividers` |
| `sgs/product-card` | 1 | `showPickers` |
| `sgs/testimonial` | 1 | `staggerDelay` |
| `sgs/trustpilot-reviews` | 1 | `reviewsAverage` |

### By attribute family — where one decision covers many blocks

The single largest theme is **motion and hover**. Settling ONE shape for it covers 28 of the 82.

| Attribute | Blocks affected |
|---|---:|
| `transitionDuration` | 7 |
| `transitionEasing` | 7 |
| `scaleHover` | 5 |
| `grayscaleHover` | 4 |
| `staggerDelay` | 3 |
| `imageZoomHover` | 2 |
| `boxShadow` | 2 |
| `boxShadowHover` | 2 |
| `customWidth` | 2 |
| `customWidthUnit` | 2 |
| `alignContent` | 2 |
| `flexDirection` | 2 |
| `flexWrap` | 2 |

## Recommended order, by client impact

1. **Motion + hover (28 findings, 8 blocks)** — `transitionDuration`, `transitionEasing`,
   `scaleHover`, `grayscaleHover`, `staggerDelay`, `imageZoomHover`. One shared panel decision
   covers a third of the worklist. Highest leverage by a wide margin.
2. **`sgs/site-footer` (16)** — the single most-affected block, and it appears on every page.
3. **`sgs/hero` (11)** — the most client-visible block on a landing page.
4. **`sgs/heading` + `sgs/text` (16)** — the same shape twice; one decision covers both.
5. **The long tail (11 blocks, 1-6 each)** — includes genuine configuration attrs
   (`sgs/form.requireLogin`, `.rateLimit`; `sgs/buybox.showLadder`, `.framingMode`, `.decoyEnabled`)
   that may be deliberate developer-only settings rather than missing controls. Confirm intent
   before building.

## The 1 known limit

`sgs/nav-drawer.variantPreset` is reachable through WordPress's **block variation picker** —
`variations.js` sets it with `isActive: [ 'variantPreset' ]`. The client CAN change it, so the
rule's claim is false. The fix is definitional (does `variations.js` count as a control surface?)
and it is a single finding, so it is recorded here rather than acted on unilaterally.
