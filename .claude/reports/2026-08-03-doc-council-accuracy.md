# QC Council — Rater A (Factual Accuracy) — 2026-08-03

Read-only audit against code, DB, and `gh api`. Scope: factual accuracy only — a second rater
covers internal consistency, so cross-doc contradictions are flagged only where they also carry a
factual error.

## Findings table

| # | Claim | Source | Verdict | Evidence |
|---|---|---|---|---|
| 1 | `gl-transitions`: "124 MIT, 1 BSD-3" | gap-register.md:43 | **WRONG** | Enumerated all 125 files in `transitions/` via `gh api repos/gl-transitions/gl-transitions/contents/transitions` and read each file's `// License:` header comment. Actual split: **123 MIT, 1 BSD-3-Clause (`InvertedPageCurl.glsl`), 1 BSD-2-Clause (`StereoViewer.glsl`)**. The register's own count of 125 files is right; the licence split is wrong on two counts — miscounts MIT (124 vs 123) and misses the BSD-2-Clause file entirely. Repo-root `LICENSE` is MIT (covers the harness, not necessarily each ported shader — the per-file headers are the correct signal, which the register rightly used, but read wrong). |
| 2 | OGL: "✅ Unlicense (public domain)" | gap-register.md:42 | **CORRECT-BUT-MISLEADING** | `gh api repos/oframe/ogl --jq .license.spdx_id` (the exact command the task specified) returns **empty/null** — GitHub's licence detector finds nothing because **there is no LICENSE file in the repo at all** (`gh api repos/oframe/ogl/contents` lists only `.gitignore .npmignore README.md examples package.json src types` — no LICENSE). The "Unlicense" claim traces only to a self-declared `"license": "Unlicense"` string in `package.json`, not to an actual licence grant text in the repository. This is legally weaker than the register implies — worth a caveat before Bean ships it as a settled fact to a paying client, though the maintainer's declared intent is genuinely Unlicense. |
| 3 | GSAP: "NOT MIT... SPDX `NONE`" | gap-register.md:21 | **CORRECT** | `gh api repos/greensock/gsap --jq .license.spdx_id` returns null/empty (GitHub detects no OSI licence). Matches the register's own characterisation. Did not independently re-verify the exact wording of the Prohibited Uses clause quote (no web-fetch tool in this dispatch) — flagging as **UNVERIFIABLE within this session's toolset**, not wrong. |
| 4 | LYGIA: "`NOASSERTION` / Prosperity" | gap-register.md:41 | **PARTIALLY VERIFIED** | `gh api repos/patriciogonzalezvivo/lygia --jq .license.spdx_id` returns `NOASSERTION`, confirming that half. Did not independently confirm "Prosperity" as the specific licence family (no web-fetch access this session) — flag as UNVERIFIABLE for that specific sub-claim. |
| 5 | Tier W byte figures: three.js 182KB, pixi 251KB, OGL 34KB (gzip) | gap-register.md (via Spec 38 §1) / Spec 38 lines 114 + plan §10 | **CORRECT** | Live bundlephobia API: three.js `gzip:182364` (0.185.1), pixi.js `gzip:251255`, ogl `gzip:34177` (1.0.11). All three match to the KB. |
| 6 | `audio/view.js:75-76` — `fftSize`/`smoothingTimeConstant` hardcoded | gap-register.md:67 | **CORRECT** | `plugins/sgs-blocks/src/blocks/audio/view.js:75` = `analyser.fftSize = 512;`, line 76 = `analyser.smoothingTimeConstant = 0.8;`. Citation resolves exactly. |
| 7 | D452 fx-morph: fix committed, unverified live | gap-register.md:69 | **CORRECT** | `.claude/decisions.md:1322-1356` — D452 documents the proven cause (attrs on `<svg>` not `<path>`) and states explicitly "⚠ OUTSTANDING: the fix is unverified... no live morph has yet been observed." Register's claim matches the decision log verbatim. |
| 8 | D451 motion-path: status "unknown" | gap-register.md:70 | **STALE WORDING, SAME CONCLUSION** | `.claude/decisions.md:1358-1394` — D451 is not "unknown", it is a specifically documented state: cause proven, harness-verified fix, but explicitly "⚠ OUTSTANDING: not verified on the live canary." The register's one-line summary ("current status unknown") undersells what's actually recorded — the status IS known (fix built + harness-proven, live-unverified) — but the actionable conclusion (needs live verification) is the same. Minor imprecision, not a wrong finding. |
| 9 | Snooza matrix: 4 sizes × 6 colours × 3 headrest = 72 | `sites/snooza-chair/CLAUDE.md:26-29` | **ARITHMETIC CORRECT** | 4×6×3 = 72. Not independently checkable against a manufacturer source in this dispatch (no web-fetch), but the arithmetic on the stated axes is right and matches the build plan's superseded-box table. |
| 10 | `class-product-manifest.php` "only models WooCommerce variations... zero concept of a separately-priced, independently-toggleable add-on" | build-plan.md:36, §5 row, §6 | **CORRECT** | Read `plugins/sgs-blocks/includes/class-product-manifest.php` in full (492 lines) — no `addon`/`add_on`/`accessory` token anywhere; the file's only concerns are variation axes, tax fingerprinting, and stock/price resolution via `wc_get_product()`. Claim holds. |
| 11 | Reference images: `assets/product-images/` "does not exist... path corrected 2026-08-03" — files now live directly in `assets/` | `sites/snooza-chair/CLAUDE.md:51-60` | **WRONG — and more expensive than the incident it claims to fix** | `ls sites/snooza-chair/assets/` shows the directory holds 19 files: 13 Wix-CDN hash-named images (`bc3963_...~mv2.jpg/png`), `image-shortlist.md`, `sam-test.png`, `segment_chair.py`, and a `3d-model/` subfolder with `snooza-chair-blue.glb`, `snooza-chair-web.glb`, `optimise-chair.py`. **None of the specific filenames the CLAUDE.md names** — `ophir-01` through `ophir-14`, `ophir-video.mp4`, `video-still-001` through `video-still-203`, `fortuna-pink/blue/green/orange.jpg`, `fortuna-legrest/pommel/tray/headrest.jpg` — **exist anywhere in the tree**, under this path or the old one. The "19 files" count is coincidentally right; the file list under it is fabricated. The doc corrected the PATH but not the CONTENTS, so it's now a more convincing trap than the original absent-directory error, because the directory genuinely exists and genuinely has 19 files — just not these ones. |
| 12 | Build plan §7: "a `Glob` for `sites/snooza-chair/assets/product-images/*`... returned zero files" | build-plan.md:106 | **CORRECT, AND THE ONLY ACCURATE VERSION OF THE CLAIM** | Confirmed no `product-images/` subdirectory exists. This is the correct, narrower claim (path absent) — it does not repeat the CLAUDE.md's error of asserting specific filenames now exist elsewhere, and Open Question 2 in the same plan (`build-plan.md:165`) correctly leaves this unresolved rather than asserting a fix. |
| 13 | `image-shortlist.md`'s own filenames (`product-images/fortuna-blue.jpg`, `ophir-01-main.png`, `curated-cropped/seg1_0201.png`, etc.) | `sites/snooza-chair/assets/image-shortlist.md` | **STALE (source of the CLAUDE.md's fabrication)** | This file, dated 2026-03-31, is the origin of every filename the CLAUDE.md lists as "corrected." None of its named files or subdirectories (`product-images/`, `curated-cropped/`) exist in the current `assets/` tree. This file itself is now stale and should not be trusted as a live pointer either. |
| 14 | Accessory count: CLAUDE.md's corrected table lists **8** accessories; build-plan §6 body text (below the superseded box) still lists **10**, including "Snooza Lite" and "Profile Headrest" (the latter now a variant axis, not an accessory, per the correction) | `sites/snooza-chair/CLAUDE.md:34-43` vs `build-plan.md:85` | **STALE cross-reference, factual not just consistency** | Flagging despite the consistency/accuracy split, because it's a count Bean will act on: the build plan's own superseded-box (lines 3-15) acknowledges the correction at the top of the file but §6's body text three sections down was never swept, so a reader who skips the box still sees 10 accessories including one that no longer belongs in that list. |
| 15 | Register §0's Prohibited Uses quote wording (Webflow-competitor clause) | gap-register.md:24-25 | **UNVERIFIABLE this session** | No web-fetch/WebSearch tool available in this dispatch to pull GSAP's actual licence text. Flagging as unverifiable rather than guessing — do not treat this as either confirmed or refuted. |
| 16 | GSAP "went free April 2025 (not 2026)" | gap-register.md:34-35 | **UNVERIFIABLE this session** | Same tool gap — no web access to check Webflow's own announcement date. |

## Not checked (out of budget / no tool access)
- FR-38-27 physics-canvas block existence/behaviour — not read this session; Spec 38 lines 156, 552, 971 cite it consistently within the spec but I did not check for a corresponding `sgs/physics-canvas` source file.
- The `curtainsjs`, `model-viewer`, `detect-gpu`, `PavelDoGreat/WebGL-Fluid-Simulation`, `hover-effect`, `particlesGL`, `react-lenis` licence rows in Section 0's table — not independently re-verified via `gh api`; time budget was spent on the three highest-stakes items (gl-transitions, OGL, GSAP) plus the Snooza asset claim, per the "rank by cost of acting on it wrong" instruction.

## Ranked by cost of acting on it wrong

1. **Finding #11 (Snooza reference images fabricated)** — highest cost. This is a "corrected" doc that is now MORE dangerous than the error it replaced: the directory exists, the count (19) coincidentally matches, so a fast reader has no signal to doubt it. If Track 1 (the pitch-demo model, on a fixed meeting date) proceeds on this doc without re-opening `image-shortlist.md` or Globbing the real directory, the model-maker gets handed filenames that don't exist. The build plan's own §7/Open-Question-2 already flags the gap correctly — the CLAUDE.md should defer to that, not assert a false fix.
2. **Finding #1 (gl-transitions licence split wrong)** — second highest. Bean ships to paying clients; a licence attribution error in a document meant to be the definitive per-file compliance record is exactly the kind of error the task called "the most expensive error here." Both the undercount of MIT files and the missed BSD-2-Clause file need fixing before this register is used to clear `gl-transitions` for client-site use.
3. **Finding #2 (OGL "Unlicense" claim rests on package.json only, no LICENSE file)** — moderate cost. Directionally correct (maintainer intends public domain) but the register asserts it with more certainty ("✅") than the evidence supports. Worth a one-line caveat, not a reversal.
4. **Finding #14 (accessory count 8 vs 10 stale cross-reference)** — moderate cost, cross-doc — flagged here because it affects a real number Bean or a build session will act on for effort-sizing.
5. **Findings #7, #8** — correct/near-correct, no action needed beyond the register's own existing caution note (which it already carries, appropriately).

## What I did NOT find wrong (of what I checked)
Tier W byte figures (three.js/pixi/OGL), the `class-product-manifest.php` add-on-gap claim, the `audio/view.js` line citation, the 72-SKU arithmetic, D452/D451 as recorded in decisions.md, and GSAP's SPDX-null status all held up under direct verification.
