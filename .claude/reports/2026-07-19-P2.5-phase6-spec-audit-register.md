---
doc_type: consolidation-audit
project: small-giants-wp
track: TRACK 2 — Header/Footer/Nav full rebuild (P2.5)
phase: Phase 6 — spec-wide nav-reference audit (find → analyse → decide placement → purge map)
date: 2026-07-19
status: analysis complete — PURGE NOT YET EXECUTED (waits on the Phase-4 unified spec = the consolidation home + Bean review)
scope: all 25 files in .claude/specs/ (+ archive/, design-brain/) + cloning-pipeline-flow/-stages
---

# P2.5 Phase 6 — nav-content consolidation register

## ⛔ 0. CRITICAL — a prior nav spec + architecture already exists (Spec 36 + the 07-18 Phase 3)
- **`.claude/specs/36-SGS-NAVIGATION-SYSTEM.md` (v1.3, DRAFT) already exists**, derived from a **2026-07-18**
  Phase-3 nav architecture — a **3-block roster** (`sgs/nav-menu` orchestrator+renderer combined +
  `sgs_mega_menu` CPT + `sgs/nav-drawer`).
- This session's **2026-07-19** Phase-3 architecture deliberately re-decided with "ZERO influence from the
  previously-planned shape" (Bean's P2.5 lock) → a **5-block roster** (`sgs/nav` + `sgs/nav-menu` +
  `sgs/mega-panel`+CPT + `sgs/nav-drawer` + `sgs/nav-cart`). The Phase-5 council passed it "GO on direction".
- **So Spec 36 v1.3 is itself a SUPERSEDED scattered doc, NOT the finished consolidation home.** Do NOT purge
  into it as-is (it's on the older 3-block roster). This is consistent with Bean's "decide fresh" lock — the
  07-19 architecture supersedes the 07-18/Spec-36 one — but it MUST be handled, not silently left.
- **Bean decision needed + Phase-4 tasks:** (a) mark Spec 36 v1.3 `SUPERSEDED` now (so nobody builds its
  3-block roster); (b) when writing the Phase-4 unified spec, RECONCILE against Spec 36 v1.3 — salvage its
  further-developed FRs (FR-36-8 collapse modes, FR-36-10 disclosure-not-menu ban, FR-36-14 controls,
  FR-36-15 converter repoint, FR-36-16 QC gate) onto the 5-block roster; (c) the unified Phase-4 spec becomes
  the canonical home (rewrite Spec 36, or a new spec id) — THEN run the purge.

## 1. Whole-doc verdicts
| Spec | Verdict | Action |
|---|---|---|
| **34** Adaptive-nav drawer | **nav-CENTRAL — DELETE entirely** | Salvage FR-34-5 drawer-settings checklist + FR-34-4 extraction-source file pointers FIRST |
| **02-SGS-BLOCKS.md §23** old mega-menu | **DELETE the section** (conflicts: uses `role=menu` + template-part panels, both banned/replaced) | Salvage the "Replaces Max Mega Menu/JetMenu/Kadence Pro" competitive line + keyboard-nav table first |
| **17** §S9 | **STRIP the nav FRs, KEEP header/footer** | Strip S9-1 hook clause / S9-2 palette entry / S9-4 / S9-5 / S9-8 / S9-10 nav refs / S9-11 pointer → new spec; keep site-header/site-footer/rows/Site-Info/scroll/responsive-model |
| **33** Part 2 | strip emit-target block names (2-3 lines), repoint | to the final 5-block roster |
| **31** §13 | NO strip | ADD MF3's new converter "create-then-reference" multi-post stage as a genuine Spec 31 addition |
| **35** | no real nav content (controls it anticipated live generically + are reused by reference) | no strip |
| **00 / 01 / 02-REFERENCE / 06 / 29** | minor pointer/roster updates, mostly POST-BUILD not purge-time | 00 §2.1 + FR-S9-1 hook allow-list need the new block-name roster; 02-REFERENCE auto-regens via `/sgs-update`; 29 roster rows + 01 mega template-part file list = post-build cleanup |
| **36** | **NOT the target — see §0** | Bean decision before any purge |
| everything else scanned | zero / incidental nav content | no action |

## 2. Features that would be LOST — carry into the Phase-4 unified spec (highest priority)
1. **FR-34-5 drawer settings checklist** (`drawerBg`/`toggleOpenColour`/`drawerAlign`/`drawerGap`/`drawerPadding`)
   — the ONLY itemised drawer-styling control list anywhere; not in Spec 36 or the 07-19 architecture.
2. **The modality supersession chain** FR-S9-5 → Spec 34 §3 (non-modal freeze) → DP4 (full modal) — three
   amendments on one requirement, never reconciled. (Independently confirms gate-register MF1.)
3. **FR-S9-8 per-ELEMENT move-to-drawer** vs the 07-19 per-ROW-only model — unresolved narrowing.
   (Independently confirms gate-register MF6.)
4. **Spec 36 v1.3 is on the superseded 07-18 roster** — §0. Biggest risk to the whole purge.
5. **MF3 converter multi-post orchestration** — new architectural surface (converter is single-page-PATCH
   today); currently only a council-register bullet, must land as a real Spec 31 §13 addition.
6. **02-SGS-BLOCKS §23 competitive positioning** ("Replaces Max Mega Menu / JetMenu / Kadence Pro; zero
   external dependencies") — good marketing framing that exists nowhere else.

## 3. DB / hook housekeeping (schedule with the build, not the purge)
`no-header-footer-block.py` allow-list (FR-S9-1) + `00-naming-conventions.md` §2.1 need the new block roster ·
`29`'s container-KIND roster rows (adaptive-nav/mobile-nav) need replacing · `01`'s mega template-part `.html`/
`.php` file list needs cleanup once the CPT ships · retire adaptive-nav/mega-menu/mobile-nav DB rows via `/sgs-update`.

## 4. Cross-check value
The audit INDEPENDENTLY re-found gate-register MF1 (modality) and MF6 (per-element move-to-drawer) from the
docs side — a good convergence signal that those are real. And it caught the Spec-36-is-superseded risk the
council (reviewing only the 07-19 doc) could not see.
