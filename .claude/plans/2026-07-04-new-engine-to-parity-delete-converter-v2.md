---
doc_type: phase-plan
project: small-giants-wp
title: New modular converter → parity → delete converter_v2 (the completion plan)
spec: 31 (§2 core mechanism, §12.6 remaining-work, §13.3 FR-31-2.6)
generated: 2026-07-04
status: DRAFT — NOT agreed. Blocked on THREE validation gates (below) before Bean approval.
---

# Completion plan — make the new modular engine the ONLY converter, then delete `converter_v2`

> ## ⛔ DRAFT — three validation gates before this plan may be approved or built from (Bean, 2026-07-04)
> This plan is a proposal, not an agreed roadmap. It MUST pass all three before approval:
> 1. **Fact-check vs the RUNNING SCRIPTS + a real clone.** ✅ **DONE 2026-07-04 (same-day session):** 3 script traces + DB queries + a real `SGS_NEW_ENGINE=1` clone to page 8. Corrections folded in below. Headlines: (a) A1 media-map claim FALSE — media is live end-to-end, LANDED-confirmed (all 6 page-8 images are WP upload URLs returning 200); (b) **the frozen fallback fires for ZERO Mama's sections** — all 7 content sections + the skip-link emit through the new engine (measured probe, 2026-07-04); (c) grid resolver is live not a stub, styling_content is unregistered, container-level flex IS lifted; (d) atomic section path EXISTS; (e) the converter_v2 deletion surface is larger than listed (4 importlib consumers + path refs). Parity baseline (Stage 11.6, 2026-07-04 run): content 77%, CSS 47/49/54% at 375/768/1440, 19 draft els unmatched. See lesson `fact-check-scope-against-running-scripts-not-docs`.
> 2. **Rules + cheats check vs Spec 31 §13.1.** Walk the plan against R-31-1..15, the 7 non-negotiable rules (CONVERT-don't-mirror / NO-CHEATS / UNIVERSAL-no-carve-outs / NO-SKIPPING / VERIFY-ON-REAL-HOMEPAGE / RESPONSIVE-IN-ATTRS / FOLLOW-SPEC), and EVERY cheat-gate description (`cheat-gate/` checks). Confirm no phase introduces a per-slug/slot/role literal, a mirror-emit, a hardcoded default over faithful CSS, or a silent drop. The registry/handler design must stay DB-driven (R-31-1) and universal (R-31-9).
> 3. **Completeness vs ALL of Spec 31.** Read Spec 31 END TO END and confirm the plan's phases cover the FULL requirement set — not just §12.6's list. Map every FR (esp. §2 core mechanism, §3 routing, §13 binding rules + content fork + variant + composite-mirror, §5 CSS properties) to a phase. Any Spec 31 requirement not covered by a phase is a plan gap to close (STOP-29: definition-of-done = the spec's full scope, never "out of scope").
>
> Only after all three pass may Bean approve. The plan's STRUCTURE (delete-last, the modular walker, the phase ordering) is the proposal; its specific claims + completeness are what these gates verify.

## Context (why this exists)

The cloning pipeline has TWO engines. The **frozen `convert.py`** (`orchestrator/converter_v2/`, 6,386 lines) is the **production default**. The **new modular engine** (`converter/`) is **opt-in** (`SGS_NEW_ENGINE=1`), LANDED-proven on the Mama's homepage, and still **borrows from the frozen tree** (db_lookup/icon_resolver imports + 2 frozen pre/post passes + the fork/seed entry). The fork CAN silently fall back to `convert.py::walk` — but **measured 2026-07-04: the fallback fires for ZERO Mama's sections** (all 7 content sections emit via the new engine). The remaining parity work is CSS-resolver completeness + untested block shapes + the A2 ledger, NOT homepage section coverage.

Bean's directive: dissect the monolithic content-extraction (`extraction.py`) into an orchestrator-chained modular walk, replace the block-level `has_inner` fork with the per-attr `emit_shape` fork (FR-31-2.6), drive the new engine to full parity, then **delete `converter_v2` entirely** and make the new engine the only path. **No build-then-rework — this plan is spec-anchored to Spec 31 §2 + §12.6 + FR-31-2.6 so the build session has zero ambiguity.**

**Non-negotiable sequencing: DELETE-LAST.** `convert.py` is the fallback the new engine leans on; it can only die *after* the new engine covers everything (Phase 5 parity). Deleting it earlier breaks production cloning.

## Target architecture (research-locked — Babel merged-visitor + registry)

Evidence: Babel `@babel/traverse`, unified/rehype, Roslyn, LLVM all use it; = Spec 31 FR-31-3's single walker.
- **ONE walker owns recursion** — unconditional descent; a node cloned into an attribute *still* has its children walked (fixes "nested element can also have children").
- **Total registry dispatch** — `node signature → handler`, each handler declaring an **explicit priority/specificity** (CSS-cascade style), NOT elif source-order.
- **Handlers are pure functions** — `(node, ctx) → {attrs_for_parent, block_markup_for_self, children_to_walk}`. No walker call, no sibling/ancestor knowledge, no shared mutable state. Each = its own script, fixture-testable.
- **Assembly is the ONE legitimate final pass** (over the result list, not a 2nd DOM walk) — grid/sibling globals only.
- Pitfall guards: handlers never recurse (they return `children_to_walk` / a skip flag); cross-node bookkeeping (gap ledger) is the walker's job; ONE `recognise()` computes the dispatch key (no per-handler re-inference).

## The per-node walker step order (incorporates Bean's point 6; Gate-3 correction 2026-07-04: variant step added per §2.2)

1. **`recognise(node)`** → Recognition (BEM↔blocks-table match — ALREADY EXISTS: `recognition.py:46` + `recognise_section:167`).
2. **Variant lookup — AT recognition (§2.2 step 2 / FR-31-20, load-bearing).** If `blocks.variant_attr IS NOT NULL`, resolve the variant from the class modifier against `variant_slots` BEFORE the generic layer pass (a variant can define the grid/slots — hero `--split`). Runs at any depth (a grid item that is a variant composite is recognised variant-first, §2.5).
3. **[NEW] container-vs-composite classify** (BEFORE the emit-gate — Bean's point 6). Arbitrary holder = `sgs/container` (reached name-free via `container_default_slug()`; the ONLY true arbitrary holder — `card-grid`/`gallery`/`post-grid` are TYPED composites, premise corrected) → recurse children independently. Typed → the per-attr `emit_shape` walk. This **formalises the narrow `slug==container_default_slug()` gate into one explicit classifier** (composed signal of DB FACTS — `container_default_slug()`, `blocks.tier`, `block_composition` — never a slug/role/slot literal, STOP-38/41; and it lives at the RECOGNITION stage, NOT as a 4th walker conditional, so R-31-3's three-exception contract holds) + a new FR (see §"Spec work").
4. **`emit_shape` gate** (per `role=content` attr): `nested` → lift into parent scalar/array; `child` → emit child InnerBlock + recurse. (`emit_shape` column DONE; the walk wiring is Phase 2.)
5. **Clone** per identity (`equivalent_block_for` → standalone_block: text/heading/media/button).
6. **CSS layer decomposition** (§3.A via the REGISTRY — §2.2 puts layers BEFORE content, and the live `build_block_markup` runs the CSS pass first with content winning collisions; qc-council 2026-07-04 order correction).
7. **Migrate content** (reuse `lift_scalar_content` / `lift_array_content` / `field_extractors`; content overwrites CSS on the same key — the documented merge contract).
8. **Post-lift per-node passes (qc-council F1/F2 — these read the ASSEMBLED attrs so they CANNOT move earlier):** (a) **variant FINGERPRINT reconciliation** — `db_lookup.detect_variant(slug, attrs)` counts which discriminating slots POPULATED this run (extraction.py:1434-1449; distinct from step 2's BEM-modifier signal — BOTH variant mechanisms are kept); (b) the §2.3 arrangement `layout` trigger (extraction.py:1394-1398); (c) `inheritStyle` preset resolution (:1458-1469); (d) the band-fold destination writes (2e2).
9. **Recurse descendants** (unconditional).
10. **Assembly** (final pass — grid-item folds, sibling arrangement; result-list only, never a 2nd DOM walk).

---

## PHASES (each ends with a LANDED gate; delete-last)

### Phase 0 — DONE (committed `df9e20cf` [D272]; fact-checked in-DB 2026-07-04)
`emit_shape` foundation: Spec 31 §13.3 FR-31-2.6; `block_attributes.emit_shape` column + `/sgs-update _populate_emit_shape` seeder (fail-loud) + `db_lookup.emit_shape_for` (`:2459`) + `render_emits.render_reads_attr` (`:116`). DB verified seeded: 105 nested + 33 child (product-card = 9 nested). Spec 22 stub re-removed; product-card leaf conversion was REVERTED (Phase 2 redoes it). **Also done 2026-07-04 (commit `3d7e7d42`, the fact-check session's quick fixes):** pseudo-element `::`-sentinel parse bug fixed + regression tests; dead `route_text_leaf`+Protocols deleted; dead test-only `media_map.py` loader deleted; Spec 31 stale A1/has_inner prose corrected.

### Phase 1 — Regenerate the flow doc (cheap, unblocks everyone)
`cloning-pipeline-flow.md` references archived files → it is STALE. Regenerate from the LIVE scripts (the real chain: `sgs-clone-orchestrator.py::main` → stages 0.1/0.5/**0.7 css_router**/1/2/3/**4 convert_section**/9/gate/deploy/11.6). Deliverable: accurate flow doc + the routing decision-tree visual (companion Artifact).
- **Files:** `.claude/cloning-pipeline-flow.md`, `-stages.md`.
- **Gate:** doc matches a fresh trace of `sgs-clone-orchestrator.py`.

### Phase 2 — Modular content walk (the `emit_shape` re-architecture; FR-31-2.6 / §12.6 item 7)
The core build. Dissect `extraction.py` (1,533 lines) into the modular universal walk.
- **2a. Split the monolith:** `build_block_markup` (assembly) → own script; `_build_css_attrs` (§3.A CSS pass) → own script; content dispatch → the walker + handlers.
- **2b. The single walker + total registry:** walker owns recursion (unconditional descent); registry keyed by node signature with explicit priority. Extend the EXISTING `resolvers/__init__.py:REGISTRY` + `dispatch_table.py` pattern (already live for CSS since D250) to cover CONTENT too (content currently bypasses REGISTRY via `extract_content`).
- **2c. The handler scripts (each its own file, pure):** recognise (exists) · **container-vs-composite classifier (new, step 2 above)** · emit_shape gate (wire `emit_shape_for`+`equivalent_block_for`+`role`) · content-lift handlers (re-house `lift_scalar_content`/`lift_array_content`/`run_mechanism_leaf` logic) · CSS-route (exists).
- **2d. DELETE the block-level fork:** remove `extract_content`'s case-fork (`extraction.py:1096-1187`), the thin `run_mechanism_a/array/styling` wrappers, and Mechanism B's branch shell — they BECOME registry handlers. Keep + re-home: the 3 resolvers, `field_extractors`+`resolve_icon_kind`, `scalar_media`+`*Mobile`, the container-recursion family (`run_container_default`/`_descend_container_children`/`_route_container_child`), G3 validation, all `ContentConservationError` raises, `expected_content_gaps`. **qc-council 2026-07-04 additions to the keep+re-home list (each a Rule-4 silent-drop hole if missed):** the MF5 `has_inner_blocks is None` corrupt-recognition guard (`extraction.py:1096-1101` — loud ContentGap, needs a home in the emit_shape walk); the **icon-bearing-leaf GATING condition** (named-leaf arm fires on `primary_content_attr(slug) OR icon_bearing_leaf` where icon_bearing_leaf = the DB role scan `role.startswith("icon-")`, `extraction.py:1163-1176` — miss it and every icon-only leaf falls to the Case-4 gap); the post-lift passes now pinned in walker step 8; and the D247 **raise-not-assert discipline** (STOP-27) preserved verbatim on every re-homed guard.
- **2e. Guards to bake in:** a **coverage test** (every producible node-signature has a registry entry — and it must ENCODE the validity constraints so the cross-product excludes non-producible combos, e.g. the D212-forbidden `has_inner=1 + scalar-content-lift`; `unrecognised` is a PRE-registry gate, not a signature — qc-council F5/F6), the **explicit-priority scheme**, the D212 invariant re-homed as a per-attr check (an attr can't be both nested+child), conservation per-node. **qc-council 2026-07-04 additions:** (a) **the registry emits ADDITIVELY per node** — today's mechanism arms COMPOSE (Case-1 = scalar + styling + array; Case-2 = B + array, the D248 fix); a one-handler-per-node registry re-introduces the exact hero.badges/cta-stats silent drop — multi-handler emission is a design requirement; (b) **"node signature" is PINNED to structural DB facts** (recognition kind / emit_shape / container_kind / role / tier / capability) — NEVER `block_slug` (a slug-keyed registry entry is an R-31-9 carve-out); (c) **widen `gates/no_slug_literal.py` scope to `dispatch_table.py` (+ `orchestrator.py`)** — `recognition.py` is ALREADY scanned (`_SCAN_FILES`, no_slug_literal.py:60 — a council claim to the contrary was fact-checked FALSE), but `dispatch_table.py`/`orchestrator.py` are "intentionally out of scope" (:29) and that is exactly where the new registry lands; the whole-tree `check_slug_literals.py` only matches `"sgs/..."` literals so it misses namespace-stripped `== "hero"` / `slot == "quote"` forms; (d) **MF-4 disambiguation** (Spec 31 §3 step 3): `attr_for_layer_property` today returns the FIRST rowid match — the unified dispatch must either key on `block_selectors.element` or FAIL LOUD when ≥2 candidate attrs exist for one (block, layer, property).
- **2e2. ONE L2 band-fold dispatch point + the FULL cascade for EVERY node (Bean, 2026-07-04 — CONFIRMED in code).** The L2 fold logic lives in ONE file (`fold_helpers.py`) but is CALLED from TWO seams in `extraction.py` (the default-container fold ~:423-456 + the composite step-3c ~:1400-1432), with a third mechanism (`resolvers/content_band.py`, per-declaration) coordinated only by setdefault ordering. **Worse — the seams run a REDUCED pipeline, not the shared cascade:** (a) the BEM-less-band fallback `lift_content_band_max_width` lifts ONLY `max-width` and silently drops every other band declaration (padding/gap/background/…, no gap record); (b) `route_interior_css_to_parent_slot` iterates all decls but routes them through a hand-rolled prop→layer ladder (`fold_helpers.py:551-571`) instead of the dispatch-table resolvers the root gets, and its GAP-3 early-return (`:522-524`) skips even the gap recording for `display`/`grid-template-*`. **Requirement: the walker routes EVERY node's full declaration stream — root, folded band, grid item — through the SAME dispatch/resolver cascade (L1-L4 in totality), with only the DESTINATION context differing (own attrs vs parent's per-layer/per-slot attrs). Kill the max-width-only fallback + the hand-rolled ladder; every declaration is transferred, EXCLUDED-with-reason, or gap-recorded — including the GAP-3 exclusions (accounted, never skipped silently). Fold-vs-resolver ordering becomes an explicit registry priority, not a convention.** **Honest scope (qc-council F3/F4, 2026-07-04): this is a RESOLVER-CONTRACT change, not a wiring task.** `Ctx` (context.py, a frozen typed contract) carries NO parent reference or destination field — every resolver writes against `ctx.block_slug` (its OWN attrs); the cross-node fold exists as a separate function precisely because Ctx can't express "write into the parent". 2e2 therefore includes: extend `Ctx` (or the dispatch signature) with a destination (owning block + target attr-dict), update every resolver + its tests (Phase 2a scope), AND reconcile the routing semantics — the root path picks ONE layer via `layer_detect`, the fold ladder tries multiple layers in fallback order; the unified cascade adopts `layer_detect`-first with the fold's multi-layer fallback re-expressed as explicit registry priorities (one recorded semantics, design-gated). **Plus MF-3 (Spec 31 §3 step 1): the L2 CONTENT-WIDTH detector must be passed the node's structural position and REJECT a root node** — a section root declaring `max-width:1200px;margin:0 auto` routes to OUTER `maxWidth`, never `contentWidth`.
- **2f. `has_inner_blocks` migration (new engine only):** the **7 non-test `converter/` files** reading `has_inner_blocks` (`dispatch_table.py:17` CSS-side fork, `context.py`, `recognition.py`, `orchestrator.py`, `coverage_report.py`, `services/extraction.py`, `services/has_inner.py`) migrate to `emit_shape` / a per-attr check. The ~11 OTHER non-test readers (coverage-matrix ×3, db-consistency ×3, `sgs-update-v2.py`, `seed-composition-roles.py`, `leftover-bucket-router.py`, `generate-markup-examples.py`, `uimax-tools/seed-slot-synonyms.py`) are separate DB tooling, NOT the new engine — they migrate in Phase 6 with the physical column drop.
- **Key files:** `converter/services/extraction.py`, `converter/recognition.py`, `converter/orchestrator.py`, `converter/dispatch_table.py`, `converter/resolvers/__init__.py`, `db_lookup.emit_shape_for` (`:2459`), `equivalent_block_for` (`:2489`).
- **Spec work:** amend Spec 31 §2.2 + §13.3 to name the container-vs-composite classifier (new FR-31-2.7) + the registry-dispatch content walk; the `emit_shape` fork is already FR-31-2.6.
- **Gate:** LANDED on the 7 Mama's sections via `SGS_NEW_ENGINE=1`; **byte-diff vs the current new-engine output = only the intended emit_shape deltas**; 374 tests + cheat-gate green; product-card lands (Gate A golden re-seeded with LANDED proof).

### Phase 3 — Extract `db_lookup` + `icon_resolver` out of `converter_v2` (SHIM DECISION added per qc-council F11/F12, 2026-07-04)
The new engine hard-depends on `converter_v2.db_lookup` (37 symbols) + `converter_v2.icon_resolver` (2 symbols), institutionalised by `converter/gates/import_ban.py`. **Two constraints collide without a shim:** frozen `convert.py` itself imports `from . import db_lookup` (`convert.py:37`) + `from .icon_resolver import resolve_icon` (`:3612`) and MUST stay byte-identical until Phase 6; and the 4 importlib path-loaders (Phase 4 list) load `converter_v2/db_lookup.py` BY PATH — a bare move breaks both one phase early.
- Move both to the permanent home (`converter/db/` + `converter/services/icon_resolver.py`); rewire every `from orchestrator.converter_v2 import db_lookup/icon_resolver` across `converter/**`; update `import_ban.py`.
- **Leave a thin RE-EXPORT SHIM at `converter_v2/db_lookup.py` + `converter_v2/icon_resolver.py`** (import-forwarding only, `from converter.db.db_lookup import *` style) so frozen `convert.py` (unedited) and the path-loaders keep working until their Phase-4 rewire; the shim dies with the tree in Phase 6. `db_lookup.py` imports stdlib only (verified — no circular dependency from its side).
- **Gate:** `converter/` imports NOTHING from `converter_v2` (the shim forwards the OTHER direction — converter_v2 → converter/db, which `import_ban.py` does not ban); tests green; a live `SGS_NEW_ENGINE=1` clone + a flag-unset frozen clone BOTH still run.

### Phase 4 — Relocate the Stage-4 entry + seeds + rewire ALL other consumers (surface CORRECTED 2026-07-04 — the original "2 consumers" understated it)
`convert_section` + the `SGS_NEW_ENGINE` fork + seed helpers (`reset_pipeline_seed`, `seed_theme_json`, `seed_gap_context`, `ensure_root_section_class`) all live in `converter_v2/__init__.py`. The full live consumer set (fact-checked):
- **Package importers:** `css_router.py:42` (Stage 0.7, `db_lookup`), `expected_rules.py:46,49` (Stage 4, frozen `convert`), and `sgs-clone-orchestrator.py` itself (5 call sites: `:1087,1257,1271,1377,2276`).
- **importlib/sys.path loaders of `converter_v2/db_lookup.py`** (break on deletion despite no package import): `orchestrator/essence_match_detector.py:47`, `recogniser/per-section-convention-voter.py:71`, `db-consistency/resolver_bridge.py:40`, `behavioural-analyser/assign-canonical.py:712`.
- **Path/data references scanning converter_v2 files:** `cheat-gate/check_bound_emit.py:40`, `check_hardcoded_dicts.py:46`, `check_parallel_bp.py:38`, `check_slug_literals.py:60`, `check-atomic-slug-literals.py:54`, `oracle/metamorphic.py:365`, `db-consistency/check_overrides_drift.py:34`, `excluded-gate/scanner.py:53-54`.
- Relocate the entry + seeds into the orchestrator / `converter/`; rewire EVERY consumer above off `converter_v2` (importlib loaders → the relocated `converter/db/` home; gate/oracle path references → the new paths).
- **Gate:** the only remaining `converter_v2` reference is the frozen `convert.py` itself (the fallback), reachable only via the relocated fork; a whole-tree grep for `converter_v2` (imports AND path strings) returns only that.

### Phase 5 — Reach §2.7 parity (the backlog — **FACT-CHECKED against the running scripts + DB, 2026-07-04**; the original §12.6-derived list had 3 wrong items)
The new engine must clone EVERYTHING with NO frozen fallback. The corrected list:
1. ~~**A1 — media-map LOADER**~~ — **REMOVED (claim was FALSE).** Media resolution is live end-to-end on both engines: the orchestrator loads the JSON (`sgs-clone-orchestrator.py:1424-1428`) and threads the dict → `build_block_markup` → `extract_field_value` → `lift_helpers.resolve_media_url:133-168`. The dead test-only loader (`media_map.py`, whose stale docstring caused the claim) was deleted `3d7e7d42`. The first STOP-28 gate is ALREADY GREEN.
2. **A2 — content-conservation ledger** (now the ONLY remaining STOP-28 gate). Extend `declare_input` to capture content routing units (currently only CSS) so a dropped scalar content node is UNACCOUNTED, not silent. `extraction.py:119-127`.
3. **CSS §5 resolver completeness** — the gap is RESOLVERS, not DB rows: 8 of the 11 properties already have `property_suffixes` rows (D250 migration — order/overflow/object-fit/position/flex/aspect-ratio/opacity/background-image); only `transform`/`filter`/`transition` need row seeding OR EXCLUDED-with-reason. Build lifts for all 11 (note: container-level `flex` IS already lifted — `display:flex`→layout + `flex-direction` via `arrangement.py:85-89`; the gap is item props flex-grow/shrink/basis). `outer_box.py:152` is the generic unowned-prop fall-through, not a background-image stub. Stub cleanup: `scalar_content.resolve` + `scalar_media.resolve` are genuine REGISTRY stubs (build via B2 `lift_styling_content` or remove); **`grid.resolve` is a full live implementation (`grid.py:63-161`) — NOT a stub**; **`styling_content` has no REGISTRY entry at all** (its live path is `lift_styling_content` via `extraction.py:857`; delete its unregistered module-level stub or register it deliberately).
4. **Pseudo-elements `::before`/`::after` LIFT** — the `::`-media-separator parse bug is FIXED (`3d7e7d42`; pseudo selectors now parse correctly but still have no lift destination — build the lift or route to UNACCOUNTED/EXCLUDED) + **non-device breakpoint painting** (the F-ii residual band — tier-aligned widths ARE painted by the D259 cascade; only the sub-tier band is logged-not-painted, plus the CSS-resolver path fully gaps non-device Decls at `grid.py:66`).
5. **Re-implement the 2 frozen pre/post passes IN `converter/`:** `_absorb_transparent_wrappers` (pre-pass, `convert.py:2944`) + `ensure_root_section_class` (post-pass, `convert.py:5181`) — both verified invoked unconditionally from `converter_v2/__init__.py:407` + `:454` even under `SGS_NEW_ENGINE=1`. ~~DELETE the dead `text_leaf.py::route_text_leaf`~~ — **DONE (`3d7e7d42`).**
6. ~~**Top-level atomic-tag swap**~~ — **REMOVED (claim was FALSE).** The path EXISTS: `recognition.py:75-79` branch 2 is the atomic swap; `recognise_section:198-200` returns it; `__init__.py:434-438` emits it. Residual (small): verify the atomic-section EMIT fidelity vs the frozen `_atomic_attrs_for` path on a live run (routing proven, byte-output not).
7. **F3 render-oracle (Playwright draft-vs-clone at 375/768/1440) + the THREE METAMORPHIC relations (§12.2 item 3 — source-order permutation / BEM-synonym rename / px-scale-by-k, the only tests that PROVE name-free universality rather than page-8 coincidence; qc-council F3) + F6 DB-as-code consistency suite** — the automated LANDED gates (`gates/` currently has only `import_ban` + `no_slug_literal`).
8. **css_router D1 fate — formally decide (MF-2, Spec 31 §1; Gate-3 addition 2026-07-04).** Stage 0.7 routes CSS to D1 but Stage 4 no longer consumes it; leaving it as a dead stage that silently strands properties is forbidden. Retire the D1 path OR rewire the new engine to consume it — one recorded decision, implemented.
9. **Variant DATA population (FR-31-20; Gate-3 addition 2026-07-04).** The variant CODE is universal but only 2 blocks carry `variant_attr` + `variant_slots` rows (DB-verified). Populate `block.json supports.sgs.variants` for the variant-bearing blocks + `/sgs-update` reseed — required for the multi-shape fixture gate (item 7) to exercise variants beyond hero/trust-bar.
- **Gate:** new engine LANDED-clones the WHOLE Mama's homepage; **`v3.walk` fallback never fires** (instrument the fork at `converter_v2/__init__.py:419-443` and assert against the measured baseline distribution from the 2026-07-04 fact-check run); computed-parity 11.6 green at all 3 breakpoints; **the 6 defect-register families B/C/D/E/F/K each verified PER-ROW on live computed-style (Spec 31 §6 goal 5 — esp. Family E media box-CSS→sgs/media, which page 8 cannot exercise; the multi-shape fixture set covers it)**; Bean eye sign-off. Fallback triggers (code-proven): flag unset / ambiguous multi-root tie / non-BEM non-atomic root / empty-or-invalid emit / any exception — a slug-None BEM section does NOT fall back (it container-defaults, `recognition.py:215-224`).

### Phase 6 — DELETE-LAST (flip + delete)
Only after Phase 5's parity gate is green.
- Flip the default: remove the `SGS_NEW_ENGINE` gate (new engine = the only path); update STOP-28.
- **Re-point the cheat-gates before the tree dies (qc-council F8, fact-checked per-gate 2026-07-04):** `check_bound_emit.py` scans ONLY `converter_v2/` (`_CONVERTER_DIR`, :40) and `check_parallel_bp.py` targets `convert.py` specifically — both become no-op silent gates on deletion → re-point at `converter/`; `check_hardcoded_dicts.py` scans the whole orchestrator tree (survives, but its convert.py baseline entries vanish → refresh the baseline); `check_important_render.py` scans the PHP/CSS render surface (unaffected).
- Delete `convert.py` + the rest of `converter_v2/` — including the Phase-4-relocated consumers' OLD path references, the Phase-3 re-export shim, AND the test-side deletion surface (converter/tests importing db_lookup via converter_v2, `tests/test_commit2_*`, `test_commit3_f6a_inheritance.py`, `test_commit4_gate_retirement.py`, `test_converter_conformance.py`'s frozen imports, `cheat-gate/tests/test_cheat_gate.py`, `recogniser/test_voter_db_legacy.py`).
- Delete `block_composition.has_inner_blocks` (column) + migrate/prune the ~11 external non-test readers (coverage-matrix ×3, db-consistency ×3 incl. `check_composition.py` Check #2, `sgs-update-v2.py` `_populate_has_inner_blocks`, `seed-composition-roles.py`, `leftover-bucket-router.py`, `generate-markup-examples.py`, `uimax-tools/seed-slot-synonyms.py`) + the F6 has_inner drift-guard test.
- Regenerate `/sgs-update`; final `/qc-council`; LANDED re-verify; commit + push.

---

## Verification discipline (every phase)
Unit-test each handler with a fixture node (the whole point of the modular split) + LANDED on page 8 + computed-parity 11.6 + assert zero frozen fallback for the covered scope (baseline: 0 fallbacks measured 2026-07-04). `convert.py` stays byte-identical until Phase 6 (D-MODULAR). **R-31-5: each PHASE splits into ≥3 path-scoped commits** (Phase 2 especially — e.g. 2a split / 2b-2c walker+registry / 2d fork deletion / 2e guards / 2f migration as separate commits), never one phase-sized commit.

## Named exclusions (STOP-29 — mapped, not silent; extended per qc-council 2026-07-04)
- **FR-31-6** (hybrid render.php roster migration) + **FR-31-21.2 `--apply`** (capability auto-propagation writer) + **FR-31-21.1** (composite-mirror invariant enforcement) — BLOCK-SIDE workstreams, not converter phases. Tracked in Spec 31/parking; nothing here depends on them (the converter transfers to whatever attrs the blocks declare today).
- **FR-31-12** (stage-2.json entry per section boundary) — owned by the EXISTING orchestrator Stage 2, which this plan does not touch; named here so it isn't presumed converter work.

## Spec work folded in
- FR-31-2.6 (`emit_shape` fork) — DONE (Phase 0).
- **NEW FR-31-2.7** — the container-vs-composite classifier (arbitrary holder vs typed composite; composed DB signal; runs after `recognise_section`, before the emit-gate). Write in Phase 2.
- Amend §2.2 / §13.3 for the registry-dispatch content walk (Phase 2).
- Regenerate the flow doc (Phase 1).

## Honest scope note
This is a **multi-session programme**, not one build. The new engine is LANDED-proven but the §12.6 backlog (esp. A1 media-map + A2 ledger + CSS §5) is the bulk of the work to retire `convert.py`. Phases 1–2 are the immediate front (flow doc + the modular emit_shape walk); 3–4 are refactor/decouple; 5 is the parity backlog; 6 is the flip+delete. Each phase is independently shippable + LANDED-gated, so there is no big-bang cutover.
