# Eye Care Wave C task 2: the product page (zero-reviews state first)

**Status:** IN PROGRESS 2026-09-25. Parent plan: `2026-09-24-eye-care-hand-build-design.md` (Wave C).
Built and live on eye-care-test: framework items 1–5 below, plus buybox draft-parity settings (picker styles and
measurement sub-line, price typography, "RRP £171" and "You save", add-to-cart style and price, blocks above the
add-to-cart, stock wording, stackBelow, no height cap on the sticky column), breadcrumbs showArchiveCrumb /
showCurrentCrumb, icon-list circle and dividers, tabs mobileLayout, option-picker tiles filling the row. Data: real
frame sizes from jpopticians.com (`woo-seed/sizes-jpopticians.json`, Bean 2026-09-25: use the comp site's real
measurements; 8 frames found there, the other 8 keep the draft's one size), colour × size variations, Sunglasses
category, draft descriptions. The page is the site's own `single-product` template built from
`build/single-product.tree.json` (generated by `build/gen_single_product.py`).
Remaining for this task: the three-width re-check after the latest deploy; "Which size am I?" link and "from +£59" on
the prescription button (tasks 5 and 4); the Sizing tab's frame diagrams (bound measurements are in place; the drawn
diagrams are deferred to the frame-measurements build in the parent plan); gallery view thumbnails need real photos.
**Written for:** Bean and any session picking up Wave C.

**Goal:** a product page matching the draft (`Eye Care Birmingham.dc.html`, sections `sgs-breadcrumb`,
`sgs-product-main`, `sgs-product-tabs`, `sgs-product-no-reviews`, `sgs-product-more-from-brand`, `sgs-product-similar`)
at 1440, 768 and 375, for a product with no reviews. Reviews with content (the `sgs-product-reviews` state) follow.

## Decisions

- **Tabs (Bean, 2026-09-25, after research `~/.claude/memory/research/2026-09-25-woocommerce-product-page-tabs.md`):**
  one editable starting layout, then edited in the Site Editor. It is WooCommerce's own model (the blockified Product
  Details block, edited once in the single-product template) and the market leaders' (Kadence Shop Kit, Barn2). In
  WordPress terms: the theme's `sgs-pdp-content` part is the neutral starting layout; a site's own edit saves a
  site-level copy that overrides it. Eye Care's copy is built through the editor from a tree in
  `sites/eye-care-ward-end/build/`. Tabs, not an accordion (the draft shows tabs). A tab whose content renders empty
  for a product hides itself (WooCommerce does the same for its sections). Per-category display rules (the
  Kadence/Barn2 extra) are deferred to a later client that needs them.
- **Per-product values** (brand, code, material, lens width…) come from the `sgs-product/field` bindings source bound
  into `sgs/text` / `sgs/heading` (design note `.claude/reports/2026-09-24-product-field-bindings-design.md`), so no
  product is hand-edited and no new "specs" block is needed.

## What exists (Wave B)

`sgs/buybox` extras slot (below add-to-cart), RRP pill, stock status, sticky column; `sgs/option-picker` tiles and
`subLabelMetaKey`; `sgs/breadcrumbs` `productPageCrumbs` (category / brand / both); `sgs/whatsapp-cta` `card`
variant; the "current product has reviews" visibility condition (`includes/conditional-visibility.php`, condition 8).

## Framework work

1. **Site templates in the page builder.** `scripts/wp-build-page.js --template-part <slug>` (and `--template <slug>`)
   opens the Site Editor, validates the tree exactly as for pages, and saves the site-level copy. Also used by task 3
   (shop archive).
2. **Product field bindings, both halves.** `Product_Bindings`: keys `brand`, `sku`, `attribute:<taxonomy>`,
   `meta:<key>`, with optional `before` / `after` text args (so "58" reads "58 mm", "More from " + brand). JS
   `registerBlockBindingsSource( 'sgs-product/field' )` with `getFieldsList()` (brand, SKU, every product attribute
   taxonomy), mirroring `src/bindings/index.js`.
3. **Buybox: blocks above the price.** `extrasBeforeCount` (how many of the slot's first blocks render above the price,
   the rest below add-to-cart): brand, name, code and rating sit above, the second CTA, WhatsApp card and assurance
   list below.
4. **Tabs hide an empty panel** (`sgs/tabs`: a tab whose rendered content has no text or media is dropped, and its
   button with it).
5. **Related lists by the current product's term.** "More from {brand}" (same `product_brand`) and "Similar shapes"
   (same `pa_shape`), excluding the current product, as a `woocommerce/product-collection` filter keyed on a class or
   a registered collection (research the WooCommerce collection API before building).

## Content (Eye Care, through the editor)

- Data: every product in a "Sunglasses" category (breadcrumb Home / Sunglasses / Ray-Ban).
- `single-product` / `sgs-pdp-*` site copies: breadcrumb (category + brand); buybox with the gallery, brand, h1, code,
  rating line, price/RRP/"You save", stock, colour tiles, second CTA "Add my prescription" (opens the lens configurator
  in task 4; until then links to the lenses page), WhatsApp card, three assurance ticks; tabs Description / Details
  (bound grid) / Sizing beside the "Good to know" FAQ; the zero-reviews block (condition 8 hidden-when-reviews);
  "More from {brand}" and "Similar shapes" using the task 1 listing card.

## Open question for Bean

Frame sizes (S/M/L with measurements). The draft invents them (every frame gets S/M/L by adding or taking 3 mm), and
its own note says production needs real per-size measurements. See the chat for the options.

## Verify

Draft vs live at 1440 / 768 / 375 (side by side, looked at) for product 71 (no reviews), plus the editor round-trip of
the template parts.
