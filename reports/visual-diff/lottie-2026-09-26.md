# Live verification: the Lottie player (Wave 3C U-17, M-33, D1151), 2026-09-26

verdict: PASS
intent_capture_passed: true
source_commits: 47182e1ed (Spec 38 Tier H), 903d6a668 (Lottie media type), 039bd248d (upload filter hotfix), 1842c220f and fb5318295 (sgs/media enum and attributes), af2de355a (player build and stylesheet), e97d17429 (runtime under src/shared/effects), d8c99630c (Media pause placement)
source_sha: not computed; see `reports/visual-diff/furniture-2026-09-26.md` (the SHA script hashes staged files only).
design: `.claude/reports/2026-09-26-u17-lottie-design.md`
blocks: media, hero, responsive-logo, container (and the other background blocks through the shared wrapper)

## Environment and method
- Site: sandybrown, fixture page 4087 `/qa-lottie/` (tree `plugins/sgs-blocks/scripts/nav-qa/lane-c/qa-lottie.json`): an `sgs/media` Lottie with a poster and an `sgs/container` with `bgLottie`. Test animation: attachment 4079 (200x200, 30 fps, 2 s), uploaded through the REST media endpoint.
- One headed Chrome window; reduced motion simulated by answering `prefers-reduced-motion: reduce` before the page's scripts ran (chrome-devtools cannot emulate that feature), with the normal run as the positive control.

## Results
| # | Check | Result | Measured |
|---|---|---|---|
| 1 | Upload validation, fail closed | PASS | valid Lottie 201 (meta stored: w 200, h 200, fr 30, duration 2); external-asset file, `null` and non-Lottie JSON refused with the validator's message; a PNG still uploads |
| 2 | Both surfaces animate | PASS | SVG drawn into both, frame transforms change over 600 ms; poster hidden once drawn |
| 3 | Loads only on demand | PASS | `fx-lottie.css` 1.1 KB, `fx-lottie.js` 2.4 KB, then `lottie-light.js` 43.3 KB transferred after the trigger; build: 46,496 + 2,595 bytes gzip, inside the 60 KB allowance |
| 4 | Reduced motion | PASS | no SVG, poster visible, `lottie-light.js` never requested |
| 5 | Pause control (WCAG 2.2.2) | PASS | 44x44 on both; Media: inside the figure's corner, holds the frame, `aria-pressed` true, resumes; background: same |
| 6 | Accessibility | PASS | Media: `role="img"` with its alt; background: `aria-hidden`; axe 0 violations |
| 7 | Pages without Lottie | PASS | no Lottie element, module or player on `/`, `/blog/`, `/qa-scrim/` |
| 8 | Existing backgrounds unchanged | PASS | 23 of 23 image, SVG and video background elements byte-identical before and after the wrapper change |

## Recorded divergences and residue
- lamalama's live canvas mark stays a still (plan section 1h). buck's own Lottie file is used only at its Wave 4 clone.
- Not built here: Lottie in `sgs/before-after` (needs a drag trigger) and in the cloning pipeline; refreshing stored meta when an attachment's file is replaced in place.
