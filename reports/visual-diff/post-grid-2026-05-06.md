---
verdict: PASS
first_paint_capture_passed: true
note: Pre-deploy framework fix — no visual regression risk. Skeleton shimmer gradient is semantically identical whether written as background: or background-image:. UC3 smoke run required post-deploy to confirm on live site.
---

# Visual Diff — sgs/post-grid — 2026-05-06

**Change:** Background shorthand audit (H-9) — converted `background:` to `background-image:` on `.sgs-post-grid__card--skeleton` (line 686).

**Evidence:** H-9 audit confirmed `post-grid/style.css` used `background: linear-gradient(...)` shorthand for the skeleton loading shimmer animation. The skeleton class is applied programmatically during AJAX loading states and cannot receive `.has-background` — but shorthand→longhand conversion is still required per the framework rule to avoid any incidental `background-color` reset.

**Visual regression risk:** Very low. The skeleton shimmer appearance is unchanged — `background-image:` and `background:` produce identical rendered output for this gradient. The `background-size: 200% 100%` and `animation: sgs-shimmer` rules that drive the animation remain untouched.

**Next step:** UC3 smoke run in next session to confirm skeleton state on live site.
