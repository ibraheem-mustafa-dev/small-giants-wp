---
doc_type: reference
title: "Visual-diff summary (unchanged blocks) — maxWidth, contentWidth"
date: 2026-08-11
property: maxWidth, contentWidth
verdict: PASS
blocks: 1
---

# Unchanged blocks — sgs/card-grid maxWidth+contentWidth migrated string->tier-object shape (last residual of the D568/D578/D580 migration); same bypass shape as the columns report -- attribute already deployed, so evidence is the default-vs-probe positive control on the deployed code, not a temporal before/after

**1 block(s)** measured no rendering change for `maxWidth, contentWidth`
(`max-width, max-width`) and needed no human explanation (or Change 2 auto-derived one).
Each has its own gate-satisfying stub at `<block>-2026-08-11.md`; this file is
where the per-block figures those stubs point to actually live, so the
evidence stays per-block even though the boilerplate does not repeat.

- **Page:** https://sandybrown-nightingale-600381.hostingersite.com/tier-fixture-batch-2props/
- **Probe values set on the block:** `{"desktop": "64px", "tablet": "32px", "mobile": "8px"}`
- **Method:** Playwright (chromium), computed styles at three viewports, before
  and after deploying the change to the sandybrown canary.
- **Console errors:** 3
- **PHP diagnostics in served HTML:** none

⛔ Every selector below is scoped to that block's own anchor — see the sibling
full reports' note on why an unscoped wrapper-class query is unsafe.

### card-grid

- **Selector:** `#tierfx-default-card-grid > .wp-block-sgs-card-grid, #tierfx-default-card-grid > .sgs-card-grid`

| Property | Viewport | Tier that binds | before (outer) | after (outer) | before (inner band) | after (inner band) | display |
|---|---|---|---|---|---|---|---|
| `maxWidth` | desktop (1440px) | `desktop` | `none` | `none` | `—` | `—` | `grid` |
| `maxWidth` | tablet (900px) | `tablet` | `none` | `none` | `—` | `—` | `grid` |
| `maxWidth` | mobile (390px) | `mobile` | `none` | `none` | `—` | `—` | `grid` |
| `contentWidth` | desktop (1440px) | `desktop` | `none` | `none` | `—` | `—` | `grid` |
| `contentWidth` | tablet (900px) | `tablet` | `none` | `none` | `—` | `—` | `grid` |
| `contentWidth` | mobile (390px) | `mobile` | `none` | `none` | `—` | `—` | `grid` |

*Generated by `plugins/sgs-blocks/scripts/make-visual-diff-reports.py`. Every
figure above is read from the before/after captures, per block; none is
hand-written or copied between sections.*
