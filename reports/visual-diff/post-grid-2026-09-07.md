# Visual Diff Report — sgs/post-grid (2026-09-07)

**Block:** sgs/post-grid  
**Date:** 2026-09-07  
**Changes:** Manifest/attribute binding fixes (no visual output changes)

## Intent

This commit adds attribute-to-element mappings in block.json that document how `categoryBadgeBgColourGradient` is used by the category-badge element. The attribute was already being used in render.php; this change only updates the manifest declaration to claim it.

Related changes:
- `edit.js`: Added missing destructure for the attribute (editor control surface only)
- `block.json`: Added `css:background-image: categoryBadgeBgColourGradient` mapping to category-badge element attrMap

## Verification

The attribute is already fully functional in render.php — it was just missing from the manifest declaration and the editor's destructure. No visual changes to the rendered block output.

**verdict: PASS**  
**intent_capture_passed: true**
