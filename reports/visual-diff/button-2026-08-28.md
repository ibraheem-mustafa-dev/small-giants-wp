# Visual diff — sgs/button, colourBorder* -> borderColour* naming normalisation (Task 0) — 2026-08-28

verdict: PASS
intent_capture_passed: true
source_sha: ed81b9e84e6333b7

## What changed

`sgs/button` was the one block-private block using `colourBorder`/`colourBorderGradient`/
`colourBorderHover`/`colourBorderHoverGradient` instead of the `borderColour*` convention every
other block-private block uses (container, heading, icon-list, option-picker, process-steps,
product-card, quote, text, timeline). Renamed for consistency ahead of the SgsBorderControl
migration. `block.json`, `edit.js`, `presets.js`, `render.php` all updated; `product-card/edit.js`
also updated (it reads `BUTTON_PRESETS.colourBorder`/`.colourBorderHover` to seed its own,
differently-named `ctaColourBorder`/`ctaColourBorderHover` attrs). Commit `89997c91f`. No theme
pattern/template authors the old names (grepped clean).

## Why intent_capture_passed, not a before/after diff

A pure attribute rename should be behaviourally invisible — the assertion is "does the button's
border still render identically after every reference to the old name was updated", not "did
something visibly change".

## Assertions (stated before measuring)

1. Setting a border colour via the (renamed) `Border colour` control in the Styles tab writes
   `borderColour`, not the retired `colourBorder`.
2. The button's border renders correctly from that new attribute name, in both editor and
   frontend.
3. No stale reference to `colourBorder*` remains in the renamed block's own render path (the
   DB classifier cache is a known, separately-disclosed exception — see D876 — not part of this
   assertion).

## Live result — fresh page 3060, sandybrown canary, 2026-08-28

Inserted a fresh `sgs/button` block, set `Border colour` (Styles tab) to the theme's `Success`
palette swatch.

- **Editor canvas**: `.wp-block-sgs-button` computed `border-color: rgb(46, 125, 79)`,
  `border-width: 2px`, `border-style: solid` — the theme's Success green, correctly resolved from
  a token, not a raw hex.
- **Frontend** (`/?page_id=3060`): identical computed values —
  `border-color: rgb(46, 125, 79)`, `border-width: 2px`, `border-style: solid` — on
  `.sgs-button.sgs-button--primary.sgs-btn-b8dbbe59`.
- **Console**: zero errors across insert, colour-set, publish, and frontend load.

`block.json` confirmed to declare only the new `borderColour*` names (grepped directly) — the
code-side rename is clean. All three assertions hold.
