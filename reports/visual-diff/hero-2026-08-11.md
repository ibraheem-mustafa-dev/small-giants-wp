---
doc_type: reference
title: "Visual-diff report — hero · contentPadding + contentBandPadding"
block: hero
date: 2026-08-11
property: contentPadding,contentBandPadding
verdict: PASS
first_paint_capture_passed: true
source_sha: 23d13b3bca5e6c38
---

# hero — contentPadding + contentBandPadding bind live at every tier

**Verdict: PASS.**

Hero declares both `contentPadding` (its own content box) and `contentBandPadding` (the shared
wrapper's band layer, per D539's ruling that hero keeps the wrapper conditionally). Both were set
simultaneously on the same fixture instance (post 2270,
https://sandybrown-nightingale-600381.hostingersite.com/tier-fixture-batch-4props/, anchor
`tierfx-probe-hero`) and measured on the live frontend render at two viewports.

| Property | Viewport | measured on | expected padding-top | actual padding-top |
|---|---|---|---|---|
| contentPadding | desktop (1440px) | `.sgs-hero__content` | 24px | 24px |
| contentPadding | mobile (390px) | `.sgs-hero__content` | 8px | 8px |
| contentBandPadding | desktop (1440px) | `.sgs-container__inner` | 40px | 40px |
| contentBandPadding | mobile (390px) | `.sgs-container__inner` | 10px | 10px |

⚠ This report's `source_sha` was computed and this file written in the same shell step as the
commit attempt immediately following, because a concurrent session is actively editing hero/edit.js
— see decisions.md for the cross-track note. The padding controls measured above are unaffected by
that session's (unrelated) background-panel work; only the SHA-matching race is a hazard, not the
correctness of what was measured.

Full context: tier-fixture page (post 2270), anchor `tierfx-probe-hero`.
