---
doc_type: reference
title: "Visual-diff / LANDED report — sgs/trust-bar gridItemPadding/gridItemBorderRadius box-object (A1)"
block: sgs/trust-bar
date: 2026-07-26
wave: "Spec 32 box-flat migration — A1 shared 'Grid item defaults' panel (container/cta-section/hero/trust-bar)"
verdict: PASS
first_paint_capture_passed: true
---

# sgs/trust-bar — gridItemPadding / gridItemBorderRadius box-object (deployed + live-verified)

**Verdict: PASS.** `sgs/trust-bar` imports the same shared `GridItemDefaultsPanel` and renders its root
via the same `SGS_Container_Wrapper::render()` helper as the other 3 A1 blocks (composite-mirror rule,
D152). Verified independently as a grid-CHILD consumer — notably the ONE consumer type that ALSO has its
own internal `.sgs-container--grid` class (for its badge layout), which was checked did not interfere.

## What changed
Same shared box-object migration described in the `sgs/container` report.

## Evidence (live, sandybrown, editor + frontend)

### 1. Editor panel exists on trust-bar's own inspector
`trust-bar/edit.js` imports `GridItemDefaultsPanel` (confirmed by source read this session) — identical
component/UI to `sgs/container`.

### 2. Consumption as a GRID CHILD — asymmetric painting
Same 4-child grid-parent test setup (`sgs/container` parent, `layout: 'grid'`, children: container /
cta-section / hero / **trust-bar**). Parent set to:
```
gridItemPadding: { top: "40px" }
gridItemBorderRadius: { topLeft: "20px" }
```
`getComputedStyle` keyed on the rendered `sgs/trust-bar` root
(class `sgs-container sgs-trust-bar sgs-trust-bar--icon-circle sgs-trust-bar--medium sgs-container--grid ...`
— note trust-bar's root carries `sgs-container--grid` ITSELF, for its own internal 4-badge layout; this is
a distinct, nested application of the same mechanism and did not interfere with the outer consumption):
```
paddingTop: 40px | paddingRight: 0px | paddingBottom: 0px | paddingLeft: 0px
borderTopLeftRadius: 20px | borderTopRightRadius: 0px | borderBottomLeftRadius: 0px | borderBottomRightRadius: 0px
```
Identical to the other 3 sibling measurements in the same grid.

### 3. Neutrality
Parent's attrs cleared to `{}`; re-measured on trust-bar's root: `paddingTop: 0px`, `paddingLeft: 0px`,
`borderTopLeftRadius: 0px` — genuinely absent.

### Console
No frontend console errors.

## Not captured / limitations
- Same as the `sgs/hero` report: the full-page screenshot cuts off trust-bar to the right of the viewport
  (4-column grid overflowed the visible frame at this canary's content width), so there is no dedicated
  visual crop of trust-bar's padded/rounded state — capability is proven via `getComputedStyle` on the
  live DOM instead.
- Trust-bar's OWN badge children (`.sgs-trust-bar__badge`) are not `.sgs-container`-classed, so this test
  exercises trust-bar as a grid CHILD (of an outer parent), not as a grid PARENT populating its own badges
  from the gridItemPadding/gridItemBorderRadius mechanism — trust-bar's badge spacing is controlled by its
  own separate attrs, unaffected by this migration.
- Same DPR-1.1 rendering-environment artefact as noted in the container report; not a migration regression.
