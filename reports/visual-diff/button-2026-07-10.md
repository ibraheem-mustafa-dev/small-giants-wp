---
doc_type: reference
title: "Visual-diff / LANDED report — sgs/button residual native colour reconcile"
block: sgs/button
date: 2026-07-10
wave: "no-inline rollout — RECONCILE"
verdict: PASS
first_paint_capture_passed: true
---

# sgs/button — residual native `color` reconcile (LANDED)

**Verdict: PASS.** button was box-migrated + LANDED (D293) but still declared its `color` native
support ENABLED without `__experimentalSkipSerialization`, so a colour set via WP's native Styles
panel auto-inlined onto the wrapper. Now skip-serialised + scoped. Full detail: see the consolidated
`reports/visual-diff/reconcile-native-supports-2026-07-10.md`.

## Evidence (harness + live curl, page 1356, colour SET)
- **Zero inline** (#1): harness scan — no inline `style="…color…"` on `.wp-block-sgs-button`.
- **Scoped, not vanished:** live page carries `#sgs-btn-<uid>{color:#ffffff;background-color:#123456;}`
  in the block's own `<style>`; `has-*-color` preset classes re-added.
- Preset/`inheritStyle`, hover, spacing, border unchanged.

## Gates
- `npm run build` prebuild (dead-controls, hardcoded-defaults 0 net-new) + webpack: PASS.
