# Live verification: the shared viewport scrim (Wave 3C U-2, M-14), 2026-09-24

verdict: PASS
intent_capture_passed: true
commit_sha: 4d445a7d2 (scrim), 5cbee72ba (one hover-shadow control), 2aa9fcd64 (gate repairs), cc1f7a1b8 (Mama's snapshot shadow keys)
design: `.claude/reports/2026-09-24-u2-scrim-design.md`
blocks: nav-drawer, nav-bar-menu, cart, gallery, modal, product-search

## Environment and method
- Site: sandybrown (`sandybrown-nightingale-600381.hostingersite.com`), deployed with `build-deploy.py --target sandybrown` after cc1f7a1b8 (full build: 124 of 124 fast gates, postbuild checks and live motion probes green).
- Fixture: the active test header (post 3777) with the menu bar set to halcyon's scrim (`#0a0a0c`, 0.28, blur 2px) and the cart in drawer mode; the active test drawer (post 3778) as a corner panel (`anchor` trigger) with lamalama's scrim (`#000000`, blur 16px) and strengths 0.4 / 0.5 / 0.4 (desktop / tablet / mobile; the mobile tier was first measured at away's 0 on a full-screen drawer, then switched to a corner panel to exercise the partial path); page 3843 `/qa-scrim/` with a modal, a gallery with its lightbox and a product search in command-palette mode, all at their default scrim. Backups of both CPT bodies are in post meta `_sgs_qa_scrim_backup`.
- One headed Chrome window (chrome-devtools), pages fully loaded, real pointer hover and clicks, `getComputedStyle` on the scrim div and its `::before`. The window cannot go below 500px wide, so the phone tier was measured at 500px (below the 768px phone breakpoint).

## Results
| # | Check | Result | Measured |
|---|---|---|---|
| 1 | All six scrims print at `wp_footer` as direct children of `<body>` | PASS | 6 `div.sgs-scrim`, parent BODY, `aria-hidden="true"`; all at opacity 0, pointer-events none at rest |
| 2 | Menu bar, 1440, hover "Our Story" (halcyon cell) | PASS | scrim opacity 1, `::before` `rgb(10, 10, 12)` at 0.28, `backdrop-filter: blur(2px)`, rect 0,0,1440,900, z-index 99 under the header's 100; a point at the header still hits the header |
| 3 | Menu bar negative control: pointer to "Shop" | PASS | `aria-expanded` false, scrim opacity 0, pointer-events none |
| 4 | Menu bar click route: open by the submenu button, click "Open the modal" under the scrim | PASS | menu closed, scrim 0, modal NOT opened (the dismissing click is absorbed) |
| 5 | Modal default | PASS | `:modal`, scrim opacity 1, `::before` `rgb(58, 46, 38)` (the text token `#3a2e26`) at 0.5; native `::backdrop` `rgba(0, 0, 0, 0)`; Escape closes and the scrim returns to 0 |
| 6 | Gallery lightbox default | PASS | scrim opacity 1, `::before` `rgb(197, 106, 122)` (primary-dark `#c56a7a`) at 0.9; the dialog's own background is now transparent; Escape closes, scrim 0 |
| 7 | Product search, command palette default | PASS | `:modal`, `::before` black at 0.55, `backdrop-filter: blur(6px)`; `::backdrop` transparent with no filter |
| 8 | Cart drawer default | PASS | opens as `:modal` (reparented to body), scrim black at 0.55; a click outside the panel closes it and the scrim returns to 0 |
| 9 | Nav drawer, phone tier, full screen (away 375 cell) | PASS | drawer 500 wide (full screen), `::before` opacity 0 |
| 10 | Nav drawer, phone tier, corner panel (lamalama cell) | PASS | panel 360x419 at 101,80; scrim `::before` black at 0.4, `backdrop-filter: blur(16px)`; `::backdrop` transparent, so ONE dim layer (the old double dim, about 0.80, is gone); header above the scrim |
| 11 | Nav drawer click outside the panel | PASS | a click on the gallery image outside the panel closed the drawer, scrim 0, lightbox NOT opened |
| 12 | Screenshots | PASS | local only (scratchpad): bar at 1440, gallery at 1440, drawer at 500; header and panel crisp, the page dimmed and blurred |

## Not measured / known limits
- Forced colours: the `@media (forced-colors:active){...-scrim{display:none}}` rule is emitted (unit-tested in `run-scrim-standalone.php`), not emulated live this pass (chrome-devtools cannot emulate forced colours; a second browser window was not opened).
- `axe-run.mjs` with a surface open was not run; the scrim is an empty `aria-hidden` div that takes no focus.
- The tablet tier (0.5) is emitted (`@media (max-width:1023px)` rule read from the served CSS) but not hovered live: this header keeps its menu inline at 768, so no drawer opens there.
- On the very first page load the gallery lightbox was found open with no click from this session; not reproduced on a fresh reload (only a click opens it). Recorded, not chased.
- One cart-trigger click made within a second of closing the product search did not open the cart (the closing dialog was still modal); the next click opened it. Timing, not the scrim.
- The one hover-shadow control (5cbee72ba) is an editor change plus a PHP precedence rule covered by `run-shadow-hover-rules-standalone.php` (37/37); not hovered live.
- Bean's eye (R-31-13): owed on the three screenshots.
