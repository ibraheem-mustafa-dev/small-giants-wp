---
doc_type: report
block: sgs/heading
generated: 2026-06-03
verdict: PASS
first_paint_capture_passed: true
---

# Visual diff — sgs/heading (Task D: redundant `hero` block-style removed)

## verdict: PASS
## first_paint_capture_passed: true
No render-path change — `variantStyle` was already retired as an attribute; the change removes ONE `register_block_style()` registration (`sgs-heading-variations.php`). The heading itself renders unchanged.

## Evidence
- **Change:** removed the `hero` block-style registration (kept `section` + `card`). An h1 default already provides the hero scale, so `hero` was redundant.
- **No content regression:** existing headings render unchanged; the only visible difference is the "Hero" option no longer appears in the editor Styles panel. Existing posts saved with `is-style-hero` still validate (deprecated.js v3 migrate maps the old `variantStyle:'hero'` → `is-style-hero` className, which is now an inert no-CSS class — harmless).
- **Build:** `npm run build` PASS; no new deprecation needed (block-style registration is runtime PHP, not a save() change). block.json 0.5.1.

## Review
Implemented + build-verified; no editor "invalid content" risk (style-registration change only).
