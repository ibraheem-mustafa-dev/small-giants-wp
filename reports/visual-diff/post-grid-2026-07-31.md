---
verdict: PASS
first_paint_capture_passed: true
block: sgs/post-grid
date: 2026-07-31
spec: 38
wave: C
surface: frontend
canary: https://sandybrown-nightingale-600381.hostingersite.com/motion-roster-canary/ (page 2085)
harness: C:/tmp/roster.mjs (ad-hoc; readings transcribed below)
---

# sgs/post-grid — joined the derived draggable roster (FR-38-13)

## What changed

`supports.sgs.fx.providesNatively: ["draggable"]` + a `dragToScroll`/`dragMomentum`
inspector toggle + a `data-sgs-fx="draggable"` emit on `.sgs-post-grid__inner`. It joined
the roster because its own `style.css:123` declares `overflow-x: auto` on the
`--carousel` inner track.

## First paint — PASS

Two instances, `carousel` layout, momentum ON then OFF. Both render **1200 × 604**,
`opacity: 1`, `visibility: visible`, **257 characters of real text**. Zero page errors.
The render is unchanged by this session's edits, which add an inspector control and a
data attribute and touch no markup the visitor sees.

## ⚠ The DRAG CAPABILITY is UNPROVEN on this block — stated, not buried

| Reading | Value |
|---|---|
| fx marker lands on | `.sgs-post-grid__inner` (correct — the scroller) |
| `overflow-x` | `auto` (correct) |
| `scrollWidth` vs `clientWidth` | **1200 vs 1200 — NO overflow** |
| `cursor` | `auto` — **the module did NOT attach** |

This is the runtime's structural guard behaving CORRECTLY: with only the posts this
canary site happens to hold, the carousel track does not overflow, so there is nothing to
drag and `fx-draggable.js` declines. It is NOT evidence the capability works, and it is
NOT evidence it is broken. It is untested.

**Owed:** a fixture with enough posts to overflow the track at 1440px, then a gesture
capture showing `scrollLeft` following the pointer and coasting with momentum on / stopping
with it off — the same two-arm control already used for `sgs/gallery`.

The `verdict: PASS` above is specifically a FIRST-PAINT verdict, which is what this gate
asks of a src change. It is not a claim that drag works here.
