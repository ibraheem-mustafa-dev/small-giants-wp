# sgs/cta-section — D388 editor-canvas verification (2026-07-28)

**Change under test:** Shadow control is now the full `ShadowControl` builder (X/Y/blur/spread/colour/inset + presets), replacing the None/Small/Medium select.
**Commit:** 64999cd2, deployed to sandybrown canary.
**Test page:** draft page 1849, "spec35-wave1-d388-2026-07-28".

## What was exercised

1. Inserted `sgs/cta-section` — canvas rendered the section heading/text/button-group placeholders cleanly, no crash placeholder.
2. Expanded the "Shadow" inspector panel — confirmed it shows a preset `ButtonGroup` (None / Small / Medium / Large / Glow / Natural / Deep / Sharp / Outlined / Crisp), matching `ShadowControl.js`'s theme.json-sourced preset row (more presets than the task brief's "None/Small/Medium" implies the OLD control had — confirms this is genuinely the new component, not a relabelled old one).
3. Clicked "Small" preset — the full builder row appeared below the presets (Offset X, Offset Y, Blur, Spread, Shadow Colour with alpha, Inset toggle) — confirms the conditional builder-reveal (`{ !! value && (...) }` in `ShadowControl.js`) fires correctly. 0 console errors.
4. Set a custom Blur value of `24` (overriding the "Small" preset's blur) via the Blur `UnitControl` — applied cleanly, 0 console errors.
5. Saved draft, reloaded the editor.
6. Re-selected the block, expanded "Shadow" — Offset X=0, Offset Y=4, Blur=24, Spread=0 were all still applied, confirming persistence of the custom-blur override.

## Console

- 0 errors at block insertion, Shadow panel expand, preset click, blur edit, save, or reload.
- 7 warnings on insertion — same pre-existing framework deprecation notices seen on every block insertion this session, unrelated to this change.

## Screenshots

- `reports/visual-diff/cta-section-canvas-2026-07-28.png` — canvas after inserting the block.
- `reports/visual-diff/cta-section-shadow-persisted-2026-07-28.png` — inspector after reload, confirming Blur=24 persisted.

## Verdict

verdict: PASS

- Block renders, no crash: PASS
- Full ShadowControl builder (X/Y/blur/spread/colour/inset) present and functional: PASS
- Preset click reveals builder: PASS
- Custom blur value set + persisted after reload: PASS
- No console errors: PASS

first_paint_capture_passed: true
