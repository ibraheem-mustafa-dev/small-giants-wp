# Visual diff — sgs/gallery — 2026-09-04

verdict: PASS
intent_capture_passed: true
source_sha: 34f92e47f4b94209

## What changed

Spec 35 Part 4, second of four target blocks — same mechanism as the card-grid report filed
immediately before this one. `mediaItems` gains per-item `_key`/`objectFit`/`focalPoint`;
`GalleryThumbnail` (sidebar) and the editor-canvas preview both gained an object-fit dropdown +
`FocalPointPicker` (image items only); render.php emits a per-item scoped rule keyed by a new
`data-gallery-item-key` attribute, kept deliberately separate from the pre-existing
`:nth-child`-keyed aspect-ratio/stagger rule two lines above it (that one is genuinely
position-bound and safe with `:nth-child`; crop is identity-bound and is not).

## Why intent capture, not before/after

Same reasoning as card-grid: no existing gallery content can set these new per-item fields, so a
before/after diff shows nothing by construction. The real question is whether the mechanism
produces distinct, correct, per-item CSS.

## Live capture — real canary, generated stylesheet

Probe: page `spec35-item3-gallery-probe` (created via REST, live-verified, then deleted), two
`mediaItems`:
- Item A: `_key: gprobe-key-a`, `objectFit: contain`, `focalPoint: {x:0.3, y:0.7}`
- Item B: `_key: gprobe-key-b`, `objectFit: cover`, `focalPoint: {x:0.85, y:0.15}`

Fetched the generated stylesheet (`<link id="sgs-blocks-collected-css">`, same
P-STYLE-TAG-CONSOLIDATION mechanism as the card-grid report — see that report for the full
explanation of why the page body itself carries no inline `<style>`):

```
[data-gallery-item-key="gprobe-key-a"] img, [data-gallery-item-key="gprobe-key-a"] video{object-fit:contain;object-position:30% 70%;}
[data-gallery-item-key="gprobe-key-b"] img, [data-gallery-item-key="gprobe-key-b"] video{object-fit:cover;object-position:85% 15%;}
```

Both rules present, correct, and distinct. **PASS.**

## Risk

Additive only. The pre-existing block-wide `sgsObjectFit`/`sgsObjectPosition` + media-atom
mechanism is untouched; the new per-item rule only fires when an item's own value differs from
default, so existing content renders byte-identical. `GalleryThumbnail`'s video items
deliberately do not get the crop control (object-fit on a video thumbnail shows a static poster
frame, not a meaningful preview) — video items still render via the pre-existing mechanism,
unaffected.

## Gates

`php -l` clean · WPCS 0 new errors (39 pre-existing warnings, 62 pre-existing errors, unchanged
count before/after this change) · `npm run build` 0 · fast-tier gates 88/88 · deployed +
live-verified on the real canary (not just gate output).
