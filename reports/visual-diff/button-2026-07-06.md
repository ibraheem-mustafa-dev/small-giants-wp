# Visual diff — sgs/button — 2026-07-06

verdict: PASS
first_paint_capture_passed: true

## Change under review

Full-width (`widthType='full'`) fix — a full-width button did not fill its container when it
was a FLEX ITEM (inside sgs/multi-button's flex row or an sgs/container flex layout): the
wrapper `.sgs-button-wrapper--full` was shrunk by the browser default `flex-shrink:1`, so
`width:100%` never filled the line. Plus two cleanups: the dead `.sgs-button--full` element
class (emitted but styled nowhere), and the editor-vs-frontend divergence (editor forced an
inline `width:100%` on the preview + never added `--full` to the preview wrapper).

- **style.css:** `.sgs-button-wrapper--full` now `width:100%; flex:0 0 100%; max-width:100%;
  align-self:stretch;` — holds the full line as a flex item (basis 100%, no grow/shrink), fills
  the cross-axis in a flex column, and is a no-op in non-flex parents (where `width:100%` already
  worked). Inner `.sgs-button` keeps `width:100%`.
- **render.php:** removed the dead `sgs-button--full` element-class emission (kept the id-scoped
  `#uid .sgs-button{width:100%}` `<style>` + the `sgs-button-wrapper--full` wrapper class).
- **edit.js:** removed the dead `sgs-button--full` push + the inline `previewStyle.width='100%'`
  hack; the editor preview wrapper now conditionally carries `sgs-button-wrapper--full`, so the
  editor exercises the SAME wrapper CSS as the frontend (divergence gone).
- **block.json:** version 1.2.0 → **1.2.1**.

Preset/colour/typography logic (the `$is_custom`/`inheritStyle` gate) deliberately untouched —
that is the separate preset-as-seed task.

## Evidence (live homepage / page 8, sandybrown, deployed 2026-07-06)

Playwright, applying `--full` non-destructively to a real button wrapper inside a live
`display:flex; flex-wrap:wrap` multi-button row (parent width 350px):
- **BEFORE** `--full`: wrapper `199px` (content-shrunk — the bug).
- **AFTER** `--full`: wrapper `350px` = full line width (`fillsLine: true`); inner button expands
  to fill the wrapper's content box.

Confirms a full-width button now fills its container as a flex item, the exact case that failed.

---

## Session 2 (preset-as-seed) — remove the locked-preset system

verdict: PASS
first_paint_capture_passed: true

Bean-approved model change (remove the old system, require re-apply): picking a Style preset
+ clicking **Apply** COPIES that preset's values into the block's own attributes; render ALWAYS
paints from attributes; every control stays editable. Multi-button gets an "Apply to all buttons".

- **NEW `button/presets.js`:** `BUTTON_PRESETS` (primary/secondary/outline) as token-slug attr
  shapes matching the old `.is-style-*` fallbacks (primary: bg `primary`, text `text-inverse`,
  border `primary`, 2px, radius 10, weight 600, hover invert; secondary: transparent/`primary`;
  outline: transparent/`text`/`border-subtle`).
- **render.php:** removed the `$is_custom` gate — colour/border/hover/typography/box-shadow now
  paint UNCONDITIONALLY from attrs; removed the `.is-style-{preset}`/`sgs-button--custom` class
  emission (`inheritStyle` now emitted only as a `data-preset` debug attr).
- **style.css + editor.css:** removed the `.is-style-primary/secondary/outline` rule blocks (dead).
- **block.json:** fresh-button defaults seeded to the primary preset; `inheritStyle` enum trimmed
  to primary/secondary/outline; version 1.2.1 → **1.3.0**.
- **edit.js:** "Style preset" SelectControl + separate "Apply preset" button (`setAttributes({...
  BUTTON_PRESETS[inheritStyle]})`); colour/typography/border panels always editable.
- **multi-button/edit.js:** "Apply to all buttons" — iterates child `sgs/button` blocks via
  `getBlock(clientId).innerBlocks` + `updateBlockAttributes` per child. Version 1.2.0 → 1.3.0.

### Evidence (live homepage / page 8, sandybrown, deployed 2026-07-06)

The 6 real `sgs/button` blocks render FAITHFULLY after removing the is-style system — computed:
`background rgb(230,138,149)` (coral `primary`), `color rgb(255,250,245)` (`text-inverse`),
`border 2px solid coral`, `radius 10px`. Styling is now attribute-driven (no is-style class), and
visually identical to before. Screenshot: `buttons-preset-seed-landed-2026-07-06.png`.

Note: the two transparent "Add to Cart"/"Try 3 for £5" CTAs are **product-card** built-in buttons
(`wp-block-sgs-product-card`, class `.sgs-button--primary` which has NO CSS rule) — a pre-existing
product-card quirk, NOT a regression from this change (3c only touched `sgs/button` + `multi-button`).

Editor Apply / apply-to-all: built with standard WP editor APIs (setAttributes /
updateBlockAttributes); build passed the dead-control guard. To click-confirm in the editor.
