# Visual-diff — sgs/buybox (NEW block, Spec 30 P1 FR-30-7) — 2026-06-11

verdict: PASS
first_paint_capture_passed: true

**Change:** NEW `sgs/buybox` dynamic block — the PDP buybox: per-axis option-picker pills, SSR-seeded live price row, stock line, proxy add-to-cart form, dismissible error region. Mounts the shipped `sgs/product-card` Interactivity store (no new JS engine). Replaces core `product-price` + `add-to-cart-with-options` in `parts/sgs-pdp-buybox.html` for variable products; simple products fall back to the core blocks via `do_blocks()`.

**Validation (live on the sandybrown canary, 2026-06-11, R-22-11):**
- **First-paint capture:** full-page screenshots at 375/768/1440 via the committed FR-30-11 script (`scripts/wc-pages-responsive-audit.js`) — `audit-pdp-{375,768,1440}.png` in `.claude/reports/spec30-p1/`. SSR price row text == seeded context value (£9.99 default combo) before any JS runs (SSR-wipe-safe pattern verified).
- **Interaction:** 3 pill combos (incl. multi-axis) update the price live (£9.99 → £18.99 → £59.99); add-to-cart POSTs the EXACT variation (id 578) via `/sgs/v1/cart/add-item`; drawer auto-opens on success; simulated 503 renders the dismissible error (44×44 dismiss) and re-enables the button; COD order 1124 completed end-to-end.
- **Composition matrix:** simple product (513) → core fallback path renders; single-variant fixture (1125) → buybox with ZERO pills + price + button; unavailable terms grey (aria-disabled), never hidden.
- **Gates:** zero horizontal overflow at all 3 widths; axe 0 violations on the PDP at 375/768 (the @1440 `list` finding is the pre-existing sitewide core nav `ul>ul` page-list fallback, not this block); qc-council (sonnet+haiku) MERGE-OK with zero blockers.
- Full evidence: `.claude/reports/spec30-p1/QA-GATE-B-2026-06-11.md`.

## Result — PASS (R-22-11)
