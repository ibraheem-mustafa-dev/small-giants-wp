# Visual diff — sgs/text — pair max-width with margin-inline:auto — 2026-09-08

verdict: PASS (live-verified, fix deployed 2026-09-08)
first_paint_capture_passed: true
source_sha: PLACEHOLDER

Covers the clone-fidelity programme's Phase 4 fix for 3.8 (ingredients section: intro and
disclaimer text blocks sit flush left even though their text is centred).

## What changed

`plugins/sgs-blocks/src/blocks/text/render.php` — the `maxWidth` CSS emission now pairs
`margin-inline:auto` alongside `max-width`, whenever `maxWidth` is set. Mirrors the identical,
already-correct pattern in `sgs/quote`/`sgs/testimonial`/`sgs/option-picker`/`sgs/before-after`
— a repo-wide grep confirmed `sgs/text` was the sole outlier among every block emitting a bare
`max-width` declaration.

## Before/after — live measurement, same page, before the deploy and after it

Captured by dedicated verification agents against https://sandybrown-nightingale-600381.hostingersite.com/
(page 2742), Playwright, 1440px viewport, immediately before and after the deploy landed.

| Element | Before margin-left/right | After margin-left/right | Centred? |
|---|---|---|---|
| Ingredients intro ("Every Zookie...", maxWidth 540) | 0px / 0px (flush left) | 210px / 210px | PASS — equal both sides |
| Disclaimer box ("We make nourishing food...", maxWidth 620) | 0px / 0px (flush left) | 170px / 170px | PASS — equal both sides |

## Regression check — every other maxWidth-bearing sgs/text instance on the page

Surveyed all 21 `sgs/text` instances rendered on the homepage. Only 3 have `maxWidth` set:
the two above, plus the hero's split-column intro paragraph (maxWidth 420px). That instance
also received `margin-inline:auto`, but its computed margins don't match a true centred
half-gap — its flex parent controls horizontal position, so the auto-margin is inert there.
Screenshot confirmed it renders unchanged, still correctly left-aligned in the hero column.

## Assertions — stated before measuring

1. Both target elements are now genuinely centred (equal gap to both parent edges).
2. No regression on any other `maxWidth`-bearing `sgs/text` instance on the same page.

## Results

| # | Assertion | Result |
|---|---|---|
| 1 | Both elements centred | **PASS** — 210px/210px and 170px/170px, exact matches both sides |
| 2 | No regression elsewhere | **PASS** — the one other affected instance (hero split-column intro) unchanged visually, flex parent overrides the inert auto-margin |

No regressions observed. Deployed via `build-deploy.py --payload plugins/sgs-blocks/src/blocks/text/render.php`
(dirty-tree deadlock break, per the project's documented pattern) — motion-QA and the
payload-checksum verify both passed on the same deploy run.
