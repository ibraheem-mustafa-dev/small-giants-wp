# Visual diff — sgs/trust-bar — strip inline style from raw-SVG icon fallback — 2026-09-08

verdict: PASS (live-verified, fix deployed 2026-09-08)
first_paint_capture_passed: true
source_sha: e8bd5c422fbd7bb1

Covers the clone-fidelity programme's Phase 4 fix for 3.4 (trust bar's 4th badge icon,
"loved by breastfeeding mums", rendering black instead of the draft's rose-pink).

## What changed

`plugins/sgs-blocks/src/blocks/trust-bar/render.php` — the icon-resolver-fallback branch
(fired when a badge's icon couldn't be matched to a Lucide slug, so the draft's raw SVG is
passed through) now strips the inline `style="…"` attribute from that raw SVG before
`wp_kses()` sanitisation. Scoped to this one branch only; `sgs_svg_kses_allowed_tags()`'s
shared allowlist is untouched (a different raw-SVG path, responsive-logo's uploaded brand
artwork, still needs its own inline colouring preserved).

## Before/after — live measurement, same page, before the deploy and after it

Captured by dedicated verification agents against https://sandybrown-nightingale-600381.hostingersite.com/
(page 2742), Playwright, desktop viewport, immediately before and after the deploy landed.

| Metric | Before | After |
|---|---|---|
| Star badge (4th icon) computed `fill` | `rgb(0, 0, 0)` (black) | `rgb(197, 106, 122)` |
| Other 3 badges' computed `color`/`stroke` | `rgb(197, 106, 122)` | `rgb(197, 106, 122)` (unchanged) |
| Star icon shape (`fill`/`stroke` attributes) | `fill="currentColor" stroke="none"` | unchanged — attributes untouched, only inline `style` removed |

## Assertions — stated before measuring

1. Star badge's computed fill matches the other 3 badges' colour (both resolve to the
   same rose-pink token).
2. Star icon's own `fill`/`stroke` attributes are unaffected (only the inline `style`
   attribute is stripped — the shape-defining attributes must survive).
3. No other badge or block visibly regresses (screenshot check).

## Results

| # | Assertion | Result |
|---|---|---|
| 1 | Colour now matches | **PASS** — `rgb(197, 106, 122)`, exact match |
| 2 | Shape attributes intact | **PASS** — `fill="currentColor" stroke="none"` unchanged, verified in the live DOM |
| 3 | No other regression | **PASS** — screenshot confirmed a normal, solid, correctly-coloured star; other 3 badges unchanged |

No regressions observed. Deployed via `build-deploy.py --payload plugins/sgs-blocks/src/blocks/trust-bar/`
(dirty-tree deadlock break, per the project's documented pattern) — motion-QA and the
payload-checksum verify both passed on the same deploy run.
