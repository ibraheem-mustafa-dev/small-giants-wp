---
block: card-grid
date: 2026-06-07
verdict: PASS
first_paint_capture_passed: true
---

# Visual-diff — sgs/card-grid gap-control consolidation — 2026-06-07

**Change:** Gap control consolidated onto the shared sgs/container ContainerWrapperControls 'Gap'. card-grid was ALWAYS slug-based (old $spacing_map mapped slug->preset var); render now routes via sgs_container_gap_value() which produces the IDENTICAL var(--wp--preset--spacing--N) for a stored slug. No deprecation needed (no type/save change). Editor: block valid, 0 console errors.

**Back-compat verification:** Stored slug '40' -> var(--wp--preset--spacing--40), byte-identical to the old $spacing_map output. No visual change for existing content.

**QC:** /adversarial-council (6 personas) gated this change set pre-commit; the convergent back-compat must-fixes (per-block render px-append, dynamic-block deprecation isEligible, trust-bar editor preview, heading render coercion, wrapper esc_html) were applied and re-verified. Existing client content renders identically.

first_paint_capture_passed: true (live frontend / editor render confirmed; no visual regression vs pre-change output for existing stored values).
