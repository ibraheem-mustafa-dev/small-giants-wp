# Visual diff — nav-menu — 2026-07-28 (Gate-3 live verification)

Supersedes `nav-menu-2026-07-27.md` (`verdict: INCOMPLETE`). Fixture as in
`mega-panel-2026-07-28.md`.

## Verified live (sandybrown, 2026-07-28)

- **Real-position render (FR-36-5):** bar order Home · **Brands[mega `<button>`]** ·
  Recipes · Contact — the mega renders at menu position 2, not last.
- **Disclosure contract (FR-36-10):** `role="menu"` count 0; trigger is
  `<button aria-expanded aria-controls>`; no `aria-haspopup`.
- **axe = 0 on the OPEN panel** (guarded: 7 links + 207 chars confirmed inside
  before the run — a scoped axe on a hidden surface passes vacuously, proven
  against `nav-qa/axe-run.mjs` with no `--open`).
- **axe = 0 on the OPEN drawer** (guard: height 0→780, 14 focusables) — closes the
  INCONCLUSIVE standing since 2026-07-23.
- **Keyboard:** Tab walks all 8 panel links then EXITS to "Recipes" (no trap);
  Escape closes + focus returns to the trigger (`focusIsTrigger:true`).
- **Reduced-motion:** sampled 120ms after open — panel + children already at
  opacity 1; indicator/magnet transitions 0.01ms/off. Nothing left hidden.
- **JS-off (FR-36-17):** all 7 panel links, 3 headings, aside copy present in the
  pre-JS HTML.
- **CF-1 recursion, run live:** panel 1745 given a nav bound to menu 100 (its own
  menu) → HTTP 200, no fatal, inner "Brands" degraded to a plain `<a>`, no nested
  panel. Fixture restored + re-verified.
- **Hover safety (Bean-directed):** 400ms slow-diagonal trigger→panel survives on
  BOTH the page nav and the header nav after the z-order fixes; leaving the panel
  closes after grace. Root cause + fix in `mega-panel-2026-07-28.md` §defects.
- **Drawer no-regression:** drawer opens, axe 0, `store('sgs/nav')` untouched.

## Recorded gaps (deferred-not-dropped)

- **Plain (non-mega) dropdowns NOT built** — submenu children flatten to the parent
  link (`render.php:103-109`, "no children this phase"); proven live: menu 100's
  "Our Story"/"Sourcing" absent from the bar. Recorded in Spec 36 §6a (STOP-29).
- Centring is **mega-only by selector**; plain dropdowns must anchor left-aligned
  under their item when built (Bean ruling 2026-07-28 — the Indus draft's centred
  "More" dropdown reads badly).

verdict: PASS (R-31-13 eye sign-off pending)
first_paint_capture_passed: true
