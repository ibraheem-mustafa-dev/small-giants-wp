---
block: trust-bar
date: 2026-06-06
verdict: PASS
first_paint_capture_passed: true
change: D182 bound-mode purge → typed native emission
surface: live homepage (page 8), sandybrown canary
method: Playwright live-DOM (R-22-11) + element screenshot + REST markup inspection
---

# Visual diff — sgs/trust-bar (D182 bound-mode purge → typed native emission)

## What changed
The cloning converter previously emitted trust-bars in `sourceMode='bound'` (echoing the
draft DOM as `$content` → nested `sgs/container` badge blocks, 1-column collapse, no native
circles/icons). It now extracts the draft badges into the block's native `items[]` and the
block renders them itself (typed path). See `.claude/reports/2026-06-06-bound-mode-purge-plan.md`.

## Live-DOM evidence (real homepage, page 8) — first paint
Playwright `getComputedStyle` + element queries on `https://sandybrown-nightingale-600381.hostingersite.com/`:

| Check | Expected | Actual | Verdict |
|---|---|---|---|
| `sgs/trust-bar` block present | yes | yes | PASS |
| `sourceMode` attr | absent/typed (NOT bound) | absent | PASS |
| Native badges rendered (`.sgs-trust-bar__badge`) | 4 | 4 | PASS |
| Hidden badges | 0 | 0 | PASS |
| Native circles (`.sgs-trust-bar__circle`) | 4 | 4 | PASS |
| Placeholder icon glyphs (svg in circles) | 4 | 4 | PASS |
| Nested `.wp-block-sgs-container` (duplicate-nesting) | 0 | 0 | PASS |
| Layout | grid, 4 columns | grid, `278px × 4` | PASS |
| Labels (incl. `£35` UTF-8) | 4 correct | "Handmade in Birmingham" / "Registered Food Business" / "Free UK Delivery Over £35" / "Loved by Breastfeeding Mums" | PASS |

first_paint_capture_passed: true — element screenshot at `reports/visual-diff/trust-bar-d182-typed.png`
shows 4 native badges (circle + placeholder tick + label) in a row on the surface-pink band.

## Regression guard
Page 589 (`sgs-configurator-test-540`) WC configurator: product-card present, 34 interactive
option pills, live price `£9.99` resolves — INTACT (the converter never touches existing post
content; only page 8 was re-cloned). PASS.

## Known interim (Bean-approved, D182)
Icons are uniform placeholder ticks pending the deferred icon-identity resolver (next task).
Native structure + correct labels are delivered. This is the agreed shippable interim.

## Verdict: PASS
