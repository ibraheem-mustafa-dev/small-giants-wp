# sgs/team-member — D388 editor-canvas verification (2026-07-28)

**Change under test:** per-item social links now `SgsLinkControl` with per-item new-tab/rel.
**Commit:** 64999cd2, deployed to sandybrown canary.
**Test page:** draft page 1849, "spec35-wave1-d388-2026-07-28".

## What was exercised

1. Inserted `sgs/team-member` — canvas rendered the photo placeholder + Name/Role/Bio fields, no crash placeholder.
2. Expanded the "Social Links" inspector panel — confirmed description text and "Add social link" button present.
3. Clicked "Add social link" — item `#1 — website` appeared with a Platform dropdown and a `SgsLinkControl` "LINK" field.
4. Set URL `https://example.com/team-social` via the combobox, selected the suggestion — applied cleanly (`example.com/team-social` shown with link chip). 0 console errors.
5. Attempted "toggle new-tab" — **could not be exercised**: same shared SgsLinkControl bug as sgs/icon (no "Open in new tab" toggle rendered anywhere in the panel — only implicit via the Platform/Link fields, confirmed no such control exists).
6. Saved draft, reloaded the editor.
7. Re-selected the block, expanded "Social Links" — `example.com/team-social` was still applied to item #1, confirming persistence.

## Console

- 0 errors at block insertion, "Add social link", URL entry, save, or reload.
- 8 pre-existing `sgs/container` block-validation errors appeared when searching the inserter for "SGS Team Member" (same pattern-preview errors seen earlier in the session for icon/pricing-table searches — triggered by the inserter rendering "Features — Icon Grid" / "Services — Feature Grid" pattern previews, unrelated to sgs/team-member itself).

## Screenshot

`reports/visual-diff/team-member-persisted-2026-07-28.png` — inspector after reload, showing the persisted social link.

## Re-verify after 7f4f399a (2026-07-28, same session, later pass)

Fix commit 7f4f399a deployed to sandybrown canary.

1. Hard-reloaded draft page 1849 — 0 console errors.
2. Selected the block, clicked "Edit link" on the existing `example.com/team-social` item, expanded "Advanced" — **"Open in new tab" now renders**.
3. **Honest note:** same as social-icons — the checkbox was already checked (`opensInNewTab: true`) from the data store before any action this pass. Toggled it OFF (confirmed `checked: false`) then back ON (confirmed `checked: true`) to prove the control is genuinely interactive, ending in the required ON state — both toggles fired with 0 console errors.
4. Clicked "Apply" — 0 console errors.
5. Read the item's attributes from the `core/block-editor` data store: `{ platform: "website", url: "https://example.com/team-social", opensInNewTab: true, rel: "noopener" }`.
6. Saved draft, hard-reloaded the editor — 0 console errors.
7. Re-read the data store post-reload: **identical values persisted**.
8. Screenshot captured: `reports/visual-diff/team-member-reverify-newtab-2026-07-28.png`.

Console errors observed during re-verify: **none**.

## Verdict

verdict: PASS

- Block renders, no crash: PASS
- URL set + persisted: PASS
- No block-caused console errors: PASS
- New-tab toggle: PASS (control renders, demonstrated interactive via off→on toggle, persists with `opensInNewTab=true` + `rel=noopener`, re-verified after 7f4f399a)

first_paint_capture_passed: true
