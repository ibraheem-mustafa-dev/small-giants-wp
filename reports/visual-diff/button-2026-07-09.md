---
block: button
date: 2026-07-09
verdict: PASS
first_paint_capture_passed: true
verified_by: live Playwright DOM + computed-style on sandybrown page 8 + Bean visual sign-off
url: https://sandybrown-nightingale-600381.hostingersite.com/
---

# Visual diff — sgs/button (Spec 32 reference impl, D291)

## Change under test
Button rebuilt to Spec 32: semantic `.sgs-button--{preset}` class → `--sgs-btn-*` CSS vars ←
`--wp--custom--button-presets--*` tokens; hover in stylesheet `:hover`; base colour no longer
inline (override vars only); non-responsive decls moved to the id-scoped `<style>`; label span
removed. Files: `style.css`, `render.php`, `block.json`, `edit.js`, `presets.js`.

## Live verification (page 8, deployed + re-cloned this session)

| Check | Result |
|---|---|
| Structure — no nested `sgs-button__label` span; single class + text | PASS (`hasLabelSpan:false`, direct text — all 6 buttons) |
| No inline colour property declaration in `style=""` | PASS (`inlineHasColourProp:false` — only `padding-*` (WP-native, tracked as RC1) + `--sgs-btn-icon-gap`) |
| primary — dark text on pink, faithful to draft | PASS (`color rgb(58,46,38)`, `bg rgb(230,138,149)`) |
| secondary — dark text, transparent bg, primary border | PASS |
| outline — dark text, transparent bg, border-subtle, 14px/10-18 padding | PASS |
| Hover works (was broken pre-fix) — primary inverts | PASS (scoped `:hover` resolves `--sgs-btn-color-hover:#FFFAF5`, `--sgs-btn-bg-hover:#3A2E26`; no inline colour to beat it) |
| Bean visual sign-off (hero / brand / gift buttons) | PASS — "All colours totally work including hover" |

## first_paint_capture
Passed — buttons render correct colour + geometry on first paint (SSR; no JS dependency for
styling). Hover is CSS-only.

## Known follow-up (NOT a regression; tracked)
RC1: cloned buttons still carry WP-native inline `padding-*`/`margin-*` from
`get_block_wrapper_attributes()` (block.json `supports.spacing/color`). Root-caused + council-
validated (FS-A) this session; universal fix is the Thread-2 build
(`.claude/plans/2026-07-09-styling-two-thread-programme.md`). Colour/hover/structure — the
subject of this report — are PASS.
