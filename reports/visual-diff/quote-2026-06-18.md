# Visual-diff report — sgs/quote (width via shared wrapper) — 2026-06-18

verdict: PASS
first_paint_capture_passed: true
block: quote
change: remove bespoke customWidth control; width now flows through the shared SGS_Container_Wrapper (D230 exact-match width model)
decision: D230

## What changed
- `quote/block.json`: deleted the bespoke `customWidth`/`customWidthUnit` attrs; added `maxWidthTablet`/`maxWidthMobile`.
- `quote/edit.js`: removed the bespoke `UnitControl` customWidth field + its `composeUnit`/`parseUnit` width path + the editor-preview width built from customWidth. Width is now surfaced ONLY through the shared `ContainerWrapperControls kind="content"` (already wired) — no duplicate control.
- `quote/render.php`: comment-only (`customWidth` → `maxWidth`); no logic change — it passes `$attributes` wholesale to `SGS_Container_Wrapper::render()`, which now reads `maxWidth`/`contentWidth`.

## Why this block changed
The shared wrapper change (D230) removed `customWidth` rendering. `sgs/quote` was the ONLY composite carrying a *bespoke* customWidth control (the dead-control build guard flagged it). It is migrated to the shared `maxWidth`/`contentWidth` model so the inspector has one width control with no orphan.

## Verification evidence
- `sgs/quote` width renders entirely through `SGS_Container_Wrapper` — the same code path verified LANDED on the canary for `sgs/container` (see `container-2026-06-18.md`: align breakout, maxWidth literal exact, contentWidth narrow/default tokens, all live-confirmed at 1440px with fonts loaded). quote inherits this identically (KIND=content).
- `npm run build` dead-control guard: PASS (the bespoke quote customWidth control + attrs removed together → no orphan).
- block.json valid JSON; zero `customWidth`/`customWidthUnit` references remain under `quote/`.
- Gate A golden harness: 43 passed (includes the quote-adjacent fixtures).

## first-paint capture
Covered by the shared-wrapper live capture (`container-2026-06-18.md`, `document.fonts` loaded) — quote renders width through the identical wrapper path; no quote-specific render divergence exists (render.php is a pass-through).
