# Visual diff — sgs/nav-menu — 2026-09-13 (submenu panel-bg lip + FR-41-36 drawer contrast)

verdict: PASS
intent_capture_passed: true
source_sha: 823c4111f6cc19c4

**This is a SECOND, LATER entry for this block on this date** — two tracks touched
`sgs/nav-menu` on 2026-09-13 (this repo's normal shared-`main` state, per
`visual-report-sha.py`'s own header comment). This entry's `source_sha` is keyed to the
CURRENT staged content (`plugins/sgs-blocks/src/blocks/nav-menu/{block.json,render.php}`,
which now include this track's own edits on top of the earlier `megaDrawerFallbackIds`
commit below). The earlier entry is preserved verbatim beneath this one, unmodified — see
`~/.claude/memory/*read-before-overwrite-dated-report-files*` for why it is not being
overwritten.

**Change:** dropdown/mega submenu panel no longer exposes a mismatched background strip
above/below its rows in any state, and the drawer's nested submenu link colour now resolves
to a genuinely contrast-checked `primary` token instead of a hardcoded `color:inherit`.

**Full evidence:** `reports/visual-diff/nav-menu-submenu-lip-drawer-contrast-2026-09-13.md`
(root cause, fix rationale, live stylesheet extraction, contrast maths, screenshots). Kept
short here to avoid duplicating that report; this entry exists to satisfy the per-block gate
file-naming convention (`reports/visual-diff/<block>-<date>.md`) without destroying the
earlier same-day entry.

**Files touched by this track:** `plugins/sgs-blocks/includes/nav-menu-submenu-css.php`,
`plugins/sgs-blocks/src/blocks/nav-menu/render.php` (context read + param pass-through),
`plugins/sgs-blocks/src/blocks/nav-menu/block.json` (`usesContext += sgs/navDrawerBg`).

---

# Visual diff — sgs/nav-menu — 2026-09-13

verdict: PASS
intent_capture_passed: true
source_sha: 4d3abde3679606b9

## What changed

New `megaDrawerFallbackIds` attribute + `MegaDrawerPanel.js` inspector panel (Settings tab).
A mega-typed menu item authored with real nested child links (a `core/navigation-submenu`
pointing at an `sgs_mega_menu` CPT, with its own menu children — not the CPT panel content)
renders those children as an ordinary accordion in the DRAWER when its identifier is listed
in `megaDrawerFallbackIds`, instead of the item's existing default: a plain-link degrade.
Bar/desktop rendering is untouched in every case (`sgs_nav_menu_render_items()` was not
touched). Files: `plugins/sgs-blocks/src/blocks/nav-menu/block.json`, `render.php`,
`edit.js`, `useNavMenuSource.js`, `utils.js`, new `MegaDrawerPanel.js`;
`plugins/sgs-blocks/includes/nav-menu-markup.php` (`sgs_nav_menu_render_items_drawer()`).

## Why intent_capture, not first_paint

There is no "before" render to diff against for the ON state — it is a brand-new opt-in
branch with no prior equivalent. The real question is "does the current state match what
was intended", checked directly against the live canary.

## Assertions

1. A mega item with **no** nested children, `megaDrawerFallbackIds` unset (default `[]`):
   drawer renders the item as a plain link — byte-identical to pre-existing behaviour.
2. A mega item **with** nested children, `megaDrawerFallbackIds` unset: drawer STILL renders
   a plain link (having children alone must not change anything — the toggle must be
   explicit).
3. The same mega item, its identifier now listed in `megaDrawerFallbackIds`: drawer renders
   it as an ordinary accordion (`<details>`, its nested child as a real sub-link), not the
   plain-link degrade.
4. In all three cases the desktop/bar form renders the full mega-panel disclosure,
   unaffected.

## Live result — sandybrown canary, page 3528 (`?ref=100` "SPIKE Mega Test" menu, mega item
"Brands", `object_id` 1745; test child "Brand A" added under it, then removed)

| Assertion | Live result |
|---|---|
| 1. No children, unset | Plain link, `href="…?sgs_mega_menu=spike-brands-panel"` — ✅ matches prior/default behaviour |
| 2. Has child, unset | Still a plain link (`<li … ><a class="sgs-nav-menu__link" …>Brands</a></li>`) — ✅ no incidental change from adding a child |
| 3. Has child, `megaDrawerFallbackIds:["id:1745"]` | Renders as `<li class="… sgs-nav-menu__item--has-submenu"><div class="sgs-nav-menu__accordion-row">…<details class="sgs-nav-menu__accordion" …>` with "Brand A" as a real `sgs-nav-menu__subitem` link inside — ✅ accordion fallback confirmed |
| 4. Desktop/bar, all three states | `<div class="sgs-nav-menu__mega" data-wp-interactive="sgs/mega" …>` mega-panel markup present and unchanged (checked raw HTML, `id="sgs-nav-menu-…-mega-1745"`) — ✅ bar untouched |

Identifier note: the checklist/attribute keys on the menu item's REST `object`/`id` field
(`id:1745`, the target `sgs_mega_menu` post id), NOT the `nav_menu_item`'s own post id
(1746) — matches `render.php::from_link()`'s existing `featuredItemIds` scheme exactly, so
`MegaDrawerPanel.js`'s checklist (fed by the same `useNavMenuSource.js`/`utils.js`
`isMega`/identifier resolution) produces a value that already matches what render.php
expects, with no extra mapping step.

## Risk

Additive-only. New attribute defaults to `[]`; the mega branch in
`sgs_nav_menu_render_items_drawer()` only takes the new path when BOTH the item has real
children AND its identifier is explicitly listed — an untouched instance, or a mega item
with no children, is byte-identical to before. No change to `sgs_nav_menu_render_items()`
(bar/desktop).

## Gates

`npm run build` 0 · hover-guard PASS · motion-bundle-budget PASS · shader-sources PASS ·
WPCS 0 new errors (pre-existing file-structure warning in `render.php`, unrelated to this
change, unrelated line range) · payload-verify (prior full deploy) 83/83.
