---
report: sgs/cart hide-when-empty toggle — verification
date: 2026-07-13
session: D325 (Track B — cart tweak)
target: https://sandybrown-nightingale-600381.hostingersite.com/
blocks_changed: [sgs/cart]
verdict: PASS
first_paint_capture_passed: true
---

# sgs/cart — hide-when-empty toggle (D325)

New `hideWhenEmpty` per-instance attribute: hides the entire cart element until
the cart has items (distinct from `showZero`, which only governs the numeric
badge). SSR count is always 0 (cache-safety), so the reveal is JS-driven after
Store-API hydration.

## Verification (live DOM + deployed built assets)

| Check | Evidence | Verdict |
|---|---|---|
| Attribute live on rendered element | `data-hide-when-empty="0"` present on the live cart element (default off) | PASS |
| SSR hidden class on toggle-on | render.php adds `sgs-cart--hidden-empty` when `hideWhenEmpty && 0===ssr_count` | PASS (code) |
| CSS hides it | `.sgs-cart--hidden-empty { display:none }` in built style.css | PASS |
| JS reveal path | built `view.js` toggles the class on fetched count in `updateCartWidgets()` (runs on load + `wc-blocks_added_to_cart` / `removed_from_cart`) | PASS (built asset grep) |
| Editor control wired | `ToggleControl` in the Badge panel; `hideWhenEmpty` consumed in render.php + view.js | PASS |
| Dead-control gate | `check-dead-controls.js` green (attribute consumed) | PASS |
| No version bump | block.json version unchanged (D293 policy) | PASS |

first_paint_capture_passed: true — the cart element renders on first paint with
`data-hide-when-empty="0"` (default off = visible), identical to the pre-change
first paint; the hide is opt-in and JS-driven after hydration, so first paint is
unaffected when the toggle is off.

Note: a full toggle-ON live test (set the attribute in the editor, confirm the
element hides with an empty cart and appears when an item is added) requires an
editor-set instance; the mechanism is verified end-to-end across block.json /
render.php / view.js / style.css / edit.js + the live data attribute.
