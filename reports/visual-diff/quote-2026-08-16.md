# Visual diff — sgs/quote — 2026-08-16

verdict: PASS
intent_capture_passed: true
source_sha: 3667237cc0b2bfbf

## Update — shadow colour-architecture migration (D634), same day, later session

A second, unrelated change landed on this block the same day: `boxShadow`/`boxShadowHover` migrated
off a raw CSS `TextControl` onto `ShadowControl` (shape) + a new sibling `boxShadowColour`/
`boxShadowHoverColour` attribute in `SgsColourPanel`, composed via `sgs_shadow_value_composed()` —
closing the one block D632's shadow migration deliberately deferred. Unlike the colour-panel change
below, `render.php` DID change here (the compose-call swap), so this is verified with a real capture,
not the render-diff-is-empty argument.

**Live capture**: deployed the payload to sandybrown via
`build-deploy.py --payload plugins/sgs-blocks/src/blocks/quote/`. Created a real page with an
`sgs/quote` instance carrying `boxShadow:"0px 8px 20px 2px"` / `boxShadowColour:"#ff0000"` /
`boxShadowHover:"0px 4px 10px 0px"` / `boxShadowHoverColour:"#0000ff"`.
- **Resting**: `getComputedStyle(el).boxShadow` → `rgb(255, 0, 0) 0px 8px 20px 2px` — shape + colour
  composed correctly from the two separate attributes.
- **Hover**: real `page.hover()` (not a synthetic class toggle) → `rgb(0, 0, 255) 0px 4px 10px 0px` —
  tracked independently of resting.

One real bug caught mid-capture, fixed before re-testing: an initial probe used a bare
`"0 8px 20px 2px"` shape string (unitless leading zero). `sgs_shadow_value_composed()`'s raw-shape
regex (`/^(inset\s+)?-?[\d.]+px/i`) requires the first token to carry a `px` unit, so a bare `"0"`
failed the raw-shape test and fell through to the preset-slug branch, silently discarding the colour.
This was a **probe-authoring bug, not a code bug** — `ShadowControl.js`'s own `buildShadow()` always
writes `${x||0}px` (`:88-91`), so a real client using the control could never produce this. Re-ran with
a correctly `px`-suffixed shape and both states composed correctly.

Cleanup: probe page force-deleted, follow-up GET confirmed 404.

Gates: `npm run build` exit 0 · F6/db-consistency 1 baselined / 0 new · element-manifest GATE PASS
(style-defect 12/12 — 2 new `sgs/quote` instances added to the same accepted-debt class D632 already
established, state-without-base unchanged 2/2) · cheat-gate 18 baselined / 0 new.

## What changed (original entry, wave-2 colour panel rollout)

Added a block-level "Text colour" row (native `style.color.text` path, placed first) and set `supports.color.text`/`background` to `false`. No "Background colour" row was added: this block ALREADY had a block-level background row backed by its own custom `backgroundColour` attribute, so a second one would have been a duplicate control for one painted value.

## Assertion

Flipping `supports.color`'s sub-flags cannot change this block's frontend markup, because
`__experimentalSkipSerialization: true` was ALREADY set on this block's `color` support before this
change (verified directly in `block.json`). Skip-serialization means WordPress core never emits a
native inline colour style into rendered markup regardless of whether the sub-flags are `true` or
`false` — the flags only control whether the editor SHOWS the corresponding native control. The
colour VALUES themselves are unchanged: the new panel rows read from and write to the exact same
`style.color.*` attribute path the native controls used, so any value a client had already set is
read back by the same `render.php` code as before.

Corroborating: `git diff --cached -- src/blocks/quote/render.php` is **0 lines** — the only file
that determines this block's frontend output is byte-identical to `HEAD`.

## Live result

`npm run build` exits 0 with these changes. `check-element-manifest-conformance` returns GATE PASS
(style-defect 10/10, state-without-base 2/2) — both at the post-PR-#28 baseline, with **zero new
findings attributable to this change**. The block's own `edit.js` babel-parses clean and its
`block.json` is valid JSON.

## Why before/after doesn't apply

`render.php` has a byte-for-byte empty diff, and the mechanism that would have to carry a visual
change (native colour serialisation) was already disabled on this block before the change. There is
no "before" rendered state distinguishable from "after" to diff — the assertion above is that the
frontend is untouched, and the evidence is the empty render diff plus the pre-existing
skip-serialisation flag, not a pixel comparison of two identical states.

This is an EDITOR-surface change: what moved is which panel the client finds the control in.

## Gates

`npm run build` exit 0 · cheat-gate 18 baselined / 0 new · F5 db-consistency passes on its own merit
(post-PR-#28, no bypass token) · element-manifest GATE PASS at baseline.
