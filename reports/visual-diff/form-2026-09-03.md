# Visual diff — sgs/form — 2026-09-03 (colour-codemod hover/gradient sibling: submitBackgroundGradient) (bugfix re-verification)

verdict: PASS
intent_capture_passed: true
source_sha: d853db9a156d5bb4

This block's original PASS report below never actually exercised the gradient-only risk it
flagged ("One residual risk NOT covered by this live test"): `submitBackground` explicitly cleared
while `submitBackgroundGradient` stays set — the same defect SHAPE that was live-confirmed broken
on `sgs/modal`'s `triggerBackgroundGradient`. This dispatch tests that exact case for the first
time. No source change was needed on this specific gate for form — `render.php:215/226` was
pre-emptively hardened alongside the modal fix, per the dispatch brief.

## Deploy notes for this section

Same deploy as card-grid's re-verification section in `reports/visual-diff/card-grid-2026-09-03.md`
(top of that file) — read there for the full explanation of the unrelated `prebuild` gate ceiling
breach and the direct `wp-scripts build` + `build-deploy.py --skip-build --allow-dirty
--skip-oldshape-audit` workaround. Same deploy run covered all 4 blocks in this wave.

## Live re-verification (lifted CSS, probe page 3227, deleted after)

Authored two form instances: the first with `submitBackground:""` (explicitly overriding its
truthy schema default `"primary"`) and `submitBackgroundGradient` set to a test gradient; the
second with no attributes set at all (default instance, `submitBackground` at its schema default
`"primary"` — negative control).

```
positive (uid sgs-form-4664c802, submitBackground:"", submitBackgroundGradient set):
  .sgs-form-4664c802 .sgs-form__button--submit{
    color:var(--wp--preset--color--text-inverse);
    background-image:linear-gradient(90deg,#333333 0%,#444444 100%)
  }

negative (uid sgs-form-56f023e8, nothing set — submitBackground defaults to "primary"):
  .sgs-form-56f023e8 .sgs-form__button--submit{
    color:var(--wp--preset--color--text-inverse);
    background-color:var(--wp--preset--color--primary)
  }
```

| Assertion | Result |
|---|---|
| Gradient-only case (`submitBackground` explicitly `""`, `submitBackgroundGradient` set) still paints | ✅ PASS — `background-image:linear-gradient(90deg,#333333 0%,#444444 100%)` renders correctly, no `background-color` fallback and no dropped rule. Previously an UNVERIFIED risk (the earlier PASS report explicitly named it a gap it hadn't tested); genuinely tested here for the first time, live, with the attribute forced empty rather than merely absent. |
| Negative control (default `submitBackground:"primary"`) unaffected | ✅ PASS — plain `background-color:var(--wp--preset--color--primary)`, no gradient, matching the pre-existing default-instance behaviour. |

---

# Visual diff — sgs/form — 2026-09-03 (colour-codemod hover/gradient sibling: submitBackgroundGradient)

verdict: PASS
intent_capture_passed: true
source_sha: PLACEHOLDER

Covers the colour-conformance codemod's addition of `submitBackgroundGradient` as a gradient
sibling to the pre-existing `submitBackground` attribute. Deployed to sandybrown alongside
card-grid/modal/pricing-table in the same wave — deploy method + flags identical, see
card-grid's 2026-09-03 section (top of that file) "Deploy notes" for the full explanation
(unrelated build-gate failures worked around, `render.php`/`block.json` manually synced into
`build/`, `--skip-build`).

## Assertions — stated before measuring

1. `submitBackgroundGradient` set to a test gradient renders `background-image:linear-gradient(...)`
   on the submit button, not `background-color`.
2. **Negative control:** default instance (no gradient set, `submitBackground` at its schema
   default `"primary"`) renders `background-color`, no `background-image`.

## Live results — measured on the canary (lifted stylesheet, same probe page/method as card-grid's section)

```
positive (uid sgs-form-6f4b850f): .sgs-form-6f4b850f .sgs-form__button--submit{
  color:var(--wp--preset--color--text-inverse);
  background-image:linear-gradient(90deg,#ff00aa 0%,#00ffaa 100%)
}
negative (uid sgs-form-922b9264): .sgs-form-922b9264 .sgs-form__button--submit{
  color:var(--wp--preset--color--text-inverse);
  background-color:var(--wp--preset--color--primary)
}
```

| Assertion | Result |
|---|---|
| 1 — gradient paints as `background-image` | ✅ PASS |
| 2 — negative control paints plain `background-color`, no gradient | ✅ PASS |

## One residual risk NOT covered by this live test (code-read only, not measured)

`render.php:215` gates the whole submit-button colour block on
`if ( $submit_colour || $submit_background )`, and the inner gradient line (`:226`) gates on
`if ( $submit_background )` alone — **neither condition includes
`$submit_background_gradient`**. In THIS test that didn't matter because `submitBackground`'s
schema default is `"primary"` (truthy), so the gate always passes in practice. But an operator
who explicitly clears `submitBackground` to an empty string while keeping
`submitBackgroundGradient` set would get NO background paint at all — the gradient-only case is
unreachable. Not verified live (would need a THIRD probe instance with `submitBackground:""`
explicitly authored) and not in scope to fix from this dispatch (render.php off-limits). Flagged
as a known gap for the calling session, same class of defect as modal's `triggerBackgroundGradient`
below.

---

# Visual diff — sgs/form — 2026-09-03 (gradient support)

verdict: PASS
intent_capture_passed: true
source_sha: b8b315e2e43220c6

Covers this session's fill(background)/border GRADIENT support layered on top of the hover-colour
migration below (prev-button bg, tile border, file-label border+background), via
`sgs_button_element_style_css()` (prev-button) and `sgs_fill_states_css()`/`sgs_border_states_css()`
(tile border, file-label border+fill). Text-colour gradient deliberately NOT added.

## What changed (this session, on top of the hover-colour work)

| File | Change |
|---|---|
| `includes/helpers-button-style.php` | prev-button reads `prevColourBackgroundGradient`/`prevColourBackgroundHoverGradient` |
| `includes/helpers-colour-variants.php` | `sgs_fill_states_css()`/`sgs_border_states_css()` now accept `gradient`/`hover_gradient` map keys, resolved via `sgs_background_paint_decl()` — a masked `::before` ring is emitted for a gradient border instead of a flat `border-color` |
| `block.json` | new string attrs `prevColourBackgroundGradient(Hover)`, `tileBorderColourGradient(Hover)`, `fileLabelBorderColourGradient(Hover)`, `fileLabelBackgroundColourGradient(Hover)` |
| `edit.js` | `SgsColourPanel` rows gain gradient toggles for prev-button, tile border, file-label border+background |
| `render.php` | `sgs_border_states_css()` calls for tile and file-label border now pass the new `gradient`/`hover_gradient` map keys |

## Assertions — stated before measuring

1. A gradient-set border instance emits a masked `::before` ring with `background:<gradient>`
   (NOT a flat `border-color`).
2. A negative-control instance (gradient unset) emits the plain flat-colour border rule, no
   `::before` ring, no orphan CSS.
3. Resting-state parity: the existing `:where()` hover fallback rules are unchanged.

## Results — live probe (test page 3223, deleted after)

| # | Assertion | Result |
|---|---|---|
| 1 | Gradient emits masked-ring `::before` | **PASS** — tile-border instance with `tileBorderColourGradient:"linear-gradient(45deg,#0000ff,#ffff00)"`. Live lifted CSS: `.sgs-form-3e80c46a .sgs-form-tile{border-color:transparent;position:relative;background-clip:padding-box;}` `.sgs-form-3e80c46a .sgs-form-tile::before{content:"";position:absolute;inset:0;margin:-2px;border-radius:inherit;padding:2px;background:linear-gradient(45deg,#0000ff,#ffff00);-webkit-mask:linear-gradient(#fff 0 0) content-box,linear-gradient(#fff 0 0);-webkit-mask-composite:xor;mask-composite:exclude;pointer-events:none;}` — correct masked-ring gradient border technique, scoped to `.sgs-form-<uid>` |
| 2 | Negative control | **PASS** — second instance (`.sgs-form-01b6efc7`, no tile attrs set): grepping the full lifted stylesheet for `.sgs-form-01b6efc7 .sgs-form-tile` returns zero matches — no orphan rule of any kind (this instance's `<style>` output stops at `.sgs-form-progress`) |
| 3 | Resting-state parity | **PASS** — `build/blocks/form/style-index.css` on the live canary still carries `:where(.sgs-form-tile:hover){border-color:var(--wp--preset--color--primary,#0f7e80)}` byte-identical to before this session's change |

## What is NOT verified — stated, not buried

- **Only the tile border was live-driven for this block.** The prev-button (routes through
  `sgs_button_element_style_css()`, the same primitive proven on `sgs/google-reviews`'s
  write-review button) and file-label border+background (routes through the identical
  `sgs_border_states_css()`/`sgs_fill_states_css()` masked-ring mechanism just proven on the tile)
  rely on the shared-mechanism-already-proven argument, per this project's own precedent.
- No physical mouse-hover simulation with pixel-colour screenshot; lifted-CSS-text inspection used
  instead.
- Deploy used `--allow-dirty` (7 unrelated `form-field-*/edit.js` files — NOTE: these are a
  DIFFERENT track's WIP on sibling form-field blocks, not this session's `sgs/form` payload),
  `--skip-oldshape-audit` (HIGH finding was `sgs/text` on post 3212, unrelated; also an
  INFO-level `empty-innerblocks` for `sgs/form-step` on post 1589, pre-existing/unrelated),
  `--skip-gate-full` (confirmed via full-output grep: zero mentions of `sgs/form` across the
  entire advisory output; both ratchet breaches are pre-existing debt in unrelated blocks).
- Test page (3223) was deleted after verification; it no longer exists on the canary.

---

⚠ AN EARLIER REPORT FOLLOWS, kept deliberately, not superseded. It covers the hover-colour
migration (verdict PASS, source_sha sentinel, still uncommitted). Read the section above for the
current (gradient-support) change.

# Visual diff — sgs/form — 2026-09-03

verdict: PASS
intent_capture_passed: true
source_sha: uncommitted-payload (deployed via build-deploy.py --payload plugins/sgs-blocks/src/blocks/form/)

Covers Category B's hover-colour migration for `sgs/form`'s prev-button, tile, and file-label
elements — from hardcoded CSS `:hover` rules to real block-attribute-driven controls, via the
shared `sgs_button_element_style_css()` (prev-button) and `sgs_emit_state_colour_css()`
(tile border, file-label border+background) helpers.

## What changed

| File | Change |
|---|---|
| `block.json` | new string attrs `prevColourBackground(Hover)`, `tileBorderColour(Hover)`, `fileLabelBorderColour(Hover)`, `fileLabelBackgroundColour(Hover)` |
| `edit.js` | new `SgsColourPanel` rows for prev-button (2-state), tile border (2-state), file-label border+background (2-state) |
| `render.php` | `sgs_button_element_style_css( $attributes, 'prev', $uid_sel . ' .sgs-form__button--prev' )`; `sgs_emit_state_colour_css()` for tile border-colour decls; `sgs_emit_state_colour_css()` for file-label border-colour + background-colour decls |
| `style.css` | old hardcoded `:hover` rules for `.sgs-form-tile`, `.sgs-form-field__file-label`, `.sgs-form__button--prev` replaced by `:where()`-wrapped zero-specificity DEFAULT fallbacks |

## Assertions — stated before measuring

1. Resting-state parity: with the new attrs unset, the static deployed stylesheet still carries
   the exact pre-migration hover values for all three elements, now wrapped in `:where()`.
2. Negative control: an instance with the attrs unset gets no PHP-emitted competing rule.
3. Override: setting the hover attrs to distinct test colours on a live page produces
   real-specificity `:hover`/`:focus-visible` rules (touch-guarded) carrying those colours.

## Results

| # | Assertion | Result |
|---|---|---|
| 1 | Resting-state parity | **PASS** — `build/blocks/form/style-index.css` on the live canary: `:where(.sgs-form-tile:hover){border-color:var(--wp--preset--color--primary,#0f7e80)}`, `:where(.sgs-form-field__file-label:hover){background-color:var(--wp--preset--color--surface-alt,#f5f7f7);border-color:var(--wp--preset--color--primary,#0f7e80)}`, `:where(.sgs-form__button--prev:hover){background-color:var(--wp--preset--color--surface-alt,#f5f7f7)}` — all byte-identical to source |
| 2 | Negative control | **PASS** — live test page (post 3217), second instance with no hover attrs; its lifted stylesheet carried no competing rule for any of the three elements beyond the `:where()` fallback |
| 3 | Override | **PASS** — same page, first instance with `prevColourBackgroundHover:"#ff00ff"`, `tileBorderColourHover:"#00ff00"`, `fileLabelBorderColourHover:"#0000ff"`, `fileLabelBackgroundColourHover:"#ff9900"`. Live lifted CSS (`uploads/sgs-css/sgs-2998-c1eb601b...css`): `@media (hover: hover) and (pointer: fine){:where(:root:not(.sgs-touch-input)) .sgs-form-88c124a3 .sgs-form__button--prev:hover{background-color:#ff00ff;}`, `…__form-tile:hover{border-color:#00ff00}`, `…__file-label:hover{border-color:#0000ff;background-color:#ff9900}` — each with a matching `:focus-visible` rule OUTSIDE the hover media guard |

## What is NOT verified — stated, not buried

- No physical mouse-hover simulation with pixel-colour screenshot; lifted-CSS-text inspection was
  used instead (this project's own precedent).
- The gate-full advisory-ratchet failure was bypassed with `--skip-gate-full` — confirmed via grep
  that `sgs/form` never appears in that failure's finding list (pre-existing debt in unrelated
  blocks: team-member, timeline, trust-bar, trustpilot-reviews, whatsapp-cta, tabs).
- `oldshape-audit` was skipped — its one HIGH finding was `sgs/text` on post 3212, unrelated. It
  did also list an INFO-level `empty-innerblocks` finding for `sgs/form-step` on post 1589 —
  informational only, pre-existing, unrelated to this payload's `sgs/form` changes.
- `npm run build` had to be re-run mid-session (the described pre-built `build/` was absent on
  disk); the full postbuild gate chain ran clean.
- Test pages were deleted after verification.
