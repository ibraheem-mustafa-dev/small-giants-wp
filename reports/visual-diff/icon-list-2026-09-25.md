# Live verification: sgs/icon-list, 2026-09-25 (per-item description line)

verdict: PASS
intent_capture_passed: true
commit_sha: 8441095ec (feat(icon-list): an optional description line per item), eafcb316d (DB rows)

Bean's option (a) for the numbered compact-links rows. Deployed to sandybrown (build-deploy, all gates, motion probes
green). Test `plugins/sgs-blocks/tests/php/wp-eval-icon-list-description.php`, run with `wp eval-file` on sandybrown,
renders a real `sgs/icon-list` (numbered, `decimal-leading-zero`, `descriptionColour` #0a7ea8, `descriptionFontSize` 13):

| Check | Before deploy | After |
|---|---|---|
| Linked item: escaped description inside its link (one click target) | FAIL | PASS |
| Unlinked item: description inside its text span | FAIL | PASS |
| Item without a description renders none (2 spans for 3 items) | FAIL | PASS |
| `descriptionColour` emits a scoped rule | FAIL | PASS |
| `descriptionFontSize` emits a scoped rule | FAIL | PASS |

Editor round-trip of the new Description field and its colour and typography rows: batched for the later pass.
