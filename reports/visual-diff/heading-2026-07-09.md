---
block: sgs/heading
date: 2026-07-09
verdict: PASS
first_paint_capture_passed: true
---

# Visual-diff — sgs/heading full no-inline + box-group migration

**Verdict: PASS** · LANDED on the asymmetric box-object test page (live computed-style, anonymous Playwright + cache-bust after OPcache reset + LiteSpeed purge) + page-8 re-clone (no recognition regression, parity steady 67→68%).

| Assertion | Result | Evidence |
|-----------|--------|----------|
| 100% no-inline | PASS | `getAttribute('style')` on the heading subtree (wrapper DIV + `__text` H2) = zero property declarations. All styling in the scoped `#uid` `<style>`. |
| 100% box-group | PASS | borderWidth object → `1px 3px 5px 7px`; base border-radius (native `style.border.radius`) → `18px 0px 18px 0px`; padding/margin base→native objects, tiers→`paddingTablet/Mobile`+`marginTablet/Mobile` objects (@media 1023/767). 26 flat attrs removed. |
| box_family guard | PASS | `check-box-family-guard.py` 0 violations; new object attrs carry `box_family` in DB. |
| Security | PASS | border-style via CSS-keyword sanitiser; `<style>` via `wp_strip_all_tags`. |
| No regression | PASS | page-8 re-clone deployed clean, headings render, no `ConservationError`, computed-parity steady. |

**B3 wrapper-removal — DONE + LANDED.** The `<div>` wrapper is removed: the `<h{level}>` is now the block root (button D288 pattern), uid is a class (`sgs-hdg-{md5}`), anchor `id` preserved when set, no `__text` child. Verified live on page-8 across levels: H1 (52px), H2 (36px), H4 (19px) all render as bare semantic h-tags, zero inline, typography lifted correctly; page-8 re-clone clean, parity 68→69% (no regression). Minor hygiene follow-up (non-blocking): the DB `block_selectors.typography` for heading still reads `.wp-block-sgs-heading__headline` (already stale pre-change) — it does not affect the lift (proven) but could be re-pointed to the root for tidiness.

first_paint_capture_passed: true — captured live on `/sgs-box-object-test/`: heading paints with the asymmetric border + radius, no FOUC, scoped `<style>` present in first paint.
