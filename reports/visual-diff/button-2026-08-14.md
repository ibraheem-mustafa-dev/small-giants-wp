---
block: sgs/button
date: 2026-08-14
verdict: PASS
editor_capture_passed: true
editor_capture_run: true
capture_method: Playwright against the live sandybrown canary (wp.data selectBlock + direct DOM computed-style read), post-deploy
deployed_build: deploy 2026-08-14 (--payload plugins/sgs-blocks/src/blocks/button/)
change: D4 — hide ToolsPanel's own duplicate <h2> title when nested in a PanelBody (Shadow panel, InspectorControls group="styles")
source_sha: 2b581fce1e8be4e1
---

## What this report verifies

`edit.js` gained `className="sgs-nested-tools-panel"` on the ToolsPanel inside the
"Shadow" PanelBody (`InspectorControls group="styles"` — the WordPress "Styles" tab,
not "Block"); `editor.css` gained the matching hide rule. Note: this block's earlier
"Colours" PanelBody (fixed the same way earlier this session) was superseded by an
unrelated same-day commit (`bd2378f4`, T4's `SgsColourPanel` rollout) that replaced
that whole section — not this fix's concern, and not part of what's staged now. The
only D4 instance remaining in this block's staged diff is "Shadow".

## Live measurement

Inserted `sgs/button` on a throwaway page (2434, force-deleted after use), selected
the block, switched the sidebar to the **Styles** tab (found via `aria-label`, not
visible text — the tab is icon-only), expanded "Shadow", and read the real computed
styles of the inner ToolsPanel's own header:

- `hasClass`: **true**
- inner `<h2>Shadow</h2>` computed style: `position: absolute`, width < 2px, clipped
  to zero — hidden from view, present for screen readers
- "..." reset-all menu: still rendered at full width, fully clickable

## Anti-vacuity

The panel was collapsed by default (`initialOpen={false}`, unchanged) and explicitly
expanded before measurement. Confirmed the "Shadow" panel is real content (not an
empty shell) — has real live child controls (colour/blur/spread/offset).
