# Visual diff — sgs/hero — 2026-08-30

verdict: PASS
intent_capture_passed: true
source_sha: d9d52bf8237683e0

## What changed

Pays two unpaid `SGS_VISUAL_GATE_SKIP` debts logged 2026-08-29 17:42/17:43 against commit
`9a69d60b5` ("restore the post-migration render fixes — they were lost uncommitted"), which at
the time noted "the border-roundtrip probe is the evidence and runs after the pending batch
deploy" but was never followed up.

The hero-specific hunk in that commit removes a dead `$border_args` guard left orphaned by the
Shape-B border migration (native `__experimentalBorder` support removed; width/style/colour are
now block-private attrs emitted elsewhere in the same file). The guard's body was never
populated after that migration, so this is a pure dead-code deletion — no output construct
changed, no variable feeding output was touched.

## Live result — canary, computed styles

`node scripts/qa/check-border-roundtrip.js --blocks sgs/hero` (disposable probe page, real
positive instance + a `borderStyle:"none"` negative control, computed styles via Playwright,
auto-cleaned up):

```
PASS  sgs/hero
      [.wp-block-sgs-hero <section>] border painted from attributes, control clean.
      Observed: positive[4px solid rgb(230, 138, 149)] · control[0px none rgb(58, 46, 38)]
      · expected colour rgb(230, 138, 149)
```

The positive instance paints its configured border correctly; the `borderStyle:"none"` negative
control paints no border — proving the probe can also report "no border" and isn't just
confirming a tautology.

## Risk

None — the removed code was dead (an always-empty `$border_args` populated by nothing), and the
live probe confirms hero's border rendering (the property class this guard belonged to) is
correct on the current canary build.

## Gates

`php -l` clean · `check-render-undefined-vars`: 33 findings → OK (this commit's own stated
result) · `check-border-roundtrip.js --blocks sgs/hero`: PASS, positive + negative control both
correct.
