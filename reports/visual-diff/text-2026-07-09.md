---
block: sgs/text
date: 2026-07-09
verdict: PASS
first_paint_capture_passed: true
---

# Visual-diff — sgs/text full no-inline + box-group migration

**Verdict: PASS** · LANDED on the asymmetric box-object test page (live computed-style, anonymous Playwright + cache-bust after OPcache reset + LiteSpeed purge) + page-8 re-clone (no recognition regression, parity steady 67→68%).

| Assertion | Result | Evidence |
|-----------|--------|----------|
| 100% no-inline | PASS | `getAttribute('style')` on the text subtree = zero property declarations; wrapper carries no `style` key. All styling in the scoped `#uid` `<style>`. |
| 100% box-group | PASS | borderWidth object → `2px 4px 6px 8px`; base radius (native) → `12px 4px 12px 4px`; base padding (native) → `9px 18px 27px 36px`; tiers → `paddingTablet/Mobile`+`marginTablet/Mobile` objects (@media 1023/767). ~37 flat attrs removed. |
| No useless wrapper (B3) | PASS | text already renders as the `<p>` directly (`.wp-block-sgs-text` on the `<p>`, no wrapper div) — B3 satisfied by construction. |
| box_family guard | PASS | `check-box-family-guard.py` 0 violations. |
| Security | PASS | border-style via CSS-keyword sanitiser (`dashed` rendered); `<style>` via `wp_strip_all_tags`. |
| No regression | PASS | page-8 re-clone clean, text renders, no `ConservationError`, parity steady. |

first_paint_capture_passed: true — captured live on `/sgs-box-object-test/`: text paints with the asymmetric dashed border + radius + padding, no FOUC, scoped `<style>` present in first paint.
