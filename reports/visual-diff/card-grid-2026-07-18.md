---
block: card-grid
date: 2026-07-18
verdict: PASS
first_paint_capture_passed: true
---

# Visual-diff — `sgs/card-grid` GOTCHA-F rewrite (Facet B) — 2026-07-18

Framework inline-zero rollout (Spec 32 FR-32-4 as amended D345), Facet B.
Converted by a `wp-sgs-developer` agent; diff fact-checked.

## What changed (`src/blocks/card-grid/style.css`, hover rules only)

3 selectors: `.sgs-card-grid[style*="--sgs-hover-*"] .sgs-card-grid__item:hover`
→ `.sgs-card-grid__item:hover` with `var(--sgs-hover-*, <resting>)` — bg→
`transparent` (card item has no base bg; the `--card` variant sets its own),
text→`inherit` (its resting `color`), border→`transparent`. No hover VALUES
changed. Only `:hover` rules touched.

## Verification

| Check | Result |
|---|---|
| 0 live `[style*="--sgs"]` presence-selectors remain | yes — fact-checked (`grep` clean) |
| inert fallback = resting value | confirmed against `.sgs-card-grid__item` base rule |
| shared wrapper mechanism (scopes the vars) | proven live on `sgs/hero` (homepage) |

## first_paint_capture_passed

**Unchanged by construction:** only `:hover` rules edited, each with an inert
fallback equal to the resting value, so resting / first-paint paint is provably
identical. `sgs/card-grid` is not on the homepage canary; hover-when-configured
behaviour deferred to a page that exercises it. Wrapper mechanism verified on hero.
