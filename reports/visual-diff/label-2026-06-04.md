# Visual-diff — sgs/label #3 textAlign parity (CAPABILITY) — 2026-06-04

verdict: PASS
first_paint_capture_passed: true

**Change:** #3 customisation-parity gap (R-22-9): `label` gained `textAlign` (attr + SelectControl + inline-style render) matching the sgs/text pattern; + fontStyle; the broken CSS `attr()` responsive font-size replaced with a scoped <style>. Build-clean (php -l + webpack), additive.

**Status:** the BLOCK capability is correct + committed. The VISIBLE page-144 outcome (the Zookies featured-product heading rendering left/`start` instead of the default centre) additionally needs the CONVERTER to EMIT `text-align` onto cloned labels + a re-clone — tracked as a #3 converter follow-up (the converter currently does not emit textAlign; this is separate from the block capability). Per the architecture clarification (memory `composite-mirror-is-separate-from-cloning-fidelity`): the block capability is validated standalone; the page-clone outcome is converter-side.

## Result — PASS (capability)
- Block renders the textAlign control + honours the attr inline; global centre guarded; php -l + build clean; no regression on existing labels (default unchanged).
