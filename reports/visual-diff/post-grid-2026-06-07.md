---
block: post-grid
date: 2026-06-07
verdict: PASS
first_paint_capture_passed: true
---

# Visual-diff — sgs/post-grid gap-control consolidation — 2026-06-07

**Change:** Gap control (RangeControl) consolidated onto the shared control; render.php px back-compat added (bare-digit '30' -> 30px before sgs_container_gap_value(), preserving the old absint().'px' semantics); deprecated.js v1 gained isEligible + migrate. Editor: block valid, 0 console errors.

**Back-compat verification:** Frontend-verified on a live page (R-22-11): old-shape stored gap '30' renders gap:30px on .sgs-post-grid__inner (NOT the collapsed var(--preset--30) the pre-fix code would have produced).

**QC:** /adversarial-council (6 personas) gated this change set pre-commit; the convergent back-compat must-fixes (per-block render px-append, dynamic-block deprecation isEligible, trust-bar editor preview, heading render coercion, wrapper esc_html) were applied and re-verified. Existing client content renders identically.

first_paint_capture_passed: true (live frontend / editor render confirmed; no visual regression vs pre-change output for existing stored values).
