---
block: trust-bar
date: 2026-06-05
verdict: PASS
first_paint_capture_passed: true
---

# Visual-diff — sgs/trust-bar de-hardcode to universal grid — 2026-06-05

**Change:** removed the hardcoded `grid-template-columns: repeat(4,1fr)` static CSS + the
`data-columns` count-override selectors from `trust-bar/style.css`; the badge grid is now
driven by the universal `sgs/container` wrapper mechanism (`layout="grid"` +
`gridTemplateColumns`/`*Tablet`/`*Mobile` attrs), with `block.json` defaults
(`layout:"grid"`, `gridTemplateColumns:"repeat(4,1fr)"`, mobile `1fr 1fr`) preserving the
prior 4-col-desktop / 2-col-mobile behaviour. This lets the cloning converter lift any
draft's grid onto a trust-bar exactly as it does for any container (no per-block hardcode).

**Why:** C2 / `P-TRUSTBAR-BOUND-GRID` — the trust-bar grid was static CSS, not attr-driven,
so it could not follow the universal extraction/conversion path (Bean directive 2026-06-05).

## Live verification (canary page 144, 1440px, after build+deploy)

| Check | Before (cloned page, static CSS) | After (this change) | Verdict |
|---|---|---|---|
| Badge row orientation | badges STACKED VERTICALLY in a single ~272px column (collapse) | badges render as a **horizontal row** — badge 1 and badge 2 share the same `getBoundingClientRect().top` (1106px) | PASS |
| Grid source | hardcoded `repeat(4,1fr)` in style.css | applied via the shared wrapper helper inline-style from the `layout="grid"` + `gridTemplateColumns` attrs | PASS |
| Default render (no lifted attrs) | n/a | block.json defaults yield 4-col desktop / 2-col mobile — existing trust-bars unchanged | PASS |
| Console errors | — | 0 | PASS |

**Result: PASS.** The vertical-stack collapse is resolved; the grid is now universal and
converter-liftable. Remaining (separate work): the converter's `display:grid → layout:"grid"`
bridge (Batch 2) so a DRAFT trust-bar's exact column layout lifts onto these attrs.
