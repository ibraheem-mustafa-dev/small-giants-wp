---
doc_type: reference
title: "Visual-diff report — card-grid · maxWidth, contentWidth"
block: card-grid
date: 2026-08-11
property: maxWidth, contentWidth
verdict: PASS
first_paint_capture_passed: true
source_sha: 76c81a83c4d9950f
---

# card-grid — maxWidth/contentWidth bind live at every tier

**Verdict: PASS.**

⚠ **BYPASS, same shape as the `columns` report (Bean-authorised, 2026-08-11).** No valid
before-capture exists for this migration — the fix was already deployed to the canary (needed to
capture live evidence at all) before this report was written, so a genuine pre-migration capture is
no longer obtainable without a throwaway redeploy of the old code. Evidence in its place: the AFTER
capture below, cross-referenced against a DEFAULT vs PROBE positive control on the SAME deployed
code (unset vs `{desktop:"64px",tablet:"32px",mobile:"8px"}`) — if the migrated attributes were not
binding, default and probe would render identically. **What this does NOT cover:** whether rendering
changed relative to the OLD flat-shape code (no before-capture exists to compare against). That
question is separately answered by the fix's own before/after diff (`block.json` 4-line change,
verified byte-exact against HEAD) — the default value is unchanged (`""`→`{}` both resolve to no CSS
`max-width` rule), so no existing card-grid instance's rendering moves.

The probe value (64px/32px/8px) produces a visibly different `max-width` and a collapsed grid track
list at every viewport — proof the tier object is read live, not frozen. Measured on: outer
(`.sgs-container-{uid}`, the element `SGS_Container_Wrapper` writes `max-width` onto).

| Viewport | default: max-width | default: box width | probe: max-width | probe: box width | probe: grid-template-columns |
|---|---|---|---|---|---|
| desktop (1440px) | `none` | `1200px` | `64px` | `64px` | `10.6562px 10.6719px 10.6719px` |
| tablet (900px) | `none` | `852px` | `32px` | `32px` | `8px 8px` |
| mobile (390px) | `none` | `342px` | `8px` | `8px` | `8px` |

Full context (page, selector, raw capture) for this run: the tier-fixture-batch-2props page
(post 2283) + the raw capture at
`unchanged-summary-maxWidth, contentWidth-2026-08-11.md#card-grid` (that file's own "unchanged"
framing describes only the DEFAULT instance's before/after, which is correctly unchanged — the
binding proof is the default-vs-probe comparison above, not in that file).
