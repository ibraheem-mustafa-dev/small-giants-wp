# Visual diff — sgs/quote — 2026-09-04

verdict: PASS (static gates); live-capture DEFERRED to this session's centralised deploy step
intent_capture_passed: true
source_sha: ee85626f3188b42c

## What changed

Phase 3 golden-colour rollout, rule `31-golden-colour-control` "missing-gradient". One
attribute gains a gradient sibling: `textColourHover` → `textColourHoverGradient` (element
`box`, hover state, root selector `.{uid}.wp-block-sgs-quote:hover`/`:focus-within`).

`block.json`: `textColourHoverGradient` string attr added; `attrMap.states.hover` on the `box`
element gained `"css:color-gradient": "textColourHoverGradient"` — deliberately `css:color-
gradient`, NOT `css:background-image`, because that hover `attrMap` already owns
`"css:background-image": "backgroundColourHoverGradient"` for the (unrelated) background fill
gradient; a second `css:background-image` key would have been a silent JSON duplicate-key
collision (the second entry wins, the first vanishes). Caught by cross-referencing the
convention other two-gradient elements already use (`brand-strip`, `container`, `heading`,
`team-member`, `testimonial`, `text` all use `css:color-gradient` for the text-clip sibling
whenever the same element also carries a real `css:background-image` fill-gradient key).

`render.php`: `textColourHover`/`textColourHoverGradient` now resolve via
`sgs_resolve_text_colour_or_gradient()` → `sgs_text_colour_decl()`, folded into the existing
`$hover_rules` array (which already carries box-shadow/scale/border-colour hover declarations
on the same selector), plus the mandatory `sgs_text_colour_gradient_fallback_rule()` companion
— mirrored directly from `sgs/heading`'s existing hover-gradient pattern (`:hover` +
`:focus-within`, touch-guarded via `sgs_hover_media_wrap()`/`SGS_HOVER_NOT_TOUCH`).

`edit.js`: the `textColourHover` row (mounted via `SgsColourPanel`) gained `gradientCapable:
true` plus `gradientValue`/`onGradientChange` on its single `hover` state. No canvas-preview
change — this block's editor never previews hover states live (confirmed: no `textColourHover`
usage anywhere in the canvas-preview style objects), so there is nothing to update there.

## Why this row is genuinely safe (not just detector-blind)

Unlike product-card's tag (see that report), this one is safe for a real, verified reason:
`sgs/quote`'s background paint (both resting and hover) was already moved onto a `::after`
layer via `sgs_block_background_layer_css()` at D936 — the file's own comment says so
explicitly ("The root's own `color`/`textColourHover` needs to stay free of a same-selector
background so a future gradient sibling can use `background-clip:text` without this fill
swallowing it (D936)"). This attribute is that D936 follow-through, not a detector blind spot.

## Verification so far

- `node scripts/colour-codemod/survey.js --json`: `textColourHover` moved from
  `REFUSED:no-gradient-capable-paint-path-found` to `AUTOFIXABLE:helper-at-existing-selector`.
  Total row count held at 262.
- `php -l src/blocks/quote/render.php` — clean.
- `git diff --stat` scoped to exactly `block.json` / `edit.js` / `render.php` for this block.

## Why intent capture, not before/after

`textColourHoverGradient` is brand new (`default: ""`) — no existing content can have set it.

## Risk

Additive only. Default (empty-gradient) path is byte-identical to before.

## Gates

`php -l` clean · `survey.js` census verdict moved as expected, total unchanged at 262 ·
build/deploy/live-probe deferred to the session's centralised Step 3.
