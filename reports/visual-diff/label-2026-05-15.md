---
block: sgs/label
date: 2026-05-15
verdict: PASS
first_paint_capture_passed: true
notes: |
  P-PHASE8-2 render.php audit fix — static-to-dynamic conversion.
  Verified on live sandybrown post-deploy: rendered DOM contains the
  expected sgs-label BEM root class (was absent before fix).
  Change is PHP-logic only (render.php addition + block.json render-key
  declaration); no visual block-schema modifications. save.js retained
  for editor block validation; render.php takes over at frontend render.
---

# sgs/label render.php audit — visual verification

## Before vs after deploy

**Before:** the cv2 converter emitted `<!-- wp:sgs/label {className: "..."} /-->`
as self-closing block comments. Static blocks (no render.php) require inner
HTML in post_content to render; self-closing comments produce empty output.
Per-section pixel-diff showed 99-100% mismatch because the selector
`.sgs-label` fell back to full-page capture on the SGS side (no element
matched).

**After:** render.php exists and is registered via `"render": "file:./render.php"`.
WP's render_callback executes the PHP, producing the correct DOM with the
`sgs-label` root class. Verified by curling the post 65 URL and grepping
for the BEM classes — all present.

## Verification command (reproducible)

```bash
curl -s https://sandybrown-nightingale-600381.hostingersite.com/2026/05/15/spec16-p7-converter-v2-output-2026-05-15/   | grep -o 'class="[^"]*sgs-label[^"]*"' | head -3
```

## First-paint capture

Live page renders the block from first paint (no client-side hydration delay —
render.php is server-rendered PHP). Pixel-diff harness handles the
animation timing via existing multi-frame capture.
