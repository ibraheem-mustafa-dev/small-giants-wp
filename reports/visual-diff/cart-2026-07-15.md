# cart — live verification (2026-07-15)

verdict: PASS
first_paint_capture_passed: true

**Change:** `render.php` returns `''` when WooCommerce is absent (`class_exists` guard, D338)
— previously the trolley rendered on every non-ecommerce client and was merely CSS-hidden.
Docblock updated to match (the stale "renders but stays hidden" text was a /qc finding).

**Live (sandybrown/Mama's, WooCommerce ACTIVE — `wp plugin list` verified):** the guard must
NOT fire and does not — `.sgs-cart__trigger` present, 1 cart block rendered, badge intact.

**No-WC path:** not live-verifiable today — the only no-WC site (palestine-lives/Indus) is
deliberately rolled back to `main` (Bean's instruction, D338) so this branch's guard is not
deployed there. The path is a 3-line early return (`! class_exists( 'WooCommerce' ) → ''`);
its live proof lands automatically when Track A next deploys to Indus — noted for that
session's checklist. Editor notice for admins unchanged (edit.js).

**First-paint:** block has no entry animation (`animation-fill-mode:both` = 0, non-zero
delay = 0 in cart CSS); SSR badge renders count 0 then hydrates — painted from frame one.
