---
block: label
date: 2026-06-12
verdict: PASS
first_paint_capture_passed: true
surface: sandybrown page 8 (https://sandybrown-nightingale-600381.hostingersite.com/)
---

# Visual diff — sgs/label border-radius render gap (P4, 2026-06-12)

## What changed
`render.php`: the lifted `borderRadius` attr was only applied via a CSS custom
property (`--sgs-label-border-radius`) that `style.css` reads ONLY inside the
`.is-style-pill-*` variant rules — a plain label never applied it, and
`.is-style-plain` forced `border-radius:0`. Now render.php ALSO emits
`border-radius:Npx` as a direct inline property (highest specificity), so the
lifted value paints on every variant.

## Live first-paint verification (page 8)
| Element | Property | Draft | Live computed | Verdict |
|---|---|---|---|---|
| gift label "GIFT IDEA" | border-radius | 6px | **6px** | PASS |
| gift label "MOST THOUGHTFUL" | border-radius | 6px | 6px | PASS |

first_paint_capture_passed: true — the lifted border-radius now paints on the
gift section labels (was 0px before). No regression to other label variants
(inline value composes with existing pill/plain styles via specificity).
