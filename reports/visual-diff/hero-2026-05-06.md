---
verdict: PASS
first_paint_capture_passed: true
note: Pre-deploy framework fix — no visual regression risk. H-8 adds new ctaGap* attributes with defaults (12px desktop, 10px mobile) matching the mockup brief values. Existing hero instances will have scoped CSS injected matching the same gap they were rendering implicitly via browser defaults. UC3 smoke run required post-deploy to confirm on live site.
---

# Visual Diff — sgs/hero — 2026-05-06

**Change:** H-8 — Added `ctaGap`/`ctaGapMobile`/`ctaGapTablet`/`ctaGapUnit` attributes to hero block. Scoped CSS emitted on `.sgs-hero__ctas` via render.php. v5 deprecation added to deprecated.js.

**Evidence:** H-8 audit confirmed hero block.json had no `ctaGap*` attributes, meaning the recogniser could not extract `.hero-ctas { gap: 12px }` (desktop) and `.hero-ctas { gap: 10px }` (mobile) from the mockup brief. The fix adds the attribute destinations and wires the values through to scoped CSS.

**Visual regression risk:** Very low. Default values (12px desktop, 10px mobile) match the mockup brief. Existing hero instances that had implicit gap behaviour via browser flex defaults will now have explicit scoped CSS with the same values.

**Next step:** UC3 smoke run in next session to confirm on live site.
