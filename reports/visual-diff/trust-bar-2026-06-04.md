# Visual-diff — sgs/trust-bar WS-4 composite-mirror (PoC) — 2026-06-04

verdict: PASS
first_paint_capture_passed: true

**Change:** WS-4 — trust-bar's outer `<div>` wrapper now rendered by the shared `SGS_Container_Wrapper::render()` (both `sourceMode` branches); 68 sgs/container section-kind attrs mirrored into block.json (v0.4.0); `ContainerWrapperControls kind="section"` wired into edit.js. Helper gained an `extra_attrs` opt (data-*/aria passthrough — byte-identical when empty; container still passes none, so its render is unchanged).

**Validation:** clean test page 591 (`/ws4-trust-bar-poc/`) — a REAL `sgs/trust-bar` typed block (4 badges), NOT a page-144 container clone. Playwright @1200 + curl markup.

## Result — PASS (live-DOM, R-22-11)
- 0 PHP errors/warnings/notices on the page.
- Wrapper markup: `<div class="sgs-container sgs-trust-bar sgs-trust-bar--icon-circle sgs-trust-bar--medium wp-block-sgs-trust-bar" ... data-columns="4">` — block's own classes preserved + the `sgs-container` mirror class added; CSS vars (`--sgs-trust-bar-*`) preserved via `extra_styles`; `data-columns="4"` + `aria-label` preserved via the new `extra_attrs` (proves the data-* passthrough that view.js + the grid need).
- Layout: `display:grid`, 4 badges at x=0/302/604/905, all y=362 — a clean 4-column row at ≥600px. Grid + columns honoured.
- Note: "icon-circle + title" places the title in grid-cell-1 (3 badges + wrap) — this is PRE-EXISTING (the old render put title+badges as the same direct grid children; `.sgs-trust-bar__title` has no `grid-column:1/-1`). NOT a WS-4 regression. Normal icon-circle (no title) = clean 4-across.

This is the WS-4 composite-mirror PoC on a section-kind composite: the shared helper faithfully wraps the block, preserving its classes, CSS vars, data-attrs and grid, and adds the mirrored container capability + editor controls. Container render remains byte-identical (extra_attrs empty on the container path).
