---
block: info-box
date: 2026-06-07
verdict: PASS
first_paint_capture_passed: true
---

# Visual-diff — sgs/info-box #130 crash fix — 2026-06-07

**Change:** Template fix: INFO_BOX_TEMPLATE now passes the child heading level as 'h3' (string) instead of 3 (number), which was crashing the editor (React #130) for info-box and any parent templating it. No render/output change for the info-box wrapper itself.

**Verification:** Pre-existing React #130 crash (numeric heading level) — confirmed live in the editor before the fix, and confirmed RESOLVED after: info-box, hero, feature-grid (templates info-box) and a fresh sgs/heading all insert with 0 console errors and isValid=true at all viewports. Valid string-level headings render exactly as before (no regression).

first_paint_capture_passed: true (editor renders the affected blocks without crashing; frontend heading tag now matches the editor for numeric-level legacy content).
