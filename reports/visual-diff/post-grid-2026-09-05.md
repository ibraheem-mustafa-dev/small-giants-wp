# Visual diff — sgs/post-grid — 2026-09-05

verdict: PASS
intent_capture_passed: true
source_sha: d59a646e67f4de9a

## What changed

New `categoryBadgeColourGradient` sibling to the existing `categoryBadgeColour`. It joins the
existing per-attribute loop (`$post_grid_text_rows`) that emits scoped colour/gradient rules to
the dual selector `.sgs-post-grid__badge, .sgs-post-grid__category` — badge pill (card/overlay
`cardStyle`) and plain inline label (flat/minimal `cardStyle`) share the one rule.

## 1. Assertions (stated before measuring)

- **A (gradient UNSET):** badge/category renders the existing flat `categoryBadgeColour`.
- **B (gradient SET):** badge/category computed style shows the gradient text trio
  (`background-image` + `background-clip:text` + `color:transparent`), plus the `@supports`
  fallback.

Tested with `cardStyle: "card"` (renders `.sgs-post-grid__badge`, per the task's own routing note).

## 2. Live result

Live canary (sandybrown), scratch page 3296 (`className: test-pg-unset` / `test-pg-set`,
`cardStyle: "card"`, `postsPerPage: 2`; real published posts on the canary render the badge with
text "Uncategorized").

**UNSET** (`uid sgs-post-grid-aa455b80`) — lifted stylesheet:
```css
.sgs-post-grid-aa455b80.wp-block-sgs-post-grid .sgs-post-grid__badge,.sgs-post-grid-aa455b80.wp-block-sgs-post-grid .sgs-post-grid__category{color:var(--wp--preset--color--text-inverse);}
```
Flat colour only — confirms A.

**SET** (`uid sgs-post-grid-376cd65b`, `categoryBadgeColourGradient:
"linear-gradient(90deg,#ff0000 0%,#0000ff 100%)"`) — lifted stylesheet:
```css
.sgs-post-grid-376cd65b.wp-block-sgs-post-grid .sgs-post-grid__badge,.sgs-post-grid-376cd65b.wp-block-sgs-post-grid .sgs-post-grid__category{background-image:linear-gradient(90deg,#ff0000 0%,#0000ff 100%);-webkit-background-clip:text;background-clip:text;color:transparent;}
.sgs-post-grid-376cd65b.wp-block-sgs-post-grid .sgs-post-grid__badge,.sgs-post-grid-376cd65b.wp-block-sgs-post-grid .sgs-post-grid__category{background-image:none;color:#ff0000;}
```
Gradient trio + fallback present, dual selector intact — confirms B. The badge element itself
(`<span class="sgs-post-grid__badge">Uncategorized</span>`) is confirmed present in the live DOM
on both instances, so the rule targets a real rendered element, not a dead selector.

## 3. Why before/after doesn't apply

`categoryBadgeColourGradient` is a brand-new attribute; no prior render path produced a gradient
badge to diff against. The UNSET instance (same flat `categoryBadgeColour` default) is the live
control, captured in the same pass.

## Verification method note

Playwright's browser was locked by a concurrent session throughout this verification wave, so
this used direct HTTP fetch of the rendered page plus the lifted CSS file
(`/wp-content/uploads/sgs-css/sgs-3116-…css`) rather than `getComputedStyle()`. Adequate here: the
claim is about a literal scoped declaration's presence, matched against a confirmed-present DOM
element by class, not an inherited/cascaded value.
