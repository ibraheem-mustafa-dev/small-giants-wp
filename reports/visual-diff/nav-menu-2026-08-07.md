---
block: sgs/nav-menu
date: 2026-08-07
verdict: PASS
first_paint_capture_passed: true
first_paint_capture_run: true
capture_method: chrome-devtools-mcp (Playwright reserved for a co-active session)
deployed_build: sandybrown canary, deploy 2026-08-07, verified by md5 of deployed files vs local build
change: Spec 32 stranded ':not([style*=...])' fallback-guard purge
---

## What changed
`data-wp-init` callback added to the toggle wrapper that removes `aria-controls` when the referenced drawer cannot be resolved.

## Live measurement (post-deploy, on the real canary)
On /poc-drawer-multi-instance/ post-deploy: 3 elements carry `aria-controls`, 0 dangle — every reference resolves (`sgs-nav-menu-...-sub-...`, `sgs-nav-drawer-1`, `sgs-nav-drawer-2`), and two real dialogs exist with those ids.

## Negative control
IMPORTANT: the motivating defect (3 dangling refs, measured earlier today) did NOT reproduce on this page before the deploy either. The guard is correct and harmless but its defect is not currently observable, so this report does NOT claim it fixed an active fault.

## Limits
Measured on the canary only. This report evidences the FIRST-PAINT rest state;
hover and focus states were not captured.
