---
doc_type: visual-diff-report
block: sgs/form
date: 2026-07-26
verdict: PASS
first_paint_capture_passed: true
change_class: inert-specificity-move (F3 false-flag resolution)
deploy_target: sandybrown
---

# sgs/form — visual-diff report (2026-07-26, F3 :where() fix)

## Change
Moved `gap: 0.5rem` from `.sgs-form-tile` (specificity 0,1,0) into the existing
`:where(.sgs-form-tile)` block (specificity 0,0,0), mirroring the `flex-direction`
rule already there. `.sgs-form-tile` is a descendant COMPONENT; the form's `gap`
attr is applied by `SGS_Container_Wrapper` to the form ROOT (a different element),
so the tile's internal gap is a legitimate overridable component default. This
resolves the F3 false-flag (the gate exempts `:where`-scoped declarations) without
baselining it.

## Why inert (no visual change)
- The value `0.5rem` is byte-identical; only the selector specificity dropped.
- Grep confirms **no other rule** sets `gap` on `.sgs-form-tile`, so `:where` is
  the sole source → computed gap stays `0.5rem`. A specificity drop with no
  competing rule cannot change the painted output.

## Evidence (LANDED on sandybrown)
- **Built CSS:** `:where(.sgs-form-tile){flex-direction:column;gap:.5rem}`.
- **Checksum:** `build/blocks/form/style-index.css` md5 local == server (`7d1956c9…`).
- **No competing rule:** grep `form-tile` + `gap` (excluding `:where`) = empty.

## Related (same commit — F3 gate improvement)
- `check-hardcoded-render-defaults.js` E13: `gridTemplateColumns`/`gap` are
  wrapper-ROOT attrs; a literal on a BEM `__sub-element` in a wrapper-delegating
  block is exempt. Resolves the content-collection + pricing-table `.__grid`
  false-flags. Robust-by-construction (no root-class guess; per-member `__`
  check so a genuine root hardcode is never exempted). Code-reviewer pass fixed
  2 issues (unsound `path.basename` root-class derivation; comma-group handling).
- Empirical regression: empty-baseline raw findings 3 → 0; F3 baseline now holds
  only `sgs/mega-menu` (Track 2).

## Verdict
**PASS** — inert specificity move, LANDED + checksum-verified; all 3 F3 false-flags
resolved (2 via gate E13, form via `:where`), zero baseline debt added.
