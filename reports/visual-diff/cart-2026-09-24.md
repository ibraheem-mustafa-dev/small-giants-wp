# sgs/cart: visual diff, 2026-09-24

**Verdict: PASS.** Commit 033b783ad, live on sandybrown `/qa-scrim/` (header 3777, basket in `displayMode drawer`), one headed Chrome window (chrome-devtools).

| # | Check | Before | After | Result |
|---|---|---|---|---|
| 1 | Trigger (a `<button>` in drawer and flyout modes) carries no browser button paint | fill rgb(240,240,240), border 1.8px outset black | fill transparent, border 0, 44 x 44 box unchanged | PASS |

Cause: the U-2 scrim test set the header's basket to drawer mode at 01:26 on 2026-09-24 (revision 3841), which renders the trigger as a `<button>`; `style.css::.sgs-cart__trigger` had never reset button defaults (page mode renders an `<a>`). Nothing was hardcoded. Bean's eye owed.
