---
block: sgs/label
date: 2026-07-12
verdict: PASS
first_paint_capture_passed: true
change: "Ungate padding to value-presence + display model + shared box helper (Fix 4)"
verified_on: "sandybrown page 8 (Mama's homepage), live computed-style 375/768/1440"
---

# sgs/label — no-inline box ungate + display model (Fix 4)

## What changed
- `padding` (base + tablet + mobile) UNGATED from the `is-style-pill-*` gate → paints on VALUE-PRESENCE, emitted scoped via the new shared `sgs_label_box_css_rule()` helper (`includes/helpers-box.php`).
- Custom `backgroundColour` channel likewise ungated (native `style.color.background` was already ungated).
- New DISPLAY model (scoped, no inline): `fullWidth`→`display:block;width:100%`; box present (native bg / custom bg / padding / meaningful radius)→`inline-block` (hug capsule); bare→`block` (so `margin-bottom` paints — matches the draft's `display:block` eyebrows). Suppressed entirely when an `is-style-*` class is present (variant CSS owns display).
- block.json defaults: `padding {}`, `borderRadius 0`, `backgroundColour ""`; new `fullWidth` boolean.
- Static `style.css` pill chrome (`.is-style-pill-fill/-wrap`) left UNTOUCHED (borrowed by product-card discount/best-value badges).

## Live computed-style verification (page 8, 3 breakpoints)
| Label | 375 | 768 | 1440 | Verdict |
|---|---|---|---|---|
| 4 section eyebrows (hero/featured/ingredients/gift, `.sgs-section-heading__label`) | bare, padding 0, no bg, display block | bare (4/6 labels bare) | bare, padding 0, no bg | PASS — regression-safe; now block so 6px margin-bottom paints (draft-faithful) |
| Gift capsules ×2 (`Gift idea` / `Most thoughtful`) | inline-block, padding 4px 10px, bg surface-pink rgb(245,194,200), radius 6, hug (w78) | same, hug w78 | same, hug w78/140 | PASS — padded hug capsule (was padding:0 tight chip) |

## No-inline compliance (Spec 32 §6.1)
- Zero inline `style="…"` property declarations on the rendered `<span>`. All box/display/padding emitted into the block's own scoped `<style>` at class-level `.{uid}.wp-block-sgs-label` (0,2,0). PASS.

## Tests
- Converter unit suite 449 pass / 1 skip (green, unchanged).
- Conformance goldens: 15 pre-existing red (stale, not this change), 0 new — label emit unchanged (this is a render-side change).
