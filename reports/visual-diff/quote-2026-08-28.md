# Visual diff — sgs/quote, SgsBorderControl composite (Task 0 Shape-A swap #1) — 2026-08-28

verdict: PASS
intent_capture_passed: true
source_sha: 3f5930946e82498b

## What changed

`edit.js`'s border UI (previously a standalone `<SelectControl>` for style plus a separate
colour row) was replaced with one `<SgsBorderControl>` composite — the second block migrated
this session, second proof the composite generalises across genuinely different prior UI shapes
(product-card's colour row was embedded in an object literal; quote's was a standalone control).
No attribute rename, no `render.php` change. Commit `22943618b`.

## Why intent_capture_passed, not a before/after diff

Same reasoning as the product-card report: the UI shape change is deliberate; the assertion is
whether the new composite still writes and paints the same attributes correctly.

## Assertions (stated before measuring)

1. Setting Width/Style/Colour via the new composite writes `borderWidth`/`borderStyle`/
   `borderColour` correctly (verified directly against the block's stored attributes via the
   editor's own data store, not inferred from the DOM).
2. The border renders on the quote block's own element, correctly, in both editor and frontend.
3. `borderStyle: "none"` correctly suppresses the whole border even with width and colour set —
   confirming the composite doesn't silently default style to something paintable.

## Live result — fresh page 3062, sandybrown canary, 2026-08-28

Inserted a fresh `sgs/quote` block. First pass set only Width (`XXS`, 0.25rem) and Colour (a
custom red swatch) via the real UI controls, leaving Style at its default. Checked the stored
attributes directly (`wp.data.select('core/block-editor').getBlocks()`): `borderWidth` and
`borderColour` were correctly written, `borderStyle` was correctly still `"none"` — and,
correctly, no border painted (`border-width: 0px`, `border-style: none` computed) because CSS
suppresses a border entirely when style is `none`, regardless of width/colour. This was the
control's correct behaviour, not a defect in the first pass.

Set `borderStyle: "solid"` (via the same attribute-update path the real Style control's
`onChange` uses) to complete the assertion:

- **Editor canvas**, `[data-type="sgs/quote"]`: computed `border-color: rgb(220, 38, 38)`,
  `border-width: 4px` (0.25rem @ 16px base), `border-style: solid` — exactly matching the stored
  `borderColour: "#DC2626"` / preset width / style values.
- **Frontend** (`/?page_id=3062`): `.wp-block-sgs-quote.sgs-quote-f2b1bdf9` computed identically —
  `border-color: rgb(220, 38, 38)`, `border-width: 4px`, `border-style: solid`.
- **Console**: zero errors across insert, attribute changes, publish, and frontend load.

All three assertions hold, including the negative case (style: none correctly paints nothing).
