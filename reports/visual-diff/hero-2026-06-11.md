# Visual-diff — sgs/hero Step 6 per-area alignment (container 4-layer programme) — 2026-06-11

verdict: PASS
first_paint_capture_passed: true

**Change:** hero aligned to the official per-area grid layer (Spec 22 §FR-22-21.3): `supports.sgs.gridAreas:["content","media"]`; `contentBackground` + `mediaBackground` attrs added + rendered on the content/media columns; `splitColumnRatio*` retired → `gridTemplateColumns*` (deprecated.js v7 `migrate()`, control removed, render.php reads gridTemplateColumns only with an explicit '1fr 1fr' default); duplicate `mediaBackgroundColour` control + bespoke contentPadding controls removed (one control per setting — shared `GridAreaPanel` renders them now). Version 0.3.0.

**Defects caught + fixed during the gate:** v7 deprecation spread `V6_ATTRIBUTES` before its declaration (TDZ ReferenceError at module evaluation) — the hero block silently unregistered in the editor (`core/missing`); moved v7 below v6 (export order unchanged). render.php `?? '1fr 1fr'` default never fired because the mirror attr defaults to `''` — explicit empty-string default added. migrate() no longer bakes '1fr 1fr' into standard-variant heroes.

**Validation (live on sandybrown, 2026-06-11):**
- Editor (page 8): `sgs/hero` registered (7 deprecations), 0 `core/missing`, 0 invalid blocks; inspector shows Content area + Media area panels in the 4-layer order; no splitColumnRatio control anywhere; per-area write round-trip = `contentPaddingTop: 33` (number) + `contentPaddingUnit: 'px'`, reverted without save.
- Frontend first paint (homepage): hero `display:grid`, 2 columns, `.sgs-hero__content` computed `padding-top: 72px` (the draft's desktop tier), body text intact. The uneven column ratio (326/1034) is the cloning thread's pre-existing wrong-stored-value ledger item, now flowing through the correct design attr.
- Build green ×3 prebuild guards; `php -l` clean on render.php.

## Result — PASS (R-22-11)
