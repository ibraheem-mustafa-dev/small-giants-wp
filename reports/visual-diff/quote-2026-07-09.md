---
block: sgs/quote
date: 2026-07-09
verdict: PASS
first_paint_capture_passed: true
---

# Visual-diff — sgs/quote CSS-keyword sanitiser (security-only, no visual change)

**Verdict: PASS** · Non-visual PHP-logic security hardening: free-text CSS values that reach the block's scoped `<style>` are now routed through a keyword/length sanitiser (`preg_replace('/[^A-Za-z0-9.%]/','',$v)` for lengths; blocklist for box-shadow) before concatenation — closing the same CSS-injection vector fixed on button. The sanitiser only strips breakout characters from MALICIOUS input; valid values render byte-identically.

first_paint_capture_passed: true — page-8 re-clone after the change: content 100%, computed-parity steady (68→69%, unchanged from pre-change), no PHP fatal, no ConservationError. Valid Mama's values render identically; no visual regression. Full no-inline migration of this block is scheduled next session (the sanitiser is contract §D, applied early here as a standalone security fix).
