---
block: sgs/quote
date: 2026-07-09
verdict: PASS
first_paint_capture_passed: true
---

# Visual-diff — sgs/quote 100% no-inline + box-object migration (D293 rollout)

**Verdict: PASS** · **first_paint_capture_passed: true**

## What changed
`sgs/quote` migrated to the locked no-inline styling contract as a **block-private** block
(qc-council 2026-07-09 verdict: content-KIND composites that only need box+width go
block-private, like button/heading — quote never used the shared wrapper's grid/section
machinery; converter routing is indifferent to `wraps_block`). The `<blockquote>` is the
block root via `get_block_wrapper_attributes()` (no `SGS_Container_Wrapper`, no wrapper div);
every styling support (`color`/`typography`/`spacing`/`__experimentalBorder`) carries
`__experimentalSkipSerialization: true`; ALL CSS (outer padding/margin base+tiers, maxWidth,
contentWidth, border, box-shadow, hover, background, colour/typography, base border-radius,
attribution typography) emits into the block's own scoped `.{uid}` `<style>`. Box families →
objects: base padding/margin → WP-native `style.spacing.*`, tiers →
`paddingTablet`/`paddingMobile`/`marginTablet`/`marginMobile` objects, `borderWidth` object,
base radius → `style.border.radius`. 22 flat per-side/corner attrs removed; scoped uid is a
CLASS (anchor-safe). No version bump.

## LANDED verification (live sandybrown, anonymous Playwright, cache-bust, post OPcache + LiteSpeed purge)
First-paint: anonymous render of the asymmetric test instance (`#quote-asym-test`, page 1356)
succeeded — `<blockquote>` present + painted at 375/768/1440 (no blank/error paint).

- **Zero inline (all 3 breakpoints, full subtree):** `getAttribute('style')` on the blockquote
  root AND every descendant (incl. the nested `sgs/text` body) = `[]` — NO property declaration.
- **Box-object mechanism proven** (asymmetric instance, 4 distinct sides + asymmetric corners):
  - padding landed exactly at every tier — `paddingMobile` 3/6/9/12 @375, `paddingTablet`
    7/14/21/28 @768, base 11/22/33/44 @1440 (all 4 sides distinct — proves the scoped tier accumulator).
  - border-radius 24/0/6/12 (asymmetric corners) landed at all breakpoints.
  - border-width 1/2/3/4 (asymmetric sides) landed at all breakpoints.
  - margin top/bottom 5/15 landed. **margin left/right (authored 20/10) is emitted correctly in
    the scoped rule** (live CSS confirmed: `.sgs-quote-…{…margin-right:10px;margin-left:20px;}`)
    but the COMPUTED left/right is overridden by the test page's content-column auto-centring
    (`margin-inline:auto`, 112.5px symmetric @1440) — a pre-existing page-layout behaviour
    affecting every content block, NOT a block defect (the block emits it right; padding via the
    identical mechanism landed perfectly).
- **Page-8 reclone regression:** content 100% @375/768/1440, computed-parity CSS 68/69/69%
  (steady vs baseline), anti-mirror gate PASS, `wp-blocks validate: valid`, no `ConservationError`.

## Gates (orchestrator re-run, STOP-16)
build PASS · box-family AST guard 0 violations · dead-controls 0 net-new · `audit-inline-styling.js`
quote inline-render = 0 (was 17) · converter suite 439 pass/1 skip · box_family seeds (borderWidth,
padding/marginTablet/Mobile) landed · 43 flat orphans pruned via `/sgs-update` Stage 10.
