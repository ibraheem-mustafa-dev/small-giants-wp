---
block: trust-bar
date: 2026-06-07
verdict: PASS
first_paint_capture_passed: true
---

# Visual-diff — sgs/trust-bar icon-identity resolver — 2026-06-07

**Change:** the cloning converter now resolves each cloned `.sgs-trust-bar__badge`'s
SVG/emoji to the correct icon slug (new `converter_v2/icon_resolver.py` — reverse
path-index from the lucide/wp-icons assets + structural heuristics + emoji map),
setting `items[].icon` (was `icon=''` → uniform fallback ticks). render.php gains a
raw-SVG fallback branch (`items[].iconSvg` → `wp_kses` with the existing
`sgs_svg_kses_allowed_tags()` allowlist) for the no-confident-match case, so an
unknown icon still renders its real glyph — never a silent wrong/default icon.
block.json gains an `items` sub-schema. No `save()` change (dynamic block).

## Live verification (canary page 8, after build+deploy + OPcache reset + re-clone, cache-bust)

The 4 cloned trust-bar badges, read from the live homepage DOM:

| Badge label | Resolved icon (SVG class) | Confidence | Verdict |
|---|---|---|---|
| Handmade in Birmingham | `lucide lucide-home` | medium (structural) | PASS |
| Registered Food Business | `lucide lucide-check` | high (exact path) | PASS |
| Free UK Delivery Over £35 | `lucide lucide-truck` | medium (structural) | PASS |
| Loved by Breastfeeding Mums | `lucide lucide-star` | medium (structural) | PASS |

- Before: all 4 badges rendered the generic `check` tick (`icon=''` fallback).
- After: each badge renders its correct icon, matching the draft's semantic intent.
- Emission cross-check: `extract.patched.json` items = `[{home},{check},{truck},{star}]`.
- No raw-SVG fallback was needed (all 4 matched named slugs present in `$lucide_map`).
- Console errors: 0.

first_paint_capture_passed: true (live DOM at 1440px confirms all 4 icons render on first paint).
