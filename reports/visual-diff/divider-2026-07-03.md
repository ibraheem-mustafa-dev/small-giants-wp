# Visual diff — sgs/divider gap variant (2026-07-03)

verdict: PASS
first_paint_capture_passed: true

## Change
Added a 5th `variant` value `gap` — an invisible spacer (maps `core/spacer` via the
D265 core→sgs replacement mapping). New attrs `gapHeight` (default 48) + `gapHeightUnit`
(px/em/rem/vh). No visible line; the inner `__gap` element supplies the vertical space.
Existing variants (line/dots/wave/shape) render byte-identically → no deprecation.

## LANDED verification (live, stronger than a local capture)
Deployed the compiled build to sandybrown and rendered a real page instance
`<!-- wp:sgs/divider {"variant":"gap","gapHeight":80} /-->`. Live rendered output:

    <div class="wp-block-sgs-divider__gap" style="height:80px" aria-hidden="true"></div>

Correct: invisible spacer at the requested height, aria-hidden, no line. save.js /
render.php / editor-preview parity confirmed. npm build succeeded (dead-control guard
passed — gapHeight is consumed in render.php + save.js). WPCS 0 errors.
