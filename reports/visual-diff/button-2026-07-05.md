# Visual diff — sgs/button — 2026-07-05

verdict: PASS
first_paint_capture_passed: true

## Change under review
Additive block-side controls, all defaulting to inherit/none so existing buttons are unchanged:
- per-device width: `customWidthTablet/Mobile` + `widthTypeTablet/Mobile` (+ per-tier units) rendered in a uid-scoped `<style>` at 768/1024; each new tier defaults `""` = inherit desktop → no override emitted when unset.
- `textDecorationHover` (default `"none"`) → adds `text-decoration:underline` to the existing `:hover`/`:focus-visible` rule only when set to `"underline"`.

## Evidence (live page 8, sandybrown, post-deploy 2026-07-05 19:46)
- 8 `a.sgs-button` render unchanged: padding `14px 24px`, natural widths (150.8 / 136.1 / 182.7px), `text-decoration-line: none` (no forced decoration) — no regression from the render.php edits.
- No console errors; first paint renders buttons visible (no animation/visibility gating on the button element).
- New tier/hover controls are additive and unset on this page (converter mapping is a separate task) → zero visual delta by construction.
