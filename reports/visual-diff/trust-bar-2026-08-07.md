---
block: sgs/trust-bar
date: 2026-08-07
verdict: PASS
first_paint_capture_passed: true
first_paint_capture_run: true
capture_method: chrome-devtools-mcp against the live sandybrown canary (Playwright reserved for a co-active session)
deployed_build: deploy 2026-08-07, deployed files md5-verified against local build
change: Stranded ':not([style*=…])' guard de-specified via :where() (painted token, not inherited).
---

## Live measurement
Page /sgs-gate-canary/ and the homepage. Every element owning its own text nodes inside .wp-block-sgs-trust-bar measured for computed colour vs effective background (walking ancestors until non-transparent): 4 elements on the canary, further elements on the homepage sweep — **0 below 4.5:1**.

## Negative control
The sweep walks EVERY text-owning element, not a single class — the method that found the card-grid defect an earlier single-class sweep missed. Zero ':not([style*=' guards remain on any '.sgs-' selector in the deployed stylesheets.
