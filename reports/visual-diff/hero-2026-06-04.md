# Visual-diff — sgs/hero WS-4 composite-mirror — 2026-06-04

verdict: PASS
first_paint_capture_passed: true

**Change:** WS-4 — hero's outer `<section>` is now rendered by the shared `SGS_Container_Wrapper::render( …, 'section', opts )` (element route, §FR-22-21.1). Hero's classes → `extra_classes`; `$styles` (incl. the split `display:grid` + `grid-template-columns`) → `extra_styles`; the bespoke interior (LCP `<img>`, bg-video, svg, overlay, `__content`, badges) → `$inner_html`. Full section-KIND container surface mirrored into block.json (v0.2.0) + `supports.sgs.containerKind:"section"`; `ContainerWrapperControls kind="section"` wired into edit.js. C3 double-emit guard: every attr that would drive a duplicate helper layer is nulled in the helper attr copy (`backgroundImage`/+T/M, `backgroundVideo`/`bgVideo`/`bgVideoMobile`, `bgSvgContent`, `minHeight`/+T/M) + `no_overlay:true`. C4 rename `overlayColour`→`backgroundOverlayColour` (`?? legacy` fallback). Split passes `wrap_inner:false`.

**Validation:** live `do_blocks()` probe on the sandybrown canary (token-gated webroot one-shot runner, opcache reset first), both hero variants. NOT a page-144 re-clone — the converter emits containers, not composites, so the editor/test-page is the correct validation surface (memory `composite-mirror-is-separate-from-cloning-fidelity`).

## Result — PASS (live-DOM, R-22-11)
- **0 PHP errors** — `PROBE_OK` printed after both renders (the helper gap-path runtime fatal that `php -l` cannot catch did NOT occur — the failure mode from memory `shared-helper-must-require-its-own-deps`).
- **split variant** — `sgs-container` mirror class applied + `sgs-hero` own class preserved; `display:grid` present (split grid carried via `extra_styles`); `sgs-container__inner` **ABSENT** (`wrap_inner:false` → no div injected between the section grid and its `__content`/`__media` grid items); `sgs-hero__content` preserved.
- **standard variant** — `sgs-container` + `sgs-hero` present; `sgs-hero__bg-img` present (the LCP `<img>` rendered bespoke — NOT double-emitted as a helper CSS background, proving the null-set); `sgs-hero__overlay` present (hero's own overlay, reading `backgroundOverlayColour` — proving both the rename and that `no_overlay` suppressed the helper's duplicate); no `__inner` (no `contentWidth` set); `sgs-hero__content` preserved.

The mirror wraps hero in the shared `sgs/container` element (uniform with a plain container — one universal conversion procedure per §FR-22-21) while preserving every bespoke interior layer and neutralising all double-emit/structural risks. widthMode defaults to `'default'` (matches shipped cta-section/trust-bar — adds no constraining class; hero stays full-bleed via its own CSS).

**Known polish follow-up:** the legacy "Overlay colour" control + the mirror's overlay control both appear in the inspector (duplicate; both functional via the `backgroundOverlayColour ?? overlayColour` fallback). Not a functional regression.
