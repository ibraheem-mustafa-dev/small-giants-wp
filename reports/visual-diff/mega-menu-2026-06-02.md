---
block: mega-menu
date: 2026-06-02
verdict: PASS
first_paint_capture_passed: true
change_type: design-fix
change_description: >
  Wave-2 /ui-ux-pro-max critique fixes to the mega-menu layout CSS: (3) featured-promo
  card contrast raised from 3.32:1 (FAIL) to 11.86:1 (PASS) by switching .sgs-mega-featured
  to text-token background + surface-token foreground; (4) card-grid gradient cards gain a
  subtle 1px border for visual definition. Theme version bumped 1.3.5→1.3.6 to cache-bust
  the theme CSS ?ver= (the fix did not apply until the theme version changed — theme CSS
  keys off $theme_version, not block.json).
verification_method: live canary render + computed contrast eval (Playwright)
pixel_diff_skipped: true
pixel_diff_skip_reason: Generic layout skeletons, no mockup baseline; verified by rendered output + WCAG contrast computation (R-22-11).
verified_by: opus-main-thread-live-playwright
---

# Mega-Menu Layouts — Wave-2 Design Fixes (2026-06-02)

## Verified live (canary, CSS ver 1.3.6)
- **Featured-promo card contrast (CRITICAL):** `.sgs-mega-featured` now bg
  `rgb(58,46,38)` (text token) × fg `rgb(251,243,220)` (surface) = **11.86:1**
  (was cream-on-pink 3.32:1 — failed WCAG AA). The dark card also reads as a clearer
  focal anchor than the previous pink-on-pink. Confirmed by eye + computation.
- **Card-grid definition:** `.sgs-mega-card--gradient` now has `1px solid` border.
- **Cache-bust learning:** theme CSS (`mega-menu-panels.css`) is enqueued with
  `$theme_version` as `?ver=`; the fix only took effect after bumping the theme
  Version header (1.3.5→1.3.6). Plugin block CSS busts via block.json version; theme
  CSS busts via the theme version. Zero console errors.
