---
block: icon
date: 2026-06-02
verdict: PASS
first_paint_capture_passed: true
change_type: feature
change_description: >
  Wave-2 sgs/icon enhancements: optional decorative shape backgrounds (backgroundShape
  extended none|circle|pill|square|outline) matching the social-icons aesthetic; clickable
  icons reuse the EXISTING linkUrl/linkTarget (no duplicate attr) with a 44px target +
  focus ring; hover effects (hoverIconColour renamed from hoverColour + new hoverShapeColour
  + hoverScale) honouring prefers-reduced-motion. deprecated.js v1 added (null-save catch-all
  + hoverColour→hoverIconColour migrate). version 0.1.0→0.2.0.
verification_method: live canary render + computed-style eval (Playwright)
pixel_diff_skipped: true
pixel_diff_skip_reason: Additive feature on an existing block; verified by rendered output + measurement (R-22-11).
verified_by: opus-main-thread-live-playwright
---

# sgs/icon — Wave-2 Enhancements (2026-06-02)

## Verified live (canary)
Rendered 5 icons in a row — each shape computes correctly:
- **circle (linked):** border-radius 50%, bg primary, `<a>` wrapper with **44px** target.
- **pill:** border-radius 999px, bg accent.
- **square:** border-radius 0, bg primary-dark.
- **outline:** transparent bg + `2px solid` ring.
- **none:** no shape (existing behaviour, unchanged).

Hover-extension classes wired (`sgs-has-hover`, `sgs-has-hover-scale`). Existing icons
unchanged (all new attrs default-off + deprecated.js v1). Clickable feature reuses the
pre-existing `linkUrl`/`linkTarget` — no duplicate `iconLink`. Zero console errors.

## Note
render.php carries a legacy fallback `hoverIconColour ?? hoverColour` for un-migrated
posts (editor-side migrate handles the rename). DB re-scan via /sgs-update pending to
refresh the icon attrs (hoverColour→hoverIconColour, +hoverShapeColour, +shape enums).
