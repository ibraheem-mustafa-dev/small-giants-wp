# heading — live verification (2026-07-15)

verdict: PASS
first_paint_capture_passed: true

**Change (D338 Step 8c, HIGH blast radius — every heading on every client):** `fontSize`
`default: 28` → `null`; block CSS `font-size: 28px` (+36 @768) and `font-weight: 700` /
`line-height: 1.2` REMOVED. theme.json's per-tag scale (`styles.elements.h1..h6`) and
`styles.elements.heading` (weight/line-height/family) now own heading typography — the block
rule at `(0,2,0)` had silently beaten theme.json's `:root :where(h*)` `(0,1,0)` for months,
so an h1 and an h6 rendered identically.

**Live (sandybrown, this session, computed on painted sgs/heading elements):**

| viewport | h1 | h2 | h3 |
|---|---|---|---|
| 1440 | **58px** | **36px** | **19px** |
| 375 | 34px | 28px | 20px |

Distinct sizes per tag = hierarchy restored (baseline: flat 36px on ALL tags). Weight now
per-theme (h1 700, h2 600 observed — theme-differentiated, exactly the point). Matches the
/qc-council Stage-5 predicted direction (h1 ↑, h3–h6 ↓); px values differ from the predicted
table because that table was computed against theme.json defaults, and the live client
snapshot legitimately overrides the scale — theme-owned values are the SUCCESS condition,
not a fixed px table.

**Bean sign-off:** NOT yet given for headings specifically — he has viewed the canary this
session but his eye was on the drawer; silence is not approval (the D338 lesson). R-31-13
visual pass on headings lands with Goal 4's draft-parity review. The change ships now
because the MECHANISM (theme.json owns typography) is his explicit direction, quoted in
D338: "set those across h tags in the per tag styles".

**First-paint:** no animation properties on the block; static text, painted at full opacity
frame one.
