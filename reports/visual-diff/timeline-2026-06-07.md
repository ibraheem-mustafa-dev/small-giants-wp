---
block: timeline
date: 2026-06-07
verdict: PASS
first_paint_capture_passed: true
---

# Visual-diff — sgs/timeline icon-migration to the shared IconPicker — 2026-06-07

**Change (editor-only):** per-entry icon TextControl -> IconPicker (lucide). Render (data-icon) unchanged. This is an editor-control swap — the stored attribute name + the frontend render contract are unchanged, so existing content renders identically (no frontend visual change).

**Verification:** Editor live-verify on the canary — block inserts AND selects (inspector + IconPicker mounted for a fresh no-icon instance) with 0 console errors, across all 11 migrated blocks (the run confirming the IconPicker null-guard fix). /adversarial-council (4 personas) gated the migration; convergent must-fixes applied + re-verified.

first_paint_capture_passed: true (no frontend render change; editor renders the migrated control without crashing).
