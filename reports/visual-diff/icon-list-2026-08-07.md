---
block: sgs/icon-list
date: 2026-08-07
verdict: PASS
first_paint_capture_passed: true
first_paint_capture_run: true
capture_method: chrome-devtools-mcp (Playwright reserved for a co-active session)
deployed_build: sandybrown canary, deploy 2026-08-07, verified by md5 of deployed files vs local build
change: Spec 32 stranded ':not([style*=...])' fallback-guard purge
---

## What changed
Rest-state colour fallback deleted (the value restated what the element inherits); the icon and hover fallbacks kept but de-specified via `:where()` so an operator colour wins.

## Live measurement (post-deploy, on the real canary)
Drawer variants centred-statement and split-zone-serif, drawer open at 1440. All SIX `.sgs-icon-list__text` elements compute `rgb(255,255,255)` on `rgb(58,46,38)` = **contrast 13.14:1**. Pre-fix the same six measured `rgb(58,46,38)` on `rgb(58,46,38)` = **1:1, invisible**.

## Negative control
A full-drawer sweep of EVERY element owning its own text nodes (not just the icon-list class) returned zero icon-list findings below 4.5:1, and the deployed stylesheet contains zero `:not([style*=` guards on any `.sgs-` selector.

## Limits
Measured on the canary only. This report evidences the FIRST-PAINT rest state;
hover and focus states were not captured.
