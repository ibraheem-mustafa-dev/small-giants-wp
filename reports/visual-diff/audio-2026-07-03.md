# Visual diff — sgs/audio (new block, 7 player styles) — 2026-07-03

verdict: PASS
first_paint_capture_passed: true

## What
New dedicated `sgs/audio` block: a native `<audio>` player upgraded by a Web Audio
view-module to one of 7 styles — minimal / waveform / spectrum / radial / oscilloscope /
gradient-pulse / hidden. Progressive enhancement (SSR native player, no-JS fallback),
theme-tokened colours, AudioObject JSON-LD schema, performance-tuned (visibility-gated
RAF, shared AudioContext, first-play graph, reduced-motion freeze). Replaces core/audio.

## LANDED /qc on sandybrown (live, all 7 variants)
Deployed the build; created test pages (external + same-origin media) and drove them:
- All 7 variants render + enhance (custom transport built; `hidden` stays unenhanced,
  audio present). All play buttons carry aria-label + aria-pressed; 44px targets.
- Playback verified with a real trusted click: audio plays, timecode "0:10 / 7:05",
  progress + seek update, play-button aria toggles. readyState 4, no media error.
- Reactive visualiser verified same-origin: analyser reads REAL data (freqMax 255,
  non-tainted) and the spectrum canvas paints bars (canvasNonBlank true).
- ZERO console errors from the block (only a site favicon 404).
- Bug found + fixed during QC: `crossorigin="anonymous"` broke playback for non-CORS
  external URLs (blocked the resource) — removed; same-origin audio still drives the
  visualisers, external audio plays cleanly (visualiser idles gracefully).
npm build clean (dead-control guard + F5/F6 + cheat-gate green), WPCS 0 errors.
