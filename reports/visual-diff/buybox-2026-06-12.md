# Visual-diff — sgs/buybox PDP product-area gallery (Spec 30 Step 10a, D218) — 2026-06-12

verdict: PASS
first_paint_capture_passed: true

**Change:** The PDP buybox now renders the full product area as a 2-column grid — a store-driven variation-aware image gallery (left) + the existing configurator (price/value-ladder/option-pickers/add-to-cart/notify-me) (right). Reuses the product-card store's FR-27-A4 swap engine (already wired to the buybox via `data-wp-init="callbacks.initPillBridge"`). New `gallery-col.php` partial renders `.product-card__media` (img bound to `context.imageSrc`) + `.product-card__thumbs` strip. Per-variation galleries delivered via a HEX-flagged JSON data-island (`script.sgs-buybox-galleries`) emitted inside the wrapper (outside the 24KB `data-wp-context` cap); the shared `product-card/view.js` `applyPillSelection` reads the island additively when a seeded combo gallery has <2 images (product-card cards have no island → null → identical prior behaviour). Theme `single-product.html` uses the buybox for the product area; the separate `woocommerce/product-image-gallery` part is no longer placed (simple-product core fallback still includes a gallery). WCAG: main-image alt falls back to the product name; thumbs are 64px buttons with `aria-current` + focus rings.

**Validation (live canary, R-22-11, Playwright DOM — product 540, 48-SKU variable fixture):**
- 2-column grid live (391px / 340px); `sgs-buybox__gallery-col` + `sgs-buybox__config-col` present; **no core fallback** even with all 48 galleries configured.
- **Main image swaps per variation** across all 48 (6 distinct images over 6 variation selections).
- **Thumbnail strip swaps per variation** for ALL variations via the island (`islandKeys=48`, every tested combo shows a 3-image strip, `thumbsHidden=false`).
- Thumbnail click → main image becomes the clicked thumb; `aria-current` moves correctly; thumb size 64×64 (≥44px).
- Main image `alt="Mama's Test Box — 48 SKU fixture"` (product-name fallback), persists across swaps.
- Cross-family rater (haiku): 0 defects — product-card regression (additive, null-safe), island XSS (HEX flags + false-guard), seed-shrink, markup escaping all PASS.

## Result — PASS (R-22-11)
- All interaction paths live-verified on the real canary PDP; configurator intact (cap removed via data-island); 0 PHP fatals; `php -l` clean; build clean; shared-store change confirmed additive + product-card-safe. R-22-13 final 3-breakpoint sign-off owed at Step 12 (desktop screenshot shared with Bean 2026-06-12).
