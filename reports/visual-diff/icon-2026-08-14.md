# Visual Diff Report: icon (2026-08-14)

## Change Category
Inspector architecture correction (block.json `supports.color` sub-flags +
edit.js/SgsColourPanel.js editor mount) + a colour-support declaration change
that touches the render surface indirectly (WordPress's own native colour
supports mechanism), so this is NOT auto-skippable by `check-editor-only.py`
or `check-markup-neutral.py` (both PHP-scoped/edit.js-sole-file checks). Real
before/after live capture below, not a category-based waiver.

## Changes Reviewed
- `SgsColourPanel.js`: no longer mounts into WordPress's native
  `InspectorControls group="color"` slot — now a standalone SGS-owned
  `PanelBody` in the default group. Pure editor-UI change.
- `icon/edit.js`: consumes the corrected `SgsColourPanel`; comment updated.
  Pure editor-UI change.
- `icon/block.json`: `supports.color` sub-flags changed from
  `{background:true, gradients:true, text:true, __experimentalSkipSerialization:true}`
  to `{background:false, gradients:false, text:false}`. This is the one
  change with a plausible frontend effect — WordPress's native colour
  support mechanism drives both a `has-*-color`/`has-*-background-color`
  class family AND an inline style, normally.

## Verification — real before/after live capture, not reasoning from memory
Deployed BOTH states to the sandybrown canary and diffed the SAME live page
(`/icon-colour-verify/`, WP-CLI-created test page 2421, `sgs/icon` with
`iconColour:"#e68a95"`), `render.php` completely untouched throughout:

**AFTER (this change, `--payload icon-colour-fix`):**
```
<div class="sgs-icn-f10a409a sgs-has-hover sgs-has-hover-shadow sgs-has-hover-scale sgs-has-img-zoom sgs-has-focus-ring sgs-icon sgs-icon--source-lucide sgs-icn-f10a409a wp-block-sgs-icon">
```

**BEFORE (HEAD `f9f39bb6`, temporarily restored via `git stash`, `--payload icon-colour-before`):**
```
<div class="sgs-icn-f10a409a sgs-has-hover sgs-has-hover-shadow sgs-has-hover-scale sgs-has-img-zoom sgs-has-focus-ring sgs-icon sgs-icon--source-lucide sgs-icn-f10a409a wp-block-sgs-icon">
```

**Byte-identical.** No `has-text-color`, no `has-*-background-color`, no
inline `style="color:…"` in either state. This confirms the mechanism this
report predicted: `__experimentalSkipSerialization: true` on the PRE-change
`supports.color` block already suppressed BOTH the className additions and
the inline style WordPress's colour support would otherwise emit, so the
sub-flags changing from true/true/true to false/false/false has no rendered
effect — output was already fully inert on the frontend before this commit,
and stays fully inert after it. The block's own colour rendering
(`--sgs-icon-*` custom properties, scoped `<style>`) is driven entirely by
`iconColour`/`backgroundColour`/etc. via `render.php`, never by native
`supports.color` — confirmed unchanged by this diff.

first_paint_capture_passed: true

## Verdict
verdict: PASS

Restored my working-tree changes after the before-capture (`git stash pop`,
confirmed via `git status` that all 3 files matched their pre-stash content)
and rebuilt before writing this report — the SHA below is measured against
that restored, final state.

source_sha: b5aafbe864c493ad

## Notes
- Task: T4 follow-up correction (uniformity-thread orchestration plan,
  `~/.claude/plans/go-track-1b-playful-hamster.md`), Bean's direct correction
  2026-08-14: "why are you using the Color panel, we're just supposed to be
  taking the code and using it for our own custom settings."
- Editor-side live-verified (page 2422, Playwright): BEFORE the redeploy
  (canary still on the mid-fix state), the Styles tab showed a native "Color"
  ToolsPanel — heading "Color", a "Color options" `+` disclosure button, native
  "Text"/"Background" buttons — with our "Icon colour" row squeezed inside it.
  This is the exact defect Bean flagged: our content living inside NATIVE's
  own ToolsPanel component, not a panel we own. AFTER redeploying this fix:
  Settings tab now shows a "Colour" panel (own PanelBody, no `+` menu, no
  native Text/Background buttons) FIRST — before Icon/Background/Hover
  effects/Spacing/Accessibility/Visibility conditions — with "Icon colour"
  and "2 colour states available: Normal, Hover"; the Styles tab's Color
  section is gone entirely (only Dimensions/Animation/Click Effects/Element
  parallax/Scroll & effects remain there).
