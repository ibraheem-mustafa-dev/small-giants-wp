# Visual diff — sgs/nav-menu — 2026-09-12

verdict: PASS
intent_capture_passed: true
source_sha: 8714beff30aa3156

## What changed

`plugins/sgs-blocks/includes/nav-menu-submenu-css.php` + `plugins/sgs-blocks/src/blocks/nav-menu/block.json`
— Wave 2 cluster 2/3 fixes (I1, C1 submenu-row half, M5, M6 submenu-css half, M4/M2 drawer
parity). Five discrete, independently-testable behaviours, each a new isolated capability or a
one-off value fix — there is no meaningful single "before" screenshot that covers all five
(different fixtures, different states: rest vs open vs hover vs tablet-resize), so intent
capture (assertion stated, then measured against the live canary) is the correct report shape
per `.githooks/README.md`.

## Assertions, live results

All captured live on the canary (sandybrown-nightingale-600381.hostingersite.com) after this
deploy, via Playwright MCP, real DOM state (click-toggled or CSS-source-confirmed, not
synthetic pseudo-class simulation where the browser doesn't support it).

| # | Assertion | Fixture | Live result |
|---|---|---|---|
| I1 | Submenu panel background falls back to `submenuLinkBg` when `submenuBg` is unset | Page 3488, "G14 negative submenuLinkBg blocks sweep" (`submenuLinkBg:"primary"`) | `panelBg = rgb(230,138,149)`, `rowBg = rgb(230,138,149)` — **match** (was a white/cream mismatch pre-fix) |
| M5 | Plain-dropdown caret flips 180° on open, not just mega-trigger | Same fixture, `.sgs-nav-menu__subtoggle` clicked open | `caretSpanTransform: matrix(-1,0,0,-1,0,0)` (was `none` pre-fix) |
| C1 | Submenu row divider: `border-light` at rest, `accent` on hover/focus-visible | Same fixture, `.sgs-nav-menu__sublink` | Rest: `border-bottom-color rgb(229,231,235)` (= `border-light` token), width `1px` solid. Live CSS source confirms `:hover`/`:focus-visible → var(--wp--preset--color--accent)` |
| M6 | Caret SVG tracks `itemFontSize` tier via `1em` (was fixed 24×24px) | Page 3487, "G20 – item + submenu font-size tiers set" (`itemFontSize: {desktop:"24px", tablet:"18px"}`) | Desktop: caret 24×24px (`font-size:24px`). Resized to 900px: caret 18×18px (`font-size:18px`) — tracks the tier at both breakpoints |
| M4/M2 | Drawer-scoped colour rule pairs `.sgs-nav-menu__link` + `.sgs-nav-menu__caret svg` with `.subtoggle`, not `.subtoggle` alone | Page 3487, "G16 – smartContrast OFF" (`itemColourHover:"#1a1a1aff"`) | Live CSS source: `.sgs-nav-menu-483f21e6 .sgs-nav-menu__subtoggle,.sgs-nav-drawer .sgs-nav-menu-483f21e6 .sgs-nav-menu__link,.sgs-nav-drawer .sgs-nav-menu-483f21e6 .sgs-nav-menu__caret svg{color:inherit;}` — matches the shipped source exactly. Bar-side `:hover` and keyboard `:focus-visible` rescue rules (sibling's file) also confirmed pairing `.link` with `.caret svg` |

## Why before/after doesn't apply

Each row is a distinct, previously-broken behaviour on a distinct named fixture (a colour
fallback, a missing CSS selector, a new border default, a size unit, a selector-pairing widen)
— there is no single coherent "before" render that would make a diff meaningful across all
five; the real question for each is "does the fixed behaviour now hold", answered directly
above against the live canary.

Zero console errors across all captures.
