---
block: mega-menu
date: 2026-07-21
verdict: PASS
first_paint_capture_passed: true
target: sandybrown-nightingale-600381.hostingersite.com
change: "Spec 35 manifest + resting-state base (linkBgColour), Bean option A"
---

# mega-menu manifest + resting-state fix — visual verification

## Standing fact-check (do not reverse)
There is BOTH a `sgs/mega-menu` BLOCK and a `sgs_mega_menu` CPT
(includes/class-sgs-mega-menu-cpt.php, Spec 36 FR-36-5, 2026-07-20). The block is NOT
superseded: the CPT stores panel CONTENT, the block is the menu ITEM. The block is live
and referenced by class-sgs-adaptive-nav-renderer.php:112 and class-sgs-nav-menu-source.php.
Do not delete, deprecate or "migrate" it.

## What changed
Manifested (5 elements: wrapper/trigger/panel/badge/icon). Closed the resting-state
defect: `linkHoverColour`/`linkHoverBgColour` existed but the trigger had no resting
BACKGROUND attribute. Added `linkBgColour`; style.css:66
`background: transparent` -> `background: var(--sgs-mm-link-bg, transparent)`.

Also found and fixed a pre-existing gap: FIVE attributes (linkColour, linkBgColour,
linkHoverColour, linkHoverBgColour, panelBgColour) had NO inspector control at all — they
rendered but no client could reach them. A new "Link Colours" panel exposes all five.

## Ownership boundary (recorded in _ownership_note)
mega-menu owns one <li> item plus its dropdown panel. The PARENT (core/navigation or
sgs/adaptive-nav) owns the surrounding <ul>/nav-bar chrome — nav background, inter-item
spacing, mobile-drawer shell. `menuTemplatePart` panel content is delegated via
do_blocks(); those blocks own their own styling and are NOT declared here.

## Deploy provenance
- Isolated worktree `C:/tmp/sgs-nv` at scratch SHA `b03b2c31`.
- `build-deploy.py --target sandybrown --blocks-only --skip-build`, exit 0.
- CHECKSUM proof local == server for build/blocks/mega-menu/style-index.css.

## Results
- `linkBgColour`, `linkHoverBgColour`, `linkColour` all REGISTERED live (70 attrs total) —
  WP did not discard them (D338).
- Live homepage header nav renders unchanged; console errors 0.
- mega-menu STATE_WITHOUT_BASE: 0 (both hover mappings resolve STATE_OK now the base exists).

## Verdict
PASS. Option A confirmed (unset -> `transparent`, byte-identical to before).

## LOGGED, NOT FIXED
`badgeColour` is DEAD CSS WIRING — emitted only as a `data-sgs-badge-colour` attribute and
never consumed by any style.css rule. A client can set it and nothing happens. Left as an
honest ORPHAN with a _note rather than silently mapped. Worth a follow-up.

## Not covered
No live mega-menu instance exists on the canary to render-test the dropdown itself; the
block is verified by attribute registration, manifest resolution and the unchanged header
nav. A real mega-menu instance render remains untested (R-31-13).
