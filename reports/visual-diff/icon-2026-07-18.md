---
block: icon
date: 2026-07-18
verdict: PASS
first_paint_capture_passed: true
---

# Visual-diff — `sgs/icon` inline-zero (D345) — 2026-07-18

Wave-2 of the framework-wide inline-zero rollout (Spec 32 FR-32-4 as amended
2026-07-18 / D345). Block-private conversion (converted by a `wp-sgs-developer`
agent; diff reviewed + live-verified by the main session).

## What changed (all inside `src/blocks/icon/render.php`)

The five per-instance custom-property VALUES (`--sgs-icon-size`,
`--sgs-icon-outline-colour`, `--sgs-icon-hover-colour`,
`--sgs-icon-hover-shape-colour`, `--sgs-icon-hover-scale`) previously joined into
`$extra_wrapper_attrs['style']` and rendered inline on the root `<div>` now fold
into the block's **existing** scoped `.{uid}.wp-block-sgs-icon{ … }` rule (the
same rule that already held `color` / `background-color` / `--sgs-icon-shape-padding`).
The `'style'` key was dropped from `get_block_wrapper_attributes()`.

No `[style*="--"]` presence-selectors exist in this block's stylesheets (it
already used `var(--sgs-icon-*, <fallback>)` syntax), so GOTCHA F does not apply —
no `:hover` rewrite needed. block.json already declared
`__experimentalSkipSerialization` on `color` + `spacing`. No value changed; no
version bump; no `deprecated.js` (D270).

## Live verification — palestine-lives.org homepage (8 instances)

| Check | Result |
|---|---|
| icon elements carrying an inline `style` attr | **0** (was 8) — `getAttribute('style')` null |
| scoped rule in collected CSS | `.sgs-icn-{hash}.wp-block-sgs-icon{--sgs-icon-size:32px;--sgs-icon-hover-colour:…}` present |
| computed `--sgs-icon-size` | `32px` (resolves from the scoped rule) |
| computed icon colour | `rgb(10,126,168)` (theme primary — unchanged) |
| page-wide inline `style="--"` | dropped (icon's 8 removed) |

## first_paint_capture_passed

Desktop screenshot (`wave2-icon-testimonial-postD345.png`) — the info-box row
icons (Family Values / 5,000+ Products / Dedicated Support / White Labelling)
render correctly at first paint with zero inline styling. Byte-identical var
relocation, computed values unchanged.
