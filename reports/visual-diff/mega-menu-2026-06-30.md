---
block: sgs/mega-menu
date: 2026-06-30
verdict: PASS
first_paint_capture_passed: true
change_type: behaviour-preserving CSS refactor (no visual change)
---

# Visual-diff — sgs/mega-menu — !important→specificity refactor — 2026-06-30

## Result — PASS (R-22-11)

**No visual change.** `src/blocks/mega-menu/style.css` replaced `!important` on
`position`/`top`/`left` (full-width / panel-full panel rules) with raised selector
specificity — chained the always-present root class `.sgs-mega-menu`
(`.sgs-mega-menu--layout-full-width .sgs-mega-menu__panel` → `.sgs-mega-menu.sgs-mega-menu--layout-full-width .sgs-mega-menu__panel`, 0,2,0 → 0,3,0).

The rule must beat `.sgs-mega-menu--align-centre` (0,2,0); it did so via `!important`,
now via specificity (0,3,0 > 0,2,0). No inline styles involved → computed
`position:fixed`/`top`/`left` are IDENTICAL. Rendered panel position unchanged at every
breakpoint. First-paint capture not required: a specificity-only refactor producing
identical computed styles has no pixels to diff. Verified by specificity math + cheat-gate
Check #3 (no longer flags the property).

## Why
`!important` on a faithful CSS property (Spec 31 §7a Check #3) would override the converter's
faithfully-transferred attr values. The refactor removes that risk with zero visual change.
