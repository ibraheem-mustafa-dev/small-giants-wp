---
doc_type: reference
title: "Visual-diff / LANDED report — sgs/mega-menu no-inline migration"
block: sgs/mega-menu
date: 2026-07-10
wave: "no-inline rollout Wave 2 (Round 3)"
verdict: PASS
first_paint_capture_passed: true
---

# sgs/mega-menu — no-inline migration (LANDED) — F3 partially drained

**Verdict: PASS (no-inline).** Keep-structure InnerBlocks navigation. Verified LANDED on
sandybrown page 1356 at 375/768/1440 via the harness (`no-inline-wave2-round3-manifest.json`)
— zero inline property declarations across the nav subtree with a background+text colour set.

## Evidence
- **Zero inline** (#1): the real inline `style="position:fixed;width:100vw;max-width:100vw;top:…;left:0"`
  on the flyout panel is now emitted into the block's own scoped `<style>#{uid}-panel{…}</style>`
  (nested inside the `<li>` — valid content model). The `color` support (previously a DEAD no-op —
  render never called `get_block_wrapper_attributes()`) now genuinely paints, scoped to `#{uid}` via
  `wp_style_engine_get_styles`. 2 `view.js` `panel.style.left/right` writes → `--sgs-mm-overflow-*` CSS vars.
  Remaining inline is `--var:value` only (permitted §A).
- No box family (colour-only support) — no box seeds.
- Keep-structure: `<li>` → trigger → panel → InnerBlocks intact; nav/keyboard behaviour untouched.

## F3 status (honest)
- **Row 1 (panel `max-width: min(900px, calc(100vw - 2rem))` → `panelWidth`/`panelMaxWidth`): DRAINED**
  to `max-width: var(--sgs-mega-menu-max-width, <old literal>)`, render emits the var for both `custom`
  AND `content` panel modes (the `content`-mode control was previously dead). The literal survives as the
  var fallback default (contract §E2-sanctioned form) — so the hardcoded-defaults gate still counts it;
  fully zeroing it needs the default emitted from render.php.
- **Rows 2 & 3 (`align-items: center` / `flex-start` → `panelAlignment`): MIS-TAGGED.** All 15 `align-items`
  uses were traced; NONE are driven by `panelAlignment` (that attr drives left/right positioning via
  classes). Not dead-control-by-override — force-wiring would silently change unrelated icon-square flex
  alignment. Left as structural CSS; flagged for a gate attr↔property mapping fix (not guessed).
- Device breakpoints `@media (max-width:1024px)/(min-width:1025px)` are STRUCTURAL (desktop flyout vs
  mobile accordion), not device-tier box attrs — legitimately preserved (device-tier-vs-visual lesson).

## Gates
- `npm run build` prebuild (dead-controls, hardcoded-defaults 0 net-new) + webpack: PASS. Converter suite: 440 passed, 1 skipped.
