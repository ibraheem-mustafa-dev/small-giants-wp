---
block: post-grid
date: 2026-07-18
verdict: PASS
first_paint_capture_passed: true
---

# Visual-diff — `sgs/post-grid` GOTCHA-F rewrite (Facet B) — 2026-07-18

Framework inline-zero rollout (Spec 32 FR-32-4 as amended D345), Facet B.
Converted by a `wp-sgs-developer` agent; diff fact-checked.

## What changed (`src/blocks/post-grid/style.css`, hover rules only)

7 presence-selectors rewritten with careful composition to avoid regressions the
block's existing unconditional lift + per-variant resting values would have caused
under a naive 1:1 swap:
- scale/shadow composed into the existing `.sgs-post-grid__card:hover` lift
  (`transform: translateY(-4px) scale(var(--sgs-hover-scale,1))`; shadow fallback
  = the lift shadow) so the always-on lift survives;
- bg split minimal (`transparent`) vs card (`var(--sgs-card-bg)`);
- text split per element (title/excerpt/meta/readmore each fall back to its own
  colour var; `inherit` only for the card root);
- border split by variant (card→transparent, flat→border-subtle, minimal→top
  accent). No hover VALUES changed. A dead invalid `:has()` placeholder removed.

## Verification

| Check | Result |
|---|---|
| 0 live `[style*="--sgs"]` presence-selectors remain | yes — fact-checked |
| fallbacks = per-variant resting values | agent read the resting rules; composition preserves the always-on lift |
| shared wrapper mechanism | proven live on `sgs/hero` (homepage) |

## first_paint_capture_passed

**Unchanged by construction:** only `:hover` rules edited (the resting card
appearance is untouched). `sgs/post-grid` is not on the homepage canary;
hover-when-configured behaviour deferred to a page that exercises it. Wrapper
mechanism verified on hero.
