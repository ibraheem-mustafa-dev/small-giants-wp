---
block: sgs/card-grid
date: 2026-08-07
verdict: PASS
first_paint_capture_passed: true
first_paint_capture_run: true
capture_method: chrome-devtools-mcp (Playwright reserved for a co-active session)
deployed_build: sandybrown canary, deploy 2026-08-07, verified by md5 of deployed files vs local build
change: Spec 32 stranded ':not([style*=...])' fallback-guard purge
---

## What changed
Overlay/card title and subtitle fallbacks de-specified via `:where()` (0,0,0) instead of deleted — the values are PAINTED, not inherited.

## Live measurement (post-deploy, on the real canary)
On the split-zone-serif drawer, `.sgs-card-grid__title` now resolves to the OPERATOR rule `.sgs-cg-540b7a39 .sgs-card-grid__title` (0,2,0) painting `var(--wp--preset--color--primary)`, where the pre-fix guard `.sgs-card-grid--card .sgs-card-grid__title:not([style*="color"])` at (0,3,0) out-ranked it and painted `text`. This is the predicted second failure mode CONFIRMED and fixed: a colour the operator chose was previously dead.

## Negative control
`el.matches()` run against every card-grid title rule in the deployed sheet: the `:where()` guard reports matches=true but LOSES, and the operator rule wins — proving the de-specification changed which rule paints, not merely which rules exist.

## Limits
Measured on the canary only. This report evidences the FIRST-PAINT rest state;
hover and focus states were not captured.
