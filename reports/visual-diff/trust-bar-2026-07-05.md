# Visual diff — sgs/trust-bar — 2026-07-05

verdict: PASS
first_paint_capture_passed: true

## Change under review
`style.css` size-variant rules for text-only badge labels wrapped in `:where()`
(specificity (0,3,0) → (0,1,0)) so a lifted/operator-set `labelFontSize`
(uid-scoped rule, (0,2,0)) can win. Values inside the rules are UNCHANGED —
for any instance with no labelFontSize set, the computed output is identical
by construction (same declarations, still matching).

## Evidence (live page 8, sandybrown, post-deploy 2026-07-05 17:47)
- icon-circle canary (the deployed variant): label computes **13px @375**,
  **14px @1440**, weight 500, colour rgb(58,46,38) — draft-exact, unchanged
  across the deploy (checked immediately before and after).
- First paint: page renders with labels visible at first paint (no animation /
  visibility gating touches `.sgs-trust-bar__badge-label`; the M1 CSS pattern
  audit in the same pre-commit run reports clean).
- text-only variant: not present on the canary; the change strictly LOWERS
  specificity of its defaults, so an unset instance is pixel-identical and a
  set instance now (correctly) honours the operator value. Computed-parity
  Stage 11.6 after deploy: content 96 / CSS 80-81-82 — no regression
  (improved vs the 79/80/82 pre-fix run).
