---
block: card-grid
date: 2026-07-21
verdict: PASS
first_paint_capture_passed: true
target: sandybrown-nightingale-600381.hostingersite.com
change: "Task 4 — resting-state controls (Bean option A, token fallback)"
---

# card-grid resting-state fix — visual verification

## What changed
Cards had hover background/border/shadow attributes but no resting equivalents;
the resting look was hardcoded at `style.css:16,29-33`. Added `cardBackground`,
`cardBorderColour`, `cardBorderWidth`, `cardRadius`, `cardShadow` with inspector
controls. Hardcoded values became custom-property fallbacks (option A), so an
untouched instance renders identically and still tracks the theme token.

## Deploy provenance
- Deployed from ISOLATED worktree `C:/tmp/sgs-cgverify` at scratch SHA `c7926c29`
  (scratch branch never merged; the shared `main` checkout was never switched).
- `build-deploy.py --target sandybrown --blocks-only`, exit 0.
- Shipped-code proof by CHECKSUM, not liveness: local and server
  `build/blocks/card-grid/style-index.css` both `d0b561fc21e70ee8a5aa03c9b2e3515a`;
  `sgs-card-background` present in the deployed file. The generic HTTP-200 verify
  leg was NOT treated as evidence (it passes on any working SGS page).

## Method
Two published pages, identical block content, differing only in the new attrs —
so the check has a real negative control and can fail:
- untouched  → /cg-untouched-verify/  (page 1556)
- client-set → /cg-client-set-verify/ (page 1557, cardBackground #ff0000, cardRadius 12px)

## Results — computed styles on the live rendered element

| Property | untouched | client-set |
|---|---|---|
| background-color | `rgb(251,243,220)` (theme surface token) | `rgb(255,0,0)` |
| border-radius | `8px` (token) | `12px` |
| box-shadow | `rgba(0,0,0,0.1) 0 4px 12px` (token) | unchanged (unset) |
| border | `0px rgba(0,0,0,0)` | unchanged |

Lifted-CSS check (block CSS is lifted to `uploads/sgs-css/`, so page-HTML grep
proves nothing):
- untouched  → `--sgs-card-background` ABSENT, `#ff0000` ABSENT
- client-set → `--sgs-card-background` PRESENT, `#ff0000` PRESENT

Spec 32 no-inline contract:
- inline `style=` property declarations: 7 on BOTH pages (pre-existing; the change
  introduced none)
- the card element's only inline output is `--sgs-item-index:0` — a custom-property
  VALUE, not a property declaration.

## Verdict
PASS. Option A confirmed: an untouched card resolves to the theme token
(`rgb(251,243,220)`), not the literal `#fff` fallback, so per-client theming via
theme-snapshot.json still tracks. A client-set value overrides correctly and is
emitted to the scoped stylesheet, never inline.

Manifest: card-grid's 3 STATE_WITHOUT_BASE findings are now STATE_OK.

## Not covered
Editor-canvas inspector panel was verified by REST attribute registration (all 5
attrs PRESENT on the live server, 128 total, so WP did not discard them per D338)
and by frontend render, NOT by opening the block editor UI. A human eye on the
"Card Styling (resting state)" panel remains outstanding (R-31-13).
