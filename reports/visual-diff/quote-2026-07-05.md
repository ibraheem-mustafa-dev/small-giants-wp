# Visual diff — sgs/quote — 2026-07-05

verdict: PASS
first_paint_capture_passed: true

## Change under review
block.json only: added `supports.typography { fontSize, lineHeight }` on the block ROOT as an inheritable default (WP-native; the SGS_Container_Wrapper's `get_block_wrapper_attributes()` merges the inline style, children inherit + override by cascade). No render.php / style.css / save.js change. HC2-safe (inheritable default, not a per-element parent control).

## Evidence (live page 8, sandybrown, post-deploy 2026-07-05 19:46)
- The brand-section quote renders unchanged: present, child `sgs/text` bodies intact ("She was struggling with breast…"), no inline font-size set (the capability is added; no value applied on this page) → zero visual delta.
- No console errors; first paint renders the quote + children visible.
- Mechanism verified in source: quote render.php delegates to `SGS_Container_Wrapper::render()` which calls `get_block_wrapper_attributes()` (class-sgs-container-wrapper.php:711/1099) — WP core injects `style="font-size:…;line-height:…"` for supports.typography. The documented STOP-44 exception there is class-specific (`has-text-align-*`), not inline typography styles. Belt-and-braces editor set-a-value confirmation deferred to next session (additive, no live regression).
