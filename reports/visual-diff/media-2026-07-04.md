# Visual diff — sgs/media: audio mode removed + branded video player (2026-07-04)

verdict: PASS
first_paint_capture_passed: true

## What
1. **Audio mode REMOVED** from sgs/media (moved to the dedicated sgs/audio block, D268):
   mediaType 'audio' enum + 8 audio* attrs + the render branch/emission + the editor UI
   + the `.sgs-media__audio` style all removed. mediaType = image/video/svg; replaces =
   [core/image, core/video]. v1.4.0 → 1.5.0.
2. **Branded video player** (new view.js viewScriptModule): the native `<video controls>`
   chrome is replaced by a themed player — centre play overlay + hover-reveal bottom bar
   (play/pause, scrubber, timecode, mute + volume, fullscreen), keyboard-operable
   (space/k, arrows seek/volume, m, f), accent = theme primary. YouTube/Vimeo iframes are
   left untouched (can't skin cross-origin). Progressive enhancement: SSR native
   `<video controls>` = no-JS fallback; view.js upgrades.

## LANDED /qc on sandybrown (live)
- Video (direct MP4): enhances — video wrapped in `.sgs-video`, native controls removed,
  centre play + full control bar built; all controls present (play/scrub/time/mute/vol/fs);
  a11y — wrapper role=group + tabindex 0, aria-labels on every control.
- Playback verified with a real trusted click: played 0 → 5.1s (full clip), timecode
  "0:05 / 0:05", is-playing state tracked, no media error, ZERO console errors.
- Audio removal: no dangling `audio` reference in the block (grep clean); image/video/svg
  render paths intact; editor loads without error.
npm build clean (dead-control guard + F5/F6 + cheat-gate green); 374 tests pass.
QC-council (2 read-only raters) — see the commit for verdict; keyboard-focus fight
between the wrapper shortcuts and the range sliders fixed pre-commit.
