# Visual diff — sgs/media audio mode (2026-07-03)

verdict: PASS
first_paint_capture_passed: true

## Change
Added `mediaType` value `audio` — a native `<audio>` player (maps `core/audio` +
`core/video` via the D265 core→sgs replacement mapping; audio was the flagged gap).
New attrs: audioUrl / audioSource (external|internal) / audioId / audioMimeType /
audioControls / audioLoop / audioAutoplay / audioPreload. Internal source resolves via
the WP media library; MIME auto-detected from the URL extension. save.js returns null
(dynamic) → no deprecation.

## LANDED verification (live, stronger than a local capture)
Deployed the compiled build to sandybrown and rendered a real page instance
`<!-- wp:sgs/media {"mediaType":"audio","audioSource":"external","audioUrl":".../SoundHelix-Song-1.mp3"} /-->`.
Live rendered output:

    <audio class="sgs-media__audio" ... controls preload="metadata" aria-label="Audio">
      <source src="https://www.soundhelix.com/examples/mp3/SoundHelix-Song-1.mp3" type="audio/mpeg" />
      Your browser does not support the audio element.
    </audio>

Correct: native audio player, controls on, MIME auto-detected (audio/mpeg from .mp3),
source URL esc_url'd, accessible label. npm build succeeded (dead-control guard passed).
WPCS 0 errors. (Minor cosmetic: shared object-fit style is emitted on the <audio> too;
harmless — audio ignores object-fit. Tracked as a polish nit.)
